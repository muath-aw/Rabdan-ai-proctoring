---
id: loader
title: PrimeLoader
section: Components
category: Shared
description: 'Full-screen or inline loading spinner with optional logo and overlay.'
tags: [shared, loader, loading]
---

import { PrimeLoader } from '@components/shared';

## Demo

<div className="relative h-40 rounded border">
  <PrimeLoader withOverlay={false} />
</div>

## Imports

```tsx
import { Loader } from '@components/shared';
```

## Code

```tsx
<PrimeLoader displayLogo={false} withOverlay={true} />
```
