---
id: form-input
title: FormInput
section: Components
category: Forms
description: 'Text input with form validation, error display, and label support.'
tags: [forms, input, text]
---

import { FormBasicDemo } from '@pages/components/components.data.demos';

## Demo

<FormBasicDemo />

## Imports

```tsx
import { FormContainer, FormInput } from '@components/forms';
import { useForm } from 'react-hook-form';
```

## Code

```tsx
const form = useForm({ defaultValues: { email: '' } });

<FormContainer formContext={form} onSuccess={console.log}>
  <FormInput name="email" label="Email" required />
  <FormInput name="password" type="password" label="Password" />
</FormContainer>;
```
