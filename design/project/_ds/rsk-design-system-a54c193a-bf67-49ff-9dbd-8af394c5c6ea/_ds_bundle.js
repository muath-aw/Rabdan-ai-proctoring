/* @ds-bundle: {"namespace":"RSK","components":[{"name":"Accordion","sourcePath":"components/ui/Accordion/Accordion.jsx"},{"name":"ActionPanel","sourcePath":"components/shared/ActionPanel/ActionPanel.jsx"},{"name":"Alert","sourcePath":"components/ui/Alert/Alert.jsx"},{"name":"AsyncSelectFilter","sourcePath":"components/tables/AsyncSelectFilter/AsyncSelectFilter.jsx"},{"name":"Avatar","sourcePath":"components/ui/Avatar/Avatar.jsx"},{"name":"Badge","sourcePath":"components/ui/Badge/Badge.jsx"},{"name":"BlankSlate","sourcePath":"components/shared/BlankSlate/BlankSlate.jsx"},{"name":"Breadcrumb","sourcePath":"components/shared/Breadcrumb/Breadcrumb.jsx"},{"name":"Button","sourcePath":"components/ui/Button/Button.jsx"},{"name":"Calendar","sourcePath":"components/ui/Calendar/Calendar.jsx"},{"name":"CalendarGrid","sourcePath":"components/calendar/CalendarGrid/CalendarGrid.jsx"},{"name":"CalendarMonthGrid","sourcePath":"components/ui/CalendarMonthGrid/CalendarMonthGrid.jsx"},{"name":"CalendarPicker","sourcePath":"components/calendar/CalendarPicker/CalendarPicker.jsx"},{"name":"CalendarTimeGrid","sourcePath":"components/ui/CalendarTimeGrid/CalendarTimeGrid.jsx"},{"name":"Card","sourcePath":"components/ui/Card/Card.jsx"},{"name":"Checkbox","sourcePath":"components/ui/Checkbox/Checkbox.jsx"},{"name":"CheckboxGroup","sourcePath":"components/ui/CheckboxGroup/CheckboxGroup.jsx"},{"name":"Chip","sourcePath":"components/ui/Chip/Chip.jsx"},{"name":"Collapsible","sourcePath":"components/ui/Collapsible/Collapsible.jsx"},{"name":"CollapsibleCard","sourcePath":"components/shared/CollapsibleCard/CollapsibleCard.jsx"},{"name":"Combobox","sourcePath":"components/shared/Combobox/Combobox.jsx"},{"name":"Command","sourcePath":"components/ui/Command/Command.jsx"},{"name":"CommandPalette","sourcePath":"components/shared/CommandPalette/CommandPalette.jsx"},{"name":"ConfirmDialog","sourcePath":"components/shared/ConfirmDialog/ConfirmDialog.jsx"},{"name":"CountTooltip","sourcePath":"components/shared/CountTooltip/CountTooltip.jsx"},{"name":"DataTable","sourcePath":"components/tables/DataTable/DataTable.jsx"},{"name":"DataTableFacetedFilter","sourcePath":"components/tables/DataTableFacetedFilter/DataTableFacetedFilter.jsx"},{"name":"DateFilter","sourcePath":"components/tables/DateFilter/DateFilter.jsx"},{"name":"DateNavigation","sourcePath":"components/shared/DateNavigation/DateNavigation.jsx"},{"name":"Dialog","sourcePath":"components/ui/Dialog/Dialog.jsx"},{"name":"Divider","sourcePath":"components/ui/Divider/Divider.jsx"},{"name":"DropdownMenu","sourcePath":"components/ui/DropdownMenu/DropdownMenu.jsx"},{"name":"ErrorTooltip","sourcePath":"components/shared/ErrorTooltip/ErrorTooltip.jsx"},{"name":"FacetedFilter","sourcePath":"components/shared/FacetedFilter/FacetedFilter.jsx"},{"name":"FieldSection","sourcePath":"components/shared/FieldSection/FieldSection.jsx"},{"name":"FileCard","sourcePath":"components/shared/FileCard/FileCard.jsx"},{"name":"FileIcon","sourcePath":"components/shared/FileIcon/FileIcon.jsx"},{"name":"FileUploader","sourcePath":"components/shared/FileUploader/FileUploader.jsx"},{"name":"FilterChipList","sourcePath":"components/tables/FilterChipList/FilterChipList.jsx"},{"name":"FormattedNumber","sourcePath":"components/ui/FormattedNumber/FormattedNumber.jsx"},{"name":"FormCheckbox","sourcePath":"components/forms/FormCheckbox/FormCheckbox.jsx"},{"name":"FormCheckboxGroup","sourcePath":"components/forms/FormCheckboxGroup/FormCheckboxGroup.jsx"},{"name":"FormCombobox","sourcePath":"components/forms/FormCombobox/FormCombobox.jsx"},{"name":"FormContainer","sourcePath":"components/forms/FormContainer/FormContainer.jsx"},{"name":"FormControl","sourcePath":"components/forms/FormControl/FormControl.jsx"},{"name":"FormDatePicker","sourcePath":"components/forms/FormDatePicker/FormDatePicker.jsx"},{"name":"FormDateRange","sourcePath":"components/forms/FormDateRange/FormDateRange.jsx"},{"name":"FormFileUploader","sourcePath":"components/forms/FormFileUploader/FormFileUploader.jsx"},{"name":"FormInput","sourcePath":"components/forms/FormInput/FormInput.jsx"},{"name":"FormLabel","sourcePath":"components/forms/FormLabel/FormLabel.jsx"},{"name":"FormMessage","sourcePath":"components/forms/FormMessage/FormMessage.jsx"},{"name":"FormMultiCombobox","sourcePath":"components/forms/FormMultiCombobox/FormMultiCombobox.jsx"},{"name":"FormNumber","sourcePath":"components/forms/FormNumber/FormNumber.jsx"},{"name":"FormRadioGroup","sourcePath":"components/forms/FormRadioGroup/FormRadioGroup.jsx"},{"name":"FormSelect","sourcePath":"components/forms/FormSelect/FormSelect.jsx"},{"name":"FormSubmitButton","sourcePath":"components/shared/FormSubmitButton/FormSubmitButton.jsx"},{"name":"FormSubmitButtons","sourcePath":"components/shared/FormSubmitButtons/FormSubmitButtons.jsx"},{"name":"FormTextarea","sourcePath":"components/forms/FormTextarea/FormTextarea.jsx"},{"name":"FormTimePicker","sourcePath":"components/forms/FormTimePicker/FormTimePicker.jsx"},{"name":"GridCard","sourcePath":"components/shared/GridCard/GridCard.jsx"},{"name":"Input","sourcePath":"components/ui/Input/Input.jsx"},{"name":"LanguageSwitcher","sourcePath":"components/shared/LanguageSwitcher/LanguageSwitcher.jsx"},{"name":"Layout","sourcePath":"components/ui/Layout/Layout.jsx"},{"name":"LinkButton","sourcePath":"components/shared/LinkButton/LinkButton.jsx"},{"name":"LoadingButton","sourcePath":"components/shared/LoadingButton/LoadingButton.jsx"},{"name":"MetaCard","sourcePath":"components/shared/MetaCard/MetaCard.jsx"},{"name":"Pagination","sourcePath":"components/ui/Pagination/Pagination.jsx"},{"name":"Popover","sourcePath":"components/ui/Popover/Popover.jsx"},{"name":"PrimeCard","sourcePath":"components/shared/PrimeCard/PrimeCard.jsx"},{"name":"PrimeDialog","sourcePath":"components/shared/PrimeDialog/PrimeDialog.jsx"},{"name":"PrimeDropdown","sourcePath":"components/shared/PrimeDropdown/PrimeDropdown.jsx"},{"name":"PrimeLoader","sourcePath":"components/shared/PrimeLoader/PrimeLoader.jsx"},{"name":"PrimeTooltip","sourcePath":"components/shared/PrimeTooltip/PrimeTooltip.jsx"},{"name":"Progress","sourcePath":"components/ui/Progress/Progress.jsx"},{"name":"RadioDot","sourcePath":"components/ui/RadioDot/RadioDot.jsx"},{"name":"RadioGroup","sourcePath":"components/ui/RadioGroup/RadioGroup.jsx"},{"name":"ScrollArea","sourcePath":"components/ui/ScrollArea/ScrollArea.jsx"},{"name":"ScrollSelector","sourcePath":"components/ui/ScrollSelector/ScrollSelector.jsx"},{"name":"Select","sourcePath":"components/ui/Select/Select.jsx"},{"name":"SelectFilter","sourcePath":"components/tables/SelectFilter/SelectFilter.jsx"},{"name":"Skeleton","sourcePath":"components/ui/Skeleton/Skeleton.jsx"},{"name":"Stepper","sourcePath":"components/shared/Stepper/Stepper.jsx"},{"name":"Switch","sourcePath":"components/ui/Switch/Switch.jsx"},{"name":"Table","sourcePath":"components/ui/Table/Table.jsx"},{"name":"Tabs","sourcePath":"components/ui/Tabs/Tabs.jsx"},{"name":"ThemeSwitcher","sourcePath":"components/shared/ThemeSwitcher/ThemeSwitcher.jsx"},{"name":"TimeFilter","sourcePath":"components/tables/TimeFilter/TimeFilter.jsx"},{"name":"TimePicker","sourcePath":"components/ui/TimePicker/TimePicker.jsx"},{"name":"Toast","sourcePath":"components/ui/Toast/Toast.jsx"},{"name":"Toaster","sourcePath":"components/shared/Toaster/Toaster.jsx"},{"name":"ToggleGroup","sourcePath":"components/ui/ToggleGroup/ToggleGroup.jsx"},{"name":"Tooltip","sourcePath":"components/ui/Tooltip/Tooltip.jsx"},{"name":"TooltipButton","sourcePath":"components/shared/TooltipButton/TooltipButton.jsx"}],"sourceHashes":{"components/ui/Accordion/Accordion.jsx":"34617c2a322d","components/ui/Accordion/Accordion.d.ts":"989271dba1c0","components/ui/Accordion/Accordion.prompt.md":"264867768387","components/shared/ActionPanel/ActionPanel.jsx":"98ad4c3cc591","components/shared/ActionPanel/ActionPanel.d.ts":"f684553fc007","components/shared/ActionPanel/ActionPanel.prompt.md":"99493d537b21","components/ui/Alert/Alert.jsx":"17a4d54cecce","components/ui/Alert/Alert.d.ts":"a68b1e0364e8","components/ui/Alert/Alert.prompt.md":"1f24f11bcaef","components/tables/AsyncSelectFilter/AsyncSelectFilter.jsx":"969f2378330e","components/tables/AsyncSelectFilter/AsyncSelectFilter.d.ts":"9f95a090461d","components/tables/AsyncSelectFilter/AsyncSelectFilter.prompt.md":"d5731cf28616","components/ui/Avatar/Avatar.jsx":"46ab71905343","components/ui/Avatar/Avatar.d.ts":"7aa386c8a32d","components/ui/Avatar/Avatar.prompt.md":"b4d68482922d","components/ui/Badge/Badge.jsx":"debab21a46a5","components/ui/Badge/Badge.d.ts":"4c3394071fa7","components/ui/Badge/Badge.prompt.md":"f93f9dabd7b8","components/shared/BlankSlate/BlankSlate.jsx":"ba86c1b4f001","components/shared/BlankSlate/BlankSlate.d.ts":"a357b195fde4","components/shared/BlankSlate/BlankSlate.prompt.md":"6451ae40d245","components/shared/Breadcrumb/Breadcrumb.jsx":"d2ad2921b6ef","components/shared/Breadcrumb/Breadcrumb.d.ts":"1582e7035f6b","components/shared/Breadcrumb/Breadcrumb.prompt.md":"f95332fbe328","components/ui/Button/Button.jsx":"d9d6a162aeb8","components/ui/Button/Button.d.ts":"374f8df8c6f6","components/ui/Button/Button.prompt.md":"2c4e1cd0c433","components/ui/Calendar/Calendar.jsx":"bf4bc4d7f10d","components/ui/Calendar/Calendar.d.ts":"29651b35b8fa","components/ui/Calendar/Calendar.prompt.md":"173615cb77b9","components/calendar/CalendarGrid/CalendarGrid.jsx":"aa608592e7ce","components/calendar/CalendarGrid/CalendarGrid.d.ts":"813dd704de52","components/calendar/CalendarGrid/CalendarGrid.prompt.md":"93754283442b","components/ui/CalendarMonthGrid/CalendarMonthGrid.jsx":"08dc5774e1e2","components/ui/CalendarMonthGrid/CalendarMonthGrid.d.ts":"2d2678ab3cb1","components/ui/CalendarMonthGrid/CalendarMonthGrid.prompt.md":"6205faafd249","components/calendar/CalendarPicker/CalendarPicker.jsx":"c5eabe466bb6","components/calendar/CalendarPicker/CalendarPicker.d.ts":"dc34054a86fa","components/calendar/CalendarPicker/CalendarPicker.prompt.md":"884821168fd5","components/ui/CalendarTimeGrid/CalendarTimeGrid.jsx":"6f7a105e0515","components/ui/CalendarTimeGrid/CalendarTimeGrid.d.ts":"ee498526695b","components/ui/CalendarTimeGrid/CalendarTimeGrid.prompt.md":"261ee718e3ed","components/ui/Card/Card.jsx":"2cc5d3fa243a","components/ui/Card/Card.d.ts":"ce263d8fd578","components/ui/Card/Card.prompt.md":"35149211110f","components/ui/Checkbox/Checkbox.jsx":"da52763e4e9b","components/ui/Checkbox/Checkbox.d.ts":"b414b270b33a","components/ui/Checkbox/Checkbox.prompt.md":"7b9cfb46289b","components/ui/CheckboxGroup/CheckboxGroup.jsx":"3db429e184c0","components/ui/CheckboxGroup/CheckboxGroup.d.ts":"6cac0eb6b773","components/ui/CheckboxGroup/CheckboxGroup.prompt.md":"900787e17654","components/ui/Chip/Chip.jsx":"add6c963205b","components/ui/Chip/Chip.d.ts":"7d99074f1f98","components/ui/Chip/Chip.prompt.md":"cd13fa0941c1","components/ui/Collapsible/Collapsible.jsx":"98363323a835","components/ui/Collapsible/Collapsible.d.ts":"40369ba67307","components/ui/Collapsible/Collapsible.prompt.md":"3b309d8642cc","components/shared/CollapsibleCard/CollapsibleCard.jsx":"ac3be1be76b8","components/shared/CollapsibleCard/CollapsibleCard.d.ts":"bb1b534f28f6","components/shared/CollapsibleCard/CollapsibleCard.prompt.md":"690d8b004d04","components/shared/Combobox/Combobox.jsx":"99f478e0bffd","components/shared/Combobox/Combobox.d.ts":"7c4a7e2769bb","components/shared/Combobox/Combobox.prompt.md":"e6e2c27e2ca2","components/ui/Command/Command.jsx":"ec18a4d5921b","components/ui/Command/Command.d.ts":"67041f2c780f","components/ui/Command/Command.prompt.md":"e72b16f71127","components/shared/CommandPalette/CommandPalette.jsx":"9506e97cecbc","components/shared/CommandPalette/CommandPalette.d.ts":"d6931347eac2","components/shared/CommandPalette/CommandPalette.prompt.md":"8819bc856908","components/shared/ConfirmDialog/ConfirmDialog.jsx":"8bb43a107b37","components/shared/ConfirmDialog/ConfirmDialog.d.ts":"9eae7f9521c9","components/shared/ConfirmDialog/ConfirmDialog.prompt.md":"1d922506e3f9","components/shared/CountTooltip/CountTooltip.jsx":"e6631e772ac9","components/shared/CountTooltip/CountTooltip.d.ts":"39f2b8242023","components/shared/CountTooltip/CountTooltip.prompt.md":"e1d69a531f98","components/tables/DataTable/DataTable.jsx":"a031291eb15e","components/tables/DataTable/DataTable.d.ts":"7e4563e2f117","components/tables/DataTable/DataTable.prompt.md":"e5c7e9bebfa9","components/tables/DataTableFacetedFilter/DataTableFacetedFilter.jsx":"15dc3833b8f4","components/tables/DataTableFacetedFilter/DataTableFacetedFilter.d.ts":"80dc135c213c","components/tables/DataTableFacetedFilter/DataTableFacetedFilter.prompt.md":"5c5deaca4be2","components/tables/DateFilter/DateFilter.jsx":"d89ba7826cdf","components/tables/DateFilter/DateFilter.d.ts":"d82af5f3948f","components/tables/DateFilter/DateFilter.prompt.md":"c0e1ffbf89fa","components/shared/DateNavigation/DateNavigation.jsx":"db55ea46ee98","components/shared/DateNavigation/DateNavigation.d.ts":"f5da8953a46f","components/shared/DateNavigation/DateNavigation.prompt.md":"6f8634367fe4","components/ui/Dialog/Dialog.jsx":"0c8f26e51c68","components/ui/Dialog/Dialog.d.ts":"2cbd872fbfbe","components/ui/Dialog/Dialog.prompt.md":"8757377fd86e","components/ui/Divider/Divider.jsx":"a546570b12fe","components/ui/Divider/Divider.d.ts":"f526f734eafc","components/ui/Divider/Divider.prompt.md":"d03cbb2c43f2","components/ui/DropdownMenu/DropdownMenu.jsx":"8f41ae73980f","components/ui/DropdownMenu/DropdownMenu.d.ts":"e153d2bf5cb1","components/ui/DropdownMenu/DropdownMenu.prompt.md":"fddf30e8e564","components/shared/ErrorTooltip/ErrorTooltip.jsx":"c2dfd1d15768","components/shared/ErrorTooltip/ErrorTooltip.d.ts":"a803a33f1e28","components/shared/ErrorTooltip/ErrorTooltip.prompt.md":"b113a385e0e0","components/shared/FacetedFilter/FacetedFilter.jsx":"d038c9af7e0d","components/shared/FacetedFilter/FacetedFilter.d.ts":"54020d3be885","components/shared/FacetedFilter/FacetedFilter.prompt.md":"33d898cfd5f2","components/shared/FieldSection/FieldSection.jsx":"511bef4cf42b","components/shared/FieldSection/FieldSection.d.ts":"1001eed95720","components/shared/FieldSection/FieldSection.prompt.md":"bee2594971f3","components/shared/FileCard/FileCard.jsx":"35b4e0067c4d","components/shared/FileCard/FileCard.d.ts":"512136d57dfd","components/shared/FileCard/FileCard.prompt.md":"b6a96caf3835","components/shared/FileIcon/FileIcon.jsx":"f5e2d914b9e1","components/shared/FileIcon/FileIcon.d.ts":"af3cc22ccd43","components/shared/FileIcon/FileIcon.prompt.md":"b5f20be65de5","components/shared/FileUploader/FileUploader.jsx":"ca109c1b6f84","components/shared/FileUploader/FileUploader.d.ts":"186b13fc6d28","components/shared/FileUploader/FileUploader.prompt.md":"8ffdae5cbffb","components/tables/FilterChipList/FilterChipList.jsx":"af14a03fd87b","components/tables/FilterChipList/FilterChipList.d.ts":"9bc601a292b0","components/tables/FilterChipList/FilterChipList.prompt.md":"154df09e8947","components/ui/FormattedNumber/FormattedNumber.jsx":"387d97c08b4f","components/ui/FormattedNumber/FormattedNumber.d.ts":"e00fcedc6c8e","components/ui/FormattedNumber/FormattedNumber.prompt.md":"d35f7cc32272","components/forms/FormCheckbox/FormCheckbox.jsx":"f0e24d063931","components/forms/FormCheckbox/FormCheckbox.d.ts":"50570bb37b4b","components/forms/FormCheckbox/FormCheckbox.prompt.md":"2216b9fb780d","components/forms/FormCheckboxGroup/FormCheckboxGroup.jsx":"1a58dfd09475","components/forms/FormCheckboxGroup/FormCheckboxGroup.d.ts":"6e6277eb3356","components/forms/FormCheckboxGroup/FormCheckboxGroup.prompt.md":"0f5ebcc9a274","components/forms/FormCombobox/FormCombobox.jsx":"a49576859e75","components/forms/FormCombobox/FormCombobox.d.ts":"1f46c0ccbba0","components/forms/FormCombobox/FormCombobox.prompt.md":"852e6d6e0928","components/forms/FormContainer/FormContainer.jsx":"6deb960e3dcf","components/forms/FormContainer/FormContainer.d.ts":"0e523de142e0","components/forms/FormContainer/FormContainer.prompt.md":"67ee7435c744","components/forms/FormControl/FormControl.jsx":"12a84760acbe","components/forms/FormControl/FormControl.d.ts":"454dc3fd10bb","components/forms/FormControl/FormControl.prompt.md":"b08de37320bb","components/forms/FormDatePicker/FormDatePicker.jsx":"6acfcc4bb048","components/forms/FormDatePicker/FormDatePicker.d.ts":"cd21905b7039","components/forms/FormDatePicker/FormDatePicker.prompt.md":"0ac3fd5140f8","components/forms/FormDateRange/FormDateRange.jsx":"cc7c97129bf8","components/forms/FormDateRange/FormDateRange.d.ts":"b8ea6be83f41","components/forms/FormDateRange/FormDateRange.prompt.md":"ac8377d109dc","components/forms/FormFileUploader/FormFileUploader.jsx":"afd4a6d39d54","components/forms/FormFileUploader/FormFileUploader.d.ts":"d58b613f34cc","components/forms/FormFileUploader/FormFileUploader.prompt.md":"49b9f2bfe0d6","components/forms/FormInput/FormInput.jsx":"efc7d99ff1bb","components/forms/FormInput/FormInput.d.ts":"3eab9df8c5e5","components/forms/FormInput/FormInput.prompt.md":"eda58abbcd5b","components/forms/FormLabel/FormLabel.jsx":"7490ccd04d06","components/forms/FormLabel/FormLabel.d.ts":"e6846d1fd7db","components/forms/FormLabel/FormLabel.prompt.md":"71bb2c3ca410","components/forms/FormMessage/FormMessage.jsx":"7ffd636f0f41","components/forms/FormMessage/FormMessage.d.ts":"064be4d92570","components/forms/FormMessage/FormMessage.prompt.md":"c95e6d160293","components/forms/FormMultiCombobox/FormMultiCombobox.jsx":"49738f1d0af2","components/forms/FormMultiCombobox/FormMultiCombobox.d.ts":"b9df3429c36a","components/forms/FormMultiCombobox/FormMultiCombobox.prompt.md":"c11ed191d326","components/forms/FormNumber/FormNumber.jsx":"55c62fc30f1c","components/forms/FormNumber/FormNumber.d.ts":"ee95ee3348e8","components/forms/FormNumber/FormNumber.prompt.md":"76d98ee09ffd","components/forms/FormRadioGroup/FormRadioGroup.jsx":"78ffaa937e2f","components/forms/FormRadioGroup/FormRadioGroup.d.ts":"504fd19cf2b0","components/forms/FormRadioGroup/FormRadioGroup.prompt.md":"9552d1eb3cbd","components/forms/FormSelect/FormSelect.jsx":"12401a41d568","components/forms/FormSelect/FormSelect.d.ts":"e52916a8de2e","components/forms/FormSelect/FormSelect.prompt.md":"dec41cd9b799","components/shared/FormSubmitButton/FormSubmitButton.jsx":"d9a92d8f0b14","components/shared/FormSubmitButton/FormSubmitButton.d.ts":"b06cb15fe31d","components/shared/FormSubmitButton/FormSubmitButton.prompt.md":"9abf795e2f5a","components/shared/FormSubmitButtons/FormSubmitButtons.jsx":"f3695de68180","components/shared/FormSubmitButtons/FormSubmitButtons.d.ts":"92a4771596cc","components/shared/FormSubmitButtons/FormSubmitButtons.prompt.md":"aa572a162b07","components/forms/FormTextarea/FormTextarea.jsx":"a4713f539370","components/forms/FormTextarea/FormTextarea.d.ts":"f11f09435b58","components/forms/FormTextarea/FormTextarea.prompt.md":"cbcacd5e42d1","components/forms/FormTimePicker/FormTimePicker.jsx":"28f9ef2280e2","components/forms/FormTimePicker/FormTimePicker.d.ts":"93afd5a86027","components/forms/FormTimePicker/FormTimePicker.prompt.md":"1a420ab3f6b8","components/shared/GridCard/GridCard.jsx":"7996adfcae72","components/shared/GridCard/GridCard.d.ts":"506da5f78474","components/shared/GridCard/GridCard.prompt.md":"ac8352b622b3","components/ui/Input/Input.jsx":"665c2cad3882","components/ui/Input/Input.d.ts":"be3ccc8d7bef","components/ui/Input/Input.prompt.md":"42e19a4cd920","components/shared/LanguageSwitcher/LanguageSwitcher.jsx":"915336ca988b","components/shared/LanguageSwitcher/LanguageSwitcher.d.ts":"e421d76dd13d","components/shared/LanguageSwitcher/LanguageSwitcher.prompt.md":"7e49cdad2946","components/ui/Layout/Layout.jsx":"ac2d734193de","components/ui/Layout/Layout.d.ts":"d077868d9795","components/ui/Layout/Layout.prompt.md":"03b100c9af01","components/shared/LinkButton/LinkButton.jsx":"d9936b030e17","components/shared/LinkButton/LinkButton.d.ts":"936a2cb9ad83","components/shared/LinkButton/LinkButton.prompt.md":"d379211d17bd","components/shared/LoadingButton/LoadingButton.jsx":"eed6c824669e","components/shared/LoadingButton/LoadingButton.d.ts":"bf5b681dd62f","components/shared/LoadingButton/LoadingButton.prompt.md":"633d97f9ac93","components/shared/MetaCard/MetaCard.jsx":"2e6e9a4f7709","components/shared/MetaCard/MetaCard.d.ts":"bc6f9479926a","components/shared/MetaCard/MetaCard.prompt.md":"334055b137fa","components/ui/Pagination/Pagination.jsx":"ab1c5b85fa9a","components/ui/Pagination/Pagination.d.ts":"79635c06cbe7","components/ui/Pagination/Pagination.prompt.md":"1db516b35d8c","components/ui/Popover/Popover.jsx":"a1e4c8fa20bb","components/ui/Popover/Popover.d.ts":"edbb97b39cd2","components/ui/Popover/Popover.prompt.md":"dad8a2f4bb14","components/shared/PrimeCard/PrimeCard.jsx":"c22a361fddb7","components/shared/PrimeCard/PrimeCard.d.ts":"4b0cb2fa37a7","components/shared/PrimeCard/PrimeCard.prompt.md":"80aa208bc989","components/shared/PrimeDialog/PrimeDialog.jsx":"8b955c7986c7","components/shared/PrimeDialog/PrimeDialog.d.ts":"17594df3c55f","components/shared/PrimeDialog/PrimeDialog.prompt.md":"1231defe4b11","components/shared/PrimeDropdown/PrimeDropdown.jsx":"86d5b2606d2f","components/shared/PrimeDropdown/PrimeDropdown.d.ts":"f97fe6eda0c5","components/shared/PrimeDropdown/PrimeDropdown.prompt.md":"4d3b25117409","components/shared/PrimeLoader/PrimeLoader.jsx":"8e429776e133","components/shared/PrimeLoader/PrimeLoader.d.ts":"b2f022811683","components/shared/PrimeLoader/PrimeLoader.prompt.md":"c0d1f2eefd71","components/shared/PrimeTooltip/PrimeTooltip.jsx":"129ec16c55e8","components/shared/PrimeTooltip/PrimeTooltip.d.ts":"1dc1e4d6cfa1","components/shared/PrimeTooltip/PrimeTooltip.prompt.md":"f0359f92d740","components/ui/Progress/Progress.jsx":"bd55cdb50f63","components/ui/Progress/Progress.d.ts":"eb9543f04fd8","components/ui/Progress/Progress.prompt.md":"5c6ac8f9da0b","components/ui/RadioDot/RadioDot.jsx":"d35ca04b0180","components/ui/RadioDot/RadioDot.d.ts":"57249034eed3","components/ui/RadioDot/RadioDot.prompt.md":"4c63cabda80f","components/ui/RadioGroup/RadioGroup.jsx":"dd32a8536385","components/ui/RadioGroup/RadioGroup.d.ts":"f46beae27809","components/ui/RadioGroup/RadioGroup.prompt.md":"7e2ea3a78baa","components/ui/ScrollArea/ScrollArea.jsx":"019cbebb2361","components/ui/ScrollArea/ScrollArea.d.ts":"124461abecfc","components/ui/ScrollArea/ScrollArea.prompt.md":"11971edc7cca","components/ui/ScrollSelector/ScrollSelector.jsx":"89f062fc6027","components/ui/ScrollSelector/ScrollSelector.d.ts":"4c29f8349ff9","components/ui/ScrollSelector/ScrollSelector.prompt.md":"74a1ae1a4f3c","components/ui/Select/Select.jsx":"d0c6a1d0dc2c","components/ui/Select/Select.d.ts":"3230014b3e67","components/ui/Select/Select.prompt.md":"1d29578d6e47","components/tables/SelectFilter/SelectFilter.jsx":"7daa6ad494a3","components/tables/SelectFilter/SelectFilter.d.ts":"85a2d5c97597","components/tables/SelectFilter/SelectFilter.prompt.md":"b13045c9f17d","components/ui/Skeleton/Skeleton.jsx":"3afbba2d53a4","components/ui/Skeleton/Skeleton.d.ts":"d02624ca50de","components/ui/Skeleton/Skeleton.prompt.md":"fec4d3774072","components/shared/Stepper/Stepper.jsx":"612eea6c1bee","components/shared/Stepper/Stepper.d.ts":"de03098cc0e2","components/shared/Stepper/Stepper.prompt.md":"613d810c33f5","components/ui/Switch/Switch.jsx":"f6d30bd58d27","components/ui/Switch/Switch.d.ts":"004040970f41","components/ui/Switch/Switch.prompt.md":"f3813ec9c0f5","components/ui/Table/Table.jsx":"73f87d2c90cb","components/ui/Table/Table.d.ts":"7e3e8afdead8","components/ui/Table/Table.prompt.md":"2b2776efd682","components/ui/Tabs/Tabs.jsx":"914101b851d0","components/ui/Tabs/Tabs.d.ts":"ac0262a29e85","components/ui/Tabs/Tabs.prompt.md":"5821d72ca819","components/shared/ThemeSwitcher/ThemeSwitcher.jsx":"a227ff6150cd","components/shared/ThemeSwitcher/ThemeSwitcher.d.ts":"fcdd85471967","components/shared/ThemeSwitcher/ThemeSwitcher.prompt.md":"18a3c5a6fe23","components/tables/TimeFilter/TimeFilter.jsx":"36fa272b85c2","components/tables/TimeFilter/TimeFilter.d.ts":"abe9eb7099b9","components/tables/TimeFilter/TimeFilter.prompt.md":"60665d368460","components/ui/TimePicker/TimePicker.jsx":"bf33dee3b55c","components/ui/TimePicker/TimePicker.d.ts":"f8e66a99bfd9","components/ui/TimePicker/TimePicker.prompt.md":"7024775415d5","components/ui/Toast/Toast.jsx":"31054dadfdb0","components/ui/Toast/Toast.d.ts":"66b238baf9fc","components/ui/Toast/Toast.prompt.md":"93d4524a8222","components/shared/Toaster/Toaster.jsx":"80e933bdd233","components/shared/Toaster/Toaster.d.ts":"342a5023d35e","components/shared/Toaster/Toaster.prompt.md":"56689526e397","components/ui/ToggleGroup/ToggleGroup.jsx":"ae3dbf8b71cd","components/ui/ToggleGroup/ToggleGroup.d.ts":"6d8172565a58","components/ui/ToggleGroup/ToggleGroup.prompt.md":"8020a7e8dad6","components/ui/Tooltip/Tooltip.jsx":"2879aa0e1bc9","components/ui/Tooltip/Tooltip.d.ts":"02f97fb7b191","components/ui/Tooltip/Tooltip.prompt.md":"ca6dc18538fe","components/shared/TooltipButton/TooltipButton.jsx":"6eb7ba1d4647","components/shared/TooltipButton/TooltipButton.d.ts":"72e2a915a805","components/shared/TooltipButton/TooltipButton.prompt.md":"aa72e435a5da"},"inlinedExternals":["@date-fns/tz","@floating-ui/core","@floating-ui/dom","@floating-ui/react-dom","@floating-ui/utils","@radix-ui/number","@radix-ui/primitive","@radix-ui/react-accordion","@radix-ui/react-arrow","@radix-ui/react-avatar","@radix-ui/react-checkbox","@radix-ui/react-collapsible","@radix-ui/react-collection","@radix-ui/react-compose-refs","@radix-ui/react-context","@radix-ui/react-dialog","@radix-ui/react-direction","@radix-ui/react-dismissable-layer","@radix-ui/react-dropdown-menu","@radix-ui/react-focus-guards","@radix-ui/react-focus-scope","@radix-ui/react-id","@radix-ui/react-label","@radix-ui/react-menu","@radix-ui/react-popover","@radix-ui/react-popper","@radix-ui/react-portal","@radix-ui/react-presence","@radix-ui/react-primitive","@radix-ui/react-progress","@radix-ui/react-radio-group","@radix-ui/react-roving-focus","@radix-ui/react-scroll-area","@radix-ui/react-select","@radix-ui/react-slot","@radix-ui/react-switch","@radix-ui/react-tabs","@radix-ui/react-toast","@radix-ui/react-toggle","@radix-ui/react-toggle-group","@radix-ui/react-tooltip","@radix-ui/react-use-callback-ref","@radix-ui/react-use-controllable-state","@radix-ui/react-use-effect-event","@radix-ui/react-use-escape-keydown","@radix-ui/react-use-is-hydrated","@radix-ui/react-use-layout-effect","@radix-ui/react-use-previous","@radix-ui/react-use-size","@radix-ui/react-visually-hidden","@remix-run/router","@tanstack/query-core","@tanstack/react-query","@tanstack/react-table","@tanstack/table-core","aria-hidden","class-variance-authority","clsx","cmdk","date-fns","detect-node-es","get-nonce","html-parse-stringify","i18next","i18next-browser-languagedetector","lucide-react","nuqs","react-day-picker","react-hook-form","react-i18next","react-number-format","react-remove-scroll","react-remove-scroll-bar","react-router","react-router-dom","react-style-singleton","tailwind-merge","tslib","use-callback-ref","use-sidecar","use-sync-external-store","void-elements"],"builtBy":"cc-design-sync"} */
var RSK = (() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
  var __esm = (fn, res, err) => function __init() {
    if (err) throw err[0];
    try {
      return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
    } catch (e2) {
      throw err = [e2], e2;
    }
  };
  var __commonJS = (cb, mod) => function __require() {
    try {
      return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
    } catch (e2) {
      throw mod = 0, e2;
    }
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
  var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);

  // <define:import.meta.env>
  var init_define_import_meta_env = __esm({
    "<define:import.meta.env>"() {
    }
  });

  // shim:react-shim
  var require_react_shim = __commonJS({
    "shim:react-shim"(exports, module) {
      init_define_import_meta_env();
      var R = window.React;
      function np(p2, k3) {
        var o = {};
        for (var x in p2) if (x !== "children") o[x] = p2[x];
        if (k3 !== void 0) o.key = k3;
        return o;
      }
      function jsx46(t3, p2, k3) {
        var c = p2 && p2.children;
        return c === void 0 ? R.createElement(t3, np(p2, k3)) : R.createElement(t3, np(p2, k3), c);
      }
      function jsxs9(t3, p2, k3) {
        return R.createElement.apply(R, [t3, np(p2, k3)].concat(p2.children));
      }
      module.exports = R;
      module.exports.jsx = jsx46;
      module.exports.jsxs = jsxs9;
      module.exports.jsxDEV = function(t3, p2, k3, s) {
        return (s ? jsxs9 : jsx46)(t3, p2, k3);
      };
      module.exports.Fragment = R.Fragment;
    }
  });

  // node_modules/void-elements/index.js
  var require_void_elements = __commonJS({
    "node_modules/void-elements/index.js"(exports, module) {
      init_define_import_meta_env();
      module.exports = {
        "area": true,
        "base": true,
        "br": true,
        "col": true,
        "embed": true,
        "hr": true,
        "img": true,
        "input": true,
        "link": true,
        "meta": true,
        "param": true,
        "source": true,
        "track": true,
        "wbr": true
      };
    }
  });

  // shim:react-dom-shim
  var require_react_dom_shim = __commonJS({
    "shim:react-dom-shim"(exports, module) {
      init_define_import_meta_env();
      var D2 = window.ReactDOM;
      var n = function() {
      };
      module.exports = Object.assign({ preload: n, preinit: n, preconnect: n, prefetchDNS: n, preloadModule: n, preinitModule: n }, D2);
    }
  });

  // node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js
  var require_use_sync_external_store_shim_development = __commonJS({
    "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js"(exports) {
      "use strict";
      init_define_import_meta_env();
      (function() {
        function is(x, y) {
          return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
        }
        function useSyncExternalStore$2(subscribe2, getSnapshot) {
          didWarnOld18Alpha || void 0 === React104.startTransition || (didWarnOld18Alpha = true, console.error(
            "You are using an outdated, pre-release alpha of React 18 that does not support useSyncExternalStore. The use-sync-external-store shim will not work correctly. Upgrade to a newer pre-release."
          ));
          var value = getSnapshot();
          if (!didWarnUncachedGetSnapshot) {
            var cachedValue = getSnapshot();
            objectIs(value, cachedValue) || (console.error(
              "The result of getSnapshot should be cached to avoid an infinite loop"
            ), didWarnUncachedGetSnapshot = true);
          }
          cachedValue = useState63({
            inst: { value, getSnapshot }
          });
          var inst = cachedValue[0].inst, forceUpdate = cachedValue[1];
          useLayoutEffect9(
            function() {
              inst.value = value;
              inst.getSnapshot = getSnapshot;
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            },
            [subscribe2, value, getSnapshot]
          );
          useEffect55(
            function() {
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
              return subscribe2(function() {
                checkIfSnapshotChanged(inst) && forceUpdate({ inst });
              });
            },
            [subscribe2]
          );
          useDebugValue(value);
          return value;
        }
        function checkIfSnapshotChanged(inst) {
          var latestGetSnapshot = inst.getSnapshot;
          inst = inst.value;
          try {
            var nextValue = latestGetSnapshot();
            return !objectIs(inst, nextValue);
          } catch (error2) {
            return true;
          }
        }
        function useSyncExternalStore$12(subscribe2, getSnapshot) {
          return getSnapshot();
        }
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
        var React104 = require_react_shim(), objectIs = "function" === typeof Object.is ? Object.is : is, useState63 = React104.useState, useEffect55 = React104.useEffect, useLayoutEffect9 = React104.useLayoutEffect, useDebugValue = React104.useDebugValue, didWarnOld18Alpha = false, didWarnUncachedGetSnapshot = false, shim = "undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement ? useSyncExternalStore$12 : useSyncExternalStore$2;
        exports.useSyncExternalStore = void 0 !== React104.useSyncExternalStore ? React104.useSyncExternalStore : shim;
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
      })();
    }
  });

  // node_modules/use-sync-external-store/shim/index.js
  var require_shim = __commonJS({
    "node_modules/use-sync-external-store/shim/index.js"(exports, module) {
      "use strict";
      init_define_import_meta_env();
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_use_sync_external_store_shim_development();
      }
    }
  });

  // ds-bundle/.bundle-entry.mjs
  var bundle_entry_exports = {};
  __export(bundle_entry_exports, {
    Accordion: () => Accordion_default,
    ActionPanel: () => ActionPanel_default,
    Alert: () => Alert_default,
    AsyncSelectFilter: () => AsyncSelectFilter_default,
    Avatar: () => Avatar_default,
    Badge: () => Badge_default,
    BlankSlate: () => BlankSlate_default,
    Breadcrumb: () => Breadcrumb_default,
    Button: () => Button_default,
    Calendar: () => Calendar_default,
    CalendarGrid: () => CalendarGrid_default,
    CalendarMonthGrid: () => CalendarMonthGrid_default,
    CalendarPicker: () => CalendarPicker_default,
    CalendarTimeGrid: () => CalendarTimeGrid_default,
    Card: () => Card_default,
    Checkbox: () => Checkbox_default,
    CheckboxGroup: () => CheckboxGroup_default,
    Chip: () => Chip_default,
    Collapsible: () => Collapsible_default,
    CollapsibleCard: () => CollapsibleCard_default,
    Combobox: () => Combobox_default,
    Command: () => Command_default,
    CommandPalette: () => CommandPalette_default,
    ComposedButton: () => ComposedButton,
    ComposedLoadingButton: () => ComposedLoadingButton,
    ComposedTooltipButton: () => ComposedTooltipButton,
    ConditionalChip: () => ConditionalChip,
    ConfirmDialog: () => ConfirmDialog_default,
    CountTooltip: () => CountTooltip_default,
    DataTable: () => DataTable_default,
    DataTableContent: () => DataTableContent_default,
    DataTableFacetedFilter: () => DataTableFacetedFilter_default,
    DataTablePagination: () => DataTablePagination_default,
    DataTableToolbar: () => DataTableToolbar_default,
    DateFilter: () => DateFilter_default,
    DateNavigation: () => DateNavigation_default,
    Dialog: () => Dialog_default,
    Divider: () => Divider_default,
    DropdownMenu: () => DropdownMenu_default,
    DsPreviewProvider: () => DsPreviewProvider,
    ErrorTooltip: () => ErrorTooltip_default,
    FacetedFilter: () => FacetedFilter_default,
    FieldSection: () => FieldSection_default,
    FileCard: () => FileCard_default,
    FileIcon: () => FileIcon_default,
    FileUploader: () => FileUploader_default,
    FilterChipList: () => FilterChipList_default,
    FormCheckbox: () => FormCheckbox_default,
    FormCheckboxGroup: () => FormCheckboxGroup_default,
    FormCombobox: () => FormCombobox_default,
    FormContainer: () => FormContainer_default,
    FormControl: () => FormControl_default,
    FormDatePicker: () => FormDatePicker_default,
    FormDateRange: () => FormDateRange_default,
    FormFileUploader: () => FormFileUploader_default,
    FormInput: () => FormInput_default,
    FormLabel: () => FormLabel_default,
    FormMessage: () => FormMessage_default,
    FormMultiCombobox: () => FormMultiCombobox_default,
    FormNumber: () => FormNumber_default,
    FormRadioGroup: () => FormRadioGroup_default,
    FormSelect: () => FormSelect_default,
    FormSubmitButton: () => FormSubmitButton_default,
    FormSubmitButtons: () => FormSubmitButtons_default,
    FormTextarea: () => FormTextarea_default,
    FormTimePicker: () => FormTimePicker_default,
    FormattedNumber: () => FormattedNumber_default,
    GridCard: () => GridCard_default,
    Input: () => Input_default,
    LanguageSwitcher: () => LanguageSwitcher_default,
    Layout: () => Layout_default,
    LinkButton: () => LinkButton_default,
    LoadingButton: () => LoadingButton_default,
    MetaCard: () => MetaCard_default,
    Pagination: () => Pagination_default,
    Popover: () => Popover_default,
    PrimeCard: () => PrimeCard_default,
    PrimeDialog: () => PrimeDialog_default,
    PrimeDropdown: () => PrimeDropdown_default,
    PrimeLoader: () => PrimeLoader_default,
    PrimeTooltip: () => PrimeTooltip_default,
    Progress: () => Progress_default,
    RadioDot: () => RadioDot_default,
    RadioGroup: () => RadioGroup_default,
    ScrollArea: () => ScrollArea_default,
    ScrollSelector: () => ScrollSelector,
    Select: () => Select_default,
    SelectFilter: () => SelectFilter_default,
    Skeleton: () => Skeleton_default,
    Stepper: () => Stepper_default,
    Switch: () => Switch_default,
    Table: () => Table_default,
    Tabs: () => Tabs_default,
    ThemeSwitcher: () => ThemeSwitcher_default,
    TimeFilter: () => TimeFilter_default,
    TimePicker: () => TimePicker_default,
    Toast: () => Toast_default,
    Toaster: () => Toaster_default,
    ToggleGroup: () => ToggleGroup_default,
    Tooltip: () => Tooltip_default,
    TooltipButton: () => TooltipButton_default,
    __dsMainNs: () => ds_entry_exports,
    buttonVariants: () => buttonVariants,
    useCalendarGridContext: () => useCalendarGridContext,
    useController: () => useController,
    useDataTableContext: () => useDataTableContext,
    useFieldArray: () => useFieldArray,
    useForm: () => useForm,
    useLayoutContext: () => useLayoutContext,
    useWatch: () => useWatch
  });
  init_define_import_meta_env();

  // .design-sync/preview-provider.tsx
  init_define_import_meta_env();

  // src/i18n.ts
  init_define_import_meta_env();

  // node_modules/i18next/dist/esm/i18next.js
  init_define_import_meta_env();
  var isString = (obj) => typeof obj === "string";
  var defer = () => {
    let res;
    let rej;
    const promise = new Promise((resolve, reject) => {
      res = resolve;
      rej = reject;
    });
    promise.resolve = res;
    promise.reject = rej;
    return promise;
  };
  var makeString = (object) => {
    if (object == null) return "";
    return "" + object;
  };
  var copy = (a, s, t3) => {
    a.forEach((m2) => {
      if (s[m2]) t3[m2] = s[m2];
    });
  };
  var lastOfPathSeparatorRegExp = /###/g;
  var cleanKey = (key) => key && key.indexOf("###") > -1 ? key.replace(lastOfPathSeparatorRegExp, ".") : key;
  var canNotTraverseDeeper = (object) => !object || isString(object);
  var getLastOfPath = (object, path2, Empty2) => {
    const stack = !isString(path2) ? path2 : path2.split(".");
    let stackIndex = 0;
    while (stackIndex < stack.length - 1) {
      if (canNotTraverseDeeper(object)) return {};
      const key = cleanKey(stack[stackIndex]);
      if (!object[key] && Empty2) object[key] = new Empty2();
      if (Object.prototype.hasOwnProperty.call(object, key)) {
        object = object[key];
      } else {
        object = {};
      }
      ++stackIndex;
    }
    if (canNotTraverseDeeper(object)) return {};
    return {
      obj: object,
      k: cleanKey(stack[stackIndex])
    };
  };
  var setPath = (object, path2, newValue) => {
    const {
      obj,
      k: k3
    } = getLastOfPath(object, path2, Object);
    if (obj !== void 0 || path2.length === 1) {
      obj[k3] = newValue;
      return;
    }
    let e2 = path2[path2.length - 1];
    let p2 = path2.slice(0, path2.length - 1);
    let last = getLastOfPath(object, p2, Object);
    while (last.obj === void 0 && p2.length) {
      e2 = `${p2[p2.length - 1]}.${e2}`;
      p2 = p2.slice(0, p2.length - 1);
      last = getLastOfPath(object, p2, Object);
      if (last?.obj && typeof last.obj[`${last.k}.${e2}`] !== "undefined") {
        last.obj = void 0;
      }
    }
    last.obj[`${last.k}.${e2}`] = newValue;
  };
  var pushPath = (object, path2, newValue, concat) => {
    const {
      obj,
      k: k3
    } = getLastOfPath(object, path2, Object);
    obj[k3] = obj[k3] || [];
    obj[k3].push(newValue);
  };
  var getPath = (object, path2) => {
    const {
      obj,
      k: k3
    } = getLastOfPath(object, path2);
    if (!obj) return void 0;
    if (!Object.prototype.hasOwnProperty.call(obj, k3)) return void 0;
    return obj[k3];
  };
  var getPathWithDefaults = (data, defaultData, key) => {
    const value = getPath(data, key);
    if (value !== void 0) {
      return value;
    }
    return getPath(defaultData, key);
  };
  var deepExtend = (target, source, overwrite) => {
    for (const prop in source) {
      if (prop !== "__proto__" && prop !== "constructor") {
        if (prop in target) {
          if (isString(target[prop]) || target[prop] instanceof String || isString(source[prop]) || source[prop] instanceof String) {
            if (overwrite) target[prop] = source[prop];
          } else {
            deepExtend(target[prop], source[prop], overwrite);
          }
        } else {
          target[prop] = source[prop];
        }
      }
    }
    return target;
  };
  var regexEscape = (str) => str.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
  var _entityMap = {
    "&": "&amp;",
    "<": "&lt;",
    ">": "&gt;",
    '"': "&quot;",
    "'": "&#39;",
    "/": "&#x2F;"
  };
  var escape = (data) => {
    if (isString(data)) {
      return data.replace(/[&<>"'\/]/g, (s) => _entityMap[s]);
    }
    return data;
  };
  var RegExpCache = class {
    constructor(capacity) {
      this.capacity = capacity;
      this.regExpMap = /* @__PURE__ */ new Map();
      this.regExpQueue = [];
    }
    getRegExp(pattern) {
      const regExpFromCache = this.regExpMap.get(pattern);
      if (regExpFromCache !== void 0) {
        return regExpFromCache;
      }
      const regExpNew = new RegExp(pattern);
      if (this.regExpQueue.length === this.capacity) {
        this.regExpMap.delete(this.regExpQueue.shift());
      }
      this.regExpMap.set(pattern, regExpNew);
      this.regExpQueue.push(pattern);
      return regExpNew;
    }
  };
  var chars = [" ", ",", "?", "!", ";"];
  var looksLikeObjectPathRegExpCache = new RegExpCache(20);
  var looksLikeObjectPath = (key, nsSeparator, keySeparator) => {
    nsSeparator = nsSeparator || "";
    keySeparator = keySeparator || "";
    const possibleChars = chars.filter((c) => nsSeparator.indexOf(c) < 0 && keySeparator.indexOf(c) < 0);
    if (possibleChars.length === 0) return true;
    const r2 = looksLikeObjectPathRegExpCache.getRegExp(`(${possibleChars.map((c) => c === "?" ? "\\?" : c).join("|")})`);
    let matched = !r2.test(key);
    if (!matched) {
      const ki = key.indexOf(keySeparator);
      if (ki > 0 && !r2.test(key.substring(0, ki))) {
        matched = true;
      }
    }
    return matched;
  };
  var deepFind = (obj, path2, keySeparator = ".") => {
    if (!obj) return void 0;
    if (obj[path2]) {
      if (!Object.prototype.hasOwnProperty.call(obj, path2)) return void 0;
      return obj[path2];
    }
    const tokens = path2.split(keySeparator);
    let current = obj;
    for (let i = 0; i < tokens.length; ) {
      if (!current || typeof current !== "object") {
        return void 0;
      }
      let next;
      let nextPath = "";
      for (let j = i; j < tokens.length; ++j) {
        if (j !== i) {
          nextPath += keySeparator;
        }
        nextPath += tokens[j];
        next = current[nextPath];
        if (next !== void 0) {
          if (["string", "number", "boolean"].indexOf(typeof next) > -1 && j < tokens.length - 1) {
            continue;
          }
          i += j - i + 1;
          break;
        }
      }
      current = next;
    }
    return current;
  };
  var getCleanedCode = (code) => code?.replace("_", "-");
  var consoleLogger = {
    type: "logger",
    log(args) {
      this.output("log", args);
    },
    warn(args) {
      this.output("warn", args);
    },
    error(args) {
      this.output("error", args);
    },
    output(type, args) {
      console?.[type]?.apply?.(console, args);
    }
  };
  var Logger = class _Logger {
    constructor(concreteLogger, options3 = {}) {
      this.init(concreteLogger, options3);
    }
    init(concreteLogger, options3 = {}) {
      this.prefix = options3.prefix || "i18next:";
      this.logger = concreteLogger || consoleLogger;
      this.options = options3;
      this.debug = options3.debug;
    }
    log(...args) {
      return this.forward(args, "log", "", true);
    }
    warn(...args) {
      return this.forward(args, "warn", "", true);
    }
    error(...args) {
      return this.forward(args, "error", "");
    }
    deprecate(...args) {
      return this.forward(args, "warn", "WARNING DEPRECATED: ", true);
    }
    forward(args, lvl, prefix, debugOnly) {
      if (debugOnly && !this.debug) return null;
      if (isString(args[0])) args[0] = `${prefix}${this.prefix} ${args[0]}`;
      return this.logger[lvl](args);
    }
    create(moduleName) {
      return new _Logger(this.logger, {
        ...{
          prefix: `${this.prefix}:${moduleName}:`
        },
        ...this.options
      });
    }
    clone(options3) {
      options3 = options3 || this.options;
      options3.prefix = options3.prefix || this.prefix;
      return new _Logger(this.logger, options3);
    }
  };
  var baseLogger = new Logger();
  var EventEmitter = class {
    constructor() {
      this.observers = {};
    }
    on(events, listener) {
      events.split(" ").forEach((event) => {
        if (!this.observers[event]) this.observers[event] = /* @__PURE__ */ new Map();
        const numListeners = this.observers[event].get(listener) || 0;
        this.observers[event].set(listener, numListeners + 1);
      });
      return this;
    }
    off(event, listener) {
      if (!this.observers[event]) return;
      if (!listener) {
        delete this.observers[event];
        return;
      }
      this.observers[event].delete(listener);
    }
    emit(event, ...args) {
      if (this.observers[event]) {
        const cloned = Array.from(this.observers[event].entries());
        cloned.forEach(([observer, numTimesAdded]) => {
          for (let i = 0; i < numTimesAdded; i++) {
            observer(...args);
          }
        });
      }
      if (this.observers["*"]) {
        const cloned = Array.from(this.observers["*"].entries());
        cloned.forEach(([observer, numTimesAdded]) => {
          for (let i = 0; i < numTimesAdded; i++) {
            observer.apply(observer, [event, ...args]);
          }
        });
      }
    }
  };
  var ResourceStore = class extends EventEmitter {
    constructor(data, options3 = {
      ns: ["translation"],
      defaultNS: "translation"
    }) {
      super();
      this.data = data || {};
      this.options = options3;
      if (this.options.keySeparator === void 0) {
        this.options.keySeparator = ".";
      }
      if (this.options.ignoreJSONStructure === void 0) {
        this.options.ignoreJSONStructure = true;
      }
    }
    addNamespaces(ns) {
      if (this.options.ns.indexOf(ns) < 0) {
        this.options.ns.push(ns);
      }
    }
    removeNamespaces(ns) {
      const index2 = this.options.ns.indexOf(ns);
      if (index2 > -1) {
        this.options.ns.splice(index2, 1);
      }
    }
    getResource(lng, ns, key, options3 = {}) {
      const keySeparator = options3.keySeparator !== void 0 ? options3.keySeparator : this.options.keySeparator;
      const ignoreJSONStructure = options3.ignoreJSONStructure !== void 0 ? options3.ignoreJSONStructure : this.options.ignoreJSONStructure;
      let path2;
      if (lng.indexOf(".") > -1) {
        path2 = lng.split(".");
      } else {
        path2 = [lng, ns];
        if (key) {
          if (Array.isArray(key)) {
            path2.push(...key);
          } else if (isString(key) && keySeparator) {
            path2.push(...key.split(keySeparator));
          } else {
            path2.push(key);
          }
        }
      }
      const result = getPath(this.data, path2);
      if (!result && !ns && !key && lng.indexOf(".") > -1) {
        lng = path2[0];
        ns = path2[1];
        key = path2.slice(2).join(".");
      }
      if (result || !ignoreJSONStructure || !isString(key)) return result;
      return deepFind(this.data?.[lng]?.[ns], key, keySeparator);
    }
    addResource(lng, ns, key, value, options3 = {
      silent: false
    }) {
      const keySeparator = options3.keySeparator !== void 0 ? options3.keySeparator : this.options.keySeparator;
      let path2 = [lng, ns];
      if (key) path2 = path2.concat(keySeparator ? key.split(keySeparator) : key);
      if (lng.indexOf(".") > -1) {
        path2 = lng.split(".");
        value = ns;
        ns = path2[1];
      }
      this.addNamespaces(ns);
      setPath(this.data, path2, value);
      if (!options3.silent) this.emit("added", lng, ns, key, value);
    }
    addResources(lng, ns, resources, options3 = {
      silent: false
    }) {
      for (const m2 in resources) {
        if (isString(resources[m2]) || Array.isArray(resources[m2])) this.addResource(lng, ns, m2, resources[m2], {
          silent: true
        });
      }
      if (!options3.silent) this.emit("added", lng, ns, resources);
    }
    addResourceBundle(lng, ns, resources, deep, overwrite, options3 = {
      silent: false,
      skipCopy: false
    }) {
      let path2 = [lng, ns];
      if (lng.indexOf(".") > -1) {
        path2 = lng.split(".");
        deep = resources;
        resources = ns;
        ns = path2[1];
      }
      this.addNamespaces(ns);
      let pack = getPath(this.data, path2) || {};
      if (!options3.skipCopy) resources = JSON.parse(JSON.stringify(resources));
      if (deep) {
        deepExtend(pack, resources, overwrite);
      } else {
        pack = {
          ...pack,
          ...resources
        };
      }
      setPath(this.data, path2, pack);
      if (!options3.silent) this.emit("added", lng, ns, resources);
    }
    removeResourceBundle(lng, ns) {
      if (this.hasResourceBundle(lng, ns)) {
        delete this.data[lng][ns];
      }
      this.removeNamespaces(ns);
      this.emit("removed", lng, ns);
    }
    hasResourceBundle(lng, ns) {
      return this.getResource(lng, ns) !== void 0;
    }
    getResourceBundle(lng, ns) {
      if (!ns) ns = this.options.defaultNS;
      return this.getResource(lng, ns);
    }
    getDataByLanguage(lng) {
      return this.data[lng];
    }
    hasLanguageSomeTranslations(lng) {
      const data = this.getDataByLanguage(lng);
      const n = data && Object.keys(data) || [];
      return !!n.find((v) => data[v] && Object.keys(data[v]).length > 0);
    }
    toJSON() {
      return this.data;
    }
  };
  var postProcessor = {
    processors: {},
    addPostProcessor(module) {
      this.processors[module.name] = module;
    },
    handle(processors, value, key, options3, translator) {
      processors.forEach((processor) => {
        value = this.processors[processor]?.process(value, key, options3, translator) ?? value;
      });
      return value;
    }
  };
  var PATH_KEY = /* @__PURE__ */ Symbol("i18next/PATH_KEY");
  function createProxy() {
    const state = [];
    const handler = /* @__PURE__ */ Object.create(null);
    let proxy;
    handler.get = (target, key) => {
      proxy?.revoke?.();
      if (key === PATH_KEY) return state;
      state.push(key);
      proxy = Proxy.revocable(target, handler);
      return proxy.proxy;
    };
    return Proxy.revocable(/* @__PURE__ */ Object.create(null), handler).proxy;
  }
  function keysFromSelector(selector, opts) {
    const {
      [PATH_KEY]: path2
    } = selector(createProxy());
    return path2.join(opts?.keySeparator ?? ".");
  }
  var checkedLoadedFor = {};
  var shouldHandleAsObject = (res) => !isString(res) && typeof res !== "boolean" && typeof res !== "number";
  var Translator = class _Translator extends EventEmitter {
    constructor(services, options3 = {}) {
      super();
      copy(["resourceStore", "languageUtils", "pluralResolver", "interpolator", "backendConnector", "i18nFormat", "utils"], services, this);
      this.options = options3;
      if (this.options.keySeparator === void 0) {
        this.options.keySeparator = ".";
      }
      this.logger = baseLogger.create("translator");
    }
    changeLanguage(lng) {
      if (lng) this.language = lng;
    }
    exists(key, o = {
      interpolation: {}
    }) {
      const opt = {
        ...o
      };
      if (key == null) return false;
      const resolved = this.resolve(key, opt);
      if (resolved?.res === void 0) return false;
      const isObject3 = shouldHandleAsObject(resolved.res);
      if (opt.returnObjects === false && isObject3) {
        return false;
      }
      return true;
    }
    extractFromKey(key, opt) {
      let nsSeparator = opt.nsSeparator !== void 0 ? opt.nsSeparator : this.options.nsSeparator;
      if (nsSeparator === void 0) nsSeparator = ":";
      const keySeparator = opt.keySeparator !== void 0 ? opt.keySeparator : this.options.keySeparator;
      let namespaces = opt.ns || this.options.defaultNS || [];
      const wouldCheckForNsInKey = nsSeparator && key.indexOf(nsSeparator) > -1;
      const seemsNaturalLanguage = !this.options.userDefinedKeySeparator && !opt.keySeparator && !this.options.userDefinedNsSeparator && !opt.nsSeparator && !looksLikeObjectPath(key, nsSeparator, keySeparator);
      if (wouldCheckForNsInKey && !seemsNaturalLanguage) {
        const m2 = key.match(this.interpolator.nestingRegexp);
        if (m2 && m2.length > 0) {
          return {
            key,
            namespaces: isString(namespaces) ? [namespaces] : namespaces
          };
        }
        const parts = key.split(nsSeparator);
        if (nsSeparator !== keySeparator || nsSeparator === keySeparator && this.options.ns.indexOf(parts[0]) > -1) namespaces = parts.shift();
        key = parts.join(keySeparator);
      }
      return {
        key,
        namespaces: isString(namespaces) ? [namespaces] : namespaces
      };
    }
    translate(keys, o, lastKey) {
      let opt = typeof o === "object" ? {
        ...o
      } : o;
      if (typeof opt !== "object" && this.options.overloadTranslationOptionHandler) {
        opt = this.options.overloadTranslationOptionHandler(arguments);
      }
      if (typeof opt === "object") opt = {
        ...opt
      };
      if (!opt) opt = {};
      if (keys == null) return "";
      if (typeof keys === "function") keys = keysFromSelector(keys, {
        ...this.options,
        ...opt
      });
      if (!Array.isArray(keys)) keys = [String(keys)];
      const returnDetails = opt.returnDetails !== void 0 ? opt.returnDetails : this.options.returnDetails;
      const keySeparator = opt.keySeparator !== void 0 ? opt.keySeparator : this.options.keySeparator;
      const {
        key,
        namespaces
      } = this.extractFromKey(keys[keys.length - 1], opt);
      const namespace = namespaces[namespaces.length - 1];
      let nsSeparator = opt.nsSeparator !== void 0 ? opt.nsSeparator : this.options.nsSeparator;
      if (nsSeparator === void 0) nsSeparator = ":";
      const lng = opt.lng || this.language;
      const appendNamespaceToCIMode = opt.appendNamespaceToCIMode || this.options.appendNamespaceToCIMode;
      if (lng?.toLowerCase() === "cimode") {
        if (appendNamespaceToCIMode) {
          if (returnDetails) {
            return {
              res: `${namespace}${nsSeparator}${key}`,
              usedKey: key,
              exactUsedKey: key,
              usedLng: lng,
              usedNS: namespace,
              usedParams: this.getUsedParamsDetails(opt)
            };
          }
          return `${namespace}${nsSeparator}${key}`;
        }
        if (returnDetails) {
          return {
            res: key,
            usedKey: key,
            exactUsedKey: key,
            usedLng: lng,
            usedNS: namespace,
            usedParams: this.getUsedParamsDetails(opt)
          };
        }
        return key;
      }
      const resolved = this.resolve(keys, opt);
      let res = resolved?.res;
      const resUsedKey = resolved?.usedKey || key;
      const resExactUsedKey = resolved?.exactUsedKey || key;
      const noObject = ["[object Number]", "[object Function]", "[object RegExp]"];
      const joinArrays = opt.joinArrays !== void 0 ? opt.joinArrays : this.options.joinArrays;
      const handleAsObjectInI18nFormat = !this.i18nFormat || this.i18nFormat.handleAsObject;
      const needsPluralHandling = opt.count !== void 0 && !isString(opt.count);
      const hasDefaultValue = _Translator.hasDefaultValue(opt);
      const defaultValueSuffix = needsPluralHandling ? this.pluralResolver.getSuffix(lng, opt.count, opt) : "";
      const defaultValueSuffixOrdinalFallback = opt.ordinal && needsPluralHandling ? this.pluralResolver.getSuffix(lng, opt.count, {
        ordinal: false
      }) : "";
      const needsZeroSuffixLookup = needsPluralHandling && !opt.ordinal && opt.count === 0;
      const defaultValue = needsZeroSuffixLookup && opt[`defaultValue${this.options.pluralSeparator}zero`] || opt[`defaultValue${defaultValueSuffix}`] || opt[`defaultValue${defaultValueSuffixOrdinalFallback}`] || opt.defaultValue;
      let resForObjHndl = res;
      if (handleAsObjectInI18nFormat && !res && hasDefaultValue) {
        resForObjHndl = defaultValue;
      }
      const handleAsObject = shouldHandleAsObject(resForObjHndl);
      const resType = Object.prototype.toString.apply(resForObjHndl);
      if (handleAsObjectInI18nFormat && resForObjHndl && handleAsObject && noObject.indexOf(resType) < 0 && !(isString(joinArrays) && Array.isArray(resForObjHndl))) {
        if (!opt.returnObjects && !this.options.returnObjects) {
          if (!this.options.returnedObjectHandler) {
            this.logger.warn("accessing an object - but returnObjects options is not enabled!");
          }
          const r2 = this.options.returnedObjectHandler ? this.options.returnedObjectHandler(resUsedKey, resForObjHndl, {
            ...opt,
            ns: namespaces
          }) : `key '${key} (${this.language})' returned an object instead of string.`;
          if (returnDetails) {
            resolved.res = r2;
            resolved.usedParams = this.getUsedParamsDetails(opt);
            return resolved;
          }
          return r2;
        }
        if (keySeparator) {
          const resTypeIsArray = Array.isArray(resForObjHndl);
          const copy2 = resTypeIsArray ? [] : {};
          const newKeyToUse = resTypeIsArray ? resExactUsedKey : resUsedKey;
          for (const m2 in resForObjHndl) {
            if (Object.prototype.hasOwnProperty.call(resForObjHndl, m2)) {
              const deepKey = `${newKeyToUse}${keySeparator}${m2}`;
              if (hasDefaultValue && !res) {
                copy2[m2] = this.translate(deepKey, {
                  ...opt,
                  defaultValue: shouldHandleAsObject(defaultValue) ? defaultValue[m2] : void 0,
                  ...{
                    joinArrays: false,
                    ns: namespaces
                  }
                });
              } else {
                copy2[m2] = this.translate(deepKey, {
                  ...opt,
                  ...{
                    joinArrays: false,
                    ns: namespaces
                  }
                });
              }
              if (copy2[m2] === deepKey) copy2[m2] = resForObjHndl[m2];
            }
          }
          res = copy2;
        }
      } else if (handleAsObjectInI18nFormat && isString(joinArrays) && Array.isArray(res)) {
        res = res.join(joinArrays);
        if (res) res = this.extendTranslation(res, keys, opt, lastKey);
      } else {
        let usedDefault = false;
        let usedKey = false;
        if (!this.isValidLookup(res) && hasDefaultValue) {
          usedDefault = true;
          res = defaultValue;
        }
        if (!this.isValidLookup(res)) {
          usedKey = true;
          res = key;
        }
        const missingKeyNoValueFallbackToKey = opt.missingKeyNoValueFallbackToKey || this.options.missingKeyNoValueFallbackToKey;
        const resForMissing = missingKeyNoValueFallbackToKey && usedKey ? void 0 : res;
        const updateMissing = hasDefaultValue && defaultValue !== res && this.options.updateMissing;
        if (usedKey || usedDefault || updateMissing) {
          this.logger.log(updateMissing ? "updateKey" : "missingKey", lng, namespace, key, updateMissing ? defaultValue : res);
          if (keySeparator) {
            const fk = this.resolve(key, {
              ...opt,
              keySeparator: false
            });
            if (fk && fk.res) this.logger.warn("Seems the loaded translations were in flat JSON format instead of nested. Either set keySeparator: false on init or make sure your translations are published in nested format.");
          }
          let lngs = [];
          const fallbackLngs = this.languageUtils.getFallbackCodes(this.options.fallbackLng, opt.lng || this.language);
          if (this.options.saveMissingTo === "fallback" && fallbackLngs && fallbackLngs[0]) {
            for (let i = 0; i < fallbackLngs.length; i++) {
              lngs.push(fallbackLngs[i]);
            }
          } else if (this.options.saveMissingTo === "all") {
            lngs = this.languageUtils.toResolveHierarchy(opt.lng || this.language);
          } else {
            lngs.push(opt.lng || this.language);
          }
          const send = (l, k3, specificDefaultValue) => {
            const defaultForMissing = hasDefaultValue && specificDefaultValue !== res ? specificDefaultValue : resForMissing;
            if (this.options.missingKeyHandler) {
              this.options.missingKeyHandler(l, namespace, k3, defaultForMissing, updateMissing, opt);
            } else if (this.backendConnector?.saveMissing) {
              this.backendConnector.saveMissing(l, namespace, k3, defaultForMissing, updateMissing, opt);
            }
            this.emit("missingKey", l, namespace, k3, res);
          };
          if (this.options.saveMissing) {
            if (this.options.saveMissingPlurals && needsPluralHandling) {
              lngs.forEach((language) => {
                const suffixes = this.pluralResolver.getSuffixes(language, opt);
                if (needsZeroSuffixLookup && opt[`defaultValue${this.options.pluralSeparator}zero`] && suffixes.indexOf(`${this.options.pluralSeparator}zero`) < 0) {
                  suffixes.push(`${this.options.pluralSeparator}zero`);
                }
                suffixes.forEach((suffix) => {
                  send([language], key + suffix, opt[`defaultValue${suffix}`] || defaultValue);
                });
              });
            } else {
              send(lngs, key, defaultValue);
            }
          }
        }
        res = this.extendTranslation(res, keys, opt, resolved, lastKey);
        if (usedKey && res === key && this.options.appendNamespaceToMissingKey) {
          res = `${namespace}${nsSeparator}${key}`;
        }
        if ((usedKey || usedDefault) && this.options.parseMissingKeyHandler) {
          res = this.options.parseMissingKeyHandler(this.options.appendNamespaceToMissingKey ? `${namespace}${nsSeparator}${key}` : key, usedDefault ? res : void 0, opt);
        }
      }
      if (returnDetails) {
        resolved.res = res;
        resolved.usedParams = this.getUsedParamsDetails(opt);
        return resolved;
      }
      return res;
    }
    extendTranslation(res, key, opt, resolved, lastKey) {
      if (this.i18nFormat?.parse) {
        res = this.i18nFormat.parse(res, {
          ...this.options.interpolation.defaultVariables,
          ...opt
        }, opt.lng || this.language || resolved.usedLng, resolved.usedNS, resolved.usedKey, {
          resolved
        });
      } else if (!opt.skipInterpolation) {
        if (opt.interpolation) this.interpolator.init({
          ...opt,
          ...{
            interpolation: {
              ...this.options.interpolation,
              ...opt.interpolation
            }
          }
        });
        const skipOnVariables = isString(res) && (opt?.interpolation?.skipOnVariables !== void 0 ? opt.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables);
        let nestBef;
        if (skipOnVariables) {
          const nb = res.match(this.interpolator.nestingRegexp);
          nestBef = nb && nb.length;
        }
        let data = opt.replace && !isString(opt.replace) ? opt.replace : opt;
        if (this.options.interpolation.defaultVariables) data = {
          ...this.options.interpolation.defaultVariables,
          ...data
        };
        res = this.interpolator.interpolate(res, data, opt.lng || this.language || resolved.usedLng, opt);
        if (skipOnVariables) {
          const na = res.match(this.interpolator.nestingRegexp);
          const nestAft = na && na.length;
          if (nestBef < nestAft) opt.nest = false;
        }
        if (!opt.lng && resolved && resolved.res) opt.lng = this.language || resolved.usedLng;
        if (opt.nest !== false) res = this.interpolator.nest(res, (...args) => {
          if (lastKey?.[0] === args[0] && !opt.context) {
            this.logger.warn(`It seems you are nesting recursively key: ${args[0]} in key: ${key[0]}`);
            return null;
          }
          return this.translate(...args, key);
        }, opt);
        if (opt.interpolation) this.interpolator.reset();
      }
      const postProcess = opt.postProcess || this.options.postProcess;
      const postProcessorNames = isString(postProcess) ? [postProcess] : postProcess;
      if (res != null && postProcessorNames?.length && opt.applyPostProcessor !== false) {
        res = postProcessor.handle(postProcessorNames, res, key, this.options && this.options.postProcessPassResolved ? {
          i18nResolved: {
            ...resolved,
            usedParams: this.getUsedParamsDetails(opt)
          },
          ...opt
        } : opt, this);
      }
      return res;
    }
    resolve(keys, opt = {}) {
      let found;
      let usedKey;
      let exactUsedKey;
      let usedLng;
      let usedNS;
      if (isString(keys)) keys = [keys];
      keys.forEach((k3) => {
        if (this.isValidLookup(found)) return;
        const extracted = this.extractFromKey(k3, opt);
        const key = extracted.key;
        usedKey = key;
        let namespaces = extracted.namespaces;
        if (this.options.fallbackNS) namespaces = namespaces.concat(this.options.fallbackNS);
        const needsPluralHandling = opt.count !== void 0 && !isString(opt.count);
        const needsZeroSuffixLookup = needsPluralHandling && !opt.ordinal && opt.count === 0;
        const needsContextHandling = opt.context !== void 0 && (isString(opt.context) || typeof opt.context === "number") && opt.context !== "";
        const codes = opt.lngs ? opt.lngs : this.languageUtils.toResolveHierarchy(opt.lng || this.language, opt.fallbackLng);
        namespaces.forEach((ns) => {
          if (this.isValidLookup(found)) return;
          usedNS = ns;
          if (!checkedLoadedFor[`${codes[0]}-${ns}`] && this.utils?.hasLoadedNamespace && !this.utils?.hasLoadedNamespace(usedNS)) {
            checkedLoadedFor[`${codes[0]}-${ns}`] = true;
            this.logger.warn(`key "${usedKey}" for languages "${codes.join(", ")}" won't get resolved as namespace "${usedNS}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!");
          }
          codes.forEach((code) => {
            if (this.isValidLookup(found)) return;
            usedLng = code;
            const finalKeys = [key];
            if (this.i18nFormat?.addLookupKeys) {
              this.i18nFormat.addLookupKeys(finalKeys, key, code, ns, opt);
            } else {
              let pluralSuffix;
              if (needsPluralHandling) pluralSuffix = this.pluralResolver.getSuffix(code, opt.count, opt);
              const zeroSuffix = `${this.options.pluralSeparator}zero`;
              const ordinalPrefix = `${this.options.pluralSeparator}ordinal${this.options.pluralSeparator}`;
              if (needsPluralHandling) {
                if (opt.ordinal && pluralSuffix.indexOf(ordinalPrefix) === 0) {
                  finalKeys.push(key + pluralSuffix.replace(ordinalPrefix, this.options.pluralSeparator));
                }
                finalKeys.push(key + pluralSuffix);
                if (needsZeroSuffixLookup) {
                  finalKeys.push(key + zeroSuffix);
                }
              }
              if (needsContextHandling) {
                const contextKey = `${key}${this.options.contextSeparator || "_"}${opt.context}`;
                finalKeys.push(contextKey);
                if (needsPluralHandling) {
                  if (opt.ordinal && pluralSuffix.indexOf(ordinalPrefix) === 0) {
                    finalKeys.push(contextKey + pluralSuffix.replace(ordinalPrefix, this.options.pluralSeparator));
                  }
                  finalKeys.push(contextKey + pluralSuffix);
                  if (needsZeroSuffixLookup) {
                    finalKeys.push(contextKey + zeroSuffix);
                  }
                }
              }
            }
            let possibleKey;
            while (possibleKey = finalKeys.pop()) {
              if (!this.isValidLookup(found)) {
                exactUsedKey = possibleKey;
                found = this.getResource(code, ns, possibleKey, opt);
              }
            }
          });
        });
      });
      return {
        res: found,
        usedKey,
        exactUsedKey,
        usedLng,
        usedNS
      };
    }
    isValidLookup(res) {
      return res !== void 0 && !(!this.options.returnNull && res === null) && !(!this.options.returnEmptyString && res === "");
    }
    getResource(code, ns, key, options3 = {}) {
      if (this.i18nFormat?.getResource) return this.i18nFormat.getResource(code, ns, key, options3);
      return this.resourceStore.getResource(code, ns, key, options3);
    }
    getUsedParamsDetails(options3 = {}) {
      const optionsKeys = ["defaultValue", "ordinal", "context", "replace", "lng", "lngs", "fallbackLng", "ns", "keySeparator", "nsSeparator", "returnObjects", "returnDetails", "joinArrays", "postProcess", "interpolation"];
      const useOptionsReplaceForData = options3.replace && !isString(options3.replace);
      let data = useOptionsReplaceForData ? options3.replace : options3;
      if (useOptionsReplaceForData && typeof options3.count !== "undefined") {
        data.count = options3.count;
      }
      if (this.options.interpolation.defaultVariables) {
        data = {
          ...this.options.interpolation.defaultVariables,
          ...data
        };
      }
      if (!useOptionsReplaceForData) {
        data = {
          ...data
        };
        for (const key of optionsKeys) {
          delete data[key];
        }
      }
      return data;
    }
    static hasDefaultValue(options3) {
      const prefix = "defaultValue";
      for (const option in options3) {
        if (Object.prototype.hasOwnProperty.call(options3, option) && prefix === option.substring(0, prefix.length) && void 0 !== options3[option]) {
          return true;
        }
      }
      return false;
    }
  };
  var LanguageUtil = class {
    constructor(options3) {
      this.options = options3;
      this.supportedLngs = this.options.supportedLngs || false;
      this.logger = baseLogger.create("languageUtils");
    }
    getScriptPartFromCode(code) {
      code = getCleanedCode(code);
      if (!code || code.indexOf("-") < 0) return null;
      const p2 = code.split("-");
      if (p2.length === 2) return null;
      p2.pop();
      if (p2[p2.length - 1].toLowerCase() === "x") return null;
      return this.formatLanguageCode(p2.join("-"));
    }
    getLanguagePartFromCode(code) {
      code = getCleanedCode(code);
      if (!code || code.indexOf("-") < 0) return code;
      const p2 = code.split("-");
      return this.formatLanguageCode(p2[0]);
    }
    formatLanguageCode(code) {
      if (isString(code) && code.indexOf("-") > -1) {
        let formattedCode;
        try {
          formattedCode = Intl.getCanonicalLocales(code)[0];
        } catch (e2) {
        }
        if (formattedCode && this.options.lowerCaseLng) {
          formattedCode = formattedCode.toLowerCase();
        }
        if (formattedCode) return formattedCode;
        if (this.options.lowerCaseLng) {
          return code.toLowerCase();
        }
        return code;
      }
      return this.options.cleanCode || this.options.lowerCaseLng ? code.toLowerCase() : code;
    }
    isSupportedCode(code) {
      if (this.options.load === "languageOnly" || this.options.nonExplicitSupportedLngs) {
        code = this.getLanguagePartFromCode(code);
      }
      return !this.supportedLngs || !this.supportedLngs.length || this.supportedLngs.indexOf(code) > -1;
    }
    getBestMatchFromCodes(codes) {
      if (!codes) return null;
      let found;
      codes.forEach((code) => {
        if (found) return;
        const cleanedLng = this.formatLanguageCode(code);
        if (!this.options.supportedLngs || this.isSupportedCode(cleanedLng)) found = cleanedLng;
      });
      if (!found && this.options.supportedLngs) {
        codes.forEach((code) => {
          if (found) return;
          const lngScOnly = this.getScriptPartFromCode(code);
          if (this.isSupportedCode(lngScOnly)) return found = lngScOnly;
          const lngOnly = this.getLanguagePartFromCode(code);
          if (this.isSupportedCode(lngOnly)) return found = lngOnly;
          found = this.options.supportedLngs.find((supportedLng) => {
            if (supportedLng === lngOnly) return supportedLng;
            if (supportedLng.indexOf("-") < 0 && lngOnly.indexOf("-") < 0) return;
            if (supportedLng.indexOf("-") > 0 && lngOnly.indexOf("-") < 0 && supportedLng.substring(0, supportedLng.indexOf("-")) === lngOnly) return supportedLng;
            if (supportedLng.indexOf(lngOnly) === 0 && lngOnly.length > 1) return supportedLng;
          });
        });
      }
      if (!found) found = this.getFallbackCodes(this.options.fallbackLng)[0];
      return found;
    }
    getFallbackCodes(fallbacks, code) {
      if (!fallbacks) return [];
      if (typeof fallbacks === "function") fallbacks = fallbacks(code);
      if (isString(fallbacks)) fallbacks = [fallbacks];
      if (Array.isArray(fallbacks)) return fallbacks;
      if (!code) return fallbacks.default || [];
      let found = fallbacks[code];
      if (!found) found = fallbacks[this.getScriptPartFromCode(code)];
      if (!found) found = fallbacks[this.formatLanguageCode(code)];
      if (!found) found = fallbacks[this.getLanguagePartFromCode(code)];
      if (!found) found = fallbacks.default;
      return found || [];
    }
    toResolveHierarchy(code, fallbackCode) {
      const fallbackCodes = this.getFallbackCodes((fallbackCode === false ? [] : fallbackCode) || this.options.fallbackLng || [], code);
      const codes = [];
      const addCode = (c) => {
        if (!c) return;
        if (this.isSupportedCode(c)) {
          codes.push(c);
        } else {
          this.logger.warn(`rejecting language code not found in supportedLngs: ${c}`);
        }
      };
      if (isString(code) && (code.indexOf("-") > -1 || code.indexOf("_") > -1)) {
        if (this.options.load !== "languageOnly") addCode(this.formatLanguageCode(code));
        if (this.options.load !== "languageOnly" && this.options.load !== "currentOnly") addCode(this.getScriptPartFromCode(code));
        if (this.options.load !== "currentOnly") addCode(this.getLanguagePartFromCode(code));
      } else if (isString(code)) {
        addCode(this.formatLanguageCode(code));
      }
      fallbackCodes.forEach((fc) => {
        if (codes.indexOf(fc) < 0) addCode(this.formatLanguageCode(fc));
      });
      return codes;
    }
  };
  var suffixesOrder = {
    zero: 0,
    one: 1,
    two: 2,
    few: 3,
    many: 4,
    other: 5
  };
  var dummyRule = {
    select: (count4) => count4 === 1 ? "one" : "other",
    resolvedOptions: () => ({
      pluralCategories: ["one", "other"]
    })
  };
  var PluralResolver = class {
    constructor(languageUtils, options3 = {}) {
      this.languageUtils = languageUtils;
      this.options = options3;
      this.logger = baseLogger.create("pluralResolver");
      this.pluralRulesCache = {};
    }
    clearCache() {
      this.pluralRulesCache = {};
    }
    getRule(code, options3 = {}) {
      const cleanedCode = getCleanedCode(code === "dev" ? "en" : code);
      const type = options3.ordinal ? "ordinal" : "cardinal";
      const cacheKey = JSON.stringify({
        cleanedCode,
        type
      });
      if (cacheKey in this.pluralRulesCache) {
        return this.pluralRulesCache[cacheKey];
      }
      let rule;
      try {
        rule = new Intl.PluralRules(cleanedCode, {
          type
        });
      } catch (err) {
        if (typeof Intl === "undefined") {
          this.logger.error("No Intl support, please use an Intl polyfill!");
          return dummyRule;
        }
        if (!code.match(/-|_/)) return dummyRule;
        const lngPart = this.languageUtils.getLanguagePartFromCode(code);
        rule = this.getRule(lngPart, options3);
      }
      this.pluralRulesCache[cacheKey] = rule;
      return rule;
    }
    needsPlural(code, options3 = {}) {
      let rule = this.getRule(code, options3);
      if (!rule) rule = this.getRule("dev", options3);
      return rule?.resolvedOptions().pluralCategories.length > 1;
    }
    getPluralFormsOfKey(code, key, options3 = {}) {
      return this.getSuffixes(code, options3).map((suffix) => `${key}${suffix}`);
    }
    getSuffixes(code, options3 = {}) {
      let rule = this.getRule(code, options3);
      if (!rule) rule = this.getRule("dev", options3);
      if (!rule) return [];
      return rule.resolvedOptions().pluralCategories.sort((pluralCategory1, pluralCategory2) => suffixesOrder[pluralCategory1] - suffixesOrder[pluralCategory2]).map((pluralCategory) => `${this.options.prepend}${options3.ordinal ? `ordinal${this.options.prepend}` : ""}${pluralCategory}`);
    }
    getSuffix(code, count4, options3 = {}) {
      const rule = this.getRule(code, options3);
      if (rule) {
        return `${this.options.prepend}${options3.ordinal ? `ordinal${this.options.prepend}` : ""}${rule.select(count4)}`;
      }
      this.logger.warn(`no plural rule found for: ${code}`);
      return this.getSuffix("dev", count4, options3);
    }
  };
  var deepFindWithDefaults = (data, defaultData, key, keySeparator = ".", ignoreJSONStructure = true) => {
    let path2 = getPathWithDefaults(data, defaultData, key);
    if (!path2 && ignoreJSONStructure && isString(key)) {
      path2 = deepFind(data, key, keySeparator);
      if (path2 === void 0) path2 = deepFind(defaultData, key, keySeparator);
    }
    return path2;
  };
  var regexSafe = (val) => val.replace(/\$/g, "$$$$");
  var Interpolator = class {
    constructor(options3 = {}) {
      this.logger = baseLogger.create("interpolator");
      this.options = options3;
      this.format = options3?.interpolation?.format || ((value) => value);
      this.init(options3);
    }
    init(options3 = {}) {
      if (!options3.interpolation) options3.interpolation = {
        escapeValue: true
      };
      const {
        escape: escape$1,
        escapeValue,
        useRawValueToEscape,
        prefix,
        prefixEscaped,
        suffix,
        suffixEscaped,
        formatSeparator,
        unescapeSuffix,
        unescapePrefix,
        nestingPrefix,
        nestingPrefixEscaped,
        nestingSuffix,
        nestingSuffixEscaped,
        nestingOptionsSeparator,
        maxReplaces,
        alwaysFormat
      } = options3.interpolation;
      this.escape = escape$1 !== void 0 ? escape$1 : escape;
      this.escapeValue = escapeValue !== void 0 ? escapeValue : true;
      this.useRawValueToEscape = useRawValueToEscape !== void 0 ? useRawValueToEscape : false;
      this.prefix = prefix ? regexEscape(prefix) : prefixEscaped || "{{";
      this.suffix = suffix ? regexEscape(suffix) : suffixEscaped || "}}";
      this.formatSeparator = formatSeparator || ",";
      this.unescapePrefix = unescapeSuffix ? "" : unescapePrefix || "-";
      this.unescapeSuffix = this.unescapePrefix ? "" : unescapeSuffix || "";
      this.nestingPrefix = nestingPrefix ? regexEscape(nestingPrefix) : nestingPrefixEscaped || regexEscape("$t(");
      this.nestingSuffix = nestingSuffix ? regexEscape(nestingSuffix) : nestingSuffixEscaped || regexEscape(")");
      this.nestingOptionsSeparator = nestingOptionsSeparator || ",";
      this.maxReplaces = maxReplaces || 1e3;
      this.alwaysFormat = alwaysFormat !== void 0 ? alwaysFormat : false;
      this.resetRegExp();
    }
    reset() {
      if (this.options) this.init(this.options);
    }
    resetRegExp() {
      const getOrResetRegExp = (existingRegExp, pattern) => {
        if (existingRegExp?.source === pattern) {
          existingRegExp.lastIndex = 0;
          return existingRegExp;
        }
        return new RegExp(pattern, "g");
      };
      this.regexp = getOrResetRegExp(this.regexp, `${this.prefix}(.+?)${this.suffix}`);
      this.regexpUnescape = getOrResetRegExp(this.regexpUnescape, `${this.prefix}${this.unescapePrefix}(.+?)${this.unescapeSuffix}${this.suffix}`);
      this.nestingRegexp = getOrResetRegExp(this.nestingRegexp, `${this.nestingPrefix}((?:[^()"']+|"[^"]*"|'[^']*'|\\((?:[^()]|"[^"]*"|'[^']*')*\\))*?)${this.nestingSuffix}`);
    }
    interpolate(str, data, lng, options3) {
      let match3;
      let value;
      let replaces;
      const defaultData = this.options && this.options.interpolation && this.options.interpolation.defaultVariables || {};
      const handleFormat = (key) => {
        if (key.indexOf(this.formatSeparator) < 0) {
          const path2 = deepFindWithDefaults(data, defaultData, key, this.options.keySeparator, this.options.ignoreJSONStructure);
          return this.alwaysFormat ? this.format(path2, void 0, lng, {
            ...options3,
            ...data,
            interpolationkey: key
          }) : path2;
        }
        const p2 = key.split(this.formatSeparator);
        const k3 = p2.shift().trim();
        const f = p2.join(this.formatSeparator).trim();
        return this.format(deepFindWithDefaults(data, defaultData, k3, this.options.keySeparator, this.options.ignoreJSONStructure), f, lng, {
          ...options3,
          ...data,
          interpolationkey: k3
        });
      };
      this.resetRegExp();
      const missingInterpolationHandler = options3?.missingInterpolationHandler || this.options.missingInterpolationHandler;
      const skipOnVariables = options3?.interpolation?.skipOnVariables !== void 0 ? options3.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables;
      const todos = [{
        regex: this.regexpUnescape,
        safeValue: (val) => regexSafe(val)
      }, {
        regex: this.regexp,
        safeValue: (val) => this.escapeValue ? regexSafe(this.escape(val)) : regexSafe(val)
      }];
      todos.forEach((todo) => {
        replaces = 0;
        while (match3 = todo.regex.exec(str)) {
          const matchedVar = match3[1].trim();
          value = handleFormat(matchedVar);
          if (value === void 0) {
            if (typeof missingInterpolationHandler === "function") {
              const temp = missingInterpolationHandler(str, match3, options3);
              value = isString(temp) ? temp : "";
            } else if (options3 && Object.prototype.hasOwnProperty.call(options3, matchedVar)) {
              value = "";
            } else if (skipOnVariables) {
              value = match3[0];
              continue;
            } else {
              this.logger.warn(`missed to pass in variable ${matchedVar} for interpolating ${str}`);
              value = "";
            }
          } else if (!isString(value) && !this.useRawValueToEscape) {
            value = makeString(value);
          }
          const safeValue = todo.safeValue(value);
          str = str.replace(match3[0], safeValue);
          if (skipOnVariables) {
            todo.regex.lastIndex += value.length;
            todo.regex.lastIndex -= match3[0].length;
          } else {
            todo.regex.lastIndex = 0;
          }
          replaces++;
          if (replaces >= this.maxReplaces) {
            break;
          }
        }
      });
      return str;
    }
    nest(str, fc, options3 = {}) {
      let match3;
      let value;
      let clonedOptions;
      const handleHasOptions = (key, inheritedOptions) => {
        const sep = this.nestingOptionsSeparator;
        if (key.indexOf(sep) < 0) return key;
        const c = key.split(new RegExp(`${regexEscape(sep)}[ ]*{`));
        let optionsString = `{${c[1]}`;
        key = c[0];
        optionsString = this.interpolate(optionsString, clonedOptions);
        const matchedSingleQuotes = optionsString.match(/'/g);
        const matchedDoubleQuotes = optionsString.match(/"/g);
        if ((matchedSingleQuotes?.length ?? 0) % 2 === 0 && !matchedDoubleQuotes || (matchedDoubleQuotes?.length ?? 0) % 2 !== 0) {
          optionsString = optionsString.replace(/'/g, '"');
        }
        try {
          clonedOptions = JSON.parse(optionsString);
          if (inheritedOptions) clonedOptions = {
            ...inheritedOptions,
            ...clonedOptions
          };
        } catch (e2) {
          this.logger.warn(`failed parsing options string in nesting for key ${key}`, e2);
          return `${key}${sep}${optionsString}`;
        }
        if (clonedOptions.defaultValue && clonedOptions.defaultValue.indexOf(this.prefix) > -1) delete clonedOptions.defaultValue;
        return key;
      };
      while (match3 = this.nestingRegexp.exec(str)) {
        let formatters2 = [];
        clonedOptions = {
          ...options3
        };
        clonedOptions = clonedOptions.replace && !isString(clonedOptions.replace) ? clonedOptions.replace : clonedOptions;
        clonedOptions.applyPostProcessor = false;
        delete clonedOptions.defaultValue;
        const keyEndIndex = /{.*}/.test(match3[1]) ? match3[1].lastIndexOf("}") + 1 : match3[1].indexOf(this.formatSeparator);
        if (keyEndIndex !== -1) {
          formatters2 = match3[1].slice(keyEndIndex).split(this.formatSeparator).map((elem) => elem.trim()).filter(Boolean);
          match3[1] = match3[1].slice(0, keyEndIndex);
        }
        value = fc(handleHasOptions.call(this, match3[1].trim(), clonedOptions), clonedOptions);
        if (value && match3[0] === str && !isString(value)) return value;
        if (!isString(value)) value = makeString(value);
        if (!value) {
          this.logger.warn(`missed to resolve ${match3[1]} for nesting ${str}`);
          value = "";
        }
        if (formatters2.length) {
          value = formatters2.reduce((v, f) => this.format(v, f, options3.lng, {
            ...options3,
            interpolationkey: match3[1].trim()
          }), value.trim());
        }
        str = str.replace(match3[0], value);
        this.regexp.lastIndex = 0;
      }
      return str;
    }
  };
  var parseFormatStr = (formatStr) => {
    let formatName = formatStr.toLowerCase().trim();
    const formatOptions = {};
    if (formatStr.indexOf("(") > -1) {
      const p2 = formatStr.split("(");
      formatName = p2[0].toLowerCase().trim();
      const optStr = p2[1].substring(0, p2[1].length - 1);
      if (formatName === "currency" && optStr.indexOf(":") < 0) {
        if (!formatOptions.currency) formatOptions.currency = optStr.trim();
      } else if (formatName === "relativetime" && optStr.indexOf(":") < 0) {
        if (!formatOptions.range) formatOptions.range = optStr.trim();
      } else {
        const opts = optStr.split(";");
        opts.forEach((opt) => {
          if (opt) {
            const [key, ...rest] = opt.split(":");
            const val = rest.join(":").trim().replace(/^'+|'+$/g, "");
            const trimmedKey = key.trim();
            if (!formatOptions[trimmedKey]) formatOptions[trimmedKey] = val;
            if (val === "false") formatOptions[trimmedKey] = false;
            if (val === "true") formatOptions[trimmedKey] = true;
            if (!isNaN(val)) formatOptions[trimmedKey] = parseInt(val, 10);
          }
        });
      }
    }
    return {
      formatName,
      formatOptions
    };
  };
  var createCachedFormatter = (fn) => {
    const cache = {};
    return (v, l, o) => {
      let optForCache = o;
      if (o && o.interpolationkey && o.formatParams && o.formatParams[o.interpolationkey] && o[o.interpolationkey]) {
        optForCache = {
          ...optForCache,
          [o.interpolationkey]: void 0
        };
      }
      const key = l + JSON.stringify(optForCache);
      let frm = cache[key];
      if (!frm) {
        frm = fn(getCleanedCode(l), o);
        cache[key] = frm;
      }
      return frm(v);
    };
  };
  var createNonCachedFormatter = (fn) => (v, l, o) => fn(getCleanedCode(l), o)(v);
  var Formatter = class {
    constructor(options3 = {}) {
      this.logger = baseLogger.create("formatter");
      this.options = options3;
      this.init(options3);
    }
    init(services, options3 = {
      interpolation: {}
    }) {
      this.formatSeparator = options3.interpolation.formatSeparator || ",";
      const cf = options3.cacheInBuiltFormats ? createCachedFormatter : createNonCachedFormatter;
      this.formats = {
        number: cf((lng, opt) => {
          const formatter = new Intl.NumberFormat(lng, {
            ...opt
          });
          return (val) => formatter.format(val);
        }),
        currency: cf((lng, opt) => {
          const formatter = new Intl.NumberFormat(lng, {
            ...opt,
            style: "currency"
          });
          return (val) => formatter.format(val);
        }),
        datetime: cf((lng, opt) => {
          const formatter = new Intl.DateTimeFormat(lng, {
            ...opt
          });
          return (val) => formatter.format(val);
        }),
        relativetime: cf((lng, opt) => {
          const formatter = new Intl.RelativeTimeFormat(lng, {
            ...opt
          });
          return (val) => formatter.format(val, opt.range || "day");
        }),
        list: cf((lng, opt) => {
          const formatter = new Intl.ListFormat(lng, {
            ...opt
          });
          return (val) => formatter.format(val);
        })
      };
    }
    add(name, fc) {
      this.formats[name.toLowerCase().trim()] = fc;
    }
    addCached(name, fc) {
      this.formats[name.toLowerCase().trim()] = createCachedFormatter(fc);
    }
    format(value, format3, lng, options3 = {}) {
      const formats = format3.split(this.formatSeparator);
      if (formats.length > 1 && formats[0].indexOf("(") > 1 && formats[0].indexOf(")") < 0 && formats.find((f) => f.indexOf(")") > -1)) {
        const lastIndex = formats.findIndex((f) => f.indexOf(")") > -1);
        formats[0] = [formats[0], ...formats.splice(1, lastIndex)].join(this.formatSeparator);
      }
      const result = formats.reduce((mem, f) => {
        const {
          formatName,
          formatOptions
        } = parseFormatStr(f);
        if (this.formats[formatName]) {
          let formatted = mem;
          try {
            const valOptions = options3?.formatParams?.[options3.interpolationkey] || {};
            const l = valOptions.locale || valOptions.lng || options3.locale || options3.lng || lng;
            formatted = this.formats[formatName](mem, l, {
              ...formatOptions,
              ...options3,
              ...valOptions
            });
          } catch (error2) {
            this.logger.warn(error2);
          }
          return formatted;
        } else {
          this.logger.warn(`there was no format function for ${formatName}`);
        }
        return mem;
      }, value);
      return result;
    }
  };
  var removePending = (q, name) => {
    if (q.pending[name] !== void 0) {
      delete q.pending[name];
      q.pendingCount--;
    }
  };
  var Connector = class extends EventEmitter {
    constructor(backend, store, services, options3 = {}) {
      super();
      this.backend = backend;
      this.store = store;
      this.services = services;
      this.languageUtils = services.languageUtils;
      this.options = options3;
      this.logger = baseLogger.create("backendConnector");
      this.waitingReads = [];
      this.maxParallelReads = options3.maxParallelReads || 10;
      this.readingCalls = 0;
      this.maxRetries = options3.maxRetries >= 0 ? options3.maxRetries : 5;
      this.retryTimeout = options3.retryTimeout >= 1 ? options3.retryTimeout : 350;
      this.state = {};
      this.queue = [];
      this.backend?.init?.(services, options3.backend, options3);
    }
    queueLoad(languages, namespaces, options3, callback) {
      const toLoad = {};
      const pending = {};
      const toLoadLanguages = {};
      const toLoadNamespaces = {};
      languages.forEach((lng) => {
        let hasAllNamespaces = true;
        namespaces.forEach((ns) => {
          const name = `${lng}|${ns}`;
          if (!options3.reload && this.store.hasResourceBundle(lng, ns)) {
            this.state[name] = 2;
          } else if (this.state[name] < 0) ;
          else if (this.state[name] === 1) {
            if (pending[name] === void 0) pending[name] = true;
          } else {
            this.state[name] = 1;
            hasAllNamespaces = false;
            if (pending[name] === void 0) pending[name] = true;
            if (toLoad[name] === void 0) toLoad[name] = true;
            if (toLoadNamespaces[ns] === void 0) toLoadNamespaces[ns] = true;
          }
        });
        if (!hasAllNamespaces) toLoadLanguages[lng] = true;
      });
      if (Object.keys(toLoad).length || Object.keys(pending).length) {
        this.queue.push({
          pending,
          pendingCount: Object.keys(pending).length,
          loaded: {},
          errors: [],
          callback
        });
      }
      return {
        toLoad: Object.keys(toLoad),
        pending: Object.keys(pending),
        toLoadLanguages: Object.keys(toLoadLanguages),
        toLoadNamespaces: Object.keys(toLoadNamespaces)
      };
    }
    loaded(name, err, data) {
      const s = name.split("|");
      const lng = s[0];
      const ns = s[1];
      if (err) this.emit("failedLoading", lng, ns, err);
      if (!err && data) {
        this.store.addResourceBundle(lng, ns, data, void 0, void 0, {
          skipCopy: true
        });
      }
      this.state[name] = err ? -1 : 2;
      if (err && data) this.state[name] = 0;
      const loaded = {};
      this.queue.forEach((q) => {
        pushPath(q.loaded, [lng], ns);
        removePending(q, name);
        if (err) q.errors.push(err);
        if (q.pendingCount === 0 && !q.done) {
          Object.keys(q.loaded).forEach((l) => {
            if (!loaded[l]) loaded[l] = {};
            const loadedKeys = q.loaded[l];
            if (loadedKeys.length) {
              loadedKeys.forEach((n) => {
                if (loaded[l][n] === void 0) loaded[l][n] = true;
              });
            }
          });
          q.done = true;
          if (q.errors.length) {
            q.callback(q.errors);
          } else {
            q.callback();
          }
        }
      });
      this.emit("loaded", loaded);
      this.queue = this.queue.filter((q) => !q.done);
    }
    read(lng, ns, fcName, tried = 0, wait = this.retryTimeout, callback) {
      if (!lng.length) return callback(null, {});
      if (this.readingCalls >= this.maxParallelReads) {
        this.waitingReads.push({
          lng,
          ns,
          fcName,
          tried,
          wait,
          callback
        });
        return;
      }
      this.readingCalls++;
      const resolver = (err, data) => {
        this.readingCalls--;
        if (this.waitingReads.length > 0) {
          const next = this.waitingReads.shift();
          this.read(next.lng, next.ns, next.fcName, next.tried, next.wait, next.callback);
        }
        if (err && data && tried < this.maxRetries) {
          setTimeout(() => {
            this.read.call(this, lng, ns, fcName, tried + 1, wait * 2, callback);
          }, wait);
          return;
        }
        callback(err, data);
      };
      const fc = this.backend[fcName].bind(this.backend);
      if (fc.length === 2) {
        try {
          const r2 = fc(lng, ns);
          if (r2 && typeof r2.then === "function") {
            r2.then((data) => resolver(null, data)).catch(resolver);
          } else {
            resolver(null, r2);
          }
        } catch (err) {
          resolver(err);
        }
        return;
      }
      return fc(lng, ns, resolver);
    }
    prepareLoading(languages, namespaces, options3 = {}, callback) {
      if (!this.backend) {
        this.logger.warn("No backend was added via i18next.use. Will not load resources.");
        return callback && callback();
      }
      if (isString(languages)) languages = this.languageUtils.toResolveHierarchy(languages);
      if (isString(namespaces)) namespaces = [namespaces];
      const toLoad = this.queueLoad(languages, namespaces, options3, callback);
      if (!toLoad.toLoad.length) {
        if (!toLoad.pending.length) callback();
        return null;
      }
      toLoad.toLoad.forEach((name) => {
        this.loadOne(name);
      });
    }
    load(languages, namespaces, callback) {
      this.prepareLoading(languages, namespaces, {}, callback);
    }
    reload(languages, namespaces, callback) {
      this.prepareLoading(languages, namespaces, {
        reload: true
      }, callback);
    }
    loadOne(name, prefix = "") {
      const s = name.split("|");
      const lng = s[0];
      const ns = s[1];
      this.read(lng, ns, "read", void 0, void 0, (err, data) => {
        if (err) this.logger.warn(`${prefix}loading namespace ${ns} for language ${lng} failed`, err);
        if (!err && data) this.logger.log(`${prefix}loaded namespace ${ns} for language ${lng}`, data);
        this.loaded(name, err, data);
      });
    }
    saveMissing(languages, namespace, key, fallbackValue, isUpdate, options3 = {}, clb = () => {
    }) {
      if (this.services?.utils?.hasLoadedNamespace && !this.services?.utils?.hasLoadedNamespace(namespace)) {
        this.logger.warn(`did not save key "${key}" as the namespace "${namespace}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!");
        return;
      }
      if (key === void 0 || key === null || key === "") return;
      if (this.backend?.create) {
        const opts = {
          ...options3,
          isUpdate
        };
        const fc = this.backend.create.bind(this.backend);
        if (fc.length < 6) {
          try {
            let r2;
            if (fc.length === 5) {
              r2 = fc(languages, namespace, key, fallbackValue, opts);
            } else {
              r2 = fc(languages, namespace, key, fallbackValue);
            }
            if (r2 && typeof r2.then === "function") {
              r2.then((data) => clb(null, data)).catch(clb);
            } else {
              clb(null, r2);
            }
          } catch (err) {
            clb(err);
          }
        } else {
          fc(languages, namespace, key, fallbackValue, clb, opts);
        }
      }
      if (!languages || !languages[0]) return;
      this.store.addResource(languages[0], namespace, key, fallbackValue);
    }
  };
  var get = () => ({
    debug: false,
    initAsync: true,
    ns: ["translation"],
    defaultNS: ["translation"],
    fallbackLng: ["dev"],
    fallbackNS: false,
    supportedLngs: false,
    nonExplicitSupportedLngs: false,
    load: "all",
    preload: false,
    simplifyPluralSuffix: true,
    keySeparator: ".",
    nsSeparator: ":",
    pluralSeparator: "_",
    contextSeparator: "_",
    partialBundledLanguages: false,
    saveMissing: false,
    updateMissing: false,
    saveMissingTo: "fallback",
    saveMissingPlurals: true,
    missingKeyHandler: false,
    missingInterpolationHandler: false,
    postProcess: false,
    postProcessPassResolved: false,
    returnNull: false,
    returnEmptyString: true,
    returnObjects: false,
    joinArrays: false,
    returnedObjectHandler: false,
    parseMissingKeyHandler: false,
    appendNamespaceToMissingKey: false,
    appendNamespaceToCIMode: false,
    overloadTranslationOptionHandler: (args) => {
      let ret = {};
      if (typeof args[1] === "object") ret = args[1];
      if (isString(args[1])) ret.defaultValue = args[1];
      if (isString(args[2])) ret.tDescription = args[2];
      if (typeof args[2] === "object" || typeof args[3] === "object") {
        const options3 = args[3] || args[2];
        Object.keys(options3).forEach((key) => {
          ret[key] = options3[key];
        });
      }
      return ret;
    },
    interpolation: {
      escapeValue: true,
      format: (value) => value,
      prefix: "{{",
      suffix: "}}",
      formatSeparator: ",",
      unescapePrefix: "-",
      nestingPrefix: "$t(",
      nestingSuffix: ")",
      nestingOptionsSeparator: ",",
      maxReplaces: 1e3,
      skipOnVariables: true
    },
    cacheInBuiltFormats: true
  });
  var transformOptions = (options3) => {
    if (isString(options3.ns)) options3.ns = [options3.ns];
    if (isString(options3.fallbackLng)) options3.fallbackLng = [options3.fallbackLng];
    if (isString(options3.fallbackNS)) options3.fallbackNS = [options3.fallbackNS];
    if (options3.supportedLngs?.indexOf?.("cimode") < 0) {
      options3.supportedLngs = options3.supportedLngs.concat(["cimode"]);
    }
    if (typeof options3.initImmediate === "boolean") options3.initAsync = options3.initImmediate;
    return options3;
  };
  var noop = () => {
  };
  var bindMemberFunctions = (inst) => {
    const mems = Object.getOwnPropertyNames(Object.getPrototypeOf(inst));
    mems.forEach((mem) => {
      if (typeof inst[mem] === "function") {
        inst[mem] = inst[mem].bind(inst);
      }
    });
  };
  var SUPPORT_NOTICE_KEY = "__i18next_supportNoticeShown";
  var getSupportNoticeShown = () => typeof globalThis !== "undefined" && !!globalThis[SUPPORT_NOTICE_KEY];
  var setSupportNoticeShown = () => {
    if (typeof globalThis !== "undefined") globalThis[SUPPORT_NOTICE_KEY] = true;
  };
  var usesLocize = (inst) => {
    if (inst?.modules?.backend?.name?.indexOf("Locize") > 0) return true;
    if (inst?.modules?.backend?.constructor?.name?.indexOf("Locize") > 0) return true;
    if (inst?.options?.backend?.backends) {
      if (inst.options.backend.backends.some((b) => b?.name?.indexOf("Locize") > 0 || b?.constructor?.name?.indexOf("Locize") > 0)) return true;
    }
    if (inst?.options?.backend?.projectId) return true;
    if (inst?.options?.backend?.backendOptions) {
      if (inst.options.backend.backendOptions.some((b) => b?.projectId)) return true;
    }
    return false;
  };
  var I18n = class _I18n extends EventEmitter {
    constructor(options3 = {}, callback) {
      super();
      this.options = transformOptions(options3);
      this.services = {};
      this.logger = baseLogger;
      this.modules = {
        external: []
      };
      bindMemberFunctions(this);
      if (callback && !this.isInitialized && !options3.isClone) {
        if (!this.options.initAsync) {
          this.init(options3, callback);
          return this;
        }
        setTimeout(() => {
          this.init(options3, callback);
        }, 0);
      }
    }
    init(options3 = {}, callback) {
      this.isInitializing = true;
      if (typeof options3 === "function") {
        callback = options3;
        options3 = {};
      }
      if (options3.defaultNS == null && options3.ns) {
        if (isString(options3.ns)) {
          options3.defaultNS = options3.ns;
        } else if (options3.ns.indexOf("translation") < 0) {
          options3.defaultNS = options3.ns[0];
        }
      }
      const defOpts = get();
      this.options = {
        ...defOpts,
        ...this.options,
        ...transformOptions(options3)
      };
      this.options.interpolation = {
        ...defOpts.interpolation,
        ...this.options.interpolation
      };
      if (options3.keySeparator !== void 0) {
        this.options.userDefinedKeySeparator = options3.keySeparator;
      }
      if (options3.nsSeparator !== void 0) {
        this.options.userDefinedNsSeparator = options3.nsSeparator;
      }
      if (typeof this.options.overloadTranslationOptionHandler !== "function") {
        this.options.overloadTranslationOptionHandler = defOpts.overloadTranslationOptionHandler;
      }
      if (this.options.showSupportNotice !== false && !usesLocize(this) && !getSupportNoticeShown()) {
        if (typeof console !== "undefined" && typeof console.info !== "undefined") console.info("\u{1F310} i18next is maintained with support from Locize \u2014 consider powering your project with managed localization (AI, CDN, integrations): https://locize.com \u{1F499}");
        setSupportNoticeShown();
      }
      const createClassOnDemand = (ClassOrObject) => {
        if (!ClassOrObject) return null;
        if (typeof ClassOrObject === "function") return new ClassOrObject();
        return ClassOrObject;
      };
      if (!this.options.isClone) {
        if (this.modules.logger) {
          baseLogger.init(createClassOnDemand(this.modules.logger), this.options);
        } else {
          baseLogger.init(null, this.options);
        }
        let formatter;
        if (this.modules.formatter) {
          formatter = this.modules.formatter;
        } else {
          formatter = Formatter;
        }
        const lu = new LanguageUtil(this.options);
        this.store = new ResourceStore(this.options.resources, this.options);
        const s = this.services;
        s.logger = baseLogger;
        s.resourceStore = this.store;
        s.languageUtils = lu;
        s.pluralResolver = new PluralResolver(lu, {
          prepend: this.options.pluralSeparator,
          simplifyPluralSuffix: this.options.simplifyPluralSuffix
        });
        const usingLegacyFormatFunction = this.options.interpolation.format && this.options.interpolation.format !== defOpts.interpolation.format;
        if (usingLegacyFormatFunction) {
          this.logger.deprecate(`init: you are still using the legacy format function, please use the new approach: https://www.i18next.com/translation-function/formatting`);
        }
        if (formatter && (!this.options.interpolation.format || this.options.interpolation.format === defOpts.interpolation.format)) {
          s.formatter = createClassOnDemand(formatter);
          if (s.formatter.init) s.formatter.init(s, this.options);
          this.options.interpolation.format = s.formatter.format.bind(s.formatter);
        }
        s.interpolator = new Interpolator(this.options);
        s.utils = {
          hasLoadedNamespace: this.hasLoadedNamespace.bind(this)
        };
        s.backendConnector = new Connector(createClassOnDemand(this.modules.backend), s.resourceStore, s, this.options);
        s.backendConnector.on("*", (event, ...args) => {
          this.emit(event, ...args);
        });
        if (this.modules.languageDetector) {
          s.languageDetector = createClassOnDemand(this.modules.languageDetector);
          if (s.languageDetector.init) s.languageDetector.init(s, this.options.detection, this.options);
        }
        if (this.modules.i18nFormat) {
          s.i18nFormat = createClassOnDemand(this.modules.i18nFormat);
          if (s.i18nFormat.init) s.i18nFormat.init(this);
        }
        this.translator = new Translator(this.services, this.options);
        this.translator.on("*", (event, ...args) => {
          this.emit(event, ...args);
        });
        this.modules.external.forEach((m2) => {
          if (m2.init) m2.init(this);
        });
      }
      this.format = this.options.interpolation.format;
      if (!callback) callback = noop;
      if (this.options.fallbackLng && !this.services.languageDetector && !this.options.lng) {
        const codes = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
        if (codes.length > 0 && codes[0] !== "dev") this.options.lng = codes[0];
      }
      if (!this.services.languageDetector && !this.options.lng) {
        this.logger.warn("init: no languageDetector is used and no lng is defined");
      }
      const storeApi = ["getResource", "hasResourceBundle", "getResourceBundle", "getDataByLanguage"];
      storeApi.forEach((fcName) => {
        this[fcName] = (...args) => this.store[fcName](...args);
      });
      const storeApiChained = ["addResource", "addResources", "addResourceBundle", "removeResourceBundle"];
      storeApiChained.forEach((fcName) => {
        this[fcName] = (...args) => {
          this.store[fcName](...args);
          return this;
        };
      });
      const deferred = defer();
      const load = () => {
        const finish = (err, t3) => {
          this.isInitializing = false;
          if (this.isInitialized && !this.initializedStoreOnce) this.logger.warn("init: i18next is already initialized. You should call init just once!");
          this.isInitialized = true;
          if (!this.options.isClone) this.logger.log("initialized", this.options);
          this.emit("initialized", this.options);
          deferred.resolve(t3);
          callback(err, t3);
        };
        if (this.languages && !this.isInitialized) return finish(null, this.t.bind(this));
        this.changeLanguage(this.options.lng, finish);
      };
      if (this.options.resources || !this.options.initAsync) {
        load();
      } else {
        setTimeout(load, 0);
      }
      return deferred;
    }
    loadResources(language, callback = noop) {
      let usedCallback = callback;
      const usedLng = isString(language) ? language : this.language;
      if (typeof language === "function") usedCallback = language;
      if (!this.options.resources || this.options.partialBundledLanguages) {
        if (usedLng?.toLowerCase() === "cimode" && (!this.options.preload || this.options.preload.length === 0)) return usedCallback();
        const toLoad = [];
        const append = (lng) => {
          if (!lng) return;
          if (lng === "cimode") return;
          const lngs = this.services.languageUtils.toResolveHierarchy(lng);
          lngs.forEach((l) => {
            if (l === "cimode") return;
            if (toLoad.indexOf(l) < 0) toLoad.push(l);
          });
        };
        if (!usedLng) {
          const fallbacks = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
          fallbacks.forEach((l) => append(l));
        } else {
          append(usedLng);
        }
        this.options.preload?.forEach?.((l) => append(l));
        this.services.backendConnector.load(toLoad, this.options.ns, (e2) => {
          if (!e2 && !this.resolvedLanguage && this.language) this.setResolvedLanguage(this.language);
          usedCallback(e2);
        });
      } else {
        usedCallback(null);
      }
    }
    reloadResources(lngs, ns, callback) {
      const deferred = defer();
      if (typeof lngs === "function") {
        callback = lngs;
        lngs = void 0;
      }
      if (typeof ns === "function") {
        callback = ns;
        ns = void 0;
      }
      if (!lngs) lngs = this.languages;
      if (!ns) ns = this.options.ns;
      if (!callback) callback = noop;
      this.services.backendConnector.reload(lngs, ns, (err) => {
        deferred.resolve();
        callback(err);
      });
      return deferred;
    }
    use(module) {
      if (!module) throw new Error("You are passing an undefined module! Please check the object you are passing to i18next.use()");
      if (!module.type) throw new Error("You are passing a wrong module! Please check the object you are passing to i18next.use()");
      if (module.type === "backend") {
        this.modules.backend = module;
      }
      if (module.type === "logger" || module.log && module.warn && module.error) {
        this.modules.logger = module;
      }
      if (module.type === "languageDetector") {
        this.modules.languageDetector = module;
      }
      if (module.type === "i18nFormat") {
        this.modules.i18nFormat = module;
      }
      if (module.type === "postProcessor") {
        postProcessor.addPostProcessor(module);
      }
      if (module.type === "formatter") {
        this.modules.formatter = module;
      }
      if (module.type === "3rdParty") {
        this.modules.external.push(module);
      }
      return this;
    }
    setResolvedLanguage(l) {
      if (!l || !this.languages) return;
      if (["cimode", "dev"].indexOf(l) > -1) return;
      for (let li = 0; li < this.languages.length; li++) {
        const lngInLngs = this.languages[li];
        if (["cimode", "dev"].indexOf(lngInLngs) > -1) continue;
        if (this.store.hasLanguageSomeTranslations(lngInLngs)) {
          this.resolvedLanguage = lngInLngs;
          break;
        }
      }
      if (!this.resolvedLanguage && this.languages.indexOf(l) < 0 && this.store.hasLanguageSomeTranslations(l)) {
        this.resolvedLanguage = l;
        this.languages.unshift(l);
      }
    }
    changeLanguage(lng, callback) {
      this.isLanguageChangingTo = lng;
      const deferred = defer();
      this.emit("languageChanging", lng);
      const setLngProps = (l) => {
        this.language = l;
        this.languages = this.services.languageUtils.toResolveHierarchy(l);
        this.resolvedLanguage = void 0;
        this.setResolvedLanguage(l);
      };
      const done = (err, l) => {
        if (l) {
          if (this.isLanguageChangingTo === lng) {
            setLngProps(l);
            this.translator.changeLanguage(l);
            this.isLanguageChangingTo = void 0;
            this.emit("languageChanged", l);
            this.logger.log("languageChanged", l);
          }
        } else {
          this.isLanguageChangingTo = void 0;
        }
        deferred.resolve((...args) => this.t(...args));
        if (callback) callback(err, (...args) => this.t(...args));
      };
      const setLng = (lngs) => {
        if (!lng && !lngs && this.services.languageDetector) lngs = [];
        const fl = isString(lngs) ? lngs : lngs && lngs[0];
        const l = this.store.hasLanguageSomeTranslations(fl) ? fl : this.services.languageUtils.getBestMatchFromCodes(isString(lngs) ? [lngs] : lngs);
        if (l) {
          if (!this.language) {
            setLngProps(l);
          }
          if (!this.translator.language) this.translator.changeLanguage(l);
          this.services.languageDetector?.cacheUserLanguage?.(l);
        }
        this.loadResources(l, (err) => {
          done(err, l);
        });
      };
      if (!lng && this.services.languageDetector && !this.services.languageDetector.async) {
        setLng(this.services.languageDetector.detect());
      } else if (!lng && this.services.languageDetector && this.services.languageDetector.async) {
        if (this.services.languageDetector.detect.length === 0) {
          this.services.languageDetector.detect().then(setLng);
        } else {
          this.services.languageDetector.detect(setLng);
        }
      } else {
        setLng(lng);
      }
      return deferred;
    }
    getFixedT(lng, ns, keyPrefix) {
      const fixedT = (key, opts, ...rest) => {
        let o;
        if (typeof opts !== "object") {
          o = this.options.overloadTranslationOptionHandler([key, opts].concat(rest));
        } else {
          o = {
            ...opts
          };
        }
        o.lng = o.lng || fixedT.lng;
        o.lngs = o.lngs || fixedT.lngs;
        o.ns = o.ns || fixedT.ns;
        if (o.keyPrefix !== "") o.keyPrefix = o.keyPrefix || keyPrefix || fixedT.keyPrefix;
        const keySeparator = this.options.keySeparator || ".";
        let resultKey;
        if (o.keyPrefix && Array.isArray(key)) {
          resultKey = key.map((k3) => {
            if (typeof k3 === "function") k3 = keysFromSelector(k3, {
              ...this.options,
              ...opts
            });
            return `${o.keyPrefix}${keySeparator}${k3}`;
          });
        } else {
          if (typeof key === "function") key = keysFromSelector(key, {
            ...this.options,
            ...opts
          });
          resultKey = o.keyPrefix ? `${o.keyPrefix}${keySeparator}${key}` : key;
        }
        return this.t(resultKey, o);
      };
      if (isString(lng)) {
        fixedT.lng = lng;
      } else {
        fixedT.lngs = lng;
      }
      fixedT.ns = ns;
      fixedT.keyPrefix = keyPrefix;
      return fixedT;
    }
    t(...args) {
      return this.translator?.translate(...args);
    }
    exists(...args) {
      return this.translator?.exists(...args);
    }
    setDefaultNamespace(ns) {
      this.options.defaultNS = ns;
    }
    hasLoadedNamespace(ns, options3 = {}) {
      if (!this.isInitialized) {
        this.logger.warn("hasLoadedNamespace: i18next was not initialized", this.languages);
        return false;
      }
      if (!this.languages || !this.languages.length) {
        this.logger.warn("hasLoadedNamespace: i18n.languages were undefined or empty", this.languages);
        return false;
      }
      const lng = options3.lng || this.resolvedLanguage || this.languages[0];
      const fallbackLng = this.options ? this.options.fallbackLng : false;
      const lastLng = this.languages[this.languages.length - 1];
      if (lng.toLowerCase() === "cimode") return true;
      const loadNotPending = (l, n) => {
        const loadState = this.services.backendConnector.state[`${l}|${n}`];
        return loadState === -1 || loadState === 0 || loadState === 2;
      };
      if (options3.precheck) {
        const preResult = options3.precheck(this, loadNotPending);
        if (preResult !== void 0) return preResult;
      }
      if (this.hasResourceBundle(lng, ns)) return true;
      if (!this.services.backendConnector.backend || this.options.resources && !this.options.partialBundledLanguages) return true;
      if (loadNotPending(lng, ns) && (!fallbackLng || loadNotPending(lastLng, ns))) return true;
      return false;
    }
    loadNamespaces(ns, callback) {
      const deferred = defer();
      if (!this.options.ns) {
        if (callback) callback();
        return Promise.resolve();
      }
      if (isString(ns)) ns = [ns];
      ns.forEach((n) => {
        if (this.options.ns.indexOf(n) < 0) this.options.ns.push(n);
      });
      this.loadResources((err) => {
        deferred.resolve();
        if (callback) callback(err);
      });
      return deferred;
    }
    loadLanguages(lngs, callback) {
      const deferred = defer();
      if (isString(lngs)) lngs = [lngs];
      const preloaded = this.options.preload || [];
      const newLngs = lngs.filter((lng) => preloaded.indexOf(lng) < 0 && this.services.languageUtils.isSupportedCode(lng));
      if (!newLngs.length) {
        if (callback) callback();
        return Promise.resolve();
      }
      this.options.preload = preloaded.concat(newLngs);
      this.loadResources((err) => {
        deferred.resolve();
        if (callback) callback(err);
      });
      return deferred;
    }
    dir(lng) {
      if (!lng) lng = this.resolvedLanguage || (this.languages?.length > 0 ? this.languages[0] : this.language);
      if (!lng) return "rtl";
      try {
        const l = new Intl.Locale(lng);
        if (l && l.getTextInfo) {
          const ti = l.getTextInfo();
          if (ti && ti.direction) return ti.direction;
        }
      } catch (e2) {
      }
      const rtlLngs = ["ar", "shu", "sqr", "ssh", "xaa", "yhd", "yud", "aao", "abh", "abv", "acm", "acq", "acw", "acx", "acy", "adf", "ads", "aeb", "aec", "afb", "ajp", "apc", "apd", "arb", "arq", "ars", "ary", "arz", "auz", "avl", "ayh", "ayl", "ayn", "ayp", "bbz", "pga", "he", "iw", "ps", "pbt", "pbu", "pst", "prp", "prd", "ug", "ur", "ydd", "yds", "yih", "ji", "yi", "hbo", "men", "xmn", "fa", "jpr", "peo", "pes", "prs", "dv", "sam", "ckb"];
      const languageUtils = this.services?.languageUtils || new LanguageUtil(get());
      if (lng.toLowerCase().indexOf("-latn") > 1) return "ltr";
      return rtlLngs.indexOf(languageUtils.getLanguagePartFromCode(lng)) > -1 || lng.toLowerCase().indexOf("-arab") > 1 ? "rtl" : "ltr";
    }
    static createInstance(options3 = {}, callback) {
      const instance2 = new _I18n(options3, callback);
      instance2.createInstance = _I18n.createInstance;
      return instance2;
    }
    cloneInstance(options3 = {}, callback = noop) {
      const forkResourceStore = options3.forkResourceStore;
      if (forkResourceStore) delete options3.forkResourceStore;
      const mergedOptions = {
        ...this.options,
        ...options3,
        ...{
          isClone: true
        }
      };
      const clone = new _I18n(mergedOptions);
      if (options3.debug !== void 0 || options3.prefix !== void 0) {
        clone.logger = clone.logger.clone(options3);
      }
      const membersToCopy = ["store", "services", "language"];
      membersToCopy.forEach((m2) => {
        clone[m2] = this[m2];
      });
      clone.services = {
        ...this.services
      };
      clone.services.utils = {
        hasLoadedNamespace: clone.hasLoadedNamespace.bind(clone)
      };
      if (forkResourceStore) {
        const clonedData = Object.keys(this.store.data).reduce((prev, l) => {
          prev[l] = {
            ...this.store.data[l]
          };
          prev[l] = Object.keys(prev[l]).reduce((acc, n) => {
            acc[n] = {
              ...prev[l][n]
            };
            return acc;
          }, prev[l]);
          return prev;
        }, {});
        clone.store = new ResourceStore(clonedData, mergedOptions);
        clone.services.resourceStore = clone.store;
      }
      if (options3.interpolation) {
        const defOpts = get();
        const mergedInterpolation = {
          ...defOpts.interpolation,
          ...this.options.interpolation,
          ...options3.interpolation
        };
        const mergedForInterpolator = {
          ...mergedOptions,
          interpolation: mergedInterpolation
        };
        clone.services.interpolator = new Interpolator(mergedForInterpolator);
      }
      clone.translator = new Translator(clone.services, mergedOptions);
      clone.translator.on("*", (event, ...args) => {
        clone.emit(event, ...args);
      });
      clone.init(mergedOptions, callback);
      clone.translator.options = mergedOptions;
      clone.translator.backendConnector.services.utils = {
        hasLoadedNamespace: clone.hasLoadedNamespace.bind(clone)
      };
      return clone;
    }
    toJSON() {
      return {
        options: this.options,
        store: this.store,
        language: this.language,
        languages: this.languages,
        resolvedLanguage: this.resolvedLanguage
      };
    }
  };
  var instance = I18n.createInstance();
  var createInstance = instance.createInstance;
  var dir = instance.dir;
  var init = instance.init;
  var loadResources = instance.loadResources;
  var reloadResources = instance.reloadResources;
  var use = instance.use;
  var changeLanguage = instance.changeLanguage;
  var getFixedT = instance.getFixedT;
  var t = instance.t;
  var exists = instance.exists;
  var setDefaultNamespace = instance.setDefaultNamespace;
  var hasLoadedNamespace = instance.hasLoadedNamespace;
  var loadNamespaces = instance.loadNamespaces;
  var loadLanguages = instance.loadLanguages;

  // node_modules/react-i18next/dist/es/index.js
  init_define_import_meta_env();

  // node_modules/react-i18next/dist/es/Trans.js
  init_define_import_meta_env();
  var import_react3 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/TransWithoutContext.js
  init_define_import_meta_env();
  var import_react = __toESM(require_react_shim(), 1);

  // node_modules/html-parse-stringify/dist/html-parse-stringify.module.js
  init_define_import_meta_env();
  var import_void_elements = __toESM(require_void_elements());

  // node_modules/react-i18next/dist/es/utils.js
  init_define_import_meta_env();
  var warn = (i18n, code, msg, rest) => {
    const args = [msg, {
      code,
      ...rest || {}
    }];
    if (i18n?.services?.logger?.forward) {
      return i18n.services.logger.forward(args, "warn", "react-i18next::", true);
    }
    if (isString2(args[0])) args[0] = `react-i18next:: ${args[0]}`;
    if (i18n?.services?.logger?.warn) {
      i18n.services.logger.warn(...args);
    } else if (console?.warn) {
      console.warn(...args);
    }
  };
  var alreadyWarned = {};
  var warnOnce = (i18n, code, msg, rest) => {
    if (isString2(msg) && alreadyWarned[msg]) return;
    if (isString2(msg)) alreadyWarned[msg] = /* @__PURE__ */ new Date();
    warn(i18n, code, msg, rest);
  };
  var loadedClb = (i18n, cb) => () => {
    if (i18n.isInitialized) {
      cb();
    } else {
      const initialized = () => {
        setTimeout(() => {
          i18n.off("initialized", initialized);
        }, 0);
        cb();
      };
      i18n.on("initialized", initialized);
    }
  };
  var loadNamespaces2 = (i18n, ns, cb) => {
    i18n.loadNamespaces(ns, loadedClb(i18n, cb));
  };
  var loadLanguages2 = (i18n, lng, ns, cb) => {
    if (isString2(ns)) ns = [ns];
    if (i18n.options.preload && i18n.options.preload.indexOf(lng) > -1) return loadNamespaces2(i18n, ns, cb);
    ns.forEach((n) => {
      if (i18n.options.ns.indexOf(n) < 0) i18n.options.ns.push(n);
    });
    i18n.loadLanguages(lng, loadedClb(i18n, cb));
  };
  var hasLoadedNamespace2 = (ns, i18n, options3 = {}) => {
    if (!i18n.languages || !i18n.languages.length) {
      warnOnce(i18n, "NO_LANGUAGES", "i18n.languages were undefined or empty", {
        languages: i18n.languages
      });
      return true;
    }
    return i18n.hasLoadedNamespace(ns, {
      lng: options3.lng,
      precheck: (i18nInstance2, loadNotPending) => {
        if (options3.bindI18n && options3.bindI18n.indexOf("languageChanging") > -1 && i18nInstance2.services.backendConnector.backend && i18nInstance2.isLanguageChangingTo && !loadNotPending(i18nInstance2.isLanguageChangingTo, ns)) return false;
      }
    });
  };
  var isString2 = (obj) => typeof obj === "string";
  var isObject = (obj) => typeof obj === "object" && obj !== null;

  // node_modules/react-i18next/dist/es/defaults.js
  init_define_import_meta_env();

  // node_modules/react-i18next/dist/es/unescape.js
  init_define_import_meta_env();
  var matchHtmlEntity = /&(?:amp|#38|lt|#60|gt|#62|apos|#39|quot|#34|nbsp|#160|copy|#169|reg|#174|hellip|#8230|#x2F|#47);/g;
  var htmlEntities = {
    "&amp;": "&",
    "&#38;": "&",
    "&lt;": "<",
    "&#60;": "<",
    "&gt;": ">",
    "&#62;": ">",
    "&apos;": "'",
    "&#39;": "'",
    "&quot;": '"',
    "&#34;": '"',
    "&nbsp;": " ",
    "&#160;": " ",
    "&copy;": "\xA9",
    "&#169;": "\xA9",
    "&reg;": "\xAE",
    "&#174;": "\xAE",
    "&hellip;": "\u2026",
    "&#8230;": "\u2026",
    "&#x2F;": "/",
    "&#47;": "/"
  };
  var unescapeHtmlEntity = (m2) => htmlEntities[m2];
  var unescape = (text) => text.replace(matchHtmlEntity, unescapeHtmlEntity);

  // node_modules/react-i18next/dist/es/defaults.js
  var defaultOptions = {
    bindI18n: "languageChanged",
    bindI18nStore: "",
    transEmptyNodeValue: "",
    transSupportBasicHtmlNodes: true,
    transWrapTextNodes: "",
    transKeepBasicHtmlNodesFor: ["br", "strong", "i", "p"],
    useSuspense: true,
    unescape
  };
  var setDefaults = (options3 = {}) => {
    defaultOptions = {
      ...defaultOptions,
      ...options3
    };
  };
  var getDefaults = () => defaultOptions;

  // node_modules/react-i18next/dist/es/i18nInstance.js
  init_define_import_meta_env();
  var i18nInstance;
  var setI18n = (instance2) => {
    i18nInstance = instance2;
  };
  var getI18n = () => i18nInstance;

  // node_modules/react-i18next/dist/es/context.js
  init_define_import_meta_env();
  var import_react2 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/initReactI18next.js
  init_define_import_meta_env();
  var initReactI18next = {
    type: "3rdParty",
    init(instance2) {
      setDefaults(instance2.options.react);
      setI18n(instance2);
    }
  };

  // node_modules/react-i18next/dist/es/context.js
  var I18nContext = (0, import_react2.createContext)();
  var ReportNamespaces = class {
    constructor() {
      this.usedNamespaces = {};
    }
    addUsedNamespaces(namespaces) {
      namespaces.forEach((ns) => {
        if (!this.usedNamespaces[ns]) this.usedNamespaces[ns] = true;
      });
    }
    getUsedNamespaces() {
      return Object.keys(this.usedNamespaces);
    }
  };

  // node_modules/react-i18next/dist/es/useTranslation.js
  init_define_import_meta_env();
  var import_react4 = __toESM(require_react_shim(), 1);
  var usePrevious = (value, ignore) => {
    const ref = (0, import_react4.useRef)();
    (0, import_react4.useEffect)(() => {
      ref.current = ignore ? ref.current : value;
    }, [value, ignore]);
    return ref.current;
  };
  var alwaysNewT = (i18n, language, namespace, keyPrefix) => i18n.getFixedT(language, namespace, keyPrefix);
  var useMemoizedT = (i18n, language, namespace, keyPrefix) => (0, import_react4.useCallback)(alwaysNewT(i18n, language, namespace, keyPrefix), [i18n, language, namespace, keyPrefix]);
  var useTranslation = (ns, props = {}) => {
    const {
      i18n: i18nFromProps
    } = props;
    const {
      i18n: i18nFromContext,
      defaultNS: defaultNSFromContext
    } = (0, import_react4.useContext)(I18nContext) || {};
    const i18n = i18nFromProps || i18nFromContext || getI18n();
    if (i18n && !i18n.reportNamespaces) i18n.reportNamespaces = new ReportNamespaces();
    if (!i18n) {
      warnOnce(i18n, "NO_I18NEXT_INSTANCE", "useTranslation: You will need to pass in an i18next instance by using initReactI18next");
      const notReadyT = (k3, optsOrDefaultValue) => {
        if (isString2(optsOrDefaultValue)) return optsOrDefaultValue;
        if (isObject(optsOrDefaultValue) && isString2(optsOrDefaultValue.defaultValue)) return optsOrDefaultValue.defaultValue;
        return Array.isArray(k3) ? k3[k3.length - 1] : k3;
      };
      const retNotReady = [notReadyT, {}, false];
      retNotReady.t = notReadyT;
      retNotReady.i18n = {};
      retNotReady.ready = false;
      return retNotReady;
    }
    if (i18n.options.react?.wait) warnOnce(i18n, "DEPRECATED_OPTION", "useTranslation: It seems you are still using the old wait option, you may migrate to the new useSuspense behaviour.");
    const i18nOptions = {
      ...getDefaults(),
      ...i18n.options.react,
      ...props
    };
    const {
      useSuspense,
      keyPrefix
    } = i18nOptions;
    let namespaces = ns || defaultNSFromContext || i18n.options?.defaultNS;
    namespaces = isString2(namespaces) ? [namespaces] : namespaces || ["translation"];
    i18n.reportNamespaces.addUsedNamespaces?.(namespaces);
    const ready = (i18n.isInitialized || i18n.initializedStoreOnce) && namespaces.every((n) => hasLoadedNamespace2(n, i18n, i18nOptions));
    const memoGetT = useMemoizedT(i18n, props.lng || null, i18nOptions.nsMode === "fallback" ? namespaces : namespaces[0], keyPrefix);
    const getT = () => memoGetT;
    const getNewT = () => alwaysNewT(i18n, props.lng || null, i18nOptions.nsMode === "fallback" ? namespaces : namespaces[0], keyPrefix);
    const [t3, setT] = (0, import_react4.useState)(getT);
    let joinedNS = namespaces.join();
    if (props.lng) joinedNS = `${props.lng}${joinedNS}`;
    const previousJoinedNS = usePrevious(joinedNS);
    const isMounted = (0, import_react4.useRef)(true);
    (0, import_react4.useEffect)(() => {
      const {
        bindI18n,
        bindI18nStore
      } = i18nOptions;
      isMounted.current = true;
      if (!ready && !useSuspense) {
        if (props.lng) {
          loadLanguages2(i18n, props.lng, namespaces, () => {
            if (isMounted.current) setT(getNewT);
          });
        } else {
          loadNamespaces2(i18n, namespaces, () => {
            if (isMounted.current) setT(getNewT);
          });
        }
      }
      if (ready && previousJoinedNS && previousJoinedNS !== joinedNS && isMounted.current) {
        setT(getNewT);
      }
      const boundReset = () => {
        if (isMounted.current) setT(getNewT);
      };
      if (bindI18n) i18n?.on(bindI18n, boundReset);
      if (bindI18nStore) i18n?.store.on(bindI18nStore, boundReset);
      return () => {
        isMounted.current = false;
        if (i18n && bindI18n) bindI18n?.split(" ").forEach((e2) => i18n.off(e2, boundReset));
        if (bindI18nStore && i18n) bindI18nStore.split(" ").forEach((e2) => i18n.store.off(e2, boundReset));
      };
    }, [i18n, joinedNS]);
    (0, import_react4.useEffect)(() => {
      if (isMounted.current && ready) {
        setT(getT);
      }
    }, [i18n, keyPrefix, ready]);
    const ret = [t3, i18n, ready];
    ret.t = t3;
    ret.i18n = i18n;
    ret.ready = ready;
    if (ready) return ret;
    if (!ready && !useSuspense) return ret;
    throw new Promise((resolve) => {
      if (props.lng) {
        loadLanguages2(i18n, props.lng, namespaces, () => resolve());
      } else {
        loadNamespaces2(i18n, namespaces, () => resolve());
      }
    });
  };

  // node_modules/react-i18next/dist/es/withTranslation.js
  init_define_import_meta_env();
  var import_react5 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/Translation.js
  init_define_import_meta_env();

  // node_modules/react-i18next/dist/es/I18nextProvider.js
  init_define_import_meta_env();
  var import_react6 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/withSSR.js
  init_define_import_meta_env();
  var import_react8 = __toESM(require_react_shim(), 1);

  // node_modules/react-i18next/dist/es/useSSR.js
  init_define_import_meta_env();
  var import_react7 = __toESM(require_react_shim(), 1);

  // node_modules/i18next-browser-languagedetector/dist/esm/i18nextBrowserLanguageDetector.js
  init_define_import_meta_env();
  var {
    slice,
    forEach
  } = [];
  function defaults(obj) {
    forEach.call(slice.call(arguments, 1), (source) => {
      if (source) {
        for (const prop in source) {
          if (obj[prop] === void 0) obj[prop] = source[prop];
        }
      }
    });
    return obj;
  }
  function hasXSS(input) {
    if (typeof input !== "string") return false;
    const xssPatterns = [/<\s*script.*?>/i, /<\s*\/\s*script\s*>/i, /<\s*img.*?on\w+\s*=/i, /<\s*\w+\s*on\w+\s*=.*?>/i, /javascript\s*:/i, /vbscript\s*:/i, /expression\s*\(/i, /eval\s*\(/i, /alert\s*\(/i, /document\.cookie/i, /document\.write\s*\(/i, /window\.location/i, /innerHTML/i];
    return xssPatterns.some((pattern) => pattern.test(input));
  }
  var fieldContentRegExp = /^[\u0009\u0020-\u007e\u0080-\u00ff]+$/;
  var serializeCookie = function(name, val) {
    let options3 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {
      path: "/"
    };
    const opt = options3;
    const value = encodeURIComponent(val);
    let str = `${name}=${value}`;
    if (opt.maxAge > 0) {
      const maxAge = opt.maxAge - 0;
      if (Number.isNaN(maxAge)) throw new Error("maxAge should be a Number");
      str += `; Max-Age=${Math.floor(maxAge)}`;
    }
    if (opt.domain) {
      if (!fieldContentRegExp.test(opt.domain)) {
        throw new TypeError("option domain is invalid");
      }
      str += `; Domain=${opt.domain}`;
    }
    if (opt.path) {
      if (!fieldContentRegExp.test(opt.path)) {
        throw new TypeError("option path is invalid");
      }
      str += `; Path=${opt.path}`;
    }
    if (opt.expires) {
      if (typeof opt.expires.toUTCString !== "function") {
        throw new TypeError("option expires is invalid");
      }
      str += `; Expires=${opt.expires.toUTCString()}`;
    }
    if (opt.httpOnly) str += "; HttpOnly";
    if (opt.secure) str += "; Secure";
    if (opt.sameSite) {
      const sameSite = typeof opt.sameSite === "string" ? opt.sameSite.toLowerCase() : opt.sameSite;
      switch (sameSite) {
        case true:
          str += "; SameSite=Strict";
          break;
        case "lax":
          str += "; SameSite=Lax";
          break;
        case "strict":
          str += "; SameSite=Strict";
          break;
        case "none":
          str += "; SameSite=None";
          break;
        default:
          throw new TypeError("option sameSite is invalid");
      }
    }
    if (opt.partitioned) str += "; Partitioned";
    return str;
  };
  var cookie = {
    create(name, value, minutes, domain) {
      let cookieOptions = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : {
        path: "/",
        sameSite: "strict"
      };
      if (minutes) {
        cookieOptions.expires = /* @__PURE__ */ new Date();
        cookieOptions.expires.setTime(cookieOptions.expires.getTime() + minutes * 60 * 1e3);
      }
      if (domain) cookieOptions.domain = domain;
      document.cookie = serializeCookie(name, value, cookieOptions);
    },
    read(name) {
      const nameEQ = `${name}=`;
      const ca = document.cookie.split(";");
      for (let i = 0; i < ca.length; i++) {
        let c = ca[i];
        while (c.charAt(0) === " ") c = c.substring(1, c.length);
        if (c.indexOf(nameEQ) === 0) return c.substring(nameEQ.length, c.length);
      }
      return null;
    },
    remove(name, domain) {
      this.create(name, "", -1, domain);
    }
  };
  var cookie$1 = {
    name: "cookie",
    // Deconstruct the options object and extract the lookupCookie property
    lookup(_ref) {
      let {
        lookupCookie
      } = _ref;
      if (lookupCookie && typeof document !== "undefined") {
        return cookie.read(lookupCookie) || void 0;
      }
      return void 0;
    },
    // Deconstruct the options object and extract the lookupCookie, cookieMinutes, cookieDomain, and cookieOptions properties
    cacheUserLanguage(lng, _ref2) {
      let {
        lookupCookie,
        cookieMinutes,
        cookieDomain,
        cookieOptions
      } = _ref2;
      if (lookupCookie && typeof document !== "undefined") {
        cookie.create(lookupCookie, lng, cookieMinutes, cookieDomain, cookieOptions);
      }
    }
  };
  var querystring = {
    name: "querystring",
    // Deconstruct the options object and extract the lookupQuerystring property
    lookup(_ref) {
      let {
        lookupQuerystring
      } = _ref;
      let found;
      if (typeof window !== "undefined") {
        let {
          search
        } = window.location;
        if (!window.location.search && window.location.hash?.indexOf("?") > -1) {
          search = window.location.hash.substring(window.location.hash.indexOf("?"));
        }
        const query = search.substring(1);
        const params = query.split("&");
        for (let i = 0; i < params.length; i++) {
          const pos = params[i].indexOf("=");
          if (pos > 0) {
            const key = params[i].substring(0, pos);
            if (key === lookupQuerystring) {
              found = params[i].substring(pos + 1);
            }
          }
        }
      }
      return found;
    }
  };
  var hash = {
    name: "hash",
    // Deconstruct the options object and extract the lookupHash property and the lookupFromHashIndex property
    lookup(_ref) {
      let {
        lookupHash,
        lookupFromHashIndex
      } = _ref;
      let found;
      if (typeof window !== "undefined") {
        const {
          hash: hash2
        } = window.location;
        if (hash2 && hash2.length > 2) {
          const query = hash2.substring(1);
          if (lookupHash) {
            const params = query.split("&");
            for (let i = 0; i < params.length; i++) {
              const pos = params[i].indexOf("=");
              if (pos > 0) {
                const key = params[i].substring(0, pos);
                if (key === lookupHash) {
                  found = params[i].substring(pos + 1);
                }
              }
            }
          }
          if (found) return found;
          if (!found && lookupFromHashIndex > -1) {
            const language = hash2.match(/\/([a-zA-Z-]*)/g);
            if (!Array.isArray(language)) return void 0;
            const index2 = typeof lookupFromHashIndex === "number" ? lookupFromHashIndex : 0;
            return language[index2]?.replace("/", "");
          }
        }
      }
      return found;
    }
  };
  var hasLocalStorageSupport = null;
  var localStorageAvailable = () => {
    if (hasLocalStorageSupport !== null) return hasLocalStorageSupport;
    try {
      hasLocalStorageSupport = typeof window !== "undefined" && window.localStorage !== null;
      if (!hasLocalStorageSupport) {
        return false;
      }
      const testKey = "i18next.translate.boo";
      window.localStorage.setItem(testKey, "foo");
      window.localStorage.removeItem(testKey);
    } catch (e2) {
      hasLocalStorageSupport = false;
    }
    return hasLocalStorageSupport;
  };
  var localStorage2 = {
    name: "localStorage",
    // Deconstruct the options object and extract the lookupLocalStorage property
    lookup(_ref) {
      let {
        lookupLocalStorage
      } = _ref;
      if (lookupLocalStorage && localStorageAvailable()) {
        return window.localStorage.getItem(lookupLocalStorage) || void 0;
      }
      return void 0;
    },
    // Deconstruct the options object and extract the lookupLocalStorage property
    cacheUserLanguage(lng, _ref2) {
      let {
        lookupLocalStorage
      } = _ref2;
      if (lookupLocalStorage && localStorageAvailable()) {
        window.localStorage.setItem(lookupLocalStorage, lng);
      }
    }
  };
  var hasSessionStorageSupport = null;
  var sessionStorageAvailable = () => {
    if (hasSessionStorageSupport !== null) return hasSessionStorageSupport;
    try {
      hasSessionStorageSupport = typeof window !== "undefined" && window.sessionStorage !== null;
      if (!hasSessionStorageSupport) {
        return false;
      }
      const testKey = "i18next.translate.boo";
      window.sessionStorage.setItem(testKey, "foo");
      window.sessionStorage.removeItem(testKey);
    } catch (e2) {
      hasSessionStorageSupport = false;
    }
    return hasSessionStorageSupport;
  };
  var sessionStorage2 = {
    name: "sessionStorage",
    lookup(_ref) {
      let {
        lookupSessionStorage
      } = _ref;
      if (lookupSessionStorage && sessionStorageAvailable()) {
        return window.sessionStorage.getItem(lookupSessionStorage) || void 0;
      }
      return void 0;
    },
    cacheUserLanguage(lng, _ref2) {
      let {
        lookupSessionStorage
      } = _ref2;
      if (lookupSessionStorage && sessionStorageAvailable()) {
        window.sessionStorage.setItem(lookupSessionStorage, lng);
      }
    }
  };
  var navigator$1 = {
    name: "navigator",
    lookup(options3) {
      const found = [];
      if (typeof navigator !== "undefined") {
        const {
          languages,
          userLanguage,
          language
        } = navigator;
        if (languages) {
          for (let i = 0; i < languages.length; i++) {
            found.push(languages[i]);
          }
        }
        if (userLanguage) {
          found.push(userLanguage);
        }
        if (language) {
          found.push(language);
        }
      }
      return found.length > 0 ? found : void 0;
    }
  };
  var htmlTag = {
    name: "htmlTag",
    // Deconstruct the options object and extract the htmlTag property
    lookup(_ref) {
      let {
        htmlTag: htmlTag2
      } = _ref;
      let found;
      const internalHtmlTag = htmlTag2 || (typeof document !== "undefined" ? document.documentElement : null);
      if (internalHtmlTag && typeof internalHtmlTag.getAttribute === "function") {
        found = internalHtmlTag.getAttribute("lang");
      }
      return found;
    }
  };
  var path = {
    name: "path",
    // Deconstruct the options object and extract the lookupFromPathIndex property
    lookup(_ref) {
      let {
        lookupFromPathIndex
      } = _ref;
      if (typeof window === "undefined") return void 0;
      const language = window.location.pathname.match(/\/([a-zA-Z-]*)/g);
      if (!Array.isArray(language)) return void 0;
      const index2 = typeof lookupFromPathIndex === "number" ? lookupFromPathIndex : 0;
      return language[index2]?.replace("/", "");
    }
  };
  var subdomain = {
    name: "subdomain",
    lookup(_ref) {
      let {
        lookupFromSubdomainIndex
      } = _ref;
      const internalLookupFromSubdomainIndex = typeof lookupFromSubdomainIndex === "number" ? lookupFromSubdomainIndex + 1 : 1;
      const language = typeof window !== "undefined" && window.location?.hostname?.match(/^(\w{2,5})\.(([a-z0-9-]{1,63}\.[a-z]{2,6})|localhost)/i);
      if (!language) return void 0;
      return language[internalLookupFromSubdomainIndex];
    }
  };
  var canCookies = false;
  try {
    document.cookie;
    canCookies = true;
  } catch (e2) {
  }
  var order = ["querystring", "cookie", "localStorage", "sessionStorage", "navigator", "htmlTag"];
  if (!canCookies) order.splice(1, 1);
  var getDefaults2 = () => ({
    order,
    lookupQuerystring: "lng",
    lookupCookie: "i18next",
    lookupLocalStorage: "i18nextLng",
    lookupSessionStorage: "i18nextLng",
    // cache user language
    caches: ["localStorage"],
    excludeCacheFor: ["cimode"],
    // cookieMinutes: 10,
    // cookieDomain: 'myDomain'
    convertDetectedLanguage: (l) => l
  });
  var Browser = class {
    constructor(services) {
      let options3 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
      this.type = "languageDetector";
      this.detectors = {};
      this.init(services, options3);
    }
    init() {
      let services = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {
        languageUtils: {}
      };
      let options3 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
      let i18nOptions = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
      this.services = services;
      this.options = defaults(options3, this.options || {}, getDefaults2());
      if (typeof this.options.convertDetectedLanguage === "string" && this.options.convertDetectedLanguage.indexOf("15897") > -1) {
        this.options.convertDetectedLanguage = (l) => l.replace("-", "_");
      }
      if (this.options.lookupFromUrlIndex) this.options.lookupFromPathIndex = this.options.lookupFromUrlIndex;
      this.i18nOptions = i18nOptions;
      this.addDetector(cookie$1);
      this.addDetector(querystring);
      this.addDetector(localStorage2);
      this.addDetector(sessionStorage2);
      this.addDetector(navigator$1);
      this.addDetector(htmlTag);
      this.addDetector(path);
      this.addDetector(subdomain);
      this.addDetector(hash);
    }
    addDetector(detector) {
      this.detectors[detector.name] = detector;
      return this;
    }
    detect() {
      let detectionOrder = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.options.order;
      let detected = [];
      detectionOrder.forEach((detectorName) => {
        if (this.detectors[detectorName]) {
          let lookup = this.detectors[detectorName].lookup(this.options);
          if (lookup && typeof lookup === "string") lookup = [lookup];
          if (lookup) detected = detected.concat(lookup);
        }
      });
      detected = detected.filter((d) => d !== void 0 && d !== null && !hasXSS(d)).map((d) => this.options.convertDetectedLanguage(d));
      if (this.services && this.services.languageUtils && this.services.languageUtils.getBestMatchFromCodes) return detected;
      return detected.length > 0 ? detected[0] : null;
    }
    cacheUserLanguage(lng) {
      let caches = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.options.caches;
      if (!caches) return;
      if (this.options.excludeCacheFor && this.options.excludeCacheFor.indexOf(lng) > -1) return;
      caches.forEach((cacheName) => {
        if (this.detectors[cacheName]) this.detectors[cacheName].cacheUserLanguage(lng, this.options);
      });
    }
  };
  Browser.type = "languageDetector";

  // src/lib/constants/index.ts
  init_define_import_meta_env();

  // src/lib/constants/StorageKeys.ts
  init_define_import_meta_env();

  // src/lib/constants/Language.ts
  init_define_import_meta_env();
  var LANGUAGE_OPTIONS = [
    { code: "en" /* EN */, name: "English", dir: "ltr", flag: "\u{1F1EC}\u{1F1E7}" },
    { code: "ar" /* AR */, name: "\u0627\u0644\u0639\u0631\u0628\u064A\u0629", dir: "rtl", flag: "\u{1F1EF}\u{1F1F4}" }
  ];
  var DEFAULT_LANGUAGE = "en" /* EN */;

  // .design-sync/stubs/locales.ts
  init_define_import_meta_env();

  // src/locales/en/auth.json
  var auth_default = {
    login: {
      title: "Login"
    }
  };

  // src/locales/en/common.json
  var common_default = {
    save: "Save",
    cancel: "Cancel",
    delete: "Delete",
    edit: "Edit",
    create: "Create",
    confirm: "Confirm",
    close: "Close",
    back: "Back",
    next: "Next",
    previous: "Previous",
    search: "Search",
    searchHere: "Search Here",
    loading: "Loading\u2026",
    noResults: "No results found.",
    clearFilters: "Clear filters",
    clearAll: "Clear All",
    filter: "Filter",
    selectedCount: "{{count}} selected",
    yes: "Yes",
    no: "No",
    actions: "Actions",
    goBack: "Go back",
    download: "Download",
    clear: "Clear",
    unsavedChanges: "Unsaved Changes",
    searchPlaceholder: "Search\u2026",
    breadcrumbLabel: "breadcrumb",
    empty: {
      title: "No Results Found",
      description: "No results found for your search. Please try again with different keywords."
    },
    confirmDialog: {
      confirm: "Confirm",
      cancel: "Cancel"
    },
    breadcrumb: {
      root: "Dashboard"
    },
    table: {
      total: "Total: {{count}} {{label}}",
      rowsPerPage: "Rows per page",
      emptyTitle: "No Results Found",
      emptyDescription: "No results found for your search. Please try again with different keywords."
    },
    pagination: {
      previous: "Previous",
      next: "Next",
      rowsPerPage: "Rows per page",
      pageOf: "Page {{current}} of {{total}}"
    }
  };

  // src/locales/en/components.json
  var components_default = {
    gallery: {
      title: "Components Gallery",
      subtitle: "Browse and explore components with live demos and usage examples."
    },
    detail: {
      notFoundTitle: "Component not found",
      notFoundDescription: 'No component with id "{{id}}" exists in the registry.',
      backToGallery: "Back to Gallery",
      breadcrumbComponents: "Components",
      mdxValidationIssues: "MDX docs validation issues",
      loadingDocumentation: "Loading documentation..."
    }
  };

  // src/locales/en/dashboard.json
  var dashboard_default = {
    title: "Dashboard",
    subtitle: "Welcome back, here's what's happening.",
    stats: {
      totalUsers: "Total Users",
      revenue: "Revenue",
      activeSessions: "Active Sessions",
      conversion: "Conversion"
    },
    activity: {
      createdProject: "created a new project",
      commentedDesignReview: "commented on Design Review",
      mergedPullRequest: "merged pull request #142",
      updatedBilling: "updated billing settings",
      invitedMembers: "invited 3 team members"
    },
    time: {
      minutesAgo_one: "{{count}}m ago",
      minutesAgo_other: "{{count}}m ago",
      hoursAgo_one: "{{count}}h ago",
      hoursAgo_other: "{{count}}h ago"
    },
    revenueOverview: "Revenue overview",
    lastTwelveMonths: "Last 12 months",
    recentActivity: "Recent activity",
    revenueTrend: "Revenue trend"
  };

  // src/locales/en/errors.json
  var errors_default = {
    boundary: {
      title: "Something went wrong",
      description: "We hit an unexpected error. Your data is safe \u2014 try one of the options below.",
      refresh: "Refresh",
      goBack: "Go Back",
      home: "Home"
    },
    notFound: {
      title: "Page not found",
      description: "The page you're looking for doesn't exist or has moved.",
      backHome: "Back to home"
    },
    validation: {
      required: "This field is required",
      email: "Please enter a valid email address",
      min: "Must be at least {{count}} characters",
      max: "Must be at most {{count}} characters"
    }
  };

  // src/locales/en/header.json
  var header_default = {
    searchPlaceholder: "Search or jump to\u2026",
    openCommandPalette: "Open command palette",
    notifications: "Notifications",
    openUserMenu: "Open user menu",
    myProfile: "My profile",
    settings: "Settings",
    logout: "Log out",
    switchLanguage: "Switch language",
    switchToLight: "Switch to light theme",
    switchToDark: "Switch to dark theme",
    toggleTheme: "Toggle theme"
  };

  // src/locales/en/home.json
  var home_default = {
    nav: {
      brand: "RSK",
      logoAlt: "RSK logo",
      features: "Features",
      components: "Components",
      github: "GitHub"
    },
    hero: {
      eyebrow: "React \xB7 TypeScript \xB7 Vite",
      titleLead: "The foundation for what's ",
      titleEmphasis: "next",
      titleTrail: ".",
      subtitle: "A considered starter for developers who value craft. 76 components, enterprise patterns, and zero compromise \u2014 wired together so you can ship on day one.",
      ctaPrimary: "Get Started",
      ctaSecondary: "Browse Components"
    },
    stats: {
      uiComponents: "UI Components",
      formComponents: "Form Components",
      mdxDocs: "MDX Docs",
      sharedComponents: "Shared Components"
    },
    techStack: {
      builtOn: "Built on"
    },
    features: {
      eyebrow: "What's inside",
      title: "Everything wired, nothing in your way.",
      componentLibrary: {
        title: "Component Library",
        desc: "35 Radix UI primitives with CVA variants. Compound component patterns \u2014 Dialog.Header, Tabs.List, Card.Content. Dark mode ready. Fully typed with TypeScript generics."
      },
      formSystem: {
        title: "Form System",
        desc: "17 form components powered by React Hook Form and Zod. Input masks, number formatting, date pickers, file uploaders, and async comboboxes with infinite scroll."
      },
      dataTables: {
        title: "Data Tables",
        desc: "TanStack Table with headless sorting, filtering, and pagination. Server-side query integration via custom hooks. Faceted filters out of the box."
      },
      authentication: {
        title: "Authentication",
        desc: "OIDC via oidc-client-ts with Azure AD support. AuthGuard layout, route-level protection, and withAuth HOC for component-level access control."
      },
      internationalization: {
        title: "Internationalization",
        desc: "i18next with browser language detection. Namespace-organized translations, RTL layout support, and lazy-loaded locale bundles."
      },
      errorMonitoring: {
        title: "Error Monitoring",
        desc: "Sentry integration with Vite source maps. React error boundaries with graceful fallback UI. Structured error tracking from day one."
      }
    },
    showcase: {
      eyebrow: "Documented",
      title: "See exactly what you get.",
      desc: "79 MDX-powered documentation pages with live previews, code snippets, and usage guidelines. Browse the full gallery to explore every component.",
      cta: "Open Gallery",
      browserUrl: "localhost:3000/components",
      searchPlaceholder: "Search\u2026",
      buttonTitle: "Button",
      buttonDesc: "Displays a button or a component that looks like a button.",
      variantDefault: "Default",
      variantOutline: "Outline",
      variantGhost: "Ghost"
    },
    cli: {
      eyebrow: "Tooling",
      title: "Generate, don't write.",
      desc: "A 7-step pipeline fetches your OpenAPI spec, parses schemas, generates fully-typed TypeScript DTOs, formats with Prettier, and writes to disk. One command replaces hours of manual typing.",
      logFetching: "Fetching OpenAPI spec\u2026",
      logParsing: "Parsing 42 schemas",
      logGenerating: "Generating TypeScript DTOs",
      logFormatting: "Formatting with Prettier",
      logWritten: "Written 42 files"
    },
    architecture: {
      eyebrow: "Structure",
      title: "Organized by design.",
      desc: "15 path aliases keep imports clean and refactoring painless. Every folder has a purpose, every alias has a convention. No guessing where things live."
    },
    gettingStarted: {
      eyebrow: "Get going",
      title: "Up and running in seconds.",
      clone: "Clone",
      install: "Install",
      start: "Start"
    },
    footer: {
      builtBy: "Built by Mohammad Safia",
      github: "GitHub"
    }
  };

  // src/locales/en/members.json
  var members_default = {
    title: "Members",
    addMember: "Add Member",
    totalLabel: "members",
    searchPlaceholder: "Search members\u2026",
    columns: {
      name: "Name",
      email: "Email",
      role: "Role",
      status: "Status",
      joined: "Joined",
      lastSeen: "Last seen",
      shiftStart: "Shift start"
    },
    roles: {
      Admin: "Admin",
      Editor: "Editor",
      Viewer: "Viewer"
    },
    statuses: {
      Active: "Active",
      Invited: "Invited",
      Suspended: "Suspended"
    },
    emptyState: {
      title: "No members found",
      description: "No members match your search. Try adjusting your filters or keywords."
    }
  };

  // src/locales/en/nav.json
  var nav_default = {
    dashboard: "Dashboard",
    components: "Components",
    members: "Members",
    settings: "Settings",
    commandPalette: "Command palette",
    navigation: "Navigation",
    actions: "Actions",
    toggleTheme: "Toggle theme",
    expandSidebar: "Expand sidebar",
    collapseSidebar: "Collapse sidebar",
    openMenu: "Open menu",
    closeMenu: "Close menu",
    groups: {
      main: "Main",
      account: "Account"
    }
  };

  // src/locales/ar/auth.json
  var auth_default2 = {
    login: {
      title: "\u062A\u0633\u062C\u064A\u0644 \u0627\u0644\u062F\u062E\u0648\u0644"
    }
  };

  // src/locales/ar/common.json
  var common_default2 = {
    save: "\u062D\u0641\u0638",
    cancel: "\u0625\u0644\u063A\u0627\u0621",
    delete: "\u062D\u0630\u0641",
    edit: "\u062A\u0639\u062F\u064A\u0644",
    create: "\u0625\u0646\u0634\u0627\u0621",
    confirm: "\u062A\u0623\u0643\u064A\u062F",
    close: "\u0625\u063A\u0644\u0627\u0642",
    back: "\u0631\u062C\u0648\u0639",
    next: "\u0627\u0644\u062A\u0627\u0644\u064A",
    previous: "\u0627\u0644\u0633\u0627\u0628\u0642",
    search: "\u0628\u062D\u062B",
    searchHere: "\u0627\u0628\u062D\u062B \u0647\u0646\u0627",
    loading: "\u062C\u0627\u0631\u064D \u0627\u0644\u062A\u062D\u0645\u064A\u0644\u2026",
    noResults: "\u0644\u0627 \u062A\u0648\u062C\u062F \u0646\u062A\u0627\u0626\u062C.",
    clearFilters: "\u0645\u0633\u062D \u0639\u0648\u0627\u0645\u0644 \u0627\u0644\u062A\u0635\u0641\u064A\u0629",
    clearAll: "\u0645\u0633\u062D \u0627\u0644\u0643\u0644",
    filter: "\u062A\u0635\u0641\u064A\u0629",
    selectedCount: "{{count}} \u0645\u062D\u062F\u062F",
    yes: "\u0646\u0639\u0645",
    no: "\u0644\u0627",
    actions: "\u0625\u062C\u0631\u0627\u0621\u0627\u062A",
    goBack: "\u0631\u062C\u0648\u0639",
    download: "\u062A\u0646\u0632\u064A\u0644",
    clear: "\u0645\u0633\u062D",
    unsavedChanges: "\u062A\u063A\u064A\u064A\u0631\u0627\u062A \u063A\u064A\u0631 \u0645\u062D\u0641\u0648\u0638\u0629",
    searchPlaceholder: "\u0628\u062D\u062B\u2026",
    breadcrumbLabel: "\u0645\u0633\u0627\u0631 \u0627\u0644\u062A\u0646\u0642\u0644",
    empty: {
      title: "\u0644\u0627 \u062A\u0648\u062C\u062F \u0646\u062A\u0627\u0626\u062C",
      description: "\u0644\u0627 \u062A\u0648\u062C\u062F \u0646\u062A\u0627\u0626\u062C \u0644\u0628\u062D\u062B\u0643. \u064A\u0631\u062C\u0649 \u0627\u0644\u0645\u062D\u0627\u0648\u0644\u0629 \u0645\u0631\u0629 \u0623\u062E\u0631\u0649 \u0628\u0643\u0644\u0645\u0627\u062A \u0645\u062E\u062A\u0644\u0641\u0629."
    },
    confirmDialog: {
      confirm: "\u062A\u0623\u0643\u064A\u062F",
      cancel: "\u0625\u0644\u063A\u0627\u0621"
    },
    breadcrumb: {
      root: "\u0644\u0648\u062D\u0629 \u0627\u0644\u062A\u062D\u0643\u0645"
    },
    table: {
      total: "\u0627\u0644\u0625\u062C\u0645\u0627\u0644\u064A: {{count}} {{label}}",
      rowsPerPage: "\u0635\u0641\u0648\u0641 \u0644\u0643\u0644 \u0635\u0641\u062D\u0629",
      emptyTitle: "\u0644\u0627 \u062A\u0648\u062C\u062F \u0646\u062A\u0627\u0626\u062C",
      emptyDescription: "\u0644\u0627 \u062A\u0648\u062C\u062F \u0646\u062A\u0627\u0626\u062C \u0644\u0628\u062D\u062B\u0643. \u064A\u0631\u062C\u0649 \u0627\u0644\u0645\u062D\u0627\u0648\u0644\u0629 \u0645\u0631\u0629 \u0623\u062E\u0631\u0649 \u0628\u0643\u0644\u0645\u0627\u062A \u0645\u062E\u062A\u0644\u0641\u0629."
    },
    pagination: {
      previous: "\u0627\u0644\u0633\u0627\u0628\u0642",
      next: "\u0627\u0644\u062A\u0627\u0644\u064A",
      rowsPerPage: "\u0635\u0641\u0648\u0641 \u0644\u0643\u0644 \u0635\u0641\u062D\u0629",
      pageOf: "\u0635\u0641\u062D\u0629 {{current}} \u0645\u0646 {{total}}"
    }
  };

  // src/locales/ar/components.json
  var components_default2 = {
    gallery: {
      title: "\u0645\u0639\u0631\u0636 \u0627\u0644\u0645\u0643\u0648\u0646\u0627\u062A",
      subtitle: "\u062A\u0635\u0641\u062D \u0648\u0627\u0633\u062A\u0643\u0634\u0641 \u0627\u0644\u0645\u0643\u0648\u0646\u0627\u062A \u0645\u0639 \u0639\u0631\u0648\u0636 \u062D\u064A\u0629 \u0648\u0623\u0645\u062B\u0644\u0629 \u0627\u0633\u062A\u062E\u062F\u0627\u0645."
    },
    detail: {
      notFoundTitle: "\u0627\u0644\u0645\u0643\u0648\u0646 \u063A\u064A\u0631 \u0645\u0648\u062C\u0648\u062F",
      notFoundDescription: '\u0644\u0627 \u064A\u0648\u062C\u062F \u0645\u0643\u0648\u0646 \u0628\u0627\u0644\u0645\u0639\u0631\u0651\u0641 "{{id}}" \u0641\u064A \u0627\u0644\u0633\u062C\u0644.',
      backToGallery: "\u0627\u0644\u0639\u0648\u062F\u0629 \u0625\u0644\u0649 \u0627\u0644\u0645\u0639\u0631\u0636",
      breadcrumbComponents: "\u0627\u0644\u0645\u0643\u0648\u0646\u0627\u062A",
      mdxValidationIssues: "\u0645\u0634\u0643\u0644\u0627\u062A \u0627\u0644\u062A\u062D\u0642\u0642 \u0645\u0646 \u0648\u062B\u0627\u0626\u0642 MDX",
      loadingDocumentation: "\u062C\u0627\u0631\u064D \u062A\u062D\u0645\u064A\u0644 \u0627\u0644\u0648\u062B\u0627\u0626\u0642..."
    }
  };

  // src/locales/ar/dashboard.json
  var dashboard_default2 = {
    title: "\u0644\u0648\u062D\u0629 \u0627\u0644\u062A\u062D\u0643\u0645",
    subtitle: "\u0645\u0631\u062D\u0628\u064B\u0627 \u0628\u0639\u0648\u062F\u062A\u0643\u060C \u0625\u0644\u064A\u0643 \u0622\u062E\u0631 \u0627\u0644\u0645\u0633\u062A\u062C\u062F\u0627\u062A.",
    stats: {
      totalUsers: "\u0625\u062C\u0645\u0627\u0644\u064A \u0627\u0644\u0645\u0633\u062A\u062E\u062F\u0645\u064A\u0646",
      revenue: "\u0627\u0644\u0625\u064A\u0631\u0627\u062F\u0627\u062A",
      activeSessions: "\u0627\u0644\u062C\u0644\u0633\u0627\u062A \u0627\u0644\u0646\u0634\u0637\u0629",
      conversion: "\u0645\u0639\u062F\u0644 \u0627\u0644\u062A\u062D\u0648\u064A\u0644"
    },
    activity: {
      createdProject: "\u0623\u0646\u0634\u0623 \u0645\u0634\u0631\u0648\u0639\u064B\u0627 \u062C\u062F\u064A\u062F\u064B\u0627",
      commentedDesignReview: "\u0639\u0644\u0651\u0642 \u0639\u0644\u0649 \u0645\u0631\u0627\u062C\u0639\u0629 \u0627\u0644\u062A\u0635\u0645\u064A\u0645",
      mergedPullRequest: "\u062F\u0645\u062C \u0637\u0644\u0628 \u0627\u0644\u0633\u062D\u0628 \u0631\u0642\u0645 142",
      updatedBilling: "\u062D\u062F\u0651\u062B \u0625\u0639\u062F\u0627\u062F\u0627\u062A \u0627\u0644\u0641\u0648\u062A\u0631\u0629",
      invitedMembers: "\u062F\u0639\u0627 3 \u0623\u0639\u0636\u0627\u0621 \u0625\u0644\u0649 \u0627\u0644\u0641\u0631\u064A\u0642"
    },
    time: {
      minutesAgo_zero: "\u0642\u0628\u0644 {{count}} \u062F\u0642\u064A\u0642\u0629",
      minutesAgo_one: "\u0642\u0628\u0644 {{count}} \u062F\u0642\u064A\u0642\u0629",
      minutesAgo_two: "\u0642\u0628\u0644 {{count}} \u062F\u0642\u064A\u0642\u062A\u064A\u0646",
      minutesAgo_few: "\u0642\u0628\u0644 {{count}} \u062F\u0642\u0627\u0626\u0642",
      minutesAgo_many: "\u0642\u0628\u0644 {{count}} \u062F\u0642\u064A\u0642\u0629",
      minutesAgo_other: "\u0642\u0628\u0644 {{count}} \u062F\u0642\u064A\u0642\u0629",
      hoursAgo_zero: "\u0642\u0628\u0644 {{count}} \u0633\u0627\u0639\u0629",
      hoursAgo_one: "\u0642\u0628\u0644 {{count}} \u0633\u0627\u0639\u0629",
      hoursAgo_two: "\u0642\u0628\u0644 {{count}} \u0633\u0627\u0639\u062A\u064A\u0646",
      hoursAgo_few: "\u0642\u0628\u0644 {{count}} \u0633\u0627\u0639\u0627\u062A",
      hoursAgo_many: "\u0642\u0628\u0644 {{count}} \u0633\u0627\u0639\u0629",
      hoursAgo_other: "\u0642\u0628\u0644 {{count}} \u0633\u0627\u0639\u0629"
    },
    revenueOverview: "\u0646\u0638\u0631\u0629 \u0639\u0627\u0645\u0629 \u0639\u0644\u0649 \u0627\u0644\u0625\u064A\u0631\u0627\u062F\u0627\u062A",
    lastTwelveMonths: "\u0622\u062E\u0631 12 \u0634\u0647\u0631\u064B\u0627",
    recentActivity: "\u0627\u0644\u0646\u0634\u0627\u0637 \u0627\u0644\u0623\u062E\u064A\u0631",
    revenueTrend: "\u0627\u062A\u062C\u0627\u0647 \u0627\u0644\u0625\u064A\u0631\u0627\u062F\u0627\u062A"
  };

  // src/locales/ar/errors.json
  var errors_default2 = {
    boundary: {
      title: "\u062D\u062F\u062B \u062E\u0637\u0623 \u0645\u0627",
      description: "\u0648\u0627\u062C\u0647\u0646\u0627 \u062E\u0637\u0623 \u063A\u064A\u0631 \u0645\u062A\u0648\u0642\u0639. \u0628\u064A\u0627\u0646\u0627\u062A\u0643 \u0622\u0645\u0646\u0629 \u2014 \u062C\u0631\u0651\u0628 \u0623\u062D\u062F \u0627\u0644\u062E\u064A\u0627\u0631\u0627\u062A \u0623\u062F\u0646\u0627\u0647.",
      refresh: "\u062A\u062D\u062F\u064A\u062B",
      goBack: "\u0631\u062C\u0648\u0639",
      home: "\u0627\u0644\u0631\u0626\u064A\u0633\u064A\u0629"
    },
    notFound: {
      title: "\u0627\u0644\u0635\u0641\u062D\u0629 \u063A\u064A\u0631 \u0645\u0648\u062C\u0648\u062F\u0629",
      description: "\u0627\u0644\u0635\u0641\u062D\u0629 \u0627\u0644\u062A\u064A \u062A\u0628\u062D\u062B \u0639\u0646\u0647\u0627 \u063A\u064A\u0631 \u0645\u0648\u062C\u0648\u062F\u0629 \u0623\u0648 \u062A\u0645 \u0646\u0642\u0644\u0647\u0627.",
      backHome: "\u0627\u0644\u0639\u0648\u062F\u0629 \u0625\u0644\u0649 \u0627\u0644\u0631\u0626\u064A\u0633\u064A\u0629"
    },
    validation: {
      required: "\u0647\u0630\u0627 \u0627\u0644\u062D\u0642\u0644 \u0645\u0637\u0644\u0648\u0628",
      email: "\u064A\u0631\u062C\u0649 \u0625\u062F\u062E\u0627\u0644 \u0628\u0631\u064A\u062F \u0625\u0644\u0643\u062A\u0631\u0648\u0646\u064A \u0635\u0627\u0644\u062D",
      min: "\u064A\u062C\u0628 \u0623\u0646 \u064A\u0643\u0648\u0646 {{count}} \u0623\u062D\u0631\u0641 \u0639\u0644\u0649 \u0627\u0644\u0623\u0642\u0644",
      max: "\u064A\u062C\u0628 \u0623\u0644\u0627 \u064A\u062A\u062C\u0627\u0648\u0632 {{count}} \u0623\u062D\u0631\u0641"
    }
  };

  // src/locales/ar/header.json
  var header_default2 = {
    searchPlaceholder: "\u0627\u0628\u062D\u062B \u0623\u0648 \u0627\u0646\u062A\u0642\u0644\u2026",
    openCommandPalette: "\u0641\u062A\u062D \u0644\u0648\u062D\u0629 \u0627\u0644\u0623\u0648\u0627\u0645\u0631",
    notifications: "\u0627\u0644\u0625\u0634\u0639\u0627\u0631\u0627\u062A",
    openUserMenu: "\u0641\u062A\u062D \u0642\u0627\u0626\u0645\u0629 \u0627\u0644\u0645\u0633\u062A\u062E\u062F\u0645",
    myProfile: "\u0645\u0644\u0641\u064A \u0627\u0644\u0634\u062E\u0635\u064A",
    settings: "\u0627\u0644\u0625\u0639\u062F\u0627\u062F\u0627\u062A",
    logout: "\u062A\u0633\u062C\u064A\u0644 \u0627\u0644\u062E\u0631\u0648\u062C",
    switchLanguage: "\u062A\u063A\u064A\u064A\u0631 \u0627\u0644\u0644\u063A\u0629",
    switchToLight: "\u0627\u0644\u062A\u0628\u062F\u064A\u0644 \u0625\u0644\u0649 \u0627\u0644\u0645\u0638\u0647\u0631 \u0627\u0644\u0641\u0627\u062A\u062D",
    switchToDark: "\u0627\u0644\u062A\u0628\u062F\u064A\u0644 \u0625\u0644\u0649 \u0627\u0644\u0645\u0638\u0647\u0631 \u0627\u0644\u062F\u0627\u0643\u0646",
    toggleTheme: "\u062A\u0628\u062F\u064A\u0644 \u0627\u0644\u0645\u0638\u0647\u0631"
  };

  // src/locales/ar/home.json
  var home_default2 = {
    nav: {
      brand: "RSK",
      logoAlt: "\u0634\u0639\u0627\u0631 RSK",
      features: "\u0627\u0644\u0645\u0645\u064A\u0632\u0627\u062A",
      components: "\u0627\u0644\u0645\u0643\u0648\u0651\u0646\u0627\u062A",
      github: "GitHub"
    },
    hero: {
      eyebrow: "React \xB7 TypeScript \xB7 Vite",
      titleLead: "\u0627\u0644\u0623\u0633\u0627\u0633 \u0644\u0645\u0627 \u0647\u0648 ",
      titleEmphasis: "\u0642\u0627\u062F\u0645",
      titleTrail: ".",
      subtitle: "\u0646\u0642\u0637\u0629 \u0627\u0646\u0637\u0644\u0627\u0642 \u0645\u062F\u0631\u0648\u0633\u0629 \u0644\u0644\u0645\u0637\u0648\u0651\u0631\u064A\u0646 \u0627\u0644\u0630\u064A\u0646 \u064A\u0642\u062F\u0651\u0631\u0648\u0646 \u0627\u0644\u0625\u062A\u0642\u0627\u0646. 76 \u0645\u0643\u0648\u0651\u0646\u0627\u064B\u060C \u0648\u0623\u0646\u0645\u0627\u0637 \u0644\u0644\u0645\u0624\u0633\u0633\u0627\u062A\u060C \u0648\u062F\u0648\u0646 \u0623\u064A \u062A\u0646\u0627\u0632\u0644 \u2014 \u0643\u0644\u0651\u0647\u0627 \u0645\u062A\u0631\u0627\u0628\u0637\u0629 \u0644\u062A\u062A\u0645\u0643\u0651\u0646 \u0645\u0646 \u0627\u0644\u0625\u0637\u0644\u0627\u0642 \u0645\u0646\u0630 \u0627\u0644\u064A\u0648\u0645 \u0627\u0644\u0623\u0648\u0644.",
      ctaPrimary: "\u0627\u0628\u062F\u0623 \u0627\u0644\u0622\u0646",
      ctaSecondary: "\u062A\u0635\u0641\u0651\u062D \u0627\u0644\u0645\u0643\u0648\u0651\u0646\u0627\u062A"
    },
    stats: {
      uiComponents: "\u0645\u0643\u0648\u0651\u0646\u0627\u062A \u0648\u0627\u062C\u0647\u0629 \u0627\u0644\u0645\u0633\u062A\u062E\u062F\u0645",
      formComponents: "\u0645\u0643\u0648\u0651\u0646\u0627\u062A \u0627\u0644\u0646\u0645\u0627\u0630\u062C",
      mdxDocs: "\u0635\u0641\u062D\u0627\u062A \u062A\u0648\u062B\u064A\u0642 MDX",
      sharedComponents: "\u0645\u0643\u0648\u0651\u0646\u0627\u062A \u0645\u0634\u062A\u0631\u0643\u0629"
    },
    techStack: {
      builtOn: "\u0645\u0628\u0646\u064A \u0639\u0644\u0649"
    },
    features: {
      eyebrow: "\u0645\u0627 \u0627\u0644\u0630\u064A \u0628\u0627\u0644\u062F\u0627\u062E\u0644",
      title: "\u0643\u0644 \u0634\u064A\u0621 \u0645\u062A\u0631\u0627\u0628\u0637\u060C \u0648\u0644\u0627 \u0634\u064A\u0621 \u064A\u0639\u062A\u0631\u0636 \u0637\u0631\u064A\u0642\u0643.",
      componentLibrary: {
        title: "\u0645\u0643\u062A\u0628\u0629 \u0627\u0644\u0645\u0643\u0648\u0651\u0646\u0627\u062A",
        desc: "35 \u0639\u0646\u0635\u0631\u0627\u064B \u0623\u0633\u0627\u0633\u064A\u0627\u064B \u0645\u0646 Radix UI \u0645\u0639 \u062E\u064A\u0627\u0631\u0627\u062A CVA. \u0623\u0646\u0645\u0627\u0637 \u0645\u0643\u0648\u0651\u0646\u0627\u062A \u0645\u0631\u0643\u0651\u0628\u0629 \u2014 Dialog.Header \u0648Tabs.List \u0648Card.Content. \u062C\u0627\u0647\u0632\u0629 \u0644\u0644\u0648\u0636\u0639 \u0627\u0644\u062F\u0627\u0643\u0646. \u0645\u0643\u062A\u0648\u0628\u0629 \u0628\u0623\u0646\u0648\u0627\u0639 \u0643\u0627\u0645\u0644\u0629 \u0639\u0628\u0631 \u0623\u0646\u0648\u0627\u0639 TypeScript \u0627\u0644\u0639\u0627\u0645\u0629."
      },
      formSystem: {
        title: "\u0646\u0638\u0627\u0645 \u0627\u0644\u0646\u0645\u0627\u0630\u062C",
        desc: "17 \u0645\u0643\u0648\u0651\u0646\u0627\u064B \u0644\u0644\u0646\u0645\u0627\u0630\u062C \u0645\u062F\u0639\u0648\u0645\u0629 \u0628\u0640 React Hook Form \u0648Zod. \u0623\u0642\u0646\u0639\u0629 \u0625\u062F\u062E\u0627\u0644\u060C \u0648\u062A\u0646\u0633\u064A\u0642 \u0644\u0644\u0623\u0631\u0642\u0627\u0645\u060C \u0648\u0645\u0646\u062A\u0642\u064A\u0627\u062A \u0644\u0644\u062A\u0648\u0627\u0631\u064A\u062E\u060C \u0648\u0631\u0627\u0641\u0639\u0627\u062A \u0644\u0644\u0645\u0644\u0641\u0627\u062A\u060C \u0648\u0642\u0648\u0627\u0626\u0645 \u0645\u0646\u0633\u062F\u0644\u0629 \u063A\u064A\u0631 \u0645\u062A\u0632\u0627\u0645\u0646\u0629 \u0645\u0639 \u062A\u0645\u0631\u064A\u0631 \u0644\u0627 \u0646\u0647\u0627\u0626\u064A."
      },
      dataTables: {
        title: "\u062C\u062F\u0627\u0648\u0644 \u0627\u0644\u0628\u064A\u0627\u0646\u0627\u062A",
        desc: "TanStack Table \u0645\u0639 \u0641\u0631\u0632 \u0648\u062A\u0635\u0641\u064A\u0629 \u0648\u062A\u0642\u0633\u064A\u0645 \u0635\u0641\u062D\u0627\u062A \u0628\u0644\u0627 \u0648\u0627\u062C\u0647\u0629 \u062C\u0627\u0647\u0632\u0629. \u062A\u0643\u0627\u0645\u0644 \u0644\u0644\u0627\u0633\u062A\u0639\u0644\u0627\u0645\u0627\u062A \u0645\u0646 \u062C\u0647\u0629 \u0627\u0644\u062E\u0627\u062F\u0645 \u0639\u0628\u0631 \u062E\u0637\u0627\u0641\u0627\u062A \u0645\u062E\u0635\u0651\u0635\u0629. \u0645\u0631\u0634\u0651\u062D\u0627\u062A \u0645\u062A\u0639\u062F\u0651\u062F\u0629 \u0627\u0644\u0623\u0648\u062C\u0647 \u062C\u0627\u0647\u0632\u0629 \u0644\u0644\u0627\u0633\u062A\u062E\u062F\u0627\u0645."
      },
      authentication: {
        title: "\u0627\u0644\u0645\u0635\u0627\u062F\u0642\u0629",
        desc: "OIDC \u0639\u0628\u0631 oidc-client-ts \u0645\u0639 \u062F\u0639\u0645 Azure AD. \u062A\u062E\u0637\u064A\u0637 AuthGuard\u060C \u0648\u062D\u0645\u0627\u064A\u0629 \u0639\u0644\u0649 \u0645\u0633\u062A\u0648\u0649 \u0627\u0644\u0645\u0633\u0627\u0631\u0627\u062A\u060C \u0648\u0645\u0643\u0648\u0651\u0646 withAuth \u0639\u0627\u0644\u064A \u0627\u0644\u062A\u0631\u062A\u064A\u0628 \u0644\u0644\u062A\u062D\u0643\u0651\u0645 \u0628\u0627\u0644\u0648\u0635\u0648\u0644 \u0639\u0644\u0649 \u0645\u0633\u062A\u0648\u0649 \u0627\u0644\u0645\u0643\u0648\u0651\u0646\u0627\u062A."
      },
      internationalization: {
        title: "\u0627\u0644\u062A\u0639\u0631\u064A\u0628 \u0648\u0627\u0644\u062A\u062F\u0648\u064A\u0644",
        desc: "i18next \u0645\u0639 \u0627\u0643\u062A\u0634\u0627\u0641 \u0644\u063A\u0629 \u0627\u0644\u0645\u062A\u0635\u0641\u0651\u062D. \u062A\u0631\u062C\u0645\u0627\u062A \u0645\u0646\u0638\u0651\u0645\u0629 \u0648\u0641\u0642 \u0627\u0644\u0645\u0633\u0627\u062D\u0627\u062A\u060C \u0648\u062F\u0639\u0645 \u0644\u062A\u062E\u0637\u064A\u0637 \u0627\u0644\u0643\u062A\u0627\u0628\u0629 \u0645\u0646 \u0627\u0644\u064A\u0645\u064A\u0646 \u0625\u0644\u0649 \u0627\u0644\u064A\u0633\u0627\u0631\u060C \u0648\u062D\u0632\u0645 \u0644\u063A\u0648\u064A\u0629 \u062A\u064F\u062D\u0645\u0651\u0644 \u0639\u0646\u062F \u0627\u0644\u062D\u0627\u062C\u0629."
      },
      errorMonitoring: {
        title: "\u0645\u0631\u0627\u0642\u0628\u0629 \u0627\u0644\u0623\u062E\u0637\u0627\u0621",
        desc: "\u062A\u0643\u0627\u0645\u0644 \u0645\u0639 Sentry \u0648\u062E\u0631\u0627\u0626\u0637 \u0645\u0635\u062F\u0631 Vite. \u062D\u062F\u0648\u062F \u0623\u062E\u0637\u0627\u0621 React \u0645\u0639 \u0648\u0627\u062C\u0647\u0629 \u0628\u062F\u064A\u0644\u0629 \u0633\u0644\u0633\u0629. \u062A\u062A\u0628\u0651\u0639 \u0645\u0646\u0638\u0651\u0645 \u0644\u0644\u0623\u062E\u0637\u0627\u0621 \u0645\u0646\u0630 \u0627\u0644\u064A\u0648\u0645 \u0627\u0644\u0623\u0648\u0644."
      }
    },
    showcase: {
      eyebrow: "\u0645\u0648\u062B\u0651\u0642",
      title: "\u0627\u0637\u0651\u0644\u0639 \u062A\u0645\u0627\u0645\u0627\u064B \u0639\u0644\u0649 \u0645\u0627 \u062A\u062D\u0635\u0644 \u0639\u0644\u064A\u0647.",
      desc: "79 \u0635\u0641\u062D\u0629 \u062A\u0648\u062B\u064A\u0642 \u0645\u062F\u0639\u0648\u0645\u0629 \u0628\u0640 MDX \u0645\u0639 \u0645\u0639\u0627\u064A\u0646\u0627\u062A \u062D\u064A\u0651\u0629\u060C \u0648\u0645\u0642\u062A\u0637\u0641\u0627\u062A \u0628\u0631\u0645\u062C\u064A\u0629\u060C \u0648\u0625\u0631\u0634\u0627\u062F\u0627\u062A \u0644\u0644\u0627\u0633\u062A\u062E\u062F\u0627\u0645. \u062A\u0635\u0641\u0651\u062D \u0627\u0644\u0645\u0639\u0631\u0636 \u0627\u0644\u0643\u0627\u0645\u0644 \u0644\u0627\u0633\u062A\u0643\u0634\u0627\u0641 \u0643\u0644 \u0645\u0643\u0648\u0651\u0646.",
      cta: "\u0627\u0641\u062A\u062D \u0627\u0644\u0645\u0639\u0631\u0636",
      browserUrl: "localhost:3000/components",
      searchPlaceholder: "\u0628\u062D\u062B\u2026",
      buttonTitle: "Button",
      buttonDesc: "\u064A\u0639\u0631\u0636 \u0632\u0631\u0627\u064B \u0623\u0648 \u0645\u0643\u0648\u0651\u0646\u0627\u064B \u064A\u0628\u062F\u0648 \u0643\u0632\u0631.",
      variantDefault: "\u0627\u0641\u062A\u0631\u0627\u0636\u064A",
      variantOutline: "\u0645\u062D\u062F\u0651\u062F",
      variantGhost: "\u0634\u0641\u0651\u0627\u0641"
    },
    cli: {
      eyebrow: "\u0627\u0644\u0623\u062F\u0648\u0627\u062A",
      title: "\u0648\u064E\u0644\u0650\u0651\u062F \u0648\u0644\u0627 \u062A\u0643\u062A\u0628.",
      desc: "\u062E\u0637 \u0645\u0639\u0627\u0644\u062C\u0629 \u0645\u0646 7 \u062E\u0637\u0648\u0627\u062A \u064A\u062C\u0644\u0628 \u0645\u0648\u0627\u0635\u0641\u0629 OpenAPI \u0627\u0644\u062E\u0627\u0635\u0629 \u0628\u0643\u060C \u0648\u064A\u062D\u0644\u0651\u0644 \u0627\u0644\u0645\u062E\u0637\u0651\u0637\u0627\u062A\u060C \u0648\u064A\u0648\u0644\u0651\u062F \u0643\u0627\u0626\u0646\u0627\u062A \u0646\u0642\u0644 \u0628\u064A\u0627\u0646\u0627\u062A TypeScript \u0645\u0643\u062A\u0648\u0628\u0629 \u0628\u0627\u0644\u0643\u0627\u0645\u0644\u060C \u0648\u064A\u0646\u0633\u0651\u0642\u0647\u0627 \u0628\u0640 Prettier\u060C \u0648\u064A\u0643\u062A\u0628\u0647\u0627 \u0625\u0644\u0649 \u0627\u0644\u0642\u0631\u0635. \u0623\u0645\u0631 \u0648\u0627\u062D\u062F \u064A\u063A\u0646\u064A \u0639\u0646 \u0633\u0627\u0639\u0627\u062A \u0645\u0646 \u0627\u0644\u0643\u062A\u0627\u0628\u0629 \u0627\u0644\u064A\u062F\u0648\u064A\u0629.",
      logFetching: "\u062C\u0644\u0628 \u0645\u0648\u0627\u0635\u0641\u0629 OpenAPI\u2026",
      logParsing: "\u062A\u062D\u0644\u064A\u0644 42 \u0645\u062E\u0637\u0651\u0637\u0627\u064B",
      logGenerating: "\u062A\u0648\u0644\u064A\u062F \u0643\u0627\u0626\u0646\u0627\u062A \u0646\u0642\u0644 \u0628\u064A\u0627\u0646\u0627\u062A TypeScript",
      logFormatting: "\u0627\u0644\u062A\u0646\u0633\u064A\u0642 \u0628\u0640 Prettier",
      logWritten: "\u062A\u0645\u062A \u0643\u062A\u0627\u0628\u0629 42 \u0645\u0644\u0641\u0627\u064B"
    },
    architecture: {
      eyebrow: "\u0627\u0644\u0628\u0646\u064A\u0629",
      title: "\u0645\u0646\u0638\u0651\u0645\u0629 \u0628\u062A\u0635\u0645\u064A\u0645 \u0645\u062F\u0631\u0648\u0633.",
      desc: "15 \u0627\u0633\u0645\u0627\u064B \u0645\u0633\u062A\u0639\u0627\u0631\u0627\u064B \u0644\u0644\u0645\u0633\u0627\u0631\u0627\u062A \u062A\u064F\u0628\u0642\u064A \u0627\u0644\u0627\u0633\u062A\u064A\u0631\u0627\u062F\u0627\u062A \u0646\u0638\u064A\u0641\u0629 \u0648\u0625\u0639\u0627\u062F\u0629 \u0627\u0644\u0647\u064A\u0643\u0644\u0629 \u0633\u0647\u0644\u0629. \u0644\u0643\u0644 \u0645\u062C\u0644\u062F \u063A\u0631\u0636\u0647\u060C \u0648\u0644\u0643\u0644 \u0627\u0633\u0645 \u0645\u0633\u062A\u0639\u0627\u0631 \u0627\u0635\u0637\u0644\u0627\u062D\u0647. \u0644\u0627 \u062A\u062E\u0645\u064A\u0646 \u0644\u0645\u0643\u0627\u0646 \u0648\u062C\u0648\u062F \u0627\u0644\u0623\u0634\u064A\u0627\u0621."
    },
    gettingStarted: {
      eyebrow: "\u0644\u0646\u0646\u0637\u0644\u0642",
      title: "\u062C\u0627\u0647\u0632 \u0644\u0644\u0639\u0645\u0644 \u0641\u064A \u062B\u0648\u0627\u0646\u064D.",
      clone: "\u0627\u0633\u062A\u0646\u0633\u0627\u062E",
      install: "\u062A\u062B\u0628\u064A\u062A",
      start: "\u062A\u0634\u063A\u064A\u0644"
    },
    footer: {
      builtBy: "\u0628\u064F\u0646\u064A \u0628\u0648\u0627\u0633\u0637\u0629 \u0645\u062D\u0645\u062F \u0635\u0627\u0641\u064A\u0627",
      github: "GitHub"
    }
  };

  // src/locales/ar/members.json
  var members_default2 = {
    title: "\u0627\u0644\u0623\u0639\u0636\u0627\u0621",
    addMember: "\u0625\u0636\u0627\u0641\u0629 \u0639\u0636\u0648",
    totalLabel: "\u0627\u0644\u0623\u0639\u0636\u0627\u0621",
    searchPlaceholder: "\u0627\u0628\u062D\u062B \u0641\u064A \u0627\u0644\u0623\u0639\u0636\u0627\u0621\u2026",
    columns: {
      name: "\u0627\u0644\u0627\u0633\u0645",
      email: "\u0627\u0644\u0628\u0631\u064A\u062F \u0627\u0644\u0625\u0644\u0643\u062A\u0631\u0648\u0646\u064A",
      role: "\u0627\u0644\u062F\u0648\u0631",
      status: "\u0627\u0644\u062D\u0627\u0644\u0629",
      joined: "\u062A\u0627\u0631\u064A\u062E \u0627\u0644\u0627\u0646\u0636\u0645\u0627\u0645",
      lastSeen: "\u0622\u062E\u0631 \u0638\u0647\u0648\u0631",
      shiftStart: "\u0628\u062F\u0627\u064A\u0629 \u0627\u0644\u062F\u0648\u0627\u0645"
    },
    roles: {
      Admin: "\u0645\u062F\u064A\u0631",
      Editor: "\u0645\u062D\u0631\u0631",
      Viewer: "\u0645\u0634\u0627\u0647\u062F"
    },
    statuses: {
      Active: "\u0646\u0634\u0637",
      Invited: "\u0645\u062F\u0639\u0648",
      Suspended: "\u0645\u0648\u0642\u0648\u0641"
    },
    emptyState: {
      title: "\u0644\u0627 \u064A\u0648\u062C\u062F \u0623\u0639\u0636\u0627\u0621",
      description: "\u0644\u0627 \u064A\u0648\u062C\u062F \u0623\u0639\u0636\u0627\u0621 \u064A\u0637\u0627\u0628\u0642\u0648\u0646 \u0628\u062D\u062B\u0643. \u062D\u0627\u0648\u0644 \u062A\u0639\u062F\u064A\u0644 \u0639\u0648\u0627\u0645\u0644 \u0627\u0644\u062A\u0635\u0641\u064A\u0629 \u0623\u0648 \u0627\u0644\u0643\u0644\u0645\u0627\u062A \u0627\u0644\u0645\u0641\u062A\u0627\u062D\u064A\u0629."
    }
  };

  // src/locales/ar/nav.json
  var nav_default2 = {
    dashboard: "\u0644\u0648\u062D\u0629 \u0627\u0644\u062A\u062D\u0643\u0645",
    components: "\u0627\u0644\u0645\u0643\u0648\u0651\u0646\u0627\u062A",
    members: "\u0627\u0644\u0623\u0639\u0636\u0627\u0621",
    settings: "\u0627\u0644\u0625\u0639\u062F\u0627\u062F\u0627\u062A",
    commandPalette: "\u0644\u0648\u062D\u0629 \u0627\u0644\u0623\u0648\u0627\u0645\u0631",
    navigation: "\u0627\u0644\u062A\u0646\u0642\u0644",
    actions: "\u0627\u0644\u0625\u062C\u0631\u0627\u0621\u0627\u062A",
    toggleTheme: "\u062A\u0628\u062F\u064A\u0644 \u0627\u0644\u0645\u0638\u0647\u0631",
    expandSidebar: "\u062A\u0648\u0633\u064A\u0639 \u0627\u0644\u0634\u0631\u064A\u0637 \u0627\u0644\u062C\u0627\u0646\u0628\u064A",
    collapseSidebar: "\u0637\u064A \u0627\u0644\u0634\u0631\u064A\u0637 \u0627\u0644\u062C\u0627\u0646\u0628\u064A",
    openMenu: "\u0641\u062A\u062D \u0627\u0644\u0642\u0627\u0626\u0645\u0629",
    closeMenu: "\u0625\u063A\u0644\u0627\u0642 \u0627\u0644\u0642\u0627\u0626\u0645\u0629",
    groups: {
      main: "\u0627\u0644\u0631\u0626\u064A\u0633\u064A\u0629",
      account: "\u0627\u0644\u062D\u0633\u0627\u0628"
    }
  };

  // .design-sync/stubs/locales.ts
  var LOCALES = {
    EN: { auth: auth_default, common: common_default, components: components_default, dashboard: dashboard_default, errors: errors_default, header: header_default, home: home_default, members: members_default, nav: nav_default },
    AR: { auth: auth_default2, common: common_default2, components: components_default2, dashboard: dashboard_default2, errors: errors_default2, header: header_default2, home: home_default2, members: members_default2, nav: nav_default2 }
  };

  // src/i18n.ts
  var setDocumentDirection = (language) => {
    document.documentElement.lang = language;
    document.documentElement.dir = language === "ar" /* AR */ ? "rtl" : "ltr";
  };
  var options = {
    fallbackLng: DEFAULT_LANGUAGE,
    debug: false,
    interpolation: { escapeValue: false },
    resources: {
      ["en" /* EN */]: LOCALES.EN,
      ["ar" /* AR */]: LOCALES.AR
    },
    defaultNS: "common",
    ns: Object.keys(LOCALES.EN),
    supportedLngs: ["en" /* EN */, "ar" /* AR */],
    nonExplicitSupportedLngs: true,
    detection: {
      order: ["localStorage"],
      caches: ["localStorage"],
      lookupLocalStorage: "i18nextLng"
    }
  };
  instance.use(Browser).use(initReactI18next).init(options);
  setDocumentDirection(instance.language ?? DEFAULT_LANGUAGE);
  instance.on("languageChanged", setDocumentDirection);

  // node_modules/react-router-dom/dist/index.js
  init_define_import_meta_env();
  var React3 = __toESM(require_react_shim());
  var ReactDOM = __toESM(require_react_dom_shim());

  // node_modules/react-router/dist/index.js
  init_define_import_meta_env();
  var React2 = __toESM(require_react_shim());

  // node_modules/@remix-run/router/dist/router.js
  init_define_import_meta_env();
  function _extends() {
    _extends = Object.assign ? Object.assign.bind() : function(target) {
      for (var i = 1; i < arguments.length; i++) {
        var source = arguments[i];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    return _extends.apply(this, arguments);
  }
  var Action;
  (function(Action4) {
    Action4["Pop"] = "POP";
    Action4["Push"] = "PUSH";
    Action4["Replace"] = "REPLACE";
  })(Action || (Action = {}));
  function createMemoryHistory(options3) {
    if (options3 === void 0) {
      options3 = {};
    }
    let {
      initialEntries = ["/"],
      initialIndex,
      v5Compat = false
    } = options3;
    let entries;
    entries = initialEntries.map((entry, index3) => createMemoryLocation(entry, typeof entry === "string" ? null : entry.state, index3 === 0 ? "default" : void 0));
    let index2 = clampIndex(initialIndex == null ? entries.length - 1 : initialIndex);
    let action = Action.Pop;
    let listener = null;
    function clampIndex(n) {
      return Math.min(Math.max(n, 0), entries.length - 1);
    }
    function getCurrentLocation() {
      return entries[index2];
    }
    function createMemoryLocation(to, state, key) {
      if (state === void 0) {
        state = null;
      }
      let location2 = createLocation(entries ? getCurrentLocation().pathname : "/", to, state, key);
      warning(location2.pathname.charAt(0) === "/", "relative pathnames are not supported in memory history: " + JSON.stringify(to));
      return location2;
    }
    function createHref(to) {
      return typeof to === "string" ? to : createPath(to);
    }
    let history2 = {
      get index() {
        return index2;
      },
      get action() {
        return action;
      },
      get location() {
        return getCurrentLocation();
      },
      createHref,
      createURL(to) {
        return new URL(createHref(to), "http://localhost");
      },
      encodeLocation(to) {
        let path2 = typeof to === "string" ? parsePath(to) : to;
        return {
          pathname: path2.pathname || "",
          search: path2.search || "",
          hash: path2.hash || ""
        };
      },
      push(to, state) {
        action = Action.Push;
        let nextLocation = createMemoryLocation(to, state);
        index2 += 1;
        entries.splice(index2, entries.length, nextLocation);
        if (v5Compat && listener) {
          listener({
            action,
            location: nextLocation,
            delta: 1
          });
        }
      },
      replace(to, state) {
        action = Action.Replace;
        let nextLocation = createMemoryLocation(to, state);
        entries[index2] = nextLocation;
        if (v5Compat && listener) {
          listener({
            action,
            location: nextLocation,
            delta: 0
          });
        }
      },
      go(delta) {
        action = Action.Pop;
        let nextIndex = clampIndex(index2 + delta);
        let nextLocation = entries[nextIndex];
        index2 = nextIndex;
        if (listener) {
          listener({
            action,
            location: nextLocation,
            delta
          });
        }
      },
      listen(fn) {
        listener = fn;
        return () => {
          listener = null;
        };
      }
    };
    return history2;
  }
  function invariant(value, message2) {
    if (value === false || value === null || typeof value === "undefined") {
      throw new Error(message2);
    }
  }
  function warning(cond, message2) {
    if (!cond) {
      if (typeof console !== "undefined") console.warn(message2);
      try {
        throw new Error(message2);
      } catch (e2) {
      }
    }
  }
  function createKey() {
    return Math.random().toString(36).substr(2, 8);
  }
  function createLocation(current, to, state, key) {
    if (state === void 0) {
      state = null;
    }
    let location2 = _extends({
      pathname: typeof current === "string" ? current : current.pathname,
      search: "",
      hash: ""
    }, typeof to === "string" ? parsePath(to) : to, {
      state,
      // TODO: This could be cleaned up.  push/replace should probably just take
      // full Locations now and avoid the need to run through this flow at all
      // But that's a pretty big refactor to the current test suite so going to
      // keep as is for the time being and just let any incoming keys take precedence
      key: to && to.key || key || createKey()
    });
    return location2;
  }
  function createPath(_ref) {
    let {
      pathname = "/",
      search = "",
      hash: hash2 = ""
    } = _ref;
    if (search && search !== "?") pathname += search.charAt(0) === "?" ? search : "?" + search;
    if (hash2 && hash2 !== "#") pathname += hash2.charAt(0) === "#" ? hash2 : "#" + hash2;
    return pathname;
  }
  function parsePath(path2) {
    let parsedPath = {};
    if (path2) {
      let hashIndex = path2.indexOf("#");
      if (hashIndex >= 0) {
        parsedPath.hash = path2.substr(hashIndex);
        path2 = path2.substr(0, hashIndex);
      }
      let searchIndex = path2.indexOf("?");
      if (searchIndex >= 0) {
        parsedPath.search = path2.substr(searchIndex);
        path2 = path2.substr(0, searchIndex);
      }
      if (path2) {
        parsedPath.pathname = path2;
      }
    }
    return parsedPath;
  }
  var ResultType;
  (function(ResultType2) {
    ResultType2["data"] = "data";
    ResultType2["deferred"] = "deferred";
    ResultType2["redirect"] = "redirect";
    ResultType2["error"] = "error";
  })(ResultType || (ResultType = {}));
  function convertRouteMatchToUiMatch(match3, loaderData) {
    let {
      route,
      pathname,
      params
    } = match3;
    return {
      id: route.id,
      pathname,
      params,
      data: loaderData[route.id],
      handle: route.handle
    };
  }
  function matchPath(pattern, pathname) {
    if (typeof pattern === "string") {
      pattern = {
        path: pattern,
        caseSensitive: false,
        end: true
      };
    }
    let [matcher, compiledParams] = compilePath(pattern.path, pattern.caseSensitive, pattern.end);
    let match3 = pathname.match(matcher);
    if (!match3) return null;
    let matchedPathname = match3[0];
    let pathnameBase = matchedPathname.replace(/(.)\/+$/, "$1");
    let captureGroups = match3.slice(1);
    let params = compiledParams.reduce((memo2, _ref, index2) => {
      let {
        paramName,
        isOptional
      } = _ref;
      if (paramName === "*") {
        let splatValue = captureGroups[index2] || "";
        pathnameBase = matchedPathname.slice(0, matchedPathname.length - splatValue.length).replace(/(.)\/+$/, "$1");
      }
      const value = captureGroups[index2];
      if (isOptional && !value) {
        memo2[paramName] = void 0;
      } else {
        memo2[paramName] = (value || "").replace(/%2F/g, "/");
      }
      return memo2;
    }, {});
    return {
      params,
      pathname: matchedPathname,
      pathnameBase,
      pattern
    };
  }
  function compilePath(path2, caseSensitive, end) {
    if (caseSensitive === void 0) {
      caseSensitive = false;
    }
    if (end === void 0) {
      end = true;
    }
    warning(path2 === "*" || !path2.endsWith("*") || path2.endsWith("/*"), 'Route path "' + path2 + '" will be treated as if it were ' + ('"' + path2.replace(/\*$/, "/*") + '" because the `*` character must ') + "always follow a `/` in the pattern. To get rid of this warning, " + ('please change the route path to "' + path2.replace(/\*$/, "/*") + '".'));
    let params = [];
    let regexpSource = "^" + path2.replace(/\/*\*?$/, "").replace(/^\/*/, "/").replace(/[\\.*+^${}|()[\]]/g, "\\$&").replace(/\/:([\w-]+)(\?)?/g, (_, paramName, isOptional) => {
      params.push({
        paramName,
        isOptional: isOptional != null
      });
      return isOptional ? "/?([^\\/]+)?" : "/([^\\/]+)";
    });
    if (path2.endsWith("*")) {
      params.push({
        paramName: "*"
      });
      regexpSource += path2 === "*" || path2 === "/*" ? "(.*)$" : "(?:\\/(.+)|\\/*)$";
    } else if (end) {
      regexpSource += "\\/*$";
    } else if (path2 !== "" && path2 !== "/") {
      regexpSource += "(?:(?=\\/|$))";
    } else ;
    let matcher = new RegExp(regexpSource, caseSensitive ? void 0 : "i");
    return [matcher, params];
  }
  function stripBasename(pathname, basename) {
    if (basename === "/") return pathname;
    if (!pathname.toLowerCase().startsWith(basename.toLowerCase())) {
      return null;
    }
    let startIndex = basename.endsWith("/") ? basename.length - 1 : basename.length;
    let nextChar = pathname.charAt(startIndex);
    if (nextChar && nextChar !== "/") {
      return null;
    }
    return pathname.slice(startIndex) || "/";
  }
  var ABSOLUTE_URL_REGEX$1 = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
  var isAbsoluteUrl = (url) => ABSOLUTE_URL_REGEX$1.test(url);
  function resolvePath(to, fromPathname) {
    if (fromPathname === void 0) {
      fromPathname = "/";
    }
    let {
      pathname: toPathname,
      search = "",
      hash: hash2 = ""
    } = typeof to === "string" ? parsePath(to) : to;
    let pathname;
    if (toPathname) {
      if (isAbsoluteUrl(toPathname)) {
        pathname = toPathname;
      } else {
        if (toPathname.includes("//")) {
          let oldPathname = toPathname;
          toPathname = toPathname.replace(/\/\/+/g, "/");
          warning(false, "Pathnames cannot have embedded double slashes - normalizing " + (oldPathname + " -> " + toPathname));
        }
        if (toPathname.startsWith("/")) {
          pathname = resolvePathname(toPathname.substring(1), "/");
        } else {
          pathname = resolvePathname(toPathname, fromPathname);
        }
      }
    } else {
      pathname = fromPathname;
    }
    return {
      pathname,
      search: normalizeSearch(search),
      hash: normalizeHash(hash2)
    };
  }
  function resolvePathname(relativePath, fromPathname) {
    let segments = fromPathname.replace(/\/+$/, "").split("/");
    let relativeSegments = relativePath.split("/");
    relativeSegments.forEach((segment) => {
      if (segment === "..") {
        if (segments.length > 1) segments.pop();
      } else if (segment !== ".") {
        segments.push(segment);
      }
    });
    return segments.length > 1 ? segments.join("/") : "/";
  }
  function getInvalidPathError(char, field, dest, path2) {
    return "Cannot include a '" + char + "' character in a manually specified " + ("`to." + field + "` field [" + JSON.stringify(path2) + "].  Please separate it out to the ") + ("`to." + dest + "` field. Alternatively you may provide the full path as ") + 'a string in <Link to="..."> and the router will parse it for you.';
  }
  function getPathContributingMatches(matches) {
    return matches.filter((match3, index2) => index2 === 0 || match3.route.path && match3.route.path.length > 0);
  }
  function getResolveToMatches(matches, v7_relativeSplatPath) {
    let pathMatches = getPathContributingMatches(matches);
    if (v7_relativeSplatPath) {
      return pathMatches.map((match3, idx) => idx === pathMatches.length - 1 ? match3.pathname : match3.pathnameBase);
    }
    return pathMatches.map((match3) => match3.pathnameBase);
  }
  function resolveTo(toArg, routePathnames, locationPathname, isPathRelative) {
    if (isPathRelative === void 0) {
      isPathRelative = false;
    }
    let to;
    if (typeof toArg === "string") {
      to = parsePath(toArg);
    } else {
      to = _extends({}, toArg);
      invariant(!to.pathname || !to.pathname.includes("?"), getInvalidPathError("?", "pathname", "search", to));
      invariant(!to.pathname || !to.pathname.includes("#"), getInvalidPathError("#", "pathname", "hash", to));
      invariant(!to.search || !to.search.includes("#"), getInvalidPathError("#", "search", "hash", to));
    }
    let isEmptyPath = toArg === "" || to.pathname === "";
    let toPathname = isEmptyPath ? "/" : to.pathname;
    let from;
    if (toPathname == null) {
      from = locationPathname;
    } else {
      let routePathnameIndex = routePathnames.length - 1;
      if (!isPathRelative && toPathname.startsWith("..")) {
        let toSegments = toPathname.split("/");
        while (toSegments[0] === "..") {
          toSegments.shift();
          routePathnameIndex -= 1;
        }
        to.pathname = toSegments.join("/");
      }
      from = routePathnameIndex >= 0 ? routePathnames[routePathnameIndex] : "/";
    }
    let path2 = resolvePath(to, from);
    let hasExplicitTrailingSlash = toPathname && toPathname !== "/" && toPathname.endsWith("/");
    let hasCurrentTrailingSlash = (isEmptyPath || toPathname === ".") && locationPathname.endsWith("/");
    if (!path2.pathname.endsWith("/") && (hasExplicitTrailingSlash || hasCurrentTrailingSlash)) {
      path2.pathname += "/";
    }
    return path2;
  }
  var joinPaths = (paths) => paths.join("/").replace(/\/\/+/g, "/");
  var normalizeSearch = (search) => !search || search === "?" ? "" : search.startsWith("?") ? search : "?" + search;
  var normalizeHash = (hash2) => !hash2 || hash2 === "#" ? "" : hash2.startsWith("#") ? hash2 : "#" + hash2;
  var validMutationMethodsArr = ["post", "put", "patch", "delete"];
  var validMutationMethods = new Set(validMutationMethodsArr);
  var validRequestMethodsArr = ["get", ...validMutationMethodsArr];
  var validRequestMethods = new Set(validRequestMethodsArr);

  // node_modules/react-router/dist/index.js
  function _extends2() {
    _extends2 = Object.assign ? Object.assign.bind() : function(target) {
      for (var i = 1; i < arguments.length; i++) {
        var source = arguments[i];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    return _extends2.apply(this, arguments);
  }
  var DataRouterContext = /* @__PURE__ */ React2.createContext(null);
  if (true) {
    DataRouterContext.displayName = "DataRouter";
  }
  var DataRouterStateContext = /* @__PURE__ */ React2.createContext(null);
  if (true) {
    DataRouterStateContext.displayName = "DataRouterState";
  }
  var AwaitContext = /* @__PURE__ */ React2.createContext(null);
  if (true) {
    AwaitContext.displayName = "Await";
  }
  var NavigationContext = /* @__PURE__ */ React2.createContext(null);
  if (true) {
    NavigationContext.displayName = "Navigation";
  }
  var LocationContext = /* @__PURE__ */ React2.createContext(null);
  if (true) {
    LocationContext.displayName = "Location";
  }
  var RouteContext = /* @__PURE__ */ React2.createContext({
    outlet: null,
    matches: [],
    isDataRoute: false
  });
  if (true) {
    RouteContext.displayName = "Route";
  }
  var RouteErrorContext = /* @__PURE__ */ React2.createContext(null);
  if (true) {
    RouteErrorContext.displayName = "RouteError";
  }
  function useHref(to, _temp) {
    let {
      relative
    } = _temp === void 0 ? {} : _temp;
    !useInRouterContext() ? true ? invariant(
      false,
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      "useHref() may be used only in the context of a <Router> component."
    ) : invariant(false) : void 0;
    let {
      basename,
      navigator: navigator2
    } = React2.useContext(NavigationContext);
    let {
      hash: hash2,
      pathname,
      search
    } = useResolvedPath(to, {
      relative
    });
    let joinedPathname = pathname;
    if (basename !== "/") {
      joinedPathname = pathname === "/" ? basename : joinPaths([basename, pathname]);
    }
    return navigator2.createHref({
      pathname: joinedPathname,
      search,
      hash: hash2
    });
  }
  function useInRouterContext() {
    return React2.useContext(LocationContext) != null;
  }
  function useLocation() {
    !useInRouterContext() ? true ? invariant(
      false,
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      "useLocation() may be used only in the context of a <Router> component."
    ) : invariant(false) : void 0;
    return React2.useContext(LocationContext).location;
  }
  var navigateEffectWarning = "You should call navigate() in a React.useEffect(), not when your component is first rendered.";
  function useIsomorphicLayoutEffect(cb) {
    let isStatic = React2.useContext(NavigationContext).static;
    if (!isStatic) {
      React2.useLayoutEffect(cb);
    }
  }
  function useNavigate() {
    let {
      isDataRoute
    } = React2.useContext(RouteContext);
    return isDataRoute ? useNavigateStable() : useNavigateUnstable();
  }
  function useNavigateUnstable() {
    !useInRouterContext() ? true ? invariant(
      false,
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      "useNavigate() may be used only in the context of a <Router> component."
    ) : invariant(false) : void 0;
    let dataRouterContext = React2.useContext(DataRouterContext);
    let {
      basename,
      future,
      navigator: navigator2
    } = React2.useContext(NavigationContext);
    let {
      matches
    } = React2.useContext(RouteContext);
    let {
      pathname: locationPathname
    } = useLocation();
    let routePathnamesJson = JSON.stringify(getResolveToMatches(matches, future.v7_relativeSplatPath));
    let activeRef = React2.useRef(false);
    useIsomorphicLayoutEffect(() => {
      activeRef.current = true;
    });
    let navigate = React2.useCallback(function(to, options3) {
      if (options3 === void 0) {
        options3 = {};
      }
      true ? warning(activeRef.current, navigateEffectWarning) : void 0;
      if (!activeRef.current) return;
      if (typeof to === "number") {
        navigator2.go(to);
        return;
      }
      let path2 = resolveTo(to, JSON.parse(routePathnamesJson), locationPathname, options3.relative === "path");
      if (dataRouterContext == null && basename !== "/") {
        path2.pathname = path2.pathname === "/" ? basename : joinPaths([basename, path2.pathname]);
      }
      (!!options3.replace ? navigator2.replace : navigator2.push)(path2, options3.state, options3);
    }, [basename, navigator2, routePathnamesJson, locationPathname, dataRouterContext]);
    return navigate;
  }
  var OutletContext = /* @__PURE__ */ React2.createContext(null);
  function useOutlet(context2) {
    let outlet = React2.useContext(RouteContext).outlet;
    if (outlet) {
      return /* @__PURE__ */ React2.createElement(OutletContext.Provider, {
        value: context2
      }, outlet);
    }
    return outlet;
  }
  function useResolvedPath(to, _temp2) {
    let {
      relative
    } = _temp2 === void 0 ? {} : _temp2;
    let {
      future
    } = React2.useContext(NavigationContext);
    let {
      matches
    } = React2.useContext(RouteContext);
    let {
      pathname: locationPathname
    } = useLocation();
    let routePathnamesJson = JSON.stringify(getResolveToMatches(matches, future.v7_relativeSplatPath));
    return React2.useMemo(() => resolveTo(to, JSON.parse(routePathnamesJson), locationPathname, relative === "path"), [to, routePathnamesJson, locationPathname, relative]);
  }
  var DataRouterHook = /* @__PURE__ */ (function(DataRouterHook3) {
    DataRouterHook3["UseBlocker"] = "useBlocker";
    DataRouterHook3["UseRevalidator"] = "useRevalidator";
    DataRouterHook3["UseNavigateStable"] = "useNavigate";
    return DataRouterHook3;
  })(DataRouterHook || {});
  var DataRouterStateHook = /* @__PURE__ */ (function(DataRouterStateHook3) {
    DataRouterStateHook3["UseBlocker"] = "useBlocker";
    DataRouterStateHook3["UseLoaderData"] = "useLoaderData";
    DataRouterStateHook3["UseActionData"] = "useActionData";
    DataRouterStateHook3["UseRouteError"] = "useRouteError";
    DataRouterStateHook3["UseNavigation"] = "useNavigation";
    DataRouterStateHook3["UseRouteLoaderData"] = "useRouteLoaderData";
    DataRouterStateHook3["UseMatches"] = "useMatches";
    DataRouterStateHook3["UseRevalidator"] = "useRevalidator";
    DataRouterStateHook3["UseNavigateStable"] = "useNavigate";
    DataRouterStateHook3["UseRouteId"] = "useRouteId";
    return DataRouterStateHook3;
  })(DataRouterStateHook || {});
  function getDataRouterConsoleError(hookName) {
    return hookName + " must be used within a data router.  See https://reactrouter.com/v6/routers/picking-a-router.";
  }
  function useDataRouterContext(hookName) {
    let ctx = React2.useContext(DataRouterContext);
    !ctx ? true ? invariant(false, getDataRouterConsoleError(hookName)) : invariant(false) : void 0;
    return ctx;
  }
  function useDataRouterState(hookName) {
    let state = React2.useContext(DataRouterStateContext);
    !state ? true ? invariant(false, getDataRouterConsoleError(hookName)) : invariant(false) : void 0;
    return state;
  }
  function useRouteContext(hookName) {
    let route = React2.useContext(RouteContext);
    !route ? true ? invariant(false, getDataRouterConsoleError(hookName)) : invariant(false) : void 0;
    return route;
  }
  function useCurrentRouteId(hookName) {
    let route = useRouteContext(hookName);
    let thisRoute = route.matches[route.matches.length - 1];
    !thisRoute.route.id ? true ? invariant(false, hookName + ' can only be used on routes that contain a unique "id"') : invariant(false) : void 0;
    return thisRoute.route.id;
  }
  function useRouteId() {
    return useCurrentRouteId(DataRouterStateHook.UseRouteId);
  }
  function useNavigation() {
    let state = useDataRouterState(DataRouterStateHook.UseNavigation);
    return state.navigation;
  }
  function useMatches() {
    let {
      matches,
      loaderData
    } = useDataRouterState(DataRouterStateHook.UseMatches);
    return React2.useMemo(() => matches.map((m2) => convertRouteMatchToUiMatch(m2, loaderData)), [matches, loaderData]);
  }
  function useNavigateStable() {
    let {
      router
    } = useDataRouterContext(DataRouterHook.UseNavigateStable);
    let id = useCurrentRouteId(DataRouterStateHook.UseNavigateStable);
    let activeRef = React2.useRef(false);
    useIsomorphicLayoutEffect(() => {
      activeRef.current = true;
    });
    let navigate = React2.useCallback(function(to, options3) {
      if (options3 === void 0) {
        options3 = {};
      }
      true ? warning(activeRef.current, navigateEffectWarning) : void 0;
      if (!activeRef.current) return;
      if (typeof to === "number") {
        router.navigate(to);
      } else {
        router.navigate(to, _extends2({
          fromRouteId: id
        }, options3));
      }
    }, [router, id]);
    return navigate;
  }
  var alreadyWarned2 = {};
  function warnOnce2(key, message2) {
    if (!alreadyWarned2[message2]) {
      alreadyWarned2[message2] = true;
      console.warn(message2);
    }
  }
  var logDeprecation = (flag, msg, link) => warnOnce2(flag, "\u26A0\uFE0F React Router Future Flag Warning: " + msg + ". " + ("You can use the `" + flag + "` future flag to opt-in early. ") + ("For more information, see " + link + "."));
  function logV6DeprecationWarnings(renderFuture, routerFuture) {
    if ((renderFuture == null ? void 0 : renderFuture.v7_startTransition) === void 0) {
      logDeprecation("v7_startTransition", "React Router will begin wrapping state updates in `React.startTransition` in v7", "https://reactrouter.com/v6/upgrading/future#v7_starttransition");
    }
    if ((renderFuture == null ? void 0 : renderFuture.v7_relativeSplatPath) === void 0 && (!routerFuture || routerFuture.v7_relativeSplatPath === void 0)) {
      logDeprecation("v7_relativeSplatPath", "Relative route resolution within Splat routes is changing in v7", "https://reactrouter.com/v6/upgrading/future#v7_relativesplatpath");
    }
    if (routerFuture) {
      if (routerFuture.v7_fetcherPersist === void 0) {
        logDeprecation("v7_fetcherPersist", "The persistence behavior of fetchers is changing in v7", "https://reactrouter.com/v6/upgrading/future#v7_fetcherpersist");
      }
      if (routerFuture.v7_normalizeFormMethod === void 0) {
        logDeprecation("v7_normalizeFormMethod", "Casing of `formMethod` fields is being normalized to uppercase in v7", "https://reactrouter.com/v6/upgrading/future#v7_normalizeformmethod");
      }
      if (routerFuture.v7_partialHydration === void 0) {
        logDeprecation("v7_partialHydration", "`RouterProvider` hydration behavior is changing in v7", "https://reactrouter.com/v6/upgrading/future#v7_partialhydration");
      }
      if (routerFuture.v7_skipActionErrorRevalidation === void 0) {
        logDeprecation("v7_skipActionErrorRevalidation", "The revalidation behavior after 4xx/5xx `action` responses is changing in v7", "https://reactrouter.com/v6/upgrading/future#v7_skipactionerrorrevalidation");
      }
    }
  }
  var START_TRANSITION = "startTransition";
  var startTransitionImpl = React2[START_TRANSITION];
  function MemoryRouter(_ref3) {
    let {
      basename,
      children,
      initialEntries,
      initialIndex,
      future
    } = _ref3;
    let historyRef = React2.useRef();
    if (historyRef.current == null) {
      historyRef.current = createMemoryHistory({
        initialEntries,
        initialIndex,
        v5Compat: true
      });
    }
    let history2 = historyRef.current;
    let [state, setStateImpl] = React2.useState({
      action: history2.action,
      location: history2.location
    });
    let {
      v7_startTransition
    } = future || {};
    let setState = React2.useCallback((newState) => {
      v7_startTransition && startTransitionImpl ? startTransitionImpl(() => setStateImpl(newState)) : setStateImpl(newState);
    }, [setStateImpl, v7_startTransition]);
    React2.useLayoutEffect(() => history2.listen(setState), [history2, setState]);
    React2.useEffect(() => logV6DeprecationWarnings(future), [future]);
    return /* @__PURE__ */ React2.createElement(Router, {
      basename,
      children,
      location: state.location,
      navigationType: state.action,
      navigator: history2,
      future
    });
  }
  function Outlet(props) {
    return useOutlet(props.context);
  }
  function Router(_ref5) {
    let {
      basename: basenameProp = "/",
      children = null,
      location: locationProp,
      navigationType = Action.Pop,
      navigator: navigator2,
      static: staticProp = false,
      future
    } = _ref5;
    !!useInRouterContext() ? true ? invariant(false, "You cannot render a <Router> inside another <Router>. You should never have more than one in your app.") : invariant(false) : void 0;
    let basename = basenameProp.replace(/^\/*/, "/");
    let navigationContext = React2.useMemo(() => ({
      basename,
      navigator: navigator2,
      static: staticProp,
      future: _extends2({
        v7_relativeSplatPath: false
      }, future)
    }), [basename, future, navigator2, staticProp]);
    if (typeof locationProp === "string") {
      locationProp = parsePath(locationProp);
    }
    let {
      pathname = "/",
      search = "",
      hash: hash2 = "",
      state = null,
      key = "default"
    } = locationProp;
    let locationContext = React2.useMemo(() => {
      let trailingPathname = stripBasename(pathname, basename);
      if (trailingPathname == null) {
        return null;
      }
      return {
        location: {
          pathname: trailingPathname,
          search,
          hash: hash2,
          state,
          key
        },
        navigationType
      };
    }, [basename, pathname, search, hash2, state, key, navigationType]);
    true ? warning(locationContext != null, '<Router basename="' + basename + '"> is not able to match the URL ' + ('"' + pathname + search + hash2 + '" because it does not start with the ') + "basename, so the <Router> won't render anything.") : void 0;
    if (locationContext == null) {
      return null;
    }
    return /* @__PURE__ */ React2.createElement(NavigationContext.Provider, {
      value: navigationContext
    }, /* @__PURE__ */ React2.createElement(LocationContext.Provider, {
      children,
      value: locationContext
    }));
  }
  var neverSettledPromise = new Promise(() => {
  });

  // node_modules/react-router-dom/dist/index.js
  function _extends3() {
    _extends3 = Object.assign ? Object.assign.bind() : function(target) {
      for (var i = 1; i < arguments.length; i++) {
        var source = arguments[i];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    return _extends3.apply(this, arguments);
  }
  function _objectWithoutPropertiesLoose(source, excluded) {
    if (source == null) return {};
    var target = {};
    var sourceKeys = Object.keys(source);
    var key, i;
    for (i = 0; i < sourceKeys.length; i++) {
      key = sourceKeys[i];
      if (excluded.indexOf(key) >= 0) continue;
      target[key] = source[key];
    }
    return target;
  }
  var defaultMethod = "get";
  var defaultEncType = "application/x-www-form-urlencoded";
  function isHtmlElement(object) {
    return object != null && typeof object.tagName === "string";
  }
  function isButtonElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "button";
  }
  function isFormElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "form";
  }
  function isInputElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "input";
  }
  function isModifiedEvent(event) {
    return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
  }
  function shouldProcessLinkClick(event, target) {
    return event.button === 0 && // Ignore everything but left clicks
    (!target || target === "_self") && // Let browser handle "target=_blank" etc.
    !isModifiedEvent(event);
  }
  var _formDataSupportsSubmitter = null;
  function isFormDataSubmitterSupported() {
    if (_formDataSupportsSubmitter === null) {
      try {
        new FormData(
          document.createElement("form"),
          // @ts-expect-error if FormData supports the submitter parameter, this will throw
          0
        );
        _formDataSupportsSubmitter = false;
      } catch (e2) {
        _formDataSupportsSubmitter = true;
      }
    }
    return _formDataSupportsSubmitter;
  }
  var supportedFormEncTypes = /* @__PURE__ */ new Set(["application/x-www-form-urlencoded", "multipart/form-data", "text/plain"]);
  function getFormEncType(encType) {
    if (encType != null && !supportedFormEncTypes.has(encType)) {
      true ? warning(false, '"' + encType + '" is not a valid `encType` for `<Form>`/`<fetcher.Form>` ' + ('and will default to "' + defaultEncType + '"')) : void 0;
      return null;
    }
    return encType;
  }
  function getFormSubmissionInfo(target, basename) {
    let method;
    let action;
    let encType;
    let formData;
    let body;
    if (isFormElement(target)) {
      let attr = target.getAttribute("action");
      action = attr ? stripBasename(attr, basename) : null;
      method = target.getAttribute("method") || defaultMethod;
      encType = getFormEncType(target.getAttribute("enctype")) || defaultEncType;
      formData = new FormData(target);
    } else if (isButtonElement(target) || isInputElement(target) && (target.type === "submit" || target.type === "image")) {
      let form = target.form;
      if (form == null) {
        throw new Error('Cannot submit a <button> or <input type="submit"> without a <form>');
      }
      let attr = target.getAttribute("formaction") || form.getAttribute("action");
      action = attr ? stripBasename(attr, basename) : null;
      method = target.getAttribute("formmethod") || form.getAttribute("method") || defaultMethod;
      encType = getFormEncType(target.getAttribute("formenctype")) || getFormEncType(form.getAttribute("enctype")) || defaultEncType;
      formData = new FormData(form, target);
      if (!isFormDataSubmitterSupported()) {
        let {
          name,
          type,
          value
        } = target;
        if (type === "image") {
          let prefix = name ? name + "." : "";
          formData.append(prefix + "x", "0");
          formData.append(prefix + "y", "0");
        } else if (name) {
          formData.append(name, value);
        }
      }
    } else if (isHtmlElement(target)) {
      throw new Error('Cannot submit element that is not <form>, <button>, or <input type="submit|image">');
    } else {
      method = defaultMethod;
      action = null;
      encType = defaultEncType;
      body = target;
    }
    if (formData && encType === "text/plain") {
      body = formData;
      formData = void 0;
    }
    return {
      action,
      method: method.toLowerCase(),
      encType,
      formData,
      body
    };
  }
  var _excluded = ["onClick", "relative", "reloadDocument", "replace", "state", "target", "to", "preventScrollReset", "viewTransition"];
  var _excluded2 = ["aria-current", "caseSensitive", "className", "end", "style", "to", "viewTransition", "children"];
  var _excluded3 = ["fetcherKey", "navigate", "reloadDocument", "replace", "state", "method", "action", "onSubmit", "relative", "preventScrollReset", "viewTransition"];
  var REACT_ROUTER_VERSION = "6";
  try {
    window.__reactRouterVersion = REACT_ROUTER_VERSION;
  } catch (e2) {
  }
  var ViewTransitionContext = /* @__PURE__ */ React3.createContext({
    isTransitioning: false
  });
  if (true) {
    ViewTransitionContext.displayName = "ViewTransition";
  }
  var FetchersContext = /* @__PURE__ */ React3.createContext(/* @__PURE__ */ new Map());
  if (true) {
    FetchersContext.displayName = "Fetchers";
  }
  var START_TRANSITION2 = "startTransition";
  var startTransitionImpl2 = React3[START_TRANSITION2];
  var FLUSH_SYNC = "flushSync";
  var flushSyncImpl = ReactDOM[FLUSH_SYNC];
  var USE_ID = "useId";
  var useIdImpl = React3[USE_ID];
  function HistoryRouter(_ref6) {
    let {
      basename,
      children,
      future,
      history: history2
    } = _ref6;
    let [state, setStateImpl] = React3.useState({
      action: history2.action,
      location: history2.location
    });
    let {
      v7_startTransition
    } = future || {};
    let setState = React3.useCallback((newState) => {
      v7_startTransition && startTransitionImpl2 ? startTransitionImpl2(() => setStateImpl(newState)) : setStateImpl(newState);
    }, [setStateImpl, v7_startTransition]);
    React3.useLayoutEffect(() => history2.listen(setState), [history2, setState]);
    React3.useEffect(() => logV6DeprecationWarnings(future), [future]);
    return /* @__PURE__ */ React3.createElement(Router, {
      basename,
      children,
      location: state.location,
      navigationType: state.action,
      navigator: history2,
      future
    });
  }
  if (true) {
    HistoryRouter.displayName = "unstable_HistoryRouter";
  }
  var isBrowser = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
  var ABSOLUTE_URL_REGEX = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
  var Link = /* @__PURE__ */ React3.forwardRef(function LinkWithRef(_ref7, ref) {
    let {
      onClick,
      relative,
      reloadDocument,
      replace: replace2,
      state,
      target,
      to,
      preventScrollReset,
      viewTransition
    } = _ref7, rest = _objectWithoutPropertiesLoose(_ref7, _excluded);
    let {
      basename
    } = React3.useContext(NavigationContext);
    let absoluteHref;
    let isExternal = false;
    if (typeof to === "string" && ABSOLUTE_URL_REGEX.test(to)) {
      absoluteHref = to;
      if (isBrowser) {
        try {
          let currentUrl = new URL(window.location.href);
          let targetUrl = to.startsWith("//") ? new URL(currentUrl.protocol + to) : new URL(to);
          let path2 = stripBasename(targetUrl.pathname, basename);
          if (targetUrl.origin === currentUrl.origin && path2 != null) {
            to = path2 + targetUrl.search + targetUrl.hash;
          } else {
            isExternal = true;
          }
        } catch (e2) {
          true ? warning(false, '<Link to="' + to + '"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.') : void 0;
        }
      }
    }
    let href = useHref(to, {
      relative
    });
    let internalOnClick = useLinkClickHandler(to, {
      replace: replace2,
      state,
      target,
      preventScrollReset,
      relative,
      viewTransition
    });
    function handleClick(event) {
      if (onClick) onClick(event);
      if (!event.defaultPrevented) {
        internalOnClick(event);
      }
    }
    return (
      // eslint-disable-next-line jsx-a11y/anchor-has-content
      /* @__PURE__ */ React3.createElement("a", _extends3({}, rest, {
        href: absoluteHref || href,
        onClick: isExternal || reloadDocument ? onClick : handleClick,
        ref,
        target
      }))
    );
  });
  if (true) {
    Link.displayName = "Link";
  }
  var NavLink = /* @__PURE__ */ React3.forwardRef(function NavLinkWithRef(_ref8, ref) {
    let {
      "aria-current": ariaCurrentProp = "page",
      caseSensitive = false,
      className: classNameProp = "",
      end = false,
      style: styleProp,
      to,
      viewTransition,
      children
    } = _ref8, rest = _objectWithoutPropertiesLoose(_ref8, _excluded2);
    let path2 = useResolvedPath(to, {
      relative: rest.relative
    });
    let location2 = useLocation();
    let routerState = React3.useContext(DataRouterStateContext);
    let {
      navigator: navigator2,
      basename
    } = React3.useContext(NavigationContext);
    let isTransitioning = routerState != null && // Conditional usage is OK here because the usage of a data router is static
    // eslint-disable-next-line react-hooks/rules-of-hooks
    useViewTransitionState(path2) && viewTransition === true;
    let toPathname = navigator2.encodeLocation ? navigator2.encodeLocation(path2).pathname : path2.pathname;
    let locationPathname = location2.pathname;
    let nextLocationPathname = routerState && routerState.navigation && routerState.navigation.location ? routerState.navigation.location.pathname : null;
    if (!caseSensitive) {
      locationPathname = locationPathname.toLowerCase();
      nextLocationPathname = nextLocationPathname ? nextLocationPathname.toLowerCase() : null;
      toPathname = toPathname.toLowerCase();
    }
    if (nextLocationPathname && basename) {
      nextLocationPathname = stripBasename(nextLocationPathname, basename) || nextLocationPathname;
    }
    const endSlashPosition = toPathname !== "/" && toPathname.endsWith("/") ? toPathname.length - 1 : toPathname.length;
    let isActive = locationPathname === toPathname || !end && locationPathname.startsWith(toPathname) && locationPathname.charAt(endSlashPosition) === "/";
    let isPending = nextLocationPathname != null && (nextLocationPathname === toPathname || !end && nextLocationPathname.startsWith(toPathname) && nextLocationPathname.charAt(toPathname.length) === "/");
    let renderProps = {
      isActive,
      isPending,
      isTransitioning
    };
    let ariaCurrent = isActive ? ariaCurrentProp : void 0;
    let className;
    if (typeof classNameProp === "function") {
      className = classNameProp(renderProps);
    } else {
      className = [classNameProp, isActive ? "active" : null, isPending ? "pending" : null, isTransitioning ? "transitioning" : null].filter(Boolean).join(" ");
    }
    let style = typeof styleProp === "function" ? styleProp(renderProps) : styleProp;
    return /* @__PURE__ */ React3.createElement(Link, _extends3({}, rest, {
      "aria-current": ariaCurrent,
      className,
      ref,
      style,
      to,
      viewTransition
    }), typeof children === "function" ? children(renderProps) : children);
  });
  if (true) {
    NavLink.displayName = "NavLink";
  }
  var Form = /* @__PURE__ */ React3.forwardRef((_ref9, forwardedRef) => {
    let {
      fetcherKey,
      navigate,
      reloadDocument,
      replace: replace2,
      state,
      method = defaultMethod,
      action,
      onSubmit,
      relative,
      preventScrollReset,
      viewTransition
    } = _ref9, props = _objectWithoutPropertiesLoose(_ref9, _excluded3);
    let submit = useSubmit();
    let formAction = useFormAction(action, {
      relative
    });
    let formMethod = method.toLowerCase() === "get" ? "get" : "post";
    let submitHandler = (event) => {
      onSubmit && onSubmit(event);
      if (event.defaultPrevented) return;
      event.preventDefault();
      let submitter = event.nativeEvent.submitter;
      let submitMethod = (submitter == null ? void 0 : submitter.getAttribute("formmethod")) || method;
      submit(submitter || event.currentTarget, {
        fetcherKey,
        method: submitMethod,
        navigate,
        replace: replace2,
        state,
        relative,
        preventScrollReset,
        viewTransition
      });
    };
    return /* @__PURE__ */ React3.createElement("form", _extends3({
      ref: forwardedRef,
      method: formMethod,
      action: formAction,
      onSubmit: reloadDocument ? onSubmit : submitHandler
    }, props));
  });
  if (true) {
    Form.displayName = "Form";
  }
  function ScrollRestoration(_ref10) {
    let {
      getKey,
      storageKey
    } = _ref10;
    useScrollRestoration({
      getKey,
      storageKey
    });
    return null;
  }
  if (true) {
    ScrollRestoration.displayName = "ScrollRestoration";
  }
  var DataRouterHook2;
  (function(DataRouterHook3) {
    DataRouterHook3["UseScrollRestoration"] = "useScrollRestoration";
    DataRouterHook3["UseSubmit"] = "useSubmit";
    DataRouterHook3["UseSubmitFetcher"] = "useSubmitFetcher";
    DataRouterHook3["UseFetcher"] = "useFetcher";
    DataRouterHook3["useViewTransitionState"] = "useViewTransitionState";
  })(DataRouterHook2 || (DataRouterHook2 = {}));
  var DataRouterStateHook2;
  (function(DataRouterStateHook3) {
    DataRouterStateHook3["UseFetcher"] = "useFetcher";
    DataRouterStateHook3["UseFetchers"] = "useFetchers";
    DataRouterStateHook3["UseScrollRestoration"] = "useScrollRestoration";
  })(DataRouterStateHook2 || (DataRouterStateHook2 = {}));
  function getDataRouterConsoleError2(hookName) {
    return hookName + " must be used within a data router.  See https://reactrouter.com/v6/routers/picking-a-router.";
  }
  function useDataRouterContext2(hookName) {
    let ctx = React3.useContext(DataRouterContext);
    !ctx ? true ? invariant(false, getDataRouterConsoleError2(hookName)) : invariant(false) : void 0;
    return ctx;
  }
  function useDataRouterState2(hookName) {
    let state = React3.useContext(DataRouterStateContext);
    !state ? true ? invariant(false, getDataRouterConsoleError2(hookName)) : invariant(false) : void 0;
    return state;
  }
  function useLinkClickHandler(to, _temp) {
    let {
      target,
      replace: replaceProp,
      state,
      preventScrollReset,
      relative,
      viewTransition
    } = _temp === void 0 ? {} : _temp;
    let navigate = useNavigate();
    let location2 = useLocation();
    let path2 = useResolvedPath(to, {
      relative
    });
    return React3.useCallback((event) => {
      if (shouldProcessLinkClick(event, target)) {
        event.preventDefault();
        let replace2 = replaceProp !== void 0 ? replaceProp : createPath(location2) === createPath(path2);
        navigate(to, {
          replace: replace2,
          state,
          preventScrollReset,
          relative,
          viewTransition
        });
      }
    }, [location2, navigate, path2, replaceProp, state, target, to, preventScrollReset, relative, viewTransition]);
  }
  function validateClientSideSubmission() {
    if (typeof document === "undefined") {
      throw new Error("You are calling submit during the server render. Try calling submit within a `useEffect` or callback instead.");
    }
  }
  var fetcherId = 0;
  var getUniqueFetcherId = () => "__" + String(++fetcherId) + "__";
  function useSubmit() {
    let {
      router
    } = useDataRouterContext2(DataRouterHook2.UseSubmit);
    let {
      basename
    } = React3.useContext(NavigationContext);
    let currentRouteId = useRouteId();
    return React3.useCallback(function(target, options3) {
      if (options3 === void 0) {
        options3 = {};
      }
      validateClientSideSubmission();
      let {
        action,
        method,
        encType,
        formData,
        body
      } = getFormSubmissionInfo(target, basename);
      if (options3.navigate === false) {
        let key = options3.fetcherKey || getUniqueFetcherId();
        router.fetch(key, currentRouteId, options3.action || action, {
          preventScrollReset: options3.preventScrollReset,
          formData,
          body,
          formMethod: options3.method || method,
          formEncType: options3.encType || encType,
          flushSync: options3.flushSync
        });
      } else {
        router.navigate(options3.action || action, {
          preventScrollReset: options3.preventScrollReset,
          formData,
          body,
          formMethod: options3.method || method,
          formEncType: options3.encType || encType,
          replace: options3.replace,
          state: options3.state,
          fromRouteId: currentRouteId,
          flushSync: options3.flushSync,
          viewTransition: options3.viewTransition
        });
      }
    }, [router, basename, currentRouteId]);
  }
  function useFormAction(action, _temp2) {
    let {
      relative
    } = _temp2 === void 0 ? {} : _temp2;
    let {
      basename
    } = React3.useContext(NavigationContext);
    let routeContext = React3.useContext(RouteContext);
    !routeContext ? true ? invariant(false, "useFormAction must be used inside a RouteContext") : invariant(false) : void 0;
    let [match3] = routeContext.matches.slice(-1);
    let path2 = _extends3({}, useResolvedPath(action ? action : ".", {
      relative
    }));
    let location2 = useLocation();
    if (action == null) {
      path2.search = location2.search;
      let params = new URLSearchParams(path2.search);
      let indexValues = params.getAll("index");
      let hasNakedIndexParam = indexValues.some((v) => v === "");
      if (hasNakedIndexParam) {
        params.delete("index");
        indexValues.filter((v) => v).forEach((v) => params.append("index", v));
        let qs = params.toString();
        path2.search = qs ? "?" + qs : "";
      }
    }
    if ((!action || action === ".") && match3.route.index) {
      path2.search = path2.search ? path2.search.replace(/^\?/, "?index&") : "?index";
    }
    if (basename !== "/") {
      path2.pathname = path2.pathname === "/" ? basename : joinPaths([basename, path2.pathname]);
    }
    return createPath(path2);
  }
  var SCROLL_RESTORATION_STORAGE_KEY = "react-router-scroll-positions";
  var savedScrollPositions = {};
  function useScrollRestoration(_temp4) {
    let {
      getKey,
      storageKey
    } = _temp4 === void 0 ? {} : _temp4;
    let {
      router
    } = useDataRouterContext2(DataRouterHook2.UseScrollRestoration);
    let {
      restoreScrollPosition,
      preventScrollReset
    } = useDataRouterState2(DataRouterStateHook2.UseScrollRestoration);
    let {
      basename
    } = React3.useContext(NavigationContext);
    let location2 = useLocation();
    let matches = useMatches();
    let navigation = useNavigation();
    React3.useEffect(() => {
      window.history.scrollRestoration = "manual";
      return () => {
        window.history.scrollRestoration = "auto";
      };
    }, []);
    usePageHide(React3.useCallback(() => {
      if (navigation.state === "idle") {
        let key = (getKey ? getKey(location2, matches) : null) || location2.key;
        savedScrollPositions[key] = window.scrollY;
      }
      try {
        sessionStorage.setItem(storageKey || SCROLL_RESTORATION_STORAGE_KEY, JSON.stringify(savedScrollPositions));
      } catch (error2) {
        true ? warning(false, "Failed to save scroll positions in sessionStorage, <ScrollRestoration /> will not work properly (" + error2 + ").") : void 0;
      }
      window.history.scrollRestoration = "auto";
    }, [storageKey, getKey, navigation.state, location2, matches]));
    if (typeof document !== "undefined") {
      React3.useLayoutEffect(() => {
        try {
          let sessionPositions = sessionStorage.getItem(storageKey || SCROLL_RESTORATION_STORAGE_KEY);
          if (sessionPositions) {
            savedScrollPositions = JSON.parse(sessionPositions);
          }
        } catch (e2) {
        }
      }, [storageKey]);
      React3.useLayoutEffect(() => {
        let getKeyWithoutBasename = getKey && basename !== "/" ? (location3, matches2) => getKey(
          // Strip the basename to match useLocation()
          _extends3({}, location3, {
            pathname: stripBasename(location3.pathname, basename) || location3.pathname
          }),
          matches2
        ) : getKey;
        let disableScrollRestoration = router == null ? void 0 : router.enableScrollRestoration(savedScrollPositions, () => window.scrollY, getKeyWithoutBasename);
        return () => disableScrollRestoration && disableScrollRestoration();
      }, [router, basename, getKey]);
      React3.useLayoutEffect(() => {
        if (restoreScrollPosition === false) {
          return;
        }
        if (typeof restoreScrollPosition === "number") {
          window.scrollTo(0, restoreScrollPosition);
          return;
        }
        if (location2.hash) {
          let el = document.getElementById(decodeURIComponent(location2.hash.slice(1)));
          if (el) {
            el.scrollIntoView();
            return;
          }
        }
        if (preventScrollReset === true) {
          return;
        }
        window.scrollTo(0, 0);
      }, [location2, restoreScrollPosition, preventScrollReset]);
    }
  }
  function usePageHide(callback, options3) {
    let {
      capture
    } = options3 || {};
    React3.useEffect(() => {
      let opts = capture != null ? {
        capture
      } : void 0;
      window.addEventListener("pagehide", callback, opts);
      return () => {
        window.removeEventListener("pagehide", callback, opts);
      };
    }, [callback, capture]);
  }
  function useViewTransitionState(to, opts) {
    if (opts === void 0) {
      opts = {};
    }
    let vtContext = React3.useContext(ViewTransitionContext);
    !(vtContext != null) ? true ? invariant(false, "`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?") : invariant(false) : void 0;
    let {
      basename
    } = useDataRouterContext2(DataRouterHook2.useViewTransitionState);
    let path2 = useResolvedPath(to, {
      relative: opts.relative
    });
    if (!vtContext.isTransitioning) {
      return false;
    }
    let currentPath = stripBasename(vtContext.currentLocation.pathname, basename) || vtContext.currentLocation.pathname;
    let nextPath = stripBasename(vtContext.nextLocation.pathname, basename) || vtContext.nextLocation.pathname;
    return matchPath(path2.pathname, nextPath) != null || matchPath(path2.pathname, currentPath) != null;
  }

  // node_modules/@tanstack/react-query/build/lib/index.mjs
  init_define_import_meta_env();

  // node_modules/@tanstack/query-core/build/lib/index.mjs
  init_define_import_meta_env();

  // node_modules/@tanstack/query-core/build/lib/retryer.mjs
  init_define_import_meta_env();

  // node_modules/@tanstack/query-core/build/lib/focusManager.mjs
  init_define_import_meta_env();

  // node_modules/@tanstack/query-core/build/lib/subscribable.mjs
  init_define_import_meta_env();
  var Subscribable = class {
    constructor() {
      this.listeners = /* @__PURE__ */ new Set();
      this.subscribe = this.subscribe.bind(this);
    }
    subscribe(listener) {
      const identity = {
        listener
      };
      this.listeners.add(identity);
      this.onSubscribe();
      return () => {
        this.listeners.delete(identity);
        this.onUnsubscribe();
      };
    }
    hasListeners() {
      return this.listeners.size > 0;
    }
    onSubscribe() {
    }
    onUnsubscribe() {
    }
  };

  // node_modules/@tanstack/query-core/build/lib/utils.mjs
  init_define_import_meta_env();
  var isServer = typeof window === "undefined" || "Deno" in window;
  function noop2() {
    return void 0;
  }
  function functionalUpdate(updater, input) {
    return typeof updater === "function" ? updater(input) : updater;
  }
  function isValidTimeout(value) {
    return typeof value === "number" && value >= 0 && value !== Infinity;
  }
  function timeUntilStale(updatedAt, staleTime) {
    return Math.max(updatedAt + (staleTime || 0) - Date.now(), 0);
  }
  function parseQueryArgs(arg1, arg2, arg3) {
    if (!isQueryKey(arg1)) {
      return arg1;
    }
    if (typeof arg2 === "function") {
      return {
        ...arg3,
        queryKey: arg1,
        queryFn: arg2
      };
    }
    return {
      ...arg2,
      queryKey: arg1
    };
  }
  function parseFilterArgs(arg1, arg2, arg3) {
    return isQueryKey(arg1) ? [{
      ...arg2,
      queryKey: arg1
    }, arg3] : [arg1 || {}, arg2];
  }
  function matchQuery(filters, query) {
    const {
      type = "all",
      exact,
      fetchStatus,
      predicate,
      queryKey,
      stale
    } = filters;
    if (isQueryKey(queryKey)) {
      if (exact) {
        if (query.queryHash !== hashQueryKeyByOptions(queryKey, query.options)) {
          return false;
        }
      } else if (!partialMatchKey(query.queryKey, queryKey)) {
        return false;
      }
    }
    if (type !== "all") {
      const isActive = query.isActive();
      if (type === "active" && !isActive) {
        return false;
      }
      if (type === "inactive" && isActive) {
        return false;
      }
    }
    if (typeof stale === "boolean" && query.isStale() !== stale) {
      return false;
    }
    if (typeof fetchStatus !== "undefined" && fetchStatus !== query.state.fetchStatus) {
      return false;
    }
    if (predicate && !predicate(query)) {
      return false;
    }
    return true;
  }
  function matchMutation(filters, mutation) {
    const {
      exact,
      fetching,
      predicate,
      mutationKey
    } = filters;
    if (isQueryKey(mutationKey)) {
      if (!mutation.options.mutationKey) {
        return false;
      }
      if (exact) {
        if (hashQueryKey(mutation.options.mutationKey) !== hashQueryKey(mutationKey)) {
          return false;
        }
      } else if (!partialMatchKey(mutation.options.mutationKey, mutationKey)) {
        return false;
      }
    }
    if (typeof fetching === "boolean" && mutation.state.status === "loading" !== fetching) {
      return false;
    }
    if (predicate && !predicate(mutation)) {
      return false;
    }
    return true;
  }
  function hashQueryKeyByOptions(queryKey, options3) {
    const hashFn = (options3 == null ? void 0 : options3.queryKeyHashFn) || hashQueryKey;
    return hashFn(queryKey);
  }
  function hashQueryKey(queryKey) {
    return JSON.stringify(queryKey, (_, val) => isPlainObject(val) ? Object.keys(val).sort().reduce((result, key) => {
      result[key] = val[key];
      return result;
    }, {}) : val);
  }
  function partialMatchKey(a, b) {
    return partialDeepEqual(a, b);
  }
  function partialDeepEqual(a, b) {
    if (a === b) {
      return true;
    }
    if (typeof a !== typeof b) {
      return false;
    }
    if (a && b && typeof a === "object" && typeof b === "object") {
      return !Object.keys(b).some((key) => !partialDeepEqual(a[key], b[key]));
    }
    return false;
  }
  function replaceEqualDeep(a, b, depth = 0) {
    if (a === b) {
      return a;
    }
    if (depth > 500) return b;
    const array = isPlainArray(a) && isPlainArray(b);
    if (array || isPlainObject(a) && isPlainObject(b)) {
      const aSize = array ? a.length : Object.keys(a).length;
      const bItems = array ? b : Object.keys(b);
      const bSize = bItems.length;
      const copy2 = array ? [] : {};
      let equalItems = 0;
      for (let i = 0; i < bSize; i++) {
        const key = array ? i : bItems[i];
        copy2[key] = replaceEqualDeep(a[key], b[key], depth + 1);
        if (copy2[key] === a[key]) {
          equalItems++;
        }
      }
      return aSize === bSize && equalItems === aSize ? a : copy2;
    }
    return b;
  }
  function shallowEqualObjects(a, b) {
    if (a && !b || b && !a) {
      return false;
    }
    for (const key in a) {
      if (a[key] !== b[key]) {
        return false;
      }
    }
    return true;
  }
  function isPlainArray(value) {
    return Array.isArray(value) && value.length === Object.keys(value).length;
  }
  function isPlainObject(o) {
    if (!hasObjectPrototype(o)) {
      return false;
    }
    const ctor = o.constructor;
    if (typeof ctor === "undefined") {
      return true;
    }
    const prot = ctor.prototype;
    if (!hasObjectPrototype(prot)) {
      return false;
    }
    if (!prot.hasOwnProperty("isPrototypeOf")) {
      return false;
    }
    return true;
  }
  function hasObjectPrototype(o) {
    return Object.prototype.toString.call(o) === "[object Object]";
  }
  function isQueryKey(value) {
    return Array.isArray(value);
  }
  function sleep(timeout2) {
    return new Promise((resolve) => {
      setTimeout(resolve, timeout2);
    });
  }
  function scheduleMicrotask(callback) {
    sleep(0).then(callback);
  }
  function getAbortController() {
    if (typeof AbortController === "function") {
      return new AbortController();
    }
    return;
  }
  function replaceData(prevData, data, options3) {
    if (options3.isDataEqual != null && options3.isDataEqual(prevData, data)) {
      return prevData;
    } else if (typeof options3.structuralSharing === "function") {
      return options3.structuralSharing(prevData, data);
    } else if (options3.structuralSharing !== false) {
      return replaceEqualDeep(prevData, data);
    }
    return data;
  }

  // node_modules/@tanstack/query-core/build/lib/focusManager.mjs
  var FocusManager = class extends Subscribable {
    constructor() {
      super();
      this.setup = (onFocus) => {
        if (!isServer && window.addEventListener) {
          const listener = () => onFocus();
          window.addEventListener("visibilitychange", listener, false);
          window.addEventListener("focus", listener, false);
          return () => {
            window.removeEventListener("visibilitychange", listener);
            window.removeEventListener("focus", listener);
          };
        }
        return;
      };
    }
    onSubscribe() {
      if (!this.cleanup) {
        this.setEventListener(this.setup);
      }
    }
    onUnsubscribe() {
      if (!this.hasListeners()) {
        var _this$cleanup;
        (_this$cleanup = this.cleanup) == null ? void 0 : _this$cleanup.call(this);
        this.cleanup = void 0;
      }
    }
    setEventListener(setup) {
      var _this$cleanup2;
      this.setup = setup;
      (_this$cleanup2 = this.cleanup) == null ? void 0 : _this$cleanup2.call(this);
      this.cleanup = setup((focused) => {
        if (typeof focused === "boolean") {
          this.setFocused(focused);
        } else {
          this.onFocus();
        }
      });
    }
    setFocused(focused) {
      const changed = this.focused !== focused;
      if (changed) {
        this.focused = focused;
        this.onFocus();
      }
    }
    onFocus() {
      this.listeners.forEach(({
        listener
      }) => {
        listener();
      });
    }
    isFocused() {
      if (typeof this.focused === "boolean") {
        return this.focused;
      }
      if (typeof document === "undefined") {
        return true;
      }
      return [void 0, "visible", "prerender"].includes(document.visibilityState);
    }
  };
  var focusManager = new FocusManager();

  // node_modules/@tanstack/query-core/build/lib/onlineManager.mjs
  init_define_import_meta_env();
  var onlineEvents = ["online", "offline"];
  var OnlineManager = class extends Subscribable {
    constructor() {
      super();
      this.setup = (onOnline) => {
        if (!isServer && window.addEventListener) {
          const listener = () => onOnline();
          onlineEvents.forEach((event) => {
            window.addEventListener(event, listener, false);
          });
          return () => {
            onlineEvents.forEach((event) => {
              window.removeEventListener(event, listener);
            });
          };
        }
        return;
      };
    }
    onSubscribe() {
      if (!this.cleanup) {
        this.setEventListener(this.setup);
      }
    }
    onUnsubscribe() {
      if (!this.hasListeners()) {
        var _this$cleanup;
        (_this$cleanup = this.cleanup) == null ? void 0 : _this$cleanup.call(this);
        this.cleanup = void 0;
      }
    }
    setEventListener(setup) {
      var _this$cleanup2;
      this.setup = setup;
      (_this$cleanup2 = this.cleanup) == null ? void 0 : _this$cleanup2.call(this);
      this.cleanup = setup((online) => {
        if (typeof online === "boolean") {
          this.setOnline(online);
        } else {
          this.onOnline();
        }
      });
    }
    setOnline(online) {
      const changed = this.online !== online;
      if (changed) {
        this.online = online;
        this.onOnline();
      }
    }
    onOnline() {
      this.listeners.forEach(({
        listener
      }) => {
        listener();
      });
    }
    isOnline() {
      if (typeof this.online === "boolean") {
        return this.online;
      }
      if (typeof navigator === "undefined" || typeof navigator.onLine === "undefined") {
        return true;
      }
      return navigator.onLine;
    }
  };
  var onlineManager = new OnlineManager();

  // node_modules/@tanstack/query-core/build/lib/retryer.mjs
  function defaultRetryDelay(failureCount) {
    return Math.min(1e3 * 2 ** failureCount, 3e4);
  }
  function canFetch(networkMode) {
    return (networkMode != null ? networkMode : "online") === "online" ? onlineManager.isOnline() : true;
  }
  var CancelledError = class {
    constructor(options3) {
      this.revert = options3 == null ? void 0 : options3.revert;
      this.silent = options3 == null ? void 0 : options3.silent;
    }
  };
  function isCancelledError(value) {
    return value instanceof CancelledError;
  }
  function createRetryer(config) {
    let isRetryCancelled = false;
    let failureCount = 0;
    let isResolved = false;
    let continueFn;
    let promiseResolve;
    let promiseReject;
    const promise = new Promise((outerResolve, outerReject) => {
      promiseResolve = outerResolve;
      promiseReject = outerReject;
    });
    const cancel = (cancelOptions) => {
      if (!isResolved) {
        reject(new CancelledError(cancelOptions));
        config.abort == null ? void 0 : config.abort();
      }
    };
    const cancelRetry = () => {
      isRetryCancelled = true;
    };
    const continueRetry = () => {
      isRetryCancelled = false;
    };
    const shouldPause = () => !focusManager.isFocused() || config.networkMode !== "always" && !onlineManager.isOnline();
    const resolve = (value) => {
      if (!isResolved) {
        isResolved = true;
        config.onSuccess == null ? void 0 : config.onSuccess(value);
        continueFn == null ? void 0 : continueFn();
        promiseResolve(value);
      }
    };
    const reject = (value) => {
      if (!isResolved) {
        isResolved = true;
        config.onError == null ? void 0 : config.onError(value);
        continueFn == null ? void 0 : continueFn();
        promiseReject(value);
      }
    };
    const pause = () => {
      return new Promise((continueResolve) => {
        continueFn = (value) => {
          const canContinue = isResolved || !shouldPause();
          if (canContinue) {
            continueResolve(value);
          }
          return canContinue;
        };
        config.onPause == null ? void 0 : config.onPause();
      }).then(() => {
        continueFn = void 0;
        if (!isResolved) {
          config.onContinue == null ? void 0 : config.onContinue();
        }
      });
    };
    const run = () => {
      if (isResolved) {
        return;
      }
      let promiseOrValue;
      try {
        promiseOrValue = config.fn();
      } catch (error2) {
        promiseOrValue = Promise.reject(error2);
      }
      Promise.resolve(promiseOrValue).then(resolve).catch((error2) => {
        var _config$retry, _config$retryDelay;
        if (isResolved) {
          return;
        }
        const retry = (_config$retry = config.retry) != null ? _config$retry : 3;
        const retryDelay = (_config$retryDelay = config.retryDelay) != null ? _config$retryDelay : defaultRetryDelay;
        const delay = typeof retryDelay === "function" ? retryDelay(failureCount, error2) : retryDelay;
        const shouldRetry = retry === true || typeof retry === "number" && failureCount < retry || typeof retry === "function" && retry(failureCount, error2);
        if (isRetryCancelled || !shouldRetry) {
          reject(error2);
          return;
        }
        failureCount++;
        config.onFail == null ? void 0 : config.onFail(failureCount, error2);
        sleep(delay).then(() => {
          if (shouldPause()) {
            return pause();
          }
          return;
        }).then(() => {
          if (isRetryCancelled) {
            reject(error2);
          } else {
            run();
          }
        });
      });
    };
    if (canFetch(config.networkMode)) {
      run();
    } else {
      pause().then(run);
    }
    return {
      promise,
      cancel,
      continue: () => {
        const didContinue = continueFn == null ? void 0 : continueFn();
        return didContinue ? promise : Promise.resolve();
      },
      cancelRetry,
      continueRetry
    };
  }

  // node_modules/@tanstack/query-core/build/lib/queryCache.mjs
  init_define_import_meta_env();

  // node_modules/@tanstack/query-core/build/lib/query.mjs
  init_define_import_meta_env();

  // node_modules/@tanstack/query-core/build/lib/logger.mjs
  init_define_import_meta_env();
  var defaultLogger = console;

  // node_modules/@tanstack/query-core/build/lib/notifyManager.mjs
  init_define_import_meta_env();
  function createNotifyManager() {
    let queue = [];
    let transactions = 0;
    let notifyFn = (callback) => {
      callback();
    };
    let batchNotifyFn = (callback) => {
      callback();
    };
    const batch = (callback) => {
      let result;
      transactions++;
      try {
        result = callback();
      } finally {
        transactions--;
        if (!transactions) {
          flush();
        }
      }
      return result;
    };
    const schedule = (callback) => {
      if (transactions) {
        queue.push(callback);
      } else {
        scheduleMicrotask(() => {
          notifyFn(callback);
        });
      }
    };
    const batchCalls = (callback) => {
      return (...args) => {
        schedule(() => {
          callback(...args);
        });
      };
    };
    const flush = () => {
      const originalQueue = queue;
      queue = [];
      if (originalQueue.length) {
        scheduleMicrotask(() => {
          batchNotifyFn(() => {
            originalQueue.forEach((callback) => {
              notifyFn(callback);
            });
          });
        });
      }
    };
    const setNotifyFunction = (fn) => {
      notifyFn = fn;
    };
    const setBatchNotifyFunction = (fn) => {
      batchNotifyFn = fn;
    };
    return {
      batch,
      batchCalls,
      schedule,
      setNotifyFunction,
      setBatchNotifyFunction
    };
  }
  var notifyManager = createNotifyManager();

  // node_modules/@tanstack/query-core/build/lib/removable.mjs
  init_define_import_meta_env();
  var Removable = class {
    destroy() {
      this.clearGcTimeout();
    }
    scheduleGc() {
      this.clearGcTimeout();
      if (isValidTimeout(this.cacheTime)) {
        this.gcTimeout = setTimeout(() => {
          this.optionalRemove();
        }, this.cacheTime);
      }
    }
    updateCacheTime(newCacheTime) {
      this.cacheTime = Math.max(this.cacheTime || 0, newCacheTime != null ? newCacheTime : isServer ? Infinity : 5 * 60 * 1e3);
    }
    clearGcTimeout() {
      if (this.gcTimeout) {
        clearTimeout(this.gcTimeout);
        this.gcTimeout = void 0;
      }
    }
  };

  // node_modules/@tanstack/query-core/build/lib/query.mjs
  var Query = class extends Removable {
    constructor(config) {
      super();
      this.abortSignalConsumed = false;
      this.defaultOptions = config.defaultOptions;
      this.setOptions(config.options);
      this.observers = [];
      this.cache = config.cache;
      this.logger = config.logger || defaultLogger;
      this.queryKey = config.queryKey;
      this.queryHash = config.queryHash;
      this.initialState = config.state || getDefaultState(this.options);
      this.state = this.initialState;
      this.scheduleGc();
    }
    get meta() {
      return this.options.meta;
    }
    setOptions(options3) {
      this.options = {
        ...this.defaultOptions,
        ...options3
      };
      this.updateCacheTime(this.options.cacheTime);
    }
    optionalRemove() {
      if (!this.observers.length && this.state.fetchStatus === "idle") {
        this.cache.remove(this);
      }
    }
    setData(newData, options3) {
      const data = replaceData(this.state.data, newData, this.options);
      this.dispatch({
        data,
        type: "success",
        dataUpdatedAt: options3 == null ? void 0 : options3.updatedAt,
        manual: options3 == null ? void 0 : options3.manual
      });
      return data;
    }
    setState(state, setStateOptions) {
      this.dispatch({
        type: "setState",
        state,
        setStateOptions
      });
    }
    cancel(options3) {
      var _this$retryer;
      const promise = this.promise;
      (_this$retryer = this.retryer) == null ? void 0 : _this$retryer.cancel(options3);
      return promise ? promise.then(noop2).catch(noop2) : Promise.resolve();
    }
    destroy() {
      super.destroy();
      this.cancel({
        silent: true
      });
    }
    reset() {
      this.destroy();
      this.setState(this.initialState);
    }
    isActive() {
      return this.observers.some((observer) => observer.options.enabled !== false);
    }
    isDisabled() {
      return this.getObserversCount() > 0 && !this.isActive();
    }
    isStale() {
      return this.state.isInvalidated || !this.state.dataUpdatedAt || this.observers.some((observer) => observer.getCurrentResult().isStale);
    }
    isStaleByTime(staleTime = 0) {
      return this.state.isInvalidated || !this.state.dataUpdatedAt || !timeUntilStale(this.state.dataUpdatedAt, staleTime);
    }
    onFocus() {
      var _this$retryer2;
      const observer = this.observers.find((x) => x.shouldFetchOnWindowFocus());
      if (observer) {
        observer.refetch({
          cancelRefetch: false
        });
      }
      (_this$retryer2 = this.retryer) == null ? void 0 : _this$retryer2.continue();
    }
    onOnline() {
      var _this$retryer3;
      const observer = this.observers.find((x) => x.shouldFetchOnReconnect());
      if (observer) {
        observer.refetch({
          cancelRefetch: false
        });
      }
      (_this$retryer3 = this.retryer) == null ? void 0 : _this$retryer3.continue();
    }
    addObserver(observer) {
      if (!this.observers.includes(observer)) {
        this.observers.push(observer);
        this.clearGcTimeout();
        this.cache.notify({
          type: "observerAdded",
          query: this,
          observer
        });
      }
    }
    removeObserver(observer) {
      if (this.observers.includes(observer)) {
        this.observers = this.observers.filter((x) => x !== observer);
        if (!this.observers.length) {
          if (this.retryer) {
            if (this.abortSignalConsumed) {
              this.retryer.cancel({
                revert: true
              });
            } else {
              this.retryer.cancelRetry();
            }
          }
          this.scheduleGc();
        }
        this.cache.notify({
          type: "observerRemoved",
          query: this,
          observer
        });
      }
    }
    getObserversCount() {
      return this.observers.length;
    }
    invalidate() {
      if (!this.state.isInvalidated) {
        this.dispatch({
          type: "invalidate"
        });
      }
    }
    fetch(options3, fetchOptions) {
      var _this$options$behavio, _context$fetchOptions;
      if (this.state.fetchStatus !== "idle") {
        if (this.state.dataUpdatedAt && fetchOptions != null && fetchOptions.cancelRefetch) {
          this.cancel({
            silent: true
          });
        } else if (this.promise) {
          var _this$retryer4;
          (_this$retryer4 = this.retryer) == null ? void 0 : _this$retryer4.continueRetry();
          return this.promise;
        }
      }
      if (options3) {
        this.setOptions(options3);
      }
      if (!this.options.queryFn) {
        const observer = this.observers.find((x) => x.options.queryFn);
        if (observer) {
          this.setOptions(observer.options);
        }
      }
      if (true) {
        if (!Array.isArray(this.options.queryKey)) {
          this.logger.error("As of v4, queryKey needs to be an Array. If you are using a string like 'repoData', please change it to an Array, e.g. ['repoData']");
        }
      }
      const abortController = getAbortController();
      const queryFnContext = {
        queryKey: this.queryKey,
        pageParam: void 0,
        meta: this.meta
      };
      const addSignalProperty = (object) => {
        Object.defineProperty(object, "signal", {
          enumerable: true,
          get: () => {
            if (abortController) {
              this.abortSignalConsumed = true;
              return abortController.signal;
            }
            return void 0;
          }
        });
      };
      addSignalProperty(queryFnContext);
      const fetchFn = () => {
        if (!this.options.queryFn) {
          return Promise.reject("Missing queryFn for queryKey '" + this.options.queryHash + "'");
        }
        this.abortSignalConsumed = false;
        return this.options.queryFn(queryFnContext);
      };
      const context2 = {
        fetchOptions,
        options: this.options,
        queryKey: this.queryKey,
        state: this.state,
        fetchFn
      };
      addSignalProperty(context2);
      (_this$options$behavio = this.options.behavior) == null ? void 0 : _this$options$behavio.onFetch(context2);
      this.revertState = this.state;
      if (this.state.fetchStatus === "idle" || this.state.fetchMeta !== ((_context$fetchOptions = context2.fetchOptions) == null ? void 0 : _context$fetchOptions.meta)) {
        var _context$fetchOptions2;
        this.dispatch({
          type: "fetch",
          meta: (_context$fetchOptions2 = context2.fetchOptions) == null ? void 0 : _context$fetchOptions2.meta
        });
      }
      const onError = (error2) => {
        if (!(isCancelledError(error2) && error2.silent)) {
          this.dispatch({
            type: "error",
            error: error2
          });
        }
        if (!isCancelledError(error2)) {
          var _this$cache$config$on, _this$cache$config, _this$cache$config$on2, _this$cache$config2;
          (_this$cache$config$on = (_this$cache$config = this.cache.config).onError) == null ? void 0 : _this$cache$config$on.call(_this$cache$config, error2, this);
          (_this$cache$config$on2 = (_this$cache$config2 = this.cache.config).onSettled) == null ? void 0 : _this$cache$config$on2.call(_this$cache$config2, this.state.data, error2, this);
          if (true) {
            this.logger.error(error2);
          }
        }
        if (!this.isFetchingOptimistic) {
          this.scheduleGc();
        }
        this.isFetchingOptimistic = false;
      };
      this.retryer = createRetryer({
        fn: context2.fetchFn,
        abort: abortController == null ? void 0 : abortController.abort.bind(abortController),
        onSuccess: (data) => {
          var _this$cache$config$on3, _this$cache$config3, _this$cache$config$on4, _this$cache$config4;
          if (typeof data === "undefined") {
            if (true) {
              this.logger.error("Query data cannot be undefined. Please make sure to return a value other than undefined from your query function. Affected query key: " + this.queryHash);
            }
            onError(new Error(this.queryHash + " data is undefined"));
            return;
          }
          this.setData(data);
          (_this$cache$config$on3 = (_this$cache$config3 = this.cache.config).onSuccess) == null ? void 0 : _this$cache$config$on3.call(_this$cache$config3, data, this);
          (_this$cache$config$on4 = (_this$cache$config4 = this.cache.config).onSettled) == null ? void 0 : _this$cache$config$on4.call(_this$cache$config4, data, this.state.error, this);
          if (!this.isFetchingOptimistic) {
            this.scheduleGc();
          }
          this.isFetchingOptimistic = false;
        },
        onError,
        onFail: (failureCount, error2) => {
          this.dispatch({
            type: "failed",
            failureCount,
            error: error2
          });
        },
        onPause: () => {
          this.dispatch({
            type: "pause"
          });
        },
        onContinue: () => {
          this.dispatch({
            type: "continue"
          });
        },
        retry: context2.options.retry,
        retryDelay: context2.options.retryDelay,
        networkMode: context2.options.networkMode
      });
      this.promise = this.retryer.promise;
      return this.promise;
    }
    dispatch(action) {
      const reducer2 = (state) => {
        var _action$meta, _action$dataUpdatedAt;
        switch (action.type) {
          case "failed":
            return {
              ...state,
              fetchFailureCount: action.failureCount,
              fetchFailureReason: action.error
            };
          case "pause":
            return {
              ...state,
              fetchStatus: "paused"
            };
          case "continue":
            return {
              ...state,
              fetchStatus: "fetching"
            };
          case "fetch":
            return {
              ...state,
              fetchFailureCount: 0,
              fetchFailureReason: null,
              fetchMeta: (_action$meta = action.meta) != null ? _action$meta : null,
              fetchStatus: canFetch(this.options.networkMode) ? "fetching" : "paused",
              ...!state.dataUpdatedAt && {
                error: null,
                status: "loading"
              }
            };
          case "success":
            return {
              ...state,
              data: action.data,
              dataUpdateCount: state.dataUpdateCount + 1,
              dataUpdatedAt: (_action$dataUpdatedAt = action.dataUpdatedAt) != null ? _action$dataUpdatedAt : Date.now(),
              error: null,
              isInvalidated: false,
              status: "success",
              ...!action.manual && {
                fetchStatus: "idle",
                fetchFailureCount: 0,
                fetchFailureReason: null
              }
            };
          case "error":
            const error2 = action.error;
            if (isCancelledError(error2) && error2.revert && this.revertState) {
              return {
                ...this.revertState,
                fetchStatus: "idle"
              };
            }
            return {
              ...state,
              error: error2,
              errorUpdateCount: state.errorUpdateCount + 1,
              errorUpdatedAt: Date.now(),
              fetchFailureCount: state.fetchFailureCount + 1,
              fetchFailureReason: error2,
              fetchStatus: "idle",
              status: "error"
            };
          case "invalidate":
            return {
              ...state,
              isInvalidated: true
            };
          case "setState":
            return {
              ...state,
              ...action.state
            };
        }
      };
      this.state = reducer2(this.state);
      notifyManager.batch(() => {
        this.observers.forEach((observer) => {
          observer.onQueryUpdate(action);
        });
        this.cache.notify({
          query: this,
          type: "updated",
          action
        });
      });
    }
  };
  function getDefaultState(options3) {
    const data = typeof options3.initialData === "function" ? options3.initialData() : options3.initialData;
    const hasData = typeof data !== "undefined";
    const initialDataUpdatedAt = hasData ? typeof options3.initialDataUpdatedAt === "function" ? options3.initialDataUpdatedAt() : options3.initialDataUpdatedAt : 0;
    return {
      data,
      dataUpdateCount: 0,
      dataUpdatedAt: hasData ? initialDataUpdatedAt != null ? initialDataUpdatedAt : Date.now() : 0,
      error: null,
      errorUpdateCount: 0,
      errorUpdatedAt: 0,
      fetchFailureCount: 0,
      fetchFailureReason: null,
      fetchMeta: null,
      isInvalidated: false,
      status: hasData ? "success" : "loading",
      fetchStatus: "idle"
    };
  }

  // node_modules/@tanstack/query-core/build/lib/queryCache.mjs
  var QueryCache = class extends Subscribable {
    constructor(config) {
      super();
      this.config = config || {};
      this.queries = [];
      this.queriesMap = {};
    }
    build(client, options3, state) {
      var _options$queryHash;
      const queryKey = options3.queryKey;
      const queryHash = (_options$queryHash = options3.queryHash) != null ? _options$queryHash : hashQueryKeyByOptions(queryKey, options3);
      let query = this.get(queryHash);
      if (!query) {
        query = new Query({
          cache: this,
          logger: client.getLogger(),
          queryKey,
          queryHash,
          options: client.defaultQueryOptions(options3),
          state,
          defaultOptions: client.getQueryDefaults(queryKey)
        });
        this.add(query);
      }
      return query;
    }
    add(query) {
      if (!this.queriesMap[query.queryHash]) {
        this.queriesMap[query.queryHash] = query;
        this.queries.push(query);
        this.notify({
          type: "added",
          query
        });
      }
    }
    remove(query) {
      const queryInMap = this.queriesMap[query.queryHash];
      if (queryInMap) {
        query.destroy();
        this.queries = this.queries.filter((x) => x !== query);
        if (queryInMap === query) {
          delete this.queriesMap[query.queryHash];
        }
        this.notify({
          type: "removed",
          query
        });
      }
    }
    clear() {
      notifyManager.batch(() => {
        this.queries.forEach((query) => {
          this.remove(query);
        });
      });
    }
    get(queryHash) {
      return this.queriesMap[queryHash];
    }
    getAll() {
      return this.queries;
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    find(arg1, arg2) {
      const [filters] = parseFilterArgs(arg1, arg2);
      if (typeof filters.exact === "undefined") {
        filters.exact = true;
      }
      return this.queries.find((query) => matchQuery(filters, query));
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    findAll(arg1, arg2) {
      const [filters] = parseFilterArgs(arg1, arg2);
      return Object.keys(filters).length > 0 ? this.queries.filter((query) => matchQuery(filters, query)) : this.queries;
    }
    notify(event) {
      notifyManager.batch(() => {
        this.listeners.forEach(({
          listener
        }) => {
          listener(event);
        });
      });
    }
    onFocus() {
      notifyManager.batch(() => {
        this.queries.forEach((query) => {
          query.onFocus();
        });
      });
    }
    onOnline() {
      notifyManager.batch(() => {
        this.queries.forEach((query) => {
          query.onOnline();
        });
      });
    }
  };

  // node_modules/@tanstack/query-core/build/lib/queryClient.mjs
  init_define_import_meta_env();

  // node_modules/@tanstack/query-core/build/lib/mutationCache.mjs
  init_define_import_meta_env();

  // node_modules/@tanstack/query-core/build/lib/mutation.mjs
  init_define_import_meta_env();
  var Mutation = class extends Removable {
    constructor(config) {
      super();
      this.defaultOptions = config.defaultOptions;
      this.mutationId = config.mutationId;
      this.mutationCache = config.mutationCache;
      this.logger = config.logger || defaultLogger;
      this.observers = [];
      this.state = config.state || getDefaultState2();
      this.setOptions(config.options);
      this.scheduleGc();
    }
    setOptions(options3) {
      this.options = {
        ...this.defaultOptions,
        ...options3
      };
      this.updateCacheTime(this.options.cacheTime);
    }
    get meta() {
      return this.options.meta;
    }
    setState(state) {
      this.dispatch({
        type: "setState",
        state
      });
    }
    addObserver(observer) {
      if (!this.observers.includes(observer)) {
        this.observers.push(observer);
        this.clearGcTimeout();
        this.mutationCache.notify({
          type: "observerAdded",
          mutation: this,
          observer
        });
      }
    }
    removeObserver(observer) {
      this.observers = this.observers.filter((x) => x !== observer);
      this.scheduleGc();
      this.mutationCache.notify({
        type: "observerRemoved",
        mutation: this,
        observer
      });
    }
    optionalRemove() {
      if (!this.observers.length) {
        if (this.state.status === "loading") {
          this.scheduleGc();
        } else {
          this.mutationCache.remove(this);
        }
      }
    }
    continue() {
      var _this$retryer$continu, _this$retryer;
      return (_this$retryer$continu = (_this$retryer = this.retryer) == null ? void 0 : _this$retryer.continue()) != null ? _this$retryer$continu : this.execute();
    }
    async execute() {
      const executeMutation = () => {
        var _this$options$retry;
        this.retryer = createRetryer({
          fn: () => {
            if (!this.options.mutationFn) {
              return Promise.reject("No mutationFn found");
            }
            return this.options.mutationFn(this.state.variables);
          },
          onFail: (failureCount, error2) => {
            this.dispatch({
              type: "failed",
              failureCount,
              error: error2
            });
          },
          onPause: () => {
            this.dispatch({
              type: "pause"
            });
          },
          onContinue: () => {
            this.dispatch({
              type: "continue"
            });
          },
          retry: (_this$options$retry = this.options.retry) != null ? _this$options$retry : 0,
          retryDelay: this.options.retryDelay,
          networkMode: this.options.networkMode
        });
        return this.retryer.promise;
      };
      const restored = this.state.status === "loading";
      try {
        var _this$mutationCache$c3, _this$mutationCache$c4, _this$options$onSucce, _this$options2, _this$mutationCache$c5, _this$mutationCache$c6, _this$options$onSettl, _this$options3;
        if (!restored) {
          var _this$mutationCache$c, _this$mutationCache$c2, _this$options$onMutat, _this$options;
          this.dispatch({
            type: "loading",
            variables: this.options.variables
          });
          await ((_this$mutationCache$c = (_this$mutationCache$c2 = this.mutationCache.config).onMutate) == null ? void 0 : _this$mutationCache$c.call(_this$mutationCache$c2, this.state.variables, this));
          const context2 = await ((_this$options$onMutat = (_this$options = this.options).onMutate) == null ? void 0 : _this$options$onMutat.call(_this$options, this.state.variables));
          if (context2 !== this.state.context) {
            this.dispatch({
              type: "loading",
              context: context2,
              variables: this.state.variables
            });
          }
        }
        const data = await executeMutation();
        await ((_this$mutationCache$c3 = (_this$mutationCache$c4 = this.mutationCache.config).onSuccess) == null ? void 0 : _this$mutationCache$c3.call(_this$mutationCache$c4, data, this.state.variables, this.state.context, this));
        await ((_this$options$onSucce = (_this$options2 = this.options).onSuccess) == null ? void 0 : _this$options$onSucce.call(_this$options2, data, this.state.variables, this.state.context));
        await ((_this$mutationCache$c5 = (_this$mutationCache$c6 = this.mutationCache.config).onSettled) == null ? void 0 : _this$mutationCache$c5.call(_this$mutationCache$c6, data, null, this.state.variables, this.state.context, this));
        await ((_this$options$onSettl = (_this$options3 = this.options).onSettled) == null ? void 0 : _this$options$onSettl.call(_this$options3, data, null, this.state.variables, this.state.context));
        this.dispatch({
          type: "success",
          data
        });
        return data;
      } catch (error2) {
        try {
          var _this$mutationCache$c7, _this$mutationCache$c8, _this$options$onError, _this$options4, _this$mutationCache$c9, _this$mutationCache$c10, _this$options$onSettl2, _this$options5;
          await ((_this$mutationCache$c7 = (_this$mutationCache$c8 = this.mutationCache.config).onError) == null ? void 0 : _this$mutationCache$c7.call(_this$mutationCache$c8, error2, this.state.variables, this.state.context, this));
          if (true) {
            this.logger.error(error2);
          }
          await ((_this$options$onError = (_this$options4 = this.options).onError) == null ? void 0 : _this$options$onError.call(_this$options4, error2, this.state.variables, this.state.context));
          await ((_this$mutationCache$c9 = (_this$mutationCache$c10 = this.mutationCache.config).onSettled) == null ? void 0 : _this$mutationCache$c9.call(_this$mutationCache$c10, void 0, error2, this.state.variables, this.state.context, this));
          await ((_this$options$onSettl2 = (_this$options5 = this.options).onSettled) == null ? void 0 : _this$options$onSettl2.call(_this$options5, void 0, error2, this.state.variables, this.state.context));
          throw error2;
        } finally {
          this.dispatch({
            type: "error",
            error: error2
          });
        }
      }
    }
    dispatch(action) {
      const reducer2 = (state) => {
        switch (action.type) {
          case "failed":
            return {
              ...state,
              failureCount: action.failureCount,
              failureReason: action.error
            };
          case "pause":
            return {
              ...state,
              isPaused: true
            };
          case "continue":
            return {
              ...state,
              isPaused: false
            };
          case "loading":
            return {
              ...state,
              context: action.context,
              data: void 0,
              failureCount: 0,
              failureReason: null,
              error: null,
              isPaused: !canFetch(this.options.networkMode),
              status: "loading",
              variables: action.variables
            };
          case "success":
            return {
              ...state,
              data: action.data,
              failureCount: 0,
              failureReason: null,
              error: null,
              status: "success",
              isPaused: false
            };
          case "error":
            return {
              ...state,
              data: void 0,
              error: action.error,
              failureCount: state.failureCount + 1,
              failureReason: action.error,
              isPaused: false,
              status: "error"
            };
          case "setState":
            return {
              ...state,
              ...action.state
            };
        }
      };
      this.state = reducer2(this.state);
      notifyManager.batch(() => {
        this.observers.forEach((observer) => {
          observer.onMutationUpdate(action);
        });
        this.mutationCache.notify({
          mutation: this,
          type: "updated",
          action
        });
      });
    }
  };
  function getDefaultState2() {
    return {
      context: void 0,
      data: void 0,
      error: null,
      failureCount: 0,
      failureReason: null,
      isPaused: false,
      status: "idle",
      variables: void 0
    };
  }

  // node_modules/@tanstack/query-core/build/lib/mutationCache.mjs
  var MutationCache = class extends Subscribable {
    constructor(config) {
      super();
      this.config = config || {};
      this.mutations = [];
      this.mutationId = 0;
    }
    build(client, options3, state) {
      const mutation = new Mutation({
        mutationCache: this,
        logger: client.getLogger(),
        mutationId: ++this.mutationId,
        options: client.defaultMutationOptions(options3),
        state,
        defaultOptions: options3.mutationKey ? client.getMutationDefaults(options3.mutationKey) : void 0
      });
      this.add(mutation);
      return mutation;
    }
    add(mutation) {
      this.mutations.push(mutation);
      this.notify({
        type: "added",
        mutation
      });
    }
    remove(mutation) {
      this.mutations = this.mutations.filter((x) => x !== mutation);
      this.notify({
        type: "removed",
        mutation
      });
    }
    clear() {
      notifyManager.batch(() => {
        this.mutations.forEach((mutation) => {
          this.remove(mutation);
        });
      });
    }
    getAll() {
      return this.mutations;
    }
    find(filters) {
      if (typeof filters.exact === "undefined") {
        filters.exact = true;
      }
      return this.mutations.find((mutation) => matchMutation(filters, mutation));
    }
    findAll(filters) {
      return this.mutations.filter((mutation) => matchMutation(filters, mutation));
    }
    notify(event) {
      notifyManager.batch(() => {
        this.listeners.forEach(({
          listener
        }) => {
          listener(event);
        });
      });
    }
    resumePausedMutations() {
      var _this$resuming;
      this.resuming = ((_this$resuming = this.resuming) != null ? _this$resuming : Promise.resolve()).then(() => {
        const pausedMutations = this.mutations.filter((x) => x.state.isPaused);
        return notifyManager.batch(() => pausedMutations.reduce((promise, mutation) => promise.then(() => mutation.continue().catch(noop2)), Promise.resolve()));
      }).then(() => {
        this.resuming = void 0;
      });
      return this.resuming;
    }
  };

  // node_modules/@tanstack/query-core/build/lib/infiniteQueryBehavior.mjs
  init_define_import_meta_env();
  function infiniteQueryBehavior() {
    return {
      onFetch: (context2) => {
        context2.fetchFn = () => {
          var _context$fetchOptions, _context$fetchOptions2, _context$fetchOptions3, _context$fetchOptions4, _context$state$data, _context$state$data2;
          const refetchPage = (_context$fetchOptions = context2.fetchOptions) == null ? void 0 : (_context$fetchOptions2 = _context$fetchOptions.meta) == null ? void 0 : _context$fetchOptions2.refetchPage;
          const fetchMore = (_context$fetchOptions3 = context2.fetchOptions) == null ? void 0 : (_context$fetchOptions4 = _context$fetchOptions3.meta) == null ? void 0 : _context$fetchOptions4.fetchMore;
          const pageParam = fetchMore == null ? void 0 : fetchMore.pageParam;
          const isFetchingNextPage = (fetchMore == null ? void 0 : fetchMore.direction) === "forward";
          const isFetchingPreviousPage = (fetchMore == null ? void 0 : fetchMore.direction) === "backward";
          const oldPages = ((_context$state$data = context2.state.data) == null ? void 0 : _context$state$data.pages) || [];
          const oldPageParams = ((_context$state$data2 = context2.state.data) == null ? void 0 : _context$state$data2.pageParams) || [];
          let newPageParams = oldPageParams;
          let cancelled = false;
          const addSignalProperty = (object) => {
            Object.defineProperty(object, "signal", {
              enumerable: true,
              get: () => {
                var _context$signal;
                if ((_context$signal = context2.signal) != null && _context$signal.aborted) {
                  cancelled = true;
                } else {
                  var _context$signal2;
                  (_context$signal2 = context2.signal) == null ? void 0 : _context$signal2.addEventListener("abort", () => {
                    cancelled = true;
                  });
                }
                return context2.signal;
              }
            });
          };
          const queryFn = context2.options.queryFn || (() => Promise.reject("Missing queryFn for queryKey '" + context2.options.queryHash + "'"));
          const buildNewPages = (pages, param, page, previous) => {
            newPageParams = previous ? [param, ...newPageParams] : [...newPageParams, param];
            return previous ? [page, ...pages] : [...pages, page];
          };
          const fetchPage = (pages, manual, param, previous) => {
            if (cancelled) {
              return Promise.reject("Cancelled");
            }
            if (typeof param === "undefined" && !manual && pages.length) {
              return Promise.resolve(pages);
            }
            const queryFnContext = {
              queryKey: context2.queryKey,
              pageParam: param,
              meta: context2.options.meta
            };
            addSignalProperty(queryFnContext);
            const queryFnResult = queryFn(queryFnContext);
            const promise2 = Promise.resolve(queryFnResult).then((page) => buildNewPages(pages, param, page, previous));
            return promise2;
          };
          let promise;
          if (!oldPages.length) {
            promise = fetchPage([]);
          } else if (isFetchingNextPage) {
            const manual = typeof pageParam !== "undefined";
            const param = manual ? pageParam : getNextPageParam(context2.options, oldPages);
            promise = fetchPage(oldPages, manual, param);
          } else if (isFetchingPreviousPage) {
            const manual = typeof pageParam !== "undefined";
            const param = manual ? pageParam : getPreviousPageParam(context2.options, oldPages);
            promise = fetchPage(oldPages, manual, param, true);
          } else {
            newPageParams = [];
            const manual = typeof context2.options.getNextPageParam === "undefined";
            const shouldFetchFirstPage = refetchPage && oldPages[0] ? refetchPage(oldPages[0], 0, oldPages) : true;
            promise = shouldFetchFirstPage ? fetchPage([], manual, oldPageParams[0]) : Promise.resolve(buildNewPages([], oldPageParams[0], oldPages[0]));
            for (let i = 1; i < oldPages.length; i++) {
              promise = promise.then((pages) => {
                const shouldFetchNextPage = refetchPage && oldPages[i] ? refetchPage(oldPages[i], i, oldPages) : true;
                if (shouldFetchNextPage) {
                  const param = manual ? oldPageParams[i] : getNextPageParam(context2.options, pages);
                  return fetchPage(pages, manual, param);
                }
                return Promise.resolve(buildNewPages(pages, oldPageParams[i], oldPages[i]));
              });
            }
          }
          const finalPromise = promise.then((pages) => ({
            pages,
            pageParams: newPageParams
          }));
          return finalPromise;
        };
      }
    };
  }
  function getNextPageParam(options3, pages) {
    return options3.getNextPageParam == null ? void 0 : options3.getNextPageParam(pages[pages.length - 1], pages);
  }
  function getPreviousPageParam(options3, pages) {
    return options3.getPreviousPageParam == null ? void 0 : options3.getPreviousPageParam(pages[0], pages);
  }
  function hasNextPage(options3, pages) {
    if (options3.getNextPageParam && Array.isArray(pages)) {
      const nextPageParam = getNextPageParam(options3, pages);
      return typeof nextPageParam !== "undefined" && nextPageParam !== null && nextPageParam !== false;
    }
    return;
  }
  function hasPreviousPage(options3, pages) {
    if (options3.getPreviousPageParam && Array.isArray(pages)) {
      const previousPageParam = getPreviousPageParam(options3, pages);
      return typeof previousPageParam !== "undefined" && previousPageParam !== null && previousPageParam !== false;
    }
    return;
  }

  // node_modules/@tanstack/query-core/build/lib/queryClient.mjs
  var QueryClient = class {
    constructor(config = {}) {
      this.queryCache = config.queryCache || new QueryCache();
      this.mutationCache = config.mutationCache || new MutationCache();
      this.logger = config.logger || defaultLogger;
      this.defaultOptions = config.defaultOptions || {};
      this.queryDefaults = [];
      this.mutationDefaults = [];
      this.mountCount = 0;
      if (config.logger) {
        this.logger.error("Passing a custom logger has been deprecated and will be removed in the next major version.");
      }
    }
    mount() {
      this.mountCount++;
      if (this.mountCount !== 1) return;
      this.unsubscribeFocus = focusManager.subscribe(() => {
        if (focusManager.isFocused()) {
          this.resumePausedMutations();
          this.queryCache.onFocus();
        }
      });
      this.unsubscribeOnline = onlineManager.subscribe(() => {
        if (onlineManager.isOnline()) {
          this.resumePausedMutations();
          this.queryCache.onOnline();
        }
      });
    }
    unmount() {
      var _this$unsubscribeFocu, _this$unsubscribeOnli;
      this.mountCount--;
      if (this.mountCount !== 0) return;
      (_this$unsubscribeFocu = this.unsubscribeFocus) == null ? void 0 : _this$unsubscribeFocu.call(this);
      this.unsubscribeFocus = void 0;
      (_this$unsubscribeOnli = this.unsubscribeOnline) == null ? void 0 : _this$unsubscribeOnli.call(this);
      this.unsubscribeOnline = void 0;
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    isFetching(arg1, arg2) {
      const [filters] = parseFilterArgs(arg1, arg2);
      filters.fetchStatus = "fetching";
      return this.queryCache.findAll(filters).length;
    }
    isMutating(filters) {
      return this.mutationCache.findAll({
        ...filters,
        fetching: true
      }).length;
    }
    /**
     * @deprecated This method will accept only queryKey in the next major version.
     */
    getQueryData(queryKey, filters) {
      var _this$queryCache$find;
      return (_this$queryCache$find = this.queryCache.find(queryKey, filters)) == null ? void 0 : _this$queryCache$find.state.data;
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    ensureQueryData(arg1, arg2, arg3) {
      const parsedOptions = parseQueryArgs(arg1, arg2, arg3);
      const cachedData = this.getQueryData(parsedOptions.queryKey);
      return cachedData ? Promise.resolve(cachedData) : this.fetchQuery(parsedOptions);
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    getQueriesData(queryKeyOrFilters) {
      return this.getQueryCache().findAll(queryKeyOrFilters).map(({
        queryKey,
        state
      }) => {
        const data = state.data;
        return [queryKey, data];
      });
    }
    setQueryData(queryKey, updater, options3) {
      const query = this.queryCache.find(queryKey);
      const prevData = query == null ? void 0 : query.state.data;
      const data = functionalUpdate(updater, prevData);
      if (typeof data === "undefined") {
        return void 0;
      }
      const parsedOptions = parseQueryArgs(queryKey);
      const defaultedOptions = this.defaultQueryOptions(parsedOptions);
      return this.queryCache.build(this, defaultedOptions).setData(data, {
        ...options3,
        manual: true
      });
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    setQueriesData(queryKeyOrFilters, updater, options3) {
      return notifyManager.batch(() => this.getQueryCache().findAll(queryKeyOrFilters).map(({
        queryKey
      }) => [queryKey, this.setQueryData(queryKey, updater, options3)]));
    }
    getQueryState(queryKey, filters) {
      var _this$queryCache$find2;
      return (_this$queryCache$find2 = this.queryCache.find(queryKey, filters)) == null ? void 0 : _this$queryCache$find2.state;
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    removeQueries(arg1, arg2) {
      const [filters] = parseFilterArgs(arg1, arg2);
      const queryCache = this.queryCache;
      notifyManager.batch(() => {
        queryCache.findAll(filters).forEach((query) => {
          queryCache.remove(query);
        });
      });
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    resetQueries(arg1, arg2, arg3) {
      const [filters, options3] = parseFilterArgs(arg1, arg2, arg3);
      const queryCache = this.queryCache;
      const refetchFilters = {
        type: "active",
        ...filters
      };
      return notifyManager.batch(() => {
        queryCache.findAll(filters).forEach((query) => {
          query.reset();
        });
        return this.refetchQueries(refetchFilters, options3);
      });
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    cancelQueries(arg1, arg2, arg3) {
      const [filters, cancelOptions = {}] = parseFilterArgs(arg1, arg2, arg3);
      if (typeof cancelOptions.revert === "undefined") {
        cancelOptions.revert = true;
      }
      const promises = notifyManager.batch(() => this.queryCache.findAll(filters).map((query) => query.cancel(cancelOptions)));
      return Promise.all(promises).then(noop2).catch(noop2);
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    invalidateQueries(arg1, arg2, arg3) {
      const [filters, options3] = parseFilterArgs(arg1, arg2, arg3);
      return notifyManager.batch(() => {
        var _ref, _filters$refetchType;
        this.queryCache.findAll(filters).forEach((query) => {
          query.invalidate();
        });
        if (filters.refetchType === "none") {
          return Promise.resolve();
        }
        const refetchFilters = {
          ...filters,
          type: (_ref = (_filters$refetchType = filters.refetchType) != null ? _filters$refetchType : filters.type) != null ? _ref : "active"
        };
        return this.refetchQueries(refetchFilters, options3);
      });
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    refetchQueries(arg1, arg2, arg3) {
      const [filters, options3] = parseFilterArgs(arg1, arg2, arg3);
      const promises = notifyManager.batch(() => this.queryCache.findAll(filters).filter((query) => !query.isDisabled()).map((query) => {
        var _options$cancelRefetc;
        return query.fetch(void 0, {
          ...options3,
          cancelRefetch: (_options$cancelRefetc = options3 == null ? void 0 : options3.cancelRefetch) != null ? _options$cancelRefetc : true,
          meta: {
            refetchPage: filters.refetchPage
          }
        });
      }));
      let promise = Promise.all(promises).then(noop2);
      if (!(options3 != null && options3.throwOnError)) {
        promise = promise.catch(noop2);
      }
      return promise;
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    fetchQuery(arg1, arg2, arg3) {
      const parsedOptions = parseQueryArgs(arg1, arg2, arg3);
      const defaultedOptions = this.defaultQueryOptions(parsedOptions);
      if (typeof defaultedOptions.retry === "undefined") {
        defaultedOptions.retry = false;
      }
      const query = this.queryCache.build(this, defaultedOptions);
      return query.isStaleByTime(defaultedOptions.staleTime) ? query.fetch(defaultedOptions) : Promise.resolve(query.state.data);
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    prefetchQuery(arg1, arg2, arg3) {
      return this.fetchQuery(arg1, arg2, arg3).then(noop2).catch(noop2);
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    fetchInfiniteQuery(arg1, arg2, arg3) {
      const parsedOptions = parseQueryArgs(arg1, arg2, arg3);
      parsedOptions.behavior = infiniteQueryBehavior();
      return this.fetchQuery(parsedOptions);
    }
    /**
     * @deprecated This method should be used with only one object argument.
     */
    prefetchInfiniteQuery(arg1, arg2, arg3) {
      return this.fetchInfiniteQuery(arg1, arg2, arg3).then(noop2).catch(noop2);
    }
    resumePausedMutations() {
      return this.mutationCache.resumePausedMutations();
    }
    getQueryCache() {
      return this.queryCache;
    }
    getMutationCache() {
      return this.mutationCache;
    }
    getLogger() {
      return this.logger;
    }
    getDefaultOptions() {
      return this.defaultOptions;
    }
    setDefaultOptions(options3) {
      this.defaultOptions = options3;
    }
    setQueryDefaults(queryKey, options3) {
      const result = this.queryDefaults.find((x) => hashQueryKey(queryKey) === hashQueryKey(x.queryKey));
      if (result) {
        result.defaultOptions = options3;
      } else {
        this.queryDefaults.push({
          queryKey,
          defaultOptions: options3
        });
      }
    }
    getQueryDefaults(queryKey) {
      if (!queryKey) {
        return void 0;
      }
      const firstMatchingDefaults = this.queryDefaults.find((x) => partialMatchKey(queryKey, x.queryKey));
      if (true) {
        const matchingDefaults = this.queryDefaults.filter((x) => partialMatchKey(queryKey, x.queryKey));
        if (matchingDefaults.length > 1) {
          this.logger.error("[QueryClient] Several query defaults match with key '" + JSON.stringify(queryKey) + "'. The first matching query defaults are used. Please check how query defaults are registered. Order does matter here. cf. https://react-query.tanstack.com/reference/QueryClient#queryclientsetquerydefaults.");
        }
      }
      return firstMatchingDefaults == null ? void 0 : firstMatchingDefaults.defaultOptions;
    }
    setMutationDefaults(mutationKey, options3) {
      const result = this.mutationDefaults.find((x) => hashQueryKey(mutationKey) === hashQueryKey(x.mutationKey));
      if (result) {
        result.defaultOptions = options3;
      } else {
        this.mutationDefaults.push({
          mutationKey,
          defaultOptions: options3
        });
      }
    }
    getMutationDefaults(mutationKey) {
      if (!mutationKey) {
        return void 0;
      }
      const firstMatchingDefaults = this.mutationDefaults.find((x) => partialMatchKey(mutationKey, x.mutationKey));
      if (true) {
        const matchingDefaults = this.mutationDefaults.filter((x) => partialMatchKey(mutationKey, x.mutationKey));
        if (matchingDefaults.length > 1) {
          this.logger.error("[QueryClient] Several mutation defaults match with key '" + JSON.stringify(mutationKey) + "'. The first matching mutation defaults are used. Please check how mutation defaults are registered. Order does matter here. cf. https://react-query.tanstack.com/reference/QueryClient#queryclientsetmutationdefaults.");
        }
      }
      return firstMatchingDefaults == null ? void 0 : firstMatchingDefaults.defaultOptions;
    }
    defaultQueryOptions(options3) {
      if (options3 != null && options3._defaulted) {
        return options3;
      }
      const defaultedOptions = {
        ...this.defaultOptions.queries,
        ...this.getQueryDefaults(options3 == null ? void 0 : options3.queryKey),
        ...options3,
        _defaulted: true
      };
      if (!defaultedOptions.queryHash && defaultedOptions.queryKey) {
        defaultedOptions.queryHash = hashQueryKeyByOptions(defaultedOptions.queryKey, defaultedOptions);
      }
      if (typeof defaultedOptions.refetchOnReconnect === "undefined") {
        defaultedOptions.refetchOnReconnect = defaultedOptions.networkMode !== "always";
      }
      if (typeof defaultedOptions.useErrorBoundary === "undefined") {
        defaultedOptions.useErrorBoundary = !!defaultedOptions.suspense;
      }
      return defaultedOptions;
    }
    defaultMutationOptions(options3) {
      if (options3 != null && options3._defaulted) {
        return options3;
      }
      return {
        ...this.defaultOptions.mutations,
        ...this.getMutationDefaults(options3 == null ? void 0 : options3.mutationKey),
        ...options3,
        _defaulted: true
      };
    }
    clear() {
      this.queryCache.clear();
      this.mutationCache.clear();
    }
  };

  // node_modules/@tanstack/query-core/build/lib/queryObserver.mjs
  init_define_import_meta_env();
  var QueryObserver = class extends Subscribable {
    constructor(client, options3) {
      super();
      this.client = client;
      this.options = options3;
      this.trackedProps = /* @__PURE__ */ new Set();
      this.selectError = null;
      this.bindMethods();
      this.setOptions(options3);
    }
    bindMethods() {
      this.remove = this.remove.bind(this);
      this.refetch = this.refetch.bind(this);
    }
    onSubscribe() {
      if (this.listeners.size === 1) {
        this.currentQuery.addObserver(this);
        if (shouldFetchOnMount(this.currentQuery, this.options)) {
          this.executeFetch();
        }
        this.updateTimers();
      }
    }
    onUnsubscribe() {
      if (!this.hasListeners()) {
        this.destroy();
      }
    }
    shouldFetchOnReconnect() {
      return shouldFetchOn(this.currentQuery, this.options, this.options.refetchOnReconnect);
    }
    shouldFetchOnWindowFocus() {
      return shouldFetchOn(this.currentQuery, this.options, this.options.refetchOnWindowFocus);
    }
    destroy() {
      this.listeners = /* @__PURE__ */ new Set();
      this.clearStaleTimeout();
      this.clearRefetchInterval();
      this.currentQuery.removeObserver(this);
    }
    setOptions(options3, notifyOptions) {
      const prevOptions = this.options;
      const prevQuery = this.currentQuery;
      this.options = this.client.defaultQueryOptions(options3);
      if (typeof (options3 == null ? void 0 : options3.isDataEqual) !== "undefined") {
        this.client.getLogger().error("The isDataEqual option has been deprecated and will be removed in the next major version. You can achieve the same functionality by passing a function as the structuralSharing option");
      }
      if (!shallowEqualObjects(prevOptions, this.options)) {
        this.client.getQueryCache().notify({
          type: "observerOptionsUpdated",
          query: this.currentQuery,
          observer: this
        });
      }
      if (typeof this.options.enabled !== "undefined" && typeof this.options.enabled !== "boolean") {
        throw new Error("Expected enabled to be a boolean");
      }
      if (!this.options.queryKey) {
        this.options.queryKey = prevOptions.queryKey;
      }
      this.updateQuery();
      const mounted = this.hasListeners();
      if (mounted && shouldFetchOptionally(this.currentQuery, prevQuery, this.options, prevOptions)) {
        this.executeFetch();
      }
      this.updateResult(notifyOptions);
      if (mounted && (this.currentQuery !== prevQuery || this.options.enabled !== prevOptions.enabled || this.options.staleTime !== prevOptions.staleTime)) {
        this.updateStaleTimeout();
      }
      const nextRefetchInterval = this.computeRefetchInterval();
      if (mounted && (this.currentQuery !== prevQuery || this.options.enabled !== prevOptions.enabled || nextRefetchInterval !== this.currentRefetchInterval)) {
        this.updateRefetchInterval(nextRefetchInterval);
      }
    }
    getOptimisticResult(options3) {
      const query = this.client.getQueryCache().build(this.client, options3);
      const result = this.createResult(query, options3);
      if (shouldAssignObserverCurrentProperties(this, result, options3)) {
        this.currentResult = result;
        this.currentResultOptions = this.options;
        this.currentResultState = this.currentQuery.state;
      }
      return result;
    }
    getCurrentResult() {
      return this.currentResult;
    }
    trackResult(result) {
      const trackedResult = {};
      Object.keys(result).forEach((key) => {
        Object.defineProperty(trackedResult, key, {
          configurable: false,
          enumerable: true,
          get: () => {
            this.trackedProps.add(key);
            return result[key];
          }
        });
      });
      return trackedResult;
    }
    getCurrentQuery() {
      return this.currentQuery;
    }
    remove() {
      this.client.getQueryCache().remove(this.currentQuery);
    }
    refetch({
      refetchPage,
      ...options3
    } = {}) {
      return this.fetch({
        ...options3,
        meta: {
          refetchPage
        }
      });
    }
    fetchOptimistic(options3) {
      const defaultedOptions = this.client.defaultQueryOptions(options3);
      const query = this.client.getQueryCache().build(this.client, defaultedOptions);
      query.isFetchingOptimistic = true;
      return query.fetch().then(() => this.createResult(query, defaultedOptions));
    }
    fetch(fetchOptions) {
      var _fetchOptions$cancelR;
      return this.executeFetch({
        ...fetchOptions,
        cancelRefetch: (_fetchOptions$cancelR = fetchOptions.cancelRefetch) != null ? _fetchOptions$cancelR : true
      }).then(() => {
        this.updateResult();
        return this.currentResult;
      });
    }
    executeFetch(fetchOptions) {
      this.updateQuery();
      let promise = this.currentQuery.fetch(this.options, fetchOptions);
      if (!(fetchOptions != null && fetchOptions.throwOnError)) {
        promise = promise.catch(noop2);
      }
      return promise;
    }
    updateStaleTimeout() {
      this.clearStaleTimeout();
      if (isServer || this.currentResult.isStale || !isValidTimeout(this.options.staleTime)) {
        return;
      }
      const time = timeUntilStale(this.currentResult.dataUpdatedAt, this.options.staleTime);
      const timeout2 = time + 1;
      this.staleTimeoutId = setTimeout(() => {
        if (!this.currentResult.isStale) {
          this.updateResult();
        }
      }, timeout2);
    }
    computeRefetchInterval() {
      var _this$options$refetch;
      return typeof this.options.refetchInterval === "function" ? this.options.refetchInterval(this.currentResult.data, this.currentQuery) : (_this$options$refetch = this.options.refetchInterval) != null ? _this$options$refetch : false;
    }
    updateRefetchInterval(nextInterval) {
      this.clearRefetchInterval();
      this.currentRefetchInterval = nextInterval;
      if (isServer || this.options.enabled === false || !isValidTimeout(this.currentRefetchInterval) || this.currentRefetchInterval === 0) {
        return;
      }
      this.refetchIntervalId = setInterval(() => {
        if (this.options.refetchIntervalInBackground || focusManager.isFocused()) {
          this.executeFetch();
        }
      }, this.currentRefetchInterval);
    }
    updateTimers() {
      this.updateStaleTimeout();
      this.updateRefetchInterval(this.computeRefetchInterval());
    }
    clearStaleTimeout() {
      if (this.staleTimeoutId) {
        clearTimeout(this.staleTimeoutId);
        this.staleTimeoutId = void 0;
      }
    }
    clearRefetchInterval() {
      if (this.refetchIntervalId) {
        clearInterval(this.refetchIntervalId);
        this.refetchIntervalId = void 0;
      }
    }
    createResult(query, options3) {
      const prevQuery = this.currentQuery;
      const prevOptions = this.options;
      const prevResult = this.currentResult;
      const prevResultState = this.currentResultState;
      const prevResultOptions = this.currentResultOptions;
      const queryChange = query !== prevQuery;
      const queryInitialState = queryChange ? query.state : this.currentQueryInitialState;
      const prevQueryResult = queryChange ? this.currentResult : this.previousQueryResult;
      const {
        state
      } = query;
      let {
        dataUpdatedAt,
        error: error2,
        errorUpdatedAt,
        fetchStatus,
        status
      } = state;
      let isPreviousData = false;
      let isPlaceholderData = false;
      let data;
      if (options3._optimisticResults) {
        const mounted = this.hasListeners();
        const fetchOnMount = !mounted && shouldFetchOnMount(query, options3);
        const fetchOptionally = mounted && shouldFetchOptionally(query, prevQuery, options3, prevOptions);
        if (fetchOnMount || fetchOptionally) {
          fetchStatus = canFetch(query.options.networkMode) ? "fetching" : "paused";
          if (!dataUpdatedAt) {
            status = "loading";
          }
        }
        if (options3._optimisticResults === "isRestoring") {
          fetchStatus = "idle";
        }
      }
      if (options3.keepPreviousData && !state.dataUpdatedAt && prevQueryResult != null && prevQueryResult.isSuccess && status !== "error") {
        data = prevQueryResult.data;
        dataUpdatedAt = prevQueryResult.dataUpdatedAt;
        status = prevQueryResult.status;
        isPreviousData = true;
      } else if (options3.select && typeof state.data !== "undefined") {
        if (prevResult && state.data === (prevResultState == null ? void 0 : prevResultState.data) && options3.select === this.selectFn) {
          data = this.selectResult;
        } else {
          try {
            this.selectFn = options3.select;
            data = options3.select(state.data);
            data = replaceData(prevResult == null ? void 0 : prevResult.data, data, options3);
            this.selectResult = data;
            this.selectError = null;
          } catch (selectError) {
            if (true) {
              this.client.getLogger().error(selectError);
            }
            this.selectError = selectError;
          }
        }
      } else {
        data = state.data;
      }
      if (typeof options3.placeholderData !== "undefined" && typeof data === "undefined" && status === "loading") {
        let placeholderData;
        if (prevResult != null && prevResult.isPlaceholderData && options3.placeholderData === (prevResultOptions == null ? void 0 : prevResultOptions.placeholderData)) {
          placeholderData = prevResult.data;
        } else {
          placeholderData = typeof options3.placeholderData === "function" ? options3.placeholderData() : options3.placeholderData;
          if (options3.select && typeof placeholderData !== "undefined") {
            try {
              placeholderData = options3.select(placeholderData);
              this.selectError = null;
            } catch (selectError) {
              if (true) {
                this.client.getLogger().error(selectError);
              }
              this.selectError = selectError;
            }
          }
        }
        if (typeof placeholderData !== "undefined") {
          status = "success";
          data = replaceData(prevResult == null ? void 0 : prevResult.data, placeholderData, options3);
          isPlaceholderData = true;
        }
      }
      if (this.selectError) {
        error2 = this.selectError;
        data = this.selectResult;
        errorUpdatedAt = Date.now();
        status = "error";
      }
      const isFetching = fetchStatus === "fetching";
      const isLoading = status === "loading";
      const isError2 = status === "error";
      const result = {
        status,
        fetchStatus,
        isLoading,
        isSuccess: status === "success",
        isError: isError2,
        isInitialLoading: isLoading && isFetching,
        data,
        dataUpdatedAt,
        error: error2,
        errorUpdatedAt,
        failureCount: state.fetchFailureCount,
        failureReason: state.fetchFailureReason,
        errorUpdateCount: state.errorUpdateCount,
        isFetched: state.dataUpdateCount > 0 || state.errorUpdateCount > 0,
        isFetchedAfterMount: state.dataUpdateCount > queryInitialState.dataUpdateCount || state.errorUpdateCount > queryInitialState.errorUpdateCount,
        isFetching,
        isRefetching: isFetching && !isLoading,
        isLoadingError: isError2 && state.dataUpdatedAt === 0,
        isPaused: fetchStatus === "paused",
        isPlaceholderData,
        isPreviousData,
        isRefetchError: isError2 && state.dataUpdatedAt !== 0,
        isStale: isStale(query, options3),
        refetch: this.refetch,
        remove: this.remove
      };
      return result;
    }
    updateResult(notifyOptions) {
      const prevResult = this.currentResult;
      const nextResult = this.createResult(this.currentQuery, this.options);
      this.currentResultState = this.currentQuery.state;
      this.currentResultOptions = this.options;
      if (shallowEqualObjects(nextResult, prevResult)) {
        return;
      }
      this.currentResult = nextResult;
      const defaultNotifyOptions = {
        cache: true
      };
      const shouldNotifyListeners = () => {
        if (!prevResult) {
          return true;
        }
        const {
          notifyOnChangeProps
        } = this.options;
        const notifyOnChangePropsValue = typeof notifyOnChangeProps === "function" ? notifyOnChangeProps() : notifyOnChangeProps;
        if (notifyOnChangePropsValue === "all" || !notifyOnChangePropsValue && !this.trackedProps.size) {
          return true;
        }
        const includedProps = new Set(notifyOnChangePropsValue != null ? notifyOnChangePropsValue : this.trackedProps);
        if (this.options.useErrorBoundary) {
          includedProps.add("error");
        }
        return Object.keys(this.currentResult).some((key) => {
          const typedKey = key;
          const changed = this.currentResult[typedKey] !== prevResult[typedKey];
          return changed && includedProps.has(typedKey);
        });
      };
      if ((notifyOptions == null ? void 0 : notifyOptions.listeners) !== false && shouldNotifyListeners()) {
        defaultNotifyOptions.listeners = true;
      }
      this.notify({
        ...defaultNotifyOptions,
        ...notifyOptions
      });
    }
    updateQuery() {
      const query = this.client.getQueryCache().build(this.client, this.options);
      if (query === this.currentQuery) {
        return;
      }
      const prevQuery = this.currentQuery;
      this.currentQuery = query;
      this.currentQueryInitialState = query.state;
      this.previousQueryResult = this.currentResult;
      if (this.hasListeners()) {
        prevQuery == null ? void 0 : prevQuery.removeObserver(this);
        query.addObserver(this);
      }
    }
    onQueryUpdate(action) {
      const notifyOptions = {};
      if (action.type === "success") {
        notifyOptions.onSuccess = !action.manual;
      } else if (action.type === "error" && !isCancelledError(action.error)) {
        notifyOptions.onError = true;
      }
      this.updateResult(notifyOptions);
      if (this.hasListeners()) {
        this.updateTimers();
      }
    }
    notify(notifyOptions) {
      notifyManager.batch(() => {
        if (notifyOptions.onSuccess) {
          var _this$options$onSucce, _this$options, _this$options$onSettl, _this$options2;
          (_this$options$onSucce = (_this$options = this.options).onSuccess) == null ? void 0 : _this$options$onSucce.call(_this$options, this.currentResult.data);
          (_this$options$onSettl = (_this$options2 = this.options).onSettled) == null ? void 0 : _this$options$onSettl.call(_this$options2, this.currentResult.data, null);
        } else if (notifyOptions.onError) {
          var _this$options$onError, _this$options3, _this$options$onSettl2, _this$options4;
          (_this$options$onError = (_this$options3 = this.options).onError) == null ? void 0 : _this$options$onError.call(_this$options3, this.currentResult.error);
          (_this$options$onSettl2 = (_this$options4 = this.options).onSettled) == null ? void 0 : _this$options$onSettl2.call(_this$options4, void 0, this.currentResult.error);
        }
        if (notifyOptions.listeners) {
          this.listeners.forEach(({
            listener
          }) => {
            listener(this.currentResult);
          });
        }
        if (notifyOptions.cache) {
          this.client.getQueryCache().notify({
            query: this.currentQuery,
            type: "observerResultsUpdated"
          });
        }
      });
    }
  };
  function shouldLoadOnMount(query, options3) {
    return options3.enabled !== false && !query.state.dataUpdatedAt && !(query.state.status === "error" && options3.retryOnMount === false);
  }
  function shouldFetchOnMount(query, options3) {
    return shouldLoadOnMount(query, options3) || query.state.dataUpdatedAt > 0 && shouldFetchOn(query, options3, options3.refetchOnMount);
  }
  function shouldFetchOn(query, options3, field) {
    if (options3.enabled !== false) {
      const value = typeof field === "function" ? field(query) : field;
      return value === "always" || value !== false && isStale(query, options3);
    }
    return false;
  }
  function shouldFetchOptionally(query, prevQuery, options3, prevOptions) {
    return options3.enabled !== false && (query !== prevQuery || prevOptions.enabled === false) && (!options3.suspense || query.state.status !== "error") && isStale(query, options3);
  }
  function isStale(query, options3) {
    return query.isStaleByTime(options3.staleTime);
  }
  function shouldAssignObserverCurrentProperties(observer, optimisticResult, options3) {
    if (options3.keepPreviousData) {
      return false;
    }
    if (options3.placeholderData !== void 0) {
      return optimisticResult.isPlaceholderData;
    }
    if (!shallowEqualObjects(observer.getCurrentResult(), optimisticResult)) {
      return true;
    }
    return false;
  }

  // node_modules/@tanstack/query-core/build/lib/infiniteQueryObserver.mjs
  init_define_import_meta_env();
  var InfiniteQueryObserver = class extends QueryObserver {
    // Type override
    // Type override
    // Type override
    // eslint-disable-next-line @typescript-eslint/no-useless-constructor
    constructor(client, options3) {
      super(client, options3);
    }
    bindMethods() {
      super.bindMethods();
      this.fetchNextPage = this.fetchNextPage.bind(this);
      this.fetchPreviousPage = this.fetchPreviousPage.bind(this);
    }
    setOptions(options3, notifyOptions) {
      super.setOptions({
        ...options3,
        behavior: infiniteQueryBehavior()
      }, notifyOptions);
    }
    getOptimisticResult(options3) {
      options3.behavior = infiniteQueryBehavior();
      return super.getOptimisticResult(options3);
    }
    fetchNextPage({
      pageParam,
      ...options3
    } = {}) {
      return this.fetch({
        ...options3,
        meta: {
          fetchMore: {
            direction: "forward",
            pageParam
          }
        }
      });
    }
    fetchPreviousPage({
      pageParam,
      ...options3
    } = {}) {
      return this.fetch({
        ...options3,
        meta: {
          fetchMore: {
            direction: "backward",
            pageParam
          }
        }
      });
    }
    createResult(query, options3) {
      var _state$fetchMeta, _state$fetchMeta$fetc, _state$fetchMeta2, _state$fetchMeta2$fet, _state$data, _state$data2;
      const {
        state
      } = query;
      const result = super.createResult(query, options3);
      const {
        isFetching,
        isRefetching
      } = result;
      const isFetchingNextPage = isFetching && ((_state$fetchMeta = state.fetchMeta) == null ? void 0 : (_state$fetchMeta$fetc = _state$fetchMeta.fetchMore) == null ? void 0 : _state$fetchMeta$fetc.direction) === "forward";
      const isFetchingPreviousPage = isFetching && ((_state$fetchMeta2 = state.fetchMeta) == null ? void 0 : (_state$fetchMeta2$fet = _state$fetchMeta2.fetchMore) == null ? void 0 : _state$fetchMeta2$fet.direction) === "backward";
      return {
        ...result,
        fetchNextPage: this.fetchNextPage,
        fetchPreviousPage: this.fetchPreviousPage,
        hasNextPage: hasNextPage(options3, (_state$data = state.data) == null ? void 0 : _state$data.pages),
        hasPreviousPage: hasPreviousPage(options3, (_state$data2 = state.data) == null ? void 0 : _state$data2.pages),
        isFetchingNextPage,
        isFetchingPreviousPage,
        isRefetching: isRefetching && !isFetchingNextPage && !isFetchingPreviousPage
      };
    }
  };

  // node_modules/@tanstack/react-query/build/lib/useSyncExternalStore.mjs
  init_define_import_meta_env();
  var import_shim = __toESM(require_shim(), 1);
  var useSyncExternalStore = import_shim.useSyncExternalStore;

  // node_modules/@tanstack/react-query/build/lib/QueryClientProvider.mjs
  init_define_import_meta_env();
  var React4 = __toESM(require_react_shim(), 1);
  var defaultContext = /* @__PURE__ */ React4.createContext(void 0);
  var QueryClientSharingContext = /* @__PURE__ */ React4.createContext(false);
  function getQueryClientContext(context2, contextSharing) {
    if (context2) {
      return context2;
    }
    if (contextSharing && typeof window !== "undefined") {
      if (!window.ReactQueryClientContext) {
        window.ReactQueryClientContext = defaultContext;
      }
      return window.ReactQueryClientContext;
    }
    return defaultContext;
  }
  var useQueryClient = ({
    context: context2
  } = {}) => {
    const queryClient2 = React4.useContext(getQueryClientContext(context2, React4.useContext(QueryClientSharingContext)));
    if (!queryClient2) {
      throw new Error("No QueryClient set, use QueryClientProvider to set one");
    }
    return queryClient2;
  };
  var QueryClientProvider = ({
    client,
    children,
    context: context2,
    contextSharing = false
  }) => {
    React4.useEffect(() => {
      client.mount();
      return () => {
        client.unmount();
      };
    }, [client]);
    if (contextSharing) {
      client.getLogger().error("The contextSharing option has been deprecated and will be removed in the next major version");
    }
    const Context = getQueryClientContext(context2, contextSharing);
    return /* @__PURE__ */ React4.createElement(QueryClientSharingContext.Provider, {
      value: !context2 && contextSharing
    }, /* @__PURE__ */ React4.createElement(Context.Provider, {
      value: client
    }, children));
  };

  // node_modules/@tanstack/react-query/build/lib/isRestoring.mjs
  init_define_import_meta_env();
  var React5 = __toESM(require_react_shim(), 1);
  var IsRestoringContext = /* @__PURE__ */ React5.createContext(false);
  var useIsRestoring = () => React5.useContext(IsRestoringContext);
  var IsRestoringProvider = IsRestoringContext.Provider;

  // node_modules/@tanstack/react-query/build/lib/QueryErrorResetBoundary.mjs
  init_define_import_meta_env();
  var React6 = __toESM(require_react_shim(), 1);
  function createValue() {
    let isReset = false;
    return {
      clearReset: () => {
        isReset = false;
      },
      reset: () => {
        isReset = true;
      },
      isReset: () => {
        return isReset;
      }
    };
  }
  var QueryErrorResetBoundaryContext = /* @__PURE__ */ React6.createContext(createValue());
  var useQueryErrorResetBoundary = () => React6.useContext(QueryErrorResetBoundaryContext);

  // node_modules/@tanstack/react-query/build/lib/errorBoundaryUtils.mjs
  init_define_import_meta_env();
  var React7 = __toESM(require_react_shim(), 1);

  // node_modules/@tanstack/react-query/build/lib/utils.mjs
  init_define_import_meta_env();
  function shouldThrowError(_useErrorBoundary, params) {
    if (typeof _useErrorBoundary === "function") {
      return _useErrorBoundary(...params);
    }
    return !!_useErrorBoundary;
  }

  // node_modules/@tanstack/react-query/build/lib/errorBoundaryUtils.mjs
  var ensurePreventErrorBoundaryRetry = (options3, errorResetBoundary) => {
    if (options3.suspense || options3.useErrorBoundary) {
      if (!errorResetBoundary.isReset()) {
        options3.retryOnMount = false;
      }
    }
  };
  var useClearResetErrorBoundary = (errorResetBoundary) => {
    React7.useEffect(() => {
      errorResetBoundary.clearReset();
    }, [errorResetBoundary]);
  };
  var getHasError = ({
    result,
    errorResetBoundary,
    useErrorBoundary,
    query
  }) => {
    return result.isError && !errorResetBoundary.isReset() && !result.isFetching && shouldThrowError(useErrorBoundary, [result.error, query]);
  };

  // node_modules/@tanstack/react-query/build/lib/suspense.mjs
  init_define_import_meta_env();
  var ensureStaleTime = (defaultedOptions) => {
    if (defaultedOptions.suspense) {
      if (typeof defaultedOptions.staleTime !== "number") {
        defaultedOptions.staleTime = 1e3;
      }
      if (typeof defaultedOptions.cacheTime === "number") {
        defaultedOptions.cacheTime = Math.max(defaultedOptions.cacheTime, 1e3);
      }
    }
  };
  var willFetch = (result, isRestoring) => result.isLoading && result.isFetching && !isRestoring;
  var shouldSuspend = (defaultedOptions, result, isRestoring) => (defaultedOptions == null ? void 0 : defaultedOptions.suspense) && willFetch(result, isRestoring);
  var fetchOptimistic = (defaultedOptions, observer, errorResetBoundary) => observer.fetchOptimistic(defaultedOptions).then(({
    data
  }) => {
    defaultedOptions.onSuccess == null ? void 0 : defaultedOptions.onSuccess(data);
    defaultedOptions.onSettled == null ? void 0 : defaultedOptions.onSettled(data, null);
  }).catch((error2) => {
    errorResetBoundary.clearReset();
    defaultedOptions.onError == null ? void 0 : defaultedOptions.onError(error2);
    defaultedOptions.onSettled == null ? void 0 : defaultedOptions.onSettled(void 0, error2);
  });

  // node_modules/@tanstack/react-query/build/lib/useBaseQuery.mjs
  init_define_import_meta_env();
  var React8 = __toESM(require_react_shim(), 1);
  function useBaseQuery(options3, Observer) {
    const queryClient2 = useQueryClient({
      context: options3.context
    });
    const isRestoring = useIsRestoring();
    const errorResetBoundary = useQueryErrorResetBoundary();
    const defaultedOptions = queryClient2.defaultQueryOptions(options3);
    defaultedOptions._optimisticResults = isRestoring ? "isRestoring" : "optimistic";
    if (defaultedOptions.onError) {
      defaultedOptions.onError = notifyManager.batchCalls(defaultedOptions.onError);
    }
    if (defaultedOptions.onSuccess) {
      defaultedOptions.onSuccess = notifyManager.batchCalls(defaultedOptions.onSuccess);
    }
    if (defaultedOptions.onSettled) {
      defaultedOptions.onSettled = notifyManager.batchCalls(defaultedOptions.onSettled);
    }
    ensureStaleTime(defaultedOptions);
    ensurePreventErrorBoundaryRetry(defaultedOptions, errorResetBoundary);
    useClearResetErrorBoundary(errorResetBoundary);
    const [observer] = React8.useState(() => new Observer(queryClient2, defaultedOptions));
    const result = observer.getOptimisticResult(defaultedOptions);
    useSyncExternalStore(React8.useCallback((onStoreChange) => {
      const unsubscribe = isRestoring ? () => void 0 : observer.subscribe(notifyManager.batchCalls(onStoreChange));
      observer.updateResult();
      return unsubscribe;
    }, [observer, isRestoring]), () => observer.getCurrentResult(), () => observer.getCurrentResult());
    React8.useEffect(() => {
      observer.setOptions(defaultedOptions, {
        listeners: false
      });
    }, [defaultedOptions, observer]);
    if (shouldSuspend(defaultedOptions, result, isRestoring)) {
      throw fetchOptimistic(defaultedOptions, observer, errorResetBoundary);
    }
    if (getHasError({
      result,
      errorResetBoundary,
      useErrorBoundary: defaultedOptions.useErrorBoundary,
      query: observer.getCurrentQuery()
    })) {
      throw result.error;
    }
    return !defaultedOptions.notifyOnChangeProps ? observer.trackResult(result) : result;
  }

  // node_modules/@tanstack/react-query/build/lib/useInfiniteQuery.mjs
  init_define_import_meta_env();
  function useInfiniteQuery(arg1, arg2, arg3) {
    const options3 = parseQueryArgs(arg1, arg2, arg3);
    return useBaseQuery(options3, InfiniteQueryObserver);
  }

  // node_modules/nuqs/dist/adapters/react.js
  init_define_import_meta_env();

  // node_modules/nuqs/dist/debounce-PSGthE_7.js
  init_define_import_meta_env();

  // node_modules/nuqs/dist/context-C4spomkL.js
  init_define_import_meta_env();
  var import_react9 = __toESM(require_react_shim(), 1);
  var debugEnabled = isDebugEnabled();
  function debug(message2, ...args) {
    if (!debugEnabled) return;
    const msg = sprintf(message2, ...args);
    performance.mark(msg);
    try {
      console.log(message2, ...args);
    } catch {
      console.log(msg);
    }
  }
  function sprintf(base, ...args) {
    return base.replace(/%[sfdO]/g, (match3) => {
      const arg = args.shift();
      return match3 === "%O" && arg ? JSON.stringify(arg).replace(/"([^"]+)":/g, "$1:") : String(arg);
    });
  }
  function isDebugEnabled() {
    if (typeof window === "undefined") return (process.env.DEBUG || "").includes("nuqs");
    try {
      const test = "nuqs-localStorage-test";
      if (typeof localStorage === "undefined") return false;
      localStorage.setItem(test, test);
      const isStorageAvailable = localStorage.getItem(test) === test;
      localStorage.removeItem(test);
      return isStorageAvailable && (localStorage.getItem("debug") || "").includes("nuqs");
    } catch {
      return false;
    }
  }
  var errors = {
    303: "Multiple adapter contexts detected. This might happen in monorepos.",
    404: "nuqs requires an adapter to work with your framework.",
    409: "Multiple versions of the library are loaded. This may lead to unexpected behavior. Currently using `%s`, but `%s` (via the %s adapter) was about to load on top.",
    414: "Max safe URL length exceeded. Some browsers may not be able to accept this URL. Consider limiting the amount of state stored in the URL.",
    422: "Invalid options combination: `limitUrlUpdates: debounce` should be used in SSR scenarios, with `shallow: false`",
    429: "URL update rate-limited by the browser. Consider increasing `throttleMs` for key(s) `%s`. %O",
    500: "Empty search params cache. Search params can't be accessed in Layouts.",
    501: "Search params cache already populated. Have you called `parse` twice?"
  };
  function error(code) {
    return `[nuqs] ${errors[code]}
  See https://nuqs.dev/NUQS-${code}`;
  }
  function renderQueryString(search) {
    if (search.size === 0) return "";
    const query = [];
    for (const [key, value] of search.entries()) {
      const safeKey = key.replace(/#/g, "%23").replace(/&/g, "%26").replace(/\+/g, "%2B").replace(/=/g, "%3D").replace(/\?/g, "%3F");
      query.push(`${safeKey}=${encodeQueryValue(value)}`);
    }
    const queryString = "?" + query.join("&");
    warnIfURLIsTooLong(queryString);
    return queryString;
  }
  function encodeQueryValue(input) {
    return input.replace(/%/g, "%25").replace(/\+/g, "%2B").replace(/ /g, "+").replace(/#/g, "%23").replace(/&/g, "%26").replace(/"/g, "%22").replace(/'/g, "%27").replace(/`/g, "%60").replace(/</g, "%3C").replace(/>/g, "%3E").replace(/[\x00-\x1F]/g, (char) => encodeURIComponent(char));
  }
  var URL_MAX_LENGTH = 2e3;
  function warnIfURLIsTooLong(queryString) {
    if (false) return;
    if (typeof location === "undefined") return;
    const url = new URL(location.href);
    url.search = queryString;
    if (url.href.length > URL_MAX_LENGTH) console.warn(error(414));
  }
  var context = (0, import_react9.createContext)({ useAdapter() {
    throw new Error(error(404));
  } });
  context.displayName = "NuqsAdapterContext";
  if (debugEnabled && typeof window !== "undefined") {
    if (window.__NuqsAdapterContext && window.__NuqsAdapterContext !== context) console.error(error(303));
    window.__NuqsAdapterContext = context;
  }
  function createAdapterProvider(useAdapter) {
    return ({ children, defaultOptions: defaultOptions4, processUrlSearchParams, ...props }) => (0, import_react9.createElement)(context.Provider, {
      ...props,
      value: {
        useAdapter,
        defaultOptions: defaultOptions4,
        processUrlSearchParams
      }
    }, children);
  }

  // node_modules/nuqs/dist/debounce-PSGthE_7.js
  var import_react10 = __toESM(require_react_shim(), 1);
  function getDefaultThrottle() {
    if (typeof window === "undefined") return 50;
    if (!Boolean(window.GestureEvent)) return 50;
    try {
      const match3 = navigator.userAgent?.match(/version\/([\d\.]+) safari/i);
      return parseFloat(match3[1]) >= 17 ? 120 : 320;
    } catch {
      return 320;
    }
  }
  function throttle(timeMs) {
    return {
      method: "throttle",
      timeMs
    };
  }
  var defaultRateLimit = throttle(getDefaultThrottle());
  function write(serialized, key, searchParams) {
    if (typeof serialized === "string") searchParams.set(key, serialized);
    else {
      searchParams.delete(key);
      for (const v of serialized) searchParams.append(key, v);
      if (!searchParams.has(key)) searchParams.set(key, "");
    }
    return searchParams;
  }
  function createEmitter() {
    const all = /* @__PURE__ */ new Map();
    return {
      on(type, handler) {
        const handlers = all.get(type) || [];
        handlers.push(handler);
        all.set(type, handlers);
        return () => this.off(type, handler);
      },
      off(type, handler) {
        const handlers = all.get(type);
        if (handlers) all.set(type, handlers.filter((h) => h !== handler));
      },
      emit(type, event) {
        all.get(type)?.forEach((handler) => handler(event));
      }
    };
  }
  function timeout(callback, ms, signal) {
    function onTick() {
      callback();
      signal.removeEventListener("abort", onAbort);
    }
    const id = setTimeout(onTick, ms);
    function onAbort() {
      clearTimeout(id);
      signal.removeEventListener("abort", onAbort);
    }
    signal.addEventListener("abort", onAbort);
  }
  function withResolvers() {
    const P2 = Promise;
    if (Promise.hasOwnProperty("withResolvers")) return Promise.withResolvers();
    let resolve = () => {
    };
    let reject = () => {
    };
    return {
      promise: new P2((res, rej) => {
        resolve = res;
        reject = rej;
      }),
      resolve,
      reject
    };
  }
  function compose(fns, final) {
    let next = final;
    for (let i = fns.length - 1; i >= 0; i--) {
      const fn = fns[i];
      if (!fn) continue;
      const prev = next;
      next = () => fn(prev);
    }
    next();
  }
  function getSearchParamsSnapshotFromLocation() {
    return new URLSearchParams(location.search);
  }
  var ThrottledQueue = class {
    constructor() {
      __publicField(this, "updateMap", /* @__PURE__ */ new Map());
      __publicField(this, "options", {
        history: "replace",
        scroll: false,
        shallow: true
      });
      __publicField(this, "timeMs", defaultRateLimit.timeMs);
      __publicField(this, "transitions", /* @__PURE__ */ new Set());
      __publicField(this, "resolvers", null);
      __publicField(this, "controller", null);
      __publicField(this, "lastFlushedAt", 0);
      __publicField(this, "resetQueueOnNextPush", false);
    }
    push({ key, query, options: options3 }, timeMs = defaultRateLimit.timeMs) {
      if (this.resetQueueOnNextPush) {
        this.reset();
        this.resetQueueOnNextPush = false;
      }
      debug("[nuqs gtq] Enqueueing %s=%s %O", key, query, options3);
      this.updateMap.set(key, query);
      if (options3.history === "push") this.options.history = "push";
      if (options3.scroll) this.options.scroll = true;
      if (options3.shallow === false) this.options.shallow = false;
      if (options3.startTransition) this.transitions.add(options3.startTransition);
      if (!Number.isFinite(this.timeMs) || timeMs > this.timeMs) this.timeMs = timeMs;
    }
    getQueuedQuery(key) {
      return this.updateMap.get(key);
    }
    getPendingPromise({ getSearchParamsSnapshot = getSearchParamsSnapshotFromLocation }) {
      return this.resolvers?.promise ?? Promise.resolve(getSearchParamsSnapshot());
    }
    flush({ getSearchParamsSnapshot = getSearchParamsSnapshotFromLocation, rateLimitFactor = 1, ...adapter }, processUrlSearchParams) {
      this.controller ?? (this.controller = new AbortController());
      if (!Number.isFinite(this.timeMs)) {
        debug("[nuqs gtq] Skipping flush due to throttleMs=Infinity");
        return Promise.resolve(getSearchParamsSnapshot());
      }
      if (this.resolvers) return this.resolvers.promise;
      this.resolvers = withResolvers();
      const flushNow = () => {
        this.lastFlushedAt = performance.now();
        const [search, error2] = this.applyPendingUpdates({
          ...adapter,
          autoResetQueueOnUpdate: adapter.autoResetQueueOnUpdate ?? true,
          getSearchParamsSnapshot
        }, processUrlSearchParams);
        if (error2 === null) {
          this.resolvers.resolve(search);
          this.resetQueueOnNextPush = true;
        } else this.resolvers.reject(search);
        this.resolvers = null;
      };
      const runOnNextTick = () => {
        const timeSinceLastFlush = performance.now() - this.lastFlushedAt;
        const timeMs = this.timeMs;
        const flushInMs = rateLimitFactor * Math.max(0, timeMs - timeSinceLastFlush);
        debug(`[nuqs gtq] Scheduling flush in %f ms. Throttled at %f ms (x%f)`, flushInMs, timeMs, rateLimitFactor);
        if (flushInMs === 0) flushNow();
        else timeout(flushNow, flushInMs, this.controller.signal);
      };
      timeout(runOnNextTick, 0, this.controller.signal);
      return this.resolvers.promise;
    }
    abort() {
      this.controller?.abort();
      this.controller = new AbortController();
      this.resolvers?.resolve(new URLSearchParams());
      this.resolvers = null;
      return this.reset();
    }
    reset() {
      const queuedKeys = Array.from(this.updateMap.keys());
      debug("[nuqs gtq] Resetting queue %s", JSON.stringify(Object.fromEntries(this.updateMap)));
      this.updateMap.clear();
      this.transitions.clear();
      this.options = {
        history: "replace",
        scroll: false,
        shallow: true
      };
      this.timeMs = defaultRateLimit.timeMs;
      return queuedKeys;
    }
    applyPendingUpdates(adapter, processUrlSearchParams) {
      const { updateUrl, getSearchParamsSnapshot } = adapter;
      let search = getSearchParamsSnapshot();
      debug(`[nuqs gtq] Applying %d pending update(s) on top of %s`, this.updateMap.size, search.toString());
      if (this.updateMap.size === 0) return [search, null];
      const items = Array.from(this.updateMap.entries());
      const options3 = { ...this.options };
      const transitions = Array.from(this.transitions);
      if (adapter.autoResetQueueOnUpdate) this.reset();
      debug("[nuqs gtq] Flushing queue %O with options %O", items, options3);
      for (const [key, value] of items) if (value === null) search.delete(key);
      else search = write(value, key, search);
      if (processUrlSearchParams) search = processUrlSearchParams(search);
      try {
        compose(transitions, () => {
          updateUrl(search, options3);
        });
        return [search, null];
      } catch (err) {
        console.error(error(429), items.map(([key]) => key).join(), err);
        return [search, err];
      }
    }
  };
  var globalThrottleQueue = new ThrottledQueue();
  function useSyncExternalStores(keys, subscribeKey, getKeySnapshot) {
    const snapshot = (0, import_react10.useCallback)(() => {
      const record = Object.fromEntries(keys.map((key) => [key, getKeySnapshot(key)]));
      return [JSON.stringify(record), record];
    }, [keys.join(","), getKeySnapshot]);
    const cacheRef = (0, import_react10.useRef)(null);
    if (cacheRef.current === null) cacheRef.current = snapshot();
    return (0, import_react10.useSyncExternalStore)((0, import_react10.useCallback)((callback) => {
      const off = keys.map((key) => subscribeKey(key, callback));
      return () => off.forEach((unsubscribe) => unsubscribe());
    }, [keys.join(","), subscribeKey]), () => {
      const [cacheKey, record] = snapshot();
      if (cacheRef.current[0] === cacheKey) return cacheRef.current[1];
      cacheRef.current = [cacheKey, record];
      return record;
    }, () => cacheRef.current[1]);
  }
  var DebouncedPromiseQueue = class {
    constructor(callback) {
      __publicField(this, "callback");
      __publicField(this, "resolvers", withResolvers());
      __publicField(this, "controller", new AbortController());
      __publicField(this, "queuedValue");
      this.callback = callback;
    }
    abort() {
      this.controller.abort();
      this.queuedValue = void 0;
    }
    push(value, timeMs) {
      this.queuedValue = value;
      this.controller.abort();
      this.controller = new AbortController();
      timeout(() => {
        const outputResolvers = this.resolvers;
        try {
          debug("[nuqs dq] Flushing debounce queue", value);
          const callbackPromise = this.callback(value);
          debug("[nuqs dq] Reset debounce queue %O", this.queuedValue);
          this.queuedValue = void 0;
          this.resolvers = withResolvers();
          callbackPromise.then((output) => outputResolvers.resolve(output)).catch((error2) => outputResolvers.reject(error2));
        } catch (error2) {
          this.queuedValue = void 0;
          outputResolvers.reject(error2);
        }
      }, timeMs, this.controller.signal);
      return this.resolvers.promise;
    }
  };
  var DebounceController = class {
    constructor(throttleQueue = new ThrottledQueue()) {
      __publicField(this, "throttleQueue");
      __publicField(this, "queues", /* @__PURE__ */ new Map());
      __publicField(this, "queuedQuerySync", createEmitter());
      this.throttleQueue = throttleQueue;
    }
    useQueuedQueries(keys) {
      return useSyncExternalStores(keys, (key, callback) => this.queuedQuerySync.on(key, callback), (key) => this.getQueuedQuery(key));
    }
    push(update, timeMs, adapter, processUrlSearchParams) {
      if (!Number.isFinite(timeMs)) {
        const getSnapshot = adapter.getSearchParamsSnapshot ?? getSearchParamsSnapshotFromLocation;
        return Promise.resolve(getSnapshot());
      }
      const key = update.key;
      if (!this.queues.has(key)) {
        debug("[nuqs dqc] Creating debounce queue for `%s`", key);
        const queue = new DebouncedPromiseQueue((update2) => {
          this.throttleQueue.push(update2);
          return this.throttleQueue.flush(adapter, processUrlSearchParams).finally(() => {
            if (this.queues.get(update2.key)?.queuedValue === void 0) {
              debug("[nuqs dqc] Cleaning up empty queue for `%s`", update2.key);
              this.queues.delete(update2.key);
            }
            this.queuedQuerySync.emit(update2.key);
          });
        });
        this.queues.set(key, queue);
      }
      debug("[nuqs dqc] Enqueueing debounce update %O", update);
      const promise = this.queues.get(key).push(update, timeMs);
      this.queuedQuerySync.emit(key);
      return promise;
    }
    abort(key) {
      const queue = this.queues.get(key);
      if (!queue) return (passThrough) => passThrough;
      debug("[nuqs dqc] Aborting debounce queue %s=%s", key, queue.queuedValue?.query);
      this.queues.delete(key);
      queue.abort();
      this.queuedQuerySync.emit(key);
      return (promise) => {
        promise.then(queue.resolvers.resolve, queue.resolvers.reject);
        return promise;
      };
    }
    abortAll() {
      for (const [key, queue] of this.queues.entries()) {
        debug("[nuqs dqc] Aborting debounce queue %s=%s", key, queue.queuedValue?.query);
        queue.abort();
        queue.resolvers.resolve(new URLSearchParams());
        this.queuedQuerySync.emit(key);
      }
      this.queues.clear();
    }
    getQueuedQuery(key) {
      const debouncedQueued = this.queues.get(key)?.queuedValue?.query;
      if (debouncedQueued !== void 0) return debouncedQueued;
      return this.throttleQueue.getQueuedQuery(key);
    }
  };
  var debounceController = new DebounceController(globalThrottleQueue);

  // node_modules/nuqs/dist/key-isolation-Y_bk4Yva.js
  init_define_import_meta_env();

  // node_modules/nuqs/dist/compare-Br3z3FUS.js
  init_define_import_meta_env();
  function compareQuery(a, b) {
    if (a === b) return true;
    if (a === null || b === null) return false;
    if (typeof a === "string" || typeof b === "string") return false;
    if (a.length !== b.length) return false;
    return a.every((value, index2) => value === b[index2]);
  }

  // node_modules/nuqs/dist/key-isolation-Y_bk4Yva.js
  function applyChange(newValue, keys, copy2) {
    return (oldValue) => {
      if (!(keys.length === 0 ? true : keys.some((key) => !compareQuery(oldValue.getAll(key), newValue.getAll(key))))) {
        debug("[nuqs `%s`] no change, returning previous", keys.join(","), oldValue);
        return oldValue;
      }
      const filtered = filterSearchParams(newValue, keys, copy2);
      debug(`[nuqs \`%s\`] subbed search params change
  from %O
  to   %O`, keys.join(","), oldValue, filtered);
      return filtered;
    };
  }
  function filterSearchParams(search, keys, copy2) {
    if (keys.length === 0) return search;
    const filtered = copy2 ? new URLSearchParams(search) : search;
    for (const key of search.keys()) if (!keys.includes(key)) filtered.delete(key);
    return filtered;
  }

  // node_modules/nuqs/dist/patch-history-CLy38Aj7.js
  init_define_import_meta_env();
  var historyUpdateMarker = "__nuqs__";

  // node_modules/nuqs/dist/adapters/react.js
  var import_react11 = __toESM(require_react_shim(), 1);
  var emitter = createEmitter();
  function generateUpdateUrlFn(fullPageNavigationOnShallowFalseUpdates) {
    return function updateUrl(search, options3) {
      const url = new URL(location.href);
      url.search = renderQueryString(search);
      debug("[nuqs react] Updating url: %s", url);
      if (fullPageNavigationOnShallowFalseUpdates && options3.shallow === false) (options3.history === "push" ? location.assign : location.replace).call(location, url);
      else (options3.history === "push" ? history.pushState : history.replaceState).call(history, history.state, historyUpdateMarker, url);
      emitter.emit("update", search);
      if (options3.scroll === true) window.scrollTo({ top: 0 });
    };
  }
  var NuqsReactAdapterContext = (0, import_react11.createContext)({ fullPageNavigationOnShallowFalseUpdates: false });
  function useNuqsReactAdapter(watchKeys) {
    const { fullPageNavigationOnShallowFalseUpdates } = (0, import_react11.useContext)(NuqsReactAdapterContext);
    const [searchParams, setSearchParams] = (0, import_react11.useState)(() => {
      if (typeof location === "undefined") return new URLSearchParams();
      return filterSearchParams(new URLSearchParams(location.search), watchKeys, false);
    });
    (0, import_react11.useEffect)(() => {
      const onPopState = () => {
        setSearchParams(applyChange(new URLSearchParams(location.search), watchKeys, false));
      };
      const onEmitterUpdate = (search) => {
        setSearchParams(applyChange(search, watchKeys, true));
      };
      emitter.on("update", onEmitterUpdate);
      window.addEventListener("popstate", onPopState);
      return () => {
        emitter.off("update", onEmitterUpdate);
        window.removeEventListener("popstate", onPopState);
      };
    }, [watchKeys.join("&")]);
    return {
      searchParams,
      updateUrl: (0, import_react11.useMemo)(() => generateUpdateUrlFn(fullPageNavigationOnShallowFalseUpdates), [fullPageNavigationOnShallowFalseUpdates])
    };
  }
  var NuqsReactAdapter = createAdapterProvider(useNuqsReactAdapter);
  function NuqsAdapter({ children, fullPageNavigationOnShallowFalseUpdates = false, ...adapterProps }) {
    return (0, import_react11.createElement)(NuqsReactAdapterContext.Provider, { value: { fullPageNavigationOnShallowFalseUpdates } }, (0, import_react11.createElement)(NuqsReactAdapter, {
      ...adapterProps,
      children
    }));
  }

  // src/lib/contexts/index.ts
  init_define_import_meta_env();

  // src/lib/contexts/ThemeContext.tsx
  init_define_import_meta_env();
  var import_react19 = __toESM(require_react_shim(), 1);

  // src/lib/hooks/shared/index.ts
  init_define_import_meta_env();

  // src/lib/hooks/shared/useToast.ts
  init_define_import_meta_env();
  var import_react12 = __toESM(require_react_shim(), 1);
  var TOAST_LIMIT = 1;
  var TOAST_REMOVE_DELAY = 1e4;
  var count = 0;
  var genId = () => {
    count = (count + 1) % Number.MAX_VALUE;
    return count.toString();
  };
  var toastTimeouts = /* @__PURE__ */ new Map();
  var addToRemoveQueue = (toastId) => {
    if (toastTimeouts.has(toastId)) return;
    const timeout2 = setTimeout(() => {
      toastTimeouts.delete(toastId);
      dispatch({ type: "REMOVE_TOAST", toastId });
    }, TOAST_REMOVE_DELAY);
    toastTimeouts.set(toastId, timeout2);
  };
  var reducer = (state, action) => {
    switch (action.type) {
      case "ADD_TOAST":
        return {
          ...state,
          toasts: [action.toast, ...state.toasts].slice(0, TOAST_LIMIT)
        };
      case "UPDATE_TOAST":
        return {
          ...state,
          toasts: state.toasts.map((t3) => t3.id === action.toast.id ? { ...t3, ...action.toast } : t3)
        };
      case "DISMISS_TOAST": {
        const { toastId } = action;
        if (toastId) addToRemoveQueue(toastId);
        else {
          state.toasts.forEach((toast2) => {
            addToRemoveQueue(toast2.id);
          });
        }
        return {
          ...state,
          toasts: state.toasts.map((t3) => t3.id === toastId || toastId === void 0 ? { ...t3, open: false } : t3)
        };
      }
      case "REMOVE_TOAST":
        if (action.toastId === void 0) return { ...state, toasts: [] };
        return {
          ...state,
          toasts: state.toasts.filter((t3) => t3.id !== action.toastId)
        };
    }
  };
  var listeners = [];
  var memoryState = { toasts: [] };
  function dispatch(action) {
    memoryState = reducer(memoryState, action);
    listeners.forEach((listener) => listener(memoryState));
  }
  var toast = (props) => {
    const id = genId();
    const update = (props2) => dispatch({ type: "UPDATE_TOAST", toast: { ...props2, id } });
    const dismiss = () => dispatch({ type: "DISMISS_TOAST", toastId: id });
    dispatch({
      type: "ADD_TOAST",
      toast: {
        ...props,
        id,
        open: true,
        onOpenChange: (open) => {
          if (!open) dismiss();
        }
      }
    });
    return {
      id,
      dismiss,
      update
    };
  };
  var useToast = () => {
    const [state, setState] = (0, import_react12.useState)(memoryState);
    (0, import_react12.useEffect)(() => {
      listeners.push(setState);
      return () => {
        const index2 = listeners.indexOf(setState);
        if (index2 > -1) listeners.splice(index2, 1);
      };
    }, []);
    return {
      ...state,
      toast,
      dismiss: (toastId) => dispatch({ type: "DISMISS_TOAST", toastId })
    };
  };

  // src/lib/hooks/shared/useStorage.ts
  init_define_import_meta_env();
  var import_react13 = __toESM(require_react_shim(), 1);

  // src/lib/utils/index.ts
  init_define_import_meta_env();

  // src/lib/utils/tailwind.ts
  init_define_import_meta_env();

  // node_modules/clsx/dist/clsx.mjs
  init_define_import_meta_env();
  function r(e2) {
    var t3, f, n = "";
    if ("string" == typeof e2 || "number" == typeof e2) n += e2;
    else if ("object" == typeof e2) if (Array.isArray(e2)) {
      var o = e2.length;
      for (t3 = 0; t3 < o; t3++) e2[t3] && (f = r(e2[t3])) && (n && (n += " "), n += f);
    } else for (f in e2) e2[f] && (n && (n += " "), n += f);
    return n;
  }
  function clsx() {
    for (var e2, t3, f = 0, n = "", o = arguments.length; f < o; f++) (e2 = arguments[f]) && (t3 = r(e2)) && (n && (n += " "), n += t3);
    return n;
  }

  // node_modules/tailwind-merge/dist/bundle-mjs.mjs
  init_define_import_meta_env();
  var CLASS_PART_SEPARATOR = "-";
  var createClassGroupUtils = (config) => {
    const classMap = createClassMap(config);
    const {
      conflictingClassGroups,
      conflictingClassGroupModifiers
    } = config;
    const getClassGroupId = (className) => {
      const classParts = className.split(CLASS_PART_SEPARATOR);
      if (classParts[0] === "" && classParts.length !== 1) {
        classParts.shift();
      }
      return getGroupRecursive(classParts, classMap) || getGroupIdForArbitraryProperty(className);
    };
    const getConflictingClassGroupIds = (classGroupId, hasPostfixModifier) => {
      const conflicts = conflictingClassGroups[classGroupId] || [];
      if (hasPostfixModifier && conflictingClassGroupModifiers[classGroupId]) {
        return [...conflicts, ...conflictingClassGroupModifiers[classGroupId]];
      }
      return conflicts;
    };
    return {
      getClassGroupId,
      getConflictingClassGroupIds
    };
  };
  var getGroupRecursive = (classParts, classPartObject) => {
    if (classParts.length === 0) {
      return classPartObject.classGroupId;
    }
    const currentClassPart = classParts[0];
    const nextClassPartObject = classPartObject.nextPart.get(currentClassPart);
    const classGroupFromNextClassPart = nextClassPartObject ? getGroupRecursive(classParts.slice(1), nextClassPartObject) : void 0;
    if (classGroupFromNextClassPart) {
      return classGroupFromNextClassPart;
    }
    if (classPartObject.validators.length === 0) {
      return void 0;
    }
    const classRest = classParts.join(CLASS_PART_SEPARATOR);
    return classPartObject.validators.find(({
      validator
    }) => validator(classRest))?.classGroupId;
  };
  var arbitraryPropertyRegex = /^\[(.+)\]$/;
  var getGroupIdForArbitraryProperty = (className) => {
    if (arbitraryPropertyRegex.test(className)) {
      const arbitraryPropertyClassName = arbitraryPropertyRegex.exec(className)[1];
      const property = arbitraryPropertyClassName?.substring(0, arbitraryPropertyClassName.indexOf(":"));
      if (property) {
        return "arbitrary.." + property;
      }
    }
  };
  var createClassMap = (config) => {
    const {
      theme,
      prefix
    } = config;
    const classMap = {
      nextPart: /* @__PURE__ */ new Map(),
      validators: []
    };
    const prefixedClassGroupEntries = getPrefixedClassGroupEntries(Object.entries(config.classGroups), prefix);
    prefixedClassGroupEntries.forEach(([classGroupId, classGroup]) => {
      processClassesRecursively(classGroup, classMap, classGroupId, theme);
    });
    return classMap;
  };
  var processClassesRecursively = (classGroup, classPartObject, classGroupId, theme) => {
    classGroup.forEach((classDefinition) => {
      if (typeof classDefinition === "string") {
        const classPartObjectToEdit = classDefinition === "" ? classPartObject : getPart(classPartObject, classDefinition);
        classPartObjectToEdit.classGroupId = classGroupId;
        return;
      }
      if (typeof classDefinition === "function") {
        if (isThemeGetter(classDefinition)) {
          processClassesRecursively(classDefinition(theme), classPartObject, classGroupId, theme);
          return;
        }
        classPartObject.validators.push({
          validator: classDefinition,
          classGroupId
        });
        return;
      }
      Object.entries(classDefinition).forEach(([key, classGroup2]) => {
        processClassesRecursively(classGroup2, getPart(classPartObject, key), classGroupId, theme);
      });
    });
  };
  var getPart = (classPartObject, path2) => {
    let currentClassPartObject = classPartObject;
    path2.split(CLASS_PART_SEPARATOR).forEach((pathPart) => {
      if (!currentClassPartObject.nextPart.has(pathPart)) {
        currentClassPartObject.nextPart.set(pathPart, {
          nextPart: /* @__PURE__ */ new Map(),
          validators: []
        });
      }
      currentClassPartObject = currentClassPartObject.nextPart.get(pathPart);
    });
    return currentClassPartObject;
  };
  var isThemeGetter = (func) => func.isThemeGetter;
  var getPrefixedClassGroupEntries = (classGroupEntries, prefix) => {
    if (!prefix) {
      return classGroupEntries;
    }
    return classGroupEntries.map(([classGroupId, classGroup]) => {
      const prefixedClassGroup = classGroup.map((classDefinition) => {
        if (typeof classDefinition === "string") {
          return prefix + classDefinition;
        }
        if (typeof classDefinition === "object") {
          return Object.fromEntries(Object.entries(classDefinition).map(([key, value]) => [prefix + key, value]));
        }
        return classDefinition;
      });
      return [classGroupId, prefixedClassGroup];
    });
  };
  var createLruCache = (maxCacheSize) => {
    if (maxCacheSize < 1) {
      return {
        get: () => void 0,
        set: () => {
        }
      };
    }
    let cacheSize = 0;
    let cache = /* @__PURE__ */ new Map();
    let previousCache = /* @__PURE__ */ new Map();
    const update = (key, value) => {
      cache.set(key, value);
      cacheSize++;
      if (cacheSize > maxCacheSize) {
        cacheSize = 0;
        previousCache = cache;
        cache = /* @__PURE__ */ new Map();
      }
    };
    return {
      get(key) {
        let value = cache.get(key);
        if (value !== void 0) {
          return value;
        }
        if ((value = previousCache.get(key)) !== void 0) {
          update(key, value);
          return value;
        }
      },
      set(key, value) {
        if (cache.has(key)) {
          cache.set(key, value);
        } else {
          update(key, value);
        }
      }
    };
  };
  var IMPORTANT_MODIFIER = "!";
  var createParseClassName = (config) => {
    const {
      separator,
      experimentalParseClassName
    } = config;
    const isSeparatorSingleCharacter = separator.length === 1;
    const firstSeparatorCharacter = separator[0];
    const separatorLength = separator.length;
    const parseClassName = (className) => {
      const modifiers = [];
      let bracketDepth = 0;
      let modifierStart = 0;
      let postfixModifierPosition;
      for (let index2 = 0; index2 < className.length; index2++) {
        let currentCharacter = className[index2];
        if (bracketDepth === 0) {
          if (currentCharacter === firstSeparatorCharacter && (isSeparatorSingleCharacter || className.slice(index2, index2 + separatorLength) === separator)) {
            modifiers.push(className.slice(modifierStart, index2));
            modifierStart = index2 + separatorLength;
            continue;
          }
          if (currentCharacter === "/") {
            postfixModifierPosition = index2;
            continue;
          }
        }
        if (currentCharacter === "[") {
          bracketDepth++;
        } else if (currentCharacter === "]") {
          bracketDepth--;
        }
      }
      const baseClassNameWithImportantModifier = modifiers.length === 0 ? className : className.substring(modifierStart);
      const hasImportantModifier = baseClassNameWithImportantModifier.startsWith(IMPORTANT_MODIFIER);
      const baseClassName = hasImportantModifier ? baseClassNameWithImportantModifier.substring(1) : baseClassNameWithImportantModifier;
      const maybePostfixModifierPosition = postfixModifierPosition && postfixModifierPosition > modifierStart ? postfixModifierPosition - modifierStart : void 0;
      return {
        modifiers,
        hasImportantModifier,
        baseClassName,
        maybePostfixModifierPosition
      };
    };
    if (experimentalParseClassName) {
      return (className) => experimentalParseClassName({
        className,
        parseClassName
      });
    }
    return parseClassName;
  };
  var sortModifiers = (modifiers) => {
    if (modifiers.length <= 1) {
      return modifiers;
    }
    const sortedModifiers = [];
    let unsortedModifiers = [];
    modifiers.forEach((modifier) => {
      const isArbitraryVariant = modifier[0] === "[";
      if (isArbitraryVariant) {
        sortedModifiers.push(...unsortedModifiers.sort(), modifier);
        unsortedModifiers = [];
      } else {
        unsortedModifiers.push(modifier);
      }
    });
    sortedModifiers.push(...unsortedModifiers.sort());
    return sortedModifiers;
  };
  var createConfigUtils = (config) => ({
    cache: createLruCache(config.cacheSize),
    parseClassName: createParseClassName(config),
    ...createClassGroupUtils(config)
  });
  var SPLIT_CLASSES_REGEX = /\s+/;
  var mergeClassList = (classList, configUtils) => {
    const {
      parseClassName,
      getClassGroupId,
      getConflictingClassGroupIds
    } = configUtils;
    const classGroupsInConflict = [];
    const classNames = classList.trim().split(SPLIT_CLASSES_REGEX);
    let result = "";
    for (let index2 = classNames.length - 1; index2 >= 0; index2 -= 1) {
      const originalClassName = classNames[index2];
      const {
        modifiers,
        hasImportantModifier,
        baseClassName,
        maybePostfixModifierPosition
      } = parseClassName(originalClassName);
      let hasPostfixModifier = Boolean(maybePostfixModifierPosition);
      let classGroupId = getClassGroupId(hasPostfixModifier ? baseClassName.substring(0, maybePostfixModifierPosition) : baseClassName);
      if (!classGroupId) {
        if (!hasPostfixModifier) {
          result = originalClassName + (result.length > 0 ? " " + result : result);
          continue;
        }
        classGroupId = getClassGroupId(baseClassName);
        if (!classGroupId) {
          result = originalClassName + (result.length > 0 ? " " + result : result);
          continue;
        }
        hasPostfixModifier = false;
      }
      const variantModifier = sortModifiers(modifiers).join(":");
      const modifierId = hasImportantModifier ? variantModifier + IMPORTANT_MODIFIER : variantModifier;
      const classId = modifierId + classGroupId;
      if (classGroupsInConflict.includes(classId)) {
        continue;
      }
      classGroupsInConflict.push(classId);
      const conflictGroups = getConflictingClassGroupIds(classGroupId, hasPostfixModifier);
      for (let i = 0; i < conflictGroups.length; ++i) {
        const group = conflictGroups[i];
        classGroupsInConflict.push(modifierId + group);
      }
      result = originalClassName + (result.length > 0 ? " " + result : result);
    }
    return result;
  };
  function twJoin() {
    let index2 = 0;
    let argument;
    let resolvedValue;
    let string = "";
    while (index2 < arguments.length) {
      if (argument = arguments[index2++]) {
        if (resolvedValue = toValue(argument)) {
          string && (string += " ");
          string += resolvedValue;
        }
      }
    }
    return string;
  }
  var toValue = (mix) => {
    if (typeof mix === "string") {
      return mix;
    }
    let resolvedValue;
    let string = "";
    for (let k3 = 0; k3 < mix.length; k3++) {
      if (mix[k3]) {
        if (resolvedValue = toValue(mix[k3])) {
          string && (string += " ");
          string += resolvedValue;
        }
      }
    }
    return string;
  };
  function createTailwindMerge(createConfigFirst, ...createConfigRest) {
    let configUtils;
    let cacheGet;
    let cacheSet;
    let functionToCall = initTailwindMerge;
    function initTailwindMerge(classList) {
      const config = createConfigRest.reduce((previousConfig, createConfigCurrent) => createConfigCurrent(previousConfig), createConfigFirst());
      configUtils = createConfigUtils(config);
      cacheGet = configUtils.cache.get;
      cacheSet = configUtils.cache.set;
      functionToCall = tailwindMerge;
      return tailwindMerge(classList);
    }
    function tailwindMerge(classList) {
      const cachedResult = cacheGet(classList);
      if (cachedResult) {
        return cachedResult;
      }
      const result = mergeClassList(classList, configUtils);
      cacheSet(classList, result);
      return result;
    }
    return function callTailwindMerge() {
      return functionToCall(twJoin.apply(null, arguments));
    };
  }
  var fromTheme = (key) => {
    const themeGetter = (theme) => theme[key] || [];
    themeGetter.isThemeGetter = true;
    return themeGetter;
  };
  var arbitraryValueRegex = /^\[(?:([a-z-]+):)?(.+)\]$/i;
  var fractionRegex = /^\d+\/\d+$/;
  var stringLengths = /* @__PURE__ */ new Set(["px", "full", "screen"]);
  var tshirtUnitRegex = /^(\d+(\.\d+)?)?(xs|sm|md|lg|xl)$/;
  var lengthUnitRegex = /\d+(%|px|r?em|[sdl]?v([hwib]|min|max)|pt|pc|in|cm|mm|cap|ch|ex|r?lh|cq(w|h|i|b|min|max))|\b(calc|min|max|clamp)\(.+\)|^0$/;
  var colorFunctionRegex = /^(rgba?|hsla?|hwb|(ok)?(lab|lch)|color-mix)\(.+\)$/;
  var shadowRegex = /^(inset_)?-?((\d+)?\.?(\d+)[a-z]+|0)_-?((\d+)?\.?(\d+)[a-z]+|0)/;
  var imageRegex = /^(url|image|image-set|cross-fade|element|(repeating-)?(linear|radial|conic)-gradient)\(.+\)$/;
  var isLength = (value) => isNumber(value) || stringLengths.has(value) || fractionRegex.test(value);
  var isArbitraryLength = (value) => getIsArbitraryValue(value, "length", isLengthOnly);
  var isNumber = (value) => Boolean(value) && !Number.isNaN(Number(value));
  var isArbitraryNumber = (value) => getIsArbitraryValue(value, "number", isNumber);
  var isInteger = (value) => Boolean(value) && Number.isInteger(Number(value));
  var isPercent = (value) => value.endsWith("%") && isNumber(value.slice(0, -1));
  var isArbitraryValue = (value) => arbitraryValueRegex.test(value);
  var isTshirtSize = (value) => tshirtUnitRegex.test(value);
  var sizeLabels = /* @__PURE__ */ new Set(["length", "size", "percentage"]);
  var isArbitrarySize = (value) => getIsArbitraryValue(value, sizeLabels, isNever);
  var isArbitraryPosition = (value) => getIsArbitraryValue(value, "position", isNever);
  var imageLabels = /* @__PURE__ */ new Set(["image", "url"]);
  var isArbitraryImage = (value) => getIsArbitraryValue(value, imageLabels, isImage);
  var isArbitraryShadow = (value) => getIsArbitraryValue(value, "", isShadow);
  var isAny = () => true;
  var getIsArbitraryValue = (value, label, testValue) => {
    const result = arbitraryValueRegex.exec(value);
    if (result) {
      if (result[1]) {
        return typeof label === "string" ? result[1] === label : label.has(result[1]);
      }
      return testValue(result[2]);
    }
    return false;
  };
  var isLengthOnly = (value) => (
    // `colorFunctionRegex` check is necessary because color functions can have percentages in them which which would be incorrectly classified as lengths.
    // For example, `hsl(0 0% 0%)` would be classified as a length without this check.
    // I could also use lookbehind assertion in `lengthUnitRegex` but that isn't supported widely enough.
    lengthUnitRegex.test(value) && !colorFunctionRegex.test(value)
  );
  var isNever = () => false;
  var isShadow = (value) => shadowRegex.test(value);
  var isImage = (value) => imageRegex.test(value);
  var getDefaultConfig = () => {
    const colors = fromTheme("colors");
    const spacing = fromTheme("spacing");
    const blur = fromTheme("blur");
    const brightness = fromTheme("brightness");
    const borderColor = fromTheme("borderColor");
    const borderRadius = fromTheme("borderRadius");
    const borderSpacing = fromTheme("borderSpacing");
    const borderWidth = fromTheme("borderWidth");
    const contrast = fromTheme("contrast");
    const grayscale = fromTheme("grayscale");
    const hueRotate = fromTheme("hueRotate");
    const invert = fromTheme("invert");
    const gap = fromTheme("gap");
    const gradientColorStops = fromTheme("gradientColorStops");
    const gradientColorStopPositions = fromTheme("gradientColorStopPositions");
    const inset = fromTheme("inset");
    const margin = fromTheme("margin");
    const opacity = fromTheme("opacity");
    const padding = fromTheme("padding");
    const saturate = fromTheme("saturate");
    const scale = fromTheme("scale");
    const sepia = fromTheme("sepia");
    const skew = fromTheme("skew");
    const space = fromTheme("space");
    const translate = fromTheme("translate");
    const getOverscroll = () => ["auto", "contain", "none"];
    const getOverflow = () => ["auto", "hidden", "clip", "visible", "scroll"];
    const getSpacingWithAutoAndArbitrary = () => ["auto", isArbitraryValue, spacing];
    const getSpacingWithArbitrary = () => [isArbitraryValue, spacing];
    const getLengthWithEmptyAndArbitrary = () => ["", isLength, isArbitraryLength];
    const getNumberWithAutoAndArbitrary = () => ["auto", isNumber, isArbitraryValue];
    const getPositions = () => ["bottom", "center", "left", "left-bottom", "left-top", "right", "right-bottom", "right-top", "top"];
    const getLineStyles = () => ["solid", "dashed", "dotted", "double", "none"];
    const getBlendModes = () => ["normal", "multiply", "screen", "overlay", "darken", "lighten", "color-dodge", "color-burn", "hard-light", "soft-light", "difference", "exclusion", "hue", "saturation", "color", "luminosity"];
    const getAlign = () => ["start", "end", "center", "between", "around", "evenly", "stretch"];
    const getZeroAndEmpty = () => ["", "0", isArbitraryValue];
    const getBreaks = () => ["auto", "avoid", "all", "avoid-page", "page", "left", "right", "column"];
    const getNumberAndArbitrary = () => [isNumber, isArbitraryValue];
    return {
      cacheSize: 500,
      separator: ":",
      theme: {
        colors: [isAny],
        spacing: [isLength, isArbitraryLength],
        blur: ["none", "", isTshirtSize, isArbitraryValue],
        brightness: getNumberAndArbitrary(),
        borderColor: [colors],
        borderRadius: ["none", "", "full", isTshirtSize, isArbitraryValue],
        borderSpacing: getSpacingWithArbitrary(),
        borderWidth: getLengthWithEmptyAndArbitrary(),
        contrast: getNumberAndArbitrary(),
        grayscale: getZeroAndEmpty(),
        hueRotate: getNumberAndArbitrary(),
        invert: getZeroAndEmpty(),
        gap: getSpacingWithArbitrary(),
        gradientColorStops: [colors],
        gradientColorStopPositions: [isPercent, isArbitraryLength],
        inset: getSpacingWithAutoAndArbitrary(),
        margin: getSpacingWithAutoAndArbitrary(),
        opacity: getNumberAndArbitrary(),
        padding: getSpacingWithArbitrary(),
        saturate: getNumberAndArbitrary(),
        scale: getNumberAndArbitrary(),
        sepia: getZeroAndEmpty(),
        skew: getNumberAndArbitrary(),
        space: getSpacingWithArbitrary(),
        translate: getSpacingWithArbitrary()
      },
      classGroups: {
        // Layout
        /**
         * Aspect Ratio
         * @see https://tailwindcss.com/docs/aspect-ratio
         */
        aspect: [{
          aspect: ["auto", "square", "video", isArbitraryValue]
        }],
        /**
         * Container
         * @see https://tailwindcss.com/docs/container
         */
        container: ["container"],
        /**
         * Columns
         * @see https://tailwindcss.com/docs/columns
         */
        columns: [{
          columns: [isTshirtSize]
        }],
        /**
         * Break After
         * @see https://tailwindcss.com/docs/break-after
         */
        "break-after": [{
          "break-after": getBreaks()
        }],
        /**
         * Break Before
         * @see https://tailwindcss.com/docs/break-before
         */
        "break-before": [{
          "break-before": getBreaks()
        }],
        /**
         * Break Inside
         * @see https://tailwindcss.com/docs/break-inside
         */
        "break-inside": [{
          "break-inside": ["auto", "avoid", "avoid-page", "avoid-column"]
        }],
        /**
         * Box Decoration Break
         * @see https://tailwindcss.com/docs/box-decoration-break
         */
        "box-decoration": [{
          "box-decoration": ["slice", "clone"]
        }],
        /**
         * Box Sizing
         * @see https://tailwindcss.com/docs/box-sizing
         */
        box: [{
          box: ["border", "content"]
        }],
        /**
         * Display
         * @see https://tailwindcss.com/docs/display
         */
        display: ["block", "inline-block", "inline", "flex", "inline-flex", "table", "inline-table", "table-caption", "table-cell", "table-column", "table-column-group", "table-footer-group", "table-header-group", "table-row-group", "table-row", "flow-root", "grid", "inline-grid", "contents", "list-item", "hidden"],
        /**
         * Floats
         * @see https://tailwindcss.com/docs/float
         */
        float: [{
          float: ["right", "left", "none", "start", "end"]
        }],
        /**
         * Clear
         * @see https://tailwindcss.com/docs/clear
         */
        clear: [{
          clear: ["left", "right", "both", "none", "start", "end"]
        }],
        /**
         * Isolation
         * @see https://tailwindcss.com/docs/isolation
         */
        isolation: ["isolate", "isolation-auto"],
        /**
         * Object Fit
         * @see https://tailwindcss.com/docs/object-fit
         */
        "object-fit": [{
          object: ["contain", "cover", "fill", "none", "scale-down"]
        }],
        /**
         * Object Position
         * @see https://tailwindcss.com/docs/object-position
         */
        "object-position": [{
          object: [...getPositions(), isArbitraryValue]
        }],
        /**
         * Overflow
         * @see https://tailwindcss.com/docs/overflow
         */
        overflow: [{
          overflow: getOverflow()
        }],
        /**
         * Overflow X
         * @see https://tailwindcss.com/docs/overflow
         */
        "overflow-x": [{
          "overflow-x": getOverflow()
        }],
        /**
         * Overflow Y
         * @see https://tailwindcss.com/docs/overflow
         */
        "overflow-y": [{
          "overflow-y": getOverflow()
        }],
        /**
         * Overscroll Behavior
         * @see https://tailwindcss.com/docs/overscroll-behavior
         */
        overscroll: [{
          overscroll: getOverscroll()
        }],
        /**
         * Overscroll Behavior X
         * @see https://tailwindcss.com/docs/overscroll-behavior
         */
        "overscroll-x": [{
          "overscroll-x": getOverscroll()
        }],
        /**
         * Overscroll Behavior Y
         * @see https://tailwindcss.com/docs/overscroll-behavior
         */
        "overscroll-y": [{
          "overscroll-y": getOverscroll()
        }],
        /**
         * Position
         * @see https://tailwindcss.com/docs/position
         */
        position: ["static", "fixed", "absolute", "relative", "sticky"],
        /**
         * Top / Right / Bottom / Left
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        inset: [{
          inset: [inset]
        }],
        /**
         * Right / Left
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        "inset-x": [{
          "inset-x": [inset]
        }],
        /**
         * Top / Bottom
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        "inset-y": [{
          "inset-y": [inset]
        }],
        /**
         * Start
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        start: [{
          start: [inset]
        }],
        /**
         * End
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        end: [{
          end: [inset]
        }],
        /**
         * Top
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        top: [{
          top: [inset]
        }],
        /**
         * Right
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        right: [{
          right: [inset]
        }],
        /**
         * Bottom
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        bottom: [{
          bottom: [inset]
        }],
        /**
         * Left
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        left: [{
          left: [inset]
        }],
        /**
         * Visibility
         * @see https://tailwindcss.com/docs/visibility
         */
        visibility: ["visible", "invisible", "collapse"],
        /**
         * Z-Index
         * @see https://tailwindcss.com/docs/z-index
         */
        z: [{
          z: ["auto", isInteger, isArbitraryValue]
        }],
        // Flexbox and Grid
        /**
         * Flex Basis
         * @see https://tailwindcss.com/docs/flex-basis
         */
        basis: [{
          basis: getSpacingWithAutoAndArbitrary()
        }],
        /**
         * Flex Direction
         * @see https://tailwindcss.com/docs/flex-direction
         */
        "flex-direction": [{
          flex: ["row", "row-reverse", "col", "col-reverse"]
        }],
        /**
         * Flex Wrap
         * @see https://tailwindcss.com/docs/flex-wrap
         */
        "flex-wrap": [{
          flex: ["wrap", "wrap-reverse", "nowrap"]
        }],
        /**
         * Flex
         * @see https://tailwindcss.com/docs/flex
         */
        flex: [{
          flex: ["1", "auto", "initial", "none", isArbitraryValue]
        }],
        /**
         * Flex Grow
         * @see https://tailwindcss.com/docs/flex-grow
         */
        grow: [{
          grow: getZeroAndEmpty()
        }],
        /**
         * Flex Shrink
         * @see https://tailwindcss.com/docs/flex-shrink
         */
        shrink: [{
          shrink: getZeroAndEmpty()
        }],
        /**
         * Order
         * @see https://tailwindcss.com/docs/order
         */
        order: [{
          order: ["first", "last", "none", isInteger, isArbitraryValue]
        }],
        /**
         * Grid Template Columns
         * @see https://tailwindcss.com/docs/grid-template-columns
         */
        "grid-cols": [{
          "grid-cols": [isAny]
        }],
        /**
         * Grid Column Start / End
         * @see https://tailwindcss.com/docs/grid-column
         */
        "col-start-end": [{
          col: ["auto", {
            span: ["full", isInteger, isArbitraryValue]
          }, isArbitraryValue]
        }],
        /**
         * Grid Column Start
         * @see https://tailwindcss.com/docs/grid-column
         */
        "col-start": [{
          "col-start": getNumberWithAutoAndArbitrary()
        }],
        /**
         * Grid Column End
         * @see https://tailwindcss.com/docs/grid-column
         */
        "col-end": [{
          "col-end": getNumberWithAutoAndArbitrary()
        }],
        /**
         * Grid Template Rows
         * @see https://tailwindcss.com/docs/grid-template-rows
         */
        "grid-rows": [{
          "grid-rows": [isAny]
        }],
        /**
         * Grid Row Start / End
         * @see https://tailwindcss.com/docs/grid-row
         */
        "row-start-end": [{
          row: ["auto", {
            span: [isInteger, isArbitraryValue]
          }, isArbitraryValue]
        }],
        /**
         * Grid Row Start
         * @see https://tailwindcss.com/docs/grid-row
         */
        "row-start": [{
          "row-start": getNumberWithAutoAndArbitrary()
        }],
        /**
         * Grid Row End
         * @see https://tailwindcss.com/docs/grid-row
         */
        "row-end": [{
          "row-end": getNumberWithAutoAndArbitrary()
        }],
        /**
         * Grid Auto Flow
         * @see https://tailwindcss.com/docs/grid-auto-flow
         */
        "grid-flow": [{
          "grid-flow": ["row", "col", "dense", "row-dense", "col-dense"]
        }],
        /**
         * Grid Auto Columns
         * @see https://tailwindcss.com/docs/grid-auto-columns
         */
        "auto-cols": [{
          "auto-cols": ["auto", "min", "max", "fr", isArbitraryValue]
        }],
        /**
         * Grid Auto Rows
         * @see https://tailwindcss.com/docs/grid-auto-rows
         */
        "auto-rows": [{
          "auto-rows": ["auto", "min", "max", "fr", isArbitraryValue]
        }],
        /**
         * Gap
         * @see https://tailwindcss.com/docs/gap
         */
        gap: [{
          gap: [gap]
        }],
        /**
         * Gap X
         * @see https://tailwindcss.com/docs/gap
         */
        "gap-x": [{
          "gap-x": [gap]
        }],
        /**
         * Gap Y
         * @see https://tailwindcss.com/docs/gap
         */
        "gap-y": [{
          "gap-y": [gap]
        }],
        /**
         * Justify Content
         * @see https://tailwindcss.com/docs/justify-content
         */
        "justify-content": [{
          justify: ["normal", ...getAlign()]
        }],
        /**
         * Justify Items
         * @see https://tailwindcss.com/docs/justify-items
         */
        "justify-items": [{
          "justify-items": ["start", "end", "center", "stretch"]
        }],
        /**
         * Justify Self
         * @see https://tailwindcss.com/docs/justify-self
         */
        "justify-self": [{
          "justify-self": ["auto", "start", "end", "center", "stretch"]
        }],
        /**
         * Align Content
         * @see https://tailwindcss.com/docs/align-content
         */
        "align-content": [{
          content: ["normal", ...getAlign(), "baseline"]
        }],
        /**
         * Align Items
         * @see https://tailwindcss.com/docs/align-items
         */
        "align-items": [{
          items: ["start", "end", "center", "baseline", "stretch"]
        }],
        /**
         * Align Self
         * @see https://tailwindcss.com/docs/align-self
         */
        "align-self": [{
          self: ["auto", "start", "end", "center", "stretch", "baseline"]
        }],
        /**
         * Place Content
         * @see https://tailwindcss.com/docs/place-content
         */
        "place-content": [{
          "place-content": [...getAlign(), "baseline"]
        }],
        /**
         * Place Items
         * @see https://tailwindcss.com/docs/place-items
         */
        "place-items": [{
          "place-items": ["start", "end", "center", "baseline", "stretch"]
        }],
        /**
         * Place Self
         * @see https://tailwindcss.com/docs/place-self
         */
        "place-self": [{
          "place-self": ["auto", "start", "end", "center", "stretch"]
        }],
        // Spacing
        /**
         * Padding
         * @see https://tailwindcss.com/docs/padding
         */
        p: [{
          p: [padding]
        }],
        /**
         * Padding X
         * @see https://tailwindcss.com/docs/padding
         */
        px: [{
          px: [padding]
        }],
        /**
         * Padding Y
         * @see https://tailwindcss.com/docs/padding
         */
        py: [{
          py: [padding]
        }],
        /**
         * Padding Start
         * @see https://tailwindcss.com/docs/padding
         */
        ps: [{
          ps: [padding]
        }],
        /**
         * Padding End
         * @see https://tailwindcss.com/docs/padding
         */
        pe: [{
          pe: [padding]
        }],
        /**
         * Padding Top
         * @see https://tailwindcss.com/docs/padding
         */
        pt: [{
          pt: [padding]
        }],
        /**
         * Padding Right
         * @see https://tailwindcss.com/docs/padding
         */
        pr: [{
          pr: [padding]
        }],
        /**
         * Padding Bottom
         * @see https://tailwindcss.com/docs/padding
         */
        pb: [{
          pb: [padding]
        }],
        /**
         * Padding Left
         * @see https://tailwindcss.com/docs/padding
         */
        pl: [{
          pl: [padding]
        }],
        /**
         * Margin
         * @see https://tailwindcss.com/docs/margin
         */
        m: [{
          m: [margin]
        }],
        /**
         * Margin X
         * @see https://tailwindcss.com/docs/margin
         */
        mx: [{
          mx: [margin]
        }],
        /**
         * Margin Y
         * @see https://tailwindcss.com/docs/margin
         */
        my: [{
          my: [margin]
        }],
        /**
         * Margin Start
         * @see https://tailwindcss.com/docs/margin
         */
        ms: [{
          ms: [margin]
        }],
        /**
         * Margin End
         * @see https://tailwindcss.com/docs/margin
         */
        me: [{
          me: [margin]
        }],
        /**
         * Margin Top
         * @see https://tailwindcss.com/docs/margin
         */
        mt: [{
          mt: [margin]
        }],
        /**
         * Margin Right
         * @see https://tailwindcss.com/docs/margin
         */
        mr: [{
          mr: [margin]
        }],
        /**
         * Margin Bottom
         * @see https://tailwindcss.com/docs/margin
         */
        mb: [{
          mb: [margin]
        }],
        /**
         * Margin Left
         * @see https://tailwindcss.com/docs/margin
         */
        ml: [{
          ml: [margin]
        }],
        /**
         * Space Between X
         * @see https://tailwindcss.com/docs/space
         */
        "space-x": [{
          "space-x": [space]
        }],
        /**
         * Space Between X Reverse
         * @see https://tailwindcss.com/docs/space
         */
        "space-x-reverse": ["space-x-reverse"],
        /**
         * Space Between Y
         * @see https://tailwindcss.com/docs/space
         */
        "space-y": [{
          "space-y": [space]
        }],
        /**
         * Space Between Y Reverse
         * @see https://tailwindcss.com/docs/space
         */
        "space-y-reverse": ["space-y-reverse"],
        // Sizing
        /**
         * Width
         * @see https://tailwindcss.com/docs/width
         */
        w: [{
          w: ["auto", "min", "max", "fit", "svw", "lvw", "dvw", isArbitraryValue, spacing]
        }],
        /**
         * Min-Width
         * @see https://tailwindcss.com/docs/min-width
         */
        "min-w": [{
          "min-w": [isArbitraryValue, spacing, "min", "max", "fit"]
        }],
        /**
         * Max-Width
         * @see https://tailwindcss.com/docs/max-width
         */
        "max-w": [{
          "max-w": [isArbitraryValue, spacing, "none", "full", "min", "max", "fit", "prose", {
            screen: [isTshirtSize]
          }, isTshirtSize]
        }],
        /**
         * Height
         * @see https://tailwindcss.com/docs/height
         */
        h: [{
          h: [isArbitraryValue, spacing, "auto", "min", "max", "fit", "svh", "lvh", "dvh"]
        }],
        /**
         * Min-Height
         * @see https://tailwindcss.com/docs/min-height
         */
        "min-h": [{
          "min-h": [isArbitraryValue, spacing, "min", "max", "fit", "svh", "lvh", "dvh"]
        }],
        /**
         * Max-Height
         * @see https://tailwindcss.com/docs/max-height
         */
        "max-h": [{
          "max-h": [isArbitraryValue, spacing, "min", "max", "fit", "svh", "lvh", "dvh"]
        }],
        /**
         * Size
         * @see https://tailwindcss.com/docs/size
         */
        size: [{
          size: [isArbitraryValue, spacing, "auto", "min", "max", "fit"]
        }],
        // Typography
        /**
         * Font Size
         * @see https://tailwindcss.com/docs/font-size
         */
        "font-size": [{
          text: ["base", isTshirtSize, isArbitraryLength]
        }],
        /**
         * Font Smoothing
         * @see https://tailwindcss.com/docs/font-smoothing
         */
        "font-smoothing": ["antialiased", "subpixel-antialiased"],
        /**
         * Font Style
         * @see https://tailwindcss.com/docs/font-style
         */
        "font-style": ["italic", "not-italic"],
        /**
         * Font Weight
         * @see https://tailwindcss.com/docs/font-weight
         */
        "font-weight": [{
          font: ["thin", "extralight", "light", "normal", "medium", "semibold", "bold", "extrabold", "black", isArbitraryNumber]
        }],
        /**
         * Font Family
         * @see https://tailwindcss.com/docs/font-family
         */
        "font-family": [{
          font: [isAny]
        }],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-normal": ["normal-nums"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-ordinal": ["ordinal"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-slashed-zero": ["slashed-zero"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-figure": ["lining-nums", "oldstyle-nums"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-spacing": ["proportional-nums", "tabular-nums"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-fraction": ["diagonal-fractions", "stacked-fractions"],
        /**
         * Letter Spacing
         * @see https://tailwindcss.com/docs/letter-spacing
         */
        tracking: [{
          tracking: ["tighter", "tight", "normal", "wide", "wider", "widest", isArbitraryValue]
        }],
        /**
         * Line Clamp
         * @see https://tailwindcss.com/docs/line-clamp
         */
        "line-clamp": [{
          "line-clamp": ["none", isNumber, isArbitraryNumber]
        }],
        /**
         * Line Height
         * @see https://tailwindcss.com/docs/line-height
         */
        leading: [{
          leading: ["none", "tight", "snug", "normal", "relaxed", "loose", isLength, isArbitraryValue]
        }],
        /**
         * List Style Image
         * @see https://tailwindcss.com/docs/list-style-image
         */
        "list-image": [{
          "list-image": ["none", isArbitraryValue]
        }],
        /**
         * List Style Type
         * @see https://tailwindcss.com/docs/list-style-type
         */
        "list-style-type": [{
          list: ["none", "disc", "decimal", isArbitraryValue]
        }],
        /**
         * List Style Position
         * @see https://tailwindcss.com/docs/list-style-position
         */
        "list-style-position": [{
          list: ["inside", "outside"]
        }],
        /**
         * Placeholder Color
         * @deprecated since Tailwind CSS v3.0.0
         * @see https://tailwindcss.com/docs/placeholder-color
         */
        "placeholder-color": [{
          placeholder: [colors]
        }],
        /**
         * Placeholder Opacity
         * @see https://tailwindcss.com/docs/placeholder-opacity
         */
        "placeholder-opacity": [{
          "placeholder-opacity": [opacity]
        }],
        /**
         * Text Alignment
         * @see https://tailwindcss.com/docs/text-align
         */
        "text-alignment": [{
          text: ["left", "center", "right", "justify", "start", "end"]
        }],
        /**
         * Text Color
         * @see https://tailwindcss.com/docs/text-color
         */
        "text-color": [{
          text: [colors]
        }],
        /**
         * Text Opacity
         * @see https://tailwindcss.com/docs/text-opacity
         */
        "text-opacity": [{
          "text-opacity": [opacity]
        }],
        /**
         * Text Decoration
         * @see https://tailwindcss.com/docs/text-decoration
         */
        "text-decoration": ["underline", "overline", "line-through", "no-underline"],
        /**
         * Text Decoration Style
         * @see https://tailwindcss.com/docs/text-decoration-style
         */
        "text-decoration-style": [{
          decoration: [...getLineStyles(), "wavy"]
        }],
        /**
         * Text Decoration Thickness
         * @see https://tailwindcss.com/docs/text-decoration-thickness
         */
        "text-decoration-thickness": [{
          decoration: ["auto", "from-font", isLength, isArbitraryLength]
        }],
        /**
         * Text Underline Offset
         * @see https://tailwindcss.com/docs/text-underline-offset
         */
        "underline-offset": [{
          "underline-offset": ["auto", isLength, isArbitraryValue]
        }],
        /**
         * Text Decoration Color
         * @see https://tailwindcss.com/docs/text-decoration-color
         */
        "text-decoration-color": [{
          decoration: [colors]
        }],
        /**
         * Text Transform
         * @see https://tailwindcss.com/docs/text-transform
         */
        "text-transform": ["uppercase", "lowercase", "capitalize", "normal-case"],
        /**
         * Text Overflow
         * @see https://tailwindcss.com/docs/text-overflow
         */
        "text-overflow": ["truncate", "text-ellipsis", "text-clip"],
        /**
         * Text Wrap
         * @see https://tailwindcss.com/docs/text-wrap
         */
        "text-wrap": [{
          text: ["wrap", "nowrap", "balance", "pretty"]
        }],
        /**
         * Text Indent
         * @see https://tailwindcss.com/docs/text-indent
         */
        indent: [{
          indent: getSpacingWithArbitrary()
        }],
        /**
         * Vertical Alignment
         * @see https://tailwindcss.com/docs/vertical-align
         */
        "vertical-align": [{
          align: ["baseline", "top", "middle", "bottom", "text-top", "text-bottom", "sub", "super", isArbitraryValue]
        }],
        /**
         * Whitespace
         * @see https://tailwindcss.com/docs/whitespace
         */
        whitespace: [{
          whitespace: ["normal", "nowrap", "pre", "pre-line", "pre-wrap", "break-spaces"]
        }],
        /**
         * Word Break
         * @see https://tailwindcss.com/docs/word-break
         */
        break: [{
          break: ["normal", "words", "all", "keep"]
        }],
        /**
         * Hyphens
         * @see https://tailwindcss.com/docs/hyphens
         */
        hyphens: [{
          hyphens: ["none", "manual", "auto"]
        }],
        /**
         * Content
         * @see https://tailwindcss.com/docs/content
         */
        content: [{
          content: ["none", isArbitraryValue]
        }],
        // Backgrounds
        /**
         * Background Attachment
         * @see https://tailwindcss.com/docs/background-attachment
         */
        "bg-attachment": [{
          bg: ["fixed", "local", "scroll"]
        }],
        /**
         * Background Clip
         * @see https://tailwindcss.com/docs/background-clip
         */
        "bg-clip": [{
          "bg-clip": ["border", "padding", "content", "text"]
        }],
        /**
         * Background Opacity
         * @deprecated since Tailwind CSS v3.0.0
         * @see https://tailwindcss.com/docs/background-opacity
         */
        "bg-opacity": [{
          "bg-opacity": [opacity]
        }],
        /**
         * Background Origin
         * @see https://tailwindcss.com/docs/background-origin
         */
        "bg-origin": [{
          "bg-origin": ["border", "padding", "content"]
        }],
        /**
         * Background Position
         * @see https://tailwindcss.com/docs/background-position
         */
        "bg-position": [{
          bg: [...getPositions(), isArbitraryPosition]
        }],
        /**
         * Background Repeat
         * @see https://tailwindcss.com/docs/background-repeat
         */
        "bg-repeat": [{
          bg: ["no-repeat", {
            repeat: ["", "x", "y", "round", "space"]
          }]
        }],
        /**
         * Background Size
         * @see https://tailwindcss.com/docs/background-size
         */
        "bg-size": [{
          bg: ["auto", "cover", "contain", isArbitrarySize]
        }],
        /**
         * Background Image
         * @see https://tailwindcss.com/docs/background-image
         */
        "bg-image": [{
          bg: ["none", {
            "gradient-to": ["t", "tr", "r", "br", "b", "bl", "l", "tl"]
          }, isArbitraryImage]
        }],
        /**
         * Background Color
         * @see https://tailwindcss.com/docs/background-color
         */
        "bg-color": [{
          bg: [colors]
        }],
        /**
         * Gradient Color Stops From Position
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-from-pos": [{
          from: [gradientColorStopPositions]
        }],
        /**
         * Gradient Color Stops Via Position
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-via-pos": [{
          via: [gradientColorStopPositions]
        }],
        /**
         * Gradient Color Stops To Position
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-to-pos": [{
          to: [gradientColorStopPositions]
        }],
        /**
         * Gradient Color Stops From
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-from": [{
          from: [gradientColorStops]
        }],
        /**
         * Gradient Color Stops Via
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-via": [{
          via: [gradientColorStops]
        }],
        /**
         * Gradient Color Stops To
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-to": [{
          to: [gradientColorStops]
        }],
        // Borders
        /**
         * Border Radius
         * @see https://tailwindcss.com/docs/border-radius
         */
        rounded: [{
          rounded: [borderRadius]
        }],
        /**
         * Border Radius Start
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-s": [{
          "rounded-s": [borderRadius]
        }],
        /**
         * Border Radius End
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-e": [{
          "rounded-e": [borderRadius]
        }],
        /**
         * Border Radius Top
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-t": [{
          "rounded-t": [borderRadius]
        }],
        /**
         * Border Radius Right
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-r": [{
          "rounded-r": [borderRadius]
        }],
        /**
         * Border Radius Bottom
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-b": [{
          "rounded-b": [borderRadius]
        }],
        /**
         * Border Radius Left
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-l": [{
          "rounded-l": [borderRadius]
        }],
        /**
         * Border Radius Start Start
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-ss": [{
          "rounded-ss": [borderRadius]
        }],
        /**
         * Border Radius Start End
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-se": [{
          "rounded-se": [borderRadius]
        }],
        /**
         * Border Radius End End
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-ee": [{
          "rounded-ee": [borderRadius]
        }],
        /**
         * Border Radius End Start
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-es": [{
          "rounded-es": [borderRadius]
        }],
        /**
         * Border Radius Top Left
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-tl": [{
          "rounded-tl": [borderRadius]
        }],
        /**
         * Border Radius Top Right
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-tr": [{
          "rounded-tr": [borderRadius]
        }],
        /**
         * Border Radius Bottom Right
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-br": [{
          "rounded-br": [borderRadius]
        }],
        /**
         * Border Radius Bottom Left
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-bl": [{
          "rounded-bl": [borderRadius]
        }],
        /**
         * Border Width
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w": [{
          border: [borderWidth]
        }],
        /**
         * Border Width X
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-x": [{
          "border-x": [borderWidth]
        }],
        /**
         * Border Width Y
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-y": [{
          "border-y": [borderWidth]
        }],
        /**
         * Border Width Start
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-s": [{
          "border-s": [borderWidth]
        }],
        /**
         * Border Width End
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-e": [{
          "border-e": [borderWidth]
        }],
        /**
         * Border Width Top
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-t": [{
          "border-t": [borderWidth]
        }],
        /**
         * Border Width Right
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-r": [{
          "border-r": [borderWidth]
        }],
        /**
         * Border Width Bottom
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-b": [{
          "border-b": [borderWidth]
        }],
        /**
         * Border Width Left
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-l": [{
          "border-l": [borderWidth]
        }],
        /**
         * Border Opacity
         * @see https://tailwindcss.com/docs/border-opacity
         */
        "border-opacity": [{
          "border-opacity": [opacity]
        }],
        /**
         * Border Style
         * @see https://tailwindcss.com/docs/border-style
         */
        "border-style": [{
          border: [...getLineStyles(), "hidden"]
        }],
        /**
         * Divide Width X
         * @see https://tailwindcss.com/docs/divide-width
         */
        "divide-x": [{
          "divide-x": [borderWidth]
        }],
        /**
         * Divide Width X Reverse
         * @see https://tailwindcss.com/docs/divide-width
         */
        "divide-x-reverse": ["divide-x-reverse"],
        /**
         * Divide Width Y
         * @see https://tailwindcss.com/docs/divide-width
         */
        "divide-y": [{
          "divide-y": [borderWidth]
        }],
        /**
         * Divide Width Y Reverse
         * @see https://tailwindcss.com/docs/divide-width
         */
        "divide-y-reverse": ["divide-y-reverse"],
        /**
         * Divide Opacity
         * @see https://tailwindcss.com/docs/divide-opacity
         */
        "divide-opacity": [{
          "divide-opacity": [opacity]
        }],
        /**
         * Divide Style
         * @see https://tailwindcss.com/docs/divide-style
         */
        "divide-style": [{
          divide: getLineStyles()
        }],
        /**
         * Border Color
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color": [{
          border: [borderColor]
        }],
        /**
         * Border Color X
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-x": [{
          "border-x": [borderColor]
        }],
        /**
         * Border Color Y
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-y": [{
          "border-y": [borderColor]
        }],
        /**
         * Border Color S
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-s": [{
          "border-s": [borderColor]
        }],
        /**
         * Border Color E
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-e": [{
          "border-e": [borderColor]
        }],
        /**
         * Border Color Top
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-t": [{
          "border-t": [borderColor]
        }],
        /**
         * Border Color Right
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-r": [{
          "border-r": [borderColor]
        }],
        /**
         * Border Color Bottom
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-b": [{
          "border-b": [borderColor]
        }],
        /**
         * Border Color Left
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-l": [{
          "border-l": [borderColor]
        }],
        /**
         * Divide Color
         * @see https://tailwindcss.com/docs/divide-color
         */
        "divide-color": [{
          divide: [borderColor]
        }],
        /**
         * Outline Style
         * @see https://tailwindcss.com/docs/outline-style
         */
        "outline-style": [{
          outline: ["", ...getLineStyles()]
        }],
        /**
         * Outline Offset
         * @see https://tailwindcss.com/docs/outline-offset
         */
        "outline-offset": [{
          "outline-offset": [isLength, isArbitraryValue]
        }],
        /**
         * Outline Width
         * @see https://tailwindcss.com/docs/outline-width
         */
        "outline-w": [{
          outline: [isLength, isArbitraryLength]
        }],
        /**
         * Outline Color
         * @see https://tailwindcss.com/docs/outline-color
         */
        "outline-color": [{
          outline: [colors]
        }],
        /**
         * Ring Width
         * @see https://tailwindcss.com/docs/ring-width
         */
        "ring-w": [{
          ring: getLengthWithEmptyAndArbitrary()
        }],
        /**
         * Ring Width Inset
         * @see https://tailwindcss.com/docs/ring-width
         */
        "ring-w-inset": ["ring-inset"],
        /**
         * Ring Color
         * @see https://tailwindcss.com/docs/ring-color
         */
        "ring-color": [{
          ring: [colors]
        }],
        /**
         * Ring Opacity
         * @see https://tailwindcss.com/docs/ring-opacity
         */
        "ring-opacity": [{
          "ring-opacity": [opacity]
        }],
        /**
         * Ring Offset Width
         * @see https://tailwindcss.com/docs/ring-offset-width
         */
        "ring-offset-w": [{
          "ring-offset": [isLength, isArbitraryLength]
        }],
        /**
         * Ring Offset Color
         * @see https://tailwindcss.com/docs/ring-offset-color
         */
        "ring-offset-color": [{
          "ring-offset": [colors]
        }],
        // Effects
        /**
         * Box Shadow
         * @see https://tailwindcss.com/docs/box-shadow
         */
        shadow: [{
          shadow: ["", "inner", "none", isTshirtSize, isArbitraryShadow]
        }],
        /**
         * Box Shadow Color
         * @see https://tailwindcss.com/docs/box-shadow-color
         */
        "shadow-color": [{
          shadow: [isAny]
        }],
        /**
         * Opacity
         * @see https://tailwindcss.com/docs/opacity
         */
        opacity: [{
          opacity: [opacity]
        }],
        /**
         * Mix Blend Mode
         * @see https://tailwindcss.com/docs/mix-blend-mode
         */
        "mix-blend": [{
          "mix-blend": [...getBlendModes(), "plus-lighter", "plus-darker"]
        }],
        /**
         * Background Blend Mode
         * @see https://tailwindcss.com/docs/background-blend-mode
         */
        "bg-blend": [{
          "bg-blend": getBlendModes()
        }],
        // Filters
        /**
         * Filter
         * @deprecated since Tailwind CSS v3.0.0
         * @see https://tailwindcss.com/docs/filter
         */
        filter: [{
          filter: ["", "none"]
        }],
        /**
         * Blur
         * @see https://tailwindcss.com/docs/blur
         */
        blur: [{
          blur: [blur]
        }],
        /**
         * Brightness
         * @see https://tailwindcss.com/docs/brightness
         */
        brightness: [{
          brightness: [brightness]
        }],
        /**
         * Contrast
         * @see https://tailwindcss.com/docs/contrast
         */
        contrast: [{
          contrast: [contrast]
        }],
        /**
         * Drop Shadow
         * @see https://tailwindcss.com/docs/drop-shadow
         */
        "drop-shadow": [{
          "drop-shadow": ["", "none", isTshirtSize, isArbitraryValue]
        }],
        /**
         * Grayscale
         * @see https://tailwindcss.com/docs/grayscale
         */
        grayscale: [{
          grayscale: [grayscale]
        }],
        /**
         * Hue Rotate
         * @see https://tailwindcss.com/docs/hue-rotate
         */
        "hue-rotate": [{
          "hue-rotate": [hueRotate]
        }],
        /**
         * Invert
         * @see https://tailwindcss.com/docs/invert
         */
        invert: [{
          invert: [invert]
        }],
        /**
         * Saturate
         * @see https://tailwindcss.com/docs/saturate
         */
        saturate: [{
          saturate: [saturate]
        }],
        /**
         * Sepia
         * @see https://tailwindcss.com/docs/sepia
         */
        sepia: [{
          sepia: [sepia]
        }],
        /**
         * Backdrop Filter
         * @deprecated since Tailwind CSS v3.0.0
         * @see https://tailwindcss.com/docs/backdrop-filter
         */
        "backdrop-filter": [{
          "backdrop-filter": ["", "none"]
        }],
        /**
         * Backdrop Blur
         * @see https://tailwindcss.com/docs/backdrop-blur
         */
        "backdrop-blur": [{
          "backdrop-blur": [blur]
        }],
        /**
         * Backdrop Brightness
         * @see https://tailwindcss.com/docs/backdrop-brightness
         */
        "backdrop-brightness": [{
          "backdrop-brightness": [brightness]
        }],
        /**
         * Backdrop Contrast
         * @see https://tailwindcss.com/docs/backdrop-contrast
         */
        "backdrop-contrast": [{
          "backdrop-contrast": [contrast]
        }],
        /**
         * Backdrop Grayscale
         * @see https://tailwindcss.com/docs/backdrop-grayscale
         */
        "backdrop-grayscale": [{
          "backdrop-grayscale": [grayscale]
        }],
        /**
         * Backdrop Hue Rotate
         * @see https://tailwindcss.com/docs/backdrop-hue-rotate
         */
        "backdrop-hue-rotate": [{
          "backdrop-hue-rotate": [hueRotate]
        }],
        /**
         * Backdrop Invert
         * @see https://tailwindcss.com/docs/backdrop-invert
         */
        "backdrop-invert": [{
          "backdrop-invert": [invert]
        }],
        /**
         * Backdrop Opacity
         * @see https://tailwindcss.com/docs/backdrop-opacity
         */
        "backdrop-opacity": [{
          "backdrop-opacity": [opacity]
        }],
        /**
         * Backdrop Saturate
         * @see https://tailwindcss.com/docs/backdrop-saturate
         */
        "backdrop-saturate": [{
          "backdrop-saturate": [saturate]
        }],
        /**
         * Backdrop Sepia
         * @see https://tailwindcss.com/docs/backdrop-sepia
         */
        "backdrop-sepia": [{
          "backdrop-sepia": [sepia]
        }],
        // Tables
        /**
         * Border Collapse
         * @see https://tailwindcss.com/docs/border-collapse
         */
        "border-collapse": [{
          border: ["collapse", "separate"]
        }],
        /**
         * Border Spacing
         * @see https://tailwindcss.com/docs/border-spacing
         */
        "border-spacing": [{
          "border-spacing": [borderSpacing]
        }],
        /**
         * Border Spacing X
         * @see https://tailwindcss.com/docs/border-spacing
         */
        "border-spacing-x": [{
          "border-spacing-x": [borderSpacing]
        }],
        /**
         * Border Spacing Y
         * @see https://tailwindcss.com/docs/border-spacing
         */
        "border-spacing-y": [{
          "border-spacing-y": [borderSpacing]
        }],
        /**
         * Table Layout
         * @see https://tailwindcss.com/docs/table-layout
         */
        "table-layout": [{
          table: ["auto", "fixed"]
        }],
        /**
         * Caption Side
         * @see https://tailwindcss.com/docs/caption-side
         */
        caption: [{
          caption: ["top", "bottom"]
        }],
        // Transitions and Animation
        /**
         * Tranisition Property
         * @see https://tailwindcss.com/docs/transition-property
         */
        transition: [{
          transition: ["none", "all", "", "colors", "opacity", "shadow", "transform", isArbitraryValue]
        }],
        /**
         * Transition Duration
         * @see https://tailwindcss.com/docs/transition-duration
         */
        duration: [{
          duration: getNumberAndArbitrary()
        }],
        /**
         * Transition Timing Function
         * @see https://tailwindcss.com/docs/transition-timing-function
         */
        ease: [{
          ease: ["linear", "in", "out", "in-out", isArbitraryValue]
        }],
        /**
         * Transition Delay
         * @see https://tailwindcss.com/docs/transition-delay
         */
        delay: [{
          delay: getNumberAndArbitrary()
        }],
        /**
         * Animation
         * @see https://tailwindcss.com/docs/animation
         */
        animate: [{
          animate: ["none", "spin", "ping", "pulse", "bounce", isArbitraryValue]
        }],
        // Transforms
        /**
         * Transform
         * @see https://tailwindcss.com/docs/transform
         */
        transform: [{
          transform: ["", "gpu", "none"]
        }],
        /**
         * Scale
         * @see https://tailwindcss.com/docs/scale
         */
        scale: [{
          scale: [scale]
        }],
        /**
         * Scale X
         * @see https://tailwindcss.com/docs/scale
         */
        "scale-x": [{
          "scale-x": [scale]
        }],
        /**
         * Scale Y
         * @see https://tailwindcss.com/docs/scale
         */
        "scale-y": [{
          "scale-y": [scale]
        }],
        /**
         * Rotate
         * @see https://tailwindcss.com/docs/rotate
         */
        rotate: [{
          rotate: [isInteger, isArbitraryValue]
        }],
        /**
         * Translate X
         * @see https://tailwindcss.com/docs/translate
         */
        "translate-x": [{
          "translate-x": [translate]
        }],
        /**
         * Translate Y
         * @see https://tailwindcss.com/docs/translate
         */
        "translate-y": [{
          "translate-y": [translate]
        }],
        /**
         * Skew X
         * @see https://tailwindcss.com/docs/skew
         */
        "skew-x": [{
          "skew-x": [skew]
        }],
        /**
         * Skew Y
         * @see https://tailwindcss.com/docs/skew
         */
        "skew-y": [{
          "skew-y": [skew]
        }],
        /**
         * Transform Origin
         * @see https://tailwindcss.com/docs/transform-origin
         */
        "transform-origin": [{
          origin: ["center", "top", "top-right", "right", "bottom-right", "bottom", "bottom-left", "left", "top-left", isArbitraryValue]
        }],
        // Interactivity
        /**
         * Accent Color
         * @see https://tailwindcss.com/docs/accent-color
         */
        accent: [{
          accent: ["auto", colors]
        }],
        /**
         * Appearance
         * @see https://tailwindcss.com/docs/appearance
         */
        appearance: [{
          appearance: ["none", "auto"]
        }],
        /**
         * Cursor
         * @see https://tailwindcss.com/docs/cursor
         */
        cursor: [{
          cursor: ["auto", "default", "pointer", "wait", "text", "move", "help", "not-allowed", "none", "context-menu", "progress", "cell", "crosshair", "vertical-text", "alias", "copy", "no-drop", "grab", "grabbing", "all-scroll", "col-resize", "row-resize", "n-resize", "e-resize", "s-resize", "w-resize", "ne-resize", "nw-resize", "se-resize", "sw-resize", "ew-resize", "ns-resize", "nesw-resize", "nwse-resize", "zoom-in", "zoom-out", isArbitraryValue]
        }],
        /**
         * Caret Color
         * @see https://tailwindcss.com/docs/just-in-time-mode#caret-color-utilities
         */
        "caret-color": [{
          caret: [colors]
        }],
        /**
         * Pointer Events
         * @see https://tailwindcss.com/docs/pointer-events
         */
        "pointer-events": [{
          "pointer-events": ["none", "auto"]
        }],
        /**
         * Resize
         * @see https://tailwindcss.com/docs/resize
         */
        resize: [{
          resize: ["none", "y", "x", ""]
        }],
        /**
         * Scroll Behavior
         * @see https://tailwindcss.com/docs/scroll-behavior
         */
        "scroll-behavior": [{
          scroll: ["auto", "smooth"]
        }],
        /**
         * Scroll Margin
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-m": [{
          "scroll-m": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Margin X
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mx": [{
          "scroll-mx": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Margin Y
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-my": [{
          "scroll-my": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Margin Start
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-ms": [{
          "scroll-ms": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Margin End
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-me": [{
          "scroll-me": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Margin Top
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mt": [{
          "scroll-mt": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Margin Right
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mr": [{
          "scroll-mr": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Margin Bottom
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mb": [{
          "scroll-mb": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Margin Left
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-ml": [{
          "scroll-ml": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Padding
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-p": [{
          "scroll-p": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Padding X
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-px": [{
          "scroll-px": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Padding Y
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-py": [{
          "scroll-py": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Padding Start
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-ps": [{
          "scroll-ps": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Padding End
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pe": [{
          "scroll-pe": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Padding Top
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pt": [{
          "scroll-pt": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Padding Right
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pr": [{
          "scroll-pr": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Padding Bottom
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pb": [{
          "scroll-pb": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Padding Left
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pl": [{
          "scroll-pl": getSpacingWithArbitrary()
        }],
        /**
         * Scroll Snap Align
         * @see https://tailwindcss.com/docs/scroll-snap-align
         */
        "snap-align": [{
          snap: ["start", "end", "center", "align-none"]
        }],
        /**
         * Scroll Snap Stop
         * @see https://tailwindcss.com/docs/scroll-snap-stop
         */
        "snap-stop": [{
          snap: ["normal", "always"]
        }],
        /**
         * Scroll Snap Type
         * @see https://tailwindcss.com/docs/scroll-snap-type
         */
        "snap-type": [{
          snap: ["none", "x", "y", "both"]
        }],
        /**
         * Scroll Snap Type Strictness
         * @see https://tailwindcss.com/docs/scroll-snap-type
         */
        "snap-strictness": [{
          snap: ["mandatory", "proximity"]
        }],
        /**
         * Touch Action
         * @see https://tailwindcss.com/docs/touch-action
         */
        touch: [{
          touch: ["auto", "none", "manipulation"]
        }],
        /**
         * Touch Action X
         * @see https://tailwindcss.com/docs/touch-action
         */
        "touch-x": [{
          "touch-pan": ["x", "left", "right"]
        }],
        /**
         * Touch Action Y
         * @see https://tailwindcss.com/docs/touch-action
         */
        "touch-y": [{
          "touch-pan": ["y", "up", "down"]
        }],
        /**
         * Touch Action Pinch Zoom
         * @see https://tailwindcss.com/docs/touch-action
         */
        "touch-pz": ["touch-pinch-zoom"],
        /**
         * User Select
         * @see https://tailwindcss.com/docs/user-select
         */
        select: [{
          select: ["none", "text", "all", "auto"]
        }],
        /**
         * Will Change
         * @see https://tailwindcss.com/docs/will-change
         */
        "will-change": [{
          "will-change": ["auto", "scroll", "contents", "transform", isArbitraryValue]
        }],
        // SVG
        /**
         * Fill
         * @see https://tailwindcss.com/docs/fill
         */
        fill: [{
          fill: [colors, "none"]
        }],
        /**
         * Stroke Width
         * @see https://tailwindcss.com/docs/stroke-width
         */
        "stroke-w": [{
          stroke: [isLength, isArbitraryLength, isArbitraryNumber]
        }],
        /**
         * Stroke
         * @see https://tailwindcss.com/docs/stroke
         */
        stroke: [{
          stroke: [colors, "none"]
        }],
        // Accessibility
        /**
         * Screen Readers
         * @see https://tailwindcss.com/docs/screen-readers
         */
        sr: ["sr-only", "not-sr-only"],
        /**
         * Forced Color Adjust
         * @see https://tailwindcss.com/docs/forced-color-adjust
         */
        "forced-color-adjust": [{
          "forced-color-adjust": ["auto", "none"]
        }]
      },
      conflictingClassGroups: {
        overflow: ["overflow-x", "overflow-y"],
        overscroll: ["overscroll-x", "overscroll-y"],
        inset: ["inset-x", "inset-y", "start", "end", "top", "right", "bottom", "left"],
        "inset-x": ["right", "left"],
        "inset-y": ["top", "bottom"],
        flex: ["basis", "grow", "shrink"],
        gap: ["gap-x", "gap-y"],
        p: ["px", "py", "ps", "pe", "pt", "pr", "pb", "pl"],
        px: ["pr", "pl"],
        py: ["pt", "pb"],
        m: ["mx", "my", "ms", "me", "mt", "mr", "mb", "ml"],
        mx: ["mr", "ml"],
        my: ["mt", "mb"],
        size: ["w", "h"],
        "font-size": ["leading"],
        "fvn-normal": ["fvn-ordinal", "fvn-slashed-zero", "fvn-figure", "fvn-spacing", "fvn-fraction"],
        "fvn-ordinal": ["fvn-normal"],
        "fvn-slashed-zero": ["fvn-normal"],
        "fvn-figure": ["fvn-normal"],
        "fvn-spacing": ["fvn-normal"],
        "fvn-fraction": ["fvn-normal"],
        "line-clamp": ["display", "overflow"],
        rounded: ["rounded-s", "rounded-e", "rounded-t", "rounded-r", "rounded-b", "rounded-l", "rounded-ss", "rounded-se", "rounded-ee", "rounded-es", "rounded-tl", "rounded-tr", "rounded-br", "rounded-bl"],
        "rounded-s": ["rounded-ss", "rounded-es"],
        "rounded-e": ["rounded-se", "rounded-ee"],
        "rounded-t": ["rounded-tl", "rounded-tr"],
        "rounded-r": ["rounded-tr", "rounded-br"],
        "rounded-b": ["rounded-br", "rounded-bl"],
        "rounded-l": ["rounded-tl", "rounded-bl"],
        "border-spacing": ["border-spacing-x", "border-spacing-y"],
        "border-w": ["border-w-s", "border-w-e", "border-w-t", "border-w-r", "border-w-b", "border-w-l"],
        "border-w-x": ["border-w-r", "border-w-l"],
        "border-w-y": ["border-w-t", "border-w-b"],
        "border-color": ["border-color-s", "border-color-e", "border-color-t", "border-color-r", "border-color-b", "border-color-l"],
        "border-color-x": ["border-color-r", "border-color-l"],
        "border-color-y": ["border-color-t", "border-color-b"],
        "scroll-m": ["scroll-mx", "scroll-my", "scroll-ms", "scroll-me", "scroll-mt", "scroll-mr", "scroll-mb", "scroll-ml"],
        "scroll-mx": ["scroll-mr", "scroll-ml"],
        "scroll-my": ["scroll-mt", "scroll-mb"],
        "scroll-p": ["scroll-px", "scroll-py", "scroll-ps", "scroll-pe", "scroll-pt", "scroll-pr", "scroll-pb", "scroll-pl"],
        "scroll-px": ["scroll-pr", "scroll-pl"],
        "scroll-py": ["scroll-pt", "scroll-pb"],
        touch: ["touch-x", "touch-y", "touch-pz"],
        "touch-x": ["touch"],
        "touch-y": ["touch"],
        "touch-pz": ["touch"]
      },
      conflictingClassGroupModifiers: {
        "font-size": ["leading"]
      }
    };
  };
  var twMerge = /* @__PURE__ */ createTailwindMerge(getDefaultConfig);

  // src/lib/utils/tailwind.ts
  var cn = (...classList) => twMerge(clsx(classList));

  // src/lib/utils/storage.ts
  init_define_import_meta_env();
  function persistData(key, value) {
    localStorage.setItem(key, JSON.stringify(value));
  }
  function getPersistentData(key, defaultValue = null) {
    try {
      return JSON.parse(localStorage.getItem(key) ?? "null") ?? defaultValue;
    } catch {
      return defaultValue;
    }
  }

  // src/lib/utils/urlUtils.ts
  init_define_import_meta_env();

  // src/lib/utils/dataTable.ts
  init_define_import_meta_env();
  var INTERACTIVE_SELECTOR = [
    "button",
    "a",
    "input",
    "select",
    "textarea",
    "label",
    '[role="button"]',
    '[role="link"]',
    '[role="menuitem"]',
    '[role="checkbox"]',
    '[role="radio"]',
    '[role="tab"]',
    '[role="switch"]',
    "[data-disable-row-click]"
  ].join(",");
  var isInteractiveClick = (event) => {
    const target = event.target;
    return !!target?.closest(INTERACTIVE_SELECTOR);
  };
  var getCommonPinningStyles = ({
    column,
    withBorder = false
  }) => {
    const isPinned = column.getIsPinned();
    const isLastLeftPinnedColumn = isPinned === "left" && column.getIsLastColumn("left");
    const isFirstRightPinnedColumn = isPinned === "right" && column.getIsFirstColumn("right");
    return {
      boxShadow: withBorder ? isLastLeftPinnedColumn ? "-4px 0 4px -4px hsl(var(--border)) inset" : isFirstRightPinnedColumn ? "4px 0 4px -4px hsl(var(--border)) inset" : void 0 : void 0,
      left: isPinned === "left" ? `${column.getStart("left")}px` : void 0,
      right: isPinned === "right" ? `${column.getAfter("right")}px` : void 0,
      opacity: isPinned ? 0.97 : 1,
      position: isPinned ? "sticky" : "relative",
      width: column.getSize(),
      zIndex: isPinned ? 1 : 0
    };
  };
  var isAsyncFilterMeta = (meta) => "options" in meta && typeof meta.options === "function";

  // src/lib/utils/toLatinDigits.ts
  init_define_import_meta_env();
  var ARABIC_INDIC_OFFSET = 1632;
  var EXTENDED_ARABIC_INDIC_OFFSET = 1776;
  var toLatinDigits = (value) => {
    if (!value) return value;
    return value.replace(/[٠-٩۰-۹]/g, (digit) => {
      const code = digit.charCodeAt(0);
      const base = code >= EXTENDED_ARABIC_INDIC_OFFSET ? EXTENDED_ARABIC_INDIC_OFFSET : ARABIC_INDIC_OFFSET;
      return String(code - base);
    });
  };

  // src/lib/utils/formatNumber.ts
  init_define_import_meta_env();

  // src/lib/hooks/shared/useStorage.ts
  var useStorage = (storageKey, defaultValue = null) => {
    const storageValue = (0, import_react13.useMemo)(() => {
      return getPersistentData(storageKey, defaultValue);
    }, [defaultValue, storageKey]);
    const setStorageValue = (0, import_react13.useCallback)(
      (storageValue2) => {
        persistData(storageKey, storageValue2);
      },
      [storageKey]
    );
    return [storageValue, setStorageValue];
  };

  // src/lib/hooks/shared/useDebounce.ts
  init_define_import_meta_env();
  var import_react14 = __toESM(require_react_shim(), 1);
  function useDebounce(value, delay = 500) {
    const [debouncedValue, setDebouncedValue] = (0, import_react14.useState)(value);
    (0, import_react14.useEffect)(() => {
      const timer = setTimeout(() => setDebouncedValue(value), delay);
      return () => {
        clearTimeout(timer);
      };
    }, [value, delay]);
    return debouncedValue;
  }

  // src/lib/hooks/shared/useDate.ts
  init_define_import_meta_env();

  // node_modules/date-fns/add.js
  init_define_import_meta_env();

  // node_modules/date-fns/addDays.js
  init_define_import_meta_env();

  // node_modules/date-fns/constructFrom.js
  init_define_import_meta_env();

  // node_modules/date-fns/constants.js
  init_define_import_meta_env();
  var daysInYear = 365.2425;
  var maxTime = Math.pow(10, 8) * 24 * 60 * 60 * 1e3;
  var minTime = -maxTime;
  var millisecondsInWeek = 6048e5;
  var millisecondsInDay = 864e5;
  var millisecondsInMinute = 6e4;
  var millisecondsInHour = 36e5;
  var millisecondsInSecond = 1e3;
  var minutesInMonth = 43200;
  var minutesInDay = 1440;
  var secondsInHour = 3600;
  var secondsInDay = secondsInHour * 24;
  var secondsInWeek = secondsInDay * 7;
  var secondsInYear = secondsInDay * daysInYear;
  var secondsInMonth = secondsInYear / 12;
  var secondsInQuarter = secondsInMonth * 3;
  var constructFromSymbol = /* @__PURE__ */ Symbol.for("constructDateFrom");

  // node_modules/date-fns/constructFrom.js
  function constructFrom(date, value) {
    if (typeof date === "function") return date(value);
    if (date && typeof date === "object" && constructFromSymbol in date)
      return date[constructFromSymbol](value);
    if (date instanceof Date) return new date.constructor(value);
    return new Date(value);
  }

  // node_modules/date-fns/toDate.js
  init_define_import_meta_env();
  function toDate(argument, context2) {
    return constructFrom(context2 || argument, argument);
  }

  // node_modules/date-fns/addDays.js
  function addDays(date, amount, options3) {
    const _date = toDate(date, options3?.in);
    if (isNaN(amount)) return constructFrom(options3?.in || date, NaN);
    if (!amount) return _date;
    _date.setDate(_date.getDate() + amount);
    return _date;
  }

  // node_modules/date-fns/addMonths.js
  init_define_import_meta_env();
  function addMonths(date, amount, options3) {
    const _date = toDate(date, options3?.in);
    if (isNaN(amount)) return constructFrom(options3?.in || date, NaN);
    if (!amount) {
      return _date;
    }
    const dayOfMonth = _date.getDate();
    const endOfDesiredMonth = constructFrom(options3?.in || date, _date.getTime());
    endOfDesiredMonth.setMonth(_date.getMonth() + amount + 1, 0);
    const daysInMonth = endOfDesiredMonth.getDate();
    if (dayOfMonth >= daysInMonth) {
      return endOfDesiredMonth;
    } else {
      _date.setFullYear(
        endOfDesiredMonth.getFullYear(),
        endOfDesiredMonth.getMonth(),
        dayOfMonth
      );
      return _date;
    }
  }

  // node_modules/date-fns/add.js
  function add(date, duration, options3) {
    const {
      years = 0,
      months = 0,
      weeks = 0,
      days = 0,
      hours = 0,
      minutes = 0,
      seconds = 0
    } = duration;
    const _date = toDate(date, options3?.in);
    const dateWithMonths = months || years ? addMonths(_date, months + years * 12) : _date;
    const dateWithDays = days || weeks ? addDays(dateWithMonths, days + weeks * 7) : dateWithMonths;
    const minutesToAdd = minutes + hours * 60;
    const secondsToAdd = seconds + minutesToAdd * 60;
    const msToAdd = secondsToAdd * 1e3;
    return constructFrom(options3?.in || date, +dateWithDays + msToAdd);
  }

  // node_modules/date-fns/getISOWeekYear.js
  init_define_import_meta_env();

  // node_modules/date-fns/startOfISOWeek.js
  init_define_import_meta_env();

  // node_modules/date-fns/startOfWeek.js
  init_define_import_meta_env();

  // node_modules/date-fns/_lib/defaultOptions.js
  init_define_import_meta_env();
  var defaultOptions2 = {};
  function getDefaultOptions() {
    return defaultOptions2;
  }

  // node_modules/date-fns/startOfWeek.js
  function startOfWeek(date, options3) {
    const defaultOptions4 = getDefaultOptions();
    const weekStartsOn = options3?.weekStartsOn ?? options3?.locale?.options?.weekStartsOn ?? defaultOptions4.weekStartsOn ?? defaultOptions4.locale?.options?.weekStartsOn ?? 0;
    const _date = toDate(date, options3?.in);
    const day = _date.getDay();
    const diff = (day < weekStartsOn ? 7 : 0) + day - weekStartsOn;
    _date.setDate(_date.getDate() - diff);
    _date.setHours(0, 0, 0, 0);
    return _date;
  }

  // node_modules/date-fns/startOfISOWeek.js
  function startOfISOWeek(date, options3) {
    return startOfWeek(date, { ...options3, weekStartsOn: 1 });
  }

  // node_modules/date-fns/getISOWeekYear.js
  function getISOWeekYear(date, options3) {
    const _date = toDate(date, options3?.in);
    const year = _date.getFullYear();
    const fourthOfJanuaryOfNextYear = constructFrom(_date, 0);
    fourthOfJanuaryOfNextYear.setFullYear(year + 1, 0, 4);
    fourthOfJanuaryOfNextYear.setHours(0, 0, 0, 0);
    const startOfNextYear = startOfISOWeek(fourthOfJanuaryOfNextYear);
    const fourthOfJanuaryOfThisYear = constructFrom(_date, 0);
    fourthOfJanuaryOfThisYear.setFullYear(year, 0, 4);
    fourthOfJanuaryOfThisYear.setHours(0, 0, 0, 0);
    const startOfThisYear = startOfISOWeek(fourthOfJanuaryOfThisYear);
    if (_date.getTime() >= startOfNextYear.getTime()) {
      return year + 1;
    } else if (_date.getTime() >= startOfThisYear.getTime()) {
      return year;
    } else {
      return year - 1;
    }
  }

  // node_modules/date-fns/differenceInCalendarDays.js
  init_define_import_meta_env();

  // node_modules/date-fns/_lib/getTimezoneOffsetInMilliseconds.js
  init_define_import_meta_env();
  function getTimezoneOffsetInMilliseconds(date) {
    const _date = toDate(date);
    const utcDate = new Date(
      Date.UTC(
        _date.getFullYear(),
        _date.getMonth(),
        _date.getDate(),
        _date.getHours(),
        _date.getMinutes(),
        _date.getSeconds(),
        _date.getMilliseconds()
      )
    );
    utcDate.setUTCFullYear(_date.getFullYear());
    return +date - +utcDate;
  }

  // node_modules/date-fns/_lib/normalizeDates.js
  init_define_import_meta_env();
  function normalizeDates(context2, ...dates) {
    const normalize2 = constructFrom.bind(
      null,
      context2 || dates.find((date) => typeof date === "object")
    );
    return dates.map(normalize2);
  }

  // node_modules/date-fns/startOfDay.js
  init_define_import_meta_env();
  function startOfDay(date, options3) {
    const _date = toDate(date, options3?.in);
    _date.setHours(0, 0, 0, 0);
    return _date;
  }

  // node_modules/date-fns/differenceInCalendarDays.js
  function differenceInCalendarDays(laterDate, earlierDate, options3) {
    const [laterDate_, earlierDate_] = normalizeDates(
      options3?.in,
      laterDate,
      earlierDate
    );
    const laterStartOfDay = startOfDay(laterDate_);
    const earlierStartOfDay = startOfDay(earlierDate_);
    const laterTimestamp = +laterStartOfDay - getTimezoneOffsetInMilliseconds(laterStartOfDay);
    const earlierTimestamp = +earlierStartOfDay - getTimezoneOffsetInMilliseconds(earlierStartOfDay);
    return Math.round((laterTimestamp - earlierTimestamp) / millisecondsInDay);
  }

  // node_modules/date-fns/startOfISOWeekYear.js
  init_define_import_meta_env();
  function startOfISOWeekYear(date, options3) {
    const year = getISOWeekYear(date, options3);
    const fourthOfJanuary = constructFrom(options3?.in || date, 0);
    fourthOfJanuary.setFullYear(year, 0, 4);
    fourthOfJanuary.setHours(0, 0, 0, 0);
    return startOfISOWeek(fourthOfJanuary);
  }

  // node_modules/date-fns/addWeeks.js
  init_define_import_meta_env();
  function addWeeks(date, amount, options3) {
    return addDays(date, amount * 7, options3);
  }

  // node_modules/date-fns/addYears.js
  init_define_import_meta_env();
  function addYears(date, amount, options3) {
    return addMonths(date, amount * 12, options3);
  }

  // node_modules/date-fns/max.js
  init_define_import_meta_env();
  function max(dates, options3) {
    let result;
    let context2 = options3?.in;
    dates.forEach((date) => {
      if (!context2 && typeof date === "object")
        context2 = constructFrom.bind(null, date);
      const date_ = toDate(date, context2);
      if (!result || result < date_ || isNaN(+date_)) result = date_;
    });
    return constructFrom(context2, result || NaN);
  }

  // node_modules/date-fns/min.js
  init_define_import_meta_env();
  function min(dates, options3) {
    let result;
    let context2 = options3?.in;
    dates.forEach((date) => {
      if (!context2 && typeof date === "object")
        context2 = constructFrom.bind(null, date);
      const date_ = toDate(date, context2);
      if (!result || result > date_ || isNaN(+date_)) result = date_;
    });
    return constructFrom(context2, result || NaN);
  }

  // node_modules/date-fns/compareAsc.js
  init_define_import_meta_env();
  function compareAsc(dateLeft, dateRight) {
    const diff = +toDate(dateLeft) - +toDate(dateRight);
    if (diff < 0) return -1;
    else if (diff > 0) return 1;
    return diff;
  }

  // node_modules/date-fns/constructNow.js
  init_define_import_meta_env();
  function constructNow(date) {
    return constructFrom(date, Date.now());
  }

  // node_modules/date-fns/isSameDay.js
  init_define_import_meta_env();
  function isSameDay(laterDate, earlierDate, options3) {
    const [dateLeft_, dateRight_] = normalizeDates(
      options3?.in,
      laterDate,
      earlierDate
    );
    return +startOfDay(dateLeft_) === +startOfDay(dateRight_);
  }

  // node_modules/date-fns/isValid.js
  init_define_import_meta_env();

  // node_modules/date-fns/isDate.js
  init_define_import_meta_env();
  function isDate(value) {
    return value instanceof Date || typeof value === "object" && Object.prototype.toString.call(value) === "[object Date]";
  }

  // node_modules/date-fns/isValid.js
  function isValid(date) {
    return !(!isDate(date) && typeof date !== "number" || isNaN(+toDate(date)));
  }

  // node_modules/date-fns/differenceInCalendarMonths.js
  init_define_import_meta_env();
  function differenceInCalendarMonths(laterDate, earlierDate, options3) {
    const [laterDate_, earlierDate_] = normalizeDates(
      options3?.in,
      laterDate,
      earlierDate
    );
    const yearsDiff = laterDate_.getFullYear() - earlierDate_.getFullYear();
    const monthsDiff = laterDate_.getMonth() - earlierDate_.getMonth();
    return yearsDiff * 12 + monthsDiff;
  }

  // node_modules/date-fns/differenceInCalendarYears.js
  init_define_import_meta_env();
  function differenceInCalendarYears(laterDate, earlierDate, options3) {
    const [laterDate_, earlierDate_] = normalizeDates(
      options3?.in,
      laterDate,
      earlierDate
    );
    return laterDate_.getFullYear() - earlierDate_.getFullYear();
  }

  // node_modules/date-fns/differenceInDays.js
  init_define_import_meta_env();
  function differenceInDays(laterDate, earlierDate, options3) {
    const [laterDate_, earlierDate_] = normalizeDates(
      options3?.in,
      laterDate,
      earlierDate
    );
    const sign = compareLocalAsc(laterDate_, earlierDate_);
    const difference = Math.abs(
      differenceInCalendarDays(laterDate_, earlierDate_)
    );
    laterDate_.setDate(laterDate_.getDate() - sign * difference);
    const isLastDayNotFull = Number(
      compareLocalAsc(laterDate_, earlierDate_) === -sign
    );
    const result = sign * (difference - isLastDayNotFull);
    return result === 0 ? 0 : result;
  }
  function compareLocalAsc(laterDate, earlierDate) {
    const diff = laterDate.getFullYear() - earlierDate.getFullYear() || laterDate.getMonth() - earlierDate.getMonth() || laterDate.getDate() - earlierDate.getDate() || laterDate.getHours() - earlierDate.getHours() || laterDate.getMinutes() - earlierDate.getMinutes() || laterDate.getSeconds() - earlierDate.getSeconds() || laterDate.getMilliseconds() - earlierDate.getMilliseconds();
    if (diff < 0) return -1;
    if (diff > 0) return 1;
    return diff;
  }

  // node_modules/date-fns/differenceInHours.js
  init_define_import_meta_env();

  // node_modules/date-fns/_lib/getRoundingMethod.js
  init_define_import_meta_env();
  function getRoundingMethod(method) {
    return (number) => {
      const round2 = method ? Math[method] : Math.trunc;
      const result = round2(number);
      return result === 0 ? 0 : result;
    };
  }

  // node_modules/date-fns/differenceInHours.js
  function differenceInHours(laterDate, earlierDate, options3) {
    const [laterDate_, earlierDate_] = normalizeDates(
      options3?.in,
      laterDate,
      earlierDate
    );
    const diff = (+laterDate_ - +earlierDate_) / millisecondsInHour;
    return getRoundingMethod(options3?.roundingMethod)(diff);
  }

  // node_modules/date-fns/differenceInMilliseconds.js
  init_define_import_meta_env();
  function differenceInMilliseconds(laterDate, earlierDate) {
    return +toDate(laterDate) - +toDate(earlierDate);
  }

  // node_modules/date-fns/differenceInMinutes.js
  init_define_import_meta_env();
  function differenceInMinutes(dateLeft, dateRight, options3) {
    const diff = differenceInMilliseconds(dateLeft, dateRight) / millisecondsInMinute;
    return getRoundingMethod(options3?.roundingMethod)(diff);
  }

  // node_modules/date-fns/differenceInMonths.js
  init_define_import_meta_env();

  // node_modules/date-fns/isLastDayOfMonth.js
  init_define_import_meta_env();

  // node_modules/date-fns/endOfDay.js
  init_define_import_meta_env();
  function endOfDay(date, options3) {
    const _date = toDate(date, options3?.in);
    _date.setHours(23, 59, 59, 999);
    return _date;
  }

  // node_modules/date-fns/endOfMonth.js
  init_define_import_meta_env();
  function endOfMonth(date, options3) {
    const _date = toDate(date, options3?.in);
    const month = _date.getMonth();
    _date.setFullYear(_date.getFullYear(), month + 1, 0);
    _date.setHours(23, 59, 59, 999);
    return _date;
  }

  // node_modules/date-fns/isLastDayOfMonth.js
  function isLastDayOfMonth(date, options3) {
    const _date = toDate(date, options3?.in);
    return +endOfDay(_date, options3) === +endOfMonth(_date, options3);
  }

  // node_modules/date-fns/differenceInMonths.js
  function differenceInMonths(laterDate, earlierDate, options3) {
    const [laterDate_, workingLaterDate, earlierDate_] = normalizeDates(
      options3?.in,
      laterDate,
      laterDate,
      earlierDate
    );
    const sign = compareAsc(workingLaterDate, earlierDate_);
    const difference = Math.abs(
      differenceInCalendarMonths(workingLaterDate, earlierDate_)
    );
    if (difference < 1) return 0;
    if (workingLaterDate.getMonth() === 1 && workingLaterDate.getDate() > 27)
      workingLaterDate.setDate(30);
    workingLaterDate.setMonth(workingLaterDate.getMonth() - sign * difference);
    let isLastMonthNotFull = compareAsc(workingLaterDate, earlierDate_) === -sign;
    if (isLastDayOfMonth(laterDate_) && difference === 1 && compareAsc(laterDate_, earlierDate_) === 1) {
      isLastMonthNotFull = false;
    }
    const result = sign * (difference - +isLastMonthNotFull);
    return result === 0 ? 0 : result;
  }

  // node_modules/date-fns/differenceInSeconds.js
  init_define_import_meta_env();
  function differenceInSeconds(laterDate, earlierDate, options3) {
    const diff = differenceInMilliseconds(laterDate, earlierDate) / 1e3;
    return getRoundingMethod(options3?.roundingMethod)(diff);
  }

  // node_modules/date-fns/differenceInYears.js
  init_define_import_meta_env();
  function differenceInYears(laterDate, earlierDate, options3) {
    const [laterDate_, earlierDate_] = normalizeDates(
      options3?.in,
      laterDate,
      earlierDate
    );
    const sign = compareAsc(laterDate_, earlierDate_);
    const diff = Math.abs(differenceInCalendarYears(laterDate_, earlierDate_));
    laterDate_.setFullYear(1584);
    earlierDate_.setFullYear(1584);
    const partial = compareAsc(laterDate_, earlierDate_) === -sign;
    const result = sign * (diff - +partial);
    return result === 0 ? 0 : result;
  }

  // node_modules/date-fns/eachDayOfInterval.js
  init_define_import_meta_env();

  // node_modules/date-fns/_lib/normalizeInterval.js
  init_define_import_meta_env();
  function normalizeInterval(context2, interval) {
    const [start, end] = normalizeDates(context2, interval.start, interval.end);
    return { start, end };
  }

  // node_modules/date-fns/eachDayOfInterval.js
  function eachDayOfInterval(interval, options3) {
    const { start, end } = normalizeInterval(options3?.in, interval);
    let reversed = +start > +end;
    const endTime = reversed ? +start : +end;
    const date = reversed ? end : start;
    date.setHours(0, 0, 0, 0);
    let step = options3?.step ?? 1;
    if (!step) return [];
    if (step < 0) {
      step = -step;
      reversed = !reversed;
    }
    const dates = [];
    while (+date <= endTime) {
      dates.push(constructFrom(start, date));
      date.setDate(date.getDate() + step);
      date.setHours(0, 0, 0, 0);
    }
    return reversed ? dates.reverse() : dates;
  }

  // node_modules/date-fns/eachMonthOfInterval.js
  init_define_import_meta_env();
  function eachMonthOfInterval(interval, options3) {
    const { start, end } = normalizeInterval(options3?.in, interval);
    let reversed = +start > +end;
    const endTime = reversed ? +start : +end;
    const date = reversed ? end : start;
    date.setHours(0, 0, 0, 0);
    date.setDate(1);
    let step = options3?.step ?? 1;
    if (!step) return [];
    if (step < 0) {
      step = -step;
      reversed = !reversed;
    }
    const dates = [];
    while (+date <= endTime) {
      dates.push(constructFrom(start, date));
      date.setMonth(date.getMonth() + step);
    }
    return reversed ? dates.reverse() : dates;
  }

  // node_modules/date-fns/startOfMonth.js
  init_define_import_meta_env();
  function startOfMonth(date, options3) {
    const _date = toDate(date, options3?.in);
    _date.setDate(1);
    _date.setHours(0, 0, 0, 0);
    return _date;
  }

  // node_modules/date-fns/endOfYear.js
  init_define_import_meta_env();
  function endOfYear(date, options3) {
    const _date = toDate(date, options3?.in);
    const year = _date.getFullYear();
    _date.setFullYear(year + 1, 0, 0);
    _date.setHours(23, 59, 59, 999);
    return _date;
  }

  // node_modules/date-fns/startOfYear.js
  init_define_import_meta_env();
  function startOfYear(date, options3) {
    const date_ = toDate(date, options3?.in);
    date_.setFullYear(date_.getFullYear(), 0, 1);
    date_.setHours(0, 0, 0, 0);
    return date_;
  }

  // node_modules/date-fns/eachYearOfInterval.js
  init_define_import_meta_env();
  function eachYearOfInterval(interval, options3) {
    const { start, end } = normalizeInterval(options3?.in, interval);
    let reversed = +start > +end;
    const endTime = reversed ? +start : +end;
    const date = reversed ? end : start;
    date.setHours(0, 0, 0, 0);
    date.setMonth(0, 1);
    let step = options3?.step ?? 1;
    if (!step) return [];
    if (step < 0) {
      step = -step;
      reversed = !reversed;
    }
    const dates = [];
    while (+date <= endTime) {
      dates.push(constructFrom(start, date));
      date.setFullYear(date.getFullYear() + step);
    }
    return reversed ? dates.reverse() : dates;
  }

  // node_modules/date-fns/endOfISOWeek.js
  init_define_import_meta_env();

  // node_modules/date-fns/endOfWeek.js
  init_define_import_meta_env();
  function endOfWeek(date, options3) {
    const defaultOptions4 = getDefaultOptions();
    const weekStartsOn = options3?.weekStartsOn ?? options3?.locale?.options?.weekStartsOn ?? defaultOptions4.weekStartsOn ?? defaultOptions4.locale?.options?.weekStartsOn ?? 0;
    const _date = toDate(date, options3?.in);
    const day = _date.getDay();
    const diff = (day < weekStartsOn ? -7 : 0) + 6 - (day - weekStartsOn);
    _date.setDate(_date.getDate() + diff);
    _date.setHours(23, 59, 59, 999);
    return _date;
  }

  // node_modules/date-fns/endOfISOWeek.js
  function endOfISOWeek(date, options3) {
    return endOfWeek(date, { ...options3, weekStartsOn: 1 });
  }

  // node_modules/date-fns/format.js
  init_define_import_meta_env();

  // node_modules/date-fns/_lib/defaultLocale.js
  init_define_import_meta_env();

  // node_modules/date-fns/locale/en-US.js
  init_define_import_meta_env();

  // node_modules/date-fns/locale/en-US/_lib/formatDistance.js
  init_define_import_meta_env();
  var formatDistanceLocale = {
    lessThanXSeconds: {
      one: "less than a second",
      other: "less than {{count}} seconds"
    },
    xSeconds: {
      one: "1 second",
      other: "{{count}} seconds"
    },
    halfAMinute: "half a minute",
    lessThanXMinutes: {
      one: "less than a minute",
      other: "less than {{count}} minutes"
    },
    xMinutes: {
      one: "1 minute",
      other: "{{count}} minutes"
    },
    aboutXHours: {
      one: "about 1 hour",
      other: "about {{count}} hours"
    },
    xHours: {
      one: "1 hour",
      other: "{{count}} hours"
    },
    xDays: {
      one: "1 day",
      other: "{{count}} days"
    },
    aboutXWeeks: {
      one: "about 1 week",
      other: "about {{count}} weeks"
    },
    xWeeks: {
      one: "1 week",
      other: "{{count}} weeks"
    },
    aboutXMonths: {
      one: "about 1 month",
      other: "about {{count}} months"
    },
    xMonths: {
      one: "1 month",
      other: "{{count}} months"
    },
    aboutXYears: {
      one: "about 1 year",
      other: "about {{count}} years"
    },
    xYears: {
      one: "1 year",
      other: "{{count}} years"
    },
    overXYears: {
      one: "over 1 year",
      other: "over {{count}} years"
    },
    almostXYears: {
      one: "almost 1 year",
      other: "almost {{count}} years"
    }
  };
  var formatDistance = (token, count4, options3) => {
    let result;
    const tokenValue = formatDistanceLocale[token];
    if (typeof tokenValue === "string") {
      result = tokenValue;
    } else if (count4 === 1) {
      result = tokenValue.one;
    } else {
      result = tokenValue.other.replace("{{count}}", count4.toString());
    }
    if (options3?.addSuffix) {
      if (options3.comparison && options3.comparison > 0) {
        return "in " + result;
      } else {
        return result + " ago";
      }
    }
    return result;
  };

  // node_modules/date-fns/locale/en-US/_lib/formatLong.js
  init_define_import_meta_env();

  // node_modules/date-fns/locale/_lib/buildFormatLongFn.js
  init_define_import_meta_env();
  function buildFormatLongFn(args) {
    return (options3 = {}) => {
      const width = options3.width ? String(options3.width) : args.defaultWidth;
      const format3 = args.formats[width] || args.formats[args.defaultWidth];
      return format3;
    };
  }

  // node_modules/date-fns/locale/en-US/_lib/formatLong.js
  var dateFormats = {
    full: "EEEE, MMMM do, y",
    long: "MMMM do, y",
    medium: "MMM d, y",
    short: "MM/dd/yyyy"
  };
  var timeFormats = {
    full: "h:mm:ss a zzzz",
    long: "h:mm:ss a z",
    medium: "h:mm:ss a",
    short: "h:mm a"
  };
  var dateTimeFormats = {
    full: "{{date}} 'at' {{time}}",
    long: "{{date}} 'at' {{time}}",
    medium: "{{date}}, {{time}}",
    short: "{{date}}, {{time}}"
  };
  var formatLong = {
    date: buildFormatLongFn({
      formats: dateFormats,
      defaultWidth: "full"
    }),
    time: buildFormatLongFn({
      formats: timeFormats,
      defaultWidth: "full"
    }),
    dateTime: buildFormatLongFn({
      formats: dateTimeFormats,
      defaultWidth: "full"
    })
  };

  // node_modules/date-fns/locale/en-US/_lib/formatRelative.js
  init_define_import_meta_env();
  var formatRelativeLocale = {
    lastWeek: "'last' eeee 'at' p",
    yesterday: "'yesterday at' p",
    today: "'today at' p",
    tomorrow: "'tomorrow at' p",
    nextWeek: "eeee 'at' p",
    other: "P"
  };
  var formatRelative = (token, _date, _baseDate, _options) => formatRelativeLocale[token];

  // node_modules/date-fns/locale/en-US/_lib/localize.js
  init_define_import_meta_env();

  // node_modules/date-fns/locale/_lib/buildLocalizeFn.js
  init_define_import_meta_env();
  function buildLocalizeFn(args) {
    return (value, options3) => {
      const context2 = options3?.context ? String(options3.context) : "standalone";
      let valuesArray;
      if (context2 === "formatting" && args.formattingValues) {
        const defaultWidth = args.defaultFormattingWidth || args.defaultWidth;
        const width = options3?.width ? String(options3.width) : defaultWidth;
        valuesArray = args.formattingValues[width] || args.formattingValues[defaultWidth];
      } else {
        const defaultWidth = args.defaultWidth;
        const width = options3?.width ? String(options3.width) : args.defaultWidth;
        valuesArray = args.values[width] || args.values[defaultWidth];
      }
      const index2 = args.argumentCallback ? args.argumentCallback(value) : value;
      return valuesArray[index2];
    };
  }

  // node_modules/date-fns/locale/en-US/_lib/localize.js
  var eraValues = {
    narrow: ["B", "A"],
    abbreviated: ["BC", "AD"],
    wide: ["Before Christ", "Anno Domini"]
  };
  var quarterValues = {
    narrow: ["1", "2", "3", "4"],
    abbreviated: ["Q1", "Q2", "Q3", "Q4"],
    wide: ["1st quarter", "2nd quarter", "3rd quarter", "4th quarter"]
  };
  var monthValues = {
    narrow: ["J", "F", "M", "A", "M", "J", "J", "A", "S", "O", "N", "D"],
    abbreviated: [
      "Jan",
      "Feb",
      "Mar",
      "Apr",
      "May",
      "Jun",
      "Jul",
      "Aug",
      "Sep",
      "Oct",
      "Nov",
      "Dec"
    ],
    wide: [
      "January",
      "February",
      "March",
      "April",
      "May",
      "June",
      "July",
      "August",
      "September",
      "October",
      "November",
      "December"
    ]
  };
  var dayValues = {
    narrow: ["S", "M", "T", "W", "T", "F", "S"],
    short: ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"],
    abbreviated: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
    wide: [
      "Sunday",
      "Monday",
      "Tuesday",
      "Wednesday",
      "Thursday",
      "Friday",
      "Saturday"
    ]
  };
  var dayPeriodValues = {
    narrow: {
      am: "a",
      pm: "p",
      midnight: "mi",
      noon: "n",
      morning: "morning",
      afternoon: "afternoon",
      evening: "evening",
      night: "night"
    },
    abbreviated: {
      am: "AM",
      pm: "PM",
      midnight: "midnight",
      noon: "noon",
      morning: "morning",
      afternoon: "afternoon",
      evening: "evening",
      night: "night"
    },
    wide: {
      am: "a.m.",
      pm: "p.m.",
      midnight: "midnight",
      noon: "noon",
      morning: "morning",
      afternoon: "afternoon",
      evening: "evening",
      night: "night"
    }
  };
  var formattingDayPeriodValues = {
    narrow: {
      am: "a",
      pm: "p",
      midnight: "mi",
      noon: "n",
      morning: "in the morning",
      afternoon: "in the afternoon",
      evening: "in the evening",
      night: "at night"
    },
    abbreviated: {
      am: "AM",
      pm: "PM",
      midnight: "midnight",
      noon: "noon",
      morning: "in the morning",
      afternoon: "in the afternoon",
      evening: "in the evening",
      night: "at night"
    },
    wide: {
      am: "a.m.",
      pm: "p.m.",
      midnight: "midnight",
      noon: "noon",
      morning: "in the morning",
      afternoon: "in the afternoon",
      evening: "in the evening",
      night: "at night"
    }
  };
  var ordinalNumber = (dirtyNumber, _options) => {
    const number = Number(dirtyNumber);
    const rem100 = number % 100;
    if (rem100 > 20 || rem100 < 10) {
      switch (rem100 % 10) {
        case 1:
          return number + "st";
        case 2:
          return number + "nd";
        case 3:
          return number + "rd";
      }
    }
    return number + "th";
  };
  var localize = {
    ordinalNumber,
    era: buildLocalizeFn({
      values: eraValues,
      defaultWidth: "wide"
    }),
    quarter: buildLocalizeFn({
      values: quarterValues,
      defaultWidth: "wide",
      argumentCallback: (quarter) => quarter - 1
    }),
    month: buildLocalizeFn({
      values: monthValues,
      defaultWidth: "wide"
    }),
    day: buildLocalizeFn({
      values: dayValues,
      defaultWidth: "wide"
    }),
    dayPeriod: buildLocalizeFn({
      values: dayPeriodValues,
      defaultWidth: "wide",
      formattingValues: formattingDayPeriodValues,
      defaultFormattingWidth: "wide"
    })
  };

  // node_modules/date-fns/locale/en-US/_lib/match.js
  init_define_import_meta_env();

  // node_modules/date-fns/locale/_lib/buildMatchFn.js
  init_define_import_meta_env();
  function buildMatchFn(args) {
    return (string, options3 = {}) => {
      const width = options3.width;
      const matchPattern = width && args.matchPatterns[width] || args.matchPatterns[args.defaultMatchWidth];
      const matchResult = string.match(matchPattern);
      if (!matchResult) {
        return null;
      }
      const matchedString = matchResult[0];
      const parsePatterns = width && args.parsePatterns[width] || args.parsePatterns[args.defaultParseWidth];
      const key = Array.isArray(parsePatterns) ? findIndex(parsePatterns, (pattern) => pattern.test(matchedString)) : (
        // [TODO] -- I challenge you to fix the type
        findKey(parsePatterns, (pattern) => pattern.test(matchedString))
      );
      let value;
      value = args.valueCallback ? args.valueCallback(key) : key;
      value = options3.valueCallback ? (
        // [TODO] -- I challenge you to fix the type
        options3.valueCallback(value)
      ) : value;
      const rest = string.slice(matchedString.length);
      return { value, rest };
    };
  }
  function findKey(object, predicate) {
    for (const key in object) {
      if (Object.prototype.hasOwnProperty.call(object, key) && predicate(object[key])) {
        return key;
      }
    }
    return void 0;
  }
  function findIndex(array, predicate) {
    for (let key = 0; key < array.length; key++) {
      if (predicate(array[key])) {
        return key;
      }
    }
    return void 0;
  }

  // node_modules/date-fns/locale/_lib/buildMatchPatternFn.js
  init_define_import_meta_env();
  function buildMatchPatternFn(args) {
    return (string, options3 = {}) => {
      const matchResult = string.match(args.matchPattern);
      if (!matchResult) return null;
      const matchedString = matchResult[0];
      const parseResult = string.match(args.parsePattern);
      if (!parseResult) return null;
      let value = args.valueCallback ? args.valueCallback(parseResult[0]) : parseResult[0];
      value = options3.valueCallback ? options3.valueCallback(value) : value;
      const rest = string.slice(matchedString.length);
      return { value, rest };
    };
  }

  // node_modules/date-fns/locale/en-US/_lib/match.js
  var matchOrdinalNumberPattern = /^(\d+)(th|st|nd|rd)?/i;
  var parseOrdinalNumberPattern = /\d+/i;
  var matchEraPatterns = {
    narrow: /^(b|a)/i,
    abbreviated: /^(b\.?\s?c\.?|b\.?\s?c\.?\s?e\.?|a\.?\s?d\.?|c\.?\s?e\.?)/i,
    wide: /^(before christ|before common era|anno domini|common era)/i
  };
  var parseEraPatterns = {
    any: [/^b/i, /^(a|c)/i]
  };
  var matchQuarterPatterns = {
    narrow: /^[1234]/i,
    abbreviated: /^q[1234]/i,
    wide: /^[1234](th|st|nd|rd)? quarter/i
  };
  var parseQuarterPatterns = {
    any: [/1/i, /2/i, /3/i, /4/i]
  };
  var matchMonthPatterns = {
    narrow: /^[jfmasond]/i,
    abbreviated: /^(jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec)/i,
    wide: /^(january|february|march|april|may|june|july|august|september|october|november|december)/i
  };
  var parseMonthPatterns = {
    narrow: [
      /^j/i,
      /^f/i,
      /^m/i,
      /^a/i,
      /^m/i,
      /^j/i,
      /^j/i,
      /^a/i,
      /^s/i,
      /^o/i,
      /^n/i,
      /^d/i
    ],
    any: [
      /^ja/i,
      /^f/i,
      /^mar/i,
      /^ap/i,
      /^may/i,
      /^jun/i,
      /^jul/i,
      /^au/i,
      /^s/i,
      /^o/i,
      /^n/i,
      /^d/i
    ]
  };
  var matchDayPatterns = {
    narrow: /^[smtwf]/i,
    short: /^(su|mo|tu|we|th|fr|sa)/i,
    abbreviated: /^(sun|mon|tue|wed|thu|fri|sat)/i,
    wide: /^(sunday|monday|tuesday|wednesday|thursday|friday|saturday)/i
  };
  var parseDayPatterns = {
    narrow: [/^s/i, /^m/i, /^t/i, /^w/i, /^t/i, /^f/i, /^s/i],
    any: [/^su/i, /^m/i, /^tu/i, /^w/i, /^th/i, /^f/i, /^sa/i]
  };
  var matchDayPeriodPatterns = {
    narrow: /^(a|p|mi|n|(in the|at) (morning|afternoon|evening|night))/i,
    any: /^([ap]\.?\s?m\.?|midnight|noon|(in the|at) (morning|afternoon|evening|night))/i
  };
  var parseDayPeriodPatterns = {
    any: {
      am: /^a/i,
      pm: /^p/i,
      midnight: /^mi/i,
      noon: /^no/i,
      morning: /morning/i,
      afternoon: /afternoon/i,
      evening: /evening/i,
      night: /night/i
    }
  };
  var match = {
    ordinalNumber: buildMatchPatternFn({
      matchPattern: matchOrdinalNumberPattern,
      parsePattern: parseOrdinalNumberPattern,
      valueCallback: (value) => parseInt(value, 10)
    }),
    era: buildMatchFn({
      matchPatterns: matchEraPatterns,
      defaultMatchWidth: "wide",
      parsePatterns: parseEraPatterns,
      defaultParseWidth: "any"
    }),
    quarter: buildMatchFn({
      matchPatterns: matchQuarterPatterns,
      defaultMatchWidth: "wide",
      parsePatterns: parseQuarterPatterns,
      defaultParseWidth: "any",
      valueCallback: (index2) => index2 + 1
    }),
    month: buildMatchFn({
      matchPatterns: matchMonthPatterns,
      defaultMatchWidth: "wide",
      parsePatterns: parseMonthPatterns,
      defaultParseWidth: "any"
    }),
    day: buildMatchFn({
      matchPatterns: matchDayPatterns,
      defaultMatchWidth: "wide",
      parsePatterns: parseDayPatterns,
      defaultParseWidth: "any"
    }),
    dayPeriod: buildMatchFn({
      matchPatterns: matchDayPeriodPatterns,
      defaultMatchWidth: "any",
      parsePatterns: parseDayPeriodPatterns,
      defaultParseWidth: "any"
    })
  };

  // node_modules/date-fns/locale/en-US.js
  var enUS = {
    code: "en-US",
    formatDistance,
    formatLong,
    formatRelative,
    localize,
    match,
    options: {
      weekStartsOn: 0,
      firstWeekContainsDate: 1
    }
  };

  // node_modules/date-fns/_lib/format/formatters.js
  init_define_import_meta_env();

  // node_modules/date-fns/getDayOfYear.js
  init_define_import_meta_env();
  function getDayOfYear(date, options3) {
    const _date = toDate(date, options3?.in);
    const diff = differenceInCalendarDays(_date, startOfYear(_date));
    const dayOfYear = diff + 1;
    return dayOfYear;
  }

  // node_modules/date-fns/getISOWeek.js
  init_define_import_meta_env();
  function getISOWeek(date, options3) {
    const _date = toDate(date, options3?.in);
    const diff = +startOfISOWeek(_date) - +startOfISOWeekYear(_date);
    return Math.round(diff / millisecondsInWeek) + 1;
  }

  // node_modules/date-fns/getWeek.js
  init_define_import_meta_env();

  // node_modules/date-fns/startOfWeekYear.js
  init_define_import_meta_env();

  // node_modules/date-fns/getWeekYear.js
  init_define_import_meta_env();
  function getWeekYear(date, options3) {
    const _date = toDate(date, options3?.in);
    const year = _date.getFullYear();
    const defaultOptions4 = getDefaultOptions();
    const firstWeekContainsDate = options3?.firstWeekContainsDate ?? options3?.locale?.options?.firstWeekContainsDate ?? defaultOptions4.firstWeekContainsDate ?? defaultOptions4.locale?.options?.firstWeekContainsDate ?? 1;
    const firstWeekOfNextYear = constructFrom(options3?.in || date, 0);
    firstWeekOfNextYear.setFullYear(year + 1, 0, firstWeekContainsDate);
    firstWeekOfNextYear.setHours(0, 0, 0, 0);
    const startOfNextYear = startOfWeek(firstWeekOfNextYear, options3);
    const firstWeekOfThisYear = constructFrom(options3?.in || date, 0);
    firstWeekOfThisYear.setFullYear(year, 0, firstWeekContainsDate);
    firstWeekOfThisYear.setHours(0, 0, 0, 0);
    const startOfThisYear = startOfWeek(firstWeekOfThisYear, options3);
    if (+_date >= +startOfNextYear) {
      return year + 1;
    } else if (+_date >= +startOfThisYear) {
      return year;
    } else {
      return year - 1;
    }
  }

  // node_modules/date-fns/startOfWeekYear.js
  function startOfWeekYear(date, options3) {
    const defaultOptions4 = getDefaultOptions();
    const firstWeekContainsDate = options3?.firstWeekContainsDate ?? options3?.locale?.options?.firstWeekContainsDate ?? defaultOptions4.firstWeekContainsDate ?? defaultOptions4.locale?.options?.firstWeekContainsDate ?? 1;
    const year = getWeekYear(date, options3);
    const firstWeek = constructFrom(options3?.in || date, 0);
    firstWeek.setFullYear(year, 0, firstWeekContainsDate);
    firstWeek.setHours(0, 0, 0, 0);
    const _date = startOfWeek(firstWeek, options3);
    return _date;
  }

  // node_modules/date-fns/getWeek.js
  function getWeek(date, options3) {
    const _date = toDate(date, options3?.in);
    const diff = +startOfWeek(_date, options3) - +startOfWeekYear(_date, options3);
    return Math.round(diff / millisecondsInWeek) + 1;
  }

  // node_modules/date-fns/_lib/addLeadingZeros.js
  init_define_import_meta_env();
  function addLeadingZeros(number, targetLength) {
    const sign = number < 0 ? "-" : "";
    const output = Math.abs(number).toString().padStart(targetLength, "0");
    return sign + output;
  }

  // node_modules/date-fns/_lib/format/lightFormatters.js
  init_define_import_meta_env();
  var lightFormatters = {
    // Year
    y(date, token) {
      const signedYear = date.getFullYear();
      const year = signedYear > 0 ? signedYear : 1 - signedYear;
      return addLeadingZeros(token === "yy" ? year % 100 : year, token.length);
    },
    // Month
    M(date, token) {
      const month = date.getMonth();
      return token === "M" ? String(month + 1) : addLeadingZeros(month + 1, 2);
    },
    // Day of the month
    d(date, token) {
      return addLeadingZeros(date.getDate(), token.length);
    },
    // AM or PM
    a(date, token) {
      const dayPeriodEnumValue = date.getHours() / 12 >= 1 ? "pm" : "am";
      switch (token) {
        case "a":
        case "aa":
          return dayPeriodEnumValue.toUpperCase();
        case "aaa":
          return dayPeriodEnumValue;
        case "aaaaa":
          return dayPeriodEnumValue[0];
        case "aaaa":
        default:
          return dayPeriodEnumValue === "am" ? "a.m." : "p.m.";
      }
    },
    // Hour [1-12]
    h(date, token) {
      return addLeadingZeros(date.getHours() % 12 || 12, token.length);
    },
    // Hour [0-23]
    H(date, token) {
      return addLeadingZeros(date.getHours(), token.length);
    },
    // Minute
    m(date, token) {
      return addLeadingZeros(date.getMinutes(), token.length);
    },
    // Second
    s(date, token) {
      return addLeadingZeros(date.getSeconds(), token.length);
    },
    // Fraction of second
    S(date, token) {
      const numberOfDigits = token.length;
      const milliseconds = date.getMilliseconds();
      const fractionalSeconds = Math.trunc(
        milliseconds * Math.pow(10, numberOfDigits - 3)
      );
      return addLeadingZeros(fractionalSeconds, token.length);
    }
  };

  // node_modules/date-fns/_lib/format/formatters.js
  var dayPeriodEnum = {
    am: "am",
    pm: "pm",
    midnight: "midnight",
    noon: "noon",
    morning: "morning",
    afternoon: "afternoon",
    evening: "evening",
    night: "night"
  };
  var formatters = {
    // Era
    G: function(date, token, localize3) {
      const era = date.getFullYear() > 0 ? 1 : 0;
      switch (token) {
        // AD, BC
        case "G":
        case "GG":
        case "GGG":
          return localize3.era(era, { width: "abbreviated" });
        // A, B
        case "GGGGG":
          return localize3.era(era, { width: "narrow" });
        // Anno Domini, Before Christ
        case "GGGG":
        default:
          return localize3.era(era, { width: "wide" });
      }
    },
    // Year
    y: function(date, token, localize3) {
      if (token === "yo") {
        const signedYear = date.getFullYear();
        const year = signedYear > 0 ? signedYear : 1 - signedYear;
        return localize3.ordinalNumber(year, { unit: "year" });
      }
      return lightFormatters.y(date, token);
    },
    // Local week-numbering year
    Y: function(date, token, localize3, options3) {
      const signedWeekYear = getWeekYear(date, options3);
      const weekYear = signedWeekYear > 0 ? signedWeekYear : 1 - signedWeekYear;
      if (token === "YY") {
        const twoDigitYear = weekYear % 100;
        return addLeadingZeros(twoDigitYear, 2);
      }
      if (token === "Yo") {
        return localize3.ordinalNumber(weekYear, { unit: "year" });
      }
      return addLeadingZeros(weekYear, token.length);
    },
    // ISO week-numbering year
    R: function(date, token) {
      const isoWeekYear = getISOWeekYear(date);
      return addLeadingZeros(isoWeekYear, token.length);
    },
    // Extended year. This is a single number designating the year of this calendar system.
    // The main difference between `y` and `u` localizers are B.C. years:
    // | Year | `y` | `u` |
    // |------|-----|-----|
    // | AC 1 |   1 |   1 |
    // | BC 1 |   1 |   0 |
    // | BC 2 |   2 |  -1 |
    // Also `yy` always returns the last two digits of a year,
    // while `uu` pads single digit years to 2 characters and returns other years unchanged.
    u: function(date, token) {
      const year = date.getFullYear();
      return addLeadingZeros(year, token.length);
    },
    // Quarter
    Q: function(date, token, localize3) {
      const quarter = Math.ceil((date.getMonth() + 1) / 3);
      switch (token) {
        // 1, 2, 3, 4
        case "Q":
          return String(quarter);
        // 01, 02, 03, 04
        case "QQ":
          return addLeadingZeros(quarter, 2);
        // 1st, 2nd, 3rd, 4th
        case "Qo":
          return localize3.ordinalNumber(quarter, { unit: "quarter" });
        // Q1, Q2, Q3, Q4
        case "QQQ":
          return localize3.quarter(quarter, {
            width: "abbreviated",
            context: "formatting"
          });
        // 1, 2, 3, 4 (narrow quarter; could be not numerical)
        case "QQQQQ":
          return localize3.quarter(quarter, {
            width: "narrow",
            context: "formatting"
          });
        // 1st quarter, 2nd quarter, ...
        case "QQQQ":
        default:
          return localize3.quarter(quarter, {
            width: "wide",
            context: "formatting"
          });
      }
    },
    // Stand-alone quarter
    q: function(date, token, localize3) {
      const quarter = Math.ceil((date.getMonth() + 1) / 3);
      switch (token) {
        // 1, 2, 3, 4
        case "q":
          return String(quarter);
        // 01, 02, 03, 04
        case "qq":
          return addLeadingZeros(quarter, 2);
        // 1st, 2nd, 3rd, 4th
        case "qo":
          return localize3.ordinalNumber(quarter, { unit: "quarter" });
        // Q1, Q2, Q3, Q4
        case "qqq":
          return localize3.quarter(quarter, {
            width: "abbreviated",
            context: "standalone"
          });
        // 1, 2, 3, 4 (narrow quarter; could be not numerical)
        case "qqqqq":
          return localize3.quarter(quarter, {
            width: "narrow",
            context: "standalone"
          });
        // 1st quarter, 2nd quarter, ...
        case "qqqq":
        default:
          return localize3.quarter(quarter, {
            width: "wide",
            context: "standalone"
          });
      }
    },
    // Month
    M: function(date, token, localize3) {
      const month = date.getMonth();
      switch (token) {
        case "M":
        case "MM":
          return lightFormatters.M(date, token);
        // 1st, 2nd, ..., 12th
        case "Mo":
          return localize3.ordinalNumber(month + 1, { unit: "month" });
        // Jan, Feb, ..., Dec
        case "MMM":
          return localize3.month(month, {
            width: "abbreviated",
            context: "formatting"
          });
        // J, F, ..., D
        case "MMMMM":
          return localize3.month(month, {
            width: "narrow",
            context: "formatting"
          });
        // January, February, ..., December
        case "MMMM":
        default:
          return localize3.month(month, { width: "wide", context: "formatting" });
      }
    },
    // Stand-alone month
    L: function(date, token, localize3) {
      const month = date.getMonth();
      switch (token) {
        // 1, 2, ..., 12
        case "L":
          return String(month + 1);
        // 01, 02, ..., 12
        case "LL":
          return addLeadingZeros(month + 1, 2);
        // 1st, 2nd, ..., 12th
        case "Lo":
          return localize3.ordinalNumber(month + 1, { unit: "month" });
        // Jan, Feb, ..., Dec
        case "LLL":
          return localize3.month(month, {
            width: "abbreviated",
            context: "standalone"
          });
        // J, F, ..., D
        case "LLLLL":
          return localize3.month(month, {
            width: "narrow",
            context: "standalone"
          });
        // January, February, ..., December
        case "LLLL":
        default:
          return localize3.month(month, { width: "wide", context: "standalone" });
      }
    },
    // Local week of year
    w: function(date, token, localize3, options3) {
      const week = getWeek(date, options3);
      if (token === "wo") {
        return localize3.ordinalNumber(week, { unit: "week" });
      }
      return addLeadingZeros(week, token.length);
    },
    // ISO week of year
    I: function(date, token, localize3) {
      const isoWeek = getISOWeek(date);
      if (token === "Io") {
        return localize3.ordinalNumber(isoWeek, { unit: "week" });
      }
      return addLeadingZeros(isoWeek, token.length);
    },
    // Day of the month
    d: function(date, token, localize3) {
      if (token === "do") {
        return localize3.ordinalNumber(date.getDate(), { unit: "date" });
      }
      return lightFormatters.d(date, token);
    },
    // Day of year
    D: function(date, token, localize3) {
      const dayOfYear = getDayOfYear(date);
      if (token === "Do") {
        return localize3.ordinalNumber(dayOfYear, { unit: "dayOfYear" });
      }
      return addLeadingZeros(dayOfYear, token.length);
    },
    // Day of week
    E: function(date, token, localize3) {
      const dayOfWeek = date.getDay();
      switch (token) {
        // Tue
        case "E":
        case "EE":
        case "EEE":
          return localize3.day(dayOfWeek, {
            width: "abbreviated",
            context: "formatting"
          });
        // T
        case "EEEEE":
          return localize3.day(dayOfWeek, {
            width: "narrow",
            context: "formatting"
          });
        // Tu
        case "EEEEEE":
          return localize3.day(dayOfWeek, {
            width: "short",
            context: "formatting"
          });
        // Tuesday
        case "EEEE":
        default:
          return localize3.day(dayOfWeek, {
            width: "wide",
            context: "formatting"
          });
      }
    },
    // Local day of week
    e: function(date, token, localize3, options3) {
      const dayOfWeek = date.getDay();
      const localDayOfWeek = (dayOfWeek - options3.weekStartsOn + 8) % 7 || 7;
      switch (token) {
        // Numerical value (Nth day of week with current locale or weekStartsOn)
        case "e":
          return String(localDayOfWeek);
        // Padded numerical value
        case "ee":
          return addLeadingZeros(localDayOfWeek, 2);
        // 1st, 2nd, ..., 7th
        case "eo":
          return localize3.ordinalNumber(localDayOfWeek, { unit: "day" });
        case "eee":
          return localize3.day(dayOfWeek, {
            width: "abbreviated",
            context: "formatting"
          });
        // T
        case "eeeee":
          return localize3.day(dayOfWeek, {
            width: "narrow",
            context: "formatting"
          });
        // Tu
        case "eeeeee":
          return localize3.day(dayOfWeek, {
            width: "short",
            context: "formatting"
          });
        // Tuesday
        case "eeee":
        default:
          return localize3.day(dayOfWeek, {
            width: "wide",
            context: "formatting"
          });
      }
    },
    // Stand-alone local day of week
    c: function(date, token, localize3, options3) {
      const dayOfWeek = date.getDay();
      const localDayOfWeek = (dayOfWeek - options3.weekStartsOn + 8) % 7 || 7;
      switch (token) {
        // Numerical value (same as in `e`)
        case "c":
          return String(localDayOfWeek);
        // Padded numerical value
        case "cc":
          return addLeadingZeros(localDayOfWeek, token.length);
        // 1st, 2nd, ..., 7th
        case "co":
          return localize3.ordinalNumber(localDayOfWeek, { unit: "day" });
        case "ccc":
          return localize3.day(dayOfWeek, {
            width: "abbreviated",
            context: "standalone"
          });
        // T
        case "ccccc":
          return localize3.day(dayOfWeek, {
            width: "narrow",
            context: "standalone"
          });
        // Tu
        case "cccccc":
          return localize3.day(dayOfWeek, {
            width: "short",
            context: "standalone"
          });
        // Tuesday
        case "cccc":
        default:
          return localize3.day(dayOfWeek, {
            width: "wide",
            context: "standalone"
          });
      }
    },
    // ISO day of week
    i: function(date, token, localize3) {
      const dayOfWeek = date.getDay();
      const isoDayOfWeek = dayOfWeek === 0 ? 7 : dayOfWeek;
      switch (token) {
        // 2
        case "i":
          return String(isoDayOfWeek);
        // 02
        case "ii":
          return addLeadingZeros(isoDayOfWeek, token.length);
        // 2nd
        case "io":
          return localize3.ordinalNumber(isoDayOfWeek, { unit: "day" });
        // Tue
        case "iii":
          return localize3.day(dayOfWeek, {
            width: "abbreviated",
            context: "formatting"
          });
        // T
        case "iiiii":
          return localize3.day(dayOfWeek, {
            width: "narrow",
            context: "formatting"
          });
        // Tu
        case "iiiiii":
          return localize3.day(dayOfWeek, {
            width: "short",
            context: "formatting"
          });
        // Tuesday
        case "iiii":
        default:
          return localize3.day(dayOfWeek, {
            width: "wide",
            context: "formatting"
          });
      }
    },
    // AM or PM
    a: function(date, token, localize3) {
      const hours = date.getHours();
      const dayPeriodEnumValue = hours / 12 >= 1 ? "pm" : "am";
      switch (token) {
        case "a":
        case "aa":
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "abbreviated",
            context: "formatting"
          });
        case "aaa":
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "abbreviated",
            context: "formatting"
          }).toLowerCase();
        case "aaaaa":
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "narrow",
            context: "formatting"
          });
        case "aaaa":
        default:
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "wide",
            context: "formatting"
          });
      }
    },
    // AM, PM, midnight, noon
    b: function(date, token, localize3) {
      const hours = date.getHours();
      let dayPeriodEnumValue;
      if (hours === 12) {
        dayPeriodEnumValue = dayPeriodEnum.noon;
      } else if (hours === 0) {
        dayPeriodEnumValue = dayPeriodEnum.midnight;
      } else {
        dayPeriodEnumValue = hours / 12 >= 1 ? "pm" : "am";
      }
      switch (token) {
        case "b":
        case "bb":
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "abbreviated",
            context: "formatting"
          });
        case "bbb":
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "abbreviated",
            context: "formatting"
          }).toLowerCase();
        case "bbbbb":
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "narrow",
            context: "formatting"
          });
        case "bbbb":
        default:
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "wide",
            context: "formatting"
          });
      }
    },
    // in the morning, in the afternoon, in the evening, at night
    B: function(date, token, localize3) {
      const hours = date.getHours();
      let dayPeriodEnumValue;
      if (hours >= 17) {
        dayPeriodEnumValue = dayPeriodEnum.evening;
      } else if (hours >= 12) {
        dayPeriodEnumValue = dayPeriodEnum.afternoon;
      } else if (hours >= 4) {
        dayPeriodEnumValue = dayPeriodEnum.morning;
      } else {
        dayPeriodEnumValue = dayPeriodEnum.night;
      }
      switch (token) {
        case "B":
        case "BB":
        case "BBB":
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "abbreviated",
            context: "formatting"
          });
        case "BBBBB":
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "narrow",
            context: "formatting"
          });
        case "BBBB":
        default:
          return localize3.dayPeriod(dayPeriodEnumValue, {
            width: "wide",
            context: "formatting"
          });
      }
    },
    // Hour [1-12]
    h: function(date, token, localize3) {
      if (token === "ho") {
        let hours = date.getHours() % 12;
        if (hours === 0) hours = 12;
        return localize3.ordinalNumber(hours, { unit: "hour" });
      }
      return lightFormatters.h(date, token);
    },
    // Hour [0-23]
    H: function(date, token, localize3) {
      if (token === "Ho") {
        return localize3.ordinalNumber(date.getHours(), { unit: "hour" });
      }
      return lightFormatters.H(date, token);
    },
    // Hour [0-11]
    K: function(date, token, localize3) {
      const hours = date.getHours() % 12;
      if (token === "Ko") {
        return localize3.ordinalNumber(hours, { unit: "hour" });
      }
      return addLeadingZeros(hours, token.length);
    },
    // Hour [1-24]
    k: function(date, token, localize3) {
      let hours = date.getHours();
      if (hours === 0) hours = 24;
      if (token === "ko") {
        return localize3.ordinalNumber(hours, { unit: "hour" });
      }
      return addLeadingZeros(hours, token.length);
    },
    // Minute
    m: function(date, token, localize3) {
      if (token === "mo") {
        return localize3.ordinalNumber(date.getMinutes(), { unit: "minute" });
      }
      return lightFormatters.m(date, token);
    },
    // Second
    s: function(date, token, localize3) {
      if (token === "so") {
        return localize3.ordinalNumber(date.getSeconds(), { unit: "second" });
      }
      return lightFormatters.s(date, token);
    },
    // Fraction of second
    S: function(date, token) {
      return lightFormatters.S(date, token);
    },
    // Timezone (ISO-8601. If offset is 0, output is always `'Z'`)
    X: function(date, token, _localize) {
      const timezoneOffset = date.getTimezoneOffset();
      if (timezoneOffset === 0) {
        return "Z";
      }
      switch (token) {
        // Hours and optional minutes
        case "X":
          return formatTimezoneWithOptionalMinutes(timezoneOffset);
        // Hours, minutes and optional seconds without `:` delimiter
        // Note: neither ISO-8601 nor JavaScript supports seconds in timezone offsets
        // so this token always has the same output as `XX`
        case "XXXX":
        case "XX":
          return formatTimezone(timezoneOffset);
        // Hours, minutes and optional seconds with `:` delimiter
        // Note: neither ISO-8601 nor JavaScript supports seconds in timezone offsets
        // so this token always has the same output as `XXX`
        case "XXXXX":
        case "XXX":
        // Hours and minutes with `:` delimiter
        default:
          return formatTimezone(timezoneOffset, ":");
      }
    },
    // Timezone (ISO-8601. If offset is 0, output is `'+00:00'` or equivalent)
    x: function(date, token, _localize) {
      const timezoneOffset = date.getTimezoneOffset();
      switch (token) {
        // Hours and optional minutes
        case "x":
          return formatTimezoneWithOptionalMinutes(timezoneOffset);
        // Hours, minutes and optional seconds without `:` delimiter
        // Note: neither ISO-8601 nor JavaScript supports seconds in timezone offsets
        // so this token always has the same output as `xx`
        case "xxxx":
        case "xx":
          return formatTimezone(timezoneOffset);
        // Hours, minutes and optional seconds with `:` delimiter
        // Note: neither ISO-8601 nor JavaScript supports seconds in timezone offsets
        // so this token always has the same output as `xxx`
        case "xxxxx":
        case "xxx":
        // Hours and minutes with `:` delimiter
        default:
          return formatTimezone(timezoneOffset, ":");
      }
    },
    // Timezone (GMT)
    O: function(date, token, _localize) {
      const timezoneOffset = date.getTimezoneOffset();
      switch (token) {
        // Short
        case "O":
        case "OO":
        case "OOO":
          return "GMT" + formatTimezoneShort(timezoneOffset, ":");
        // Long
        case "OOOO":
        default:
          return "GMT" + formatTimezone(timezoneOffset, ":");
      }
    },
    // Timezone (specific non-location)
    z: function(date, token, _localize) {
      const timezoneOffset = date.getTimezoneOffset();
      switch (token) {
        // Short
        case "z":
        case "zz":
        case "zzz":
          return "GMT" + formatTimezoneShort(timezoneOffset, ":");
        // Long
        case "zzzz":
        default:
          return "GMT" + formatTimezone(timezoneOffset, ":");
      }
    },
    // Seconds timestamp
    t: function(date, token, _localize) {
      const timestamp = Math.trunc(+date / 1e3);
      return addLeadingZeros(timestamp, token.length);
    },
    // Milliseconds timestamp
    T: function(date, token, _localize) {
      return addLeadingZeros(+date, token.length);
    }
  };
  function formatTimezoneShort(offset4, delimiter = "") {
    const sign = offset4 > 0 ? "-" : "+";
    const absOffset = Math.abs(offset4);
    const hours = Math.trunc(absOffset / 60);
    const minutes = absOffset % 60;
    if (minutes === 0) {
      return sign + String(hours);
    }
    return sign + String(hours) + delimiter + addLeadingZeros(minutes, 2);
  }
  function formatTimezoneWithOptionalMinutes(offset4, delimiter) {
    if (offset4 % 60 === 0) {
      const sign = offset4 > 0 ? "-" : "+";
      return sign + addLeadingZeros(Math.abs(offset4) / 60, 2);
    }
    return formatTimezone(offset4, delimiter);
  }
  function formatTimezone(offset4, delimiter = "") {
    const sign = offset4 > 0 ? "-" : "+";
    const absOffset = Math.abs(offset4);
    const hours = addLeadingZeros(Math.trunc(absOffset / 60), 2);
    const minutes = addLeadingZeros(absOffset % 60, 2);
    return sign + hours + delimiter + minutes;
  }

  // node_modules/date-fns/_lib/format/longFormatters.js
  init_define_import_meta_env();
  var dateLongFormatter = (pattern, formatLong3) => {
    switch (pattern) {
      case "P":
        return formatLong3.date({ width: "short" });
      case "PP":
        return formatLong3.date({ width: "medium" });
      case "PPP":
        return formatLong3.date({ width: "long" });
      case "PPPP":
      default:
        return formatLong3.date({ width: "full" });
    }
  };
  var timeLongFormatter = (pattern, formatLong3) => {
    switch (pattern) {
      case "p":
        return formatLong3.time({ width: "short" });
      case "pp":
        return formatLong3.time({ width: "medium" });
      case "ppp":
        return formatLong3.time({ width: "long" });
      case "pppp":
      default:
        return formatLong3.time({ width: "full" });
    }
  };
  var dateTimeLongFormatter = (pattern, formatLong3) => {
    const matchResult = pattern.match(/(P+)(p+)?/) || [];
    const datePattern = matchResult[1];
    const timePattern = matchResult[2];
    if (!timePattern) {
      return dateLongFormatter(pattern, formatLong3);
    }
    let dateTimeFormat;
    switch (datePattern) {
      case "P":
        dateTimeFormat = formatLong3.dateTime({ width: "short" });
        break;
      case "PP":
        dateTimeFormat = formatLong3.dateTime({ width: "medium" });
        break;
      case "PPP":
        dateTimeFormat = formatLong3.dateTime({ width: "long" });
        break;
      case "PPPP":
      default:
        dateTimeFormat = formatLong3.dateTime({ width: "full" });
        break;
    }
    return dateTimeFormat.replace("{{date}}", dateLongFormatter(datePattern, formatLong3)).replace("{{time}}", timeLongFormatter(timePattern, formatLong3));
  };
  var longFormatters = {
    p: timeLongFormatter,
    P: dateTimeLongFormatter
  };

  // node_modules/date-fns/_lib/protectedTokens.js
  init_define_import_meta_env();
  var dayOfYearTokenRE = /^D+$/;
  var weekYearTokenRE = /^Y+$/;
  var throwTokens = ["D", "DD", "YY", "YYYY"];
  function isProtectedDayOfYearToken(token) {
    return dayOfYearTokenRE.test(token);
  }
  function isProtectedWeekYearToken(token) {
    return weekYearTokenRE.test(token);
  }
  function warnOrThrowProtectedError(token, format3, input) {
    const _message = message(token, format3, input);
    console.warn(_message);
    if (throwTokens.includes(token)) throw new RangeError(_message);
  }
  function message(token, format3, input) {
    const subject = token[0] === "Y" ? "years" : "days of the month";
    return `Use \`${token.toLowerCase()}\` instead of \`${token}\` (in \`${format3}\`) for formatting ${subject} to the input \`${input}\`; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md`;
  }

  // node_modules/date-fns/format.js
  var formattingTokensRegExp = /[yYQqMLwIdDecihHKkms]o|(\w)\1*|''|'(''|[^'])+('|$)|./g;
  var longFormattingTokensRegExp = /P+p+|P+|p+|''|'(''|[^'])+('|$)|./g;
  var escapedStringRegExp = /^'([^]*?)'?$/;
  var doubleQuoteRegExp = /''/g;
  var unescapedLatinCharacterRegExp = /[a-zA-Z]/;
  function format(date, formatStr, options3) {
    const defaultOptions4 = getDefaultOptions();
    const locale = options3?.locale ?? defaultOptions4.locale ?? enUS;
    const firstWeekContainsDate = options3?.firstWeekContainsDate ?? options3?.locale?.options?.firstWeekContainsDate ?? defaultOptions4.firstWeekContainsDate ?? defaultOptions4.locale?.options?.firstWeekContainsDate ?? 1;
    const weekStartsOn = options3?.weekStartsOn ?? options3?.locale?.options?.weekStartsOn ?? defaultOptions4.weekStartsOn ?? defaultOptions4.locale?.options?.weekStartsOn ?? 0;
    const originalDate = toDate(date, options3?.in);
    if (!isValid(originalDate)) {
      throw new RangeError("Invalid time value");
    }
    let parts = formatStr.match(longFormattingTokensRegExp).map((substring) => {
      const firstCharacter = substring[0];
      if (firstCharacter === "p" || firstCharacter === "P") {
        const longFormatter = longFormatters[firstCharacter];
        return longFormatter(substring, locale.formatLong);
      }
      return substring;
    }).join("").match(formattingTokensRegExp).map((substring) => {
      if (substring === "''") {
        return { isToken: false, value: "'" };
      }
      const firstCharacter = substring[0];
      if (firstCharacter === "'") {
        return { isToken: false, value: cleanEscapedString(substring) };
      }
      if (formatters[firstCharacter]) {
        return { isToken: true, value: substring };
      }
      if (firstCharacter.match(unescapedLatinCharacterRegExp)) {
        throw new RangeError(
          "Format string contains an unescaped latin alphabet character `" + firstCharacter + "`"
        );
      }
      return { isToken: false, value: substring };
    });
    if (locale.localize.preprocessor) {
      parts = locale.localize.preprocessor(originalDate, parts);
    }
    const formatterOptions = {
      firstWeekContainsDate,
      weekStartsOn,
      locale
    };
    return parts.map((part) => {
      if (!part.isToken) return part.value;
      const token = part.value;
      if (!options3?.useAdditionalWeekYearTokens && isProtectedWeekYearToken(token) || !options3?.useAdditionalDayOfYearTokens && isProtectedDayOfYearToken(token)) {
        warnOrThrowProtectedError(token, formatStr, String(date));
      }
      const formatter = formatters[token[0]];
      return formatter(originalDate, token, locale.localize, formatterOptions);
    }).join("");
  }
  function cleanEscapedString(input) {
    const matched = input.match(escapedStringRegExp);
    if (!matched) {
      return input;
    }
    return matched[1].replace(doubleQuoteRegExp, "'");
  }

  // node_modules/date-fns/formatDistance.js
  init_define_import_meta_env();
  function formatDistance2(laterDate, earlierDate, options3) {
    const defaultOptions4 = getDefaultOptions();
    const locale = options3?.locale ?? defaultOptions4.locale ?? enUS;
    const minutesInAlmostTwoDays = 2520;
    const comparison = compareAsc(laterDate, earlierDate);
    if (isNaN(comparison)) throw new RangeError("Invalid time value");
    const localizeOptions = Object.assign({}, options3, {
      addSuffix: options3?.addSuffix,
      comparison
    });
    const [laterDate_, earlierDate_] = normalizeDates(
      options3?.in,
      ...comparison > 0 ? [earlierDate, laterDate] : [laterDate, earlierDate]
    );
    const seconds = differenceInSeconds(earlierDate_, laterDate_);
    const offsetInSeconds = (getTimezoneOffsetInMilliseconds(earlierDate_) - getTimezoneOffsetInMilliseconds(laterDate_)) / 1e3;
    const minutes = Math.round((seconds - offsetInSeconds) / 60);
    let months;
    if (minutes < 2) {
      if (options3?.includeSeconds) {
        if (seconds < 5) {
          return locale.formatDistance("lessThanXSeconds", 5, localizeOptions);
        } else if (seconds < 10) {
          return locale.formatDistance("lessThanXSeconds", 10, localizeOptions);
        } else if (seconds < 20) {
          return locale.formatDistance("lessThanXSeconds", 20, localizeOptions);
        } else if (seconds < 40) {
          return locale.formatDistance("halfAMinute", 0, localizeOptions);
        } else if (seconds < 60) {
          return locale.formatDistance("lessThanXMinutes", 1, localizeOptions);
        } else {
          return locale.formatDistance("xMinutes", 1, localizeOptions);
        }
      } else {
        if (minutes === 0) {
          return locale.formatDistance("lessThanXMinutes", 1, localizeOptions);
        } else {
          return locale.formatDistance("xMinutes", minutes, localizeOptions);
        }
      }
    } else if (minutes < 45) {
      return locale.formatDistance("xMinutes", minutes, localizeOptions);
    } else if (minutes < 90) {
      return locale.formatDistance("aboutXHours", 1, localizeOptions);
    } else if (minutes < minutesInDay) {
      const hours = Math.round(minutes / 60);
      return locale.formatDistance("aboutXHours", hours, localizeOptions);
    } else if (minutes < minutesInAlmostTwoDays) {
      return locale.formatDistance("xDays", 1, localizeOptions);
    } else if (minutes < minutesInMonth) {
      const days = Math.round(minutes / minutesInDay);
      return locale.formatDistance("xDays", days, localizeOptions);
    } else if (minutes < minutesInMonth * 2) {
      months = Math.round(minutes / minutesInMonth);
      return locale.formatDistance("aboutXMonths", months, localizeOptions);
    }
    months = differenceInMonths(earlierDate_, laterDate_);
    if (months < 12) {
      const nearestMonth = Math.round(minutes / minutesInMonth);
      return locale.formatDistance("xMonths", nearestMonth, localizeOptions);
    } else {
      const monthsSinceStartOfYear = months % 12;
      const years = Math.trunc(months / 12);
      if (monthsSinceStartOfYear < 3) {
        return locale.formatDistance("aboutXYears", years, localizeOptions);
      } else if (monthsSinceStartOfYear < 9) {
        return locale.formatDistance("overXYears", years, localizeOptions);
      } else {
        return locale.formatDistance("almostXYears", years + 1, localizeOptions);
      }
    }
  }

  // node_modules/date-fns/formatDistanceToNow.js
  init_define_import_meta_env();
  function formatDistanceToNow(date, options3) {
    return formatDistance2(date, constructNow(date), options3);
  }

  // node_modules/date-fns/getDay.js
  init_define_import_meta_env();
  function getDay(date, options3) {
    return toDate(date, options3?.in).getDay();
  }

  // node_modules/date-fns/getDaysInMonth.js
  init_define_import_meta_env();
  function getDaysInMonth(date, options3) {
    const _date = toDate(date, options3?.in);
    const year = _date.getFullYear();
    const monthIndex = _date.getMonth();
    const lastDayOfMonth = constructFrom(_date, 0);
    lastDayOfMonth.setFullYear(year, monthIndex + 1, 0);
    lastDayOfMonth.setHours(0, 0, 0, 0);
    return lastDayOfMonth.getDate();
  }

  // node_modules/date-fns/getDefaultOptions.js
  init_define_import_meta_env();
  function getDefaultOptions2() {
    return Object.assign({}, getDefaultOptions());
  }

  // node_modules/date-fns/getHours.js
  init_define_import_meta_env();
  function getHours(date, options3) {
    return toDate(date, options3?.in).getHours();
  }

  // node_modules/date-fns/getISODay.js
  init_define_import_meta_env();
  function getISODay(date, options3) {
    const day = toDate(date, options3?.in).getDay();
    return day === 0 ? 7 : day;
  }

  // node_modules/date-fns/getMilliseconds.js
  init_define_import_meta_env();
  function getMilliseconds(date) {
    return toDate(date).getMilliseconds();
  }

  // node_modules/date-fns/getMinutes.js
  init_define_import_meta_env();
  function getMinutes(date, options3) {
    return toDate(date, options3?.in).getMinutes();
  }

  // node_modules/date-fns/getMonth.js
  init_define_import_meta_env();
  function getMonth(date, options3) {
    return toDate(date, options3?.in).getMonth();
  }

  // node_modules/date-fns/getSeconds.js
  init_define_import_meta_env();
  function getSeconds(date) {
    return toDate(date).getSeconds();
  }

  // node_modules/date-fns/getYear.js
  init_define_import_meta_env();
  function getYear(date, options3) {
    return toDate(date, options3?.in).getFullYear();
  }

  // node_modules/date-fns/isAfter.js
  init_define_import_meta_env();
  function isAfter(date, dateToCompare) {
    return +toDate(date) > +toDate(dateToCompare);
  }

  // node_modules/date-fns/isBefore.js
  init_define_import_meta_env();
  function isBefore(date, dateToCompare) {
    return +toDate(date) < +toDate(dateToCompare);
  }

  // node_modules/date-fns/parse.js
  init_define_import_meta_env();

  // node_modules/date-fns/parse/_lib/Setter.js
  init_define_import_meta_env();

  // node_modules/date-fns/transpose.js
  init_define_import_meta_env();
  function transpose(date, constructor) {
    const date_ = isConstructor(constructor) ? new constructor(0) : constructFrom(constructor, 0);
    date_.setFullYear(date.getFullYear(), date.getMonth(), date.getDate());
    date_.setHours(
      date.getHours(),
      date.getMinutes(),
      date.getSeconds(),
      date.getMilliseconds()
    );
    return date_;
  }
  function isConstructor(constructor) {
    return typeof constructor === "function" && constructor.prototype?.constructor === constructor;
  }

  // node_modules/date-fns/parse/_lib/Setter.js
  var TIMEZONE_UNIT_PRIORITY = 10;
  var Setter = class {
    constructor() {
      __publicField(this, "subPriority", 0);
    }
    validate(_utcDate, _options) {
      return true;
    }
  };
  var ValueSetter = class extends Setter {
    constructor(value, validateValue, setValue, priority, subPriority) {
      super();
      this.value = value;
      this.validateValue = validateValue;
      this.setValue = setValue;
      this.priority = priority;
      if (subPriority) {
        this.subPriority = subPriority;
      }
    }
    validate(date, options3) {
      return this.validateValue(date, this.value, options3);
    }
    set(date, flags, options3) {
      return this.setValue(date, flags, this.value, options3);
    }
  };
  var DateTimezoneSetter = class extends Setter {
    constructor(context2, reference) {
      super();
      __publicField(this, "priority", TIMEZONE_UNIT_PRIORITY);
      __publicField(this, "subPriority", -1);
      this.context = context2 || ((date) => constructFrom(reference, date));
    }
    set(date, flags) {
      if (flags.timestampIsSet) return date;
      return constructFrom(date, transpose(date, this.context));
    }
  };

  // node_modules/date-fns/parse/_lib/parsers.js
  init_define_import_meta_env();

  // node_modules/date-fns/parse/_lib/parsers/EraParser.js
  init_define_import_meta_env();

  // node_modules/date-fns/parse/_lib/Parser.js
  init_define_import_meta_env();
  var Parser = class {
    run(dateString, token, match3, options3) {
      const result = this.parse(dateString, token, match3, options3);
      if (!result) {
        return null;
      }
      return {
        setter: new ValueSetter(
          result.value,
          this.validate,
          this.set,
          this.priority,
          this.subPriority
        ),
        rest: result.rest
      };
    }
    validate(_utcDate, _value, _options) {
      return true;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/EraParser.js
  var EraParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 140);
      __publicField(this, "incompatibleTokens", ["R", "u", "t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        // AD, BC
        case "G":
        case "GG":
        case "GGG":
          return match3.era(dateString, { width: "abbreviated" }) || match3.era(dateString, { width: "narrow" });
        // A, B
        case "GGGGG":
          return match3.era(dateString, { width: "narrow" });
        // Anno Domini, Before Christ
        case "GGGG":
        default:
          return match3.era(dateString, { width: "wide" }) || match3.era(dateString, { width: "abbreviated" }) || match3.era(dateString, { width: "narrow" });
      }
    }
    set(date, flags, value) {
      flags.era = value;
      date.setFullYear(value, 0, 1);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/YearParser.js
  init_define_import_meta_env();

  // node_modules/date-fns/parse/_lib/utils.js
  init_define_import_meta_env();

  // node_modules/date-fns/parse/_lib/constants.js
  init_define_import_meta_env();
  var numericPatterns = {
    month: /^(1[0-2]|0?\d)/,
    // 0 to 12
    date: /^(3[0-1]|[0-2]?\d)/,
    // 0 to 31
    dayOfYear: /^(36[0-6]|3[0-5]\d|[0-2]?\d?\d)/,
    // 0 to 366
    week: /^(5[0-3]|[0-4]?\d)/,
    // 0 to 53
    hour23h: /^(2[0-3]|[0-1]?\d)/,
    // 0 to 23
    hour24h: /^(2[0-4]|[0-1]?\d)/,
    // 0 to 24
    hour11h: /^(1[0-1]|0?\d)/,
    // 0 to 11
    hour12h: /^(1[0-2]|0?\d)/,
    // 0 to 12
    minute: /^[0-5]?\d/,
    // 0 to 59
    second: /^[0-5]?\d/,
    // 0 to 59
    singleDigit: /^\d/,
    // 0 to 9
    twoDigits: /^\d{1,2}/,
    // 0 to 99
    threeDigits: /^\d{1,3}/,
    // 0 to 999
    fourDigits: /^\d{1,4}/,
    // 0 to 9999
    anyDigitsSigned: /^-?\d+/,
    singleDigitSigned: /^-?\d/,
    // 0 to 9, -0 to -9
    twoDigitsSigned: /^-?\d{1,2}/,
    // 0 to 99, -0 to -99
    threeDigitsSigned: /^-?\d{1,3}/,
    // 0 to 999, -0 to -999
    fourDigitsSigned: /^-?\d{1,4}/
    // 0 to 9999, -0 to -9999
  };
  var timezonePatterns = {
    basicOptionalMinutes: /^([+-])(\d{2})(\d{2})?|Z/,
    basic: /^([+-])(\d{2})(\d{2})|Z/,
    basicOptionalSeconds: /^([+-])(\d{2})(\d{2})((\d{2}))?|Z/,
    extended: /^([+-])(\d{2}):(\d{2})|Z/,
    extendedOptionalSeconds: /^([+-])(\d{2}):(\d{2})(:(\d{2}))?|Z/
  };

  // node_modules/date-fns/parse/_lib/utils.js
  function mapValue(parseFnResult, mapFn) {
    if (!parseFnResult) {
      return parseFnResult;
    }
    return {
      value: mapFn(parseFnResult.value),
      rest: parseFnResult.rest
    };
  }
  function parseNumericPattern(pattern, dateString) {
    const matchResult = dateString.match(pattern);
    if (!matchResult) {
      return null;
    }
    return {
      value: parseInt(matchResult[0], 10),
      rest: dateString.slice(matchResult[0].length)
    };
  }
  function parseTimezonePattern(pattern, dateString) {
    const matchResult = dateString.match(pattern);
    if (!matchResult) {
      return null;
    }
    if (matchResult[0] === "Z") {
      return {
        value: 0,
        rest: dateString.slice(1)
      };
    }
    const sign = matchResult[1] === "+" ? 1 : -1;
    const hours = matchResult[2] ? parseInt(matchResult[2], 10) : 0;
    const minutes = matchResult[3] ? parseInt(matchResult[3], 10) : 0;
    const seconds = matchResult[5] ? parseInt(matchResult[5], 10) : 0;
    return {
      value: sign * (hours * millisecondsInHour + minutes * millisecondsInMinute + seconds * millisecondsInSecond),
      rest: dateString.slice(matchResult[0].length)
    };
  }
  function parseAnyDigitsSigned(dateString) {
    return parseNumericPattern(numericPatterns.anyDigitsSigned, dateString);
  }
  function parseNDigits(n, dateString) {
    switch (n) {
      case 1:
        return parseNumericPattern(numericPatterns.singleDigit, dateString);
      case 2:
        return parseNumericPattern(numericPatterns.twoDigits, dateString);
      case 3:
        return parseNumericPattern(numericPatterns.threeDigits, dateString);
      case 4:
        return parseNumericPattern(numericPatterns.fourDigits, dateString);
      default:
        return parseNumericPattern(new RegExp("^\\d{1," + n + "}"), dateString);
    }
  }
  function parseNDigitsSigned(n, dateString) {
    switch (n) {
      case 1:
        return parseNumericPattern(numericPatterns.singleDigitSigned, dateString);
      case 2:
        return parseNumericPattern(numericPatterns.twoDigitsSigned, dateString);
      case 3:
        return parseNumericPattern(numericPatterns.threeDigitsSigned, dateString);
      case 4:
        return parseNumericPattern(numericPatterns.fourDigitsSigned, dateString);
      default:
        return parseNumericPattern(new RegExp("^-?\\d{1," + n + "}"), dateString);
    }
  }
  function dayPeriodEnumToHours(dayPeriod) {
    switch (dayPeriod) {
      case "morning":
        return 4;
      case "evening":
        return 17;
      case "pm":
      case "noon":
      case "afternoon":
        return 12;
      case "am":
      case "midnight":
      case "night":
      default:
        return 0;
    }
  }
  function normalizeTwoDigitYear(twoDigitYear, currentYear) {
    const isCommonEra = currentYear > 0;
    const absCurrentYear = isCommonEra ? currentYear : 1 - currentYear;
    let result;
    if (absCurrentYear <= 50) {
      result = twoDigitYear || 100;
    } else {
      const rangeEnd = absCurrentYear + 50;
      const rangeEndCentury = Math.trunc(rangeEnd / 100) * 100;
      const isPreviousCentury = twoDigitYear >= rangeEnd % 100;
      result = twoDigitYear + rangeEndCentury - (isPreviousCentury ? 100 : 0);
    }
    return isCommonEra ? result : 1 - result;
  }
  function isLeapYearIndex(year) {
    return year % 400 === 0 || year % 4 === 0 && year % 100 !== 0;
  }

  // node_modules/date-fns/parse/_lib/parsers/YearParser.js
  var YearParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 130);
      __publicField(this, "incompatibleTokens", ["Y", "R", "u", "w", "I", "i", "e", "c", "t", "T"]);
    }
    parse(dateString, token, match3) {
      const valueCallback = (year) => ({
        year,
        isTwoDigitYear: token === "yy"
      });
      switch (token) {
        case "y":
          return mapValue(parseNDigits(4, dateString), valueCallback);
        case "yo":
          return mapValue(
            match3.ordinalNumber(dateString, {
              unit: "year"
            }),
            valueCallback
          );
        default:
          return mapValue(parseNDigits(token.length, dateString), valueCallback);
      }
    }
    validate(_date, value) {
      return value.isTwoDigitYear || value.year > 0;
    }
    set(date, flags, value) {
      const currentYear = date.getFullYear();
      if (value.isTwoDigitYear) {
        const normalizedTwoDigitYear = normalizeTwoDigitYear(
          value.year,
          currentYear
        );
        date.setFullYear(normalizedTwoDigitYear, 0, 1);
        date.setHours(0, 0, 0, 0);
        return date;
      }
      const year = !("era" in flags) || flags.era === 1 ? value.year : 1 - value.year;
      date.setFullYear(year, 0, 1);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/LocalWeekYearParser.js
  init_define_import_meta_env();
  var LocalWeekYearParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 130);
      __publicField(this, "incompatibleTokens", [
        "y",
        "R",
        "u",
        "Q",
        "q",
        "M",
        "L",
        "I",
        "d",
        "D",
        "i",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3) {
      const valueCallback = (year) => ({
        year,
        isTwoDigitYear: token === "YY"
      });
      switch (token) {
        case "Y":
          return mapValue(parseNDigits(4, dateString), valueCallback);
        case "Yo":
          return mapValue(
            match3.ordinalNumber(dateString, {
              unit: "year"
            }),
            valueCallback
          );
        default:
          return mapValue(parseNDigits(token.length, dateString), valueCallback);
      }
    }
    validate(_date, value) {
      return value.isTwoDigitYear || value.year > 0;
    }
    set(date, flags, value, options3) {
      const currentYear = getWeekYear(date, options3);
      if (value.isTwoDigitYear) {
        const normalizedTwoDigitYear = normalizeTwoDigitYear(
          value.year,
          currentYear
        );
        date.setFullYear(
          normalizedTwoDigitYear,
          0,
          options3.firstWeekContainsDate
        );
        date.setHours(0, 0, 0, 0);
        return startOfWeek(date, options3);
      }
      const year = !("era" in flags) || flags.era === 1 ? value.year : 1 - value.year;
      date.setFullYear(year, 0, options3.firstWeekContainsDate);
      date.setHours(0, 0, 0, 0);
      return startOfWeek(date, options3);
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/ISOWeekYearParser.js
  init_define_import_meta_env();
  var ISOWeekYearParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 130);
      __publicField(this, "incompatibleTokens", [
        "G",
        "y",
        "Y",
        "u",
        "Q",
        "q",
        "M",
        "L",
        "w",
        "d",
        "D",
        "e",
        "c",
        "t",
        "T"
      ]);
    }
    parse(dateString, token) {
      if (token === "R") {
        return parseNDigitsSigned(4, dateString);
      }
      return parseNDigitsSigned(token.length, dateString);
    }
    set(date, _flags, value) {
      const firstWeekOfYear = constructFrom(date, 0);
      firstWeekOfYear.setFullYear(value, 0, 4);
      firstWeekOfYear.setHours(0, 0, 0, 0);
      return startOfISOWeek(firstWeekOfYear);
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/ExtendedYearParser.js
  init_define_import_meta_env();
  var ExtendedYearParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 130);
      __publicField(this, "incompatibleTokens", ["G", "y", "Y", "R", "w", "I", "i", "e", "c", "t", "T"]);
    }
    parse(dateString, token) {
      if (token === "u") {
        return parseNDigitsSigned(4, dateString);
      }
      return parseNDigitsSigned(token.length, dateString);
    }
    set(date, _flags, value) {
      date.setFullYear(value, 0, 1);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/QuarterParser.js
  init_define_import_meta_env();
  var QuarterParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 120);
      __publicField(this, "incompatibleTokens", [
        "Y",
        "R",
        "q",
        "M",
        "L",
        "w",
        "I",
        "d",
        "D",
        "i",
        "e",
        "c",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        // 1, 2, 3, 4
        case "Q":
        case "QQ":
          return parseNDigits(token.length, dateString);
        // 1st, 2nd, 3rd, 4th
        case "Qo":
          return match3.ordinalNumber(dateString, { unit: "quarter" });
        // Q1, Q2, Q3, Q4
        case "QQQ":
          return match3.quarter(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.quarter(dateString, {
            width: "narrow",
            context: "formatting"
          });
        // 1, 2, 3, 4 (narrow quarter; could be not numerical)
        case "QQQQQ":
          return match3.quarter(dateString, {
            width: "narrow",
            context: "formatting"
          });
        // 1st quarter, 2nd quarter, ...
        case "QQQQ":
        default:
          return match3.quarter(dateString, {
            width: "wide",
            context: "formatting"
          }) || match3.quarter(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.quarter(dateString, {
            width: "narrow",
            context: "formatting"
          });
      }
    }
    validate(_date, value) {
      return value >= 1 && value <= 4;
    }
    set(date, _flags, value) {
      date.setMonth((value - 1) * 3, 1);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/StandAloneQuarterParser.js
  init_define_import_meta_env();
  var StandAloneQuarterParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 120);
      __publicField(this, "incompatibleTokens", [
        "Y",
        "R",
        "Q",
        "M",
        "L",
        "w",
        "I",
        "d",
        "D",
        "i",
        "e",
        "c",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        // 1, 2, 3, 4
        case "q":
        case "qq":
          return parseNDigits(token.length, dateString);
        // 1st, 2nd, 3rd, 4th
        case "qo":
          return match3.ordinalNumber(dateString, { unit: "quarter" });
        // Q1, Q2, Q3, Q4
        case "qqq":
          return match3.quarter(dateString, {
            width: "abbreviated",
            context: "standalone"
          }) || match3.quarter(dateString, {
            width: "narrow",
            context: "standalone"
          });
        // 1, 2, 3, 4 (narrow quarter; could be not numerical)
        case "qqqqq":
          return match3.quarter(dateString, {
            width: "narrow",
            context: "standalone"
          });
        // 1st quarter, 2nd quarter, ...
        case "qqqq":
        default:
          return match3.quarter(dateString, {
            width: "wide",
            context: "standalone"
          }) || match3.quarter(dateString, {
            width: "abbreviated",
            context: "standalone"
          }) || match3.quarter(dateString, {
            width: "narrow",
            context: "standalone"
          });
      }
    }
    validate(_date, value) {
      return value >= 1 && value <= 4;
    }
    set(date, _flags, value) {
      date.setMonth((value - 1) * 3, 1);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/MonthParser.js
  init_define_import_meta_env();
  var MonthParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "incompatibleTokens", [
        "Y",
        "R",
        "q",
        "Q",
        "L",
        "w",
        "I",
        "D",
        "i",
        "e",
        "c",
        "t",
        "T"
      ]);
      __publicField(this, "priority", 110);
    }
    parse(dateString, token, match3) {
      const valueCallback = (value) => value - 1;
      switch (token) {
        // 1, 2, ..., 12
        case "M":
          return mapValue(
            parseNumericPattern(numericPatterns.month, dateString),
            valueCallback
          );
        // 01, 02, ..., 12
        case "MM":
          return mapValue(parseNDigits(2, dateString), valueCallback);
        // 1st, 2nd, ..., 12th
        case "Mo":
          return mapValue(
            match3.ordinalNumber(dateString, {
              unit: "month"
            }),
            valueCallback
          );
        // Jan, Feb, ..., Dec
        case "MMM":
          return match3.month(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.month(dateString, { width: "narrow", context: "formatting" });
        // J, F, ..., D
        case "MMMMM":
          return match3.month(dateString, {
            width: "narrow",
            context: "formatting"
          });
        // January, February, ..., December
        case "MMMM":
        default:
          return match3.month(dateString, { width: "wide", context: "formatting" }) || match3.month(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.month(dateString, { width: "narrow", context: "formatting" });
      }
    }
    validate(_date, value) {
      return value >= 0 && value <= 11;
    }
    set(date, _flags, value) {
      date.setMonth(value, 1);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/StandAloneMonthParser.js
  init_define_import_meta_env();
  var StandAloneMonthParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 110);
      __publicField(this, "incompatibleTokens", [
        "Y",
        "R",
        "q",
        "Q",
        "M",
        "w",
        "I",
        "D",
        "i",
        "e",
        "c",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3) {
      const valueCallback = (value) => value - 1;
      switch (token) {
        // 1, 2, ..., 12
        case "L":
          return mapValue(
            parseNumericPattern(numericPatterns.month, dateString),
            valueCallback
          );
        // 01, 02, ..., 12
        case "LL":
          return mapValue(parseNDigits(2, dateString), valueCallback);
        // 1st, 2nd, ..., 12th
        case "Lo":
          return mapValue(
            match3.ordinalNumber(dateString, {
              unit: "month"
            }),
            valueCallback
          );
        // Jan, Feb, ..., Dec
        case "LLL":
          return match3.month(dateString, {
            width: "abbreviated",
            context: "standalone"
          }) || match3.month(dateString, { width: "narrow", context: "standalone" });
        // J, F, ..., D
        case "LLLLL":
          return match3.month(dateString, {
            width: "narrow",
            context: "standalone"
          });
        // January, February, ..., December
        case "LLLL":
        default:
          return match3.month(dateString, { width: "wide", context: "standalone" }) || match3.month(dateString, {
            width: "abbreviated",
            context: "standalone"
          }) || match3.month(dateString, { width: "narrow", context: "standalone" });
      }
    }
    validate(_date, value) {
      return value >= 0 && value <= 11;
    }
    set(date, _flags, value) {
      date.setMonth(value, 1);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/LocalWeekParser.js
  init_define_import_meta_env();

  // node_modules/date-fns/setWeek.js
  init_define_import_meta_env();
  function setWeek(date, week, options3) {
    const date_ = toDate(date, options3?.in);
    const diff = getWeek(date_, options3) - week;
    date_.setDate(date_.getDate() - diff * 7);
    return toDate(date_, options3?.in);
  }

  // node_modules/date-fns/parse/_lib/parsers/LocalWeekParser.js
  var LocalWeekParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 100);
      __publicField(this, "incompatibleTokens", [
        "y",
        "R",
        "u",
        "q",
        "Q",
        "M",
        "L",
        "I",
        "d",
        "D",
        "i",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "w":
          return parseNumericPattern(numericPatterns.week, dateString);
        case "wo":
          return match3.ordinalNumber(dateString, { unit: "week" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(_date, value) {
      return value >= 1 && value <= 53;
    }
    set(date, _flags, value, options3) {
      return startOfWeek(setWeek(date, value, options3), options3);
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/ISOWeekParser.js
  init_define_import_meta_env();

  // node_modules/date-fns/setISOWeek.js
  init_define_import_meta_env();
  function setISOWeek(date, week, options3) {
    const _date = toDate(date, options3?.in);
    const diff = getISOWeek(_date, options3) - week;
    _date.setDate(_date.getDate() - diff * 7);
    return _date;
  }

  // node_modules/date-fns/parse/_lib/parsers/ISOWeekParser.js
  var ISOWeekParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 100);
      __publicField(this, "incompatibleTokens", [
        "y",
        "Y",
        "u",
        "q",
        "Q",
        "M",
        "L",
        "w",
        "d",
        "D",
        "e",
        "c",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "I":
          return parseNumericPattern(numericPatterns.week, dateString);
        case "Io":
          return match3.ordinalNumber(dateString, { unit: "week" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(_date, value) {
      return value >= 1 && value <= 53;
    }
    set(date, _flags, value) {
      return startOfISOWeek(setISOWeek(date, value));
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/DateParser.js
  init_define_import_meta_env();
  var DAYS_IN_MONTH = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
  var DAYS_IN_MONTH_LEAP_YEAR = [
    31,
    29,
    31,
    30,
    31,
    30,
    31,
    31,
    30,
    31,
    30,
    31
  ];
  var DateParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 90);
      __publicField(this, "subPriority", 1);
      __publicField(this, "incompatibleTokens", [
        "Y",
        "R",
        "q",
        "Q",
        "w",
        "I",
        "D",
        "i",
        "e",
        "c",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "d":
          return parseNumericPattern(numericPatterns.date, dateString);
        case "do":
          return match3.ordinalNumber(dateString, { unit: "date" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(date, value) {
      const year = date.getFullYear();
      const isLeapYear = isLeapYearIndex(year);
      const month = date.getMonth();
      if (isLeapYear) {
        return value >= 1 && value <= DAYS_IN_MONTH_LEAP_YEAR[month];
      } else {
        return value >= 1 && value <= DAYS_IN_MONTH[month];
      }
    }
    set(date, _flags, value) {
      date.setDate(value);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/DayOfYearParser.js
  init_define_import_meta_env();
  var DayOfYearParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 90);
      __publicField(this, "subpriority", 1);
      __publicField(this, "incompatibleTokens", [
        "Y",
        "R",
        "q",
        "Q",
        "M",
        "L",
        "w",
        "I",
        "d",
        "E",
        "i",
        "e",
        "c",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "D":
        case "DD":
          return parseNumericPattern(numericPatterns.dayOfYear, dateString);
        case "Do":
          return match3.ordinalNumber(dateString, { unit: "date" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(date, value) {
      const year = date.getFullYear();
      const isLeapYear = isLeapYearIndex(year);
      if (isLeapYear) {
        return value >= 1 && value <= 366;
      } else {
        return value >= 1 && value <= 365;
      }
    }
    set(date, _flags, value) {
      date.setMonth(0, value);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/DayParser.js
  init_define_import_meta_env();

  // node_modules/date-fns/setDay.js
  init_define_import_meta_env();
  function setDay(date, day, options3) {
    const defaultOptions4 = getDefaultOptions();
    const weekStartsOn = options3?.weekStartsOn ?? options3?.locale?.options?.weekStartsOn ?? defaultOptions4.weekStartsOn ?? defaultOptions4.locale?.options?.weekStartsOn ?? 0;
    const date_ = toDate(date, options3?.in);
    const currentDay = date_.getDay();
    const remainder = day % 7;
    const dayIndex = (remainder + 7) % 7;
    const delta = 7 - weekStartsOn;
    const diff = day < 0 || day > 6 ? day - (currentDay + delta) % 7 : (dayIndex + delta) % 7 - (currentDay + delta) % 7;
    return addDays(date_, diff, options3);
  }

  // node_modules/date-fns/parse/_lib/parsers/DayParser.js
  var DayParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 90);
      __publicField(this, "incompatibleTokens", ["D", "i", "e", "c", "t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        // Tue
        case "E":
        case "EE":
        case "EEE":
          return match3.day(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.day(dateString, { width: "short", context: "formatting" }) || match3.day(dateString, { width: "narrow", context: "formatting" });
        // T
        case "EEEEE":
          return match3.day(dateString, {
            width: "narrow",
            context: "formatting"
          });
        // Tu
        case "EEEEEE":
          return match3.day(dateString, { width: "short", context: "formatting" }) || match3.day(dateString, { width: "narrow", context: "formatting" });
        // Tuesday
        case "EEEE":
        default:
          return match3.day(dateString, { width: "wide", context: "formatting" }) || match3.day(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.day(dateString, { width: "short", context: "formatting" }) || match3.day(dateString, { width: "narrow", context: "formatting" });
      }
    }
    validate(_date, value) {
      return value >= 0 && value <= 6;
    }
    set(date, _flags, value, options3) {
      date = setDay(date, value, options3);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/LocalDayParser.js
  init_define_import_meta_env();
  var LocalDayParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 90);
      __publicField(this, "incompatibleTokens", [
        "y",
        "R",
        "u",
        "q",
        "Q",
        "M",
        "L",
        "I",
        "d",
        "D",
        "E",
        "i",
        "c",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3, options3) {
      const valueCallback = (value) => {
        const wholeWeekDays = Math.floor((value - 1) / 7) * 7;
        return (value + options3.weekStartsOn + 6) % 7 + wholeWeekDays;
      };
      switch (token) {
        // 3
        case "e":
        case "ee":
          return mapValue(parseNDigits(token.length, dateString), valueCallback);
        // 3rd
        case "eo":
          return mapValue(
            match3.ordinalNumber(dateString, {
              unit: "day"
            }),
            valueCallback
          );
        // Tue
        case "eee":
          return match3.day(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.day(dateString, { width: "short", context: "formatting" }) || match3.day(dateString, { width: "narrow", context: "formatting" });
        // T
        case "eeeee":
          return match3.day(dateString, {
            width: "narrow",
            context: "formatting"
          });
        // Tu
        case "eeeeee":
          return match3.day(dateString, { width: "short", context: "formatting" }) || match3.day(dateString, { width: "narrow", context: "formatting" });
        // Tuesday
        case "eeee":
        default:
          return match3.day(dateString, { width: "wide", context: "formatting" }) || match3.day(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.day(dateString, { width: "short", context: "formatting" }) || match3.day(dateString, { width: "narrow", context: "formatting" });
      }
    }
    validate(_date, value) {
      return value >= 0 && value <= 6;
    }
    set(date, _flags, value, options3) {
      date = setDay(date, value, options3);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/StandAloneLocalDayParser.js
  init_define_import_meta_env();
  var StandAloneLocalDayParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 90);
      __publicField(this, "incompatibleTokens", [
        "y",
        "R",
        "u",
        "q",
        "Q",
        "M",
        "L",
        "I",
        "d",
        "D",
        "E",
        "i",
        "e",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3, options3) {
      const valueCallback = (value) => {
        const wholeWeekDays = Math.floor((value - 1) / 7) * 7;
        return (value + options3.weekStartsOn + 6) % 7 + wholeWeekDays;
      };
      switch (token) {
        // 3
        case "c":
        case "cc":
          return mapValue(parseNDigits(token.length, dateString), valueCallback);
        // 3rd
        case "co":
          return mapValue(
            match3.ordinalNumber(dateString, {
              unit: "day"
            }),
            valueCallback
          );
        // Tue
        case "ccc":
          return match3.day(dateString, {
            width: "abbreviated",
            context: "standalone"
          }) || match3.day(dateString, { width: "short", context: "standalone" }) || match3.day(dateString, { width: "narrow", context: "standalone" });
        // T
        case "ccccc":
          return match3.day(dateString, {
            width: "narrow",
            context: "standalone"
          });
        // Tu
        case "cccccc":
          return match3.day(dateString, { width: "short", context: "standalone" }) || match3.day(dateString, { width: "narrow", context: "standalone" });
        // Tuesday
        case "cccc":
        default:
          return match3.day(dateString, { width: "wide", context: "standalone" }) || match3.day(dateString, {
            width: "abbreviated",
            context: "standalone"
          }) || match3.day(dateString, { width: "short", context: "standalone" }) || match3.day(dateString, { width: "narrow", context: "standalone" });
      }
    }
    validate(_date, value) {
      return value >= 0 && value <= 6;
    }
    set(date, _flags, value, options3) {
      date = setDay(date, value, options3);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/ISODayParser.js
  init_define_import_meta_env();

  // node_modules/date-fns/setISODay.js
  init_define_import_meta_env();
  function setISODay(date, day, options3) {
    const date_ = toDate(date, options3?.in);
    const currentDay = getISODay(date_, options3);
    const diff = day - currentDay;
    return addDays(date_, diff, options3);
  }

  // node_modules/date-fns/parse/_lib/parsers/ISODayParser.js
  var ISODayParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 90);
      __publicField(this, "incompatibleTokens", [
        "y",
        "Y",
        "u",
        "q",
        "Q",
        "M",
        "L",
        "w",
        "d",
        "D",
        "E",
        "e",
        "c",
        "t",
        "T"
      ]);
    }
    parse(dateString, token, match3) {
      const valueCallback = (value) => {
        if (value === 0) {
          return 7;
        }
        return value;
      };
      switch (token) {
        // 2
        case "i":
        case "ii":
          return parseNDigits(token.length, dateString);
        // 2nd
        case "io":
          return match3.ordinalNumber(dateString, { unit: "day" });
        // Tue
        case "iii":
          return mapValue(
            match3.day(dateString, {
              width: "abbreviated",
              context: "formatting"
            }) || match3.day(dateString, {
              width: "short",
              context: "formatting"
            }) || match3.day(dateString, {
              width: "narrow",
              context: "formatting"
            }),
            valueCallback
          );
        // T
        case "iiiii":
          return mapValue(
            match3.day(dateString, {
              width: "narrow",
              context: "formatting"
            }),
            valueCallback
          );
        // Tu
        case "iiiiii":
          return mapValue(
            match3.day(dateString, {
              width: "short",
              context: "formatting"
            }) || match3.day(dateString, {
              width: "narrow",
              context: "formatting"
            }),
            valueCallback
          );
        // Tuesday
        case "iiii":
        default:
          return mapValue(
            match3.day(dateString, {
              width: "wide",
              context: "formatting"
            }) || match3.day(dateString, {
              width: "abbreviated",
              context: "formatting"
            }) || match3.day(dateString, {
              width: "short",
              context: "formatting"
            }) || match3.day(dateString, {
              width: "narrow",
              context: "formatting"
            }),
            valueCallback
          );
      }
    }
    validate(_date, value) {
      return value >= 1 && value <= 7;
    }
    set(date, _flags, value) {
      date = setISODay(date, value);
      date.setHours(0, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/AMPMParser.js
  init_define_import_meta_env();
  var AMPMParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 80);
      __publicField(this, "incompatibleTokens", ["b", "B", "H", "k", "t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "a":
        case "aa":
        case "aaa":
          return match3.dayPeriod(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.dayPeriod(dateString, {
            width: "narrow",
            context: "formatting"
          });
        case "aaaaa":
          return match3.dayPeriod(dateString, {
            width: "narrow",
            context: "formatting"
          });
        case "aaaa":
        default:
          return match3.dayPeriod(dateString, {
            width: "wide",
            context: "formatting"
          }) || match3.dayPeriod(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.dayPeriod(dateString, {
            width: "narrow",
            context: "formatting"
          });
      }
    }
    set(date, _flags, value) {
      date.setHours(dayPeriodEnumToHours(value), 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/AMPMMidnightParser.js
  init_define_import_meta_env();
  var AMPMMidnightParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 80);
      __publicField(this, "incompatibleTokens", ["a", "B", "H", "k", "t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "b":
        case "bb":
        case "bbb":
          return match3.dayPeriod(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.dayPeriod(dateString, {
            width: "narrow",
            context: "formatting"
          });
        case "bbbbb":
          return match3.dayPeriod(dateString, {
            width: "narrow",
            context: "formatting"
          });
        case "bbbb":
        default:
          return match3.dayPeriod(dateString, {
            width: "wide",
            context: "formatting"
          }) || match3.dayPeriod(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.dayPeriod(dateString, {
            width: "narrow",
            context: "formatting"
          });
      }
    }
    set(date, _flags, value) {
      date.setHours(dayPeriodEnumToHours(value), 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/DayPeriodParser.js
  init_define_import_meta_env();
  var DayPeriodParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 80);
      __publicField(this, "incompatibleTokens", ["a", "b", "t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "B":
        case "BB":
        case "BBB":
          return match3.dayPeriod(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.dayPeriod(dateString, {
            width: "narrow",
            context: "formatting"
          });
        case "BBBBB":
          return match3.dayPeriod(dateString, {
            width: "narrow",
            context: "formatting"
          });
        case "BBBB":
        default:
          return match3.dayPeriod(dateString, {
            width: "wide",
            context: "formatting"
          }) || match3.dayPeriod(dateString, {
            width: "abbreviated",
            context: "formatting"
          }) || match3.dayPeriod(dateString, {
            width: "narrow",
            context: "formatting"
          });
      }
    }
    set(date, _flags, value) {
      date.setHours(dayPeriodEnumToHours(value), 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/Hour1to12Parser.js
  init_define_import_meta_env();
  var Hour1to12Parser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 70);
      __publicField(this, "incompatibleTokens", ["H", "K", "k", "t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "h":
          return parseNumericPattern(numericPatterns.hour12h, dateString);
        case "ho":
          return match3.ordinalNumber(dateString, { unit: "hour" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(_date, value) {
      return value >= 1 && value <= 12;
    }
    set(date, _flags, value) {
      const isPM = date.getHours() >= 12;
      if (isPM && value < 12) {
        date.setHours(value + 12, 0, 0, 0);
      } else if (!isPM && value === 12) {
        date.setHours(0, 0, 0, 0);
      } else {
        date.setHours(value, 0, 0, 0);
      }
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/Hour0to23Parser.js
  init_define_import_meta_env();
  var Hour0to23Parser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 70);
      __publicField(this, "incompatibleTokens", ["a", "b", "h", "K", "k", "t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "H":
          return parseNumericPattern(numericPatterns.hour23h, dateString);
        case "Ho":
          return match3.ordinalNumber(dateString, { unit: "hour" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(_date, value) {
      return value >= 0 && value <= 23;
    }
    set(date, _flags, value) {
      date.setHours(value, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/Hour0To11Parser.js
  init_define_import_meta_env();
  var Hour0To11Parser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 70);
      __publicField(this, "incompatibleTokens", ["h", "H", "k", "t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "K":
          return parseNumericPattern(numericPatterns.hour11h, dateString);
        case "Ko":
          return match3.ordinalNumber(dateString, { unit: "hour" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(_date, value) {
      return value >= 0 && value <= 11;
    }
    set(date, _flags, value) {
      const isPM = date.getHours() >= 12;
      if (isPM && value < 12) {
        date.setHours(value + 12, 0, 0, 0);
      } else {
        date.setHours(value, 0, 0, 0);
      }
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/Hour1To24Parser.js
  init_define_import_meta_env();
  var Hour1To24Parser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 70);
      __publicField(this, "incompatibleTokens", ["a", "b", "h", "H", "K", "t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "k":
          return parseNumericPattern(numericPatterns.hour24h, dateString);
        case "ko":
          return match3.ordinalNumber(dateString, { unit: "hour" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(_date, value) {
      return value >= 1 && value <= 24;
    }
    set(date, _flags, value) {
      const hours = value <= 24 ? value % 24 : value;
      date.setHours(hours, 0, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/MinuteParser.js
  init_define_import_meta_env();
  var MinuteParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 60);
      __publicField(this, "incompatibleTokens", ["t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "m":
          return parseNumericPattern(numericPatterns.minute, dateString);
        case "mo":
          return match3.ordinalNumber(dateString, { unit: "minute" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(_date, value) {
      return value >= 0 && value <= 59;
    }
    set(date, _flags, value) {
      date.setMinutes(value, 0, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/SecondParser.js
  init_define_import_meta_env();
  var SecondParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 50);
      __publicField(this, "incompatibleTokens", ["t", "T"]);
    }
    parse(dateString, token, match3) {
      switch (token) {
        case "s":
          return parseNumericPattern(numericPatterns.second, dateString);
        case "so":
          return match3.ordinalNumber(dateString, { unit: "second" });
        default:
          return parseNDigits(token.length, dateString);
      }
    }
    validate(_date, value) {
      return value >= 0 && value <= 59;
    }
    set(date, _flags, value) {
      date.setSeconds(value, 0);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/FractionOfSecondParser.js
  init_define_import_meta_env();
  var FractionOfSecondParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 30);
      __publicField(this, "incompatibleTokens", ["t", "T"]);
    }
    parse(dateString, token) {
      const valueCallback = (value) => Math.trunc(value * Math.pow(10, -token.length + 3));
      return mapValue(parseNDigits(token.length, dateString), valueCallback);
    }
    set(date, _flags, value) {
      date.setMilliseconds(value);
      return date;
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/ISOTimezoneWithZParser.js
  init_define_import_meta_env();
  var ISOTimezoneWithZParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 10);
      __publicField(this, "incompatibleTokens", ["t", "T", "x"]);
    }
    parse(dateString, token) {
      switch (token) {
        case "X":
          return parseTimezonePattern(
            timezonePatterns.basicOptionalMinutes,
            dateString
          );
        case "XX":
          return parseTimezonePattern(timezonePatterns.basic, dateString);
        case "XXXX":
          return parseTimezonePattern(
            timezonePatterns.basicOptionalSeconds,
            dateString
          );
        case "XXXXX":
          return parseTimezonePattern(
            timezonePatterns.extendedOptionalSeconds,
            dateString
          );
        case "XXX":
        default:
          return parseTimezonePattern(timezonePatterns.extended, dateString);
      }
    }
    set(date, flags, value) {
      if (flags.timestampIsSet) return date;
      return constructFrom(
        date,
        date.getTime() - getTimezoneOffsetInMilliseconds(date) - value
      );
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/ISOTimezoneParser.js
  init_define_import_meta_env();
  var ISOTimezoneParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 10);
      __publicField(this, "incompatibleTokens", ["t", "T", "X"]);
    }
    parse(dateString, token) {
      switch (token) {
        case "x":
          return parseTimezonePattern(
            timezonePatterns.basicOptionalMinutes,
            dateString
          );
        case "xx":
          return parseTimezonePattern(timezonePatterns.basic, dateString);
        case "xxxx":
          return parseTimezonePattern(
            timezonePatterns.basicOptionalSeconds,
            dateString
          );
        case "xxxxx":
          return parseTimezonePattern(
            timezonePatterns.extendedOptionalSeconds,
            dateString
          );
        case "xxx":
        default:
          return parseTimezonePattern(timezonePatterns.extended, dateString);
      }
    }
    set(date, flags, value) {
      if (flags.timestampIsSet) return date;
      return constructFrom(
        date,
        date.getTime() - getTimezoneOffsetInMilliseconds(date) - value
      );
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/TimestampSecondsParser.js
  init_define_import_meta_env();
  var TimestampSecondsParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 40);
      __publicField(this, "incompatibleTokens", "*");
    }
    parse(dateString) {
      return parseAnyDigitsSigned(dateString);
    }
    set(date, _flags, value) {
      return [constructFrom(date, value * 1e3), { timestampIsSet: true }];
    }
  };

  // node_modules/date-fns/parse/_lib/parsers/TimestampMillisecondsParser.js
  init_define_import_meta_env();
  var TimestampMillisecondsParser = class extends Parser {
    constructor() {
      super(...arguments);
      __publicField(this, "priority", 20);
      __publicField(this, "incompatibleTokens", "*");
    }
    parse(dateString) {
      return parseAnyDigitsSigned(dateString);
    }
    set(date, _flags, value) {
      return [constructFrom(date, value), { timestampIsSet: true }];
    }
  };

  // node_modules/date-fns/parse/_lib/parsers.js
  var parsers = {
    G: new EraParser(),
    y: new YearParser(),
    Y: new LocalWeekYearParser(),
    R: new ISOWeekYearParser(),
    u: new ExtendedYearParser(),
    Q: new QuarterParser(),
    q: new StandAloneQuarterParser(),
    M: new MonthParser(),
    L: new StandAloneMonthParser(),
    w: new LocalWeekParser(),
    I: new ISOWeekParser(),
    d: new DateParser(),
    D: new DayOfYearParser(),
    E: new DayParser(),
    e: new LocalDayParser(),
    c: new StandAloneLocalDayParser(),
    i: new ISODayParser(),
    a: new AMPMParser(),
    b: new AMPMMidnightParser(),
    B: new DayPeriodParser(),
    h: new Hour1to12Parser(),
    H: new Hour0to23Parser(),
    K: new Hour0To11Parser(),
    k: new Hour1To24Parser(),
    m: new MinuteParser(),
    s: new SecondParser(),
    S: new FractionOfSecondParser(),
    X: new ISOTimezoneWithZParser(),
    x: new ISOTimezoneParser(),
    t: new TimestampSecondsParser(),
    T: new TimestampMillisecondsParser()
  };

  // node_modules/date-fns/parse.js
  var formattingTokensRegExp2 = /[yYQqMLwIdDecihHKkms]o|(\w)\1*|''|'(''|[^'])+('|$)|./g;
  var longFormattingTokensRegExp2 = /P+p+|P+|p+|''|'(''|[^'])+('|$)|./g;
  var escapedStringRegExp2 = /^'([^]*?)'?$/;
  var doubleQuoteRegExp2 = /''/g;
  var notWhitespaceRegExp = /\S/;
  var unescapedLatinCharacterRegExp2 = /[a-zA-Z]/;
  function parse(dateStr, formatStr, referenceDate, options3) {
    const invalidDate = () => constructFrom(options3?.in || referenceDate, NaN);
    const defaultOptions4 = getDefaultOptions2();
    const locale = options3?.locale ?? defaultOptions4.locale ?? enUS;
    const firstWeekContainsDate = options3?.firstWeekContainsDate ?? options3?.locale?.options?.firstWeekContainsDate ?? defaultOptions4.firstWeekContainsDate ?? defaultOptions4.locale?.options?.firstWeekContainsDate ?? 1;
    const weekStartsOn = options3?.weekStartsOn ?? options3?.locale?.options?.weekStartsOn ?? defaultOptions4.weekStartsOn ?? defaultOptions4.locale?.options?.weekStartsOn ?? 0;
    if (!formatStr)
      return dateStr ? invalidDate() : toDate(referenceDate, options3?.in);
    const subFnOptions = {
      firstWeekContainsDate,
      weekStartsOn,
      locale
    };
    const setters = [new DateTimezoneSetter(options3?.in, referenceDate)];
    const tokens = formatStr.match(longFormattingTokensRegExp2).map((substring) => {
      const firstCharacter = substring[0];
      if (firstCharacter in longFormatters) {
        const longFormatter = longFormatters[firstCharacter];
        return longFormatter(substring, locale.formatLong);
      }
      return substring;
    }).join("").match(formattingTokensRegExp2);
    const usedTokens = [];
    for (let token of tokens) {
      if (!options3?.useAdditionalWeekYearTokens && isProtectedWeekYearToken(token)) {
        warnOrThrowProtectedError(token, formatStr, dateStr);
      }
      if (!options3?.useAdditionalDayOfYearTokens && isProtectedDayOfYearToken(token)) {
        warnOrThrowProtectedError(token, formatStr, dateStr);
      }
      const firstCharacter = token[0];
      const parser = parsers[firstCharacter];
      if (parser) {
        const { incompatibleTokens } = parser;
        if (Array.isArray(incompatibleTokens)) {
          const incompatibleToken = usedTokens.find(
            (usedToken) => incompatibleTokens.includes(usedToken.token) || usedToken.token === firstCharacter
          );
          if (incompatibleToken) {
            throw new RangeError(
              `The format string mustn't contain \`${incompatibleToken.fullToken}\` and \`${token}\` at the same time`
            );
          }
        } else if (parser.incompatibleTokens === "*" && usedTokens.length > 0) {
          throw new RangeError(
            `The format string mustn't contain \`${token}\` and any other token at the same time`
          );
        }
        usedTokens.push({ token: firstCharacter, fullToken: token });
        const parseResult = parser.run(
          dateStr,
          token,
          locale.match,
          subFnOptions
        );
        if (!parseResult) {
          return invalidDate();
        }
        setters.push(parseResult.setter);
        dateStr = parseResult.rest;
      } else {
        if (firstCharacter.match(unescapedLatinCharacterRegExp2)) {
          throw new RangeError(
            "Format string contains an unescaped latin alphabet character `" + firstCharacter + "`"
          );
        }
        if (token === "''") {
          token = "'";
        } else if (firstCharacter === "'") {
          token = cleanEscapedString2(token);
        }
        if (dateStr.indexOf(token) === 0) {
          dateStr = dateStr.slice(token.length);
        } else {
          return invalidDate();
        }
      }
    }
    if (dateStr.length > 0 && notWhitespaceRegExp.test(dateStr)) {
      return invalidDate();
    }
    const uniquePrioritySetters = setters.map((setter) => setter.priority).sort((a, b) => b - a).filter((priority, index2, array) => array.indexOf(priority) === index2).map(
      (priority) => setters.filter((setter) => setter.priority === priority).sort((a, b) => b.subPriority - a.subPriority)
    ).map((setterArray) => setterArray[0]);
    let date = toDate(referenceDate, options3?.in);
    if (isNaN(+date)) return invalidDate();
    const flags = {};
    for (const setter of uniquePrioritySetters) {
      if (!setter.validate(date, subFnOptions)) {
        return invalidDate();
      }
      const result = setter.set(date, flags, subFnOptions);
      if (Array.isArray(result)) {
        date = result[0];
        Object.assign(flags, result[1]);
      } else {
        date = result;
      }
    }
    return date;
  }
  function cleanEscapedString2(input) {
    return input.match(escapedStringRegExp2)[1].replace(doubleQuoteRegExp2, "'");
  }

  // node_modules/date-fns/isSameMonth.js
  init_define_import_meta_env();
  function isSameMonth(laterDate, earlierDate, options3) {
    const [laterDate_, earlierDate_] = normalizeDates(
      options3?.in,
      laterDate,
      earlierDate
    );
    return laterDate_.getFullYear() === earlierDate_.getFullYear() && laterDate_.getMonth() === earlierDate_.getMonth();
  }

  // node_modules/date-fns/isSameYear.js
  init_define_import_meta_env();
  function isSameYear(laterDate, earlierDate, options3) {
    const [laterDate_, earlierDate_] = normalizeDates(
      options3?.in,
      laterDate,
      earlierDate
    );
    return laterDate_.getFullYear() === earlierDate_.getFullYear();
  }

  // node_modules/date-fns/isWithinInterval.js
  init_define_import_meta_env();
  function isWithinInterval(date, interval, options3) {
    const time = +toDate(date, options3?.in);
    const [startTime, endTime] = [
      +toDate(interval.start, options3?.in),
      +toDate(interval.end, options3?.in)
    ].sort((a, b) => a - b);
    return time >= startTime && time <= endTime;
  }

  // node_modules/date-fns/subDays.js
  init_define_import_meta_env();
  function subDays(date, amount, options3) {
    return addDays(date, -amount, options3);
  }

  // node_modules/date-fns/setMonth.js
  init_define_import_meta_env();
  function setMonth(date, month, options3) {
    const _date = toDate(date, options3?.in);
    const year = _date.getFullYear();
    const day = _date.getDate();
    const midMonth = constructFrom(options3?.in || date, 0);
    midMonth.setFullYear(year, month, 15);
    midMonth.setHours(0, 0, 0, 0);
    const daysInMonth = getDaysInMonth(midMonth);
    _date.setMonth(month, Math.min(day, daysInMonth));
    return _date;
  }

  // node_modules/date-fns/setYear.js
  init_define_import_meta_env();
  function setYear(date, year, options3) {
    const date_ = toDate(date, options3?.in);
    if (isNaN(+date_)) return constructFrom(options3?.in || date, NaN);
    date_.setFullYear(year);
    return date_;
  }

  // node_modules/date-fns/sub.js
  init_define_import_meta_env();

  // node_modules/date-fns/subMonths.js
  init_define_import_meta_env();
  function subMonths(date, amount, options3) {
    return addMonths(date, -amount, options3);
  }

  // node_modules/date-fns/sub.js
  function sub(date, duration, options3) {
    const {
      years = 0,
      months = 0,
      weeks = 0,
      days = 0,
      hours = 0,
      minutes = 0,
      seconds = 0
    } = duration;
    const withoutMonths = subMonths(date, months + years * 12, options3);
    const withoutDays = subDays(withoutMonths, days + weeks * 7, options3);
    const minutesToSub = minutes + hours * 60;
    const secondsToSub = seconds + minutesToSub * 60;
    const msToSub = secondsToSub * 1e3;
    return constructFrom(options3?.in || date, +withoutDays - msToSub);
  }

  // node_modules/date-fns/locale/ar.js
  init_define_import_meta_env();

  // node_modules/date-fns/locale/ar/_lib/formatDistance.js
  init_define_import_meta_env();
  var formatDistanceLocale2 = {
    lessThanXSeconds: {
      one: "\u0623\u0642\u0644 \u0645\u0646 \u062B\u0627\u0646\u064A\u0629",
      two: "\u0623\u0642\u0644 \u0645\u0646 \u062B\u0627\u0646\u064A\u062A\u064A\u0646",
      threeToTen: "\u0623\u0642\u0644 \u0645\u0646 {{count}} \u062B\u0648\u0627\u0646\u064A",
      other: "\u0623\u0642\u0644 \u0645\u0646 {{count}} \u062B\u0627\u0646\u064A\u0629"
    },
    xSeconds: {
      one: "\u062B\u0627\u0646\u064A\u0629 \u0648\u0627\u062D\u062F\u0629",
      two: "\u062B\u0627\u0646\u064A\u062A\u0627\u0646",
      threeToTen: "{{count}} \u062B\u0648\u0627\u0646\u064A",
      other: "{{count}} \u062B\u0627\u0646\u064A\u0629"
    },
    halfAMinute: "\u0646\u0635\u0641 \u062F\u0642\u064A\u0642\u0629",
    lessThanXMinutes: {
      one: "\u0623\u0642\u0644 \u0645\u0646 \u062F\u0642\u064A\u0642\u0629",
      two: "\u0623\u0642\u0644 \u0645\u0646 \u062F\u0642\u064A\u0642\u062A\u064A\u0646",
      threeToTen: "\u0623\u0642\u0644 \u0645\u0646 {{count}} \u062F\u0642\u0627\u0626\u0642",
      other: "\u0623\u0642\u0644 \u0645\u0646 {{count}} \u062F\u0642\u064A\u0642\u0629"
    },
    xMinutes: {
      one: "\u062F\u0642\u064A\u0642\u0629 \u0648\u0627\u062D\u062F\u0629",
      two: "\u062F\u0642\u064A\u0642\u062A\u0627\u0646",
      threeToTen: "{{count}} \u062F\u0642\u0627\u0626\u0642",
      other: "{{count}} \u062F\u0642\u064A\u0642\u0629"
    },
    aboutXHours: {
      one: "\u0633\u0627\u0639\u0629 \u0648\u0627\u062D\u062F\u0629 \u062A\u0642\u0631\u064A\u0628\u0627\u064B",
      two: "\u0633\u0627\u0639\u062A\u064A\u0646 \u062A\u0642\u0631\u064A\u0628\u0627",
      threeToTen: "{{count}} \u0633\u0627\u0639\u0627\u062A \u062A\u0642\u0631\u064A\u0628\u0627\u064B",
      other: "{{count}} \u0633\u0627\u0639\u0629 \u062A\u0642\u0631\u064A\u0628\u0627\u064B"
    },
    xHours: {
      one: "\u0633\u0627\u0639\u0629 \u0648\u0627\u062D\u062F\u0629",
      two: "\u0633\u0627\u0639\u062A\u0627\u0646",
      threeToTen: "{{count}} \u0633\u0627\u0639\u0627\u062A",
      other: "{{count}} \u0633\u0627\u0639\u0629"
    },
    xDays: {
      one: "\u064A\u0648\u0645 \u0648\u0627\u062D\u062F",
      two: "\u064A\u0648\u0645\u0627\u0646",
      threeToTen: "{{count}} \u0623\u064A\u0627\u0645",
      other: "{{count}} \u064A\u0648\u0645"
    },
    aboutXWeeks: {
      one: "\u0623\u0633\u0628\u0648\u0639 \u0648\u0627\u062D\u062F \u062A\u0642\u0631\u064A\u0628\u0627",
      two: "\u0623\u0633\u0628\u0648\u0639\u064A\u0646 \u062A\u0642\u0631\u064A\u0628\u0627",
      threeToTen: "{{count}} \u0623\u0633\u0627\u0628\u064A\u0639 \u062A\u0642\u0631\u064A\u0628\u0627",
      other: "{{count}} \u0623\u0633\u0628\u0648\u0639\u0627 \u062A\u0642\u0631\u064A\u0628\u0627"
    },
    xWeeks: {
      one: "\u0623\u0633\u0628\u0648\u0639 \u0648\u0627\u062D\u062F",
      two: "\u0623\u0633\u0628\u0648\u0639\u0627\u0646",
      threeToTen: "{{count}} \u0623\u0633\u0627\u0628\u064A\u0639",
      other: "{{count}} \u0623\u0633\u0628\u0648\u0639\u0627"
    },
    aboutXMonths: {
      one: "\u0634\u0647\u0631 \u0648\u0627\u062D\u062F \u062A\u0642\u0631\u064A\u0628\u0627\u064B",
      two: "\u0634\u0647\u0631\u064A\u0646 \u062A\u0642\u0631\u064A\u0628\u0627",
      threeToTen: "{{count}} \u0623\u0634\u0647\u0631 \u062A\u0642\u0631\u064A\u0628\u0627",
      other: "{{count}} \u0634\u0647\u0631\u0627 \u062A\u0642\u0631\u064A\u0628\u0627\u064B"
    },
    xMonths: {
      one: "\u0634\u0647\u0631 \u0648\u0627\u062D\u062F",
      two: "\u0634\u0647\u0631\u0627\u0646",
      threeToTen: "{{count}} \u0623\u0634\u0647\u0631",
      other: "{{count}} \u0634\u0647\u0631\u0627"
    },
    aboutXYears: {
      one: "\u0633\u0646\u0629 \u0648\u0627\u062D\u062F\u0629 \u062A\u0642\u0631\u064A\u0628\u0627\u064B",
      two: "\u0633\u0646\u062A\u064A\u0646 \u062A\u0642\u0631\u064A\u0628\u0627",
      threeToTen: "{{count}} \u0633\u0646\u0648\u0627\u062A \u062A\u0642\u0631\u064A\u0628\u0627\u064B",
      other: "{{count}} \u0633\u0646\u0629 \u062A\u0642\u0631\u064A\u0628\u0627\u064B"
    },
    xYears: {
      one: "\u0633\u0646\u0629 \u0648\u0627\u062D\u062F",
      two: "\u0633\u0646\u062A\u0627\u0646",
      threeToTen: "{{count}} \u0633\u0646\u0648\u0627\u062A",
      other: "{{count}} \u0633\u0646\u0629"
    },
    overXYears: {
      one: "\u0623\u0643\u062B\u0631 \u0645\u0646 \u0633\u0646\u0629",
      two: "\u0623\u0643\u062B\u0631 \u0645\u0646 \u0633\u0646\u062A\u064A\u0646",
      threeToTen: "\u0623\u0643\u062B\u0631 \u0645\u0646 {{count}} \u0633\u0646\u0648\u0627\u062A",
      other: "\u0623\u0643\u062B\u0631 \u0645\u0646 {{count}} \u0633\u0646\u0629"
    },
    almostXYears: {
      one: "\u0645\u0627 \u064A\u0642\u0627\u0631\u0628 \u0633\u0646\u0629 \u0648\u0627\u062D\u062F\u0629",
      two: "\u0645\u0627 \u064A\u0642\u0627\u0631\u0628 \u0633\u0646\u062A\u064A\u0646",
      threeToTen: "\u0645\u0627 \u064A\u0642\u0627\u0631\u0628 {{count}} \u0633\u0646\u0648\u0627\u062A",
      other: "\u0645\u0627 \u064A\u0642\u0627\u0631\u0628 {{count}} \u0633\u0646\u0629"
    }
  };
  var formatDistance3 = (token, count4, options3) => {
    const usageGroup = formatDistanceLocale2[token];
    let result;
    if (typeof usageGroup === "string") {
      result = usageGroup;
    } else if (count4 === 1) {
      result = usageGroup.one;
    } else if (count4 === 2) {
      result = usageGroup.two;
    } else if (count4 <= 10) {
      result = usageGroup.threeToTen.replace("{{count}}", String(count4));
    } else {
      result = usageGroup.other.replace("{{count}}", String(count4));
    }
    if (options3?.addSuffix) {
      if (options3.comparison && options3.comparison > 0) {
        return "\u062E\u0644\u0627\u0644 " + result;
      } else {
        return "\u0645\u0646\u0630 " + result;
      }
    }
    return result;
  };

  // node_modules/date-fns/locale/ar/_lib/formatLong.js
  init_define_import_meta_env();
  var dateFormats2 = {
    full: "EEEE\u060C do MMMM y",
    long: "do MMMM y",
    medium: "d MMM y",
    short: "dd/MM/yyyy"
  };
  var timeFormats2 = {
    full: "HH:mm:ss",
    long: "HH:mm:ss",
    medium: "HH:mm:ss",
    short: "HH:mm"
  };
  var dateTimeFormats2 = {
    full: "{{date}} '\u0639\u0646\u062F \u0627\u0644\u0633\u0627\u0639\u0629' {{time}}",
    long: "{{date}} '\u0639\u0646\u062F \u0627\u0644\u0633\u0627\u0639\u0629' {{time}}",
    medium: "{{date}}, {{time}}",
    short: "{{date}}, {{time}}"
  };
  var formatLong2 = {
    date: buildFormatLongFn({
      formats: dateFormats2,
      defaultWidth: "full"
    }),
    time: buildFormatLongFn({
      formats: timeFormats2,
      defaultWidth: "full"
    }),
    dateTime: buildFormatLongFn({
      formats: dateTimeFormats2,
      defaultWidth: "full"
    })
  };

  // node_modules/date-fns/locale/ar/_lib/formatRelative.js
  init_define_import_meta_env();
  var formatRelativeLocale2 = {
    lastWeek: "eeee '\u0627\u0644\u0645\u0627\u0636\u064A \u0639\u0646\u062F \u0627\u0644\u0633\u0627\u0639\u0629' p",
    yesterday: "'\u0627\u0644\u0623\u0645\u0633 \u0639\u0646\u062F \u0627\u0644\u0633\u0627\u0639\u0629' p",
    today: "'\u0627\u0644\u064A\u0648\u0645 \u0639\u0646\u062F \u0627\u0644\u0633\u0627\u0639\u0629' p",
    tomorrow: "'\u063A\u062F\u0627 \u0639\u0646\u062F \u0627\u0644\u0633\u0627\u0639\u0629' p",
    nextWeek: "eeee '\u0627\u0644\u0642\u0627\u062F\u0645 \u0639\u0646\u062F \u0627\u0644\u0633\u0627\u0639\u0629' p",
    other: "P"
  };
  var formatRelative2 = (token) => formatRelativeLocale2[token];

  // node_modules/date-fns/locale/ar/_lib/localize.js
  init_define_import_meta_env();
  var eraValues2 = {
    narrow: ["\u0642", "\u0628"],
    abbreviated: ["\u0642.\u0645.", "\u0628.\u0645."],
    wide: ["\u0642\u0628\u0644 \u0627\u0644\u0645\u064A\u0644\u0627\u062F", "\u0628\u0639\u062F \u0627\u0644\u0645\u064A\u0644\u0627\u062F"]
  };
  var quarterValues2 = {
    narrow: ["1", "2", "3", "4"],
    abbreviated: ["\u06311", "\u06312", "\u06313", "\u06314"],
    wide: ["\u0627\u0644\u0631\u0628\u0639 \u0627\u0644\u0623\u0648\u0644", "\u0627\u0644\u0631\u0628\u0639 \u0627\u0644\u062B\u0627\u0646\u064A", "\u0627\u0644\u0631\u0628\u0639 \u0627\u0644\u062B\u0627\u0644\u062B", "\u0627\u0644\u0631\u0628\u0639 \u0627\u0644\u0631\u0627\u0628\u0639"]
  };
  var monthValues2 = {
    narrow: ["\u064A", "\u0641", "\u0645", "\u0623", "\u0645", "\u064A", "\u064A", "\u0623", "\u0633", "\u0623", "\u0646", "\u062F"],
    abbreviated: [
      "\u064A\u0646\u0627\u064A\u0631",
      "\u0641\u0628\u0631\u0627\u064A\u0631",
      "\u0645\u0627\u0631\u0633",
      "\u0623\u0628\u0631\u064A\u0644",
      "\u0645\u0627\u064A\u0648",
      "\u064A\u0648\u0646\u064A\u0648",
      "\u064A\u0648\u0644\u064A\u0648",
      "\u0623\u063A\u0633\u0637\u0633",
      "\u0633\u0628\u062A\u0645\u0628\u0631",
      "\u0623\u0643\u062A\u0648\u0628\u0631",
      "\u0646\u0648\u0641\u0645\u0628\u0631",
      "\u062F\u064A\u0633\u0645\u0628\u0631"
    ],
    wide: [
      "\u064A\u0646\u0627\u064A\u0631",
      "\u0641\u0628\u0631\u0627\u064A\u0631",
      "\u0645\u0627\u0631\u0633",
      "\u0623\u0628\u0631\u064A\u0644",
      "\u0645\u0627\u064A\u0648",
      "\u064A\u0648\u0646\u064A\u0648",
      "\u064A\u0648\u0644\u064A\u0648",
      "\u0623\u063A\u0633\u0637\u0633",
      "\u0633\u0628\u062A\u0645\u0628\u0631",
      "\u0623\u0643\u062A\u0648\u0628\u0631",
      "\u0646\u0648\u0641\u0645\u0628\u0631",
      "\u062F\u064A\u0633\u0645\u0628\u0631"
    ]
  };
  var dayValues2 = {
    narrow: ["\u062D", "\u0646", "\u062B", "\u0631", "\u062E", "\u062C", "\u0633"],
    short: ["\u0623\u062D\u062F", "\u0627\u062B\u0646\u064A\u0646", "\u062B\u0644\u0627\u062B\u0627\u0621", "\u0623\u0631\u0628\u0639\u0627\u0621", "\u062E\u0645\u064A\u0633", "\u062C\u0645\u0639\u0629", "\u0633\u0628\u062A"],
    abbreviated: ["\u0623\u062D\u062F", "\u0627\u062B\u0646\u064A\u0646", "\u062B\u0644\u0627\u062B\u0627\u0621", "\u0623\u0631\u0628\u0639\u0627\u0621", "\u062E\u0645\u064A\u0633", "\u062C\u0645\u0639\u0629", "\u0633\u0628\u062A"],
    wide: [
      "\u0627\u0644\u0623\u062D\u062F",
      "\u0627\u0644\u0627\u062B\u0646\u064A\u0646",
      "\u0627\u0644\u062B\u0644\u0627\u062B\u0627\u0621",
      "\u0627\u0644\u0623\u0631\u0628\u0639\u0627\u0621",
      "\u0627\u0644\u062E\u0645\u064A\u0633",
      "\u0627\u0644\u062C\u0645\u0639\u0629",
      "\u0627\u0644\u0633\u0628\u062A"
    ]
  };
  var dayPeriodValues2 = {
    narrow: {
      am: "\u0635",
      pm: "\u0645",
      morning: "\u0627\u0644\u0635\u0628\u0627\u062D",
      noon: "\u0627\u0644\u0638\u0647\u0631",
      afternoon: "\u0628\u0639\u062F \u0627\u0644\u0638\u0647\u0631",
      evening: "\u0627\u0644\u0645\u0633\u0627\u0621",
      night: "\u0627\u0644\u0644\u064A\u0644",
      midnight: "\u0645\u0646\u062A\u0635\u0641 \u0627\u0644\u0644\u064A\u0644"
    },
    abbreviated: {
      am: "\u0635",
      pm: "\u0645",
      morning: "\u0627\u0644\u0635\u0628\u0627\u062D",
      noon: "\u0627\u0644\u0638\u0647\u0631",
      afternoon: "\u0628\u0639\u062F \u0627\u0644\u0638\u0647\u0631",
      evening: "\u0627\u0644\u0645\u0633\u0627\u0621",
      night: "\u0627\u0644\u0644\u064A\u0644",
      midnight: "\u0645\u0646\u062A\u0635\u0641 \u0627\u0644\u0644\u064A\u0644"
    },
    wide: {
      am: "\u0635",
      pm: "\u0645",
      morning: "\u0627\u0644\u0635\u0628\u0627\u062D",
      noon: "\u0627\u0644\u0638\u0647\u0631",
      afternoon: "\u0628\u0639\u062F \u0627\u0644\u0638\u0647\u0631",
      evening: "\u0627\u0644\u0645\u0633\u0627\u0621",
      night: "\u0627\u0644\u0644\u064A\u0644",
      midnight: "\u0645\u0646\u062A\u0635\u0641 \u0627\u0644\u0644\u064A\u0644"
    }
  };
  var formattingDayPeriodValues2 = {
    narrow: {
      am: "\u0635",
      pm: "\u0645",
      morning: "\u0641\u064A \u0627\u0644\u0635\u0628\u0627\u062D",
      noon: "\u0627\u0644\u0638\u0647\u0631",
      afternoon: "\u0628\u0639\u062F \u0627\u0644\u0638\u0647\u0631",
      evening: "\u0641\u064A \u0627\u0644\u0645\u0633\u0627\u0621",
      night: "\u0641\u064A \u0627\u0644\u0644\u064A\u0644",
      midnight: "\u0645\u0646\u062A\u0635\u0641 \u0627\u0644\u0644\u064A\u0644"
    },
    abbreviated: {
      am: "\u0635",
      pm: "\u0645",
      morning: "\u0641\u064A \u0627\u0644\u0635\u0628\u0627\u062D",
      noon: "\u0627\u0644\u0638\u0647\u0631",
      afternoon: "\u0628\u0639\u062F \u0627\u0644\u0638\u0647\u0631",
      evening: "\u0641\u064A \u0627\u0644\u0645\u0633\u0627\u0621",
      night: "\u0641\u064A \u0627\u0644\u0644\u064A\u0644",
      midnight: "\u0645\u0646\u062A\u0635\u0641 \u0627\u0644\u0644\u064A\u0644"
    },
    wide: {
      am: "\u0635",
      pm: "\u0645",
      morning: "\u0641\u064A \u0627\u0644\u0635\u0628\u0627\u062D",
      noon: "\u0627\u0644\u0638\u0647\u0631",
      afternoon: "\u0628\u0639\u062F \u0627\u0644\u0638\u0647\u0631",
      evening: "\u0641\u064A \u0627\u0644\u0645\u0633\u0627\u0621",
      night: "\u0641\u064A \u0627\u0644\u0644\u064A\u0644",
      midnight: "\u0645\u0646\u062A\u0635\u0641 \u0627\u0644\u0644\u064A\u0644"
    }
  };
  var ordinalNumber2 = (num) => String(num);
  var localize2 = {
    ordinalNumber: ordinalNumber2,
    era: buildLocalizeFn({
      values: eraValues2,
      defaultWidth: "wide"
    }),
    quarter: buildLocalizeFn({
      values: quarterValues2,
      defaultWidth: "wide",
      argumentCallback: (quarter) => quarter - 1
    }),
    month: buildLocalizeFn({
      values: monthValues2,
      defaultWidth: "wide"
    }),
    day: buildLocalizeFn({
      values: dayValues2,
      defaultWidth: "wide"
    }),
    dayPeriod: buildLocalizeFn({
      values: dayPeriodValues2,
      defaultWidth: "wide",
      formattingValues: formattingDayPeriodValues2,
      defaultFormattingWidth: "wide"
    })
  };

  // node_modules/date-fns/locale/ar/_lib/match.js
  init_define_import_meta_env();
  var matchOrdinalNumberPattern2 = /^(\d+)(th|st|nd|rd)?/i;
  var parseOrdinalNumberPattern2 = /\d+/i;
  var matchEraPatterns2 = {
    narrow: /[قب]/,
    abbreviated: /[قب]\.م\./,
    wide: /(قبل|بعد) الميلاد/
  };
  var parseEraPatterns2 = {
    any: [/قبل/, /بعد/]
  };
  var matchQuarterPatterns2 = {
    narrow: /^[1234]/i,
    abbreviated: /ر[1234]/,
    wide: /الربع (الأول|الثاني|الثالث|الرابع)/
  };
  var parseQuarterPatterns2 = {
    any: [/1/i, /2/i, /3/i, /4/i]
  };
  var matchMonthPatterns2 = {
    narrow: /^[أيفمسند]/,
    abbreviated: /^(يناير|فبراير|مارس|أبريل|مايو|يونيو|يوليو|أغسطس|سبتمبر|أكتوبر|نوفمبر|ديسمبر)/,
    wide: /^(يناير|فبراير|مارس|أبريل|مايو|يونيو|يوليو|أغسطس|سبتمبر|أكتوبر|نوفمبر|ديسمبر)/
  };
  var parseMonthPatterns2 = {
    narrow: [
      /^ي/i,
      /^ف/i,
      /^م/i,
      /^أ/i,
      /^م/i,
      /^ي/i,
      /^ي/i,
      /^أ/i,
      /^س/i,
      /^أ/i,
      /^ن/i,
      /^د/i
    ],
    any: [
      /^يناير/i,
      /^فبراير/i,
      /^مارس/i,
      /^أبريل/i,
      /^مايو/i,
      /^يونيو/i,
      /^يوليو/i,
      /^أغسطس/i,
      /^سبتمبر/i,
      /^أكتوبر/i,
      /^نوفمبر/i,
      /^ديسمبر/i
    ]
  };
  var matchDayPatterns2 = {
    narrow: /^[حنثرخجس]/i,
    short: /^(أحد|اثنين|ثلاثاء|أربعاء|خميس|جمعة|سبت)/i,
    abbreviated: /^(أحد|اثنين|ثلاثاء|أربعاء|خميس|جمعة|سبت)/i,
    wide: /^(الأحد|الاثنين|الثلاثاء|الأربعاء|الخميس|الجمعة|السبت)/i
  };
  var parseDayPatterns2 = {
    narrow: [/^ح/i, /^ن/i, /^ث/i, /^ر/i, /^خ/i, /^ج/i, /^س/i],
    wide: [
      /^الأحد/i,
      /^الاثنين/i,
      /^الثلاثاء/i,
      /^الأربعاء/i,
      /^الخميس/i,
      /^الجمعة/i,
      /^السبت/i
    ],
    any: [/^أح/i, /^اث/i, /^ث/i, /^أر/i, /^خ/i, /^ج/i, /^س/i]
  };
  var matchDayPeriodPatterns2 = {
    narrow: /^(ص|م|منتصف الليل|الظهر|بعد الظهر|في الصباح|في المساء|في الليل)/,
    any: /^(ص|م|منتصف الليل|الظهر|بعد الظهر|في الصباح|في المساء|في الليل)/
  };
  var parseDayPeriodPatterns2 = {
    any: {
      am: /^ص/,
      pm: /^م/,
      midnight: /منتصف الليل/,
      noon: /الظهر/,
      afternoon: /بعد الظهر/,
      morning: /في الصباح/,
      evening: /في المساء/,
      night: /في الليل/
    }
  };
  var match2 = {
    ordinalNumber: buildMatchPatternFn({
      matchPattern: matchOrdinalNumberPattern2,
      parsePattern: parseOrdinalNumberPattern2,
      valueCallback: (value) => parseInt(value, 10)
    }),
    era: buildMatchFn({
      matchPatterns: matchEraPatterns2,
      defaultMatchWidth: "wide",
      parsePatterns: parseEraPatterns2,
      defaultParseWidth: "any"
    }),
    quarter: buildMatchFn({
      matchPatterns: matchQuarterPatterns2,
      defaultMatchWidth: "wide",
      parsePatterns: parseQuarterPatterns2,
      defaultParseWidth: "any",
      valueCallback: (index2) => index2 + 1
    }),
    month: buildMatchFn({
      matchPatterns: matchMonthPatterns2,
      defaultMatchWidth: "wide",
      parsePatterns: parseMonthPatterns2,
      defaultParseWidth: "any"
    }),
    day: buildMatchFn({
      matchPatterns: matchDayPatterns2,
      defaultMatchWidth: "wide",
      parsePatterns: parseDayPatterns2,
      defaultParseWidth: "any"
    }),
    dayPeriod: buildMatchFn({
      matchPatterns: matchDayPeriodPatterns2,
      defaultMatchWidth: "any",
      parsePatterns: parseDayPeriodPatterns2,
      defaultParseWidth: "any"
    })
  };

  // node_modules/date-fns/locale/ar.js
  var ar = {
    code: "ar",
    formatDistance: formatDistance3,
    formatLong: formatLong2,
    formatRelative: formatRelative2,
    localize: localize2,
    match: match2,
    options: {
      weekStartsOn: 6,
      firstWeekContainsDate: 1
    }
  };

  // src/lib/hooks/shared/useDate.ts
  var DateFormats = /* @__PURE__ */ ((DateFormats2) => {
    DateFormats2["YEAR_MONTH_DAY"] = "yyyy/MM/dd";
    DateFormats2["SHORT_MONTH_DAY_YEAR"] = "MMM dd, yyyy";
    DateFormats2["FULL_MONTH_DAY_YEAR"] = "MMMM d, yyyy";
    DateFormats2["ISO_DATE"] = "yyyy-MM-dd";
    DateFormats2["ISO_DATETIME"] = "yyyy-MM-dd'T'HH:mm:ss.SSSxxx";
    DateFormats2["US_DATE"] = "MM/dd/yyyy";
    DateFormats2["EU_DATE"] = "dd/MM/yyyy";
    DateFormats2["FULL_MONTH_YEAR"] = "MMMM yyyy";
    DateFormats2["SHORT_MONTH_YEAR"] = "MMM yyyy";
    DateFormats2["YEAR"] = "yyyy";
    DateFormats2["SHORT_MONTH_DAY_YEAR_TIME"] = "MMM dd, yyyy h:mm a";
    DateFormats2["DAY_MONTH_DATE_TIME"] = "EEEE, MMM dd h:mm a";
    DateFormats2["FULL_WEEKDAY_SHORT_MONTH_DAY_YEAR"] = "EEEE, MMMM d, yyyy";
    DateFormats2["FULL_DAY_MONTH_DATE"] = "EEEE, MMM dd";
    DateFormats2["FULL_DAY_MONTH_DATE_YEAR_TIME"] = "EEEE, MMM dd, yyyy h:mm a";
    DateFormats2["FULL_DAY"] = "EEEE";
    DateFormats2["SHORT_DAY"] = "EEE";
    DateFormats2["DAY_OF_MONTH"] = "d";
    DateFormats2["DAY_OF_MONTH_PADDED"] = "dd";
    DateFormats2["FULL_MONTH"] = "MMMM";
    DateFormats2["SHORT_MONTH"] = "MMM";
    DateFormats2["DAY_SHORT_MONTH"] = "EEE, MMM d";
    return DateFormats2;
  })(DateFormats || {});
  var TimeFormats = /* @__PURE__ */ ((TimeFormats2) => {
    TimeFormats2["HOUR_MINUTES_12"] = "h:mm a";
    TimeFormats2["HOUR_MINUTES_SECONDS_12"] = "h:mm:ss a";
    TimeFormats2["HOUR_MINUTES_24"] = "HH:mm";
    TimeFormats2["HOUR_MINUTES_SECONDS_24"] = "HH:mm:ss";
    TimeFormats2["MINUTES_SECONDS"] = "mm:ss";
    return TimeFormats2;
  })(TimeFormats || {});
  var UnitOfTime = /* @__PURE__ */ ((UnitOfTime2) => {
    UnitOfTime2["YEAR"] = "year";
    UnitOfTime2["MONTH"] = "month";
    UnitOfTime2["WEEK"] = "week";
    UnitOfTime2["DAY"] = "day";
    UnitOfTime2["HOURS"] = "hours";
    UnitOfTime2["MINUTES"] = "minutes";
    UnitOfTime2["SECONDS"] = "seconds";
    UnitOfTime2["MILLISECONDS"] = "milliseconds";
    return UnitOfTime2;
  })(UnitOfTime || {});
  var DATE_FORMATS = DateFormats;
  var TIME_FORMATS = TimeFormats;
  var toNativeDate = (input) => {
    if (input instanceof Date) return input;
    if (typeof input === "string" && /^\d{4}-\d{2}-\d{2}$/.test(input)) {
      const [year, month, day] = input.split("-").map(Number);
      return new Date(year, month - 1, day);
    }
    return new Date(input || /* @__PURE__ */ new Date());
  };
  var useDate = () => {
    const { i18n } = useTranslation();
    const locale = i18n.language === "ar" /* AR */ ? ar : enUS;
    const format3 = (date, pattern) => toLatinDigits(format(date, pattern, { locale }));
    const formatDistanceToNow2 = (date, options3) => toLatinDigits(formatDistanceToNow(date, { ...options3, locale }));
    const toDate2 = (input = /* @__PURE__ */ new Date(), inputFormat) => {
      const date = toNativeDate(input);
      return format3(date, inputFormat || DATE_FORMATS.ISO_DATE);
    };
    const toBoundaryDate = (input = /* @__PURE__ */ new Date()) => toNativeDate(input);
    const toDateFromParts = (year, month, day) => {
      const date = new Date(year, month, day);
      if (!isValid(date)) throw new Error("useDate: Invalid date parts provided");
      return toDate2(date);
    };
    const formatDate = (input, dateFormat) => {
      const date = toNativeDate(input);
      if (!isValid(date)) throw new Error("useDate: Invalid date input");
      return format3(date, dateFormat);
    };
    const formatHour = (hour, formatPattern) => {
      const date = new Date(2e3, 0, 1, hour, 0);
      return format3(date, formatPattern);
    };
    const formatNullableDate = (input, dateFormat, fallback = "-") => {
      if (!input) return fallback;
      const date = toNativeDate(input);
      if (!isValid(date)) return fallback;
      return format3(date, dateFormat);
    };
    const toFormat = (value, fromPattern, toPattern, fallback = "-") => {
      if (!value) return fallback;
      const parsed = parse(value, fromPattern, /* @__PURE__ */ new Date());
      return isValid(parsed) ? format3(parsed, toPattern) : fallback;
    };
    const formatNullableTime = (input, timeFormat, fallback = "-") => {
      if (!input) return fallback;
      const parsed = parse(input, TIME_FORMATS.HOUR_MINUTES_SECONDS_24, /* @__PURE__ */ new Date());
      return isValid(parsed) ? format3(parsed, timeFormat) : fallback;
    };
    const addDuration = (input, amount, unit) => {
      const date = toNativeDate(input);
      if (!isValid(date)) throw new Error("useDate: Invalid date input");
      const unitToKey = {
        year: "years",
        month: "months",
        week: "weeks",
        day: "days",
        hours: "hours",
        minutes: "minutes",
        seconds: "seconds",
        milliseconds: "milliseconds"
      };
      const duration = {};
      duration[unitToKey[unit] || unit] = amount;
      return add(date, duration).toISOString();
    };
    const subtractDuration = (input, amount, unit) => {
      const date = toNativeDate(input);
      if (!isValid(date)) throw new Error("useDate: Invalid date input");
      const unitToKey = {
        year: "years",
        month: "months",
        week: "weeks",
        day: "days",
        hours: "hours",
        minutes: "minutes",
        seconds: "seconds",
        milliseconds: "milliseconds"
      };
      const duration = {};
      duration[unitToKey[unit] || unit] = amount;
      return sub(date, duration).toISOString();
    };
    const getDifference = (start, end, unit = "day" /* DAY */) => {
      const startDate = toNativeDate(start);
      const endDate = toNativeDate(end);
      switch (unit) {
        case "year":
          return differenceInYears(endDate, startDate);
        case "month":
          return differenceInMonths(endDate, startDate);
        case "day":
          return differenceInDays(endDate, startDate);
        case "hours":
          return differenceInHours(endDate, startDate);
        case "minutes":
          return differenceInMinutes(endDate, startDate);
        case "seconds":
          return differenceInSeconds(endDate, startDate);
        case "milliseconds":
          return differenceInMilliseconds(endDate, startDate);
        default:
          return differenceInDays(endDate, startDate);
      }
    };
    const getUTC = (date) => {
      const d = toNativeDate(date);
      return d.toISOString();
    };
    const getTimeAgo = (date) => {
      const d = toNativeDate(date);
      return formatDistanceToNow2(d, { addSuffix: true });
    };
    const toDurationUnits = (value, unit = "seconds" /* SECONDS */) => {
      let totalMs = value;
      switch (unit) {
        case "seconds":
          totalMs = value * 1e3;
          break;
        case "minutes":
          totalMs = value * 60 * 1e3;
          break;
        case "hours":
          totalMs = value * 60 * 60 * 1e3;
          break;
        case "day":
          totalMs = value * 24 * 60 * 60 * 1e3;
          break;
        case "week":
          totalMs = value * 7 * 24 * 60 * 60 * 1e3;
          break;
        case "month":
          totalMs = value * 30 * 24 * 60 * 60 * 1e3;
          break;
        case "year":
          totalMs = value * 365 * 24 * 60 * 60 * 1e3;
          break;
      }
      const years = Math.floor(totalMs / (365 * 24 * 60 * 60 * 1e3));
      const months = Math.floor(totalMs % (365 * 24 * 60 * 60 * 1e3) / (30 * 24 * 60 * 60 * 1e3));
      const days = Math.floor(totalMs % (30 * 24 * 60 * 60 * 1e3) / (24 * 60 * 60 * 1e3));
      const weeks = Math.floor(days / 7);
      const hours = Math.floor(totalMs % (24 * 60 * 60 * 1e3) / (60 * 60 * 1e3));
      const minutes = Math.floor(totalMs % (60 * 60 * 1e3) / (60 * 1e3));
      const seconds = Math.floor(totalMs % (60 * 1e3) / 1e3);
      const milliseconds = Math.floor(totalMs % 1e3);
      return {
        years,
        months,
        weeks,
        days: days % 7,
        hours,
        minutes,
        seconds,
        milliseconds
      };
    };
    const toDuration = (value, unit = "seconds" /* SECONDS */) => {
      const multipliers = {
        ["milliseconds" /* MILLISECONDS */]: 1,
        ["seconds" /* SECONDS */]: 1e3,
        ["minutes" /* MINUTES */]: 6e4,
        ["hours" /* HOURS */]: 36e5,
        ["day" /* DAY */]: 864e5,
        ["week" /* WEEK */]: 6048e5,
        ["month" /* MONTH */]: 2629746e3,
        ["year" /* YEAR */]: 31556952e3
      };
      return value * (multipliers[unit] || 1e3);
    };
    const toFormattedDuration = (value, timeFormat, unit = "seconds" /* SECONDS */) => {
      const totalMs = toDuration(value, unit);
      const date = new Date(totalMs);
      return format3(date, timeFormat);
    };
    const toDateUnits = (date = /* @__PURE__ */ new Date()) => {
      const d = toNativeDate(date);
      return {
        years: getYear(d),
        months: getMonth(d),
        // 0-based in date-fns
        weeks: getWeek(d),
        days: getDay(d),
        // Day of week (0-6)
        hours: getHours(d),
        minutes: getMinutes(d),
        seconds: getSeconds(d),
        milliseconds: getMilliseconds(d)
      };
    };
    const toStartAndEndOf = (date, unitOfTime = "month" /* MONTH */) => {
      const inputDate = toNativeDate(date);
      let startOf;
      let endOf;
      switch (unitOfTime) {
        case "week":
          startOf = startOfWeek(inputDate);
          endOf = endOfWeek(inputDate);
          break;
        case "month":
          startOf = startOfMonth(inputDate);
          endOf = endOfMonth(inputDate);
          break;
        case "year":
          startOf = startOfYear(inputDate);
          endOf = endOfYear(inputDate);
          break;
        default:
          startOf = startOfMonth(inputDate);
          endOf = endOfMonth(inputDate);
      }
      return {
        startOf: format3(startOf, DATE_FORMATS.ISO_DATE),
        endOf: format3(endOf, DATE_FORMATS.ISO_DATE)
      };
    };
    const getMonthGrid = (year, month, weekStartsOn = 0) => {
      const firstOfMonth = new Date(year, month, 1);
      if (!isValid(firstOfMonth)) throw new Error("useDate: Invalid year/month for getMonthGrid");
      const start = startOfWeek(startOfMonth(firstOfMonth), { weekStartsOn });
      const end = endOfWeek(endOfMonth(firstOfMonth), { weekStartsOn });
      return eachDayOfInterval({ start, end }).map((d) => format3(d, DATE_FORMATS.ISO_DATE));
    };
    const isBeforeDate = (date, comparison) => {
      const d1 = toNativeDate(date);
      const d2 = toNativeDate(comparison);
      return isBefore(d1, d2);
    };
    const isAfterDate = (date, comparison) => {
      const d1 = toNativeDate(date);
      const d2 = toNativeDate(comparison);
      return isAfter(d1, d2);
    };
    const isBetween = (input, start, end) => {
      const inputDate = toNativeDate(input);
      const startDate = toNativeDate(start);
      const endDate = toNativeDate(end);
      return isWithinInterval(inputDate, { start: startDate, end: endDate });
    };
    const isValidDate = (input) => {
      if (!input) return false;
      const date = toNativeDate(input);
      return isValid(date);
    };
    const parseDate = (dateString, dateFormat) => {
      if (!dateString) return void 0;
      const normalizedString = dateString.replace(/[-/]/g, "/");
      const normalizedFormat = dateFormat.replace(/[-/]/g, "/");
      const parsed = parse(normalizedString, normalizedFormat, /* @__PURE__ */ new Date());
      if (isValid(parsed)) {
        const year = parsed.getFullYear();
        if (year >= 1900 && year <= 2100) {
          return parsed;
        }
      }
      return void 0;
    };
    const getDateRangeArray = (start, end, dateFormat = DATE_FORMATS.ISO_DATE) => {
      const startDate = toNativeDate(start);
      const endDate = toNativeDate(end);
      if (!isValid(startDate)) throw new Error("Invalid start date");
      if (!isValid(endDate)) throw new Error("Invalid end date");
      if (isAfter(startDate, endDate)) throw new Error("Start date must be before or equal to end date");
      const dateArray = eachDayOfInterval({ start: startDate, end: endDate });
      return dateArray.map((date) => format3(date, dateFormat));
    };
    const yearRanges = (currentYear, range = 10) => {
      const years = [];
      for (let y = currentYear - range; y <= currentYear + range; y++) years.push(y);
      return years;
    };
    return {
      toDate: toDate2,
      toBoundaryDate,
      toDateFromParts,
      formatDate,
      formatHour,
      formatNullableDate,
      formatNullableTime,
      toFormat,
      parseDate,
      addDuration,
      subtractDuration,
      getDifference,
      getUTC,
      toDurationUnits,
      toDuration,
      toFormattedDuration,
      toDateUnits,
      toStartAndEndOf,
      getMonthGrid,
      isBefore: isBeforeDate,
      isAfter: isAfterDate,
      isBetween,
      getTimeAgo,
      isValid: isValidDate,
      getDateRangeArray,
      yearRanges,
      constants: {
        DateFormats,
        TimeFormats,
        UnitOfTime
      }
    };
  };

  // src/lib/hooks/shared/useDateMask.ts
  init_define_import_meta_env();
  var import_react15 = __toESM(require_react_shim(), 1);
  var useDateFunctionsRef = () => {
    const dateUtils = useDate();
    const ref = (0, import_react15.useRef)(dateUtils);
    ref.current = dateUtils;
    return ref;
  };
  var DATE_INPUT_FORMATS = {
    US_SLASH: "MM/dd/yyyy",
    US_DASH: "MM-dd-yyyy",
    EU_SLASH: "dd/MM/yyyy",
    EU_DASH: "dd-MM-yyyy"
  };
  var getSeparator = (dateFormat) => {
    return dateFormat.includes("/") ? "/" : "-";
  };
  var getFormatOrder = (dateFormat) => {
    return dateFormat.startsWith("MM") ? "MDY" : "DMY";
  };
  var applyMask = (value, dateFormat) => {
    const separator = getSeparator(dateFormat);
    const digitsOnly = value.replace(/\D/g, "");
    let result = "";
    for (let i = 0; i < digitsOnly.length && i < 8; i++) {
      if (i === 2 || i === 4) {
        result += separator;
      }
      result += digitsOnly[i];
    }
    return result;
  };
  var getPlaceholder = (dateFormat) => {
    const separator = getSeparator(dateFormat);
    const order2 = getFormatOrder(dateFormat);
    if (order2 === "MDY") {
      return `MM${separator}DD${separator}YYYY`;
    }
    return `DD${separator}MM${separator}YYYY`;
  };
  var getDatePartFromCursor = (cursorPos) => {
    if (cursorPos <= 2) return "first";
    if (cursorPos <= 5) return "second";
    return "year";
  };
  var getDatePartSelectionRange = (part) => {
    switch (part) {
      case "first":
        return [0, 2];
      case "second":
        return [3, 5];
      case "year":
        return [6, 10];
    }
  };
  var useDateMask = (options3 = {}) => {
    const { dateFormat = DATE_INPUT_FORMATS.US_SLASH, onChange } = options3;
    const dateUtilsRef = useDateFunctionsRef();
    const [inputValue, setInputValueInternal] = (0, import_react15.useState)("");
    const inputRef = (0, import_react15.useRef)(null);
    const pendingSelectionRef = (0, import_react15.useRef)(null);
    const onChangeRef = (0, import_react15.useRef)(onChange);
    onChangeRef.current = onChange;
    const separator = (0, import_react15.useMemo)(() => getSeparator(dateFormat), [dateFormat]);
    const placeholder = (0, import_react15.useMemo)(() => getPlaceholder(dateFormat), [dateFormat]);
    const formatOrder = (0, import_react15.useMemo)(() => getFormatOrder(dateFormat), [dateFormat]);
    const formatDateToString = (0, import_react15.useCallback)(
      (date) => {
        return dateUtilsRef.current.formatNullableDate(date, dateFormat, "");
      },
      [dateFormat, dateUtilsRef]
    );
    const parseDateString = (0, import_react15.useCallback)(
      (value) => {
        if (!value) return void 0;
        return dateUtilsRef.current.parseDate(value, dateFormat);
      },
      [dateFormat, dateUtilsRef]
    );
    const parsedDate = (0, import_react15.useMemo)(() => {
      return parseDateString(inputValue);
    }, [inputValue, parseDateString]);
    const isValidInput = (0, import_react15.useMemo)(() => {
      if (!inputValue) return true;
      return parsedDate !== void 0;
    }, [inputValue, parsedDate]);
    (0, import_react15.useEffect)(() => {
      if (pendingSelectionRef.current && inputRef.current) {
        const [start, end] = pendingSelectionRef.current;
        requestAnimationFrame(() => {
          if (inputRef.current) {
            inputRef.current.setSelectionRange(start, end);
          }
          pendingSelectionRef.current = null;
        });
      }
    }, [inputValue]);
    const setInputValue = (0, import_react15.useCallback)((value) => {
      setInputValueInternal(value);
    }, []);
    const setFromDate = (0, import_react15.useCallback)(
      (date) => {
        const formatted = dateUtilsRef.current.formatNullableDate(date, dateFormat, "");
        setInputValueInternal(formatted);
      },
      [dateFormat, dateUtilsRef]
    );
    const handleChange = (0, import_react15.useCallback)(
      (e2) => {
        const rawValue = e2.target.value;
        const maskedValue = applyMask(rawValue, dateFormat);
        inputRef.current = e2.target;
        setInputValueInternal(maskedValue);
        const parsed = dateUtilsRef.current.parseDate(maskedValue, dateFormat);
        onChangeRef.current?.(parsed);
      },
      [dateFormat, dateUtilsRef]
    );
    const handleKeyDown = (0, import_react15.useCallback)(
      (e2) => {
        const target = e2.target;
        inputRef.current = target;
        const cursorPos = target.selectionStart || 0;
        if (e2.key === "Enter") {
          e2.preventDefault();
          target.blur();
          return;
        }
        if (e2.key === "Backspace") {
          if (cursorPos > 0 && inputValue[cursorPos - 1] === separator) {
            e2.preventDefault();
            const newValue = inputValue.slice(0, cursorPos - 2) + inputValue.slice(cursorPos);
            const maskedValue = applyMask(newValue, dateFormat);
            pendingSelectionRef.current = [cursorPos - 2, cursorPos - 2];
            setInputValueInternal(maskedValue);
            const parsed = dateUtilsRef.current.parseDate(maskedValue, dateFormat);
            onChangeRef.current?.(parsed);
          }
          return;
        }
        if (e2.key === "ArrowUp" || e2.key === "ArrowDown") {
          e2.preventDefault();
          if (!parsedDate) {
            return;
          }
          const part = getDatePartFromCursor(cursorPos);
          const direction = e2.key === "ArrowUp" ? "up" : "down";
          const { addDuration, subtractDuration, constants } = dateUtilsRef.current;
          const durationFn = direction === "up" ? addDuration : subtractDuration;
          let newDate;
          if (part === "year") {
            newDate = new Date(durationFn(parsedDate, 1, constants.UnitOfTime.YEAR));
          } else if (formatOrder === "MDY") {
            if (part === "first") {
              newDate = new Date(durationFn(parsedDate, 1, constants.UnitOfTime.MONTH));
            } else {
              newDate = new Date(durationFn(parsedDate, 1, constants.UnitOfTime.DAY));
            }
          } else {
            if (part === "first") {
              newDate = new Date(durationFn(parsedDate, 1, constants.UnitOfTime.DAY));
            } else {
              newDate = new Date(durationFn(parsedDate, 1, constants.UnitOfTime.MONTH));
            }
          }
          if (newDate.getFullYear() >= 1900 && newDate.getFullYear() <= 2100) {
            const newValue = dateUtilsRef.current.formatNullableDate(newDate, dateFormat, "");
            const [selStart, selEnd] = getDatePartSelectionRange(part);
            pendingSelectionRef.current = [selStart, selEnd];
            setInputValueInternal(newValue);
            onChangeRef.current?.(newDate);
          }
        }
      },
      [inputValue, separator, dateFormat, parsedDate, formatOrder, dateUtilsRef]
    );
    const handlePaste = (0, import_react15.useCallback)(
      (e2) => {
        e2.preventDefault();
        const pastedText = e2.clipboardData.getData("text");
        const maskedValue = applyMask(pastedText, dateFormat);
        setInputValueInternal(maskedValue);
        const parsed = dateUtilsRef.current.parseDate(maskedValue, dateFormat);
        onChangeRef.current?.(parsed);
      },
      [dateFormat, dateUtilsRef]
    );
    const handleBlur = (0, import_react15.useCallback)(() => {
      if (inputValue && !parsedDate) {
        setInputValueInternal("");
        onChangeRef.current?.(void 0);
      }
    }, [inputValue, parsedDate]);
    return {
      inputValue,
      setInputValue,
      handleChange,
      handleKeyDown,
      handlePaste,
      handleBlur,
      setFromDate,
      parsedDate,
      isValidInput,
      placeholder
    };
  };
  var useDateRangeMask = (options3 = {}) => {
    const { dateFormat = DATE_INPUT_FORMATS.US_SLASH, onChange } = options3;
    const dateUtilsRef = useDateFunctionsRef();
    const [inputValue, setInputValueInternal] = (0, import_react15.useState)("");
    const inputRef = (0, import_react15.useRef)(null);
    const pendingSelectionRef = (0, import_react15.useRef)(null);
    const onChangeRef = (0, import_react15.useRef)(onChange);
    onChangeRef.current = onChange;
    const separator = (0, import_react15.useMemo)(() => getSeparator(dateFormat), [dateFormat]);
    const placeholder = (0, import_react15.useMemo)(() => {
      const singlePlaceholder = getPlaceholder(dateFormat);
      return `${singlePlaceholder} - ${singlePlaceholder}`;
    }, [dateFormat]);
    const formatOrder = (0, import_react15.useMemo)(() => getFormatOrder(dateFormat), [dateFormat]);
    const RANGE_SEPARATOR = " - ";
    const SINGLE_DATE_LENGTH = 10;
    const parsedRange = (0, import_react15.useMemo)(() => {
      if (!inputValue) return { from: void 0, to: void 0 };
      const parts = inputValue.split(RANGE_SEPARATOR);
      const fromStr = parts[0]?.trim() || "";
      const toStr = parts[1]?.trim() || "";
      return {
        from: dateUtilsRef.current.parseDate(fromStr, dateFormat),
        to: dateUtilsRef.current.parseDate(toStr, dateFormat)
      };
    }, [inputValue, dateFormat, dateUtilsRef]);
    const isValidInput = (0, import_react15.useMemo)(() => {
      if (!inputValue) return true;
      const parts = inputValue.split(RANGE_SEPARATOR);
      const fromStr = parts[0]?.trim() || "";
      const toStr = parts[1]?.trim() || "";
      const fromValid = !fromStr || fromStr.length < SINGLE_DATE_LENGTH || parsedRange.from !== void 0;
      const toValid = !toStr || toStr.length < SINGLE_DATE_LENGTH || parsedRange.to !== void 0;
      return fromValid && toValid;
    }, [inputValue, parsedRange]);
    (0, import_react15.useEffect)(() => {
      if (pendingSelectionRef.current && inputRef.current) {
        const [start, end] = pendingSelectionRef.current;
        requestAnimationFrame(() => {
          if (inputRef.current) {
            inputRef.current.setSelectionRange(start, end);
          }
          pendingSelectionRef.current = null;
        });
      }
    }, [inputValue]);
    const setFromDateRange = (0, import_react15.useCallback)(
      (range) => {
        if (!range) {
          setInputValueInternal("");
          return;
        }
        const fromStr = dateUtilsRef.current.formatNullableDate(range.from, dateFormat, "");
        const toStr = dateUtilsRef.current.formatNullableDate(range.to, dateFormat, "");
        if (fromStr && toStr) {
          setInputValueInternal(`${fromStr}${RANGE_SEPARATOR}${toStr}`);
        } else if (fromStr) {
          setInputValueInternal(fromStr);
        } else if (toStr) {
          setInputValueInternal(`${RANGE_SEPARATOR}${toStr}`);
        } else {
          setInputValueInternal("");
        }
      },
      [dateFormat, dateUtilsRef]
    );
    const handleChange = (0, import_react15.useCallback)(
      (e2) => {
        const rawValue = e2.target.value;
        inputRef.current = e2.target;
        const rangeSepIndex = rawValue.indexOf(" - ");
        const altRangeSepIndex = rawValue.search(/\s+-\s+/);
        const sepIndex = rangeSepIndex !== -1 ? rangeSepIndex : altRangeSepIndex;
        let fromPart;
        let toPart;
        let hasSeparator;
        if (sepIndex !== -1) {
          fromPart = rawValue.slice(0, sepIndex);
          const afterSep = rawValue.slice(sepIndex).replace(/^\s*-\s*/, "");
          toPart = afterSep;
          hasSeparator = true;
        } else {
          fromPart = rawValue;
          toPart = "";
          hasSeparator = false;
        }
        const maskedFrom = applyMask(fromPart, dateFormat);
        const maskedTo = applyMask(toPart, dateFormat);
        let result = maskedFrom;
        if (hasSeparator) {
          result = `${maskedFrom}${RANGE_SEPARATOR}${maskedTo}`;
        } else if (maskedFrom.length === SINGLE_DATE_LENGTH) {
          const digitsAfterComplete = fromPart.slice(SINGLE_DATE_LENGTH).replace(/\D/g, "");
          if (digitsAfterComplete.length > 0) {
            const maskedExtra = applyMask(digitsAfterComplete, dateFormat);
            result = `${maskedFrom}${RANGE_SEPARATOR}${maskedExtra}`;
          }
        }
        setInputValueInternal(result);
        const fromDate = dateUtilsRef.current.parseDate(maskedFrom, dateFormat);
        const toDate2 = dateUtilsRef.current.parseDate(maskedTo, dateFormat);
        onChangeRef.current?.({ from: fromDate, to: toDate2 });
      },
      [dateFormat, dateUtilsRef]
    );
    const handleKeyDown = (0, import_react15.useCallback)(
      (e2) => {
        const target = e2.target;
        inputRef.current = target;
        const cursorPos = target.selectionStart || 0;
        if (e2.key === "Enter") {
          e2.preventDefault();
          target.blur();
          return;
        }
        if (e2.key === "Backspace") {
          if (cursorPos > 0 && inputValue[cursorPos - 1] === separator) {
            e2.preventDefault();
            const newValue = inputValue.slice(0, cursorPos - 2) + inputValue.slice(cursorPos);
            pendingSelectionRef.current = [cursorPos - 2, cursorPos - 2];
            setInputValueInternal(newValue);
          }
          const separatorIndex = inputValue.indexOf(RANGE_SEPARATOR);
          if (separatorIndex !== -1 && cursorPos > separatorIndex && cursorPos <= separatorIndex + RANGE_SEPARATOR.length) {
            e2.preventDefault();
            const newValue = inputValue.slice(0, separatorIndex);
            pendingSelectionRef.current = [separatorIndex, separatorIndex];
            setInputValueInternal(newValue);
          }
          return;
        }
        if (e2.key === "ArrowUp" || e2.key === "ArrowDown") {
          e2.preventDefault();
          const rangeSeparatorIndex = inputValue.indexOf(RANGE_SEPARATOR);
          const toDateStartIndex = rangeSeparatorIndex !== -1 ? rangeSeparatorIndex + RANGE_SEPARATOR.length : -1;
          const isInToDate = toDateStartIndex !== -1 && cursorPos >= toDateStartIndex;
          const localCursorPos = isInToDate ? cursorPos - toDateStartIndex : cursorPos;
          const part = getDatePartFromCursor(localCursorPos);
          const direction = e2.key === "ArrowUp" ? "up" : "down";
          const { addDuration, subtractDuration, constants } = dateUtilsRef.current;
          const durationFn = direction === "up" ? addDuration : subtractDuration;
          const calculateNewDate = (currentDate) => {
            if (part === "year") {
              return new Date(durationFn(currentDate, 1, constants.UnitOfTime.YEAR));
            }
            if (formatOrder === "MDY") {
              if (part === "first") {
                return new Date(durationFn(currentDate, 1, constants.UnitOfTime.MONTH));
              }
              return new Date(durationFn(currentDate, 1, constants.UnitOfTime.DAY));
            }
            if (part === "first") {
              return new Date(durationFn(currentDate, 1, constants.UnitOfTime.DAY));
            }
            return new Date(durationFn(currentDate, 1, constants.UnitOfTime.MONTH));
          };
          if (isInToDate) {
            if (!parsedRange.to) {
              return;
            }
            const newDate = calculateNewDate(parsedRange.to);
            if (newDate.getFullYear() >= 1900 && newDate.getFullYear() <= 2100) {
              const fromStr = inputValue.slice(0, rangeSeparatorIndex);
              const newToStr = dateUtilsRef.current.formatNullableDate(newDate, dateFormat, "");
              const newValue = `${fromStr}${RANGE_SEPARATOR}${newToStr}`;
              const [selStart, selEnd] = getDatePartSelectionRange(part);
              pendingSelectionRef.current = [toDateStartIndex + selStart, toDateStartIndex + selEnd];
              setInputValueInternal(newValue);
              onChangeRef.current?.({ from: parsedRange.from, to: newDate });
            }
          } else {
            if (!parsedRange.from) {
              return;
            }
            const newDate = calculateNewDate(parsedRange.from);
            if (newDate.getFullYear() >= 1900 && newDate.getFullYear() <= 2100) {
              const newFromStr = dateUtilsRef.current.formatNullableDate(newDate, dateFormat, "");
              let newValue = newFromStr;
              if (rangeSeparatorIndex !== -1) {
                const toStr = inputValue.slice(toDateStartIndex);
                newValue = `${newFromStr}${RANGE_SEPARATOR}${toStr}`;
              }
              const [selStart, selEnd] = getDatePartSelectionRange(part);
              pendingSelectionRef.current = [selStart, selEnd];
              setInputValueInternal(newValue);
              onChangeRef.current?.({ from: newDate, to: parsedRange.to });
            }
          }
        }
      },
      [inputValue, separator, dateFormat, parsedRange, formatOrder, dateUtilsRef]
    );
    const handlePaste = (0, import_react15.useCallback)(
      (e2) => {
        e2.preventDefault();
        const pastedText = e2.clipboardData.getData("text");
        const parts = pastedText.split(/\s*[-–—]\s*/);
        if (parts.length >= 2) {
          const maskedFrom = applyMask(parts[0], dateFormat);
          const maskedTo = applyMask(parts[1], dateFormat);
          setInputValueInternal(`${maskedFrom}${RANGE_SEPARATOR}${maskedTo}`);
        } else {
          const maskedValue = applyMask(pastedText, dateFormat);
          setInputValueInternal(maskedValue);
        }
      },
      [dateFormat]
    );
    const handleBlur = (0, import_react15.useCallback)(() => {
      const parts = inputValue.split(RANGE_SEPARATOR);
      const fromStr = parts[0]?.trim() || "";
      const toStr = parts[1]?.trim() || "";
      const fromValid = !fromStr || dateUtilsRef.current.parseDate(fromStr, dateFormat) !== void 0;
      const toValid = !toStr || dateUtilsRef.current.parseDate(toStr, dateFormat) !== void 0;
      if (fromStr && !fromValid) {
        if (toStr && toValid) {
          setInputValueInternal(`${RANGE_SEPARATOR}${toStr}`);
        } else {
          setInputValueInternal("");
        }
      } else if (toStr && !toValid) {
        setInputValueInternal(fromStr);
      }
    }, [inputValue, dateFormat, dateUtilsRef]);
    return {
      inputValue,
      setInputValue: setInputValueInternal,
      handleChange,
      handleKeyDown,
      handlePaste,
      handleBlur,
      setFromDateRange,
      parsedRange,
      isValidInput,
      placeholder
    };
  };

  // src/lib/hooks/shared/useInfiniteScroll.ts
  init_define_import_meta_env();
  var import_react16 = __toESM(require_react_shim(), 1);
  function useInfiniteScroll({
    infiniteQuery,
    enabled = true,
    threshold = 100
  }) {
    const handleScroll2 = (0, import_react16.useCallback)(
      (event) => {
        if (!enabled) return;
        const listNode = event.currentTarget;
        const scrollBottom = listNode.scrollHeight - listNode.scrollTop - listNode.clientHeight;
        if (scrollBottom < threshold && infiniteQuery.hasNextPage && !infiniteQuery.isFetchingNextPage) {
          infiniteQuery.fetchNextPage();
        }
      },
      [enabled, threshold, infiniteQuery.hasNextPage, infiniteQuery.isFetchingNextPage, infiniteQuery.fetchNextPage]
    );
    return {
      handleScroll: handleScroll2
    };
  }

  // src/lib/hooks/shared/useAsyncOptionsInfiniteQuery.ts
  init_define_import_meta_env();
  function useAsyncOptionsInfiniteQuery({
    fetchOptions,
    queryKey,
    searchTerm = "",
    selectedItemsIds = [],
    urlSearchParams,
    pageSize = 20,
    enabled = true
  }) {
    return useInfiniteQuery({
      queryKey: [queryKey, searchTerm, urlSearchParams?.toString()],
      queryFn: async ({ pageParam = 1 }) => {
        const queryParams = new URLSearchParams();
        if (urlSearchParams) {
          const baseParams = typeof urlSearchParams === "string" ? new URLSearchParams(urlSearchParams) : urlSearchParams;
          for (const [key, val] of baseParams.entries()) {
            queryParams.append(key, val);
          }
        }
        queryParams.append("pageNumber", pageParam.toString());
        queryParams.append("pageSize", pageSize.toString());
        if (searchTerm) {
          queryParams.append("searchTerm", searchTerm);
        }
        selectedItemsIds.forEach((id) => {
          queryParams.append("selectedItemsIds", id);
        });
        const response = await fetchOptions(queryParams.toString());
        if (Array.isArray(response)) {
          return { data: response, pagination: { page: 1, pageSize: response.length, total: response.length, totalPages: 1 } };
        }
        return response;
      },
      getNextPageParam: (lastPage, pages) => {
        const totalLoaded = pages.length * pageSize;
        return totalLoaded < lastPage.pagination.total ? pages.length + 1 : void 0;
      },
      enabled,
      keepPreviousData: true,
      staleTime: 0,
      cacheTime: 0
    });
  }

  // src/lib/hooks/shared/useComboboxOptions.ts
  init_define_import_meta_env();
  var import_react17 = __toESM(require_react_shim(), 1);
  var PAGE_SIZE = 20;
  var DEFAULT_QUERY_KEY = "combobox-options";
  var toURLSearchParams = (input) => {
    if (!input) return void 0;
    if (input instanceof URLSearchParams || typeof input === "string") return input;
    const params = new URLSearchParams();
    Object.entries(input).forEach(([key, value]) => {
      if (value === void 0) return;
      if (Array.isArray(value)) value.forEach((v) => params.append(key, v));
      else params.append(key, value);
    });
    return params;
  };
  function useComboboxOptions({
    options: options3,
    open,
    queryKey = DEFAULT_QUERY_KEY,
    urlSearchParams,
    selectedItemsIds
  }) {
    const [search, setSearch] = (0, import_react17.useState)("");
    const debouncedSearch = useDebounce(search, 500);
    const isAsync = typeof options3 === "function";
    const infiniteQuery = useAsyncOptionsInfiniteQuery({
      queryKey,
      fetchOptions: isAsync ? options3 : () => Promise.resolve({ data: [], pagination: { page: 1, total: 0, totalPages: 0, pageSize: PAGE_SIZE } }),
      searchTerm: debouncedSearch,
      selectedItemsIds,
      urlSearchParams: toURLSearchParams(urlSearchParams),
      pageSize: PAGE_SIZE,
      enabled: isAsync && open
    });
    const { handleScroll: handleScroll2 } = useInfiniteScroll({ infiniteQuery, enabled: isAsync });
    const allOptions = isAsync ? infiniteQuery.data?.pages.flatMap((p2) => p2.data) ?? [] : options3;
    return {
      search,
      setSearch,
      allOptions,
      isAsync,
      onScroll: isAsync ? handleScroll2 : void 0,
      isLoading: isAsync && (infiniteQuery.isFetching || search !== debouncedSearch),
      isFetchingNextPage: isAsync && infiniteQuery.isFetchingNextPage
    };
  }

  // src/lib/hooks/shared/useTime.ts
  init_define_import_meta_env();
  var useTime = () => {
    const padN = (n) => n.toString().padStart(2, "0");
    const parseTimeString = (val) => {
      let [h, m2, am] = val.split(/\W/g);
      if (h === "12" && am !== void 0) h = "0";
      return [parseInt(h) + (am?.toLowerCase() === "pm" ? 12 : 0), parseInt(m2), am];
    };
    const minsToTimeString = (mins, ampm = true) => {
      const [h, m2] = [Math.floor(mins / 60), mins % 60];
      if (ampm) return `${padN(h === 12 ? 12 : h % 12)}:${padN(m2)} ${h >= 12 ? "PM" : "AM"}`;
      return `${padN(h)}:${padN(m2)}`;
    };
    const timeStringToMins = (timeStr) => {
      const [h, m2] = parseTimeString(timeStr);
      return h * 60 + m2;
    };
    const nullableTimeStringToMins = (timeStr, fallback = 0) => {
      if (!timeStr) return fallback;
      const [h, m2] = parseTimeString(timeStr);
      return h * 60 + m2;
    };
    const dateToTimeString = (date, ampm = true) => {
      const h = date.getHours();
      const m2 = date.getMinutes();
      return minsToTimeString(h * 60 + m2, ampm);
    };
    const addMinutesToTimeString = (timeString, minutes) => {
      const [h, m2] = parseTimeString(timeString);
      return minsToTimeString(h * 60 + m2 + minutes);
    };
    const getTimeOptions = (start, end, step, ampm = true) => {
      const [startH, startM] = parseTimeString(start);
      const [endH, endM] = parseTimeString(end);
      const options3 = [];
      if (startH * 60 + startM >= endH * 60 + endM) return options3;
      for (let mins = startH * 60 + startM; mins < endH * 60 + endM; mins += step) {
        options3.push(minsToTimeString(mins, ampm));
      }
      return options3;
    };
    const getNextTimeSlot = (timeString, step) => {
      let [h, m2] = parseTimeString(timeString);
      m2 = m2 + step;
      if (m2 > 60) {
        m2 -= 60;
        h++;
      }
      if (h > 23) h -= 24;
      return `${padN(h)}:${padN(m2)}`;
    };
    return {
      parseTimeString,
      minsToTimeString,
      addMinutesToTimeString,
      getTimeOptions,
      getNextTimeSlot,
      dateToTimeString,
      timeStringToMins,
      nullableTimeStringToMins
    };
  };

  // src/lib/hooks/shared/useAuth.ts
  init_define_import_meta_env();
  var useAuth = () => {
    return {
      currentUser: {
        name: "Mohammad Safia",
        email: "mohammadsafia17@gmail.com",
        picture: "",
        userId: "1"
      },
      removeCurrentUser: () => {
      },
      isAuthed: true
    };
  };

  // src/lib/hooks/shared/useDataTableQuery.ts
  init_define_import_meta_env();
  var import_react18 = __toESM(require_react_shim(), 1);

  // src/lib/hooks/shared/useAppTranslation.ts
  init_define_import_meta_env();
  var useAppTranslation = (namespace) => {
    const { t: t3, i18n, ready } = useTranslation(namespace ?? "common");
    const currentLanguage = i18n.language;
    const isRTL2 = currentLanguage === "ar" /* AR */;
    const direction = isRTL2 ? "rtl" : "ltr";
    const currentLanguageConfig = LANGUAGE_OPTIONS.find((lang) => lang.code === currentLanguage);
    const changeLanguage2 = (lng) => {
      void i18n.changeLanguage(lng);
    };
    return {
      t: t3,
      i18n,
      ready,
      currentLanguage,
      currentLanguageConfig,
      availableLanguages: LANGUAGE_OPTIONS,
      isRTL: isRTL2,
      direction,
      changeLanguage: changeLanguage2
    };
  };

  // src/lib/contexts/ThemeContext.tsx
  var THEME_TYPES = {
    SYSTEM: "system",
    LIGHT: "light",
    DARK: "dark"
  };
  var initialState = {
    theme: THEME_TYPES.DARK,
    setTheme: () => null
  };
  var DEFAULT_STORAGE_KEY = "vite-ui-theme";
  function ThemeProvider({ children, defaultTheme = THEME_TYPES.DARK, storageKey = DEFAULT_STORAGE_KEY, ...props }) {
    const [storageTheme, setStorageTheme] = useStorage(storageKey, defaultTheme);
    const [theme, setTheme] = (0, import_react19.useState)(storageTheme);
    const onSetTheme = (0, import_react19.useCallback)(
      (newTheme) => {
        setStorageTheme(newTheme);
        setTheme(newTheme);
      },
      [setStorageTheme]
    );
    (0, import_react19.useEffect)(() => {
      const root = window.document.documentElement;
      root.classList.remove(THEME_TYPES.LIGHT, THEME_TYPES.DARK);
      if (theme === THEME_TYPES.SYSTEM) {
        const systemTheme = window.matchMedia("(prefers-color-scheme: dark)").matches ? THEME_TYPES.DARK : THEME_TYPES.LIGHT;
        root.classList.add(systemTheme);
        return;
      }
      root.classList.add(theme);
    }, [theme]);
    const contextValue = {
      theme,
      setTheme: onSetTheme
    };
    return /* @__PURE__ */ React.createElement(ThemeContext.Provider, { ...props, value: contextValue }, children);
  }
  var ThemeContext = (0, import_react19.createContext)(initialState);
  var useTheme = () => {
    const context2 = (0, import_react19.useContext)(ThemeContext);
    if (context2 === void 0) throw new Error("useTheme must be used within a ThemeProvider");
    return context2;
  };

  // src/lib/contexts/CommandPaletteContext.tsx
  init_define_import_meta_env();
  var import_react20 = __toESM(require_react_shim(), 1);
  var CommandPaletteContext = (0, import_react20.createContext)(void 0);
  var useCommandPalette = () => {
    const context2 = (0, import_react20.useContext)(CommandPaletteContext);
    if (context2 === void 0) throw new Error("useCommandPalette must be used within a CommandPaletteProvider");
    return context2;
  };

  // src/lib/contexts/DirectionContext.tsx
  init_define_import_meta_env();

  // node_modules/@radix-ui/react-direction/dist/index.mjs
  init_define_import_meta_env();
  var React9 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime = __toESM(require_react_shim(), 1);
  var DirectionContext = React9.createContext(void 0);
  var DirectionProvider = (props) => {
    const { dir: dir2, children } = props;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DirectionContext.Provider, { value: dir2, children });
  };
  function useDirection(localDir) {
    const globalDir = React9.useContext(DirectionContext);
    return localDir || globalDir || "ltr";
  }

  // src/lib/contexts/DirectionContext.tsx
  function AppDirectionProvider({ children }) {
    const { direction } = useAppTranslation();
    return /* @__PURE__ */ React.createElement(DirectionProvider, { dir: direction }, children);
  }

  // node_modules/react-hook-form/dist/index.esm.mjs
  init_define_import_meta_env();
  var import_react21 = __toESM(require_react_shim(), 1);
  var isCheckBoxInput = (element) => element.type === "checkbox";
  var isDateObject = (value) => value instanceof Date;
  var isNullOrUndefined = (value) => value == null;
  var isObjectType = (value) => typeof value === "object";
  var isObject2 = (value) => !isNullOrUndefined(value) && !Array.isArray(value) && isObjectType(value) && !isDateObject(value);
  var getEventValue = (event) => isObject2(event) && event.target ? isCheckBoxInput(event.target) ? event.target.checked : event.target.value : event;
  var getNodeParentName = (name) => name.substring(0, name.search(/\.\d+(\.|$)/)) || name;
  var isNameInFieldArray = (names, name) => names.has(getNodeParentName(name));
  var isPlainObject2 = (tempObject) => {
    const prototypeCopy = tempObject.constructor && tempObject.constructor.prototype;
    return isObject2(prototypeCopy) && prototypeCopy.hasOwnProperty("isPrototypeOf");
  };
  var isWeb = typeof window !== "undefined" && typeof window.HTMLElement !== "undefined" && typeof document !== "undefined";
  function cloneObject(data) {
    if (data instanceof Date) {
      return new Date(data);
    }
    const isFileListInstance = typeof FileList !== "undefined" && data instanceof FileList;
    if (isWeb && (data instanceof Blob || isFileListInstance)) {
      return data;
    }
    const isArray = Array.isArray(data);
    if (!isArray && !(isObject2(data) && isPlainObject2(data))) {
      return data;
    }
    const copy2 = isArray ? [] : Object.create(Object.getPrototypeOf(data));
    for (const key in data) {
      if (Object.prototype.hasOwnProperty.call(data, key)) {
        copy2[key] = cloneObject(data[key]);
      }
    }
    return copy2;
  }
  var isKey = (value) => /^\w*$/.test(value);
  var isUndefined = (val) => val === void 0;
  var compact = (value) => Array.isArray(value) ? value.filter(Boolean) : [];
  var stringToPath = (input) => compact(input.replace(/["|']|\]/g, "").split(/\.|\[/));
  var get2 = (object, path2, defaultValue) => {
    if (!path2 || !isObject2(object)) {
      return defaultValue;
    }
    const result = (isKey(path2) ? [path2] : stringToPath(path2)).reduce((result2, key) => isNullOrUndefined(result2) ? result2 : result2[key], object);
    return isUndefined(result) || result === object ? isUndefined(object[path2]) ? defaultValue : object[path2] : result;
  };
  var isBoolean = (value) => typeof value === "boolean";
  var isFunction = (value) => typeof value === "function";
  var set = (object, path2, value) => {
    let index2 = -1;
    const tempPath = isKey(path2) ? [path2] : stringToPath(path2);
    const length = tempPath.length;
    const lastIndex = length - 1;
    while (++index2 < length) {
      const key = tempPath[index2];
      let newValue = value;
      if (index2 !== lastIndex) {
        const objValue = object[key];
        newValue = isObject2(objValue) || Array.isArray(objValue) ? objValue : !isNaN(+tempPath[index2 + 1]) ? [] : {};
      }
      if (key === "__proto__" || key === "constructor" || key === "prototype") {
        return;
      }
      object[key] = newValue;
      object = object[key];
    }
  };
  var EVENTS = {
    BLUR: "blur",
    FOCUS_OUT: "focusout",
    CHANGE: "change"
  };
  var VALIDATION_MODE = {
    onBlur: "onBlur",
    onChange: "onChange",
    onSubmit: "onSubmit",
    onTouched: "onTouched",
    all: "all"
  };
  var INPUT_VALIDATION_RULES = {
    max: "max",
    min: "min",
    maxLength: "maxLength",
    minLength: "minLength",
    pattern: "pattern",
    required: "required",
    validate: "validate"
  };
  var HookFormControlContext = import_react21.default.createContext(null);
  HookFormControlContext.displayName = "HookFormControlContext";
  var useFormControlContext = () => import_react21.default.useContext(HookFormControlContext);
  var getProxyFormState = (formState, control, localProxyFormState, isRoot = true) => {
    const result = {
      defaultValues: control._defaultValues
    };
    for (const key in formState) {
      Object.defineProperty(result, key, {
        get: () => {
          const _key = key;
          if (control._proxyFormState[_key] !== VALIDATION_MODE.all) {
            control._proxyFormState[_key] = !isRoot || VALIDATION_MODE.all;
          }
          localProxyFormState && (localProxyFormState[_key] = true);
          return formState[_key];
        }
      });
    }
    return result;
  };
  var useIsomorphicLayoutEffect2 = typeof window !== "undefined" ? import_react21.default.useLayoutEffect : import_react21.default.useEffect;
  function useFormState(props) {
    const formControl = useFormControlContext();
    const { control = formControl, disabled, name, exact } = props || {};
    const [formState, updateFormState] = import_react21.default.useState(control._formState);
    const _localProxyFormState = import_react21.default.useRef({
      isDirty: false,
      isLoading: false,
      dirtyFields: false,
      touchedFields: false,
      validatingFields: false,
      isValidating: false,
      isValid: false,
      errors: false
    });
    useIsomorphicLayoutEffect2(() => control._subscribe({
      name,
      formState: _localProxyFormState.current,
      exact,
      callback: (formState2) => {
        !disabled && updateFormState({
          ...control._formState,
          ...formState2
        });
      }
    }), [name, disabled, exact]);
    import_react21.default.useEffect(() => {
      _localProxyFormState.current.isValid && control._setValid(true);
    }, [control]);
    return import_react21.default.useMemo(() => getProxyFormState(formState, control, _localProxyFormState.current, false), [formState, control]);
  }
  var isString3 = (value) => typeof value === "string";
  var generateWatchOutput = (names, _names, formValues, isGlobal, defaultValue) => {
    if (isString3(names)) {
      isGlobal && _names.watch.add(names);
      return get2(formValues, names, defaultValue);
    }
    if (Array.isArray(names)) {
      return names.map((fieldName) => (isGlobal && _names.watch.add(fieldName), get2(formValues, fieldName)));
    }
    isGlobal && (_names.watchAll = true);
    return formValues;
  };
  var isPrimitive = (value) => isNullOrUndefined(value) || !isObjectType(value);
  function deepEqual(object1, object2, _internal_visited = /* @__PURE__ */ new WeakSet()) {
    if (isPrimitive(object1) || isPrimitive(object2)) {
      return Object.is(object1, object2);
    }
    if (isDateObject(object1) && isDateObject(object2)) {
      return Object.is(object1.getTime(), object2.getTime());
    }
    const keys1 = Object.keys(object1);
    const keys2 = Object.keys(object2);
    if (keys1.length !== keys2.length) {
      return false;
    }
    if (_internal_visited.has(object1) || _internal_visited.has(object2)) {
      return true;
    }
    _internal_visited.add(object1);
    _internal_visited.add(object2);
    for (const key of keys1) {
      const val1 = object1[key];
      if (!keys2.includes(key)) {
        return false;
      }
      if (key !== "ref") {
        const val2 = object2[key];
        if (isDateObject(val1) && isDateObject(val2) || isObject2(val1) && isObject2(val2) || Array.isArray(val1) && Array.isArray(val2) ? !deepEqual(val1, val2, _internal_visited) : !Object.is(val1, val2)) {
          return false;
        }
      }
    }
    return true;
  }
  function useWatch(props) {
    const formControl = useFormControlContext();
    const { control = formControl, name, defaultValue, disabled, exact, compute } = props || {};
    const _defaultValue = import_react21.default.useRef(defaultValue);
    const _compute = import_react21.default.useRef(compute);
    const _computeFormValues = import_react21.default.useRef(void 0);
    const _prevControl = import_react21.default.useRef(control);
    const _prevName = import_react21.default.useRef(name);
    _compute.current = compute;
    const [value, updateValue] = import_react21.default.useState(() => {
      const defaultValue2 = control._getWatch(name, _defaultValue.current);
      return _compute.current ? _compute.current(defaultValue2) : defaultValue2;
    });
    const getCurrentOutput = import_react21.default.useCallback((values) => {
      const formValues = generateWatchOutput(name, control._names, values || control._formValues, false, _defaultValue.current);
      return _compute.current ? _compute.current(formValues) : formValues;
    }, [control._formValues, control._names, name]);
    const refreshValue = import_react21.default.useCallback((values) => {
      if (!disabled) {
        const formValues = generateWatchOutput(name, control._names, values || control._formValues, false, _defaultValue.current);
        if (_compute.current) {
          const computedFormValues = _compute.current(formValues);
          if (!deepEqual(computedFormValues, _computeFormValues.current)) {
            updateValue(computedFormValues);
            _computeFormValues.current = computedFormValues;
          }
        } else {
          updateValue(formValues);
        }
      }
    }, [control._formValues, control._names, disabled, name]);
    useIsomorphicLayoutEffect2(() => {
      if (_prevControl.current !== control || !deepEqual(_prevName.current, name)) {
        _prevControl.current = control;
        _prevName.current = name;
        refreshValue();
      }
      return control._subscribe({
        name,
        formState: {
          values: true
        },
        exact,
        callback: (formState) => {
          refreshValue(formState.values);
        }
      });
    }, [control, exact, name, refreshValue]);
    import_react21.default.useEffect(() => control._removeUnmounted());
    const controlChanged = _prevControl.current !== control;
    const prevName = _prevName.current;
    const computedOutput = import_react21.default.useMemo(() => {
      if (disabled) {
        return null;
      }
      const nameChanged = !controlChanged && !deepEqual(prevName, name);
      const shouldReturnImmediate = controlChanged || nameChanged;
      return shouldReturnImmediate ? getCurrentOutput() : null;
    }, [disabled, controlChanged, name, prevName, getCurrentOutput]);
    return computedOutput !== null ? computedOutput : value;
  }
  function useController(props) {
    const formControl = useFormControlContext();
    const { name, disabled, control = formControl, shouldUnregister, defaultValue, exact = true } = props;
    const isArrayField = isNameInFieldArray(control._names.array, name);
    const defaultValueMemo = import_react21.default.useMemo(() => get2(control._formValues, name, get2(control._defaultValues, name, defaultValue)), [control, name, defaultValue]);
    const value = useWatch({
      control,
      name,
      defaultValue: defaultValueMemo,
      exact
    });
    const formState = useFormState({
      control,
      name,
      exact
    });
    const _props = import_react21.default.useRef(props);
    const _previousNameRef = import_react21.default.useRef(void 0);
    const _registerProps = import_react21.default.useRef(control.register(name, {
      ...props.rules,
      value,
      ...isBoolean(props.disabled) ? { disabled: props.disabled } : {}
    }));
    _props.current = props;
    const fieldState = import_react21.default.useMemo(() => Object.defineProperties({}, {
      invalid: {
        enumerable: true,
        get: () => !!get2(formState.errors, name)
      },
      isDirty: {
        enumerable: true,
        get: () => !!get2(formState.dirtyFields, name)
      },
      isTouched: {
        enumerable: true,
        get: () => !!get2(formState.touchedFields, name)
      },
      isValidating: {
        enumerable: true,
        get: () => !!get2(formState.validatingFields, name)
      },
      error: {
        enumerable: true,
        get: () => get2(formState.errors, name)
      }
    }), [formState, name]);
    const onChange = import_react21.default.useCallback((event) => _registerProps.current.onChange({
      target: {
        value: getEventValue(event),
        name
      },
      type: EVENTS.CHANGE
    }), [name]);
    const onBlur = import_react21.default.useCallback(() => _registerProps.current.onBlur({
      target: {
        value: get2(control._formValues, name),
        name
      },
      type: EVENTS.BLUR
    }), [name, control._formValues]);
    const ref = import_react21.default.useCallback((elm) => {
      const field2 = get2(control._fields, name);
      if (field2 && field2._f && elm) {
        field2._f.ref = {
          focus: () => isFunction(elm.focus) && elm.focus(),
          select: () => isFunction(elm.select) && elm.select(),
          setCustomValidity: (message2) => isFunction(elm.setCustomValidity) && elm.setCustomValidity(message2),
          reportValidity: () => isFunction(elm.reportValidity) && elm.reportValidity()
        };
      }
    }, [control._fields, name]);
    const field = import_react21.default.useMemo(() => ({
      name,
      value,
      ...isBoolean(disabled) || formState.disabled ? { disabled: formState.disabled || disabled } : {},
      onChange,
      onBlur,
      ref
    }), [name, disabled, formState.disabled, onChange, onBlur, ref, value]);
    import_react21.default.useEffect(() => {
      const _shouldUnregisterField = control._options.shouldUnregister || shouldUnregister;
      const previousName = _previousNameRef.current;
      if (previousName && previousName !== name && !isArrayField) {
        control.unregister(previousName);
      }
      control.register(name, {
        ..._props.current.rules,
        ...isBoolean(_props.current.disabled) ? { disabled: _props.current.disabled } : {}
      });
      const updateMounted = (name2, value2) => {
        const field2 = get2(control._fields, name2);
        if (field2 && field2._f) {
          field2._f.mount = value2;
        }
      };
      updateMounted(name, true);
      if (_shouldUnregisterField) {
        const value2 = cloneObject(get2(control._options.defaultValues, name, _props.current.defaultValue));
        set(control._defaultValues, name, value2);
        if (isUndefined(get2(control._formValues, name))) {
          set(control._formValues, name, value2);
        }
      }
      !isArrayField && control.register(name);
      _previousNameRef.current = name;
      return () => {
        (isArrayField ? _shouldUnregisterField && !control._state.action : _shouldUnregisterField) ? control.unregister(name) : updateMounted(name, false);
      };
    }, [name, control, isArrayField, shouldUnregister]);
    import_react21.default.useEffect(() => {
      control._setDisabledField({
        disabled,
        name
      });
    }, [disabled, name, control]);
    return import_react21.default.useMemo(() => ({
      field,
      formState,
      fieldState
    }), [field, formState, fieldState]);
  }
  var Controller = (props) => props.render(useController(props));
  var HookFormContext = import_react21.default.createContext(null);
  HookFormContext.displayName = "HookFormContext";
  var useFormContext = () => import_react21.default.useContext(HookFormContext);
  var FormProvider = (props) => {
    const { children, watch, getValues, getFieldState, setError, clearErrors, setValue, trigger, formState, resetField, reset, handleSubmit, unregister, control, register, setFocus, subscribe: subscribe2 } = props;
    return import_react21.default.createElement(
      HookFormContext.Provider,
      { value: import_react21.default.useMemo(() => ({
        watch,
        getValues,
        getFieldState,
        setError,
        clearErrors,
        setValue,
        trigger,
        formState,
        resetField,
        reset,
        handleSubmit,
        unregister,
        control,
        register,
        setFocus,
        subscribe: subscribe2
      }), [
        clearErrors,
        control,
        formState,
        getFieldState,
        getValues,
        handleSubmit,
        register,
        reset,
        resetField,
        setError,
        setFocus,
        setValue,
        subscribe2,
        trigger,
        unregister,
        watch
      ]) },
      import_react21.default.createElement(HookFormControlContext.Provider, { value: control }, children)
    );
  };
  var appendErrors = (name, validateAllFieldCriteria, errors2, type, message2) => validateAllFieldCriteria ? {
    ...errors2[name],
    types: {
      ...errors2[name] && errors2[name].types ? errors2[name].types : {},
      [type]: message2 || true
    }
  } : {};
  var convertToArrayPayload = (value) => Array.isArray(value) ? value : [value];
  var createSubject = () => {
    let _observers = [];
    const next = (value) => {
      for (const observer of _observers) {
        observer.next && observer.next(value);
      }
    };
    const subscribe2 = (observer) => {
      _observers.push(observer);
      return {
        unsubscribe: () => {
          _observers = _observers.filter((o) => o !== observer);
        }
      };
    };
    const unsubscribe = () => {
      _observers = [];
    };
    return {
      get observers() {
        return _observers;
      },
      next,
      subscribe: subscribe2,
      unsubscribe
    };
  };
  function extractFormValues(fieldsState, formValues) {
    const values = {};
    for (const key in fieldsState) {
      if (fieldsState.hasOwnProperty(key)) {
        const fieldState = fieldsState[key];
        const fieldValue = formValues[key];
        if (fieldState && isObject2(fieldState) && fieldValue) {
          const nestedFieldsState = extractFormValues(fieldState, fieldValue);
          if (isObject2(nestedFieldsState)) {
            values[key] = nestedFieldsState;
          }
        } else if (fieldsState[key]) {
          values[key] = fieldValue;
        }
      }
    }
    return values;
  }
  var isEmptyObject = (value) => isObject2(value) && !Object.keys(value).length;
  var isFileInput = (element) => element.type === "file";
  var isHTMLElement = (value) => {
    if (!isWeb) {
      return false;
    }
    const owner = value ? value.ownerDocument : 0;
    return value instanceof (owner && owner.defaultView ? owner.defaultView.HTMLElement : HTMLElement);
  };
  var isMultipleSelect = (element) => element.type === `select-multiple`;
  var isRadioInput = (element) => element.type === "radio";
  var isRadioOrCheckbox = (ref) => isRadioInput(ref) || isCheckBoxInput(ref);
  var live = (ref) => isHTMLElement(ref) && ref.isConnected;
  function baseGet(object, updatePath) {
    const length = updatePath.slice(0, -1).length;
    let index2 = 0;
    while (index2 < length) {
      object = isUndefined(object) ? index2++ : object[updatePath[index2++]];
    }
    return object;
  }
  function isEmptyArray(obj) {
    for (const key in obj) {
      if (obj.hasOwnProperty(key) && !isUndefined(obj[key])) {
        return false;
      }
    }
    return true;
  }
  function unset(object, path2) {
    const paths = Array.isArray(path2) ? path2 : isKey(path2) ? [path2] : stringToPath(path2);
    const childObject = paths.length === 1 ? object : baseGet(object, paths);
    const index2 = paths.length - 1;
    const key = paths[index2];
    if (childObject) {
      delete childObject[key];
    }
    if (index2 !== 0 && (isObject2(childObject) && isEmptyObject(childObject) || Array.isArray(childObject) && isEmptyArray(childObject))) {
      unset(object, paths.slice(0, -1));
    }
    return object;
  }
  var objectHasFunction = (data) => {
    for (const key in data) {
      if (isFunction(data[key])) {
        return true;
      }
    }
    return false;
  };
  function isTraversable(value) {
    return Array.isArray(value) || isObject2(value) && !objectHasFunction(value);
  }
  function markFieldsDirty(data, fields = {}) {
    for (const key in data) {
      const value = data[key];
      if (isTraversable(value)) {
        fields[key] = Array.isArray(value) ? [] : {};
        markFieldsDirty(value, fields[key]);
      } else if (!isUndefined(value)) {
        fields[key] = true;
      }
    }
    return fields;
  }
  function getDirtyFields(data, formValues, dirtyFieldsFromValues) {
    if (!dirtyFieldsFromValues) {
      dirtyFieldsFromValues = markFieldsDirty(formValues);
    }
    for (const key in data) {
      const value = data[key];
      if (isTraversable(value)) {
        if (isUndefined(formValues) || isPrimitive(dirtyFieldsFromValues[key])) {
          dirtyFieldsFromValues[key] = markFieldsDirty(value, Array.isArray(value) ? [] : {});
        } else {
          getDirtyFields(value, isNullOrUndefined(formValues) ? {} : formValues[key], dirtyFieldsFromValues[key]);
        }
      } else {
        const formValue = formValues[key];
        dirtyFieldsFromValues[key] = !deepEqual(value, formValue);
      }
    }
    return dirtyFieldsFromValues;
  }
  var defaultResult = {
    value: false,
    isValid: false
  };
  var validResult = { value: true, isValid: true };
  var getCheckboxValue = (options3) => {
    if (Array.isArray(options3)) {
      if (options3.length > 1) {
        const values = options3.filter((option) => option && option.checked && !option.disabled).map((option) => option.value);
        return { value: values, isValid: !!values.length };
      }
      return options3[0].checked && !options3[0].disabled ? (
        // @ts-expect-error expected to work in the browser
        options3[0].attributes && !isUndefined(options3[0].attributes.value) ? isUndefined(options3[0].value) || options3[0].value === "" ? validResult : { value: options3[0].value, isValid: true } : validResult
      ) : defaultResult;
    }
    return defaultResult;
  };
  var getFieldValueAs = (value, { valueAsNumber, valueAsDate, setValueAs }) => isUndefined(value) ? value : valueAsNumber ? value === "" ? NaN : value ? +value : value : valueAsDate && isString3(value) ? new Date(value) : setValueAs ? setValueAs(value) : value;
  var defaultReturn = {
    isValid: false,
    value: null
  };
  var getRadioValue = (options3) => Array.isArray(options3) ? options3.reduce((previous, option) => option && option.checked && !option.disabled ? {
    isValid: true,
    value: option.value
  } : previous, defaultReturn) : defaultReturn;
  function getFieldValue(_f) {
    const ref = _f.ref;
    if (isFileInput(ref)) {
      return ref.files;
    }
    if (isRadioInput(ref)) {
      return getRadioValue(_f.refs).value;
    }
    if (isMultipleSelect(ref)) {
      return [...ref.selectedOptions].map(({ value }) => value);
    }
    if (isCheckBoxInput(ref)) {
      return getCheckboxValue(_f.refs).value;
    }
    return getFieldValueAs(isUndefined(ref.value) ? _f.ref.value : ref.value, _f);
  }
  var getResolverOptions = (fieldsNames, _fields, criteriaMode, shouldUseNativeValidation) => {
    const fields = {};
    for (const name of fieldsNames) {
      const field = get2(_fields, name);
      field && set(fields, name, field._f);
    }
    return {
      criteriaMode,
      names: [...fieldsNames],
      fields,
      shouldUseNativeValidation
    };
  };
  var isRegex = (value) => value instanceof RegExp;
  var getRuleValue = (rule) => isUndefined(rule) ? rule : isRegex(rule) ? rule.source : isObject2(rule) ? isRegex(rule.value) ? rule.value.source : rule.value : rule;
  var getValidationModes = (mode) => ({
    isOnSubmit: !mode || mode === VALIDATION_MODE.onSubmit,
    isOnBlur: mode === VALIDATION_MODE.onBlur,
    isOnChange: mode === VALIDATION_MODE.onChange,
    isOnAll: mode === VALIDATION_MODE.all,
    isOnTouch: mode === VALIDATION_MODE.onTouched
  });
  var ASYNC_FUNCTION = "AsyncFunction";
  var hasPromiseValidation = (fieldReference) => !!fieldReference && !!fieldReference.validate && !!(isFunction(fieldReference.validate) && fieldReference.validate.constructor.name === ASYNC_FUNCTION || isObject2(fieldReference.validate) && Object.values(fieldReference.validate).find((validateFunction) => validateFunction.constructor.name === ASYNC_FUNCTION));
  var hasValidation = (options3) => options3.mount && (options3.required || options3.min || options3.max || options3.maxLength || options3.minLength || options3.pattern || options3.validate);
  var isWatched = (name, _names, isBlurEvent) => !isBlurEvent && (_names.watchAll || _names.watch.has(name) || [..._names.watch].some((watchName) => name.startsWith(watchName) && /^\.\w+/.test(name.slice(watchName.length))));
  var iterateFieldsByAction = (fields, action, fieldsNames, abortEarly) => {
    for (const key of fieldsNames || Object.keys(fields)) {
      const field = get2(fields, key);
      if (field) {
        const { _f, ...currentField } = field;
        if (_f) {
          if (_f.refs && _f.refs[0] && action(_f.refs[0], key) && !abortEarly) {
            return true;
          } else if (_f.ref && action(_f.ref, _f.name) && !abortEarly) {
            return true;
          } else {
            if (iterateFieldsByAction(currentField, action)) {
              break;
            }
          }
        } else if (isObject2(currentField)) {
          if (iterateFieldsByAction(currentField, action)) {
            break;
          }
        }
      }
    }
    return;
  };
  function schemaErrorLookup(errors2, _fields, name) {
    const error2 = get2(errors2, name);
    if (error2 || isKey(name)) {
      return {
        error: error2,
        name
      };
    }
    const names = name.split(".");
    while (names.length) {
      const fieldName = names.join(".");
      const field = get2(_fields, fieldName);
      const foundError = get2(errors2, fieldName);
      if (field && !Array.isArray(field) && name !== fieldName) {
        return { name };
      }
      if (foundError && foundError.type) {
        return {
          name: fieldName,
          error: foundError
        };
      }
      if (foundError && foundError.root && foundError.root.type) {
        return {
          name: `${fieldName}.root`,
          error: foundError.root
        };
      }
      names.pop();
    }
    return {
      name
    };
  }
  var shouldRenderFormState = (formStateData, _proxyFormState, updateFormState, isRoot) => {
    updateFormState(formStateData);
    const { name, ...formState } = formStateData;
    return isEmptyObject(formState) || Object.keys(formState).length >= Object.keys(_proxyFormState).length || Object.keys(formState).find((key) => _proxyFormState[key] === (!isRoot || VALIDATION_MODE.all));
  };
  var shouldSubscribeByName = (name, signalName, exact) => !name || !signalName || name === signalName || convertToArrayPayload(name).some((currentName) => currentName && (exact ? currentName === signalName : currentName.startsWith(signalName) || signalName.startsWith(currentName)));
  var skipValidation = (isBlurEvent, isTouched, isSubmitted, reValidateMode, mode) => {
    if (mode.isOnAll) {
      return false;
    } else if (!isSubmitted && mode.isOnTouch) {
      return !(isTouched || isBlurEvent);
    } else if (isSubmitted ? reValidateMode.isOnBlur : mode.isOnBlur) {
      return !isBlurEvent;
    } else if (isSubmitted ? reValidateMode.isOnChange : mode.isOnChange) {
      return isBlurEvent;
    }
    return true;
  };
  var unsetEmptyArray = (ref, name) => !compact(get2(ref, name)).length && unset(ref, name);
  var updateFieldArrayRootError = (errors2, error2, name) => {
    const fieldArrayErrors = convertToArrayPayload(get2(errors2, name));
    set(fieldArrayErrors, "root", error2[name]);
    set(errors2, name, fieldArrayErrors);
    return errors2;
  };
  function getValidateError(result, ref, type = "validate") {
    if (isString3(result) || Array.isArray(result) && result.every(isString3) || isBoolean(result) && !result) {
      return {
        type,
        message: isString3(result) ? result : "",
        ref
      };
    }
  }
  var getValueAndMessage = (validationData) => isObject2(validationData) && !isRegex(validationData) ? validationData : {
    value: validationData,
    message: ""
  };
  var validateField = async (field, disabledFieldNames, formValues, validateAllFieldCriteria, shouldUseNativeValidation, isFieldArray) => {
    const { ref, refs, required, maxLength, minLength, min: min3, max: max3, pattern, validate, name, valueAsNumber, mount } = field._f;
    const inputValue = get2(formValues, name);
    if (!mount || disabledFieldNames.has(name)) {
      return {};
    }
    const inputRef = refs ? refs[0] : ref;
    const setCustomValidity = (message2) => {
      if (shouldUseNativeValidation && inputRef.reportValidity) {
        inputRef.setCustomValidity(isBoolean(message2) ? "" : message2 || "");
        inputRef.reportValidity();
      }
    };
    const error2 = {};
    const isRadio = isRadioInput(ref);
    const isCheckBox = isCheckBoxInput(ref);
    const isRadioOrCheckbox2 = isRadio || isCheckBox;
    const isEmpty = (valueAsNumber || isFileInput(ref)) && isUndefined(ref.value) && isUndefined(inputValue) || isHTMLElement(ref) && ref.value === "" || inputValue === "" || Array.isArray(inputValue) && !inputValue.length;
    const appendErrorsCurry = appendErrors.bind(null, name, validateAllFieldCriteria, error2);
    const getMinMaxMessage = (exceedMax, maxLengthMessage, minLengthMessage, maxType = INPUT_VALIDATION_RULES.maxLength, minType = INPUT_VALIDATION_RULES.minLength) => {
      const message2 = exceedMax ? maxLengthMessage : minLengthMessage;
      error2[name] = {
        type: exceedMax ? maxType : minType,
        message: message2,
        ref,
        ...appendErrorsCurry(exceedMax ? maxType : minType, message2)
      };
    };
    if (isFieldArray ? !Array.isArray(inputValue) || !inputValue.length : required && (!isRadioOrCheckbox2 && (isEmpty || isNullOrUndefined(inputValue)) || isBoolean(inputValue) && !inputValue || isCheckBox && !getCheckboxValue(refs).isValid || isRadio && !getRadioValue(refs).isValid)) {
      const { value, message: message2 } = isString3(required) ? { value: !!required, message: required } : getValueAndMessage(required);
      if (value) {
        error2[name] = {
          type: INPUT_VALIDATION_RULES.required,
          message: message2,
          ref: inputRef,
          ...appendErrorsCurry(INPUT_VALIDATION_RULES.required, message2)
        };
        if (!validateAllFieldCriteria) {
          setCustomValidity(message2);
          return error2;
        }
      }
    }
    if (!isEmpty && (!isNullOrUndefined(min3) || !isNullOrUndefined(max3))) {
      let exceedMax;
      let exceedMin;
      const maxOutput = getValueAndMessage(max3);
      const minOutput = getValueAndMessage(min3);
      if (!isNullOrUndefined(inputValue) && !isNaN(inputValue)) {
        const valueNumber = ref.valueAsNumber || (inputValue ? +inputValue : inputValue);
        if (!isNullOrUndefined(maxOutput.value)) {
          exceedMax = valueNumber > maxOutput.value;
        }
        if (!isNullOrUndefined(minOutput.value)) {
          exceedMin = valueNumber < minOutput.value;
        }
      } else {
        const valueDate = ref.valueAsDate || new Date(inputValue);
        const convertTimeToDate = (time) => /* @__PURE__ */ new Date((/* @__PURE__ */ new Date()).toDateString() + " " + time);
        const isTime = ref.type == "time";
        const isWeek = ref.type == "week";
        if (isString3(maxOutput.value) && inputValue) {
          exceedMax = isTime ? convertTimeToDate(inputValue) > convertTimeToDate(maxOutput.value) : isWeek ? inputValue > maxOutput.value : valueDate > new Date(maxOutput.value);
        }
        if (isString3(minOutput.value) && inputValue) {
          exceedMin = isTime ? convertTimeToDate(inputValue) < convertTimeToDate(minOutput.value) : isWeek ? inputValue < minOutput.value : valueDate < new Date(minOutput.value);
        }
      }
      if (exceedMax || exceedMin) {
        getMinMaxMessage(!!exceedMax, maxOutput.message, minOutput.message, INPUT_VALIDATION_RULES.max, INPUT_VALIDATION_RULES.min);
        if (!validateAllFieldCriteria) {
          setCustomValidity(error2[name].message);
          return error2;
        }
      }
    }
    if ((maxLength || minLength) && !isEmpty && (isString3(inputValue) || isFieldArray && Array.isArray(inputValue))) {
      const maxLengthOutput = getValueAndMessage(maxLength);
      const minLengthOutput = getValueAndMessage(minLength);
      const exceedMax = !isNullOrUndefined(maxLengthOutput.value) && inputValue.length > +maxLengthOutput.value;
      const exceedMin = !isNullOrUndefined(minLengthOutput.value) && inputValue.length < +minLengthOutput.value;
      if (exceedMax || exceedMin) {
        getMinMaxMessage(exceedMax, maxLengthOutput.message, minLengthOutput.message);
        if (!validateAllFieldCriteria) {
          setCustomValidity(error2[name].message);
          return error2;
        }
      }
    }
    if (pattern && !isEmpty && isString3(inputValue)) {
      const { value: patternValue, message: message2 } = getValueAndMessage(pattern);
      if (isRegex(patternValue) && !inputValue.match(patternValue)) {
        error2[name] = {
          type: INPUT_VALIDATION_RULES.pattern,
          message: message2,
          ref,
          ...appendErrorsCurry(INPUT_VALIDATION_RULES.pattern, message2)
        };
        if (!validateAllFieldCriteria) {
          setCustomValidity(message2);
          return error2;
        }
      }
    }
    if (validate) {
      if (isFunction(validate)) {
        const result = await validate(inputValue, formValues);
        const validateError = getValidateError(result, inputRef);
        if (validateError) {
          error2[name] = {
            ...validateError,
            ...appendErrorsCurry(INPUT_VALIDATION_RULES.validate, validateError.message)
          };
          if (!validateAllFieldCriteria) {
            setCustomValidity(validateError.message);
            return error2;
          }
        }
      } else if (isObject2(validate)) {
        let validationResult = {};
        for (const key in validate) {
          if (!isEmptyObject(validationResult) && !validateAllFieldCriteria) {
            break;
          }
          const validateError = getValidateError(await validate[key](inputValue, formValues), inputRef, key);
          if (validateError) {
            validationResult = {
              ...validateError,
              ...appendErrorsCurry(key, validateError.message)
            };
            setCustomValidity(validateError.message);
            if (validateAllFieldCriteria) {
              error2[name] = validationResult;
            }
          }
        }
        if (!isEmptyObject(validationResult)) {
          error2[name] = {
            ref: inputRef,
            ...validationResult
          };
          if (!validateAllFieldCriteria) {
            return error2;
          }
        }
      }
    }
    setCustomValidity(true);
    return error2;
  };
  var defaultOptions3 = {
    mode: VALIDATION_MODE.onSubmit,
    reValidateMode: VALIDATION_MODE.onChange,
    shouldFocusError: true
  };
  function createFormControl(props = {}) {
    let _options = {
      ...defaultOptions3,
      ...props
    };
    let _formState = {
      submitCount: 0,
      isDirty: false,
      isReady: false,
      isLoading: isFunction(_options.defaultValues),
      isValidating: false,
      isSubmitted: false,
      isSubmitting: false,
      isSubmitSuccessful: false,
      isValid: false,
      touchedFields: {},
      dirtyFields: {},
      validatingFields: {},
      errors: _options.errors || {},
      disabled: _options.disabled || false
    };
    let _fields = {};
    let _defaultValues = isObject2(_options.defaultValues) || isObject2(_options.values) ? cloneObject(_options.defaultValues || _options.values) || {} : {};
    let _formValues = _options.shouldUnregister ? {} : cloneObject(_defaultValues);
    let _state = {
      action: false,
      mount: false,
      watch: false,
      keepIsValid: false
    };
    let _names = {
      mount: /* @__PURE__ */ new Set(),
      disabled: /* @__PURE__ */ new Set(),
      unMount: /* @__PURE__ */ new Set(),
      array: /* @__PURE__ */ new Set(),
      watch: /* @__PURE__ */ new Set()
    };
    let delayErrorCallback;
    let timer = 0;
    const defaultProxyFormState = {
      isDirty: false,
      dirtyFields: false,
      validatingFields: false,
      touchedFields: false,
      isValidating: false,
      isValid: false,
      errors: false
    };
    const _proxyFormState = {
      ...defaultProxyFormState
    };
    let _proxySubscribeFormState = {
      ..._proxyFormState
    };
    const _subjects = {
      array: createSubject(),
      state: createSubject()
    };
    const shouldDisplayAllAssociatedErrors = _options.criteriaMode === VALIDATION_MODE.all;
    const debounce = (callback) => (wait) => {
      clearTimeout(timer);
      timer = setTimeout(callback, wait);
    };
    const _setValid = async (shouldUpdateValid) => {
      if (_state.keepIsValid) {
        return;
      }
      if (!_options.disabled && (_proxyFormState.isValid || _proxySubscribeFormState.isValid || shouldUpdateValid)) {
        let isValid2;
        if (_options.resolver) {
          isValid2 = isEmptyObject((await _runSchema()).errors);
          _updateIsValidating();
        } else {
          isValid2 = await executeBuiltInValidation(_fields, true);
        }
        if (isValid2 !== _formState.isValid) {
          _subjects.state.next({
            isValid: isValid2
          });
        }
      }
    };
    const _updateIsValidating = (names, isValidating) => {
      if (!_options.disabled && (_proxyFormState.isValidating || _proxyFormState.validatingFields || _proxySubscribeFormState.isValidating || _proxySubscribeFormState.validatingFields)) {
        (names || Array.from(_names.mount)).forEach((name) => {
          if (name) {
            isValidating ? set(_formState.validatingFields, name, isValidating) : unset(_formState.validatingFields, name);
          }
        });
        _subjects.state.next({
          validatingFields: _formState.validatingFields,
          isValidating: !isEmptyObject(_formState.validatingFields)
        });
      }
    };
    const _setFieldArray = (name, values = [], method, args, shouldSetValues = true, shouldUpdateFieldsAndState = true) => {
      if (args && method && !_options.disabled) {
        _state.action = true;
        if (shouldUpdateFieldsAndState && Array.isArray(get2(_fields, name))) {
          const fieldValues = method(get2(_fields, name), args.argA, args.argB);
          shouldSetValues && set(_fields, name, fieldValues);
        }
        if (shouldUpdateFieldsAndState && Array.isArray(get2(_formState.errors, name))) {
          const errors2 = method(get2(_formState.errors, name), args.argA, args.argB);
          shouldSetValues && set(_formState.errors, name, errors2);
          unsetEmptyArray(_formState.errors, name);
        }
        if ((_proxyFormState.touchedFields || _proxySubscribeFormState.touchedFields) && shouldUpdateFieldsAndState && Array.isArray(get2(_formState.touchedFields, name))) {
          const touchedFields = method(get2(_formState.touchedFields, name), args.argA, args.argB);
          shouldSetValues && set(_formState.touchedFields, name, touchedFields);
        }
        if (_proxyFormState.dirtyFields || _proxySubscribeFormState.dirtyFields) {
          _formState.dirtyFields = getDirtyFields(_defaultValues, _formValues);
        }
        _subjects.state.next({
          name,
          isDirty: _getDirty(name, values),
          dirtyFields: _formState.dirtyFields,
          errors: _formState.errors,
          isValid: _formState.isValid
        });
      } else {
        set(_formValues, name, values);
      }
    };
    const updateErrors = (name, error2) => {
      set(_formState.errors, name, error2);
      _subjects.state.next({
        errors: _formState.errors
      });
    };
    const _setErrors = (errors2) => {
      _formState.errors = errors2;
      _subjects.state.next({
        errors: _formState.errors,
        isValid: false
      });
    };
    const updateValidAndValue = (name, shouldSkipSetValueAs, value, ref) => {
      const field = get2(_fields, name);
      if (field) {
        const defaultValue = get2(_formValues, name, isUndefined(value) ? get2(_defaultValues, name) : value);
        isUndefined(defaultValue) || ref && ref.defaultChecked || shouldSkipSetValueAs ? set(_formValues, name, shouldSkipSetValueAs ? defaultValue : getFieldValue(field._f)) : setFieldValue(name, defaultValue);
        _state.mount && !_state.action && _setValid();
      }
    };
    const updateTouchAndDirty = (name, fieldValue, isBlurEvent, shouldDirty, shouldRender) => {
      let shouldUpdateField = false;
      let isPreviousDirty = false;
      const output = {
        name
      };
      if (!_options.disabled) {
        if (!isBlurEvent || shouldDirty) {
          if (_proxyFormState.isDirty || _proxySubscribeFormState.isDirty) {
            isPreviousDirty = _formState.isDirty;
            _formState.isDirty = output.isDirty = _getDirty();
            shouldUpdateField = isPreviousDirty !== output.isDirty;
          }
          const isCurrentFieldPristine = deepEqual(get2(_defaultValues, name), fieldValue);
          isPreviousDirty = !!get2(_formState.dirtyFields, name);
          isCurrentFieldPristine ? unset(_formState.dirtyFields, name) : set(_formState.dirtyFields, name, true);
          output.dirtyFields = _formState.dirtyFields;
          shouldUpdateField = shouldUpdateField || (_proxyFormState.dirtyFields || _proxySubscribeFormState.dirtyFields) && isPreviousDirty !== !isCurrentFieldPristine;
        }
        if (isBlurEvent) {
          const isPreviousFieldTouched = get2(_formState.touchedFields, name);
          if (!isPreviousFieldTouched) {
            set(_formState.touchedFields, name, isBlurEvent);
            output.touchedFields = _formState.touchedFields;
            shouldUpdateField = shouldUpdateField || (_proxyFormState.touchedFields || _proxySubscribeFormState.touchedFields) && isPreviousFieldTouched !== isBlurEvent;
          }
        }
        shouldUpdateField && shouldRender && _subjects.state.next(output);
      }
      return shouldUpdateField ? output : {};
    };
    const shouldRenderByError = (name, isValid2, error2, fieldState) => {
      const previousFieldError = get2(_formState.errors, name);
      const shouldUpdateValid = (_proxyFormState.isValid || _proxySubscribeFormState.isValid) && isBoolean(isValid2) && _formState.isValid !== isValid2;
      if (_options.delayError && error2) {
        delayErrorCallback = debounce(() => updateErrors(name, error2));
        delayErrorCallback(_options.delayError);
      } else {
        clearTimeout(timer);
        delayErrorCallback = null;
        error2 ? set(_formState.errors, name, error2) : unset(_formState.errors, name);
      }
      if ((error2 ? !deepEqual(previousFieldError, error2) : previousFieldError) || !isEmptyObject(fieldState) || shouldUpdateValid) {
        const updatedFormState = {
          ...fieldState,
          ...shouldUpdateValid && isBoolean(isValid2) ? { isValid: isValid2 } : {},
          errors: _formState.errors,
          name
        };
        _formState = {
          ..._formState,
          ...updatedFormState
        };
        _subjects.state.next(updatedFormState);
      }
    };
    const _runSchema = async (name) => {
      _updateIsValidating(name, true);
      const result = await _options.resolver(_formValues, _options.context, getResolverOptions(name || _names.mount, _fields, _options.criteriaMode, _options.shouldUseNativeValidation));
      return result;
    };
    const executeSchemaAndUpdateState = async (names) => {
      const { errors: errors2 } = await _runSchema(names);
      _updateIsValidating(names);
      if (names) {
        for (const name of names) {
          const error2 = get2(errors2, name);
          error2 ? set(_formState.errors, name, error2) : unset(_formState.errors, name);
        }
      } else {
        _formState.errors = errors2;
      }
      return errors2;
    };
    const executeBuiltInValidation = async (fields, shouldOnlyCheckValid, context2 = {
      valid: true
    }) => {
      for (const name in fields) {
        const field = fields[name];
        if (field) {
          const { _f, ...fieldValue } = field;
          if (_f) {
            const isFieldArrayRoot = _names.array.has(_f.name);
            const isPromiseFunction = field._f && hasPromiseValidation(field._f);
            if (isPromiseFunction && _proxyFormState.validatingFields) {
              _updateIsValidating([_f.name], true);
            }
            const fieldError = await validateField(field, _names.disabled, _formValues, shouldDisplayAllAssociatedErrors, _options.shouldUseNativeValidation && !shouldOnlyCheckValid, isFieldArrayRoot);
            if (isPromiseFunction && _proxyFormState.validatingFields) {
              _updateIsValidating([_f.name]);
            }
            if (fieldError[_f.name]) {
              context2.valid = false;
              if (shouldOnlyCheckValid || props.shouldUseNativeValidation) {
                break;
              }
            }
            !shouldOnlyCheckValid && (get2(fieldError, _f.name) ? isFieldArrayRoot ? updateFieldArrayRootError(_formState.errors, fieldError, _f.name) : set(_formState.errors, _f.name, fieldError[_f.name]) : unset(_formState.errors, _f.name));
          }
          !isEmptyObject(fieldValue) && await executeBuiltInValidation(fieldValue, shouldOnlyCheckValid, context2);
        }
      }
      return context2.valid;
    };
    const _removeUnmounted = () => {
      for (const name of _names.unMount) {
        const field = get2(_fields, name);
        field && (field._f.refs ? field._f.refs.every((ref) => !live(ref)) : !live(field._f.ref)) && unregister(name);
      }
      _names.unMount = /* @__PURE__ */ new Set();
    };
    const _getDirty = (name, data) => !_options.disabled && (name && data && set(_formValues, name, data), !deepEqual(getValues(), _defaultValues));
    const _getWatch = (names, defaultValue, isGlobal) => generateWatchOutput(names, _names, {
      ..._state.mount ? _formValues : isUndefined(defaultValue) ? _defaultValues : isString3(names) ? { [names]: defaultValue } : defaultValue
    }, isGlobal, defaultValue);
    const _getFieldArray = (name) => compact(get2(_state.mount ? _formValues : _defaultValues, name, _options.shouldUnregister ? get2(_defaultValues, name, []) : []));
    const setFieldValue = (name, value, options3 = {}) => {
      const field = get2(_fields, name);
      let fieldValue = value;
      if (field) {
        const fieldReference = field._f;
        if (fieldReference) {
          !fieldReference.disabled && set(_formValues, name, getFieldValueAs(value, fieldReference));
          fieldValue = isHTMLElement(fieldReference.ref) && isNullOrUndefined(value) ? "" : value;
          if (isMultipleSelect(fieldReference.ref)) {
            [...fieldReference.ref.options].forEach((optionRef) => optionRef.selected = fieldValue.includes(optionRef.value));
          } else if (fieldReference.refs) {
            if (isCheckBoxInput(fieldReference.ref)) {
              fieldReference.refs.forEach((checkboxRef) => {
                if (!checkboxRef.defaultChecked || !checkboxRef.disabled) {
                  if (Array.isArray(fieldValue)) {
                    checkboxRef.checked = !!fieldValue.find((data) => data === checkboxRef.value);
                  } else {
                    checkboxRef.checked = fieldValue === checkboxRef.value || !!fieldValue;
                  }
                }
              });
            } else {
              fieldReference.refs.forEach((radioRef) => radioRef.checked = radioRef.value === fieldValue);
            }
          } else if (isFileInput(fieldReference.ref)) {
            fieldReference.ref.value = "";
          } else {
            fieldReference.ref.value = fieldValue;
            if (!fieldReference.ref.type) {
              _subjects.state.next({
                name,
                values: cloneObject(_formValues)
              });
            }
          }
        }
      }
      (options3.shouldDirty || options3.shouldTouch) && updateTouchAndDirty(name, fieldValue, options3.shouldTouch, options3.shouldDirty, true);
      options3.shouldValidate && trigger(name);
    };
    const setValues = (name, value, options3) => {
      for (const fieldKey in value) {
        if (!value.hasOwnProperty(fieldKey)) {
          return;
        }
        const fieldValue = value[fieldKey];
        const fieldName = name + "." + fieldKey;
        const field = get2(_fields, fieldName);
        (_names.array.has(name) || isObject2(fieldValue) || field && !field._f) && !isDateObject(fieldValue) ? setValues(fieldName, fieldValue, options3) : setFieldValue(fieldName, fieldValue, options3);
      }
    };
    const setValue = (name, value, options3 = {}) => {
      const field = get2(_fields, name);
      const isFieldArray = _names.array.has(name);
      const cloneValue = cloneObject(value);
      set(_formValues, name, cloneValue);
      if (isFieldArray) {
        _subjects.array.next({
          name,
          values: cloneObject(_formValues)
        });
        if ((_proxyFormState.isDirty || _proxyFormState.dirtyFields || _proxySubscribeFormState.isDirty || _proxySubscribeFormState.dirtyFields) && options3.shouldDirty) {
          _subjects.state.next({
            name,
            dirtyFields: getDirtyFields(_defaultValues, _formValues),
            isDirty: _getDirty(name, cloneValue)
          });
        }
      } else {
        field && !field._f && !isNullOrUndefined(cloneValue) ? setValues(name, cloneValue, options3) : setFieldValue(name, cloneValue, options3);
      }
      if (isWatched(name, _names)) {
        _subjects.state.next({
          ..._formState,
          name,
          values: cloneObject(_formValues)
        });
      } else {
        _subjects.state.next({
          name: _state.mount ? name : void 0,
          values: cloneObject(_formValues)
        });
      }
    };
    const onChange = async (event) => {
      _state.mount = true;
      const target = event.target;
      let name = target.name;
      let isFieldValueUpdated = true;
      const field = get2(_fields, name);
      const _updateIsFieldValueUpdated = (fieldValue) => {
        isFieldValueUpdated = Number.isNaN(fieldValue) || isDateObject(fieldValue) && isNaN(fieldValue.getTime()) || deepEqual(fieldValue, get2(_formValues, name, fieldValue));
      };
      const validationModeBeforeSubmit = getValidationModes(_options.mode);
      const validationModeAfterSubmit = getValidationModes(_options.reValidateMode);
      if (field) {
        let error2;
        let isValid2;
        const fieldValue = target.type ? getFieldValue(field._f) : getEventValue(event);
        const isBlurEvent = event.type === EVENTS.BLUR || event.type === EVENTS.FOCUS_OUT;
        const shouldSkipValidation = !hasValidation(field._f) && !_options.resolver && !get2(_formState.errors, name) && !field._f.deps || skipValidation(isBlurEvent, get2(_formState.touchedFields, name), _formState.isSubmitted, validationModeAfterSubmit, validationModeBeforeSubmit);
        const watched = isWatched(name, _names, isBlurEvent);
        set(_formValues, name, fieldValue);
        if (isBlurEvent) {
          if (!target || !target.readOnly) {
            field._f.onBlur && field._f.onBlur(event);
            delayErrorCallback && delayErrorCallback(0);
          }
        } else if (field._f.onChange) {
          field._f.onChange(event);
        }
        const fieldState = updateTouchAndDirty(name, fieldValue, isBlurEvent);
        const shouldRender = !isEmptyObject(fieldState) || watched;
        !isBlurEvent && _subjects.state.next({
          name,
          type: event.type,
          values: cloneObject(_formValues)
        });
        if (shouldSkipValidation) {
          if (_proxyFormState.isValid || _proxySubscribeFormState.isValid) {
            if (_options.mode === "onBlur") {
              if (isBlurEvent) {
                _setValid();
              }
            } else if (!isBlurEvent) {
              _setValid();
            }
          }
          return shouldRender && _subjects.state.next({ name, ...watched ? {} : fieldState });
        }
        !isBlurEvent && watched && _subjects.state.next({ ..._formState });
        if (_options.resolver) {
          const { errors: errors2 } = await _runSchema([name]);
          _updateIsValidating([name]);
          _updateIsFieldValueUpdated(fieldValue);
          if (isFieldValueUpdated) {
            const previousErrorLookupResult = schemaErrorLookup(_formState.errors, _fields, name);
            const errorLookupResult = schemaErrorLookup(errors2, _fields, previousErrorLookupResult.name || name);
            error2 = errorLookupResult.error;
            name = errorLookupResult.name;
            isValid2 = isEmptyObject(errors2);
          }
        } else {
          _updateIsValidating([name], true);
          error2 = (await validateField(field, _names.disabled, _formValues, shouldDisplayAllAssociatedErrors, _options.shouldUseNativeValidation))[name];
          _updateIsValidating([name]);
          _updateIsFieldValueUpdated(fieldValue);
          if (isFieldValueUpdated) {
            if (error2) {
              isValid2 = false;
            } else if (_proxyFormState.isValid || _proxySubscribeFormState.isValid) {
              isValid2 = await executeBuiltInValidation(_fields, true);
            }
          }
        }
        if (isFieldValueUpdated) {
          field._f.deps && (!Array.isArray(field._f.deps) || field._f.deps.length > 0) && trigger(field._f.deps);
          shouldRenderByError(name, isValid2, error2, fieldState);
        }
      }
    };
    const _focusInput = (ref, key) => {
      if (get2(_formState.errors, key) && ref.focus) {
        ref.focus();
        return 1;
      }
      return;
    };
    const trigger = async (name, options3 = {}) => {
      let isValid2;
      let validationResult;
      const fieldNames = convertToArrayPayload(name);
      if (_options.resolver) {
        const errors2 = await executeSchemaAndUpdateState(isUndefined(name) ? name : fieldNames);
        isValid2 = isEmptyObject(errors2);
        validationResult = name ? !fieldNames.some((name2) => get2(errors2, name2)) : isValid2;
      } else if (name) {
        validationResult = (await Promise.all(fieldNames.map(async (fieldName) => {
          const field = get2(_fields, fieldName);
          return await executeBuiltInValidation(field && field._f ? { [fieldName]: field } : field);
        }))).every(Boolean);
        !(!validationResult && !_formState.isValid) && _setValid();
      } else {
        validationResult = isValid2 = await executeBuiltInValidation(_fields);
      }
      _subjects.state.next({
        ...!isString3(name) || (_proxyFormState.isValid || _proxySubscribeFormState.isValid) && isValid2 !== _formState.isValid ? {} : { name },
        ..._options.resolver || !name ? { isValid: isValid2 } : {},
        errors: _formState.errors
      });
      options3.shouldFocus && !validationResult && iterateFieldsByAction(_fields, _focusInput, name ? fieldNames : _names.mount);
      return validationResult;
    };
    const getValues = (fieldNames, config) => {
      let values = {
        ..._state.mount ? _formValues : _defaultValues
      };
      if (config) {
        values = extractFormValues(config.dirtyFields ? _formState.dirtyFields : _formState.touchedFields, values);
      }
      return isUndefined(fieldNames) ? values : isString3(fieldNames) ? get2(values, fieldNames) : fieldNames.map((name) => get2(values, name));
    };
    const getFieldState = (name, formState) => ({
      invalid: !!get2((formState || _formState).errors, name),
      isDirty: !!get2((formState || _formState).dirtyFields, name),
      error: get2((formState || _formState).errors, name),
      isValidating: !!get2(_formState.validatingFields, name),
      isTouched: !!get2((formState || _formState).touchedFields, name)
    });
    const clearErrors = (name) => {
      const names = name ? convertToArrayPayload(name) : void 0;
      names === null || names === void 0 ? void 0 : names.forEach((inputName) => unset(_formState.errors, inputName));
      if (names) {
        names.forEach((inputName) => {
          _subjects.state.next({
            name: inputName,
            errors: _formState.errors
          });
        });
      } else {
        _subjects.state.next({
          errors: {}
        });
      }
    };
    const setError = (name, error2, options3) => {
      const ref = (get2(_fields, name, { _f: {} })._f || {}).ref;
      const currentError = get2(_formState.errors, name) || {};
      const { ref: currentRef, message: message2, type, ...restOfErrorTree } = currentError;
      set(_formState.errors, name, {
        ...restOfErrorTree,
        ...error2,
        ref
      });
      _subjects.state.next({
        name,
        errors: _formState.errors,
        isValid: false
      });
      options3 && options3.shouldFocus && ref && ref.focus && ref.focus();
    };
    const watch = (name, defaultValue) => isFunction(name) ? _subjects.state.subscribe({
      next: (payload) => "values" in payload && name(_getWatch(void 0, defaultValue), payload)
    }) : _getWatch(name, defaultValue, true);
    const _subscribe = (props2) => _subjects.state.subscribe({
      next: (formState) => {
        if (shouldSubscribeByName(props2.name, formState.name, props2.exact) && shouldRenderFormState(formState, props2.formState || _proxyFormState, _setFormState, props2.reRenderRoot)) {
          props2.callback({
            values: { ..._formValues },
            ..._formState,
            ...formState,
            defaultValues: _defaultValues
          });
        }
      }
    }).unsubscribe;
    const subscribe2 = (props2) => {
      _state.mount = true;
      _proxySubscribeFormState = {
        ..._proxySubscribeFormState,
        ...props2.formState
      };
      return _subscribe({
        ...props2,
        formState: {
          ...defaultProxyFormState,
          ...props2.formState
        }
      });
    };
    const unregister = (name, options3 = {}) => {
      for (const fieldName of name ? convertToArrayPayload(name) : _names.mount) {
        _names.mount.delete(fieldName);
        _names.array.delete(fieldName);
        if (!options3.keepValue) {
          unset(_fields, fieldName);
          unset(_formValues, fieldName);
        }
        !options3.keepError && unset(_formState.errors, fieldName);
        !options3.keepDirty && unset(_formState.dirtyFields, fieldName);
        !options3.keepTouched && unset(_formState.touchedFields, fieldName);
        !options3.keepIsValidating && unset(_formState.validatingFields, fieldName);
        !_options.shouldUnregister && !options3.keepDefaultValue && unset(_defaultValues, fieldName);
      }
      _subjects.state.next({
        values: cloneObject(_formValues)
      });
      _subjects.state.next({
        ..._formState,
        ...!options3.keepDirty ? {} : { isDirty: _getDirty() }
      });
      !options3.keepIsValid && _setValid();
    };
    const _setDisabledField = ({ disabled, name }) => {
      if (isBoolean(disabled) && _state.mount || !!disabled || _names.disabled.has(name)) {
        const wasDisabled = _names.disabled.has(name);
        const isDisabled = !!disabled;
        const disabledStateChanged = wasDisabled !== isDisabled;
        disabled ? _names.disabled.add(name) : _names.disabled.delete(name);
        disabledStateChanged && _state.mount && !_state.action && _setValid();
      }
    };
    const register = (name, options3 = {}) => {
      let field = get2(_fields, name);
      const disabledIsDefined = isBoolean(options3.disabled) || isBoolean(_options.disabled);
      set(_fields, name, {
        ...field || {},
        _f: {
          ...field && field._f ? field._f : { ref: { name } },
          name,
          mount: true,
          ...options3
        }
      });
      _names.mount.add(name);
      if (field) {
        _setDisabledField({
          disabled: isBoolean(options3.disabled) ? options3.disabled : _options.disabled,
          name
        });
      } else {
        updateValidAndValue(name, true, options3.value);
      }
      return {
        ...disabledIsDefined ? { disabled: options3.disabled || _options.disabled } : {},
        ..._options.progressive ? {
          required: !!options3.required,
          min: getRuleValue(options3.min),
          max: getRuleValue(options3.max),
          minLength: getRuleValue(options3.minLength),
          maxLength: getRuleValue(options3.maxLength),
          pattern: getRuleValue(options3.pattern)
        } : {},
        name,
        onChange,
        onBlur: onChange,
        ref: (ref) => {
          if (ref) {
            register(name, options3);
            field = get2(_fields, name);
            const fieldRef = isUndefined(ref.value) ? ref.querySelectorAll ? ref.querySelectorAll("input,select,textarea")[0] || ref : ref : ref;
            const radioOrCheckbox = isRadioOrCheckbox(fieldRef);
            const refs = field._f.refs || [];
            if (radioOrCheckbox ? refs.find((option) => option === fieldRef) : fieldRef === field._f.ref) {
              return;
            }
            set(_fields, name, {
              _f: {
                ...field._f,
                ...radioOrCheckbox ? {
                  refs: [
                    ...refs.filter(live),
                    fieldRef,
                    ...Array.isArray(get2(_defaultValues, name)) ? [{}] : []
                  ],
                  ref: { type: fieldRef.type, name }
                } : { ref: fieldRef }
              }
            });
            updateValidAndValue(name, false, void 0, fieldRef);
          } else {
            field = get2(_fields, name, {});
            if (field._f) {
              field._f.mount = false;
            }
            (_options.shouldUnregister || options3.shouldUnregister) && !(isNameInFieldArray(_names.array, name) && _state.action) && _names.unMount.add(name);
          }
        }
      };
    };
    const _focusError = () => _options.shouldFocusError && iterateFieldsByAction(_fields, _focusInput, _names.mount);
    const _disableForm = (disabled) => {
      if (isBoolean(disabled)) {
        _subjects.state.next({ disabled });
        iterateFieldsByAction(_fields, (ref, name) => {
          const currentField = get2(_fields, name);
          if (currentField) {
            ref.disabled = currentField._f.disabled || disabled;
            if (Array.isArray(currentField._f.refs)) {
              currentField._f.refs.forEach((inputRef) => {
                inputRef.disabled = currentField._f.disabled || disabled;
              });
            }
          }
        }, 0, false);
      }
    };
    const handleSubmit = (onValid, onInvalid) => async (e2) => {
      let onValidError = void 0;
      if (e2) {
        e2.preventDefault && e2.preventDefault();
        e2.persist && e2.persist();
      }
      let fieldValues = cloneObject(_formValues);
      _subjects.state.next({
        isSubmitting: true
      });
      if (_options.resolver) {
        const { errors: errors2, values } = await _runSchema();
        _updateIsValidating();
        _formState.errors = errors2;
        fieldValues = cloneObject(values);
      } else {
        await executeBuiltInValidation(_fields);
      }
      if (_names.disabled.size) {
        for (const name of _names.disabled) {
          unset(fieldValues, name);
        }
      }
      unset(_formState.errors, "root");
      if (isEmptyObject(_formState.errors)) {
        _subjects.state.next({
          errors: {}
        });
        try {
          await onValid(fieldValues, e2);
        } catch (error2) {
          onValidError = error2;
        }
      } else {
        if (onInvalid) {
          await onInvalid({ ..._formState.errors }, e2);
        }
        _focusError();
        setTimeout(_focusError);
      }
      _subjects.state.next({
        isSubmitted: true,
        isSubmitting: false,
        isSubmitSuccessful: isEmptyObject(_formState.errors) && !onValidError,
        submitCount: _formState.submitCount + 1,
        errors: _formState.errors
      });
      if (onValidError) {
        throw onValidError;
      }
    };
    const resetField = (name, options3 = {}) => {
      if (get2(_fields, name)) {
        if (isUndefined(options3.defaultValue)) {
          setValue(name, cloneObject(get2(_defaultValues, name)));
        } else {
          setValue(name, options3.defaultValue);
          set(_defaultValues, name, cloneObject(options3.defaultValue));
        }
        if (!options3.keepTouched) {
          unset(_formState.touchedFields, name);
        }
        if (!options3.keepDirty) {
          unset(_formState.dirtyFields, name);
          _formState.isDirty = options3.defaultValue ? _getDirty(name, cloneObject(get2(_defaultValues, name))) : _getDirty();
        }
        if (!options3.keepError) {
          unset(_formState.errors, name);
          _proxyFormState.isValid && _setValid();
        }
        _subjects.state.next({ ..._formState });
      }
    };
    const _reset = (formValues, keepStateOptions = {}) => {
      const updatedValues = formValues ? cloneObject(formValues) : _defaultValues;
      const cloneUpdatedValues = cloneObject(updatedValues);
      const isEmptyResetValues = isEmptyObject(formValues);
      const values = isEmptyResetValues ? _defaultValues : cloneUpdatedValues;
      if (!keepStateOptions.keepDefaultValues) {
        _defaultValues = updatedValues;
      }
      if (!keepStateOptions.keepValues) {
        if (keepStateOptions.keepDirtyValues) {
          const fieldsToCheck = /* @__PURE__ */ new Set([
            ..._names.mount,
            ...Object.keys(getDirtyFields(_defaultValues, _formValues))
          ]);
          for (const fieldName of Array.from(fieldsToCheck)) {
            const isDirty = get2(_formState.dirtyFields, fieldName);
            const existingValue = get2(_formValues, fieldName);
            const newValue = get2(values, fieldName);
            if (isDirty && !isUndefined(existingValue)) {
              set(values, fieldName, existingValue);
            } else if (!isDirty && !isUndefined(newValue)) {
              setValue(fieldName, newValue);
            }
          }
        } else {
          if (isWeb && isUndefined(formValues)) {
            for (const name of _names.mount) {
              const field = get2(_fields, name);
              if (field && field._f) {
                const fieldReference = Array.isArray(field._f.refs) ? field._f.refs[0] : field._f.ref;
                if (isHTMLElement(fieldReference)) {
                  const form = fieldReference.closest("form");
                  if (form) {
                    form.reset();
                    break;
                  }
                }
              }
            }
          }
          if (keepStateOptions.keepFieldsRef) {
            for (const fieldName of _names.mount) {
              setValue(fieldName, get2(values, fieldName));
            }
          } else {
            _fields = {};
          }
        }
        _formValues = _options.shouldUnregister ? keepStateOptions.keepDefaultValues ? cloneObject(_defaultValues) : {} : cloneObject(values);
        _subjects.array.next({
          values: { ...values }
        });
        _subjects.state.next({
          values: { ...values }
        });
      }
      _names = {
        mount: keepStateOptions.keepDirtyValues ? _names.mount : /* @__PURE__ */ new Set(),
        unMount: /* @__PURE__ */ new Set(),
        array: /* @__PURE__ */ new Set(),
        disabled: /* @__PURE__ */ new Set(),
        watch: /* @__PURE__ */ new Set(),
        watchAll: false,
        focus: ""
      };
      _state.mount = !_proxyFormState.isValid || !!keepStateOptions.keepIsValid || !!keepStateOptions.keepDirtyValues || !_options.shouldUnregister && !isEmptyObject(values);
      _state.watch = !!_options.shouldUnregister;
      _state.keepIsValid = !!keepStateOptions.keepIsValid;
      _state.action = false;
      if (!keepStateOptions.keepErrors) {
        _formState.errors = {};
      }
      _subjects.state.next({
        submitCount: keepStateOptions.keepSubmitCount ? _formState.submitCount : 0,
        isDirty: isEmptyResetValues ? false : keepStateOptions.keepDirty ? _formState.isDirty : !!(keepStateOptions.keepDefaultValues && !deepEqual(formValues, _defaultValues)),
        isSubmitted: keepStateOptions.keepIsSubmitted ? _formState.isSubmitted : false,
        dirtyFields: isEmptyResetValues ? {} : keepStateOptions.keepDirtyValues ? keepStateOptions.keepDefaultValues && _formValues ? getDirtyFields(_defaultValues, _formValues) : _formState.dirtyFields : keepStateOptions.keepDefaultValues && formValues ? getDirtyFields(_defaultValues, formValues) : keepStateOptions.keepDirty ? _formState.dirtyFields : {},
        touchedFields: keepStateOptions.keepTouched ? _formState.touchedFields : {},
        errors: keepStateOptions.keepErrors ? _formState.errors : {},
        isSubmitSuccessful: keepStateOptions.keepIsSubmitSuccessful ? _formState.isSubmitSuccessful : false,
        isSubmitting: false,
        defaultValues: _defaultValues
      });
    };
    const reset = (formValues, keepStateOptions) => _reset(isFunction(formValues) ? formValues(_formValues) : formValues, { ..._options.resetOptions, ...keepStateOptions });
    const setFocus = (name, options3 = {}) => {
      const field = get2(_fields, name);
      const fieldReference = field && field._f;
      if (fieldReference) {
        const fieldRef = fieldReference.refs ? fieldReference.refs[0] : fieldReference.ref;
        if (fieldRef.focus) {
          setTimeout(() => {
            fieldRef.focus();
            options3.shouldSelect && isFunction(fieldRef.select) && fieldRef.select();
          });
        }
      }
    };
    const _setFormState = (updatedFormState) => {
      _formState = {
        ..._formState,
        ...updatedFormState
      };
    };
    const _resetDefaultValues = () => isFunction(_options.defaultValues) && _options.defaultValues().then((values) => {
      reset(values, _options.resetOptions);
      _subjects.state.next({
        isLoading: false
      });
    });
    const methods = {
      control: {
        register,
        unregister,
        getFieldState,
        handleSubmit,
        setError,
        _subscribe,
        _runSchema,
        _updateIsValidating,
        _focusError,
        _getWatch,
        _getDirty,
        _setValid,
        _setFieldArray,
        _setDisabledField,
        _setErrors,
        _getFieldArray,
        _reset,
        _resetDefaultValues,
        _removeUnmounted,
        _disableForm,
        _subjects,
        _proxyFormState,
        get _fields() {
          return _fields;
        },
        get _formValues() {
          return _formValues;
        },
        get _state() {
          return _state;
        },
        set _state(value) {
          _state = value;
        },
        get _defaultValues() {
          return _defaultValues;
        },
        get _names() {
          return _names;
        },
        set _names(value) {
          _names = value;
        },
        get _formState() {
          return _formState;
        },
        get _options() {
          return _options;
        },
        set _options(value) {
          _options = {
            ..._options,
            ...value
          };
        }
      },
      subscribe: subscribe2,
      trigger,
      register,
      handleSubmit,
      watch,
      setValue,
      getValues,
      reset,
      resetField,
      clearErrors,
      unregister,
      setError,
      setFocus,
      getFieldState
    };
    return {
      ...methods,
      formControl: methods
    };
  }
  var generateId = () => {
    if (typeof crypto !== "undefined" && crypto.randomUUID) {
      return crypto.randomUUID();
    }
    const d = typeof performance === "undefined" ? Date.now() : performance.now() * 1e3;
    return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, (c) => {
      const r2 = (Math.random() * 16 + d) % 16 | 0;
      return (c == "x" ? r2 : r2 & 3 | 8).toString(16);
    });
  };
  var getFocusFieldName = (name, index2, options3 = {}) => options3.shouldFocus || isUndefined(options3.shouldFocus) ? options3.focusName || `${name}.${isUndefined(options3.focusIndex) ? index2 : options3.focusIndex}.` : "";
  var appendAt = (data, value) => [
    ...data,
    ...convertToArrayPayload(value)
  ];
  var fillEmptyArray = (value) => Array.isArray(value) ? value.map(() => void 0) : void 0;
  function insert(data, index2, value) {
    return [
      ...data.slice(0, index2),
      ...convertToArrayPayload(value),
      ...data.slice(index2)
    ];
  }
  var moveArrayAt = (data, from, to) => {
    if (!Array.isArray(data)) {
      return [];
    }
    if (isUndefined(data[to])) {
      data[to] = void 0;
    }
    data.splice(to, 0, data.splice(from, 1)[0]);
    return data;
  };
  var prependAt = (data, value) => [
    ...convertToArrayPayload(value),
    ...convertToArrayPayload(data)
  ];
  function removeAtIndexes(data, indexes) {
    let i = 0;
    const temp = [...data];
    for (const index2 of indexes) {
      temp.splice(index2 - i, 1);
      i++;
    }
    return compact(temp).length ? temp : [];
  }
  var removeArrayAt = (data, index2) => isUndefined(index2) ? [] : removeAtIndexes(data, convertToArrayPayload(index2).sort((a, b) => a - b));
  var swapArrayAt = (data, indexA, indexB) => {
    [data[indexA], data[indexB]] = [data[indexB], data[indexA]];
  };
  var updateAt = (fieldValues, index2, value) => {
    fieldValues[index2] = value;
    return fieldValues;
  };
  function useFieldArray(props) {
    const formControl = useFormControlContext();
    const { control = formControl, name, keyName = "id", shouldUnregister, rules } = props;
    const [fields, setFields] = import_react21.default.useState(control._getFieldArray(name));
    const ids = import_react21.default.useRef(control._getFieldArray(name).map(generateId));
    const _actioned = import_react21.default.useRef(false);
    control._names.array.add(name);
    import_react21.default.useMemo(() => rules && fields.length >= 0 && control.register(name, rules), [control, name, fields.length, rules]);
    useIsomorphicLayoutEffect2(() => control._subjects.array.subscribe({
      next: ({ values, name: fieldArrayName }) => {
        if (fieldArrayName === name || !fieldArrayName) {
          const fieldValues = get2(values, name);
          if (Array.isArray(fieldValues)) {
            setFields(fieldValues);
            ids.current = fieldValues.map(generateId);
          }
        }
      }
    }).unsubscribe, [control, name]);
    const updateValues = import_react21.default.useCallback((updatedFieldArrayValues) => {
      _actioned.current = true;
      control._setFieldArray(name, updatedFieldArrayValues);
    }, [control, name]);
    const append = (value, options3) => {
      const appendValue = convertToArrayPayload(cloneObject(value));
      const updatedFieldArrayValues = appendAt(control._getFieldArray(name), appendValue);
      control._names.focus = getFocusFieldName(name, updatedFieldArrayValues.length - 1, options3);
      ids.current = appendAt(ids.current, appendValue.map(generateId));
      updateValues(updatedFieldArrayValues);
      setFields(updatedFieldArrayValues);
      control._setFieldArray(name, updatedFieldArrayValues, appendAt, {
        argA: fillEmptyArray(value)
      });
    };
    const prepend = (value, options3) => {
      const prependValue = convertToArrayPayload(cloneObject(value));
      const updatedFieldArrayValues = prependAt(control._getFieldArray(name), prependValue);
      control._names.focus = getFocusFieldName(name, 0, options3);
      ids.current = prependAt(ids.current, prependValue.map(generateId));
      updateValues(updatedFieldArrayValues);
      setFields(updatedFieldArrayValues);
      control._setFieldArray(name, updatedFieldArrayValues, prependAt, {
        argA: fillEmptyArray(value)
      });
    };
    const remove = (index2) => {
      const updatedFieldArrayValues = removeArrayAt(control._getFieldArray(name), index2);
      ids.current = removeArrayAt(ids.current, index2);
      updateValues(updatedFieldArrayValues);
      setFields(updatedFieldArrayValues);
      !Array.isArray(get2(control._fields, name)) && set(control._fields, name, void 0);
      control._setFieldArray(name, updatedFieldArrayValues, removeArrayAt, {
        argA: index2
      });
    };
    const insert$1 = (index2, value, options3) => {
      const insertValue = convertToArrayPayload(cloneObject(value));
      const updatedFieldArrayValues = insert(control._getFieldArray(name), index2, insertValue);
      control._names.focus = getFocusFieldName(name, index2, options3);
      ids.current = insert(ids.current, index2, insertValue.map(generateId));
      updateValues(updatedFieldArrayValues);
      setFields(updatedFieldArrayValues);
      control._setFieldArray(name, updatedFieldArrayValues, insert, {
        argA: index2,
        argB: fillEmptyArray(value)
      });
    };
    const swap = (indexA, indexB) => {
      const updatedFieldArrayValues = control._getFieldArray(name);
      swapArrayAt(updatedFieldArrayValues, indexA, indexB);
      swapArrayAt(ids.current, indexA, indexB);
      updateValues(updatedFieldArrayValues);
      setFields(updatedFieldArrayValues);
      control._setFieldArray(name, updatedFieldArrayValues, swapArrayAt, {
        argA: indexA,
        argB: indexB
      }, false);
    };
    const move = (from, to) => {
      const updatedFieldArrayValues = control._getFieldArray(name);
      moveArrayAt(updatedFieldArrayValues, from, to);
      moveArrayAt(ids.current, from, to);
      updateValues(updatedFieldArrayValues);
      setFields(updatedFieldArrayValues);
      control._setFieldArray(name, updatedFieldArrayValues, moveArrayAt, {
        argA: from,
        argB: to
      }, false);
    };
    const update = (index2, value) => {
      const updateValue = cloneObject(value);
      const updatedFieldArrayValues = updateAt(control._getFieldArray(name), index2, updateValue);
      ids.current = [...updatedFieldArrayValues].map((item, i) => !item || i === index2 ? generateId() : ids.current[i]);
      updateValues(updatedFieldArrayValues);
      setFields([...updatedFieldArrayValues]);
      control._setFieldArray(name, updatedFieldArrayValues, updateAt, {
        argA: index2,
        argB: updateValue
      }, true, false);
    };
    const replace2 = (value) => {
      const updatedFieldArrayValues = convertToArrayPayload(cloneObject(value));
      ids.current = updatedFieldArrayValues.map(generateId);
      updateValues([...updatedFieldArrayValues]);
      setFields([...updatedFieldArrayValues]);
      control._setFieldArray(name, [...updatedFieldArrayValues], (data) => data, {}, true, false);
    };
    import_react21.default.useEffect(() => {
      control._state.action = false;
      isWatched(name, control._names) && control._subjects.state.next({
        ...control._formState
      });
      if (_actioned.current && (!getValidationModes(control._options.mode).isOnSubmit || control._formState.isSubmitted) && !getValidationModes(control._options.reValidateMode).isOnSubmit) {
        if (control._options.resolver) {
          control._runSchema([name]).then((result) => {
            control._updateIsValidating([name]);
            const error2 = get2(result.errors, name);
            const existingError = get2(control._formState.errors, name);
            if (existingError ? !error2 && existingError.type || error2 && (existingError.type !== error2.type || existingError.message !== error2.message) : error2 && error2.type) {
              error2 ? set(control._formState.errors, name, error2) : unset(control._formState.errors, name);
              control._subjects.state.next({
                errors: control._formState.errors
              });
            }
          });
        } else {
          const field = get2(control._fields, name);
          if (field && field._f && !(getValidationModes(control._options.reValidateMode).isOnSubmit && getValidationModes(control._options.mode).isOnSubmit)) {
            validateField(field, control._names.disabled, control._formValues, control._options.criteriaMode === VALIDATION_MODE.all, control._options.shouldUseNativeValidation, true).then((error2) => !isEmptyObject(error2) && control._subjects.state.next({
              errors: updateFieldArrayRootError(control._formState.errors, error2, name)
            }));
          }
        }
      }
      control._subjects.state.next({
        name,
        values: cloneObject(control._formValues)
      });
      control._names.focus && iterateFieldsByAction(control._fields, (ref, key) => {
        if (control._names.focus && key.startsWith(control._names.focus) && ref.focus) {
          ref.focus();
          return 1;
        }
        return;
      });
      control._names.focus = "";
      control._setValid();
      _actioned.current = false;
    }, [fields, name, control]);
    import_react21.default.useEffect(() => {
      !get2(control._formValues, name) && control._setFieldArray(name);
      return () => {
        const updateMounted = (name2, value) => {
          const field = get2(control._fields, name2);
          if (field && field._f) {
            field._f.mount = value;
          }
        };
        control._options.shouldUnregister || shouldUnregister ? control.unregister(name) : updateMounted(name, false);
      };
    }, [name, control, keyName, shouldUnregister]);
    return {
      swap: import_react21.default.useCallback(swap, [updateValues, name, control]),
      move: import_react21.default.useCallback(move, [updateValues, name, control]),
      prepend: import_react21.default.useCallback(prepend, [updateValues, name, control]),
      append: import_react21.default.useCallback(append, [updateValues, name, control]),
      remove: import_react21.default.useCallback(remove, [updateValues, name, control]),
      insert: import_react21.default.useCallback(insert$1, [updateValues, name, control]),
      update: import_react21.default.useCallback(update, [updateValues, name, control]),
      replace: import_react21.default.useCallback(replace2, [updateValues, name, control]),
      fields: import_react21.default.useMemo(() => fields.map((field, index2) => ({
        ...field,
        [keyName]: ids.current[index2] || generateId()
      })), [fields, keyName])
    };
  }
  function useForm(props = {}) {
    const _formControl = import_react21.default.useRef(void 0);
    const _values = import_react21.default.useRef(void 0);
    const [formState, updateFormState] = import_react21.default.useState({
      isDirty: false,
      isValidating: false,
      isLoading: isFunction(props.defaultValues),
      isSubmitted: false,
      isSubmitting: false,
      isSubmitSuccessful: false,
      isValid: false,
      submitCount: 0,
      dirtyFields: {},
      touchedFields: {},
      validatingFields: {},
      errors: props.errors || {},
      disabled: props.disabled || false,
      isReady: false,
      defaultValues: isFunction(props.defaultValues) ? void 0 : props.defaultValues
    });
    if (!_formControl.current) {
      if (props.formControl) {
        _formControl.current = {
          ...props.formControl,
          formState
        };
        if (props.defaultValues && !isFunction(props.defaultValues)) {
          props.formControl.reset(props.defaultValues, props.resetOptions);
        }
      } else {
        const { formControl, ...rest } = createFormControl(props);
        _formControl.current = {
          ...rest,
          formState
        };
      }
    }
    const control = _formControl.current.control;
    control._options = props;
    useIsomorphicLayoutEffect2(() => {
      const sub2 = control._subscribe({
        formState: control._proxyFormState,
        callback: () => updateFormState({ ...control._formState }),
        reRenderRoot: true
      });
      updateFormState((data) => ({
        ...data,
        isReady: true
      }));
      control._formState.isReady = true;
      return sub2;
    }, [control]);
    import_react21.default.useEffect(() => control._disableForm(props.disabled), [control, props.disabled]);
    import_react21.default.useEffect(() => {
      if (props.mode) {
        control._options.mode = props.mode;
      }
      if (props.reValidateMode) {
        control._options.reValidateMode = props.reValidateMode;
      }
    }, [control, props.mode, props.reValidateMode]);
    import_react21.default.useEffect(() => {
      if (props.errors) {
        control._setErrors(props.errors);
        control._focusError();
      }
    }, [control, props.errors]);
    import_react21.default.useEffect(() => {
      props.shouldUnregister && control._subjects.state.next({
        values: control._getWatch()
      });
    }, [control, props.shouldUnregister]);
    import_react21.default.useEffect(() => {
      if (control._proxyFormState.isDirty) {
        const isDirty = control._getDirty();
        if (isDirty !== formState.isDirty) {
          control._subjects.state.next({
            isDirty
          });
        }
      }
    }, [control, formState.isDirty]);
    import_react21.default.useEffect(() => {
      var _a;
      if (props.values && !deepEqual(props.values, _values.current)) {
        control._reset(props.values, {
          keepFieldsRef: true,
          ...control._options.resetOptions
        });
        if (!((_a = control._options.resetOptions) === null || _a === void 0 ? void 0 : _a.keepIsValid)) {
          control._setValid();
        }
        _values.current = props.values;
        updateFormState((state) => ({ ...state }));
      } else {
        control._resetDefaultValues();
      }
    }, [control, props.values]);
    import_react21.default.useEffect(() => {
      if (!control._state.mount) {
        control._setValid();
        control._state.mount = true;
      }
      if (control._state.watch) {
        control._state.watch = false;
        control._subjects.state.next({ ...control._formState });
      }
      control._removeUnmounted();
    });
    _formControl.current.formState = import_react21.default.useMemo(() => getProxyFormState(formState, control), [control, formState]);
    return _formControl.current;
  }

  // .design-sync/preview-provider.tsx
  var queryClient = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  function DsPreviewProvider({ children }) {
    return /* @__PURE__ */ React.createElement(ThemeProvider, { defaultTheme: "light" }, /* @__PURE__ */ React.createElement(QueryClientProvider, { client: queryClient }, /* @__PURE__ */ React.createElement(AppDirectionProvider, null, /* @__PURE__ */ React.createElement(MemoryRouter, null, /* @__PURE__ */ React.createElement(NuqsAdapter, null, children)))));
  }

  // .design-sync/ds-entry.tsx
  var ds_entry_exports = {};
  __export(ds_entry_exports, {
    Accordion: () => Accordion_default,
    ActionPanel: () => ActionPanel_default,
    Alert: () => Alert_default,
    AsyncSelectFilter: () => AsyncSelectFilter_default,
    Avatar: () => Avatar_default,
    Badge: () => Badge_default,
    BlankSlate: () => BlankSlate_default,
    Breadcrumb: () => Breadcrumb_default,
    Button: () => Button_default,
    Calendar: () => Calendar_default,
    CalendarGrid: () => CalendarGrid_default,
    CalendarMonthGrid: () => CalendarMonthGrid_default,
    CalendarPicker: () => CalendarPicker_default,
    CalendarTimeGrid: () => CalendarTimeGrid_default,
    Card: () => Card_default,
    Checkbox: () => Checkbox_default,
    CheckboxGroup: () => CheckboxGroup_default,
    Chip: () => Chip_default,
    Collapsible: () => Collapsible_default,
    CollapsibleCard: () => CollapsibleCard_default,
    Combobox: () => Combobox_default,
    Command: () => Command_default,
    CommandPalette: () => CommandPalette_default,
    ComposedButton: () => ComposedButton,
    ComposedLoadingButton: () => ComposedLoadingButton,
    ComposedTooltipButton: () => ComposedTooltipButton,
    ConditionalChip: () => ConditionalChip,
    ConfirmDialog: () => ConfirmDialog_default,
    CountTooltip: () => CountTooltip_default,
    DataTable: () => DataTable_default,
    DataTableContent: () => DataTableContent_default,
    DataTableFacetedFilter: () => DataTableFacetedFilter_default,
    DataTablePagination: () => DataTablePagination_default,
    DataTableToolbar: () => DataTableToolbar_default,
    DateFilter: () => DateFilter_default,
    DateNavigation: () => DateNavigation_default,
    Dialog: () => Dialog_default,
    Divider: () => Divider_default,
    DropdownMenu: () => DropdownMenu_default,
    ErrorTooltip: () => ErrorTooltip_default,
    FacetedFilter: () => FacetedFilter_default,
    FieldSection: () => FieldSection_default,
    FileCard: () => FileCard_default,
    FileIcon: () => FileIcon_default,
    FileUploader: () => FileUploader_default,
    FilterChipList: () => FilterChipList_default,
    FormCheckbox: () => FormCheckbox_default,
    FormCheckboxGroup: () => FormCheckboxGroup_default,
    FormCombobox: () => FormCombobox_default,
    FormContainer: () => FormContainer_default,
    FormControl: () => FormControl_default,
    FormDatePicker: () => FormDatePicker_default,
    FormDateRange: () => FormDateRange_default,
    FormFileUploader: () => FormFileUploader_default,
    FormInput: () => FormInput_default,
    FormLabel: () => FormLabel_default,
    FormMessage: () => FormMessage_default,
    FormMultiCombobox: () => FormMultiCombobox_default,
    FormNumber: () => FormNumber_default,
    FormRadioGroup: () => FormRadioGroup_default,
    FormSelect: () => FormSelect_default,
    FormSubmitButton: () => FormSubmitButton_default,
    FormSubmitButtons: () => FormSubmitButtons_default,
    FormTextarea: () => FormTextarea_default,
    FormTimePicker: () => FormTimePicker_default,
    FormattedNumber: () => FormattedNumber_default,
    GridCard: () => GridCard_default,
    Input: () => Input_default,
    LanguageSwitcher: () => LanguageSwitcher_default,
    Layout: () => Layout_default,
    LinkButton: () => LinkButton_default,
    LoadingButton: () => LoadingButton_default,
    MetaCard: () => MetaCard_default,
    Pagination: () => Pagination_default,
    Popover: () => Popover_default,
    PrimeCard: () => PrimeCard_default,
    PrimeDialog: () => PrimeDialog_default,
    PrimeDropdown: () => PrimeDropdown_default,
    PrimeLoader: () => PrimeLoader_default,
    PrimeTooltip: () => PrimeTooltip_default,
    Progress: () => Progress_default,
    RadioDot: () => RadioDot_default,
    RadioGroup: () => RadioGroup_default,
    ScrollArea: () => ScrollArea_default,
    ScrollSelector: () => ScrollSelector,
    Select: () => Select_default,
    SelectFilter: () => SelectFilter_default,
    Skeleton: () => Skeleton_default,
    Stepper: () => Stepper_default,
    Switch: () => Switch_default,
    Table: () => Table_default,
    Tabs: () => Tabs_default,
    ThemeSwitcher: () => ThemeSwitcher_default,
    TimeFilter: () => TimeFilter_default,
    TimePicker: () => TimePicker_default,
    Toast: () => Toast_default,
    Toaster: () => Toaster_default,
    ToggleGroup: () => ToggleGroup_default,
    Tooltip: () => Tooltip_default,
    TooltipButton: () => TooltipButton_default,
    buttonVariants: () => buttonVariants,
    useCalendarGridContext: () => useCalendarGridContext,
    useDataTableContext: () => useDataTableContext,
    useLayoutContext: () => useLayoutContext
  });
  init_define_import_meta_env();

  // src/components/ui/index.ts
  init_define_import_meta_env();

  // src/components/ui/layout/index.ts
  init_define_import_meta_env();

  // src/components/ui/layout/Layout.tsx
  init_define_import_meta_env();
  var import_react51 = __toESM(require_react_shim(), 1);

  // src/components/shared/index.ts
  init_define_import_meta_env();

  // src/components/shared/blank-slate/index.ts
  init_define_import_meta_env();

  // src/components/shared/blank-slate/BlankSlate.tsx
  init_define_import_meta_env();
  var import_react29 = __toESM(require_react_shim(), 1);

  // src/components/shared/composed-components/index.ts
  init_define_import_meta_env();

  // src/lib/hoc/index.ts
  init_define_import_meta_env();

  // src/lib/hoc/withAuth.tsx
  init_define_import_meta_env();
  function withAuth(WrappedComponent) {
    return ({ permissions, roles, ...props }) => {
      const { hasAccess, hasRoles } = {
        hasAccess: true,
        hasRoles: true
      };
      if (!permissions && !roles) return /* @__PURE__ */ React.createElement(WrappedComponent, { ...props });
      if (roles && roles.length > 0 && !hasRoles) return null;
      if (!hasAccess && !hasRoles) return null;
      return /* @__PURE__ */ React.createElement(WrappedComponent, { ...props });
    };
  }
  var withAuth_default = withAuth;

  // src/lib/hoc/withConditional.tsx
  init_define_import_meta_env();
  function withConditional(WrappedComponent) {
    return ({ condition, ...props }) => {
      if (condition === false) return null;
      return /* @__PURE__ */ React.createElement(WrappedComponent, { ...props });
    };
  }
  var withConditional_default = withConditional;

  // src/components/ui/button/Button.tsx
  init_define_import_meta_env();
  var import_react23 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-slot/dist/index.mjs
  init_define_import_meta_env();
  var React12 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-compose-refs/dist/index.mjs
  init_define_import_meta_env();
  var React11 = __toESM(require_react_shim(), 1);
  function setRef(ref, value) {
    if (typeof ref === "function") {
      return ref(value);
    } else if (ref !== null && ref !== void 0) {
      ref.current = value;
    }
  }
  function composeRefs(...refs) {
    return (node) => {
      let hasCleanup = false;
      const cleanups = refs.map((ref) => {
        const cleanup = setRef(ref, node);
        if (!hasCleanup && typeof cleanup == "function") {
          hasCleanup = true;
        }
        return cleanup;
      });
      if (hasCleanup) {
        return () => {
          for (let i = 0; i < cleanups.length; i++) {
            const cleanup = cleanups[i];
            if (typeof cleanup == "function") {
              cleanup();
            } else {
              setRef(refs[i], null);
            }
          }
        };
      }
    };
  }
  function useComposedRefs(...refs) {
    return React11.useCallback(composeRefs(...refs), refs);
  }

  // node_modules/@radix-ui/react-slot/dist/index.mjs
  var import_jsx_runtime2 = __toESM(require_react_shim(), 1);
  var REACT_LAZY_TYPE = /* @__PURE__ */ Symbol.for("react.lazy");
  var use2 = React12[" use ".trim().toString()];
  function isPromiseLike(value) {
    return typeof value === "object" && value !== null && "then" in value;
  }
  function isLazyComponent(element) {
    return element != null && typeof element === "object" && "$$typeof" in element && element.$$typeof === REACT_LAZY_TYPE && "_payload" in element && isPromiseLike(element._payload);
  }
  // @__NO_SIDE_EFFECTS__
  function createSlot(ownerName) {
    const SlotClone = /* @__PURE__ */ createSlotClone(ownerName);
    const Slot22 = React12.forwardRef((props, forwardedRef) => {
      let { children, ...slotProps } = props;
      if (isLazyComponent(children) && typeof use2 === "function") {
        children = use2(children._payload);
      }
      const childrenArray = React12.Children.toArray(children);
      const slottable = childrenArray.find(isSlottable);
      if (slottable) {
        const newElement = slottable.props.children;
        const newChildren = childrenArray.map((child) => {
          if (child === slottable) {
            if (React12.Children.count(newElement) > 1) return React12.Children.only(null);
            return React12.isValidElement(newElement) ? newElement.props.children : null;
          } else {
            return child;
          }
        });
        return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children: React12.isValidElement(newElement) ? React12.cloneElement(newElement, void 0, newChildren) : null });
      }
      return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children });
    });
    Slot22.displayName = `${ownerName}.Slot`;
    return Slot22;
  }
  var Slot = /* @__PURE__ */ createSlot("Slot");
  // @__NO_SIDE_EFFECTS__
  function createSlotClone(ownerName) {
    const SlotClone = React12.forwardRef((props, forwardedRef) => {
      let { children, ...slotProps } = props;
      if (isLazyComponent(children) && typeof use2 === "function") {
        children = use2(children._payload);
      }
      if (React12.isValidElement(children)) {
        const childrenRef = getElementRef(children);
        const props2 = mergeProps(slotProps, children.props);
        if (children.type !== React12.Fragment) {
          props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
        }
        return React12.cloneElement(children, props2);
      }
      return React12.Children.count(children) > 1 ? React12.Children.only(null) : null;
    });
    SlotClone.displayName = `${ownerName}.SlotClone`;
    return SlotClone;
  }
  var SLOTTABLE_IDENTIFIER = /* @__PURE__ */ Symbol("radix.slottable");
  function isSlottable(child) {
    return React12.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER;
  }
  function mergeProps(slotProps, childProps) {
    const overrideProps = { ...childProps };
    for (const propName in childProps) {
      const slotPropValue = slotProps[propName];
      const childPropValue = childProps[propName];
      const isHandler = /^on[A-Z]/.test(propName);
      if (isHandler) {
        if (slotPropValue && childPropValue) {
          overrideProps[propName] = (...args) => {
            const result = childPropValue(...args);
            slotPropValue(...args);
            return result;
          };
        } else if (slotPropValue) {
          overrideProps[propName] = slotPropValue;
        }
      } else if (propName === "style") {
        overrideProps[propName] = { ...slotPropValue, ...childPropValue };
      } else if (propName === "className") {
        overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
      }
    }
    return { ...slotProps, ...overrideProps };
  }
  function getElementRef(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }

  // node_modules/class-variance-authority/dist/index.mjs
  init_define_import_meta_env();
  var falsyToString = (value) => typeof value === "boolean" ? `${value}` : value === 0 ? "0" : value;
  var cx = clsx;
  var cva = (base, config) => (props) => {
    var _config_compoundVariants;
    if ((config === null || config === void 0 ? void 0 : config.variants) == null) return cx(base, props === null || props === void 0 ? void 0 : props.class, props === null || props === void 0 ? void 0 : props.className);
    const { variants, defaultVariants } = config;
    const getVariantClassNames = Object.keys(variants).map((variant) => {
      const variantProp = props === null || props === void 0 ? void 0 : props[variant];
      const defaultVariantProp = defaultVariants === null || defaultVariants === void 0 ? void 0 : defaultVariants[variant];
      if (variantProp === null) return null;
      const variantKey = falsyToString(variantProp) || falsyToString(defaultVariantProp);
      return variants[variant][variantKey];
    });
    const propsWithoutUndefined = props && Object.entries(props).reduce((acc, param) => {
      let [key, value] = param;
      if (value === void 0) {
        return acc;
      }
      acc[key] = value;
      return acc;
    }, {});
    const getCompoundVariantClassNames = config === null || config === void 0 ? void 0 : (_config_compoundVariants = config.compoundVariants) === null || _config_compoundVariants === void 0 ? void 0 : _config_compoundVariants.reduce((acc, param) => {
      let { class: cvClass, className: cvClassName, ...compoundVariantOptions } = param;
      return Object.entries(compoundVariantOptions).every((param2) => {
        let [key, value] = param2;
        return Array.isArray(value) ? value.includes({
          ...defaultVariants,
          ...propsWithoutUndefined
        }[key]) : {
          ...defaultVariants,
          ...propsWithoutUndefined
        }[key] === value;
      }) ? [
        ...acc,
        cvClass,
        cvClassName
      ] : acc;
    }, []);
    return cx(base, getVariantClassNames, getCompoundVariantClassNames, props === null || props === void 0 ? void 0 : props.class, props === null || props === void 0 ? void 0 : props.className);
  };

  // src/components/ui/button/Button.tsx
  var buttonVariants = cva(
    `
    inline-flex items-center justify-center rounded-md text-sm font-medium ring-offset-background
    transition-colors duration-300 ease-in-out focus-visible:outline-none cursor-pointer
    disabled:pointer-events-none disabled:opacity-50
    aria-disabled:pointer-events-none aria-disabled:opacity-50
  `,
    {
      variants: {
        variant: {
          default: "bg-primary text-primary-foreground hover:not-disabled:bg-primary-600 dark:bg-primary-300 dark:hover:not-disabled:bg-primary-200",
          secondary: "bg-secondary text-secondary-foreground hover:not-disabled:bg-secondary-600",
          muted: "bg-muted-200 text-foreground hover:not-disabled:bg-muted",
          destructive: "text-destructive-foreground bg-destructive hover:not-disabled:bg-destructive/80 dark:bg-destructive-400 dark:hover:not-disabled:bg-destructive-400/90",
          success: "bg-success text-success-foreground hover:not-disabled:bg-success/80",
          link: "text-primary underline-offset-4 hover:not-disabled:underline",
          icon: "bg-primary-600 text-primary-foreground hover:not-disabled:bg-primary dark:bg-primary-300 dark:hover:not-disabled:bg-primary-200",
          "soft-primary": "bg-primary-15 text-primary hover:not-disabled:bg-primary-25",
          "soft-muted": "bg-muted-50 text-muted-foreground hover:not-disabled:bg-muted-100",
          ghost: "text-foreground hover:not-disabled:text-primary",
          "ghost-primary": "text-primary hover:not-disabled:text-primary-700",
          "ghost-muted": "text-muted-foreground hover:not-disabled:text-primary hover:not-disabled:bg-muted-200",
          "ghost-destructive": "text-foreground hover:not-disabled:text-destructive",
          "ghost-muted-primary": "text-muted-400 hover:not-disabled:text-primary",
          "ghost-muted-destructive": "text-muted-400 hover:not-disabled:text-destructive",
          "quiet-primary": "text-primary hover:not-disabled:bg-primary hover:not-disabled:text-primary-foreground dark:hover:not-disabled:bg-primary-300",
          "surface-primary": "border border-primary-50 bg-background text-primary shadow-xs hover:not-disabled:bg-primary-15",
          outline: "border border-primary text-primary bg-background shadow-xs hover:not-disabled:border-primary-600 hover:not-disabled:text-primary-600",
          "outline-muted": "border border-muted-200 text-muted-foreground bg-background shadow-xs hover:not-disabled:bg-muted-50",
          "outline-destructive": "border border-destructive text-destructive bg-destructive-200 hover:not-disabled:bg-destructive-200/80",
          "outline-success": "border border-success text-success bg-success-200 hover:not-disabled:bg-success-200/80",
          unstyled: ""
        },
        size: {
          default: "px-3 py-2",
          xs: "rounded-md text-xs px-2 py-1",
          sm: "rounded-md px-3 py-1",
          lg: "rounded-md px-8 py-2.5",
          icon: "h-8 w-8 rounded-full",
          "icon-sm": "p-1.5 rounded",
          full: "w-full px-4 py-2",
          unstyled: ""
        }
      },
      defaultVariants: {
        variant: "default",
        size: "default"
      }
    }
  );
  var Button = ({ ref, className, variant, size: size4, asChild = false, ...props }) => {
    const Btn = asChild ? Slot : "button";
    return /* @__PURE__ */ React.createElement(
      Btn,
      {
        ref,
        "data-slot": "button",
        "aria-disabled": props.disabled,
        className: cn(buttonVariants({ variant, size: size4 }), className),
        ...props
      }
    );
  };
  var Button_default = (0, import_react23.forwardRef)((props, ref) => Button({ ...props, ref }));

  // src/components/shared/loading-button/LoadingButton.tsx
  init_define_import_meta_env();
  var import_react27 = __toESM(require_react_shim(), 1);

  // src/components/utils/index.ts
  init_define_import_meta_env();

  // src/components/utils/conditional/index.ts
  init_define_import_meta_env();

  // src/components/utils/conditional/Conditional.tsx
  init_define_import_meta_env();
  var import_react24 = __toESM(require_react_shim(), 1);
  var When = ({ condition, children }) => condition ? /* @__PURE__ */ React.createElement(React.Fragment, null, children) : null;
  var Otherwise = ({ children }) => /* @__PURE__ */ React.createElement(React.Fragment, null, children);
  var Conditional = ({ children }) => {
    const validComponents = [When, Otherwise];
    children.forEach((child) => {
      const componentName = child.type?.name ?? child.type?.render?.name ?? child.type;
      if (!validComponents.includes(child.type)) {
        throw new Error(
          `Conditional component only accepts <When /> and <Otherwise /> components as children. Received: <${componentName} />`
        );
      }
    });
    const matchedChild = children.find((child) => (0, import_react24.isValidElement)(child) && child.type === When && child.props.condition);
    return matchedChild || children.find((child) => child.type === Otherwise) || null;
  };
  Conditional.If = When;
  Conditional.Else = Otherwise;
  var Conditional_default = Conditional;

  // node_modules/lucide-react/dist/esm/lucide-react.js
  init_define_import_meta_env();

  // node_modules/lucide-react/dist/esm/createLucideIcon.js
  init_define_import_meta_env();
  var import_react26 = __toESM(require_react_shim());

  // node_modules/lucide-react/dist/esm/shared/src/utils.js
  init_define_import_meta_env();
  var toKebabCase = (string) => string.replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase();
  var mergeClasses = (...classes) => classes.filter((className, index2, array) => {
    return Boolean(className) && className.trim() !== "" && array.indexOf(className) === index2;
  }).join(" ").trim();

  // node_modules/lucide-react/dist/esm/Icon.js
  init_define_import_meta_env();
  var import_react25 = __toESM(require_react_shim());

  // node_modules/lucide-react/dist/esm/defaultAttributes.js
  init_define_import_meta_env();
  var defaultAttributes = {
    xmlns: "http://www.w3.org/2000/svg",
    width: 24,
    height: 24,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: 2,
    strokeLinecap: "round",
    strokeLinejoin: "round"
  };

  // node_modules/lucide-react/dist/esm/Icon.js
  var Icon = (0, import_react25.forwardRef)(
    ({
      color = "currentColor",
      size: size4 = 24,
      strokeWidth = 2,
      absoluteStrokeWidth,
      className = "",
      children,
      iconNode,
      ...rest
    }, ref) => {
      return (0, import_react25.createElement)(
        "svg",
        {
          ref,
          ...defaultAttributes,
          width: size4,
          height: size4,
          stroke: color,
          strokeWidth: absoluteStrokeWidth ? Number(strokeWidth) * 24 / Number(size4) : strokeWidth,
          className: mergeClasses("lucide", className),
          ...rest
        },
        [
          ...iconNode.map(([tag, attrs]) => (0, import_react25.createElement)(tag, attrs)),
          ...Array.isArray(children) ? children : [children]
        ]
      );
    }
  );

  // node_modules/lucide-react/dist/esm/createLucideIcon.js
  var createLucideIcon = (iconName, iconNode) => {
    const Component3 = (0, import_react26.forwardRef)(
      ({ className, ...props }, ref) => (0, import_react26.createElement)(Icon, {
        ref,
        iconNode,
        className: mergeClasses(`lucide-${toKebabCase(iconName)}`, className),
        ...props
      })
    );
    Component3.displayName = `${iconName}`;
    return Component3;
  };

  // node_modules/lucide-react/dist/esm/icons/arrow-left.js
  init_define_import_meta_env();
  var ArrowLeft = createLucideIcon("ArrowLeft", [
    ["path", { d: "m12 19-7-7 7-7", key: "1l729n" }],
    ["path", { d: "M19 12H5", key: "x3x0zl" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/arrow-right.js
  init_define_import_meta_env();
  var ArrowRight = createLucideIcon("ArrowRight", [
    ["path", { d: "M5 12h14", key: "1ays0h" }],
    ["path", { d: "m12 5 7 7-7 7", key: "xquz4c" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/asterisk.js
  init_define_import_meta_env();
  var Asterisk = createLucideIcon("Asterisk", [
    ["path", { d: "M12 6v12", key: "1vza4d" }],
    ["path", { d: "M17.196 9 6.804 15", key: "1ah31z" }],
    ["path", { d: "m6.804 9 10.392 6", key: "1b6pxd" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/calendar.js
  init_define_import_meta_env();
  var Calendar = createLucideIcon("Calendar", [
    ["path", { d: "M8 2v4", key: "1cmpym" }],
    ["path", { d: "M16 2v4", key: "4m81vk" }],
    ["rect", { width: "18", height: "18", x: "3", y: "4", rx: "2", key: "1hopcy" }],
    ["path", { d: "M3 10h18", key: "8toen8" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/check.js
  init_define_import_meta_env();
  var Check = createLucideIcon("Check", [["path", { d: "M20 6 9 17l-5-5", key: "1gmf2c" }]]);

  // node_modules/lucide-react/dist/esm/icons/chevron-down.js
  init_define_import_meta_env();
  var ChevronDown = createLucideIcon("ChevronDown", [
    ["path", { d: "m6 9 6 6 6-6", key: "qrunsl" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/chevron-left.js
  init_define_import_meta_env();
  var ChevronLeft = createLucideIcon("ChevronLeft", [
    ["path", { d: "m15 18-6-6 6-6", key: "1wnfg3" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/chevron-right.js
  init_define_import_meta_env();
  var ChevronRight = createLucideIcon("ChevronRight", [
    ["path", { d: "m9 18 6-6-6-6", key: "mthhwq" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/chevron-up.js
  init_define_import_meta_env();
  var ChevronUp = createLucideIcon("ChevronUp", [["path", { d: "m18 15-6-6-6 6", key: "153udz" }]]);

  // node_modules/lucide-react/dist/esm/icons/chevrons-up-down.js
  init_define_import_meta_env();
  var ChevronsUpDown = createLucideIcon("ChevronsUpDown", [
    ["path", { d: "m7 15 5 5 5-5", key: "1hf1tw" }],
    ["path", { d: "m7 9 5-5 5 5", key: "sgt6xg" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/circle-alert.js
  init_define_import_meta_env();
  var CircleAlert = createLucideIcon("CircleAlert", [
    ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
    ["line", { x1: "12", x2: "12", y1: "8", y2: "12", key: "1pkeuh" }],
    ["line", { x1: "12", x2: "12.01", y1: "16", y2: "16", key: "4dfq90" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/circle-check-big.js
  init_define_import_meta_env();
  var CircleCheckBig = createLucideIcon("CircleCheckBig", [
    ["path", { d: "M21.801 10A10 10 0 1 1 17 3.335", key: "yps3ct" }],
    ["path", { d: "m9 11 3 3L22 4", key: "1pflzl" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/circle-check.js
  init_define_import_meta_env();
  var CircleCheck = createLucideIcon("CircleCheck", [
    ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
    ["path", { d: "m9 12 2 2 4-4", key: "dzmm74" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/circle-minus.js
  init_define_import_meta_env();
  var CircleMinus = createLucideIcon("CircleMinus", [
    ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
    ["path", { d: "M8 12h8", key: "1wcyev" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/circle-x.js
  init_define_import_meta_env();
  var CircleX = createLucideIcon("CircleX", [
    ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
    ["path", { d: "m15 9-6 6", key: "1uzhvr" }],
    ["path", { d: "m9 9 6 6", key: "z0biqf" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/circle.js
  init_define_import_meta_env();
  var Circle = createLucideIcon("Circle", [
    ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/clock-4.js
  init_define_import_meta_env();
  var Clock4 = createLucideIcon("Clock4", [
    ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
    ["polyline", { points: "12 6 12 12 16 14", key: "68esgv" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/component.js
  init_define_import_meta_env();
  var Component2 = createLucideIcon("Component", [
    [
      "path",
      {
        d: "M15.536 11.293a1 1 0 0 0 0 1.414l2.376 2.377a1 1 0 0 0 1.414 0l2.377-2.377a1 1 0 0 0 0-1.414l-2.377-2.377a1 1 0 0 0-1.414 0z",
        key: "1uwlt4"
      }
    ],
    [
      "path",
      {
        d: "M2.297 11.293a1 1 0 0 0 0 1.414l2.377 2.377a1 1 0 0 0 1.414 0l2.377-2.377a1 1 0 0 0 0-1.414L6.088 8.916a1 1 0 0 0-1.414 0z",
        key: "10291m"
      }
    ],
    [
      "path",
      {
        d: "M8.916 17.912a1 1 0 0 0 0 1.415l2.377 2.376a1 1 0 0 0 1.414 0l2.377-2.376a1 1 0 0 0 0-1.415l-2.377-2.376a1 1 0 0 0-1.414 0z",
        key: "1tqoq1"
      }
    ],
    [
      "path",
      {
        d: "M8.916 4.674a1 1 0 0 0 0 1.414l2.377 2.376a1 1 0 0 0 1.414 0l2.377-2.376a1 1 0 0 0 0-1.414l-2.377-2.377a1 1 0 0 0-1.414 0z",
        key: "1x6lto"
      }
    ]
  ]);

  // node_modules/lucide-react/dist/esm/icons/download.js
  init_define_import_meta_env();
  var Download = createLucideIcon("Download", [
    ["path", { d: "M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4", key: "ih7n3h" }],
    ["polyline", { points: "7 10 12 15 17 10", key: "2ggqvy" }],
    ["line", { x1: "12", x2: "12", y1: "15", y2: "3", key: "1vk2je" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/ellipsis.js
  init_define_import_meta_env();
  var Ellipsis = createLucideIcon("Ellipsis", [
    ["circle", { cx: "12", cy: "12", r: "1", key: "41hilf" }],
    ["circle", { cx: "19", cy: "12", r: "1", key: "1wjl8i" }],
    ["circle", { cx: "5", cy: "12", r: "1", key: "1pcz8c" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/file-audio.js
  init_define_import_meta_env();
  var FileAudio = createLucideIcon("FileAudio", [
    ["path", { d: "M17.5 22h.5a2 2 0 0 0 2-2V7l-5-5H6a2 2 0 0 0-2 2v3", key: "rslqgf" }],
    ["path", { d: "M14 2v4a2 2 0 0 0 2 2h4", key: "tnqrlb" }],
    [
      "path",
      {
        d: "M2 19a2 2 0 1 1 4 0v1a2 2 0 1 1-4 0v-4a6 6 0 0 1 12 0v4a2 2 0 1 1-4 0v-1a2 2 0 1 1 4 0",
        key: "9f7x3i"
      }
    ]
  ]);

  // node_modules/lucide-react/dist/esm/icons/file-image.js
  init_define_import_meta_env();
  var FileImage = createLucideIcon("FileImage", [
    ["path", { d: "M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z", key: "1rqfz7" }],
    ["path", { d: "M14 2v4a2 2 0 0 0 2 2h4", key: "tnqrlb" }],
    ["circle", { cx: "10", cy: "12", r: "2", key: "737tya" }],
    ["path", { d: "m20 17-1.296-1.296a2.41 2.41 0 0 0-3.408 0L9 22", key: "wt3hpn" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/file-spreadsheet.js
  init_define_import_meta_env();
  var FileSpreadsheet = createLucideIcon("FileSpreadsheet", [
    ["path", { d: "M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z", key: "1rqfz7" }],
    ["path", { d: "M14 2v4a2 2 0 0 0 2 2h4", key: "tnqrlb" }],
    ["path", { d: "M8 13h2", key: "yr2amv" }],
    ["path", { d: "M14 13h2", key: "un5t4a" }],
    ["path", { d: "M8 17h2", key: "2yhykz" }],
    ["path", { d: "M14 17h2", key: "10kma7" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/file-text.js
  init_define_import_meta_env();
  var FileText = createLucideIcon("FileText", [
    ["path", { d: "M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z", key: "1rqfz7" }],
    ["path", { d: "M14 2v4a2 2 0 0 0 2 2h4", key: "tnqrlb" }],
    ["path", { d: "M10 9H8", key: "b1mrlr" }],
    ["path", { d: "M16 13H8", key: "t4e002" }],
    ["path", { d: "M16 17H8", key: "z1uh3a" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/file-video.js
  init_define_import_meta_env();
  var FileVideo = createLucideIcon("FileVideo", [
    ["path", { d: "M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z", key: "1rqfz7" }],
    ["path", { d: "M14 2v4a2 2 0 0 0 2 2h4", key: "tnqrlb" }],
    ["path", { d: "m10 11 5 3-5 3v-6Z", key: "7ntvm4" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/file.js
  init_define_import_meta_env();
  var File = createLucideIcon("File", [
    ["path", { d: "M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z", key: "1rqfz7" }],
    ["path", { d: "M14 2v4a2 2 0 0 0 2 2h4", key: "tnqrlb" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/folder-open.js
  init_define_import_meta_env();
  var FolderOpen = createLucideIcon("FolderOpen", [
    [
      "path",
      {
        d: "m6 14 1.5-2.9A2 2 0 0 1 9.24 10H20a2 2 0 0 1 1.94 2.5l-1.54 6a2 2 0 0 1-1.95 1.5H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h3.9a2 2 0 0 1 1.69.9l.81 1.2a2 2 0 0 0 1.67.9H18a2 2 0 0 1 2 2v2",
        key: "usdka0"
      }
    ]
  ]);

  // node_modules/lucide-react/dist/esm/icons/folder.js
  init_define_import_meta_env();
  var Folder = createLucideIcon("Folder", [
    [
      "path",
      {
        d: "M20 20a2 2 0 0 0 2-2V8a2 2 0 0 0-2-2h-7.9a2 2 0 0 1-1.69-.9L9.6 3.9A2 2 0 0 0 7.93 3H4a2 2 0 0 0-2 2v13a2 2 0 0 0 2 2Z",
        key: "1kt360"
      }
    ]
  ]);

  // node_modules/lucide-react/dist/esm/icons/info.js
  init_define_import_meta_env();
  var Info = createLucideIcon("Info", [
    ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
    ["path", { d: "M12 16v-4", key: "1dtifu" }],
    ["path", { d: "M12 8h.01", key: "e9boi3" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/layout-dashboard.js
  init_define_import_meta_env();
  var LayoutDashboard = createLucideIcon("LayoutDashboard", [
    ["rect", { width: "7", height: "9", x: "3", y: "3", rx: "1", key: "10lvy0" }],
    ["rect", { width: "7", height: "5", x: "14", y: "3", rx: "1", key: "16une8" }],
    ["rect", { width: "7", height: "9", x: "14", y: "12", rx: "1", key: "1hutg5" }],
    ["rect", { width: "7", height: "5", x: "3", y: "16", rx: "1", key: "ldoo1y" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/list.js
  init_define_import_meta_env();
  var List = createLucideIcon("List", [
    ["path", { d: "M3 12h.01", key: "nlz23k" }],
    ["path", { d: "M3 18h.01", key: "1tta3j" }],
    ["path", { d: "M3 6h.01", key: "1rqtza" }],
    ["path", { d: "M8 12h13", key: "1za7za" }],
    ["path", { d: "M8 18h13", key: "1lx6n3" }],
    ["path", { d: "M8 6h13", key: "ik3vkj" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/loader-circle.js
  init_define_import_meta_env();
  var LoaderCircle = createLucideIcon("LoaderCircle", [
    ["path", { d: "M21 12a9 9 0 1 1-6.219-8.56", key: "13zald" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/log-out.js
  init_define_import_meta_env();
  var LogOut = createLucideIcon("LogOut", [
    ["path", { d: "M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4", key: "1uf3rs" }],
    ["polyline", { points: "16 17 21 12 16 7", key: "1gabdz" }],
    ["line", { x1: "21", x2: "9", y1: "12", y2: "12", key: "1uyos4" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/minus.js
  init_define_import_meta_env();
  var Minus = createLucideIcon("Minus", [["path", { d: "M5 12h14", key: "1ays0h" }]]);

  // node_modules/lucide-react/dist/esm/icons/moon.js
  init_define_import_meta_env();
  var Moon = createLucideIcon("Moon", [
    ["path", { d: "M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z", key: "a7tn18" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/paperclip.js
  init_define_import_meta_env();
  var Paperclip = createLucideIcon("Paperclip", [
    ["path", { d: "M13.234 20.252 21 12.3", key: "1cbrk9" }],
    [
      "path",
      {
        d: "m16 6-8.414 8.586a2 2 0 0 0 0 2.828 2 2 0 0 0 2.828 0l8.414-8.586a4 4 0 0 0 0-5.656 4 4 0 0 0-5.656 0l-8.415 8.585a6 6 0 1 0 8.486 8.486",
        key: "1pkts6"
      }
    ]
  ]);

  // node_modules/lucide-react/dist/esm/icons/plus.js
  init_define_import_meta_env();
  var Plus = createLucideIcon("Plus", [
    ["path", { d: "M5 12h14", key: "1ays0h" }],
    ["path", { d: "M12 5v14", key: "s699le" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/search.js
  init_define_import_meta_env();
  var Search = createLucideIcon("Search", [
    ["circle", { cx: "11", cy: "11", r: "8", key: "4ej97u" }],
    ["path", { d: "m21 21-4.3-4.3", key: "1qie3q" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/settings.js
  init_define_import_meta_env();
  var Settings = createLucideIcon("Settings", [
    [
      "path",
      {
        d: "M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z",
        key: "1qme2f"
      }
    ],
    ["circle", { cx: "12", cy: "12", r: "3", key: "1v7zrd" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/sun.js
  init_define_import_meta_env();
  var Sun = createLucideIcon("Sun", [
    ["circle", { cx: "12", cy: "12", r: "4", key: "4exip2" }],
    ["path", { d: "M12 2v2", key: "tus03m" }],
    ["path", { d: "M12 20v2", key: "1lh1kg" }],
    ["path", { d: "m4.93 4.93 1.41 1.41", key: "149t6j" }],
    ["path", { d: "m17.66 17.66 1.41 1.41", key: "ptbguv" }],
    ["path", { d: "M2 12h2", key: "1t8f8n" }],
    ["path", { d: "M20 12h2", key: "1q8mjw" }],
    ["path", { d: "m6.34 17.66-1.41 1.41", key: "1m8zz5" }],
    ["path", { d: "m19.07 4.93-1.41 1.41", key: "1shlcs" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/triangle-alert.js
  init_define_import_meta_env();
  var TriangleAlert = createLucideIcon("TriangleAlert", [
    [
      "path",
      {
        d: "m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3",
        key: "wmoenq"
      }
    ],
    ["path", { d: "M12 9v4", key: "juzpu7" }],
    ["path", { d: "M12 17h.01", key: "p32p05" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/users.js
  init_define_import_meta_env();
  var Users = createLucideIcon("Users", [
    ["path", { d: "M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2", key: "1yyitq" }],
    ["circle", { cx: "9", cy: "7", r: "4", key: "nufk8" }],
    ["path", { d: "M22 21v-2a4 4 0 0 0-3-3.87", key: "kshegd" }],
    ["path", { d: "M16 3.13a4 4 0 0 1 0 7.75", key: "1da9ce" }]
  ]);

  // node_modules/lucide-react/dist/esm/icons/x.js
  init_define_import_meta_env();
  var X = createLucideIcon("X", [
    ["path", { d: "M18 6 6 18", key: "1bl5f8" }],
    ["path", { d: "m6 6 12 12", key: "d8bk6v" }]
  ]);

  // src/components/shared/loading-button/LoadingButton.tsx
  var LoadingButton = ({ ref, className, loading = false, disabled, children, ...props }) => {
    const { t: t3 } = useAppTranslation("common");
    return /* @__PURE__ */ React.createElement(Button_default, { className: cn("gap relative", className), disabled: disabled ?? loading, ref, ...props }, /* @__PURE__ */ React.createElement("div", { role: "button" }, /* @__PURE__ */ React.createElement("span", { className: cn(loading ? "opacity-0" : "opacity-100", className) }, children), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: loading }, /* @__PURE__ */ React.createElement("div", { className: "absolute inset-s-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2" }, /* @__PURE__ */ React.createElement(LoaderCircle, { className: "animate-spin text-inherit", size: 20, "aria-label": t3("loading") })))));
  };
  var LoadingButton_default = (0, import_react27.forwardRef)((props, ref) => LoadingButton({ ...props, ref }));

  // src/components/shared/tooltip-button/TooltipButton.tsx
  init_define_import_meta_env();
  var import_react28 = __toESM(require_react_shim(), 1);
  var TooltipButton = ({ ref, title, className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(Tooltip_default.Provider, null, /* @__PURE__ */ React.createElement(Tooltip_default, null, /* @__PURE__ */ React.createElement(Tooltip_default.Trigger, { asChild: true }, /* @__PURE__ */ React.createElement(LoadingButton_default, { ref, className: cn(className), ...props }, children)), /* @__PURE__ */ React.createElement(Tooltip_default.Content, { hidden: !title }, title)));
  };
  var TooltipButton_default = (0, import_react28.forwardRef)((props, ref) => TooltipButton({ ...props, ref }));

  // src/components/ui/chip/Chip.tsx
  init_define_import_meta_env();
  var chipVariants = cva(
    "inline-flex items-center rounded-lg border border-transparent transition-colors outline-hidden shadow-sm font-semibold cursor-default",
    {
      variants: {
        variant: {
          default: "bg-primary-25 text-primary-700 hover:text-primary-foreground hover:bg-primary dark:hover:bg-primary-300",
          secondary: "bg-secondary-25 text-foreground hover:text-secondary-foreground hover:bg-secondary-400",
          success: "bg-success-200 text-success hover:text-success-foreground hover:bg-success",
          destructive: "bg-destructive-200 text-destructive hover:text-destructive-foreground hover:bg-destructive dark:hover:bg-destructive-400",
          warning: "bg-warning-200 text-foreground hover:text-warning-foreground hover:bg-warning-400",
          accent: "bg-accent-200 text-accent-foreground hover:bg-accent-400",
          muted: "bg-muted-200 text-foreground hover:bg-muted"
        },
        size: {
          default: "px-3 py-0.5 text-xs",
          xxs: "px-0.5 py-0.5 text-2xs leading-none",
          xs: "px-2 py-0.5 text-2xs leading-none",
          sm: "px-2.5 py-1 text-sm",
          md: "px-3 py-1.5 text-md",
          lg: "px-3.5 py-2 text-lg",
          unstyled: ""
        }
      },
      defaultVariants: {
        variant: "default",
        size: "default"
      }
    }
  );
  var Chip = ({ className, variant, size: size4, ...props }) => /* @__PURE__ */ React.createElement("div", { "data-slot": "chip", className: cn(chipVariants({ variant, size: size4 }), className), ...props });
  var Chip_default = Chip;

  // src/components/shared/composed-components/index.ts
  var ConditionalChip = withConditional_default(Chip_default);
  var ComposedButton = withConditional_default(withAuth_default(Button_default));
  var ComposedLoadingButton = withConditional_default(withAuth_default(LoadingButton_default));
  var ComposedTooltipButton = withConditional_default(withAuth_default(TooltipButton_default));

  // src/components/shared/blank-slate/BlankSlate.tsx
  var blankSlateVariants = cva("flex h-full w-full flex-col items-center justify-center", {
    variants: {
      size: {
        xs: "p-3 gap-1 **:data-[slot=blank-slate-container]:min-h-0 **:data-[slot=blank-slate-icon]:size-16 **:data-[slot=blank-slate-title]:text-xs **:data-[slot=blank-slate-description]:text-2xs",
        sm: "p-6 gap-1 **:data-[slot=blank-slate-container]:min-h-24 **:data-[slot=blank-slate-icon]:size-20 **:data-[slot=blank-slate-title]:text-sm **:data-[slot=blank-slate-description]:text-2xs",
        md: "p-6 gap-2 **:data-[slot=blank-slate-container]:min-h-36 **:data-[slot=blank-slate-icon]:size-30 **:data-[slot=blank-slate-title]:text-lg **:data-[slot=blank-slate-description]:text-sm",
        lg: "p-6 gap-3 **:data-[slot=blank-slate-container]:min-h-48 **:data-[slot=blank-slate-icon]:size-40 **:data-[slot=blank-slate-title]:text-xl **:data-[slot=blank-slate-description]:text-base"
      }
    },
    defaultVariants: { size: "md" }
  });
  var Icon2 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "blank-slate-icon",
      className: cn("text-muted-200 pointer-events-none absolute inset-0 m-auto [&>svg]:size-full", className),
      ...props
    },
    children ?? /* @__PURE__ */ React.createElement(FolderOpen, { strokeWidth: 1 })
  );
  var Title = ({ className, ...props }) => /* @__PURE__ */ React.createElement("h3", { "data-slot": "blank-slate-title", className: cn("text-muted-400 font-medium", className), ...props });
  var Description = ({ className, ...props }) => /* @__PURE__ */ React.createElement("p", { "data-slot": "blank-slate-description", className: cn("text-muted-400 max-w-md text-center", className), ...props });
  var Actions = ({ className, ...props }) => /* @__PURE__ */ React.createElement("div", { "data-slot": "blank-slate-actions", className: cn("flex flex-wrap items-center gap-3", className), ...props });
  var Action2 = ({ ref, className, ...props }) => /* @__PURE__ */ React.createElement(ComposedTooltipButton, { ref, variant: "outline", className: cn("flex items-center gap-2", className), ...props });
  var BlankSlate = ({ icon, title, description, when, size: size4, className, children, ...props }) => {
    if (!when) return null;
    const childArray = import_react29.Children.toArray(children);
    const iconChild = childArray.find((child) => (0, import_react29.isValidElement)(child) && child.type === BlankSlate.Icon);
    const titleChild = childArray.find((child) => (0, import_react29.isValidElement)(child) && child.type === BlankSlate.Title);
    const descriptionChild = childArray.find((child) => (0, import_react29.isValidElement)(child) && child.type === BlankSlate.Description);
    const actionsChild = childArray.find((child) => (0, import_react29.isValidElement)(child) && child.type === BlankSlate.Actions);
    const renderedTitle = titleChild ?? (title ? /* @__PURE__ */ React.createElement(Title, null, title) : null);
    const renderedDescription = descriptionChild ?? (description ? /* @__PURE__ */ React.createElement(Description, null, description) : null);
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "blank-slate", className: cn(blankSlateVariants({ size: size4 }), className), ...props }, /* @__PURE__ */ React.createElement("div", { "data-slot": "blank-slate-container", className: "relative flex flex-col items-center justify-center" }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!iconChild }, iconChild), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Icon2, null, icon))), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!renderedTitle || !!renderedDescription }, /* @__PURE__ */ React.createElement("div", { className: "isolate flex flex-col items-center gap-2" }, renderedTitle, renderedDescription))), actionsChild);
  };
  BlankSlate.Icon = Icon2;
  BlankSlate.Title = Title;
  BlankSlate.Description = Description;
  BlankSlate.Actions = Actions;
  BlankSlate.Action = (0, import_react29.forwardRef)((props, ref) => Action2({ ...props, ref }));
  Icon2.displayName = "BlankSlateIcon";
  Title.displayName = "BlankSlateTitle";
  Description.displayName = "BlankSlateDescription";
  Actions.displayName = "BlankSlateActions";
  BlankSlate.Action.displayName = "BlankSlateAction";
  var BlankSlate_default = BlankSlate;

  // src/components/shared/form-submit-button/index.ts
  init_define_import_meta_env();

  // src/components/shared/form-submit-button/FormSubmitButton.tsx
  init_define_import_meta_env();
  function FormSubmitButton({ className, size: size4 = "default", textContent = "Save", variant = "default", ...props }) {
    return /* @__PURE__ */ React.createElement(LoadingButton_default, { className: cn("block w-fit", className), variant, size: size4, type: "submit", ...props }, textContent);
  }
  var FormSubmitButton_default = FormSubmitButton;

  // src/components/shared/link-button/index.ts
  init_define_import_meta_env();

  // src/components/shared/link-button/LinkButton.tsx
  init_define_import_meta_env();
  function LinkButton({ to, textContent, className, containerClassName, withContainer = true, ...props }) {
    const child = /* @__PURE__ */ React.createElement(Button_default, { asChild: true, className: cn(className), ...props }, /* @__PURE__ */ React.createElement(Link, { to }, textContent));
    return !withContainer ? child : /* @__PURE__ */ React.createElement("div", { className: cn("bg-primary mb-10 flex items-center rounded-xl px-5 py-3", containerClassName) }, child);
  }
  var LinkButton_default = LinkButton;

  // src/components/shared/loading-button/index.ts
  init_define_import_meta_env();

  // src/components/shared/tooltip-button/index.ts
  init_define_import_meta_env();

  // src/components/shared/prime-loader/index.ts
  init_define_import_meta_env();

  // src/components/shared/prime-loader/PrimeLoader.tsx
  init_define_import_meta_env();
  function PrimeLoader({ withOverlay = true, message: message2 = "Please Wait ...", ..._ }) {
    if (!withOverlay) return /* @__PURE__ */ React.createElement(LoaderCircle, { size: 64, className: "text-primary animate-spin" });
    return /* @__PURE__ */ React.createElement("div", { className: "bg-background/80 fixed inset-0 z-50 flex flex-col items-center justify-center" }, /* @__PURE__ */ React.createElement("div", { className: "animate-out fade-out direction-alternate repeat-infinite fill-mode-both duration-[3s]" }, /* @__PURE__ */ React.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "-466.4 259.6 280.2 47.3", width: 300 }, /* @__PURE__ */ React.createElement(
      "polyline",
      {
        className: "fill-none stroke-primary-400 stroke-[4px] [stroke-linecap:square] [stroke-miterlimit:10] [stroke-dasharray:600] animate-prime-loader-move",
        points: "-465.4,281 -436,281 -418.9,281 -423.9,281 -363.2,281 -355.2,269 -345.2,303 -335.2,263 -325.2,291 -319.2,281 -187.2,281"
      }
    ))), /* @__PURE__ */ React.createElement("p", { className: "text-muted-foreground mt-4 text-sm" }, message2));
  }
  var PrimeLoader_default = PrimeLoader;

  // src/components/shared/theme-switcher/index.ts
  init_define_import_meta_env();

  // src/components/shared/theme-switcher/ThemeSwitcher.tsx
  init_define_import_meta_env();
  function ThemeSwitcher() {
    const { t: t3 } = useAppTranslation("header");
    const { theme, setTheme } = useTheme();
    const isDark = theme === THEME_TYPES.DARK;
    const toggleTheme = () => setTheme(isDark ? THEME_TYPES.LIGHT : THEME_TYPES.DARK);
    return /* @__PURE__ */ React.createElement(
      Button_default,
      {
        variant: "ghost",
        size: "icon",
        onClick: toggleTheme,
        "aria-label": isDark ? t3("switchToLight") : t3("switchToDark"),
        className: "text-muted-foreground hover:text-foreground relative h-9 w-9"
      },
      /* @__PURE__ */ React.createElement(Sun, { className: "h-4 w-4 scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" }),
      /* @__PURE__ */ React.createElement(Moon, { className: "absolute h-4 w-4 scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" }),
      /* @__PURE__ */ React.createElement("span", { className: "sr-only" }, t3("toggleTheme"))
    );
  }
  var ThemeSwitcher_default = ThemeSwitcher;

  // src/components/shared/language-switcher/index.ts
  init_define_import_meta_env();

  // src/components/shared/language-switcher/LanguageSwitcher.tsx
  init_define_import_meta_env();
  function LanguageSwitcher() {
    const { t: t3, currentLanguage, changeLanguage: changeLanguage2 } = useAppTranslation("header");
    const nextLanguage = LANGUAGE_OPTIONS.find((lang) => lang.code !== currentLanguage) ?? LANGUAGE_OPTIONS[0];
    const handleToggle = () => changeLanguage2(nextLanguage.code);
    return /* @__PURE__ */ React.createElement(
      Button_default,
      {
        variant: "ghost",
        size: "icon",
        onClick: handleToggle,
        "aria-label": t3("switchLanguage"),
        title: t3("switchLanguage"),
        className: "text-muted-foreground hover:text-foreground h-9 w-9 text-xs font-semibold tracking-wider uppercase"
      },
      nextLanguage.code.toUpperCase()
    );
  }
  var LanguageSwitcher_default = LanguageSwitcher;

  // src/components/shared/toaster/index.ts
  init_define_import_meta_env();

  // src/components/shared/toaster/Toaster.tsx
  init_define_import_meta_env();
  function Toaster() {
    const { toasts } = useToast();
    return /* @__PURE__ */ React.createElement(Toast_default.Provider, { duration: 5e3 }, toasts.map(({ id, title, description, action, ...props }) => {
      return /* @__PURE__ */ React.createElement(Toast_default, { key: id, ...props }, /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-[35px_1fr] items-center gap-0.5" }, /* @__PURE__ */ React.createElement(Toast_default.Icon, null), title && /* @__PURE__ */ React.createElement(Toast_default.Title, null, title), description && /* @__PURE__ */ React.createElement(Toast_default.Description, { className: "col-start-2" }, description)), action && /* @__PURE__ */ React.createElement(Toast_default.Action, { asChild: true, altText: title ?? "Toast Action" }, action), /* @__PURE__ */ React.createElement(Toast_default.Close, null));
    }), /* @__PURE__ */ React.createElement(Toast_default.Viewport, { position: toasts[0]?.position }));
  }
  var Toaster_default = Toaster;

  // src/components/shared/prime-dialog/index.ts
  init_define_import_meta_env();

  // src/components/shared/prime-dialog/PrimeDialog.tsx
  init_define_import_meta_env();
  var import_react30 = __toESM(require_react_shim(), 1);
  var getDialogModeText = (dialogMode) => {
    if (dialogMode === void 0) return "";
    if (typeof dialogMode === "boolean") return dialogMode ? "Edit" : "Add";
    return dialogMode ?? "Add";
  };
  var PrimeDialogContext = (0, import_react30.createContext)({});
  var Trigger = ({ children, ...props }) => /* @__PURE__ */ React.createElement(Dialog_default.Trigger, { asChild: true, ...props }, children);
  var Panel = ({ dismissible = false, className, children, ...props }) => {
    const { t: t3 } = useAppTranslation("common");
    return /* @__PURE__ */ React.createElement(
      Dialog_default.Panel,
      {
        className: cn("flex max-h-[90dvh] flex-col overflow-clip py-0 md:max-w-200", className),
        "aria-describedby": void 0,
        onInteractOutside: (e2) => !dismissible && e2.preventDefault(),
        onEscapeKeyDown: (e2) => !dismissible && e2.preventDefault(),
        ...props
      },
      /* @__PURE__ */ React.createElement(
        Dialog_default.Close,
        {
          "aria-label": t3("close"),
          className: "absolute inset-e-4 top-4 cursor-pointer rounded-sm opacity-70 transition-opacity hover:opacity-100 focus:outline-none"
        },
        /* @__PURE__ */ React.createElement(X, { size: 18 })
      ),
      children
    );
  };
  var Header = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Dialog_default.Header, { className: cn("bg-primary/5 border-primary/20 shrink-0 border-b py-6", className), ...props }, children);
  var Title2 = ({ className, children, dialogMode, ...props }) => {
    const context2 = (0, import_react30.useContext)(PrimeDialogContext);
    return /* @__PURE__ */ React.createElement(Dialog_default.Title, { className: cn(className), ...props }, dialogMode ?? getDialogModeText(context2.dialogMode), " ", children);
  };
  var Description2 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Dialog_default.Description, { className: cn(className), ...props }, children);
  var Content = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Dialog_default.Content, { className: cn("min-h-0 flex-1 overflow-y-auto last:pb-6", className), ...props }, children);
  var Actions2 = ({ className, isLoading, dialogMode, primaryButtonProps, secondaryButtonProps, children, ...props }) => {
    const context2 = (0, import_react30.useContext)(PrimeDialogContext);
    return /* @__PURE__ */ React.createElement(Dialog_default.Footer, { className: cn("flex justify-end gap-x-1.5 pb-6", className), ...props }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!children }, children), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Dialog_default.Close, { asChild: true }, /* @__PURE__ */ React.createElement(
      LoadingButton_default,
      {
        type: "reset",
        variant: "outline",
        className: cn(context2.dialogMode !== "View" && "grow", secondaryButtonProps?.className),
        ...secondaryButtonProps
      },
      secondaryButtonProps?.children || "Cancel"
    )), /* @__PURE__ */ React.createElement(
      LoadingButton_default,
      {
        type: "submit",
        loading: isLoading ?? context2.isLoading,
        className: cn("grow", context2.dialogMode === "View" && "hidden", primaryButtonProps?.className),
        ...primaryButtonProps
      },
      primaryButtonProps?.children || (dialogMode ?? (getDialogModeText(context2.dialogMode) || "Create"))
    ))));
  };
  var PrimeDialog = ({ open, closeOnSuccess, dialogMode, onOpenChange, children, ...props }) => {
    const [internalOpen, setInternalOpen] = (0, import_react30.useState)(open ?? false);
    const onInternalOpenChange = (open2) => {
      setInternalOpen((prevState) => !prevState);
      onOpenChange?.(!open2);
    };
    (0, import_react30.useEffect)(() => {
      if (closeOnSuccess) setInternalOpen(false);
    }, [closeOnSuccess]);
    return /* @__PURE__ */ React.createElement(PrimeDialogContext.Provider, { value: { open: internalOpen, onOpenChange: onInternalOpenChange, dialogMode, ...props } }, /* @__PURE__ */ React.createElement(Dialog_default, { open: internalOpen, onOpenChange: onInternalOpenChange, ...props }, children));
  };
  PrimeDialog.Trigger = Trigger;
  PrimeDialog.Panel = Panel;
  PrimeDialog.Header = Header;
  PrimeDialog.Title = Title2;
  PrimeDialog.Description = Description2;
  PrimeDialog.Content = Content;
  PrimeDialog.Actions = Actions2;
  var PrimeDialog_default = PrimeDialog;

  // src/components/shared/prime-dropdown/index.ts
  init_define_import_meta_env();

  // src/components/shared/prime-dropdown/PrimeDropdown.tsx
  init_define_import_meta_env();
  var import_react31 = __toESM(require_react_shim(), 1);
  var primeDropdownItemVariants = cva("", {
    variants: {
      variant: {
        primary: "",
        destructive: "text-destructive hover:bg-destructive dark:hover:bg-destructive-400 hover:not-disabled:text-destructive-foreground"
      }
    },
    defaultVariants: { variant: "primary" }
  });
  var Trigger2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(DropdownMenu_default.Trigger, { className: cn(className), ...props });
  var List2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(DropdownMenu_default.Content, { className: cn(className), ...props });
  var Label = ({ className, ...props }) => /* @__PURE__ */ React.createElement(DropdownMenu_default.Label, { className: cn(className), ...props });
  var BaseItem = ({ className, icon, variant, children, ...props }) => /* @__PURE__ */ React.createElement(DropdownMenu_default.Item, { className: cn(primeDropdownItemVariants({ variant }), className), ...props }, icon && /* @__PURE__ */ React.createElement("span", { className: "flex shrink-0 items-center" }, icon), children);
  var Separator = ({ className, ...props }) => /* @__PURE__ */ React.createElement(DropdownMenu_default.Separator, { className: cn(className), ...props });
  var PrimeDropdown = ({ align, side, sideOffset, className, children, ...rootProps }) => {
    const childArray = import_react31.Children.toArray(children);
    const triggerChild = childArray.find((child) => (0, import_react31.isValidElement)(child) && child.type === PrimeDropdown.Trigger);
    const listChild = childArray.find((child) => (0, import_react31.isValidElement)(child) && child.type === PrimeDropdown.List);
    const menuItems = childArray.filter(
      (child) => !((0, import_react31.isValidElement)(child) && (child.type === PrimeDropdown.Trigger || child.type === PrimeDropdown.List))
    );
    return /* @__PURE__ */ React.createElement(DropdownMenu_default, { ...rootProps }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!triggerChild }, triggerChild), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(DropdownMenu_default.Trigger, { asChild: true }, childArray[0]))), /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!listChild }, listChild), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(DropdownMenu_default.Content, { align, side, sideOffset, className: cn(className) }, triggerChild ? menuItems : childArray.slice(1)))));
  };
  var ComposedPrimeDropdownItem = withConditional_default(withAuth_default(BaseItem));
  PrimeDropdown.Trigger = Trigger2;
  PrimeDropdown.List = List2;
  PrimeDropdown.Label = Label;
  PrimeDropdown.Item = ComposedPrimeDropdownItem;
  PrimeDropdown.Separator = Separator;
  Trigger2.displayName = "PrimeDropdownTrigger";
  List2.displayName = "PrimeDropdownList";
  Label.displayName = "PrimeDropdownLabel";
  BaseItem.displayName = "PrimeDropdownItem";
  Separator.displayName = "PrimeDropdownSeparator";
  var PrimeDropdown_default = PrimeDropdown;

  // src/components/shared/prime-card/index.ts
  init_define_import_meta_env();

  // src/components/shared/prime-card/PrimeCard.tsx
  init_define_import_meta_env();
  var import_react32 = __toESM(require_react_shim(), 1);
  var primeCardVariants = cva("", {
    variants: {
      variant: {
        default: "",
        primary: "bg-primary-15",
        secondary: "bg-secondary-15",
        success: "bg-success-200",
        destructive: "bg-destructive-200",
        warning: "bg-warning-200",
        info: "bg-accent-200",
        outline: "border-2 border-primary bg-transparent transition-colors hover:bg-primary-600 hover:text-primary-foreground dark:hover:bg-primary-300",
        "outline-secondary": "border-2 border-secondary bg-transparent transition-colors hover:bg-secondary-600 hover:text-secondary-foreground",
        glass: "backdrop-blur-sm bg-background/10",
        gradient: "bg-linear-to-br from-primary-25 to-secondary-25"
      }
    },
    defaultVariants: {
      variant: "default"
    }
  });
  var PrimeCardHeader = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Card_default.Header, { className: cn("bg-background border-primary-25 rounded-ss-[inherit] rounded-se-[inherit] border-b", className), ...props });
  var PrimeCardHeaderLeading = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "prime-card-header-leading",
      className: cn("grid grid-cols-[auto_1fr] items-center gap-x-2", "*:data-[slot=card-description]:col-start-2", className),
      ...props
    },
    children
  );
  var PrimeCardTitle = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Card_default.Title, { className: cn("flex items-center gap-2", className), ...props });
  var PrimeCardTooltip = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(PrimeTooltip_default, { align: "start", ...props }, /* @__PURE__ */ React.createElement(PrimeTooltip_default.Trigger, { type: "button" }, /* @__PURE__ */ React.createElement(Info, { size: 20, className: "text-muted-400 inline-block cursor-help" })), /* @__PURE__ */ React.createElement(PrimeTooltip_default.Item, null, children));
  };
  var PrimeCardDescription = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Card_default.Description, { className: cn(className), ...props });
  var PrimeCardContent = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Card_default.Content, { className: cn(className), ...props });
  var PrimeCard = ({ className, variant, title, icon, tooltip, description, children, ...props }) => {
    const childArray = import_react32.Children.toArray(children);
    const headerChild = childArray.find((child) => (0, import_react32.isValidElement)(child) && child.type === PrimeCardHeader);
    const contentChild = childArray.find((child) => (0, import_react32.isValidElement)(child) && child.type === PrimeCardContent);
    const otherChildren = childArray.filter(
      (child) => !((0, import_react32.isValidElement)(child) && (child.type === PrimeCardHeader || child.type === PrimeCardContent))
    );
    return /* @__PURE__ */ React.createElement(Card_default, { className: cn(primeCardVariants({ variant }), className), ...props }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!headerChild }, headerChild), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(PrimeCardHeader, null, /* @__PURE__ */ React.createElement(PrimeCardHeaderLeading, { className: cn(!icon && "contents") }, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!icon }, /* @__PURE__ */ React.createElement("span", { className: "text-primary-900" }, icon)), /* @__PURE__ */ React.createElement(PrimeCardTitle, null, title, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!tooltip }, /* @__PURE__ */ React.createElement(PrimeCardTooltip, null, tooltip))), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!description }, /* @__PURE__ */ React.createElement(PrimeCardDescription, null, description)))))), /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!contentChild }, contentChild), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(PrimeCardContent, null, otherChildren))));
  };
  PrimeCard.Header = PrimeCardHeader;
  PrimeCard.Leading = PrimeCardHeaderLeading;
  PrimeCard.Title = PrimeCardTitle;
  PrimeCard.Tooltip = PrimeCardTooltip;
  PrimeCard.Description = PrimeCardDescription;
  PrimeCard.Content = PrimeCardContent;
  var PrimeCard_default = PrimeCard;

  // src/components/shared/prime-tooltip/index.ts
  init_define_import_meta_env();

  // src/components/shared/prime-tooltip/PrimeTooltip.tsx
  init_define_import_meta_env();
  var import_react33 = __toESM(require_react_shim(), 1);
  var Trigger3 = ({ ref, className, ...props }) => /* @__PURE__ */ React.createElement(
    Tooltip_default.Trigger,
    {
      ref,
      className: cn(className),
      onClick: (event) => event.preventDefault(),
      onPointerDown: (event) => event.preventDefault(),
      ...props
    }
  );
  var Item = ({ className, icon, children, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "prime-tooltip-item",
      className: cn("border-primary-400 flex items-center gap-2 border-s-2 ps-2 font-medium", className),
      ...props
    },
    icon && /* @__PURE__ */ React.createElement("span", { className: "flex items-center" }, icon),
    /* @__PURE__ */ React.createElement("span", { className: "flex-1" }, children)
  );
  var PrimeTooltip = ({
    content,
    children,
    className,
    open,
    defaultOpen,
    onOpenChange,
    delayDuration,
    disableHoverableContent,
    ...contentProps
  }) => {
    const childArray = import_react33.Children.toArray(children);
    const triggerChild = childArray.find((child) => (0, import_react33.isValidElement)(child) && child.type === PrimeTooltip.Trigger);
    const otherChildren = childArray.filter((child) => !((0, import_react33.isValidElement)(child) && child.type === PrimeTooltip.Trigger));
    const tooltipContent = triggerChild ? otherChildren.length > 0 ? otherChildren : content : content;
    return /* @__PURE__ */ React.createElement(Tooltip_default.Provider, null, /* @__PURE__ */ React.createElement(
      Tooltip_default,
      {
        open,
        defaultOpen,
        onOpenChange,
        delayDuration,
        disableHoverableContent
      },
      /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!triggerChild }, triggerChild), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Tooltip_default.Trigger, { asChild: true }, children))),
      /* @__PURE__ */ React.createElement(
        Tooltip_default.Content,
        {
          className: cn("grid gap-y-1 md:max-w-[30dvw]", className),
          hidden: !tooltipContent,
          onPointerDownOutside: (event) => event.preventDefault(),
          ...contentProps
        },
        tooltipContent
      )
    ));
  };
  PrimeTooltip.Trigger = (0, import_react33.forwardRef)((props, ref) => Trigger3({ ...props, ref }));
  PrimeTooltip.Item = Item;
  PrimeTooltip.Trigger.displayName = "PrimeTooltipTrigger";
  Item.displayName = "PrimeTooltipItem";
  var PrimeTooltip_default = PrimeTooltip;

  // src/components/shared/meta-card/index.ts
  init_define_import_meta_env();

  // src/components/shared/meta-card/MetaCard.tsx
  init_define_import_meta_env();
  var metaCardVariants = cva("flex gap-1", {
    variants: {
      direction: {
        vertical: "flex-col",
        horizontal: "flex-wrap items-center text-start"
      }
    },
    defaultVariants: {
      direction: "vertical"
    }
  });
  var metaCardHeaderVariants = cva("p-0", {
    variants: {
      direction: {
        vertical: "",
        horizontal: "flex-1"
      }
    },
    defaultVariants: {
      direction: "vertical"
    }
  });
  var MetaCardLoader = () => /* @__PURE__ */ React.createElement("div", { className: "flex w-full flex-col space-y-2" }, /* @__PURE__ */ React.createElement(Skeleton_default, null), /* @__PURE__ */ React.createElement(Skeleton_default, { size: "md" }));
  var MetaCard = ({
    className,
    title,
    subtitle,
    description,
    imageURL,
    isLoading,
    direction,
    truncate,
    children,
    ...props
  }) => {
    if (isLoading) return /* @__PURE__ */ React.createElement(MetaCardLoader, null);
    return /* @__PURE__ */ React.createElement(Card_default, { shadow: "none", className: cn("max-w-full", metaCardVariants({ direction }), className), ...props }, /* @__PURE__ */ React.createElement(Card_default.Header, { className: cn(metaCardHeaderVariants({ direction })) }, /* @__PURE__ */ React.createElement(Card_default.Title, { className: "text-muted-400 text-sm font-normal" }, title), /* @__PURE__ */ React.createElement(Card_default.Description, null, subtitle)), /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!truncate }, /* @__PURE__ */ React.createElement(PrimeTooltip_default, { content: description }, /* @__PURE__ */ React.createElement("div", { className: "truncate p-0 text-sm font-medium" }, description ?? "-"))), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Card_default.Content, { className: "p-0 text-sm font-medium" }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!imageURL }, /* @__PURE__ */ React.createElement("img", { className: "block object-contain", src: imageURL, alt: title })), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!description }, description), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!children }, children), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, "-"))))));
  };
  var MetaCard_default = MetaCard;

  // src/components/shared/collapsible-card/index.ts
  init_define_import_meta_env();

  // src/components/shared/collapsible-card/CollapsibleCard.tsx
  init_define_import_meta_env();
  var import_react34 = __toESM(require_react_shim(), 1);
  var collapsibleCardHeaderVariants = cva("", {
    variants: {
      variant: {
        default: "bg-background border-primary-25",
        primary: "bg-background border-primary-25",
        secondary: "bg-background border-primary-25",
        success: "bg-background border-primary-25",
        destructive: "bg-background border-primary-25",
        warning: "bg-background border-primary-25",
        info: "bg-background border-primary-25",
        outline: "bg-background border-primary-25",
        "outline-secondary": "bg-background border-primary-25",
        glass: "bg-background border-primary-25",
        gradient: "bg-background border-primary-25"
      }
    },
    defaultVariants: {
      variant: "default"
    }
  });
  var collapsibleCardVariants = cva("", {
    variants: {
      variant: {
        default: "",
        primary: "bg-primary-15",
        secondary: "bg-secondary-15",
        success: "bg-success-200",
        destructive: "bg-destructive-200",
        warning: "bg-warning-200",
        info: "bg-accent-200",
        outline: "border-2 border-primary bg-transparent transition-colors hover:bg-primary-600 hover:text-primary-foreground dark:hover:bg-primary-300",
        "outline-secondary": "border-2 border-secondary bg-transparent transition-colors hover:bg-secondary-600 hover:text-secondary-foreground",
        glass: "backdrop-blur-sm bg-background/10",
        gradient: "bg-linear-to-br from-primary-25 to-secondary-25"
      }
    },
    defaultVariants: {
      variant: "default"
    }
  });
  var CollapsibleCardContext = (0, import_react34.createContext)({ variant: "default" });
  var useCollapsibleCardContext = () => {
    const context2 = (0, import_react34.useContext)(CollapsibleCardContext);
    if (!context2) throw new Error("CollapsibleCard subcomponents must be used within <CollapsibleCard>");
    return context2;
  };
  var CollapsibleCardHeader = ({ variant, className, children, ...props }) => {
    const context2 = useCollapsibleCardContext();
    return /* @__PURE__ */ React.createElement(
      Card_default.Header,
      {
        className: cn(
          "flex items-center justify-between transition-all duration-75",
          "rounded-t-[inherit]",
          "group-data-[state=open]/collapsible-card:border-b group-data-[state=closed]/collapsible-card:[&:has(~[data-slot=card-footer])]:border-b group-data-[state=closed]/collapsible-card:[&:not(:has(~[data-slot=card-footer]))]:border-b-0",
          "group-data-[state=closed]/collapsible-card:[&:not(:has(~[data-slot=card-footer]))]:rounded-b-[inherit]",
          collapsibleCardHeaderVariants({ variant: variant ?? context2.variant }),
          className
        ),
        ...props
      },
      children
    );
  };
  var CollapsibleCardHeaderLeading = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "collapsible-card-header-leading",
      className: cn("grid grid-cols-[auto_1fr] items-center gap-x-2", "*:data-[slot=card-description]:col-start-2", className),
      ...props
    },
    children
  );
  var CollapsibleCardTitle = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Card_default.Title, { className: cn("flex items-center gap-2", className), ...props }, children);
  var CollapsibleCardTrigger = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Collapsible_default.Trigger, { className: cn("group/collapsible-trigger transition-all hover:opacity-80", className), ...props }, children);
  var CollapsibleCardTriggerIcon = ({ className, children, ...props }) => {
    return children ?? /* @__PURE__ */ React.createElement(
      ChevronDown,
      {
        size: 16,
        className: cn("shrink-0 transition-transform duration-200 group-data-[state=open]/collapsible-trigger:rotate-180", className),
        ...props
      }
    );
  };
  var CollapsibleCardTooltip = ({ children, ...props }) => /* @__PURE__ */ React.createElement(PrimeTooltip_default, { align: "start", ...props }, /* @__PURE__ */ React.createElement(PrimeTooltip_default.Trigger, { type: "button" }, /* @__PURE__ */ React.createElement(Info, { size: 20, className: "text-muted-400 inline-block cursor-help" })), /* @__PURE__ */ React.createElement(PrimeTooltip_default.Item, null, children));
  var CollapsibleCardDescription = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Card_default.Description, { className: cn(className), ...props }, children);
  var CollapsibleCardContent = ({ variant, className, children, ...props }) => /* @__PURE__ */ React.createElement(Collapsible_default.Content, null, /* @__PURE__ */ React.createElement(Card_default.Content, { className: cn(className), ...props }, children));
  var CollapsibleCardFooter = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Card_default.Footer, { className: cn(className), ...props }, children);
  var CollapsibleCard = ({
    open,
    onOpenChange,
    defaultOpen = true,
    disabled,
    className,
    variant,
    children,
    ...props
  }) => {
    return /* @__PURE__ */ React.createElement(CollapsibleCardContext.Provider, { value: { variant } }, /* @__PURE__ */ React.createElement(Collapsible_default, { className: "group/collapsible-card", open, defaultOpen, disabled, onOpenChange }, /* @__PURE__ */ React.createElement(Card_default, { className: cn(collapsibleCardVariants({ variant }), className), ...props }, children)));
  };
  CollapsibleCard.Header = CollapsibleCardHeader;
  CollapsibleCard.Leading = CollapsibleCardHeaderLeading;
  CollapsibleCard.Title = CollapsibleCardTitle;
  CollapsibleCard.Trigger = CollapsibleCardTrigger;
  CollapsibleCard.TriggerIcon = CollapsibleCardTriggerIcon;
  CollapsibleCard.Tooltip = CollapsibleCardTooltip;
  CollapsibleCard.Description = CollapsibleCardDescription;
  CollapsibleCard.Content = CollapsibleCardContent;
  CollapsibleCard.Footer = CollapsibleCardFooter;
  var CollapsibleCard_default = CollapsibleCard;

  // src/components/shared/count-tooltip/index.ts
  init_define_import_meta_env();

  // src/components/shared/count-tooltip/CountTooltip.tsx
  init_define_import_meta_env();
  var import_react35 = __toESM(require_react_shim(), 1);
  var CountTooltipContext = (0, import_react35.createContext)(null);
  var useCountTooltipContext = () => {
    const context2 = (0, import_react35.useContext)(CountTooltipContext);
    if (!context2) throw new Error("CountTooltip sub-components must be used within CountTooltip");
    return context2;
  };
  var Trigger4 = ({ ref, className, children, ...props }) => {
    const { count: count4 } = useCountTooltipContext();
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        ref,
        "data-slot": "count-tooltip-trigger",
        className: cn("bg-muted-100 inline-flex items-center justify-center rounded-full px-1.5 text-xs", className),
        ...props
      },
      children ?? `+${count4}`
    );
  };
  var Item2 = (props) => /* @__PURE__ */ React.createElement(PrimeTooltip_default.Item, { ...props });
  var CountTooltip = ({ count: count4, condition = true, children, ...props }) => {
    const childArray = import_react35.Children.toArray(children);
    const triggerChild = childArray.find((child) => (0, import_react35.isValidElement)(child) && child.type === CountTooltip.Trigger);
    const otherChildren = childArray.filter((child) => !((0, import_react35.isValidElement)(child) && child.type === CountTooltip.Trigger));
    if (!condition) return null;
    return /* @__PURE__ */ React.createElement(CountTooltipContext.Provider, { value: { count: count4 } }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!triggerChild }, /* @__PURE__ */ React.createElement(PrimeTooltip_default, { ...props }, /* @__PURE__ */ React.createElement(PrimeTooltip_default.Trigger, null, triggerChild), otherChildren)), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(PrimeTooltip_default, { content: childArray.length > 1 ? childArray.slice(1) : void 0, ...props }, childArray[0]))));
  };
  CountTooltip.Trigger = (0, import_react35.forwardRef)((props, ref) => Trigger4({ ...props, ref }));
  CountTooltip.Item = Item2;
  CountTooltip.Trigger.displayName = "CountTooltipTrigger";
  Item2.displayName = "CountTooltipItem";
  var CountTooltip_default = CountTooltip;

  // src/components/shared/confirm-dialog/index.ts
  init_define_import_meta_env();

  // src/components/shared/confirm-dialog/ConfirmDialog.tsx
  init_define_import_meta_env();
  var import_react36 = __toESM(require_react_shim(), 1);
  var confirmDialogVariantToIconsMapping = {
    warning: TriangleAlert,
    success: CircleCheckBig,
    destructive: CircleX,
    info: Info
  };
  var confirmDialogVariantToButtonVariantMapping = {
    destructive: "destructive",
    success: "success",
    warning: "default",
    info: "default"
  };
  var confirmDialogIconVariants = cva("mb-4", {
    variants: {
      variant: {
        warning: "text-warning",
        success: "text-success",
        destructive: "text-destructive",
        info: "text-primary"
      }
    },
    defaultVariants: {
      variant: "destructive"
    }
  });
  var confirmDialogVariants = cva("border-t-4", {
    variants: {
      variant: {
        warning: "border-t-warning",
        success: "border-t-success",
        destructive: "border-t-destructive",
        info: "border-t-primary"
      }
    },
    defaultVariants: {
      variant: "destructive"
    }
  });
  function ConfirmDialog({
    open,
    onOpenChange,
    variant = "destructive",
    primaryVariant,
    title,
    description,
    confirmLabel,
    cancelLabel,
    children,
    className,
    loading,
    dismissible = false,
    onConfirm
  }) {
    const { t: t3 } = useAppTranslation("common");
    const resolvedConfirmLabel = confirmLabel ?? t3("confirmDialog.confirm");
    const resolvedCancelLabel = cancelLabel ?? t3("confirmDialog.cancel");
    const [internalOpen, setInternalOpen] = (0, import_react36.useState)(false);
    const [internalLoading, setInternalLoading] = (0, import_react36.useState)(false);
    const isControlled = open !== void 0;
    const isOpen = isControlled ? open : internalOpen;
    const isLoading = loading ?? internalLoading;
    const Icon5 = confirmDialogVariantToIconsMapping[variant];
    const childrenArray = import_react36.Children.toArray(children);
    const trigger = isControlled ? null : childrenArray[0];
    const content = isControlled ? childrenArray[0] : childrenArray[1];
    const actions = isControlled ? childrenArray[1] : childrenArray[2];
    const setOpen = (0, import_react36.useCallback)(
      (value) => {
        if (!isControlled) setInternalOpen(value);
        onOpenChange?.(value);
      },
      [isControlled, onOpenChange]
    );
    const handleConfirm = (0, import_react36.useCallback)(async () => {
      if (!onConfirm) {
        setOpen(false);
        return;
      }
      const result = onConfirm();
      if (loading !== void 0) return;
      if (result && typeof result.then === "function") {
        setInternalLoading(true);
        try {
          await result;
          setOpen(false);
        } finally {
          setInternalLoading(false);
        }
      } else {
        setOpen(false);
      }
    }, [onConfirm, loading, setOpen]);
    return /* @__PURE__ */ React.createElement(Dialog_default, { open: isOpen, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !isControlled && !!trigger }, /* @__PURE__ */ React.createElement(Dialog_default.Trigger, { asChild: true }, trigger)), /* @__PURE__ */ React.createElement(
      Dialog_default.Panel,
      {
        className: cn(confirmDialogVariants({ variant }), className),
        onInteractOutside: (e2) => !dismissible && e2.preventDefault(),
        onEscapeKeyDown: (e2) => !dismissible && e2.preventDefault(),
        "aria-describedby": void 0
      },
      /* @__PURE__ */ React.createElement(
        Dialog_default.Close,
        {
          "aria-label": t3("close"),
          className: "absolute inset-e-4 top-4 cursor-pointer rounded-sm opacity-70 transition-opacity hover:opacity-100 focus:outline-none"
        },
        /* @__PURE__ */ React.createElement(X, { size: 18 })
      ),
      /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!title }, /* @__PURE__ */ React.createElement(Dialog_default.Header, { className: "flex flex-col items-center gap-y-1.5 text-center" }, /* @__PURE__ */ React.createElement(Icon5, { className: confirmDialogIconVariants({ variant }), size: 48 }), /* @__PURE__ */ React.createElement(Dialog_default.Title, null, title), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!description }, /* @__PURE__ */ React.createElement(Dialog_default.Description, null, description)))),
      /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!content }, /* @__PURE__ */ React.createElement(Dialog_default.Content, { className: "flex flex-col items-center" }, content)),
      /* @__PURE__ */ React.createElement(Dialog_default.Footer, { className: "space-x-2 text-center" }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!actions }, actions), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: variant !== "success" && variant !== "info" }, /* @__PURE__ */ React.createElement(Dialog_default.Close, { asChild: true }, /* @__PURE__ */ React.createElement(LoadingButton_default, { variant: "muted", size: "lg" }, resolvedCancelLabel))), /* @__PURE__ */ React.createElement(
        LoadingButton_default,
        {
          variant: primaryVariant ?? confirmDialogVariantToButtonVariantMapping[variant],
          size: "lg",
          loading: isLoading,
          onClick: handleConfirm
        },
        resolvedConfirmLabel
      ))))
    ));
  }
  var ConfirmDialog_default = ConfirmDialog;

  // src/components/shared/faceted-filter/index.ts
  init_define_import_meta_env();

  // src/components/shared/faceted-filter/FacetedFilter.tsx
  init_define_import_meta_env();

  // src/components/forms/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-container/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-container/FormContainer.tsx
  init_define_import_meta_env();
  function FormContainer({
    children,
    formContext,
    onSuccess,
    ...props
  }) {
    return /* @__PURE__ */ React.createElement(FormProvider, { ...formContext }, /* @__PURE__ */ React.createElement("form", { autoComplete: "new-password", noValidate: true, ...props, onSubmit: formContext.handleSubmit(onSuccess) }, children));
  }
  var FormContainer_default = FormContainer;

  // src/components/forms/form-control/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-control/FormControl.tsx
  init_define_import_meta_env();
  var FormControl = ({ className, ...props }) => {
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "form-control", className: cn("relative space-y-2", className), ...props });
  };
  var FormControl_default = FormControl;

  // src/components/forms/form-label/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-label/FormLabel.tsx
  init_define_import_meta_env();

  // node_modules/@radix-ui/react-label/dist/index.mjs
  init_define_import_meta_env();
  var React14 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-label/node_modules/@radix-ui/react-primitive/dist/index.mjs
  init_define_import_meta_env();
  var React13 = __toESM(require_react_shim(), 1);
  var ReactDOM2 = __toESM(require_react_dom_shim(), 1);
  var import_jsx_runtime3 = __toESM(require_react_shim(), 1);
  var NODES = [
    "a",
    "button",
    "div",
    "form",
    "h2",
    "h3",
    "img",
    "input",
    "label",
    "li",
    "nav",
    "ol",
    "p",
    "select",
    "span",
    "svg",
    "ul"
  ];
  var Primitive = NODES.reduce((primitive, node) => {
    const Slot6 = createSlot(`Primitive.${node}`);
    const Node2 = React13.forwardRef((props, forwardedRef) => {
      const { asChild, ...primitiveProps } = props;
      const Comp = asChild ? Slot6 : node;
      if (typeof window !== "undefined") {
        window[/* @__PURE__ */ Symbol.for("radix-ui")] = true;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(Comp, { ...primitiveProps, ref: forwardedRef });
    });
    Node2.displayName = `Primitive.${node}`;
    return { ...primitive, [node]: Node2 };
  }, {});

  // node_modules/@radix-ui/react-label/dist/index.mjs
  var import_jsx_runtime4 = __toESM(require_react_shim(), 1);
  var NAME = "Label";
  var Label2 = React14.forwardRef((props, forwardedRef) => {
    return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
      Primitive.label,
      {
        ...props,
        ref: forwardedRef,
        onMouseDown: (event) => {
          const target = event.target;
          if (target.closest("button, input, select, textarea")) return;
          props.onMouseDown?.(event);
          if (!event.defaultPrevented && event.detail > 1) event.preventDefault();
        }
      }
    );
  });
  Label2.displayName = NAME;
  var Root = Label2;

  // src/components/forms/form-label/FormLabel.tsx
  var labelVariants = cva(
    "text-muted-foreground inline-block text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70"
  );
  var FormLabel = ({ className, error: error2, children, tooltip, required, hidden, ...props }) => {
    if (hidden) return null;
    return /* @__PURE__ */ React.createElement(Root, { "data-slot": "form-label", className: cn(labelVariants(), error2 && "text-destructive", className), ...props }, children, required && /* @__PURE__ */ React.createElement(Asterisk, { className: "ms-0.5 inline-block align-text-top text-inherit", size: 10 }), tooltip && /* @__PURE__ */ React.createElement(PrimeTooltip_default, { align: "start" }, /* @__PURE__ */ React.createElement(PrimeTooltip_default.Trigger, { type: "button" }, /* @__PURE__ */ React.createElement(Info, { size: 16, className: "text-muted-400 ms-1 inline-block cursor-help" })), /* @__PURE__ */ React.createElement(PrimeTooltip_default.Item, { onClick: (e2) => e2.preventDefault() }, tooltip)));
  };
  FormLabel.displayName = "FormLabel";
  var FormLabel_default = FormLabel;

  // src/components/forms/form-message/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-message/FormMessage.tsx
  init_define_import_meta_env();
  var FormMessage = ({ className, errorType = "tooltip", children, ...props }) => {
    const body = props.error ? String(props.error?.message) : children;
    if (!body) return null;
    return /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: errorType === "inline" }, /* @__PURE__ */ React.createElement("p", { className: cn("text-destructive text-sm", className), ...props }, body)), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(ErrorTooltip_default, { message: body, className })));
  };
  var FormMessage_default = FormMessage;

  // src/components/forms/form-input/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-input/FormInput.tsx
  init_define_import_meta_env();
  var import_react37 = __toESM(require_react_shim(), 1);
  function FormInput({
    name,
    label,
    tooltip,
    className,
    containerClassName,
    labelClassName,
    errorClassName,
    required,
    rules,
    ...props
  }) {
    const { register } = useFormContext();
    const { fieldState } = useController({ name });
    const { error: error2 } = fieldState;
    const id = `${name}-${(0, import_react37.useId)()}`;
    return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: id, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(
      Input_default,
      {
        id,
        autoComplete: "off",
        className: cn(
          error2 && "border-destructive hover:not-disabled:border-destructive hover:not-disabled:ring-destructive focus-visible:border-destructive focus-visible:ring-destructive placeholder:text-destructive ps-8",
          className
        ),
        ...register(name, rules),
        ...props
      }
    ), /* @__PURE__ */ React.createElement(FormMessage_default, { className: errorClassName, hidden: !error2, error: error2 })));
  }
  var FormInput_default = FormInput;

  // src/components/forms/form-select/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-select/FormSelect.tsx
  init_define_import_meta_env();
  var import_react38 = __toESM(require_react_shim(), 1);
  function FormSelect({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    options: options3,
    placeholder = "Select",
    noOptionsText = "No Options",
    valueType = "flat",
    clearable = true,
    getOptionLabel,
    getOptionValue,
    optionMapper,
    ...props
  }) {
    const mappedOptions = options3.map((option) => optionMapper ? optionMapper(option) : option);
    const findOptionByValue = (value) => {
      return options3.find((option) => getOptionValue(option) === value);
    };
    return /* @__PURE__ */ React.createElement(
      Controller,
      {
        name,
        control,
        rules,
        render: ({ field: { ref, value, onChange, ...field }, fieldState: { error: error2 } }) => {
          const internalValue = valueType === "contain" && value !== null ? getOptionValue(value) : value;
          const onValueChange = (selectedValue) => {
            onChange(valueType === "contain" ? findOptionByValue(selectedValue) : selectedValue);
          };
          const onClear = () => {
            onChange(null);
          };
          return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: name, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(Select_default, { value: internalValue ?? "", onValueChange, ...field, ...props }, /* @__PURE__ */ React.createElement(
            Select_default.Trigger,
            {
              ref,
              id: name,
              className: cn(
                !!error2 && "border-destructive hover:not-disabled:border-destructive hover:not-disabled:ring-destructive focus-visible:border-destructive focus-visible:ring-destructive text-destructive ps-8",
                className
              ),
              disabled: field.disabled || props.disabled
            },
            /* @__PURE__ */ React.createElement(Select_default.Value, { placeholder }),
            /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!internalValue && !field.disabled && !props.disabled && clearable }, /* @__PURE__ */ React.createElement(
              TooltipButton_default,
              {
                asChild: true,
                title: "Clear",
                variant: "ghost-muted-destructive",
                size: "unstyled",
                onPointerDown: (e2) => e2.stopPropagation(),
                onClick: (e2) => {
                  e2.stopPropagation();
                  e2.preventDefault();
                  onClear();
                }
              },
              /* @__PURE__ */ React.createElement(X, { size: 16 })
            )),
            /* @__PURE__ */ React.createElement(Select_default.Icon, null)
          ), /* @__PURE__ */ React.createElement(Select_default.Content, { position: "popper" }, /* @__PURE__ */ React.createElement(Select_default.Item, { className: cn(mappedOptions.length > 0 && "hidden"), "aria-hidden": "true", value: "none", disabled: true }, /* @__PURE__ */ React.createElement(Select_default.Text, null, noOptionsText)), mappedOptions.map((option) => /* @__PURE__ */ React.createElement(import_react38.Fragment, { key: getOptionValue(option) }, /* @__PURE__ */ React.createElement(Select_default.Item, { value: getOptionValue(option) }, /* @__PURE__ */ React.createElement(Select_default.Text, null, getOptionLabel(option))))))), /* @__PURE__ */ React.createElement(FormMessage_default, { className: errorClassName, hidden: !error2, error: error2 })));
        }
      }
    );
  }
  FormSelect.displayName = "FormSelect";
  var FormSelect_default = FormSelect;

  // src/components/forms/form-checkbox/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-checkbox/FormCheckbox.tsx
  init_define_import_meta_env();
  var import_react39 = __toESM(require_react_shim(), 1);
  function FormCheckbox({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    disabled,
    checkboxClassName,
    onChange,
    ...props
  }) {
    const id = `${name}-${(0, import_react39.useId)()}`;
    return /* @__PURE__ */ React.createElement(
      Controller,
      {
        name,
        control,
        rules,
        render: ({ field, fieldState: { error: error2 } }) => /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: id, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement(
          "label",
          {
            htmlFor: id,
            className: cn(
              "border-muted-200 bg-background relative flex w-full cursor-pointer items-center gap-3 rounded-md border p-3 transition-[color,box-shadow]",
              !!field.value && "bg-secondary-100 border-secondary-200",
              !disabled && "hover:border-primary hover:ring-primary focus-visible:ring-primary focus-visible:border-primary hover:ring focus-visible:ring focus-visible:outline-none",
              !disabled && !!field.value && "hover:border-secondary-200 hover:ring-secondary-200 focus-visible:border-secondary-200 focus-visible:ring-secondary-200",
              disabled && "bg-muted-50 text-muted-300 pointer-events-none",
              disabled && !!field.value && "border-muted-200",
              error2 && "border-destructive text-destructive",
              !disabled && error2 && "hover:border-destructive hover:ring-destructive focus-visible:ring-destructive",
              className
            )
          },
          /* @__PURE__ */ React.createElement("span", { className: "flex flex-1 items-center text-sm font-normal" }, label),
          /* @__PURE__ */ React.createElement(
            Checkbox_default,
            {
              id,
              ref: field.ref,
              name: field.name,
              className: cn(
                error2 && "border-destructive hover:not-disabled:border-destructive hover:not-disabled:ring-destructive",
                checkboxClassName
              ),
              checked: !!field.value,
              disabled: field.disabled || disabled,
              onCheckedChange: (checked) => {
                field.onChange(checked);
                onChange?.(checked);
              },
              onBlur: field.onBlur,
              ...props
            }
          ),
          /* @__PURE__ */ React.createElement(FormMessage_default, { className: cn("inset-s-auto inset-e-9", errorClassName), hidden: !error2, error: error2 })
        ))
      }
    );
  }
  FormCheckbox.displayName = "FormCheckbox";
  var FormCheckbox_default = FormCheckbox;

  // src/components/forms/form-textarea/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-textarea/FormTextarea.tsx
  init_define_import_meta_env();
  var import_react40 = __toESM(require_react_shim(), 1);
  function FormTextarea({
    name,
    label,
    tooltip,
    className,
    containerClassName,
    labelClassName,
    errorClassName,
    required,
    rules,
    ...props
  }) {
    const { register } = useFormContext();
    const { fieldState } = useController({ name });
    const { error: error2 } = fieldState;
    const id = `${name}-${(0, import_react40.useId)()}`;
    return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: id, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(
      "textarea",
      {
        id,
        "data-slot": "textarea",
        className: cn(
          "border-muted-200 bg-background flex field-sizing-content w-full resize-y rounded-md border p-3 text-sm shadow-xs transition-[color,box-shadow] outline-none",
          "placeholder:text-muted-foreground",
          "hover:not-disabled:border-primary hover:not-disabled:ring-primary hover:not-disabled:ring",
          "focus-visible:ring-primary focus-visible:border-primary focus-visible:ring",
          "disabled:bg-muted-50 disabled:text-muted-300 disabled:placeholder:text-muted-300 disabled:pointer-events-none disabled:field-sizing-fixed",
          error2 && "border-destructive hover:not-disabled:border-destructive hover:not-disabled:ring-destructive focus-visible:border-destructive focus-visible:ring-destructive placeholder:text-destructive ps-8",
          className
        ),
        ...register(name, rules),
        ...props
      }
    ), /* @__PURE__ */ React.createElement(FormMessage_default, { className: cn("inset-bs-6", errorClassName), hidden: !error2, error: error2 })));
  }
  var FormTextarea_default = FormTextarea;

  // src/components/forms/form-file-uploader/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-file-uploader/FormFileUploader.tsx
  init_define_import_meta_env();
  function FormFileUploader({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    disabled,
    accept,
    multiple = false,
    isUploading,
    placeholder,
    onDownload,
    clearable,
    listVariant = "list",
    showPreview = false
  }) {
    return /* @__PURE__ */ React.createElement(
      Controller,
      {
        name,
        control,
        rules,
        render: ({ field: { ref, value, onChange, onBlur, ...field }, fieldState: { error: error2 } }) => /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(
          FormLabel_default,
          {
            className: labelClassName,
            htmlFor: `${name}-file-upload`,
            tooltip,
            required,
            hidden: !label,
            error: error2
          },
          label
        ), /* @__PURE__ */ React.createElement(
          FileUploader_default,
          {
            name,
            value,
            onChange,
            accept,
            multiple,
            disabled: field.disabled || disabled,
            isUploading,
            className: cn(
              error2 && "**:data-[slot=file-uploader-dropzone-group]:border-destructive **:data-[slot=file-uploader-dropzone-group]:text-destructive **:data-[slot=file-uploader-dropzone-group]:hover:border-destructive",
              className
            )
          },
          /* @__PURE__ */ React.createElement(FileUploader_default.DropzoneGroup, null, /* @__PURE__ */ React.createElement(FileUploader_default.Dropzone, { ref, onBlur, placeholder: placeholder ?? label }), /* @__PURE__ */ React.createElement(FileUploader_default.Actions, { onDownload, clearable }), /* @__PURE__ */ React.createElement(FormMessage_default, { className: errorClassName, hidden: !error2, error: error2 })),
          /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: multiple && !showPreview }, /* @__PURE__ */ React.createElement(FileUploader_default.FileList, { variant: listVariant })),
          /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: showPreview }, /* @__PURE__ */ React.createElement(FileUploader_default.ImagePreview, null))
        ))
      }
    );
  }
  FormFileUploader.displayName = "FormFileUploader";
  var FormFileUploader_default = FormFileUploader;

  // src/components/forms/form-combobox/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-combobox/FormCombobox.tsx
  init_define_import_meta_env();
  var import_react41 = __toESM(require_react_shim(), 1);
  function FormCombobox({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    disabled,
    options: options3,
    getOptionLabel,
    getOptionValue,
    getOptionDisabled,
    optionMapper,
    onOptionChange,
    valueType = "flat",
    placeholder = "Select...",
    noOptionsText = "No options",
    urlSearchParams,
    debounceMs = 500,
    pageSize = 20
  }) {
    const {
      field: { ref, value, onChange, ...field },
      fieldState: { error: error2 }
    } = useController({ name, control, rules });
    const [open, setOpen] = (0, import_react41.useState)(false);
    const [search, setSearch] = (0, import_react41.useState)("");
    const debouncedSearch = useDebounce(search, debounceMs);
    const isAsync = typeof options3 === "function";
    const stringValue = (() => {
      if (!value) return null;
      if (valueType === "contain") return getOptionValue(value);
      return value;
    })();
    const selectedItemsIds = stringValue ? [stringValue] : [];
    const hasValueToResolve = valueType === "flat" && selectedItemsIds.length > 0;
    const infiniteQuery = useAsyncOptionsInfiniteQuery({
      queryKey: `${label}-${name}-combobox-options`,
      fetchOptions: isAsync ? options3 : () => Promise.resolve({ data: [], pagination: { page: 1, total: 0, totalPages: 0, pageSize } }),
      searchTerm: debouncedSearch,
      selectedItemsIds,
      urlSearchParams,
      pageSize,
      enabled: isAsync && (open || hasValueToResolve)
    });
    const { handleScroll: handleScroll2 } = useInfiniteScroll({ infiniteQuery, enabled: isAsync });
    const allOptions = isAsync ? infiniteQuery.data?.pages.flatMap((p2) => p2.data) ?? [] : options3;
    const mappedOptions = optionMapper ? allOptions.map(optionMapper) : allOptions;
    const selectedOption = (() => {
      if (!stringValue) return void 0;
      if (valueType === "contain" && value) return value;
      return allOptions.find((o) => getOptionValue(o) === stringValue);
    })();
    const onSelectChange = (optValue) => {
      const option = allOptions.find((o) => getOptionValue(o) === optValue);
      if (!option) return;
      if (stringValue === optValue) {
        onChange(null);
        onOptionChange?.(null);
      } else {
        onChange(valueType === "contain" ? option : optValue);
        onOptionChange?.(option);
      }
      setOpen(false);
    };
    const onClear = () => {
      onChange(null);
      onOptionChange?.(null);
    };
    return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: name, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(Combobox_default, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(
      Combobox_default.Trigger,
      {
        ref,
        id: name,
        className: cn(
          error2 && "border-destructive hover:not-disabled:border-destructive hover:not-disabled:ring-destructive focus-visible:border-destructive focus-visible:ring-destructive text-destructive ps-8",
          className
        ),
        disabled: field.disabled || disabled,
        onBlur: field.onBlur
      },
      /* @__PURE__ */ React.createElement(Combobox_default.Value, { className: "truncate", placeholder }, selectedOption ? getOptionLabel(selectedOption) : void 0),
      /* @__PURE__ */ React.createElement(Combobox_default.Clear, { when: !!stringValue && !field.disabled && !disabled, onClear }),
      /* @__PURE__ */ React.createElement(Combobox_default.Icon, null)
    ), /* @__PURE__ */ React.createElement(Combobox_default.Content, { shouldFilter: !isAsync }, /* @__PURE__ */ React.createElement(Combobox_default.Input, { placeholder, ...isAsync && { value: search, onValueChange: setSearch } }, /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: isAsync && (infiniteQuery.isFetching || search !== debouncedSearch) })), /* @__PURE__ */ React.createElement(Combobox_default.List, { onScroll: isAsync ? handleScroll2 : void 0 }, /* @__PURE__ */ React.createElement(
      Combobox_default.Empty,
      {
        when: !isAsync && mappedOptions.length === 0 || isAsync && !infiniteQuery.isFetching && mappedOptions.length === 0
      },
      noOptionsText
    ), mappedOptions.map((option) => {
      const optValue = getOptionValue(option);
      const isChecked = stringValue === optValue;
      return /* @__PURE__ */ React.createElement(
        Combobox_default.Item,
        {
          key: optValue,
          value: getOptionLabel(option),
          disabled: getOptionDisabled?.(option),
          "data-checked": isChecked || void 0,
          onSelect: () => onSelectChange(optValue)
        },
        /* @__PURE__ */ React.createElement(Combobox_default.Indicator, null),
        getOptionLabel(option)
      );
    }), /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: isAsync && infiniteQuery.isFetchingNextPage, message: "Loading more..." })))), /* @__PURE__ */ React.createElement(FormMessage_default, { className: errorClassName, hidden: !error2, error: error2 })));
  }
  FormCombobox.displayName = "FormCombobox";
  var FormCombobox_default = FormCombobox;

  // src/components/forms/form-multi-combobox/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-multi-combobox/FormMultiCombobox.tsx
  init_define_import_meta_env();
  var import_react42 = __toESM(require_react_shim(), 1);
  function FormMultiCombobox({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    disabled,
    options: options3,
    getOptionLabel,
    getOptionValue,
    getOptionDisabled,
    optionMapper,
    onOptionChange,
    valueType = "flat",
    placeholder = "Select...",
    noOptionsText = "No options",
    urlSearchParams,
    debounceMs = 500,
    pageSize = 20
  }) {
    const {
      field: { ref, value, onChange, ...field },
      fieldState: { error: error2 }
    } = useController({ name, control, rules });
    const [open, setOpen] = (0, import_react42.useState)(false);
    const [search, setSearch] = (0, import_react42.useState)("");
    const debouncedSearch = useDebounce(search, debounceMs);
    const isAsync = typeof options3 === "function";
    const stringValues = (() => {
      if (!value || !Array.isArray(value)) return [];
      if (valueType === "contain") return value.map((option) => getOptionValue(option));
      return value;
    })();
    const selectedItemsIds = stringValues;
    const hasValueToResolve = valueType === "flat" && selectedItemsIds.length > 0;
    const infiniteQuery = useAsyncOptionsInfiniteQuery({
      queryKey: `${label}-${name}-multi-combobox-options`,
      fetchOptions: isAsync ? options3 : () => Promise.resolve({ data: [], pagination: { page: 1, total: 0, totalPages: 0, pageSize } }),
      searchTerm: debouncedSearch,
      selectedItemsIds,
      urlSearchParams,
      pageSize,
      enabled: isAsync && (open || hasValueToResolve)
    });
    const { handleScroll: handleScroll2 } = useInfiniteScroll({ infiniteQuery, enabled: isAsync });
    const allOptions = isAsync ? infiniteQuery.data?.pages.flatMap((p2) => p2.data) ?? [] : options3;
    const mappedOptions = optionMapper ? allOptions.map(optionMapper) : allOptions;
    const selectedSet = new Set(stringValues);
    const selectedOptions = valueType === "contain" && Array.isArray(value) ? value : allOptions.filter((o) => selectedSet.has(getOptionValue(o)));
    const onToggleValue = (optValue) => {
      const isRemoving = selectedSet.has(optValue);
      if (valueType === "contain") {
        const current = value ?? [];
        const next = isRemoving ? current.filter((o) => getOptionValue(o) !== optValue) : [...current, allOptions.find((o) => getOptionValue(o) === optValue)];
        onChange(next);
        onOptionChange?.(next);
      } else {
        const next = new Set(selectedSet);
        isRemoving ? next.delete(optValue) : next.add(optValue);
        const nextValues = Array.from(next);
        onChange(nextValues);
        onOptionChange?.(allOptions.filter((o) => next.has(getOptionValue(o))));
      }
    };
    const onClear = () => {
      onChange([]);
      onOptionChange?.([]);
    };
    return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: name, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(Combobox_default, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(
      Combobox_default.Trigger,
      {
        ref,
        id: name,
        onBlur: field.onBlur,
        disabled: field.disabled || disabled,
        className: cn(
          error2 && "border-destructive hover:not-disabled:border-destructive hover:not-disabled:ring-destructive focus-visible:border-destructive focus-visible:ring-destructive text-destructive ps-8",
          className
        )
      },
      /* @__PURE__ */ React.createElement(Combobox_default.Value, { placeholder }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: selectedOptions.length > 0 }, /* @__PURE__ */ React.createElement("span", { className: "line-clamp-1 truncate" }, selectedOptions.map((opt) => /* @__PURE__ */ React.createElement(Badge_default, { key: getOptionValue(opt), className: "not-last:me-1" }, getOptionLabel(opt), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !field.disabled && !disabled }, /* @__PURE__ */ React.createElement(
        Button_default,
        {
          asChild: true,
          variant: "ghost-muted-destructive",
          size: "unstyled",
          onPointerDown: (e2) => e2.stopPropagation(),
          onClick: (e2) => {
            e2.stopPropagation();
            e2.preventDefault();
            onToggleValue(getOptionValue(opt));
          }
        },
        /* @__PURE__ */ React.createElement(X, { size: 12 })
      )))))), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, placeholder))),
      /* @__PURE__ */ React.createElement(Combobox_default.Clear, { when: selectedSet.size > 0 && !field.disabled && !disabled, onClear }),
      /* @__PURE__ */ React.createElement(Combobox_default.Icon, null)
    ), /* @__PURE__ */ React.createElement(Combobox_default.Content, { shouldFilter: !isAsync }, /* @__PURE__ */ React.createElement(Combobox_default.Input, { placeholder, ...isAsync && { value: search, onValueChange: setSearch } }, /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: isAsync && (infiniteQuery.isFetching || search !== debouncedSearch) })), /* @__PURE__ */ React.createElement(Combobox_default.List, { onScroll: isAsync ? handleScroll2 : void 0 }, /* @__PURE__ */ React.createElement(
      Combobox_default.Empty,
      {
        when: !isAsync && mappedOptions.length === 0 || isAsync && !infiniteQuery.isFetching && mappedOptions.length === 0
      },
      noOptionsText
    ), mappedOptions.map((option) => {
      const optValue = getOptionValue(option);
      const isChecked = selectedSet.has(optValue);
      return /* @__PURE__ */ React.createElement(
        Combobox_default.Item,
        {
          key: optValue,
          value: getOptionLabel(option),
          disabled: getOptionDisabled?.(option),
          "data-checked": isChecked || void 0,
          onSelect: () => onToggleValue(optValue)
        },
        /* @__PURE__ */ React.createElement(Combobox_default.Indicator, null, /* @__PURE__ */ React.createElement(Checkbox_default, { checked: isChecked, variant: "primary", tabIndex: -1, className: "pointer-events-none" })),
        getOptionLabel(option)
      );
    }), /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: isAsync && infiniteQuery.isFetchingNextPage, message: "Loading more..." })))), /* @__PURE__ */ React.createElement(FormMessage_default, { className: errorClassName, hidden: !error2, error: error2 })));
  }
  FormMultiCombobox.displayName = "FormMultiCombobox";
  var FormMultiCombobox_default = FormMultiCombobox;

  // src/components/forms/form-date-picker/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-date-picker/FormDatePicker.tsx
  init_define_import_meta_env();
  var import_react43 = __toESM(require_react_shim(), 1);
  function FormDatePicker({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    disabled,
    placeholder,
    dateFormat = DATE_INPUT_FORMATS.US_SLASH,
    formatDate,
    ...props
  }) {
    const [isOpen, setIsOpen] = (0, import_react43.useState)(false);
    const [calendarMonth, setCalendarMonth] = (0, import_react43.useState)(void 0);
    const {
      field: { onChange, value, ref, onBlur: fieldOnBlur, ...field },
      fieldState: { error: error2 }
    } = useController({
      name,
      control,
      rules
    });
    const date = (0, import_react43.useMemo)(() => {
      if (!value) return void 0;
      if (Object.prototype.toString.call(value) === "[object Date]") {
        return value;
      }
      if (typeof value === "string") {
        const parsed = new Date(value);
        return isNaN(parsed.getTime()) ? void 0 : parsed;
      }
      return void 0;
    }, [value]);
    const isUserInputRef = (0, import_react43.useRef)(false);
    const lastSyncedValueRef = (0, import_react43.useRef)(null);
    const formatDateValue = (0, import_react43.useCallback)(
      (dateValue) => {
        if (formatDate) {
          return formatDate(dateValue);
        }
        if (dateValue) {
          const year = dateValue.getFullYear();
          const month = String(dateValue.getMonth() + 1).padStart(2, "0");
          const day = String(dateValue.getDate()).padStart(2, "0");
          return `${year}-${month}-${day}`;
        }
        return void 0;
      },
      [formatDate]
    );
    const handleDateChange = (0, import_react43.useCallback)(
      (newDate) => {
        isUserInputRef.current = true;
        onChange(formatDateValue(newDate));
        if (newDate) {
          setCalendarMonth(newDate);
        }
      },
      [onChange, formatDateValue]
    );
    const {
      inputValue,
      handleChange,
      handleKeyDown,
      handlePaste,
      handleBlur,
      setFromDate,
      parsedDate,
      isValidInput,
      placeholder: maskPlaceholder
    } = useDateMask({
      dateFormat,
      onChange: handleDateChange
    });
    (0, import_react43.useEffect)(() => {
      if (isUserInputRef.current) {
        isUserInputRef.current = false;
        return;
      }
      const valueKey = date ? date.getTime() : null;
      if (lastSyncedValueRef.current === valueKey) {
        return;
      }
      lastSyncedValueRef.current = valueKey;
      setFromDate(date);
      if (date) {
        setCalendarMonth(date);
      }
    }, [date, setFromDate]);
    (0, import_react43.useEffect)(() => {
      if (parsedDate) {
        setCalendarMonth(parsedDate);
      }
    }, [parsedDate]);
    const handleCalendarSelect = (selectedDate) => {
      onChange(formatDateValue(selectedDate));
      setFromDate(selectedDate);
      if (selectedDate) {
        setCalendarMonth(selectedDate);
      }
      setIsOpen(false);
    };
    const handleClear = (e2) => {
      e2.preventDefault();
      e2.stopPropagation();
      onChange(formatDateValue(void 0));
      setFromDate(void 0);
    };
    const handleIconClick = (e2) => {
      e2.preventDefault();
      e2.stopPropagation();
      if (!disabled) {
        setIsOpen(true);
      }
    };
    const handleInputBlur = () => {
      handleBlur();
      fieldOnBlur();
      if (!isValidInput) {
        onChange(formatDateValue(void 0));
        setFromDate(void 0);
      }
    };
    const displayPlaceholder = placeholder || maskPlaceholder;
    return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: name, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(Popover_default, { open: isOpen, onOpenChange: setIsOpen }, /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "date-picker-container",
        className: cn(
          "group bg-background relative flex h-12 w-full items-center rounded-md border px-3 py-3 text-xs shadow-xs",
          "transition-colors focus-within:outline-none",
          {
            "border-muted-200 hover:border-muted-300 focus-within:border-primary focus-within:ring-primary focus-within:ring-1": !error2,
            "border-destructive focus-within:border-destructive focus-within:ring-destructive ps-8 focus-within:ring-1": !!error2,
            "pointer-events-none cursor-not-allowed opacity-50": disabled
          },
          className
        )
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex min-w-0 flex-1 items-center gap-2" }, /* @__PURE__ */ React.createElement(Popover_default.Trigger, { asChild: true }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          disabled,
          onClick: handleIconClick,
          className: "shrink-0 focus:outline-none",
          "aria-label": "Open calendar",
          tabIndex: -1
        },
        /* @__PURE__ */ React.createElement(
          Calendar,
          {
            size: 20,
            className: cn("h-5 w-5 cursor-pointer transition-colors", {
              "text-destructive": !!error2,
              "text-muted-300 opacity-50": disabled,
              "text-muted-400 hover:text-primary": !error2 && !disabled,
              "text-primary": isOpen && !error2 && !disabled
            })
          }
        )
      )), /* @__PURE__ */ React.createElement(
        "input",
        {
          ref,
          id: name,
          type: "text",
          inputMode: "numeric",
          autoComplete: "off",
          disabled,
          placeholder: displayPlaceholder,
          value: inputValue,
          onChange: handleChange,
          onKeyDown: handleKeyDown,
          onPaste: handlePaste,
          onBlur: handleInputBlur,
          "aria-invalid": !!error2,
          className: cn("min-w-0 flex-1 bg-transparent text-xs outline-none", "placeholder:text-muted-400", {
            "text-primary-900": !!inputValue && isValidInput,
            "text-destructive": !!inputValue && !isValidInput
          }),
          ...field
        }
      )),
      /* @__PURE__ */ React.createElement("div", { className: "flex shrink-0 items-center gap-2" }, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!date && !disabled }, /* @__PURE__ */ React.createElement(TooltipButton_default, { tabIndex: -1, title: "Clear", variant: "ghost-muted-destructive", size: "unstyled", onClick: handleClear }, /* @__PURE__ */ React.createElement(X, { size: 16 }))))
    ), /* @__PURE__ */ React.createElement(Popover_default.Content, { className: "w-auto p-0", align: "start" }, /* @__PURE__ */ React.createElement(
      Calendar_default,
      {
        mode: "single",
        captionLayout: "dropdown",
        selected: date,
        month: calendarMonth,
        onMonthChange: setCalendarMonth,
        onSelect: handleCalendarSelect,
        autoFocus: true,
        ...props
      }
    ))), /* @__PURE__ */ React.createElement(FormMessage_default, { className: errorClassName, hidden: !error2, error: error2 })));
  }
  FormDatePicker.displayName = "FormDatePicker";
  var FormDatePicker_default = FormDatePicker;

  // src/components/forms/form-date-range/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-date-range/FormDateRange.tsx
  init_define_import_meta_env();
  var import_react44 = __toESM(require_react_shim(), 1);
  var parseDateValue = (value) => {
    if (!value) return void 0;
    if (value instanceof Date) return isNaN(value.getTime()) ? void 0 : value;
    if (typeof value === "string") {
      const parsed = new Date(value);
      return isNaN(parsed.getTime()) ? void 0 : parsed;
    }
    return void 0;
  };
  var parseCompositeValue = (value) => {
    if (!value) return void 0;
    const obj = value;
    const from = parseDateValue(obj.from);
    const to = parseDateValue(obj.to);
    if (!from && !to) return void 0;
    return { from, to };
  };
  var FormDateRangeCore = ({
    name,
    dateRange,
    onRangeChange,
    onBlur,
    inputRef,
    error: error2,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    disabled,
    placeholder,
    dateFormat = DATE_INPUT_FORMATS.US_SLASH,
    ...props
  }) => {
    const [isOpen, setIsOpen] = (0, import_react44.useState)(false);
    const [calendarMonth, setCalendarMonth] = (0, import_react44.useState)(void 0);
    const isUserInputRef = (0, import_react44.useRef)(false);
    const lastSyncedValueRef = (0, import_react44.useRef)(null);
    const handleRangeChange = (0, import_react44.useCallback)((newRange) => {
      isUserInputRef.current = true;
      if (!newRange) {
        onRangeChange(void 0);
        return;
      }
      onRangeChange({ from: newRange.from, to: newRange.to });
      if (newRange.from) {
        setCalendarMonth(newRange.from);
      } else if (newRange.to) {
        setCalendarMonth(newRange.to);
      }
    }, []);
    const {
      inputValue,
      handleChange,
      handleKeyDown,
      handlePaste,
      handleBlur,
      setFromDateRange,
      parsedRange,
      isValidInput,
      placeholder: maskPlaceholder
    } = useDateRangeMask({
      dateFormat,
      onChange: handleRangeChange
    });
    (0, import_react44.useEffect)(() => {
      if (isUserInputRef.current) {
        isUserInputRef.current = false;
        return;
      }
      const valueKey = dateRange ? `${dateRange.from?.getTime()}-${dateRange.to?.getTime()}` : "empty";
      if (lastSyncedValueRef.current === valueKey) {
        return;
      }
      lastSyncedValueRef.current = valueKey;
      if (dateRange) {
        setFromDateRange({ from: dateRange.from, to: dateRange.to });
        if (dateRange.from) {
          setCalendarMonth(dateRange.from);
        }
      } else {
        setFromDateRange(void 0);
      }
    }, [dateRange]);
    (0, import_react44.useEffect)(() => {
      if (parsedRange.from) {
        setCalendarMonth(parsedRange.from);
      } else if (parsedRange.to) {
        setCalendarMonth(parsedRange.to);
      }
    }, [parsedRange.from, parsedRange.to]);
    const handleCalendarSelect = (selectedRange) => {
      onRangeChange(selectedRange);
      if (selectedRange) {
        setFromDateRange({ from: selectedRange.from, to: selectedRange.to });
        if (selectedRange.from) {
          setCalendarMonth(selectedRange.from);
        }
      }
    };
    const handleClear = (e2) => {
      e2.preventDefault();
      e2.stopPropagation();
      onRangeChange(void 0);
      setFromDateRange(void 0);
    };
    const handleIconClick = (e2) => {
      e2.preventDefault();
      e2.stopPropagation();
      if (!disabled) {
        setIsOpen(true);
      }
    };
    const handleInputBlur = () => {
      handleBlur();
      onBlur();
    };
    const displayPlaceholder = placeholder || maskPlaceholder;
    const hasValue = dateRange?.from || dateRange?.to;
    return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: name, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(Popover_default, { open: isOpen, onOpenChange: setIsOpen }, /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "date-range-container",
        className: cn(
          "group bg-background relative flex h-12 w-full items-center rounded-md border px-3 py-3 text-xs shadow-xs",
          "transition-colors focus-within:outline-none",
          {
            "border-muted-200 hover:border-muted-300 focus-within:border-primary focus-within:ring-primary focus-within:ring-1": !error2,
            "border-destructive focus-within:border-destructive focus-within:ring-destructive ps-8 focus-within:ring-1": !!error2,
            "pointer-events-none cursor-not-allowed opacity-50": disabled
          },
          className
        )
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex min-w-0 flex-1 items-center gap-2" }, /* @__PURE__ */ React.createElement(Popover_default.Trigger, { asChild: true }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          disabled,
          onClick: handleIconClick,
          className: "shrink-0 focus:outline-none",
          "aria-label": "Open calendar",
          tabIndex: -1
        },
        /* @__PURE__ */ React.createElement(
          Calendar,
          {
            size: 20,
            className: cn("h-5 w-5 cursor-pointer transition-colors", {
              "text-destructive": !!error2,
              "text-muted-300 opacity-50": disabled,
              "text-muted-400 hover:text-primary": !error2 && !disabled,
              "text-primary": isOpen && !error2 && !disabled
            })
          }
        )
      )), /* @__PURE__ */ React.createElement(
        "input",
        {
          ref: inputRef,
          id: name,
          type: "text",
          inputMode: "numeric",
          autoComplete: "off",
          disabled,
          placeholder: displayPlaceholder,
          value: inputValue,
          onChange: handleChange,
          onKeyDown: handleKeyDown,
          onPaste: handlePaste,
          onBlur: handleInputBlur,
          "aria-invalid": !!error2,
          className: cn("min-w-0 flex-1 bg-transparent text-xs outline-none", "placeholder:text-muted-400", {
            "text-primary-900": !!inputValue && isValidInput,
            "text-destructive": !!inputValue && !isValidInput
          })
        }
      )),
      /* @__PURE__ */ React.createElement("div", { className: "flex shrink-0 items-center gap-2" }, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!hasValue && !disabled }, /* @__PURE__ */ React.createElement(TooltipButton_default, { tabIndex: -1, title: "Clear", variant: "ghost-muted-destructive", size: "unstyled", onClick: handleClear }, /* @__PURE__ */ React.createElement(X, { size: 16 }))))
    ), /* @__PURE__ */ React.createElement(Popover_default.Content, { className: "w-auto p-0", align: "start" }, /* @__PURE__ */ React.createElement(
      Calendar_default,
      {
        mode: "range",
        captionLayout: "dropdown",
        selected: dateRange,
        month: calendarMonth,
        onMonthChange: setCalendarMonth,
        numberOfMonths: 2,
        onSelect: handleCalendarSelect,
        autoFocus: true,
        ...props
      }
    ))), /* @__PURE__ */ React.createElement(FormMessage_default, { className: errorClassName, hidden: !error2, error: error2 })));
  };
  function FormDateRangeComposite({
    name,
    rules,
    control,
    formatDateValue,
    ...rest
  }) {
    const {
      field: { onChange, value, ref, onBlur },
      fieldState: { error: error2 }
    } = useController({ name, control, rules });
    const dateRange = (0, import_react44.useMemo)(() => parseCompositeValue(value), [value]);
    const onRangeChange = (range) => {
      if (!formatDateValue) {
        onChange(range);
        return;
      }
      onChange(range ? { from: formatDateValue(range.from), to: formatDateValue(range.to) } : void 0);
    };
    return /* @__PURE__ */ React.createElement(
      FormDateRangeCore,
      {
        ...rest,
        name,
        dateRange,
        onRangeChange,
        onBlur,
        inputRef: ref,
        error: error2
      }
    );
  }
  function FormDateRangeFlat({
    fromName,
    toName,
    rules,
    control,
    formatDateValue,
    ...rest
  }) {
    const fromController = useController({ name: fromName, control, rules });
    const toController = useController({ name: toName, control, rules });
    const fromValue = fromController.field.value;
    const toValue2 = toController.field.value;
    const dateRange = (0, import_react44.useMemo)(() => {
      const from = parseDateValue(fromValue);
      const to = parseDateValue(toValue2);
      if (!from && !to) return void 0;
      return { from, to };
    }, [fromValue, toValue2]);
    const onRangeChange = (range) => {
      const fromOut = formatDateValue ? formatDateValue(range?.from) : range?.from;
      const toOut = formatDateValue ? formatDateValue(range?.to) : range?.to;
      fromController.field.onChange(fromOut ?? null);
      toController.field.onChange(toOut ?? null);
    };
    const onBlur = () => {
      fromController.field.onBlur();
      toController.field.onBlur();
    };
    const error2 = fromController.fieldState.error ?? toController.fieldState.error;
    return /* @__PURE__ */ React.createElement(
      FormDateRangeCore,
      {
        ...rest,
        name: fromName,
        dateRange,
        onRangeChange,
        onBlur,
        inputRef: fromController.field.ref,
        error: error2
      }
    );
  }
  function FormDateRange(props) {
    if ("fromName" in props && props.fromName) {
      return /* @__PURE__ */ React.createElement(FormDateRangeFlat, { ...props });
    }
    return /* @__PURE__ */ React.createElement(FormDateRangeComposite, { ...props });
  }
  FormDateRange.displayName = "FormDateRange";
  var FormDateRange_default = FormDateRange;

  // src/components/forms/form-number/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-number/FormNumber.tsx
  init_define_import_meta_env();
  var import_react45 = __toESM(require_react_shim(), 1);
  function FormNumber({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    ...props
  }) {
    const id = (0, import_react45.useId)();
    return /* @__PURE__ */ React.createElement(
      Controller,
      {
        name,
        control,
        rules,
        render: ({ field, fieldState: { error: error2 } }) => {
          return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(
            FormLabel_default,
            {
              className: labelClassName,
              htmlFor: `${id}-${name}`,
              tooltip,
              required,
              hidden: !label,
              error: error2
            },
            label
          ), /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(
            FormattedNumber_default,
            {
              id: `${id}-${name}`,
              className: cn(
                error2 && "border-destructive hover:not-disabled:border-destructive hover:not-disabled:ring-destructive focus-visible:border-destructive focus-visible:ring-destructive placeholder:text-destructive ps-8",
                className
              ),
              ...field,
              ...props
            }
          ), /* @__PURE__ */ React.createElement(FormMessage_default, { className: errorClassName, hidden: !error2, error: error2 })));
        }
      }
    );
  }
  FormNumber.displayName = "FormNumber";
  var FormNumber_default = FormNumber;

  // src/components/forms/form-time-picker/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-time-picker/FormTimePicker.tsx
  init_define_import_meta_env();
  function FormTimePicker({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    valueFormat,
    use24hFormat,
    ...props
  }) {
    const { toFormat, constants } = useDate();
    const displayFormat = use24hFormat ? constants.TimeFormats.HOUR_MINUTES_24 : constants.TimeFormats.HOUR_MINUTES_12;
    return /* @__PURE__ */ React.createElement(
      Controller,
      {
        name,
        control,
        rules,
        render: ({ field: { onChange, value }, fieldState: { error: error2 } }) => {
          const storedString = typeof value === "string" ? value : value === null || value === void 0 ? void 0 : String(value);
          const timeValue = valueFormat && storedString ? toFormat(storedString, valueFormat, displayFormat) : storedString;
          const handleChange = (newValue) => {
            if (!newValue) {
              onChange(null);
              return;
            }
            const out = valueFormat ? toFormat(newValue, displayFormat, valueFormat) : newValue;
            onChange(out || null);
          };
          return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: name, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(
            TimePicker_default,
            {
              name,
              value: timeValue,
              onChange: handleChange,
              use24hFormat,
              className: cn(
                error2 && "border-destructive hover:ring-destructive focus-visible:ring-destructive focus-visible:border-destructive ps-8 focus-visible:ring-1",
                className
              ),
              error: !!error2,
              ...props
            }
          ), /* @__PURE__ */ React.createElement(FormMessage_default, { className: errorClassName, hidden: !error2, error: error2 })));
        }
      }
    );
  }
  FormTimePicker.displayName = "FormTimePicker";
  var FormTimePicker_default = FormTimePicker;

  // src/components/forms/form-checkbox-group/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-checkbox-group/FormCheckboxGroup.tsx
  init_define_import_meta_env();
  function FormCheckboxGroup({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    disabled,
    options: options3,
    getOptionLabel,
    getOptionValue,
    checkboxItemClassName,
    ...props
  }) {
    return /* @__PURE__ */ React.createElement(
      Controller,
      {
        name,
        control,
        rules,
        render: ({ field, fieldState: { error: error2 } }) => {
          const selectedValues = Array.isArray(field.value) ? field.value : [];
          const handleCheckedChange = (optionValue, checked) => {
            const updated = checked ? [...selectedValues, optionValue] : selectedValues.filter((v) => v !== optionValue);
            field.onChange(updated);
          };
          return /* @__PURE__ */ React.createElement(FormControl_default, { className: containerClassName }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: name, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement(CheckboxGroup_default, { className: cn("relative flex flex-wrap gap-3 md:gap-6", className), ...props }, options3.map((option) => {
            const value = getOptionValue(option);
            return /* @__PURE__ */ React.createElement(
              CheckboxGroup_default.Item,
              {
                key: value,
                ref: field.ref,
                className: cn(
                  "flex-1 shrink-0",
                  error2 && "border-destructive hover:border-destructive hover:ring-destructive focus-visible:border-destructive focus-visible:ring-destructive text-destructive",
                  checkboxItemClassName
                ),
                checked: selectedValues.includes(value),
                disabled: field.disabled || disabled,
                onCheckedChange: (checked) => handleCheckedChange(value, !!checked)
              },
              /* @__PURE__ */ React.createElement("span", { className: "flex flex-1 items-center text-sm font-normal" }, getOptionLabel(option)),
              /* @__PURE__ */ React.createElement(
                CheckboxGroup_default.Indicator,
                {
                  className: cn(
                    error2 && "border-destructive data-[state=checked]:bg-destructive data-[state=checked]:border-destructive group-hover/checkbox-item:border-destructive group-hover/checkbox-item:ring-destructive"
                  )
                },
                /* @__PURE__ */ React.createElement(Check, { size: 16, stroke: "white", className: "hidden group-data-[state=checked]/checkbox-item:block" })
              )
            );
          }), /* @__PURE__ */ React.createElement(FormMessage_default, { className: cn("inset-s-auto inset-e-9 inset-bs-6", errorClassName), hidden: !error2, error: error2 })));
        }
      }
    );
  }
  FormCheckboxGroup.displayName = "FormCheckboxGroup";
  var FormCheckboxGroup_default = FormCheckboxGroup;

  // src/components/forms/form-radio-group/index.ts
  init_define_import_meta_env();

  // src/components/forms/form-radio-group/FormRadioGroup.tsx
  init_define_import_meta_env();
  function FormRadioGroup({
    name,
    rules,
    control,
    label,
    tooltip,
    containerClassName,
    labelClassName,
    className,
    errorClassName,
    required,
    disabled,
    options: options3,
    getOptionLabel,
    getOptionValue,
    radioItemClassName,
    ...props
  }) {
    return /* @__PURE__ */ React.createElement(
      Controller,
      {
        name,
        control,
        rules,
        render: ({ field, fieldState: { error: error2 } }) => /* @__PURE__ */ React.createElement(FormControl_default, { className: cn(containerClassName) }, /* @__PURE__ */ React.createElement(FormLabel_default, { className: labelClassName, htmlFor: name, tooltip, required, hidden: !label, error: error2 }, label), /* @__PURE__ */ React.createElement(
          RadioGroup_default,
          {
            className: cn("relative flex flex-wrap gap-3 md:gap-6", className),
            value: field.value ?? "",
            disabled: field.disabled || disabled,
            onValueChange: field.onChange,
            ...props
          },
          options3.map((option) => /* @__PURE__ */ React.createElement(
            RadioGroup_default.Item,
            {
              key: getOptionValue(option),
              id: name,
              ref: field.ref,
              className: cn(
                "flex-1 shrink-0",
                error2 && "border-destructive hover:border-destructive hover:ring-destructive focus-visible:border-destructive focus-visible:ring-destructive text-destructive",
                radioItemClassName
              ),
              value: getOptionValue(option)
            },
            /* @__PURE__ */ React.createElement("span", { className: "flex flex-1 items-center text-sm font-normal" }, getOptionLabel(option)),
            /* @__PURE__ */ React.createElement(
              RadioGroup_default.Indicator,
              {
                className: cn(
                  error2 && "border-destructive data-[state=checked]:bg-destructive data-[state=checked]:border-destructive group-hover/radio-item:border-destructive group-hover/radio-item:ring-destructive"
                )
              },
              /* @__PURE__ */ React.createElement(Check, { size: 16, stroke: "white", className: "hidden group-data-[state=checked]/radio-item:block" })
            )
          )),
          /* @__PURE__ */ React.createElement(FormMessage_default, { className: cn("inset-s-auto inset-e-9 inset-bs-6", errorClassName), hidden: !error2, error: error2 })
        ))
      }
    );
  }
  FormRadioGroup.displayName = "FormRadioGroup";
  var FormRadioGroup_default = FormRadioGroup;

  // src/components/shared/faceted-filter/FacetedFilter.tsx
  function FacetedFilter({ title, name, control, filterVariant = "text", filterOptions = [] }) {
    const { t: t3 } = useAppTranslation("common");
    const {
      field: { value, onChange }
    } = useController({ name, control });
    const multiSelect = filterVariant === "multi-select";
    const selectedValues = Array.isArray(value) ? value : typeof value === "string" && value ? [value] : [];
    const selectedCount = selectedValues.length;
    const onSelect = (val) => {
      if (!multiSelect) {
        onChange(value === val ? void 0 : val);
        return;
      }
      const newValues = selectedValues.includes(val) ? selectedValues.filter((v) => v !== val) : [...selectedValues, val];
      onChange(newValues.length ? newValues : void 0);
    };
    if (filterVariant === "date-range") return /* @__PURE__ */ React.createElement(FormDateRange_default, { name });
    if (filterVariant === "select" || filterVariant === "multi-select") {
      return /* @__PURE__ */ React.createElement(Popover_default, null, /* @__PURE__ */ React.createElement(Popover_default.Trigger, { asChild: true }, /* @__PURE__ */ React.createElement(Button_default, { variant: "outline", size: "sm", className: "border-dashed" }, /* @__PURE__ */ React.createElement(Plus, { className: "me-2 h-4 w-4" }), title, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: selectedCount > 0 }, /* @__PURE__ */ React.createElement(Divider_default, { orientation: "vertical", className: "mx-2 h-4" }), /* @__PURE__ */ React.createElement(Badge_default, { variant: "secondary", className: "rounded-sm px-1 font-normal lg:hidden" }, selectedCount), /* @__PURE__ */ React.createElement("div", { className: "hidden space-x-1 lg:flex" }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: selectedCount < 3 }, filterOptions.filter((opt) => selectedValues.includes(opt.value)).map((opt) => /* @__PURE__ */ React.createElement(Badge_default, { key: opt.value, variant: "secondary", className: "rounded-sm px-1 font-normal" }, opt.label))), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Badge_default, { variant: "secondary", className: "rounded-sm px-1 font-normal" }, selectedCount, " selected"))))))), /* @__PURE__ */ React.createElement(Popover_default.Content, { className: "w-[200px] p-0", align: "start" }, /* @__PURE__ */ React.createElement(Command_default, null, /* @__PURE__ */ React.createElement(Command_default.Input, { placeholder: title }), /* @__PURE__ */ React.createElement(Command_default.List, null, /* @__PURE__ */ React.createElement(Command_default.Empty, null, "No results found."), /* @__PURE__ */ React.createElement(Command_default.Group, null, filterOptions.map((opt) => {
        const isSelected = selectedValues.includes(opt.value);
        return /* @__PURE__ */ React.createElement(Command_default.Item, { key: opt.value, onSelect: () => onSelect(opt.value) }, /* @__PURE__ */ React.createElement(
          "div",
          {
            className: cn("border-primary me-2 flex h-4 w-4 items-center justify-center border", {
              "bg-primary text-primary-foreground": isSelected,
              "opacity-50 [&_svg]:invisible": !isSelected,
              "rounded-sm": multiSelect,
              "rounded-full": !multiSelect
            })
          },
          /* @__PURE__ */ React.createElement(Check, { size: 16 })
        ), /* @__PURE__ */ React.createElement("span", null, opt.label));
      })), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: selectedCount > 0 }, /* @__PURE__ */ React.createElement(Command_default.Separator, null), /* @__PURE__ */ React.createElement(Command_default.Group, null, /* @__PURE__ */ React.createElement(Command_default.Item, { onSelect: () => onChange(void 0), className: "justify-center text-center" }, "Clear filters")))))));
    }
    return /* @__PURE__ */ React.createElement(FormInput_default, { name, placeholder: title ?? "Search", className: "m-0 me-0 h-[36px] w-[150px] lg:w-[250px]" });
  }
  var FacetedFilter_default = FacetedFilter;

  // src/components/shared/file-icon/index.ts
  init_define_import_meta_env();

  // src/components/shared/file-icon/FileIcon.tsx
  init_define_import_meta_env();
  var EXTENSION_ICON_MAP = {
    pdf: FileText,
    doc: FileText,
    docx: FileText,
    txt: FileText,
    csv: FileSpreadsheet,
    xls: FileSpreadsheet,
    xlsx: FileSpreadsheet,
    ppt: File,
    pptx: File,
    jpg: FileImage,
    jpeg: FileImage,
    png: FileImage,
    gif: FileImage,
    mp3: FileAudio,
    wav: FileAudio,
    ogg: FileAudio,
    webm: FileAudio,
    mp4: FileVideo,
    avi: FileVideo,
    mov: FileVideo,
    mkv: FileVideo,
    folder: Folder,
    unknown: File
  };
  var fileIconVariants = cva("inline-flex shrink-0 items-center justify-center", {
    variants: {
      variant: {
        plain: "text-muted-400",
        chip: "bg-primary-15 text-primary gap-1 rounded-md p-1.5"
      }
    },
    defaultVariants: {
      variant: "plain"
    }
  });
  var normalize = (extension) => {
    const key = extension?.toLowerCase().replace(/^\./, "") ?? "";
    return key in EXTENSION_ICON_MAP ? key : "unknown";
  };
  var FileIcon = ({ className, extension, size: size4 = 20, variant, ...props }) => {
    const key = normalize(extension);
    const Icon5 = EXTENSION_ICON_MAP[key];
    const showLabel = variant === "chip" && key !== "unknown" && key !== "folder";
    return /* @__PURE__ */ React.createElement("span", { "data-slot": "file-icon", className: cn(fileIconVariants({ variant }), className), ...props }, /* @__PURE__ */ React.createElement(Icon5, { size: size4 }), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: showLabel }, /* @__PURE__ */ React.createElement("span", { className: "text-2xs font-bold uppercase" }, key)));
  };
  FileIcon.displayName = "FileIcon";
  var FileIcon_default = FileIcon;

  // src/components/shared/file-uploader/index.ts
  init_define_import_meta_env();

  // src/components/shared/file-uploader/FileUploader.tsx
  init_define_import_meta_env();
  var import_react46 = __toESM(require_react_shim(), 1);
  var formatFileSize = (bytes) => {
    if (bytes >= 1024 * 1024) return `${(bytes / (1024 * 1024)).toFixed(2)} MB`;
    return `${(bytes / 1024).toFixed(2)} KB`;
  };
  var isImageFile = (file) => file.type.startsWith("image/");
  var getUniqueFiles = (existing, incoming) => incoming.filter((f) => !existing.some((e2) => e2.name === f.name && e2.size === f.size));
  var FileUploaderContext = (0, import_react46.createContext)(null);
  var useFileUploaderContext = () => {
    const context2 = (0, import_react46.useContext)(FileUploaderContext);
    if (!context2) throw new Error("FileUploader subcomponents must be used within <FileUploader>");
    return context2;
  };
  var FileUploaderDropzoneGroup = ({ className, children, ...props }) => {
    const { disabled, isUploading } = useFileUploaderContext();
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "file-uploader-dropzone-group",
        className: cn(
          "border-muted-200 bg-background text-muted-400 relative flex items-center gap-2 rounded-lg border border-dashed p-3 text-sm transition-[color,box-shadow] outline-none",
          !disabled && "hover:border-primary",
          disabled && "bg-muted-50 text-muted-300",
          isUploading && "pointer-events-none",
          className
        ),
        ...props
      },
      children
    );
  };
  var FileUploaderDropzone = ({ ref, placeholder, className, ...props }) => {
    const { files, onChange, accept, multiple, disabled, isUploading, name, inputId } = useFileUploaderContext();
    const singleFile = !multiple && files.length > 0 ? files[0] : null;
    const handleChange = (event) => {
      const incoming = Array.from(event.target.files ?? []);
      if (!incoming.length) return;
      onChange(multiple ? [...files, ...getUniqueFiles(files, incoming)] : [incoming[0]]);
      event.target.value = "";
    };
    return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
      "input",
      {
        ref,
        accept,
        type: "file",
        id: inputId,
        name,
        className: "hidden",
        disabled: disabled || isUploading,
        onChange: handleChange,
        multiple,
        ...props
      }
    ), /* @__PURE__ */ React.createElement(
      "label",
      {
        htmlFor: inputId,
        "data-slot": "file-uploader-dropzone",
        className: cn(
          "flex min-w-0 grow cursor-pointer items-center justify-center gap-2 after:absolute after:inset-0",
          disabled && "pointer-events-none",
          className
        )
      },
      /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: isUploading }, /* @__PURE__ */ React.createElement(LoaderCircle, { size: 20, className: "animate-spin" })), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!singleFile }, /* @__PURE__ */ React.createElement(Paperclip, { size: 16, className: "shrink-0" }), /* @__PURE__ */ React.createElement("span", { className: "text-foreground truncate" }, singleFile?.name), /* @__PURE__ */ React.createElement("span", { className: "shrink-0 text-xs" }, formatFileSize(singleFile?.size ?? 0))), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Paperclip, { size: 20 }), /* @__PURE__ */ React.createElement("span", null, placeholder)))
    ));
  };
  var FileUploaderActions = ({ onDownload, clearable = true, className, ...props }) => {
    const { files, onRemove, multiple, disabled, isUploading } = useFileUploaderContext();
    const { t: t3 } = useAppTranslation("common");
    const showClear = clearable && !multiple && files.length > 0 && !disabled && !isUploading;
    const showDownload = !!onDownload && !isUploading;
    if (!showClear && !showDownload) return null;
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "file-uploader-actions", className: cn("relative z-10 flex shrink-0 items-center gap-2", className), ...props }, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: showDownload }, /* @__PURE__ */ React.createElement(TooltipButton_default, { asChild: true, title: t3("download"), variant: "ghost", size: "unstyled", type: "button", onClick: onDownload }, /* @__PURE__ */ React.createElement(Download, { size: 16, className: "text-primary" }))), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: showClear }, /* @__PURE__ */ React.createElement(TooltipButton_default, { asChild: true, title: t3("clear"), variant: "ghost-muted-destructive", size: "unstyled", type: "button", onClick: () => onRemove(0) }, /* @__PURE__ */ React.createElement(CircleX, { size: 16 }))));
  };
  var FileUploaderFileList = ({ variant = "list", className }) => {
    const { files, onRemove, disabled, isUploading } = useFileUploaderContext();
    if (files.length === 0) return null;
    if (variant === "chips") {
      return /* @__PURE__ */ React.createElement(ScrollArea_default, null, /* @__PURE__ */ React.createElement("div", { "data-slot": "file-uploader-file-list", className: cn("flex max-h-16 flex-wrap items-center gap-1", className) }, files.map((file, index2) => /* @__PURE__ */ React.createElement(Chip_default, { key: `${file.name}-${index2}`, variant: "muted", size: "xs", className: "justify-between gap-1" }, /* @__PURE__ */ React.createElement("span", { className: "max-w-36 truncate" }, file.name), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !disabled && !isUploading }, /* @__PURE__ */ React.createElement(Button_default, { className: "shrink-0", variant: "ghost-destructive", size: "unstyled", type: "button", onClick: () => onRemove(index2) }, /* @__PURE__ */ React.createElement(CircleX, { size: 14 })))))), /* @__PURE__ */ React.createElement(ScrollArea_default.Bar, null));
    }
    return /* @__PURE__ */ React.createElement(ScrollArea_default, null, /* @__PURE__ */ React.createElement("ul", { "data-slot": "file-uploader-file-list", className: cn("grid max-h-16 grid-cols-[minmax(0,1fr)] gap-1", className) }, files.map((file, index2) => /* @__PURE__ */ React.createElement("li", { key: `${file.name}-${index2}`, className: "border-muted-200 flex items-center gap-2 rounded-lg border px-3 py-2 text-sm" }, /* @__PURE__ */ React.createElement(Paperclip, { size: 14, className: "text-primary shrink-0" }), /* @__PURE__ */ React.createElement("span", { className: "flex-1 truncate", title: file.name }, file.name), /* @__PURE__ */ React.createElement("span", { className: "text-muted-400 shrink-0 text-xs" }, formatFileSize(file.size)), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !disabled && !isUploading }, /* @__PURE__ */ React.createElement(Button_default, { variant: "ghost-muted-destructive", size: "unstyled", type: "button", onClick: () => onRemove(index2) }, /* @__PURE__ */ React.createElement(CircleX, { size: 16 })))))), /* @__PURE__ */ React.createElement(ScrollArea_default.Bar, null));
  };
  var FileUploaderImagePreview = ({ className, ...props }) => {
    const { files, onRemove, multiple, disabled, isUploading } = useFileUploaderContext();
    const [urls, setUrls] = (0, import_react46.useState)([]);
    const imageEntries = (0, import_react46.useMemo)(
      () => files.reduce((acc, file, index2) => isImageFile(file) ? [...acc, { file, index: index2 }] : acc, []),
      [files]
    );
    const isCompact = imageEntries.length <= 2;
    (0, import_react46.useEffect)(() => {
      const created = imageEntries.map(({ file }) => URL.createObjectURL(file));
      setUrls(created);
      return () => created.forEach(URL.revokeObjectURL);
    }, [imageEntries]);
    if (imageEntries.length === 0) return null;
    return /* @__PURE__ */ React.createElement(ScrollArea_default, null, /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "file-uploader-image-preview",
        className: cn("gap-2", isCompact ? "grid grid-cols-2" : "flex max-h-18 flex-wrap", className),
        ...props
      },
      imageEntries.map(({ file, index: index2 }, i) => {
        const url = urls[i];
        if (!url) return null;
        return /* @__PURE__ */ React.createElement("div", { key: `${file.name}-${file.size}`, className: "group/file-uploader-image-preview relative overflow-hidden rounded-lg" }, /* @__PURE__ */ React.createElement(
          "img",
          {
            src: url,
            alt: file.name,
            className: cn("border-muted-200 shrink-0 rounded-lg border object-fill", isCompact ? "h-28 w-full" : "size-16")
          }
        ), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: multiple && !disabled && !isUploading }, /* @__PURE__ */ React.createElement(
          Button_default,
          {
            className: "absolute inset-e-1 inset-bs-1 hidden items-center justify-center rounded-full group-hover/file-uploader-image-preview:flex",
            variant: "outline-destructive",
            size: "unstyled",
            type: "button",
            onClick: () => onRemove(index2)
          },
          /* @__PURE__ */ React.createElement(X, { size: 16 })
        )));
      })
    ), /* @__PURE__ */ React.createElement(ScrollArea_default.Bar, null));
  };
  var FileUploader = ({
    value,
    onChange,
    accept = "image/*",
    multiple = false,
    disabled = false,
    isUploading = false,
    name,
    children,
    className
  }) => {
    const inputId = `${name}-file-upload`;
    const files = (0, import_react46.useMemo)(() => {
      if (!value) return [];
      if (Array.isArray(value)) return value;
      return [value];
    }, [value]);
    const handleChange = (0, import_react46.useCallback)(
      (nextFiles) => onChange(nextFiles.length === 0 ? null : multiple ? nextFiles : nextFiles[0]),
      [multiple, onChange]
    );
    const handleRemove = (0, import_react46.useCallback)(
      (index2) => {
        handleChange(files.filter((_, i) => i !== index2));
      },
      [files, handleChange]
    );
    const contextValue = (0, import_react46.useMemo)(
      () => ({ files, onChange: handleChange, onRemove: handleRemove, accept, multiple, disabled, isUploading, name, inputId }),
      [files, handleChange, handleRemove, accept, multiple, disabled, isUploading, name, inputId]
    );
    return /* @__PURE__ */ React.createElement(FileUploaderContext.Provider, { value: contextValue }, /* @__PURE__ */ React.createElement("div", { "data-slot": "file-uploader", className: cn("flex w-full flex-col gap-2", className) }, children));
  };
  FileUploader.DropzoneGroup = FileUploaderDropzoneGroup;
  FileUploader.Dropzone = (0, import_react46.forwardRef)((props, ref) => FileUploaderDropzone({ ...props, ref }));
  FileUploader.Actions = FileUploaderActions;
  FileUploader.FileList = FileUploaderFileList;
  FileUploader.ImagePreview = FileUploaderImagePreview;
  FileUploaderDropzoneGroup.displayName = "FileUploaderDropzoneGroup";
  FileUploader.Dropzone.displayName = "FileUploaderDropzone";
  FileUploaderActions.displayName = "FileUploaderActions";
  FileUploaderFileList.displayName = "FileUploaderFileList";
  FileUploaderImagePreview.displayName = "FileUploaderImagePreview";
  FileUploader.displayName = "FileUploader";
  var FileUploader_default = FileUploader;

  // src/components/shared/date-navigation/index.ts
  init_define_import_meta_env();

  // src/components/shared/date-navigation/DateNavigation.tsx
  init_define_import_meta_env();
  function DateNavigation({ handlePrevious, handleNext, onSetDate, date }) {
    const { toDate: toDate2, constants } = useDate();
    return /* @__PURE__ */ React.createElement("div", { className: "flex items-center space-x-2" }, /* @__PURE__ */ React.createElement(Button_default, { onClick: handlePrevious, variant: "outline", className: "md:h-9 md:w-9 md:py-2", size: "icon" }, /* @__PURE__ */ React.createElement(ArrowLeft, { size: 16 })), /* @__PURE__ */ React.createElement(Button_default, { onClick: handleNext, variant: "outline", className: "md:h-9 md:w-9 md:py-2", size: "icon" }, /* @__PURE__ */ React.createElement(ArrowRight, { size: 16 })), /* @__PURE__ */ React.createElement(
      Button_default,
      {
        className: "md:h-9 md:px-4 md:py-2",
        size: "sm",
        onClick: () => onSetDate(toDate2(void 0, constants.DateFormats.ISO_DATE)),
        variant: "outline"
      },
      "Today"
    ), /* @__PURE__ */ React.createElement("span", { className: "md:text-md text-xs font-medium lg:text-lg xl:text-xl" }, toDate2(date, constants.DateFormats.FULL_WEEKDAY_SHORT_MONTH_DAY_YEAR)));
  }
  var DateNavigation_default = DateNavigation;

  // src/components/shared/error-tooltip/index.ts
  init_define_import_meta_env();

  // src/components/shared/error-tooltip/ErrorTooltip.tsx
  init_define_import_meta_env();
  function ErrorTooltip({ className, message: message2, ...props }) {
    return /* @__PURE__ */ React.createElement(Tooltip_default.Provider, null, /* @__PURE__ */ React.createElement(Tooltip_default, null, /* @__PURE__ */ React.createElement(
      Tooltip_default.Trigger,
      {
        className: cn(
          "bg-destructive text-destructive-foreground absolute inset-s-2 inset-bs-1/2 z-10 mb-0 flex -translate-y-1/2 cursor-help items-center justify-center rounded-full",
          className
        ),
        asChild: true,
        ...props
      },
      /* @__PURE__ */ React.createElement(Info, { size: 20 })
    ), /* @__PURE__ */ React.createElement(Tooltip_default.Content, { className: "bg-destructive text-destructive-foreground text-sm" }, /* @__PURE__ */ React.createElement("p", null, message2))));
  }
  var ErrorTooltip_default = ErrorTooltip;

  // src/components/shared/breadcrumb/index.ts
  init_define_import_meta_env();

  // src/components/shared/breadcrumb/Breadcrumb.tsx
  init_define_import_meta_env();

  // src/routes/routes.ts
  init_define_import_meta_env();
  var FULL_ROUTES_PATH = {
    HOME: {
      INDEX: "/",
      DASHBOARD: "/dashboard"
    },
    AUTH: {
      INDEX: "/auth",
      LOGIN: "/auth/login"
    },
    COMPONENTS: {
      INDEX: "/components",
      DETAIL: "/components/:id"
    },
    SETTINGS: {
      INDEX: "/settings"
    },
    MEMBERS: {
      INDEX: "/members"
    },
    ROOT: {
      INDEX: ".."
    }
  };
  var PLAIN_ROUTES = FULL_ROUTES_PATH;
  var APP_MENU = [
    {
      id: "dashboard",
      path: FULL_ROUTES_PATH.HOME.DASHBOARD,
      name: "Dashboard",
      group: "main",
      icon: LayoutDashboard
    },
    {
      id: "components",
      path: FULL_ROUTES_PATH.COMPONENTS.INDEX,
      name: "Components",
      group: "main",
      icon: Component2
    },
    {
      id: "members",
      path: FULL_ROUTES_PATH.MEMBERS.INDEX,
      name: "Members",
      group: "main",
      icon: Users
    },
    {
      id: "settings",
      path: FULL_ROUTES_PATH.SETTINGS.INDEX,
      name: "Settings",
      group: "account",
      icon: Settings
    }
  ];

  // src/components/shared/breadcrumb/Breadcrumb.tsx
  var Breadcrumb = ({ className, links, separator, withRoot = false }) => {
    const { t: t3 } = useAppTranslation("common");
    if (links.length === 0 && !withRoot) return null;
    const finalLinks = withRoot ? [{ title: "Dashboard", href: PLAIN_ROUTES.HOME.INDEX }, ...links.filter((link) => link.href !== PLAIN_ROUTES.HOME.INDEX)] : links;
    return /* @__PURE__ */ React.createElement("div", { className: cn(className) }, /* @__PURE__ */ React.createElement("nav", { "aria-label": t3("breadcrumbLabel") }, /* @__PURE__ */ React.createElement("ol", { className: "flex" }, finalLinks.map(({ title, href }, idx) => {
      const isLastItem = idx === finalLinks.length - 1;
      return /* @__PURE__ */ React.createElement("li", { key: `${title}-${idx}`, className: "text-muted-400 text-xs leading-none" }, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: (!!href || href === PLAIN_ROUTES.HOME.INDEX) && !isLastItem }, /* @__PURE__ */ React.createElement(
        Link,
        {
          className: "hover:text-primary transition-colors",
          to: href === PLAIN_ROUTES.HOME.INDEX || href === PLAIN_ROUTES.ROOT.INDEX ? href : `/${href}`
        },
        title
      )), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !href && href !== PLAIN_ROUTES.HOME.INDEX }, /* @__PURE__ */ React.createElement("span", null, title)), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !isLastItem }, /* @__PURE__ */ React.createElement("span", { className: "mx-1 *:inline-block!" }, separator ?? /* @__PURE__ */ React.createElement(ChevronRight, { size: 14 }))));
    }))));
  };
  var Breadcrumb_default = Breadcrumb;

  // src/components/shared/form-submit-buttons/index.ts
  init_define_import_meta_env();

  // src/components/shared/form-submit-buttons/FormSubmitButtons.tsx
  init_define_import_meta_env();
  var formSubmitBtnsVariants = cva("", {
    variants: {
      align: {
        start: "justify-start",
        center: "justify-center",
        end: "justify-end"
      }
    },
    defaultVariants: {
      align: "end"
    }
  });
  function FormSubmitButtons({ align, className, ...props }) {
    const navigate = useNavigate();
    const { t: t3 } = useAppTranslation("common");
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: cn(
          "bg-background shadow-boundary-ghost sticky bottom-0 z-10 mt-auto flex gap-1 rounded-md p-2",
          formSubmitBtnsVariants({ align }),
          className
        )
      },
      /* @__PURE__ */ React.createElement(
        ConfirmDialog_default,
        {
          variant: "warning",
          title: t3("unsavedChanges"),
          description: "Are you sure you want to cancel? Any unsaved changes will be lost.",
          confirmLabel: "Yes, Cancel",
          onConfirm: () => navigate(-1)
        },
        /* @__PURE__ */ React.createElement(Button_default, { size: "lg", variant: "muted", type: "button" }, "Cancel")
      ),
      /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!props.children }, props.children), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(
        FormSubmitButton_default,
        {
          className: "lg:ms-0",
          size: "lg",
          loading: props.defaultPrimaryLoading,
          textContent: props.defaultPrimaryText || (props.defaultPrimaryCreateUpdate ? "Update" : "Create")
        }
      )))
    );
  }
  var FormSubmitButtons_default = FormSubmitButtons;

  // src/components/shared/action-panel/index.ts
  init_define_import_meta_env();

  // src/components/shared/action-panel/ActionPanel.tsx
  init_define_import_meta_env();
  var ActionPanelBack = ({ className, onClick, ...props }) => {
    const navigate = useNavigate();
    const { t: t3 } = useAppTranslation("common");
    const handleClick = (e2) => {
      if (onClick) {
        onClick(e2);
        return;
      }
      navigate(-1);
    };
    return /* @__PURE__ */ React.createElement(Button_default, { size: "icon-sm", variant: "ghost-muted", className: cn(className), "aria-label": t3("goBack"), onClick: handleClick, ...props }, /* @__PURE__ */ React.createElement(ChevronLeft, { size: 20 }));
  };
  var ActionPanelHeader = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "action-panel-header", className: cn("flex gap-2", className), ...props }, children);
  };
  var ActionPanelTitle = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement("h2", { "data-slot": "action-panel-title", className: cn("text-primary text-xl font-bold", className), ...props }, children);
  var ActionPanelActions = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement("div", { "data-slot": "action-panel-actions", className: cn("ms-auto flex items-center gap-3", className), ...props }, children);
  var ActionPanelAction = ({ className, icon, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(ComposedTooltipButton, { className: cn("flex items-center gap-2", className), ...props }, icon, " ", children);
  };
  var ActionPanel = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement("div", { "data-slot": "action-panel", className: cn("flex items-center gap-3", className), ...props }, children);
  ActionPanel.Back = ActionPanelBack;
  ActionPanel.Header = ActionPanelHeader;
  ActionPanel.Title = ActionPanelTitle;
  ActionPanel.Actions = ActionPanelActions;
  ActionPanel.Action = ActionPanelAction;
  var ActionPanel_default = ActionPanel;

  // src/components/shared/combobox/index.ts
  init_define_import_meta_env();

  // src/components/shared/combobox/Combobox.tsx
  init_define_import_meta_env();
  var import_react47 = __toESM(require_react_shim(), 1);
  var ComboboxTrigger = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    Popover_default.Trigger,
    {
      "data-slot": "combobox-trigger",
      role: "combobox",
      className: cn(
        "group/combobox-trigger bg-background relative flex w-full cursor-pointer items-center gap-2 rounded-md border p-3 text-sm shadow-xs",
        "text-muted-foreground border-muted-200 transition-[color,box-shadow]",
        "data-[state=open]:border-primary data-[state=open]:ring-primary data-[state=open]:ring",
        "hover:not-disabled:border-primary hover:not-disabled:ring-primary hover:not-disabled:ring",
        "focus-visible:ring-primary focus-visible:border-primary focus-visible:ring focus-visible:outline-none",
        "disabled:bg-muted-50 disabled:text-muted-300 disabled:pointer-events-none",
        className
      ),
      ...props
    },
    children
  );
  var ComboboxValue = ({ placeholder, children, className, ...props }) => /* @__PURE__ */ React.createElement("span", { "data-slot": "combobox-value", className: cn("flex-1 truncate text-start", className), ...props }, children ?? placeholder);
  var ComboboxClear = ({ when, title = "Clear", onClear }) => {
    if (!when) return null;
    return /* @__PURE__ */ React.createElement(
      TooltipButton_default,
      {
        asChild: true,
        title,
        variant: "ghost-muted-destructive",
        size: "unstyled",
        onPointerDown: (e2) => e2.stopPropagation(),
        onClick: (e2) => {
          e2.stopPropagation();
          e2.preventDefault();
          onClear();
        }
      },
      /* @__PURE__ */ React.createElement(X, { size: 16 })
    );
  };
  var ComboboxIcon = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement("span", { "data-slot": "combobox-icon", ...props }, children ?? /* @__PURE__ */ React.createElement(
    ChevronsUpDown,
    {
      size: 20,
      className: cn(
        "text-muted-400 shrink-0 transition-colors",
        "group-data-[state=open]/combobox-trigger:text-primary",
        "group-disabled/combobox-trigger:text-muted-300",
        className
      )
    }
  ));
  var ComboboxContent = ({ children, className, shouldFilter, align = "start", ...props }) => /* @__PURE__ */ React.createElement(
    Popover_default.Content,
    {
      align,
      className: cn("w-(--radix-popover-trigger-width) p-0", className),
      onOpenAutoFocus: (e2) => e2.preventDefault(),
      ...props
    },
    /* @__PURE__ */ React.createElement(Command_default, { shouldFilter }, children)
  );
  var ComboboxInput = (props) => /* @__PURE__ */ React.createElement(Command_default.Input, { ...props });
  var ComboboxLoader = ({ when, message: message2, className }) => {
    if (!when) return null;
    if (!message2) return /* @__PURE__ */ React.createElement(LoaderCircle, { size: 20, className: cn("text-muted-400 animate-spin", className) });
    return /* @__PURE__ */ React.createElement("div", { className: cn("text-muted-400 flex items-center justify-center gap-2 py-2", className) }, /* @__PURE__ */ React.createElement(LoaderCircle, { size: 16, className: "animate-spin" }), /* @__PURE__ */ React.createElement("span", { className: "text-xs" }, message2));
  };
  var ComboboxList = (props) => /* @__PURE__ */ React.createElement(Command_default.List, { ...props });
  var ComboboxEmpty = (props) => /* @__PURE__ */ React.createElement(Command_default.Empty, { ...props });
  var ComboboxItem = (props) => /* @__PURE__ */ React.createElement(Command_default.Item, { ...props });
  var ComboboxIndicator = (props) => /* @__PURE__ */ React.createElement(Command_default.Indicator, { ...props });
  var Combobox = ({ open, onOpenChange, children }) => /* @__PURE__ */ React.createElement(Popover_default, { open, onOpenChange }, children);
  Combobox.Trigger = (0, import_react47.forwardRef)((props, ref) => ComboboxTrigger({ ...props, ref }));
  Combobox.Value = ComboboxValue;
  Combobox.Clear = ComboboxClear;
  Combobox.Icon = ComboboxIcon;
  Combobox.Content = ComboboxContent;
  Combobox.Input = ComboboxInput;
  Combobox.Loader = ComboboxLoader;
  Combobox.List = ComboboxList;
  Combobox.Empty = ComboboxEmpty;
  Combobox.Item = ComboboxItem;
  Combobox.Indicator = ComboboxIndicator;
  Combobox.Trigger.displayName = "ComboboxTrigger";
  ComboboxValue.displayName = "ComboboxValue";
  ComboboxClear.displayName = "ComboboxClear";
  ComboboxIcon.displayName = "ComboboxIcon";
  ComboboxContent.displayName = "ComboboxContent";
  ComboboxInput.displayName = "ComboboxInput";
  ComboboxLoader.displayName = "ComboboxLoader";
  ComboboxList.displayName = "ComboboxList";
  ComboboxEmpty.displayName = "ComboboxEmpty";
  ComboboxItem.displayName = "ComboboxItem";
  ComboboxIndicator.displayName = "ComboboxIndicator";
  Combobox.displayName = "Combobox";
  var Combobox_default = Combobox;

  // src/components/shared/grid-card/index.ts
  init_define_import_meta_env();

  // src/components/shared/grid-card/GridCard.tsx
  init_define_import_meta_env();
  var gridCardListVariants = cva("", {
    variants: {
      variant: {
        muted: "bg-muted-100",
        primary: "bg-primary-25"
      },
      orientation: {
        vertical: "[&>[data-slot=card]:first-child:not(:only-child)]:rounded-s-none [&>[data-slot=card]:last-child:not(:only-child)]:rounded-e-none",
        horizontal: "",
        "flush-top": "rounded-se-none [&>[data-slot=card]:first-child:not(:only-child)]:rounded-ss-none [&>[data-slot=card]:last-child:not(:only-child)]:rounded-se-none",
        "flush-bottom": "rounded-be-none [&>[data-slot=card]:first-child:not(:only-child)]:rounded-es-none [&>[data-slot=card]:last-child:not(:only-child)]:rounded-ee-none"
      }
    },
    defaultVariants: {
      variant: "muted",
      orientation: "horizontal"
    }
  });
  var List3 = ({ className, variant, orientation, children, ...props }) => /* @__PURE__ */ React.createElement(
    "ul",
    {
      "data-slot": "grid-card-list",
      className: cn(
        "grid list-none auto-cols-[minmax(20rem,1fr)] grid-flow-col gap-0.5 rounded-xl",
        "*:data-[slot=card]:rounded-none",
        "[&>[data-slot=card]:only-child]:rounded-xl",
        "[&>[data-slot=card]:first-child:not(:only-child)]:rounded-s-xl",
        "[&>[data-slot=card]:last-child:not(:only-child)]:rounded-e-xl",
        gridCardListVariants({ variant, orientation }),
        className
      ),
      ...props
    },
    children
  );
  var Item3 = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(Card_default, { shadow: "none", className: cn("flex flex-col items-center justify-start gap-3 px-6 py-3 text-center", className), ...props }, children);
  };
  var Header2 = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(Card_default.Header, { className: cn("text-muted-300 flex flex-col items-center justify-center gap-y-1 px-0 py-0", className), ...props }, children);
  };
  var Title3 = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(Card_default.Title, { className: cn("text-muted-400 text-sm font-bold", className), ...props }, children);
  };
  var Description3 = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(Card_default.Description, { className: cn(className), ...props }, children);
  };
  var Content2 = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(Card_default.Content, { className: cn("flex w-full flex-col items-center justify-center gap-1 px-0 py-0 font-bold", className), ...props }, children);
  };
  var TruncatedText = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(PrimeTooltip_default, { content: children }, /* @__PURE__ */ React.createElement("div", { "data-slot": "grid-card-truncated-text", className: cn("w-full truncate", className), ...props }, children ?? "-"));
  };
  var Footer = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(Card_default.Footer, { className: cn("flex items-center justify-center px-0 py-0", className), ...props }, children);
  };
  var GridCard = ({ className, children, ...props }) => {
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "grid-card", className: cn("w-full min-w-0 overflow-x-auto", className), ...props }, children);
  };
  GridCard.List = List3;
  GridCard.Item = Item3;
  GridCard.Header = Header2;
  GridCard.Title = Title3;
  GridCard.Description = Description3;
  GridCard.Content = Content2;
  GridCard.TruncatedText = TruncatedText;
  GridCard.Footer = Footer;
  var GridCard_default = GridCard;

  // src/components/shared/stepper/index.ts
  init_define_import_meta_env();

  // src/components/shared/stepper/Stepper.tsx
  init_define_import_meta_env();
  var import_react48 = __toESM(require_react_shim(), 1);

  // src/assets/icons/index.ts
  init_define_import_meta_env();

  // src/assets/icons/ActiveStepIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/CheckSquareFilled.tsx
  init_define_import_meta_env();

  // src/assets/icons/CheckCircleFilled.tsx
  init_define_import_meta_env();

  // src/assets/icons/CertificationIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/FlagIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/ExcelSheetIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/RescheduleIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/QuraAILogo.tsx
  init_define_import_meta_env();

  // src/assets/icons/QuraAILoader.tsx
  init_define_import_meta_env();

  // src/assets/icons/SignatureIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/AppointmentIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/LocationIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/TaxReturnIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/DoctorIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/BillIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/SmsIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/GenderIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/SortIcon.tsx
  init_define_import_meta_env();
  function SortIcon({ direction, ...props }) {
    return /* @__PURE__ */ React.createElement("span", { className: "inline-flex flex-col items-center justify-center gap-0.5" }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: direction === "asc" }, /* @__PURE__ */ React.createElement("svg", { width: "8", height: "6", viewBox: "0 0 8 6", fill: "none", xmlns: "http://www.w3.org/2000/svg", ...props }, /* @__PURE__ */ React.createElement("path", { d: "M4 0L7.4641 6H0.535898L4 0Z", className: "fill-primary-700" })), /* @__PURE__ */ React.createElement("svg", { width: "8", height: "6", viewBox: "0 0 8 6", fill: "none", xmlns: "http://www.w3.org/2000/svg" }, /* @__PURE__ */ React.createElement("path", { d: "M4 6L0.535898 0L7.4641 0L4 6Z", className: "fill-primary-50" }))), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: direction === "desc" }, /* @__PURE__ */ React.createElement("svg", { width: "8", height: "6", viewBox: "0 0 8 6", fill: "none", xmlns: "http://www.w3.org/2000/svg", ...props }, /* @__PURE__ */ React.createElement("path", { d: "M4 0L7.4641 6H0.535898L4 0Z", className: "fill-primary-50" })), /* @__PURE__ */ React.createElement("svg", { width: "8", height: "6", viewBox: "0 0 8 6", fill: "none", xmlns: "http://www.w3.org/2000/svg" }, /* @__PURE__ */ React.createElement("path", { d: "M4 6L0.535898 0L7.4641 0L4 6Z", className: "fill-primary-700" }))), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement("svg", { width: "8", height: "6", viewBox: "0 0 8 6", fill: "none", xmlns: "http://www.w3.org/2000/svg", ...props }, /* @__PURE__ */ React.createElement("path", { d: "M4 0L7.4641 6H0.535898L4 0Z", className: "fill-primary-50" })), /* @__PURE__ */ React.createElement("svg", { width: "8", height: "6", viewBox: "0 0 8 6", fill: "none", xmlns: "http://www.w3.org/2000/svg" }, /* @__PURE__ */ React.createElement("path", { d: "M4 6L0.535898 0L7.4641 0L4 6Z", className: "fill-primary-50" })))));
  }
  var SortIcon_default = SortIcon;

  // src/assets/icons/CertificateIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/StepCircleCheckedIcon.tsx
  init_define_import_meta_env();
  function StepCircleCheckedIcon(props) {
    return /* @__PURE__ */ React.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", xmlns: "http://www.w3.org/2000/svg", ...props }, /* @__PURE__ */ React.createElement(
      "path",
      {
        d: "M12 22C17.5 22 22 17.5 22 12C22 6.5 17.5 2 12 2C6.5 2 2 6.5 2 12C2 17.5 6.5 22 12 22Z",
        stroke: "currentColor",
        strokeWidth: "1.5",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }
    ), /* @__PURE__ */ React.createElement("path", { d: "M12 20C16.4 20 20 16.4 20 12C20 7.6 16.4 4 12 4C7.6 4 4 7.6 4 12C4 16.4 7.6 20 12 20Z", fill: "currentColor" }), /* @__PURE__ */ React.createElement("path", { d: "M8 12.5L10.58 14.8299L15.5 10", stroke: "white", strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }));
  }
  var StepCircleCheckedIcon_default = StepCircleCheckedIcon;

  // src/assets/icons/StepCircleFilledIcon.tsx
  init_define_import_meta_env();
  function StepCircleFilledIcon(props) {
    return /* @__PURE__ */ React.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", xmlns: "http://www.w3.org/2000/svg", ...props }, /* @__PURE__ */ React.createElement(
      "path",
      {
        d: "M12 22C17.5 22 22 17.5 22 12C22 6.5 17.5 2 12 2C6.5 2 2 6.5 2 12C2 17.5 6.5 22 12 22Z",
        stroke: "currentColor",
        strokeWidth: "1.5",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }
    ), /* @__PURE__ */ React.createElement("path", { d: "M12 16C14.2 16 16 14.2 16 12C16 9.8 14.2 8 12 8C9.8 8 8 9.8 8 12C8 14.2 9.8 16 12 16Z", fill: "currentColor" }));
  }
  var StepCircleFilledIcon_default = StepCircleFilledIcon;

  // src/assets/icons/StepCircleRejectIcon.tsx
  init_define_import_meta_env();
  function StepCircleRejectIcon(props) {
    return /* @__PURE__ */ React.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", xmlns: "http://www.w3.org/2000/svg", ...props }, /* @__PURE__ */ React.createElement(
      "path",
      {
        d: "M12 22C17.5 22 22 17.5 22 12C22 6.5 17.5 2 12 2C6.5 2 2 6.5 2 12C2 17.5 6.5 22 12 22Z",
        stroke: "currentColor",
        strokeWidth: "1.5",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }
    ), /* @__PURE__ */ React.createElement("path", { d: "M12 20C16.4 20 20 16.4 20 12C20 7.6 16.4 4 12 4C7.6 4 4 7.6 4 12C4 16.4 7.6 20 12 20Z", fill: "currentColor" }), /* @__PURE__ */ React.createElement("path", { d: "M9.16992 14.8299L14.8299 9.16992", stroke: "white", strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }), /* @__PURE__ */ React.createElement("path", { d: "M14.8299 14.8299L9.16992 9.16992", stroke: "white", strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }));
  }
  var StepCircleRejectIcon_default = StepCircleRejectIcon;

  // src/assets/icons/StepCircleEmptyIcon.tsx
  init_define_import_meta_env();
  function StepCircleEmptyIcon(props) {
    return /* @__PURE__ */ React.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", xmlns: "http://www.w3.org/2000/svg", ...props }, /* @__PURE__ */ React.createElement(
      "path",
      {
        d: "M12 22C17.5 22 22 17.5 22 12C22 6.5 17.5 2 12 2C6.5 2 2 6.5 2 12C2 17.5 6.5 22 12 22Z",
        stroke: "currentColor",
        strokeWidth: "1.5",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }
    ));
  }
  var StepCircleEmptyIcon_default = StepCircleEmptyIcon;

  // src/assets/icons/PatientBedIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/CalendarTimeIcon.tsx
  init_define_import_meta_env();

  // src/assets/icons/LocationTimeIcon.tsx
  init_define_import_meta_env();

  // src/components/shared/stepper/Stepper.tsx
  var STEP_ICONS = {
    done: StepCircleCheckedIcon_default,
    current: StepCircleFilledIcon_default,
    error: StepCircleRejectIcon_default,
    next: StepCircleEmptyIcon_default
  };
  var stepIconVariants = cva("size-6 shrink-0", {
    variants: {
      status: {
        done: "text-primary-400",
        current: "text-primary-400",
        error: "text-destructive-400",
        next: "text-primary-50"
      }
    },
    defaultVariants: {
      status: "current"
    }
  });
  var Step = ({ className, ...props }) => /* @__PURE__ */ React.createElement("div", { "data-slot": "stepper-step", className: cn("flex shrink-0 flex-col items-center gap-1", className), ...props });
  var Icon3 = ({ status, className }) => {
    const IconComponent = STEP_ICONS[status];
    return /* @__PURE__ */ React.createElement(IconComponent, { className: cn(stepIconVariants({ status }), className) });
  };
  var Label3 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("span", { "data-slot": "stepper-label", className: cn("truncate text-xs", className), ...props });
  var Stepper = ({ className, children, ...props }) => {
    const steps = import_react48.Children.toArray(children);
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "stepper", className: cn("flex w-full items-start gap-3", className), ...props }, steps.map((step, index2) => /* @__PURE__ */ React.createElement(import_react48.Fragment, { key: index2 }, step, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: index2 < steps.length - 1 }, /* @__PURE__ */ React.createElement("div", { className: "flex flex-1 items-center self-start pt-3" }, /* @__PURE__ */ React.createElement(Divider_default, null))))));
  };
  Step.displayName = "StepperStep";
  Stepper.Step = Step;
  Stepper.Icon = Icon3;
  Stepper.Label = Label3;
  var Stepper_default = Stepper;

  // src/components/shared/field-section/index.ts
  init_define_import_meta_env();

  // src/components/shared/field-section/FieldSection.tsx
  init_define_import_meta_env();
  var import_react49 = __toESM(require_react_shim(), 1);
  var fieldSectionHeaderVariants = cva("text-sm", {
    variants: {
      variant: {
        default: "text-foreground font-medium",
        primary: "text-primary font-medium",
        secondary: "text-muted-foreground font-medium",
        subtle: "text-muted-400"
      }
    },
    defaultVariants: {
      variant: "default"
    }
  });
  var FieldSectionContext = (0, import_react49.createContext)(null);
  var useFieldSectionContext = () => {
    const context2 = (0, import_react49.useContext)(FieldSectionContext);
    if (!context2) throw new Error("FieldSection subcomponents must be used within <FieldSection>");
    return context2;
  };
  var FieldSectionHeader = ({ className, children, ...props }) => {
    const { variant } = useFieldSectionContext();
    return /* @__PURE__ */ React.createElement(
      "header",
      {
        "data-slot": "field-section-header",
        className: cn("flex items-center gap-2", fieldSectionHeaderVariants({ variant }), className),
        ...props
      },
      children
    );
  };
  var FieldSectionDivider = ({ className, variant = "dashed", ...props }) => /* @__PURE__ */ React.createElement(Divider_default, { variant, className: cn("w-auto grow", className), ...props });
  var FieldSectionContent = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement("div", { "data-slot": "field-section-content", className, ...props }, children);
  var FieldSection = ({ variant = "default", className, children, ...props }) => /* @__PURE__ */ React.createElement(FieldSectionContext.Provider, { value: { variant } }, /* @__PURE__ */ React.createElement("section", { "data-slot": "field-section", className: cn("flex flex-col gap-3", className), ...props }, children));
  FieldSection.Header = FieldSectionHeader;
  FieldSection.Divider = FieldSectionDivider;
  FieldSection.Content = FieldSectionContent;
  FieldSectionHeader.displayName = "FieldSectionHeader";
  FieldSectionDivider.displayName = "FieldSectionDivider";
  FieldSectionContent.displayName = "FieldSectionContent";
  FieldSection.displayName = "FieldSection";
  var FieldSection_default = FieldSection;

  // src/components/shared/file-card/index.ts
  init_define_import_meta_env();

  // src/components/shared/file-card/FileCard.tsx
  init_define_import_meta_env();
  var import_react50 = __toESM(require_react_shim(), 1);
  var FileCardContext = (0, import_react50.createContext)(null);
  var useFileCardContext = () => {
    const context2 = (0, import_react50.useContext)(FileCardContext);
    if (!context2) throw new Error("FileCard subcomponents must be used within <FileCard>");
    return context2;
  };
  var Header3 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Card_default.Header, { className: cn("flex items-center justify-between gap-2 px-4 py-3", className), ...props }, children);
  var Title4 = ({ className, ...props }) => {
    const { entry, accessors } = useFileCardContext();
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "file-card-title", className: cn("flex min-w-0 items-center gap-2", className), ...props }, /* @__PURE__ */ React.createElement(FileIcon_default, { extension: accessors.getFileType(entry), variant: "chip", size: 20 }), /* @__PURE__ */ React.createElement(PrimeTooltip_default, { content: accessors.getTitle(entry) }, /* @__PURE__ */ React.createElement("span", { className: "truncate text-sm font-bold" }, accessors.getTitle(entry))));
  };
  var Actions3 = ({ children, align = "end" }) => {
    const { entry } = useFileCardContext();
    const { t: t3 } = useAppTranslation("common");
    return /* @__PURE__ */ React.createElement(PrimeDropdown_default, { align }, /* @__PURE__ */ React.createElement(PrimeDropdown_default.Trigger, { asChild: true }, /* @__PURE__ */ React.createElement(TooltipButton_default, { title: t3("actions"), type: "button", variant: "ghost", size: "icon-sm" }, /* @__PURE__ */ React.createElement(Ellipsis, { size: 16 }))), /* @__PURE__ */ React.createElement(PrimeDropdown_default.List, null, children(entry)));
  };
  var Preview = ({ children, className }) => {
    const { entry } = useFileCardContext();
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "file-card-preview", className: cn("flex min-h-44 items-center justify-center px-4 py-2", className) }, children(entry));
  };
  var FileShape = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "file-card-file-shape",
      className: cn("relative isolate flex min-h-40 w-full flex-col items-center justify-center gap-3 px-6 py-4", className),
      ...props
    },
    /* @__PURE__ */ React.createElement(FileText, { size: 160, strokeWidth: 1.25, className: "text-primary-15 absolute inset-0 -z-10 m-auto" }),
    /* @__PURE__ */ React.createElement("div", { className: "flex flex-col items-center gap-3" }, children)
  );
  var FolderShape = ({ className, count: count4, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "file-card-folder-shape",
      className: cn(
        "bg-primary-15 relative flex min-h-40 w-full max-w-44 flex-col items-center justify-center gap-2 rounded-md px-4 py-6",
        className
      ),
      ...props
    },
    /* @__PURE__ */ React.createElement(FolderOpen, { size: 36, className: "text-primary-200" }),
    /* @__PURE__ */ React.createElement("span", { className: "text-2xl font-bold" }, count4),
    /* @__PURE__ */ React.createElement("span", { className: "text-2xs text-muted-400 font-medium uppercase" }, count4 === 1 ? "document" : "documents")
  );
  var ApprovalPrompt = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement("p", { "data-slot": "file-card-approval-prompt", className: cn("text-muted-400 text-xs font-medium", className), ...props }, children);
  var Approve = ({ children = "Approve", ...props }) => /* @__PURE__ */ React.createElement(ComposedLoadingButton, { variant: "outline-success", size: "lg", ...props }, children);
  var Reject = ({ children = "Reject", ...props }) => /* @__PURE__ */ React.createElement(ComposedLoadingButton, { variant: "outline-destructive", size: "lg", ...props }, children);
  var Status = ({
    options: options3,
    value,
    getOptionLabel,
    getOptionValue,
    getOptionDot,
    getOptionDisabled,
    queryKey = "file-card-status-options",
    urlSearchParams,
    placeholder = "Select status",
    disabled,
    onChange
  }) => {
    const { t: t3 } = useAppTranslation("common");
    const [open, setOpen] = (0, import_react50.useState)(false);
    const { search, setSearch, allOptions, isAsync, onScroll, isLoading, isFetchingNextPage } = useComboboxOptions({
      options: options3,
      open,
      queryKey,
      urlSearchParams
    });
    const selectedValue = value ? getOptionValue(value) : null;
    const onSelect = (option) => {
      onChange(option);
      setOpen(false);
    };
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "file-card-status", className: "px-4 py-3" }, /* @__PURE__ */ React.createElement(Combobox_default, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(Combobox_default.Trigger, { className: "bg-primary-15 rounded-full border-none px-4 py-2 shadow-none", disabled }, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!value && !!getOptionDot }, /* @__PURE__ */ React.createElement("span", { className: cn("size-3 rounded-full", value && getOptionDot?.(value)) })), /* @__PURE__ */ React.createElement(Combobox_default.Value, { className: "text-sm font-bold" }, value ? getOptionLabel(value) : placeholder), /* @__PURE__ */ React.createElement(Combobox_default.Icon, null)), /* @__PURE__ */ React.createElement(Combobox_default.Content, { shouldFilter: !isAsync }, /* @__PURE__ */ React.createElement(Combobox_default.Input, { placeholder: t3("searchPlaceholder"), ...isAsync && { value: search, onValueChange: setSearch } }, /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: isLoading })), /* @__PURE__ */ React.createElement(Combobox_default.List, { onScroll }, /* @__PURE__ */ React.createElement(Combobox_default.Empty, { when: !isLoading && allOptions.length === 0 }, "No options"), allOptions.map((option) => {
      const optValue = getOptionValue(option);
      return /* @__PURE__ */ React.createElement(
        Combobox_default.Item,
        {
          key: optValue,
          value: getOptionLabel(option),
          disabled: getOptionDisabled?.(option),
          "data-checked": selectedValue === optValue || void 0,
          onSelect: () => onSelect(option)
        },
        /* @__PURE__ */ React.createElement(Combobox_default.Indicator, null),
        /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!getOptionDot }, /* @__PURE__ */ React.createElement("span", { className: cn("size-3 rounded-full", getOptionDot?.(option)) })),
        getOptionLabel(option)
      );
    }), /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: isFetchingNextPage, message: "Loading more..." })))));
  };
  var Footer2 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Card_default.Footer, { className: cn("flex items-center justify-between gap-2 px-4 py-3", className), ...props }, children);
  var Assignee = ({
    options: options3,
    value,
    getOptionLabel,
    getOptionValue,
    getOptionAvatar,
    queryKey = "file-card-assignee-options",
    urlSearchParams,
    placeholder = "Unassigned",
    clearTitle,
    disabled,
    onChange
  }) => {
    const { t: t3 } = useAppTranslation("common");
    const [open, setOpen] = (0, import_react50.useState)(false);
    const { search, setSearch, allOptions, isAsync, onScroll, isLoading, isFetchingNextPage } = useComboboxOptions({
      options: options3,
      open,
      queryKey,
      urlSearchParams,
      selectedItemsIds: value ? [getOptionValue(value)] : void 0
    });
    const selectedValue = value ? getOptionValue(value) : null;
    const selectedLabel = value ? getOptionLabel(value) : placeholder;
    const selectedAvatarUrl = value ? getOptionAvatar?.(value) : void 0;
    const selectedInitials = value ? getOptionLabel(value).slice(0, 2).toUpperCase() : "?";
    const onSelect = (option) => {
      onChange(option);
      setOpen(false);
    };
    const onClear = () => onChange(null);
    return /* @__PURE__ */ React.createElement(Combobox_default, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(
      Combobox_default.Trigger,
      {
        className: cn(
          "w-auto max-w-3/4 rounded-none border-0 bg-transparent p-0 shadow-none ring-0",
          "hover:not-disabled:border-transparent hover:not-disabled:ring-0",
          "focus-visible:border-transparent focus-visible:ring-0 focus-visible:outline-none",
          "data-[state=open]:border-transparent data-[state=open]:ring-0"
        ),
        disabled
      },
      /* @__PURE__ */ React.createElement(Avatar_default, { className: "text-2xs ring-primary-25 size-7 shrink-0 font-bold ring-2" }, /* @__PURE__ */ React.createElement(Avatar_default.Image, { src: selectedAvatarUrl, alt: selectedLabel }), /* @__PURE__ */ React.createElement(Avatar_default.Fallback, { className: "bg-primary text-primary-foreground" }, selectedInitials)),
      /* @__PURE__ */ React.createElement(PrimeTooltip_default, { content: selectedLabel }, /* @__PURE__ */ React.createElement("span", { className: "truncate text-sm font-medium" }, selectedLabel)),
      /* @__PURE__ */ React.createElement(Combobox_default.Clear, { when: !!value && !disabled, title: clearTitle, onClear }),
      /* @__PURE__ */ React.createElement(ChevronDown, { size: 14, className: "text-muted-400 shrink-0" })
    ), /* @__PURE__ */ React.createElement(Combobox_default.Content, { className: "w-(--radix-popover-content-available-width) min-w-64", shouldFilter: !isAsync }, /* @__PURE__ */ React.createElement(Combobox_default.Input, { placeholder: t3("searchPlaceholder"), ...isAsync && { value: search, onValueChange: setSearch } }, /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: isLoading })), /* @__PURE__ */ React.createElement(Combobox_default.List, { onScroll }, /* @__PURE__ */ React.createElement(Combobox_default.Empty, { when: !isLoading && allOptions.length === 0 }, "No assignees"), allOptions.map((option) => {
      const optValue = getOptionValue(option);
      return /* @__PURE__ */ React.createElement(
        Combobox_default.Item,
        {
          key: optValue,
          value: getOptionLabel(option),
          "data-checked": selectedValue === optValue || void 0,
          onSelect: () => onSelect(option)
        },
        /* @__PURE__ */ React.createElement(Combobox_default.Indicator, null),
        /* @__PURE__ */ React.createElement(Avatar_default, { className: "text-2xs ring-primary-25 size-6 font-bold ring-2" }, /* @__PURE__ */ React.createElement(Avatar_default.Image, { src: getOptionAvatar?.(option), alt: getOptionLabel(option) }), /* @__PURE__ */ React.createElement(Avatar_default.Fallback, { className: "bg-primary text-primary-foreground" }, getOptionLabel(option).slice(0, 2).toUpperCase())),
        getOptionLabel(option)
      );
    }), /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: isFetchingNextPage, message: "Loading more..." }))));
  };
  var Date2 = ({ className, ...props }) => {
    const { entry, accessors } = useFileCardContext();
    const { formatNullableDate, constants } = useDate();
    return /* @__PURE__ */ React.createElement("span", { "data-slot": "file-card-date", className: cn("text-muted-400 text-xs font-medium", className), ...props }, formatNullableDate(accessors.getDate(entry), constants.DateFormats.US_DATE, ""));
  };
  function FileCardRoot({ entry, accessors, onClick, className, children, ...props }) {
    const contextValue = (0, import_react50.useMemo)(() => ({ entry, accessors }), [entry, accessors]);
    return /* @__PURE__ */ React.createElement(FileCardContext.Provider, { value: contextValue }, /* @__PURE__ */ React.createElement(
      Card_default,
      {
        className: cn(
          "border-primary-25 flex flex-col border transition-transform",
          onClick && "cursor-pointer hover:-translate-y-1",
          className
        ),
        onClick: onClick ? () => onClick(entry) : void 0,
        ...props
      },
      children
    ));
  }
  var FileCard = FileCardRoot;
  FileCard.Header = Header3;
  FileCard.Title = Title4;
  FileCard.Actions = Actions3;
  FileCard.Item = PrimeDropdown_default.Item;
  FileCard.Separator = PrimeDropdown_default.Separator;
  FileCard.Preview = Preview;
  FileCard.FileShape = FileShape;
  FileCard.FolderShape = FolderShape;
  FileCard.StatusPill = Chip_default;
  FileCard.ApprovalPrompt = ApprovalPrompt;
  FileCard.Approve = (0, import_react50.forwardRef)((props, ref) => Approve({ ...props, ref }));
  FileCard.Reject = (0, import_react50.forwardRef)((props, ref) => Reject({ ...props, ref }));
  FileCard.Status = Status;
  FileCard.Footer = Footer2;
  FileCard.Assignee = Assignee;
  FileCard.Date = Date2;
  Header3.displayName = "FileCardHeader";
  Title4.displayName = "FileCardTitle";
  Preview.displayName = "FileCardPreview";
  FileShape.displayName = "FileCardFileShape";
  FolderShape.displayName = "FileCardFolderShape";
  ApprovalPrompt.displayName = "FileCardApprovalPrompt";
  FileCard.Approve.displayName = "FileCardApprove";
  FileCard.Reject.displayName = "FileCardReject";
  Footer2.displayName = "FileCardFooter";
  Date2.displayName = "FileCardDate";
  var FileCard_default = FileCard;

  // src/components/shared/command-palette/index.ts
  init_define_import_meta_env();

  // src/components/shared/command-palette/CommandPalette.tsx
  init_define_import_meta_env();
  function CommandPalette() {
    const navigate = useNavigate();
    const { open, setOpen } = useCommandPalette();
    const { theme, setTheme } = useTheme();
    const { removeCurrentUser } = useAuth();
    const { t: t3 } = useAppTranslation("nav");
    const isDark = theme === THEME_TYPES.DARK;
    const runCommand = (command) => {
      setOpen(false);
      command();
    };
    return /* @__PURE__ */ React.createElement(Command_default.Dialog, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(Dialog_default.Title, { className: "sr-only" }, t3("commandPalette")), /* @__PURE__ */ React.createElement(Command_default.Input, { placeholder: t3("header:searchPlaceholder") }), /* @__PURE__ */ React.createElement(Command_default.List, null, /* @__PURE__ */ React.createElement(Command_default.Empty, { when: true }, t3("common:noResults")), /* @__PURE__ */ React.createElement(Command_default.Group, { heading: t3("navigation") }, APP_MENU.map((route) => /* @__PURE__ */ React.createElement(Command_default.Item, { key: route.id, value: t3(route.id), onSelect: () => runCommand(() => navigate(route.path)) }, /* @__PURE__ */ React.createElement(route.icon, { className: "h-4 w-4" }), t3(route.id)))), /* @__PURE__ */ React.createElement(Command_default.Separator, null), /* @__PURE__ */ React.createElement(Command_default.Group, { heading: t3("actions") }, /* @__PURE__ */ React.createElement(
      Command_default.Item,
      {
        value: "toggle theme dark light",
        onSelect: () => runCommand(() => setTheme(isDark ? THEME_TYPES.LIGHT : THEME_TYPES.DARK))
      },
      isDark ? /* @__PURE__ */ React.createElement(Sun, { className: "h-4 w-4" }) : /* @__PURE__ */ React.createElement(Moon, { className: "h-4 w-4" }),
      t3("toggleTheme")
    ), /* @__PURE__ */ React.createElement(Command_default.Item, { value: "log out sign out", onSelect: () => runCommand(removeCurrentUser) }, /* @__PURE__ */ React.createElement(LogOut, { className: "h-4 w-4" }), t3("header:logout")))));
  }
  var CommandPalette_default = CommandPalette;

  // src/components/ui/layout/Layout.tsx
  var LayoutContext = (0, import_react51.createContext)(null);
  var useLayoutContext = () => {
    const context2 = (0, import_react51.useContext)(LayoutContext);
    if (context2 === null) {
      throw new Error("Layout components must be used within Layout");
    }
    return context2;
  };
  var Header4 = ({ className, sticky = false, ...props }) => {
    const { offset: offset4, fixed } = useLayoutContext();
    return /* @__PURE__ */ React.createElement(
      "header",
      {
        "data-slot": "layout.header",
        className: cn(
          "bg-background flex items-center gap-4 p-4",
          offset4 > 10 && sticky ? "shadow-sm" : "shadow-none",
          fixed && "",
          sticky && "sticky top-0 z-10",
          className
        ),
        ...props
      }
    );
  };
  var Sidebar = ({ className, ...props }) => {
    const { fixed } = useLayoutContext();
    return /* @__PURE__ */ React.createElement(
      "aside",
      {
        "data-slot": "layout.sidebar",
        className: cn("bg-background relative z-50 transition-[width]", fixed && "sticky top-0", className),
        ...props
      }
    );
  };
  var Content3 = ({ className, children, ...props }) => {
    const { fixed } = useLayoutContext();
    return /* @__PURE__ */ React.createElement("main", { "data-slot": "layout.content", className: cn("relative p-2 md:px-10 md:py-4", fixed && className), ...props }, /* @__PURE__ */ React.createElement(import_react51.Suspense, { fallback: /* @__PURE__ */ React.createElement(PrimeLoader_default, null) }, children || /* @__PURE__ */ React.createElement(Outlet, null)));
  };
  var Footer3 = ({ className, ...props }) => {
    const { fixed } = useLayoutContext();
    return /* @__PURE__ */ React.createElement("footer", { "data-slot": "layout.footer", className: cn(fixed && "", className), ...props });
  };
  var Layout = ({ className, fixed = false, ...props }) => {
    const divRef = (0, import_react51.useRef)(null);
    const [offset4, setOffset] = (0, import_react51.useState)(0);
    (0, import_react51.useEffect)(() => {
      const div = divRef.current;
      if (!div) return void 0;
      const onScroll = () => setOffset(div.scrollTop);
      div.removeEventListener("scroll", onScroll);
      div.addEventListener("scroll", onScroll, { passive: true });
      return () => div.removeEventListener("scroll", onScroll);
    }, []);
    return /* @__PURE__ */ React.createElement(LayoutContext.Provider, { value: { offset: offset4, fixed } }, /* @__PURE__ */ React.createElement(
      "div",
      {
        ref: divRef,
        "data-slot": "layout",
        className: cn("min-h-dvh", fixed && "md:grid md:grid-cols-[auto_1fr] md:grid-rows-[auto_1fr]", className),
        ...props
      }
    ));
  };
  Layout.Header = Header4;
  Layout.Sidebar = Sidebar;
  Layout.Content = Content3;
  Layout.Footer = Footer3;
  Layout.displayName = "Layout";
  Header4.displayName = "Layout.Header";
  Sidebar.displayName = "Layout.Sidebar";
  Content3.displayName = "Layout.Content";
  Footer3.displayName = "Layout.Footer";
  var Layout_default = Layout;

  // src/components/ui/alert/index.ts
  init_define_import_meta_env();

  // src/components/ui/alert/Alert.tsx
  init_define_import_meta_env();
  var alertVariants = cva(
    `
  grid w-full grid-cols-[auto_1fr] items-center gap-y-1 rounded-lg p-4
  has-[>svg]:gap-x-3 *:data-[slot=alert-title]:col-start-2 *:data-[slot=alert-description]:col-start-2 [&:has(>[data-slot=alert-description])>svg]:row-span-2
  `,
    {
      variants: {
        variant: {
          default: "bg-muted-50 text-muted-foreground border border-muted-200 [&>svg]:text-muted-foreground",
          info: "bg-primary-15 text-foreground border border-primary-25 [&>svg]:text-primary",
          destructive: "bg-destructive-200 text-foreground border border-destructive-300 [&>svg]:text-destructive",
          warning: "bg-warning-200 text-foreground border border-warning-300 [&>svg]:text-warning",
          success: "bg-success-200 text-foreground border border-success-300 [&>svg]:text-success",
          outline: "border border-primary-400 text-primary [&>svg]:text-primary",
          "outline-info": "border border-muted-400 bg-background text-foreground [&>svg]:text-foreground",
          "outline-destructive": "border border-destructive-400 text-destructive [&>svg]:text-destructive",
          "outline-success": "border border-success-400 text-success [&>svg]:text-success"
        }
      },
      defaultVariants: {
        variant: "default"
      }
    }
  );
  function Title5({ className, ...props }) {
    return /* @__PURE__ */ React.createElement("h5", { "data-slot": "alert-title", className: cn("mb-0.5 leading-none font-medium tracking-tight", className), ...props });
  }
  function Description4({ className, ...props }) {
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "alert-description", className: cn("text-sm [&_p]:leading-relaxed", className), ...props });
  }
  var Alert = ({ className, variant, ...props }) => /* @__PURE__ */ React.createElement("div", { role: "alert", "data-slot": "alert", className: cn(alertVariants({ variant }), className), ...props });
  Alert.Title = Title5;
  Alert.Description = Description4;
  var Alert_default = Alert;

  // src/components/ui/button/index.ts
  init_define_import_meta_env();

  // src/components/ui/toast/index.ts
  init_define_import_meta_env();

  // src/components/ui/toast/Toast.tsx
  init_define_import_meta_env();
  var import_react54 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-toast/dist/index.mjs
  init_define_import_meta_env();
  var React31 = __toESM(require_react_shim(), 1);
  var ReactDOM5 = __toESM(require_react_dom_shim(), 1);

  // node_modules/@radix-ui/primitive/dist/index.mjs
  init_define_import_meta_env();
  var canUseDOM = !!(typeof window !== "undefined" && window.document && window.document.createElement);
  function composeEventHandlers(originalEventHandler, ourEventHandler, { checkForDefaultPrevented = true } = {}) {
    return function handleEvent(event) {
      originalEventHandler?.(event);
      if (checkForDefaultPrevented === false || !event.defaultPrevented) {
        return ourEventHandler?.(event);
      }
    };
  }

  // node_modules/@radix-ui/react-collection/dist/index.mjs
  init_define_import_meta_env();
  var import_react52 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-context/dist/index.mjs
  init_define_import_meta_env();
  var React15 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime5 = __toESM(require_react_shim(), 1);
  function createContext22(rootComponentName, defaultContext2) {
    const Context = React15.createContext(defaultContext2);
    const Provider4 = (props) => {
      const { children, ...context2 } = props;
      const value = React15.useMemo(() => context2, Object.values(context2));
      return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(Context.Provider, { value, children });
    };
    Provider4.displayName = rootComponentName + "Provider";
    function useContext210(consumerName) {
      const context2 = React15.useContext(Context);
      if (context2) return context2;
      if (defaultContext2 !== void 0) return defaultContext2;
      throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
    }
    return [Provider4, useContext210];
  }
  function createContextScope(scopeName, createContextScopeDeps = []) {
    let defaultContexts = [];
    function createContext32(rootComponentName, defaultContext2) {
      const BaseContext = React15.createContext(defaultContext2);
      const index2 = defaultContexts.length;
      defaultContexts = [...defaultContexts, defaultContext2];
      const Provider4 = (props) => {
        const { scope, children, ...context2 } = props;
        const Context = scope?.[scopeName]?.[index2] || BaseContext;
        const value = React15.useMemo(() => context2, Object.values(context2));
        return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(Context.Provider, { value, children });
      };
      Provider4.displayName = rootComponentName + "Provider";
      function useContext210(consumerName, scope) {
        const Context = scope?.[scopeName]?.[index2] || BaseContext;
        const context2 = React15.useContext(Context);
        if (context2) return context2;
        if (defaultContext2 !== void 0) return defaultContext2;
        throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
      }
      return [Provider4, useContext210];
    }
    const createScope = () => {
      const scopeContexts = defaultContexts.map((defaultContext2) => {
        return React15.createContext(defaultContext2);
      });
      return function useScope(scope) {
        const contexts = scope?.[scopeName] || scopeContexts;
        return React15.useMemo(
          () => ({ [`__scope${scopeName}`]: { ...scope, [scopeName]: contexts } }),
          [scope, contexts]
        );
      };
    };
    createScope.scopeName = scopeName;
    return [createContext32, composeContextScopes(createScope, ...createContextScopeDeps)];
  }
  function composeContextScopes(...scopes) {
    const baseScope = scopes[0];
    if (scopes.length === 1) return baseScope;
    const createScope = () => {
      const scopeHooks = scopes.map((createScope2) => ({
        useScope: createScope2(),
        scopeName: createScope2.scopeName
      }));
      return function useComposedScopes(overrideScopes) {
        const nextScopes = scopeHooks.reduce((nextScopes2, { useScope, scopeName }) => {
          const scopeProps = useScope(overrideScopes);
          const currentScope = scopeProps[`__scope${scopeName}`];
          return { ...nextScopes2, ...currentScope };
        }, {});
        return React15.useMemo(() => ({ [`__scope${baseScope.scopeName}`]: nextScopes }), [nextScopes]);
      };
    };
    createScope.scopeName = baseScope.scopeName;
    return createScope;
  }

  // node_modules/@radix-ui/react-collection/node_modules/@radix-ui/react-slot/dist/index.mjs
  init_define_import_meta_env();
  var React16 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime6 = __toESM(require_react_shim(), 1);
  // @__NO_SIDE_EFFECTS__
  function createSlot2(ownerName) {
    const SlotClone = /* @__PURE__ */ createSlotClone2(ownerName);
    const Slot22 = React16.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      const childrenArray = React16.Children.toArray(children);
      const slottable = childrenArray.find(isSlottable2);
      if (slottable) {
        const newElement = slottable.props.children;
        const newChildren = childrenArray.map((child) => {
          if (child === slottable) {
            if (React16.Children.count(newElement) > 1) return React16.Children.only(null);
            return React16.isValidElement(newElement) ? newElement.props.children : null;
          } else {
            return child;
          }
        });
        return /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children: React16.isValidElement(newElement) ? React16.cloneElement(newElement, void 0, newChildren) : null });
      }
      return /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children });
    });
    Slot22.displayName = `${ownerName}.Slot`;
    return Slot22;
  }
  // @__NO_SIDE_EFFECTS__
  function createSlotClone2(ownerName) {
    const SlotClone = React16.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      if (React16.isValidElement(children)) {
        const childrenRef = getElementRef2(children);
        const props2 = mergeProps2(slotProps, children.props);
        if (children.type !== React16.Fragment) {
          props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
        }
        return React16.cloneElement(children, props2);
      }
      return React16.Children.count(children) > 1 ? React16.Children.only(null) : null;
    });
    SlotClone.displayName = `${ownerName}.SlotClone`;
    return SlotClone;
  }
  var SLOTTABLE_IDENTIFIER2 = /* @__PURE__ */ Symbol("radix.slottable");
  function isSlottable2(child) {
    return React16.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER2;
  }
  function mergeProps2(slotProps, childProps) {
    const overrideProps = { ...childProps };
    for (const propName in childProps) {
      const slotPropValue = slotProps[propName];
      const childPropValue = childProps[propName];
      const isHandler = /^on[A-Z]/.test(propName);
      if (isHandler) {
        if (slotPropValue && childPropValue) {
          overrideProps[propName] = (...args) => {
            const result = childPropValue(...args);
            slotPropValue(...args);
            return result;
          };
        } else if (slotPropValue) {
          overrideProps[propName] = slotPropValue;
        }
      } else if (propName === "style") {
        overrideProps[propName] = { ...slotPropValue, ...childPropValue };
      } else if (propName === "className") {
        overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
      }
    }
    return { ...slotProps, ...overrideProps };
  }
  function getElementRef2(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }

  // node_modules/@radix-ui/react-collection/dist/index.mjs
  var import_jsx_runtime7 = __toESM(require_react_shim(), 1);
  var import_react53 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime8 = __toESM(require_react_shim(), 1);
  function createCollection(name) {
    const PROVIDER_NAME3 = name + "CollectionProvider";
    const [createCollectionContext, createCollectionScope6] = createContextScope(PROVIDER_NAME3);
    const [CollectionProviderImpl, useCollectionContext] = createCollectionContext(
      PROVIDER_NAME3,
      { collectionRef: { current: null }, itemMap: /* @__PURE__ */ new Map() }
    );
    const CollectionProvider = (props) => {
      const { scope, children } = props;
      const ref = import_react52.default.useRef(null);
      const itemMap = import_react52.default.useRef(/* @__PURE__ */ new Map()).current;
      return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(CollectionProviderImpl, { scope, itemMap, collectionRef: ref, children });
    };
    CollectionProvider.displayName = PROVIDER_NAME3;
    const COLLECTION_SLOT_NAME = name + "CollectionSlot";
    const CollectionSlotImpl = createSlot2(COLLECTION_SLOT_NAME);
    const CollectionSlot = import_react52.default.forwardRef(
      (props, forwardedRef) => {
        const { scope, children } = props;
        const context2 = useCollectionContext(COLLECTION_SLOT_NAME, scope);
        const composedRefs = useComposedRefs(forwardedRef, context2.collectionRef);
        return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(CollectionSlotImpl, { ref: composedRefs, children });
      }
    );
    CollectionSlot.displayName = COLLECTION_SLOT_NAME;
    const ITEM_SLOT_NAME = name + "CollectionItemSlot";
    const ITEM_DATA_ATTR = "data-radix-collection-item";
    const CollectionItemSlotImpl = createSlot2(ITEM_SLOT_NAME);
    const CollectionItemSlot = import_react52.default.forwardRef(
      (props, forwardedRef) => {
        const { scope, children, ...itemData } = props;
        const ref = import_react52.default.useRef(null);
        const composedRefs = useComposedRefs(forwardedRef, ref);
        const context2 = useCollectionContext(ITEM_SLOT_NAME, scope);
        import_react52.default.useEffect(() => {
          context2.itemMap.set(ref, { ref, ...itemData });
          return () => void context2.itemMap.delete(ref);
        });
        return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(CollectionItemSlotImpl, { ...{ [ITEM_DATA_ATTR]: "" }, ref: composedRefs, children });
      }
    );
    CollectionItemSlot.displayName = ITEM_SLOT_NAME;
    function useCollection6(scope) {
      const context2 = useCollectionContext(name + "CollectionConsumer", scope);
      const getItems = import_react52.default.useCallback(() => {
        const collectionNode = context2.collectionRef.current;
        if (!collectionNode) return [];
        const orderedNodes = Array.from(collectionNode.querySelectorAll(`[${ITEM_DATA_ATTR}]`));
        const items = Array.from(context2.itemMap.values());
        const orderedItems = items.sort(
          (a, b) => orderedNodes.indexOf(a.ref.current) - orderedNodes.indexOf(b.ref.current)
        );
        return orderedItems;
      }, [context2.collectionRef, context2.itemMap]);
      return getItems;
    }
    return [
      { Provider: CollectionProvider, Slot: CollectionSlot, ItemSlot: CollectionItemSlot },
      useCollection6,
      createCollectionScope6
    ];
  }

  // node_modules/@radix-ui/react-dismissable-layer/dist/index.mjs
  init_define_import_meta_env();
  var React23 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-primitive/dist/index.mjs
  init_define_import_meta_env();
  var React19 = __toESM(require_react_shim(), 1);
  var ReactDOM3 = __toESM(require_react_dom_shim(), 1);

  // node_modules/@radix-ui/react-primitive/node_modules/@radix-ui/react-slot/dist/index.mjs
  init_define_import_meta_env();
  var React18 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime9 = __toESM(require_react_shim(), 1);
  // @__NO_SIDE_EFFECTS__
  function createSlot3(ownerName) {
    const SlotClone = /* @__PURE__ */ createSlotClone3(ownerName);
    const Slot22 = React18.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      const childrenArray = React18.Children.toArray(children);
      const slottable = childrenArray.find(isSlottable3);
      if (slottable) {
        const newElement = slottable.props.children;
        const newChildren = childrenArray.map((child) => {
          if (child === slottable) {
            if (React18.Children.count(newElement) > 1) return React18.Children.only(null);
            return React18.isValidElement(newElement) ? newElement.props.children : null;
          } else {
            return child;
          }
        });
        return /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children: React18.isValidElement(newElement) ? React18.cloneElement(newElement, void 0, newChildren) : null });
      }
      return /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children });
    });
    Slot22.displayName = `${ownerName}.Slot`;
    return Slot22;
  }
  // @__NO_SIDE_EFFECTS__
  function createSlotClone3(ownerName) {
    const SlotClone = React18.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      if (React18.isValidElement(children)) {
        const childrenRef = getElementRef3(children);
        const props2 = mergeProps3(slotProps, children.props);
        if (children.type !== React18.Fragment) {
          props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
        }
        return React18.cloneElement(children, props2);
      }
      return React18.Children.count(children) > 1 ? React18.Children.only(null) : null;
    });
    SlotClone.displayName = `${ownerName}.SlotClone`;
    return SlotClone;
  }
  var SLOTTABLE_IDENTIFIER3 = /* @__PURE__ */ Symbol("radix.slottable");
  function isSlottable3(child) {
    return React18.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER3;
  }
  function mergeProps3(slotProps, childProps) {
    const overrideProps = { ...childProps };
    for (const propName in childProps) {
      const slotPropValue = slotProps[propName];
      const childPropValue = childProps[propName];
      const isHandler = /^on[A-Z]/.test(propName);
      if (isHandler) {
        if (slotPropValue && childPropValue) {
          overrideProps[propName] = (...args) => {
            const result = childPropValue(...args);
            slotPropValue(...args);
            return result;
          };
        } else if (slotPropValue) {
          overrideProps[propName] = slotPropValue;
        }
      } else if (propName === "style") {
        overrideProps[propName] = { ...slotPropValue, ...childPropValue };
      } else if (propName === "className") {
        overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
      }
    }
    return { ...slotProps, ...overrideProps };
  }
  function getElementRef3(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }

  // node_modules/@radix-ui/react-primitive/dist/index.mjs
  var import_jsx_runtime10 = __toESM(require_react_shim(), 1);
  var NODES2 = [
    "a",
    "button",
    "div",
    "form",
    "h2",
    "h3",
    "img",
    "input",
    "label",
    "li",
    "nav",
    "ol",
    "p",
    "select",
    "span",
    "svg",
    "ul"
  ];
  var Primitive2 = NODES2.reduce((primitive, node) => {
    const Slot6 = createSlot3(`Primitive.${node}`);
    const Node2 = React19.forwardRef((props, forwardedRef) => {
      const { asChild, ...primitiveProps } = props;
      const Comp = asChild ? Slot6 : node;
      if (typeof window !== "undefined") {
        window[/* @__PURE__ */ Symbol.for("radix-ui")] = true;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(Comp, { ...primitiveProps, ref: forwardedRef });
    });
    Node2.displayName = `Primitive.${node}`;
    return { ...primitive, [node]: Node2 };
  }, {});
  function dispatchDiscreteCustomEvent(target, event) {
    if (target) ReactDOM3.flushSync(() => target.dispatchEvent(event));
  }

  // node_modules/@radix-ui/react-use-callback-ref/dist/index.mjs
  init_define_import_meta_env();
  var React20 = __toESM(require_react_shim(), 1);
  function useCallbackRef(callback) {
    const callbackRef = React20.useRef(callback);
    React20.useEffect(() => {
      callbackRef.current = callback;
    });
    return React20.useMemo(() => (...args) => callbackRef.current?.(...args), []);
  }

  // node_modules/@radix-ui/react-use-escape-keydown/dist/index.mjs
  init_define_import_meta_env();
  var React21 = __toESM(require_react_shim(), 1);
  function useEscapeKeydown(onEscapeKeyDownProp, ownerDocument = globalThis?.document) {
    const onEscapeKeyDown = useCallbackRef(onEscapeKeyDownProp);
    React21.useEffect(() => {
      const handleKeyDown = (event) => {
        if (event.key === "Escape") {
          onEscapeKeyDown(event);
        }
      };
      ownerDocument.addEventListener("keydown", handleKeyDown, { capture: true });
      return () => ownerDocument.removeEventListener("keydown", handleKeyDown, { capture: true });
    }, [onEscapeKeyDown, ownerDocument]);
  }

  // node_modules/@radix-ui/react-dismissable-layer/dist/index.mjs
  var import_jsx_runtime11 = __toESM(require_react_shim(), 1);
  var DISMISSABLE_LAYER_NAME = "DismissableLayer";
  var CONTEXT_UPDATE = "dismissableLayer.update";
  var POINTER_DOWN_OUTSIDE = "dismissableLayer.pointerDownOutside";
  var FOCUS_OUTSIDE = "dismissableLayer.focusOutside";
  var originalBodyPointerEvents;
  var DismissableLayerContext = React23.createContext({
    layers: /* @__PURE__ */ new Set(),
    layersWithOutsidePointerEventsDisabled: /* @__PURE__ */ new Set(),
    branches: /* @__PURE__ */ new Set()
  });
  var DismissableLayer = React23.forwardRef(
    (props, forwardedRef) => {
      const {
        disableOutsidePointerEvents = false,
        onEscapeKeyDown,
        onPointerDownOutside,
        onFocusOutside,
        onInteractOutside,
        onDismiss,
        ...layerProps
      } = props;
      const context2 = React23.useContext(DismissableLayerContext);
      const [node, setNode] = React23.useState(null);
      const ownerDocument = node?.ownerDocument ?? globalThis?.document;
      const [, force] = React23.useState({});
      const composedRefs = useComposedRefs(forwardedRef, (node2) => setNode(node2));
      const layers = Array.from(context2.layers);
      const [highestLayerWithOutsidePointerEventsDisabled] = [...context2.layersWithOutsidePointerEventsDisabled].slice(-1);
      const highestLayerWithOutsidePointerEventsDisabledIndex = layers.indexOf(highestLayerWithOutsidePointerEventsDisabled);
      const index2 = node ? layers.indexOf(node) : -1;
      const isBodyPointerEventsDisabled = context2.layersWithOutsidePointerEventsDisabled.size > 0;
      const isPointerEventsEnabled = index2 >= highestLayerWithOutsidePointerEventsDisabledIndex;
      const pointerDownOutside = usePointerDownOutside((event) => {
        const target = event.target;
        const isPointerDownOnBranch = [...context2.branches].some((branch) => branch.contains(target));
        if (!isPointerEventsEnabled || isPointerDownOnBranch) return;
        onPointerDownOutside?.(event);
        onInteractOutside?.(event);
        if (!event.defaultPrevented) onDismiss?.();
      }, ownerDocument);
      const focusOutside = useFocusOutside((event) => {
        const target = event.target;
        const isFocusInBranch = [...context2.branches].some((branch) => branch.contains(target));
        if (isFocusInBranch) return;
        onFocusOutside?.(event);
        onInteractOutside?.(event);
        if (!event.defaultPrevented) onDismiss?.();
      }, ownerDocument);
      useEscapeKeydown((event) => {
        const isHighestLayer = index2 === context2.layers.size - 1;
        if (!isHighestLayer) return;
        onEscapeKeyDown?.(event);
        if (!event.defaultPrevented && onDismiss) {
          event.preventDefault();
          onDismiss();
        }
      }, ownerDocument);
      React23.useEffect(() => {
        if (!node) return;
        if (disableOutsidePointerEvents) {
          if (context2.layersWithOutsidePointerEventsDisabled.size === 0) {
            originalBodyPointerEvents = ownerDocument.body.style.pointerEvents;
            ownerDocument.body.style.pointerEvents = "none";
          }
          context2.layersWithOutsidePointerEventsDisabled.add(node);
        }
        context2.layers.add(node);
        dispatchUpdate();
        return () => {
          if (disableOutsidePointerEvents && context2.layersWithOutsidePointerEventsDisabled.size === 1) {
            ownerDocument.body.style.pointerEvents = originalBodyPointerEvents;
          }
        };
      }, [node, ownerDocument, disableOutsidePointerEvents, context2]);
      React23.useEffect(() => {
        return () => {
          if (!node) return;
          context2.layers.delete(node);
          context2.layersWithOutsidePointerEventsDisabled.delete(node);
          dispatchUpdate();
        };
      }, [node, context2]);
      React23.useEffect(() => {
        const handleUpdate = () => force({});
        document.addEventListener(CONTEXT_UPDATE, handleUpdate);
        return () => document.removeEventListener(CONTEXT_UPDATE, handleUpdate);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
        Primitive2.div,
        {
          ...layerProps,
          ref: composedRefs,
          style: {
            pointerEvents: isBodyPointerEventsDisabled ? isPointerEventsEnabled ? "auto" : "none" : void 0,
            ...props.style
          },
          onFocusCapture: composeEventHandlers(props.onFocusCapture, focusOutside.onFocusCapture),
          onBlurCapture: composeEventHandlers(props.onBlurCapture, focusOutside.onBlurCapture),
          onPointerDownCapture: composeEventHandlers(
            props.onPointerDownCapture,
            pointerDownOutside.onPointerDownCapture
          )
        }
      );
    }
  );
  DismissableLayer.displayName = DISMISSABLE_LAYER_NAME;
  var BRANCH_NAME = "DismissableLayerBranch";
  var DismissableLayerBranch = React23.forwardRef((props, forwardedRef) => {
    const context2 = React23.useContext(DismissableLayerContext);
    const ref = React23.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    React23.useEffect(() => {
      const node = ref.current;
      if (node) {
        context2.branches.add(node);
        return () => {
          context2.branches.delete(node);
        };
      }
    }, [context2.branches]);
    return /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(Primitive2.div, { ...props, ref: composedRefs });
  });
  DismissableLayerBranch.displayName = BRANCH_NAME;
  function usePointerDownOutside(onPointerDownOutside, ownerDocument = globalThis?.document) {
    const handlePointerDownOutside = useCallbackRef(onPointerDownOutside);
    const isPointerInsideReactTreeRef = React23.useRef(false);
    const handleClickRef = React23.useRef(() => {
    });
    React23.useEffect(() => {
      const handlePointerDown = (event) => {
        if (event.target && !isPointerInsideReactTreeRef.current) {
          let handleAndDispatchPointerDownOutsideEvent2 = function() {
            handleAndDispatchCustomEvent(
              POINTER_DOWN_OUTSIDE,
              handlePointerDownOutside,
              eventDetail,
              { discrete: true }
            );
          };
          var handleAndDispatchPointerDownOutsideEvent = handleAndDispatchPointerDownOutsideEvent2;
          const eventDetail = { originalEvent: event };
          if (event.pointerType === "touch") {
            ownerDocument.removeEventListener("click", handleClickRef.current);
            handleClickRef.current = handleAndDispatchPointerDownOutsideEvent2;
            ownerDocument.addEventListener("click", handleClickRef.current, { once: true });
          } else {
            handleAndDispatchPointerDownOutsideEvent2();
          }
        } else {
          ownerDocument.removeEventListener("click", handleClickRef.current);
        }
        isPointerInsideReactTreeRef.current = false;
      };
      const timerId = window.setTimeout(() => {
        ownerDocument.addEventListener("pointerdown", handlePointerDown);
      }, 0);
      return () => {
        window.clearTimeout(timerId);
        ownerDocument.removeEventListener("pointerdown", handlePointerDown);
        ownerDocument.removeEventListener("click", handleClickRef.current);
      };
    }, [ownerDocument, handlePointerDownOutside]);
    return {
      // ensures we check React component tree (not just DOM tree)
      onPointerDownCapture: () => isPointerInsideReactTreeRef.current = true
    };
  }
  function useFocusOutside(onFocusOutside, ownerDocument = globalThis?.document) {
    const handleFocusOutside = useCallbackRef(onFocusOutside);
    const isFocusInsideReactTreeRef = React23.useRef(false);
    React23.useEffect(() => {
      const handleFocus = (event) => {
        if (event.target && !isFocusInsideReactTreeRef.current) {
          const eventDetail = { originalEvent: event };
          handleAndDispatchCustomEvent(FOCUS_OUTSIDE, handleFocusOutside, eventDetail, {
            discrete: false
          });
        }
      };
      ownerDocument.addEventListener("focusin", handleFocus);
      return () => ownerDocument.removeEventListener("focusin", handleFocus);
    }, [ownerDocument, handleFocusOutside]);
    return {
      onFocusCapture: () => isFocusInsideReactTreeRef.current = true,
      onBlurCapture: () => isFocusInsideReactTreeRef.current = false
    };
  }
  function dispatchUpdate() {
    const event = new CustomEvent(CONTEXT_UPDATE);
    document.dispatchEvent(event);
  }
  function handleAndDispatchCustomEvent(name, handler, detail, { discrete }) {
    const target = detail.originalEvent.target;
    const event = new CustomEvent(name, { bubbles: false, cancelable: true, detail });
    if (handler) target.addEventListener(name, handler, { once: true });
    if (discrete) {
      dispatchDiscreteCustomEvent(target, event);
    } else {
      target.dispatchEvent(event);
    }
  }
  var Root2 = DismissableLayer;
  var Branch = DismissableLayerBranch;

  // node_modules/@radix-ui/react-portal/dist/index.mjs
  init_define_import_meta_env();
  var React25 = __toESM(require_react_shim(), 1);
  var import_react_dom = __toESM(require_react_dom_shim(), 1);

  // node_modules/@radix-ui/react-use-layout-effect/dist/index.mjs
  init_define_import_meta_env();
  var React24 = __toESM(require_react_shim(), 1);
  var useLayoutEffect22 = globalThis?.document ? React24.useLayoutEffect : () => {
  };

  // node_modules/@radix-ui/react-portal/dist/index.mjs
  var import_jsx_runtime12 = __toESM(require_react_shim(), 1);
  var PORTAL_NAME = "Portal";
  var Portal = React25.forwardRef((props, forwardedRef) => {
    const { container: containerProp, ...portalProps } = props;
    const [mounted, setMounted] = React25.useState(false);
    useLayoutEffect22(() => setMounted(true), []);
    const container = containerProp || mounted && globalThis?.document?.body;
    return container ? import_react_dom.default.createPortal(/* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Primitive2.div, { ...portalProps, ref: forwardedRef }), container) : null;
  });
  Portal.displayName = PORTAL_NAME;

  // node_modules/@radix-ui/react-presence/dist/index.mjs
  init_define_import_meta_env();
  var React26 = __toESM(require_react_shim(), 1);
  var React27 = __toESM(require_react_shim(), 1);
  function useStateMachine(initialState2, machine) {
    return React27.useReducer((state, event) => {
      const nextState = machine[state][event];
      return nextState ?? state;
    }, initialState2);
  }
  var Presence = (props) => {
    const { present, children } = props;
    const presence = usePresence(present);
    const child = typeof children === "function" ? children({ present: presence.isPresent }) : React26.Children.only(children);
    const ref = useComposedRefs(presence.ref, getElementRef4(child));
    const forceMount = typeof children === "function";
    return forceMount || presence.isPresent ? React26.cloneElement(child, { ref }) : null;
  };
  Presence.displayName = "Presence";
  function usePresence(present) {
    const [node, setNode] = React26.useState();
    const stylesRef = React26.useRef(null);
    const prevPresentRef = React26.useRef(present);
    const prevAnimationNameRef = React26.useRef("none");
    const initialState2 = present ? "mounted" : "unmounted";
    const [state, send] = useStateMachine(initialState2, {
      mounted: {
        UNMOUNT: "unmounted",
        ANIMATION_OUT: "unmountSuspended"
      },
      unmountSuspended: {
        MOUNT: "mounted",
        ANIMATION_END: "unmounted"
      },
      unmounted: {
        MOUNT: "mounted"
      }
    });
    React26.useEffect(() => {
      const currentAnimationName = getAnimationName(stylesRef.current);
      prevAnimationNameRef.current = state === "mounted" ? currentAnimationName : "none";
    }, [state]);
    useLayoutEffect22(() => {
      const styles = stylesRef.current;
      const wasPresent = prevPresentRef.current;
      const hasPresentChanged = wasPresent !== present;
      if (hasPresentChanged) {
        const prevAnimationName = prevAnimationNameRef.current;
        const currentAnimationName = getAnimationName(styles);
        if (present) {
          send("MOUNT");
        } else if (currentAnimationName === "none" || styles?.display === "none") {
          send("UNMOUNT");
        } else {
          const isAnimating = prevAnimationName !== currentAnimationName;
          if (wasPresent && isAnimating) {
            send("ANIMATION_OUT");
          } else {
            send("UNMOUNT");
          }
        }
        prevPresentRef.current = present;
      }
    }, [present, send]);
    useLayoutEffect22(() => {
      if (node) {
        let timeoutId;
        const ownerWindow = node.ownerDocument.defaultView ?? window;
        const handleAnimationEnd = (event) => {
          const currentAnimationName = getAnimationName(stylesRef.current);
          const isCurrentAnimation = currentAnimationName.includes(CSS.escape(event.animationName));
          if (event.target === node && isCurrentAnimation) {
            send("ANIMATION_END");
            if (!prevPresentRef.current) {
              const currentFillMode = node.style.animationFillMode;
              node.style.animationFillMode = "forwards";
              timeoutId = ownerWindow.setTimeout(() => {
                if (node.style.animationFillMode === "forwards") {
                  node.style.animationFillMode = currentFillMode;
                }
              });
            }
          }
        };
        const handleAnimationStart = (event) => {
          if (event.target === node) {
            prevAnimationNameRef.current = getAnimationName(stylesRef.current);
          }
        };
        node.addEventListener("animationstart", handleAnimationStart);
        node.addEventListener("animationcancel", handleAnimationEnd);
        node.addEventListener("animationend", handleAnimationEnd);
        return () => {
          ownerWindow.clearTimeout(timeoutId);
          node.removeEventListener("animationstart", handleAnimationStart);
          node.removeEventListener("animationcancel", handleAnimationEnd);
          node.removeEventListener("animationend", handleAnimationEnd);
        };
      } else {
        send("ANIMATION_END");
      }
    }, [node, send]);
    return {
      isPresent: ["mounted", "unmountSuspended"].includes(state),
      ref: React26.useCallback((node2) => {
        stylesRef.current = node2 ? getComputedStyle(node2) : null;
        setNode(node2);
      }, [])
    };
  }
  function getAnimationName(styles) {
    return styles?.animationName || "none";
  }
  function getElementRef4(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }

  // node_modules/@radix-ui/react-use-controllable-state/dist/index.mjs
  init_define_import_meta_env();
  var React28 = __toESM(require_react_shim(), 1);
  var React29 = __toESM(require_react_shim(), 1);
  var useInsertionEffect = React28[" useInsertionEffect ".trim().toString()] || useLayoutEffect22;
  function useControllableState({
    prop,
    defaultProp,
    onChange = () => {
    },
    caller
  }) {
    const [uncontrolledProp, setUncontrolledProp, onChangeRef] = useUncontrolledState({
      defaultProp,
      onChange
    });
    const isControlled = prop !== void 0;
    const value = isControlled ? prop : uncontrolledProp;
    if (true) {
      const isControlledRef = React28.useRef(prop !== void 0);
      React28.useEffect(() => {
        const wasControlled = isControlledRef.current;
        if (wasControlled !== isControlled) {
          const from = wasControlled ? "controlled" : "uncontrolled";
          const to = isControlled ? "controlled" : "uncontrolled";
          console.warn(
            `${caller} is changing from ${from} to ${to}. Components should not switch from controlled to uncontrolled (or vice versa). Decide between using a controlled or uncontrolled value for the lifetime of the component.`
          );
        }
        isControlledRef.current = isControlled;
      }, [isControlled, caller]);
    }
    const setValue = React28.useCallback(
      (nextValue) => {
        if (isControlled) {
          const value2 = isFunction2(nextValue) ? nextValue(prop) : nextValue;
          if (value2 !== prop) {
            onChangeRef.current?.(value2);
          }
        } else {
          setUncontrolledProp(nextValue);
        }
      },
      [isControlled, prop, setUncontrolledProp, onChangeRef]
    );
    return [value, setValue];
  }
  function useUncontrolledState({
    defaultProp,
    onChange
  }) {
    const [value, setValue] = React28.useState(defaultProp);
    const prevValueRef = React28.useRef(value);
    const onChangeRef = React28.useRef(onChange);
    useInsertionEffect(() => {
      onChangeRef.current = onChange;
    }, [onChange]);
    React28.useEffect(() => {
      if (prevValueRef.current !== value) {
        onChangeRef.current?.(value);
        prevValueRef.current = value;
      }
    }, [value, prevValueRef]);
    return [value, setValue, onChangeRef];
  }
  function isFunction2(value) {
    return typeof value === "function";
  }

  // node_modules/@radix-ui/react-visually-hidden/dist/index.mjs
  init_define_import_meta_env();
  var React30 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime13 = __toESM(require_react_shim(), 1);
  var VISUALLY_HIDDEN_STYLES = Object.freeze({
    // See: https://github.com/twbs/bootstrap/blob/main/scss/mixins/_visually-hidden.scss
    position: "absolute",
    border: 0,
    width: 1,
    height: 1,
    padding: 0,
    margin: -1,
    overflow: "hidden",
    clip: "rect(0, 0, 0, 0)",
    whiteSpace: "nowrap",
    wordWrap: "normal"
  });
  var NAME2 = "VisuallyHidden";
  var VisuallyHidden = React30.forwardRef(
    (props, forwardedRef) => {
      return /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
        Primitive2.span,
        {
          ...props,
          ref: forwardedRef,
          style: { ...VISUALLY_HIDDEN_STYLES, ...props.style }
        }
      );
    }
  );
  VisuallyHidden.displayName = NAME2;
  var Root3 = VisuallyHidden;

  // node_modules/@radix-ui/react-toast/dist/index.mjs
  var import_jsx_runtime14 = __toESM(require_react_shim(), 1);
  var PROVIDER_NAME = "ToastProvider";
  var [Collection, useCollection, createCollectionScope] = createCollection("Toast");
  var [createToastContext, createToastScope] = createContextScope("Toast", [createCollectionScope]);
  var [ToastProviderProvider, useToastProviderContext] = createToastContext(PROVIDER_NAME);
  var ToastProvider = (props) => {
    const {
      __scopeToast,
      label = "Notification",
      duration = 5e3,
      swipeDirection = "right",
      swipeThreshold = 50,
      children
    } = props;
    const [viewport, setViewport] = React31.useState(null);
    const [toastCount, setToastCount] = React31.useState(0);
    const isFocusedToastEscapeKeyDownRef = React31.useRef(false);
    const isClosePausedRef = React31.useRef(false);
    if (!label.trim()) {
      console.error(
        `Invalid prop \`label\` supplied to \`${PROVIDER_NAME}\`. Expected non-empty \`string\`.`
      );
    }
    return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(Collection.Provider, { scope: __scopeToast, children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
      ToastProviderProvider,
      {
        scope: __scopeToast,
        label,
        duration,
        swipeDirection,
        swipeThreshold,
        toastCount,
        viewport,
        onViewportChange: setViewport,
        onToastAdd: React31.useCallback(() => setToastCount((prevCount) => prevCount + 1), []),
        onToastRemove: React31.useCallback(() => setToastCount((prevCount) => prevCount - 1), []),
        isFocusedToastEscapeKeyDownRef,
        isClosePausedRef,
        children
      }
    ) });
  };
  ToastProvider.displayName = PROVIDER_NAME;
  var VIEWPORT_NAME = "ToastViewport";
  var VIEWPORT_DEFAULT_HOTKEY = ["F8"];
  var VIEWPORT_PAUSE = "toast.viewportPause";
  var VIEWPORT_RESUME = "toast.viewportResume";
  var ToastViewport = React31.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeToast,
        hotkey = VIEWPORT_DEFAULT_HOTKEY,
        label = "Notifications ({hotkey})",
        ...viewportProps
      } = props;
      const context2 = useToastProviderContext(VIEWPORT_NAME, __scopeToast);
      const getItems = useCollection(__scopeToast);
      const wrapperRef = React31.useRef(null);
      const headFocusProxyRef = React31.useRef(null);
      const tailFocusProxyRef = React31.useRef(null);
      const ref = React31.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref, context2.onViewportChange);
      const hotkeyLabel = hotkey.join("+").replace(/Key/g, "").replace(/Digit/g, "");
      const hasToasts = context2.toastCount > 0;
      React31.useEffect(() => {
        const handleKeyDown = (event) => {
          const isHotkeyPressed = hotkey.length !== 0 && hotkey.every((key) => event[key] || event.code === key);
          if (isHotkeyPressed) ref.current?.focus();
        };
        document.addEventListener("keydown", handleKeyDown);
        return () => document.removeEventListener("keydown", handleKeyDown);
      }, [hotkey]);
      React31.useEffect(() => {
        const wrapper = wrapperRef.current;
        const viewport = ref.current;
        if (hasToasts && wrapper && viewport) {
          const handlePause = () => {
            if (!context2.isClosePausedRef.current) {
              const pauseEvent = new CustomEvent(VIEWPORT_PAUSE);
              viewport.dispatchEvent(pauseEvent);
              context2.isClosePausedRef.current = true;
            }
          };
          const handleResume = () => {
            if (context2.isClosePausedRef.current) {
              const resumeEvent = new CustomEvent(VIEWPORT_RESUME);
              viewport.dispatchEvent(resumeEvent);
              context2.isClosePausedRef.current = false;
            }
          };
          const handleFocusOutResume = (event) => {
            const isFocusMovingOutside = !wrapper.contains(event.relatedTarget);
            if (isFocusMovingOutside) handleResume();
          };
          const handlePointerLeaveResume = () => {
            const isFocusInside = wrapper.contains(document.activeElement);
            if (!isFocusInside) handleResume();
          };
          wrapper.addEventListener("focusin", handlePause);
          wrapper.addEventListener("focusout", handleFocusOutResume);
          wrapper.addEventListener("pointermove", handlePause);
          wrapper.addEventListener("pointerleave", handlePointerLeaveResume);
          window.addEventListener("blur", handlePause);
          window.addEventListener("focus", handleResume);
          return () => {
            wrapper.removeEventListener("focusin", handlePause);
            wrapper.removeEventListener("focusout", handleFocusOutResume);
            wrapper.removeEventListener("pointermove", handlePause);
            wrapper.removeEventListener("pointerleave", handlePointerLeaveResume);
            window.removeEventListener("blur", handlePause);
            window.removeEventListener("focus", handleResume);
          };
        }
      }, [hasToasts, context2.isClosePausedRef]);
      const getSortedTabbableCandidates = React31.useCallback(
        ({ tabbingDirection }) => {
          const toastItems = getItems();
          const tabbableCandidates = toastItems.map((toastItem) => {
            const toastNode = toastItem.ref.current;
            const toastTabbableCandidates = [toastNode, ...getTabbableCandidates(toastNode)];
            return tabbingDirection === "forwards" ? toastTabbableCandidates : toastTabbableCandidates.reverse();
          });
          return (tabbingDirection === "forwards" ? tabbableCandidates.reverse() : tabbableCandidates).flat();
        },
        [getItems]
      );
      React31.useEffect(() => {
        const viewport = ref.current;
        if (viewport) {
          const handleKeyDown = (event) => {
            const isMetaKey = event.altKey || event.ctrlKey || event.metaKey;
            const isTabKey = event.key === "Tab" && !isMetaKey;
            if (isTabKey) {
              const focusedElement = document.activeElement;
              const isTabbingBackwards = event.shiftKey;
              const targetIsViewport = event.target === viewport;
              if (targetIsViewport && isTabbingBackwards) {
                headFocusProxyRef.current?.focus();
                return;
              }
              const tabbingDirection = isTabbingBackwards ? "backwards" : "forwards";
              const sortedCandidates = getSortedTabbableCandidates({ tabbingDirection });
              const index2 = sortedCandidates.findIndex((candidate) => candidate === focusedElement);
              if (focusFirst(sortedCandidates.slice(index2 + 1))) {
                event.preventDefault();
              } else {
                isTabbingBackwards ? headFocusProxyRef.current?.focus() : tailFocusProxyRef.current?.focus();
              }
            }
          };
          viewport.addEventListener("keydown", handleKeyDown);
          return () => viewport.removeEventListener("keydown", handleKeyDown);
        }
      }, [getItems, getSortedTabbableCandidates]);
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)(
        Branch,
        {
          ref: wrapperRef,
          role: "region",
          "aria-label": label.replace("{hotkey}", hotkeyLabel),
          tabIndex: -1,
          style: { pointerEvents: hasToasts ? void 0 : "none" },
          children: [
            hasToasts && /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
              FocusProxy,
              {
                ref: headFocusProxyRef,
                onFocusFromOutsideViewport: () => {
                  const tabbableCandidates = getSortedTabbableCandidates({
                    tabbingDirection: "forwards"
                  });
                  focusFirst(tabbableCandidates);
                }
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(Collection.Slot, { scope: __scopeToast, children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(Primitive2.ol, { tabIndex: -1, ...viewportProps, ref: composedRefs }) }),
            hasToasts && /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
              FocusProxy,
              {
                ref: tailFocusProxyRef,
                onFocusFromOutsideViewport: () => {
                  const tabbableCandidates = getSortedTabbableCandidates({
                    tabbingDirection: "backwards"
                  });
                  focusFirst(tabbableCandidates);
                }
              }
            )
          ]
        }
      );
    }
  );
  ToastViewport.displayName = VIEWPORT_NAME;
  var FOCUS_PROXY_NAME = "ToastFocusProxy";
  var FocusProxy = React31.forwardRef(
    (props, forwardedRef) => {
      const { __scopeToast, onFocusFromOutsideViewport, ...proxyProps } = props;
      const context2 = useToastProviderContext(FOCUS_PROXY_NAME, __scopeToast);
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
        VisuallyHidden,
        {
          tabIndex: 0,
          ...proxyProps,
          ref: forwardedRef,
          style: { position: "fixed" },
          onFocus: (event) => {
            const prevFocusedElement = event.relatedTarget;
            const isFocusFromOutsideViewport = !context2.viewport?.contains(prevFocusedElement);
            if (isFocusFromOutsideViewport) onFocusFromOutsideViewport();
          }
        }
      );
    }
  );
  FocusProxy.displayName = FOCUS_PROXY_NAME;
  var TOAST_NAME = "Toast";
  var TOAST_SWIPE_START = "toast.swipeStart";
  var TOAST_SWIPE_MOVE = "toast.swipeMove";
  var TOAST_SWIPE_CANCEL = "toast.swipeCancel";
  var TOAST_SWIPE_END = "toast.swipeEnd";
  var Toast = React31.forwardRef(
    (props, forwardedRef) => {
      const { forceMount, open: openProp, defaultOpen, onOpenChange, ...toastProps } = props;
      const [open, setOpen] = useControllableState({
        prop: openProp,
        defaultProp: defaultOpen ?? true,
        onChange: onOpenChange,
        caller: TOAST_NAME
      });
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(Presence, { present: forceMount || open, children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
        ToastImpl,
        {
          open,
          ...toastProps,
          ref: forwardedRef,
          onClose: () => setOpen(false),
          onPause: useCallbackRef(props.onPause),
          onResume: useCallbackRef(props.onResume),
          onSwipeStart: composeEventHandlers(props.onSwipeStart, (event) => {
            event.currentTarget.setAttribute("data-swipe", "start");
          }),
          onSwipeMove: composeEventHandlers(props.onSwipeMove, (event) => {
            const { x, y } = event.detail.delta;
            event.currentTarget.setAttribute("data-swipe", "move");
            event.currentTarget.style.setProperty("--radix-toast-swipe-move-x", `${x}px`);
            event.currentTarget.style.setProperty("--radix-toast-swipe-move-y", `${y}px`);
          }),
          onSwipeCancel: composeEventHandlers(props.onSwipeCancel, (event) => {
            event.currentTarget.setAttribute("data-swipe", "cancel");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-move-x");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-move-y");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-end-x");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-end-y");
          }),
          onSwipeEnd: composeEventHandlers(props.onSwipeEnd, (event) => {
            const { x, y } = event.detail.delta;
            event.currentTarget.setAttribute("data-swipe", "end");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-move-x");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-move-y");
            event.currentTarget.style.setProperty("--radix-toast-swipe-end-x", `${x}px`);
            event.currentTarget.style.setProperty("--radix-toast-swipe-end-y", `${y}px`);
            setOpen(false);
          })
        }
      ) });
    }
  );
  Toast.displayName = TOAST_NAME;
  var [ToastInteractiveProvider, useToastInteractiveContext] = createToastContext(TOAST_NAME, {
    onClose() {
    }
  });
  var ToastImpl = React31.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeToast,
        type = "foreground",
        duration: durationProp,
        open,
        onClose,
        onEscapeKeyDown,
        onPause,
        onResume,
        onSwipeStart,
        onSwipeMove,
        onSwipeCancel,
        onSwipeEnd,
        ...toastProps
      } = props;
      const context2 = useToastProviderContext(TOAST_NAME, __scopeToast);
      const [node, setNode] = React31.useState(null);
      const composedRefs = useComposedRefs(forwardedRef, (node2) => setNode(node2));
      const pointerStartRef = React31.useRef(null);
      const swipeDeltaRef = React31.useRef(null);
      const duration = durationProp || context2.duration;
      const closeTimerStartTimeRef = React31.useRef(0);
      const closeTimerRemainingTimeRef = React31.useRef(duration);
      const closeTimerRef = React31.useRef(0);
      const { onToastAdd, onToastRemove } = context2;
      const handleClose = useCallbackRef(() => {
        const isFocusInToast = node?.contains(document.activeElement);
        if (isFocusInToast) context2.viewport?.focus();
        onClose();
      });
      const startTimer = React31.useCallback(
        (duration2) => {
          if (!duration2 || duration2 === Infinity) return;
          window.clearTimeout(closeTimerRef.current);
          closeTimerStartTimeRef.current = (/* @__PURE__ */ new Date()).getTime();
          closeTimerRef.current = window.setTimeout(handleClose, duration2);
        },
        [handleClose]
      );
      React31.useEffect(() => {
        const viewport = context2.viewport;
        if (viewport) {
          const handleResume = () => {
            startTimer(closeTimerRemainingTimeRef.current);
            onResume?.();
          };
          const handlePause = () => {
            const elapsedTime = (/* @__PURE__ */ new Date()).getTime() - closeTimerStartTimeRef.current;
            closeTimerRemainingTimeRef.current = closeTimerRemainingTimeRef.current - elapsedTime;
            window.clearTimeout(closeTimerRef.current);
            onPause?.();
          };
          viewport.addEventListener(VIEWPORT_PAUSE, handlePause);
          viewport.addEventListener(VIEWPORT_RESUME, handleResume);
          return () => {
            viewport.removeEventListener(VIEWPORT_PAUSE, handlePause);
            viewport.removeEventListener(VIEWPORT_RESUME, handleResume);
          };
        }
      }, [context2.viewport, duration, onPause, onResume, startTimer]);
      React31.useEffect(() => {
        if (open && !context2.isClosePausedRef.current) startTimer(duration);
      }, [open, duration, context2.isClosePausedRef, startTimer]);
      React31.useEffect(() => {
        onToastAdd();
        return () => onToastRemove();
      }, [onToastAdd, onToastRemove]);
      const announceTextContent = React31.useMemo(() => {
        return node ? getAnnounceTextContent(node) : null;
      }, [node]);
      if (!context2.viewport) return null;
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)(import_jsx_runtime14.Fragment, { children: [
        announceTextContent && /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
          ToastAnnounce,
          {
            __scopeToast,
            role: "status",
            "aria-live": type === "foreground" ? "assertive" : "polite",
            children: announceTextContent
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(ToastInteractiveProvider, { scope: __scopeToast, onClose: handleClose, children: ReactDOM5.createPortal(
          /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(Collection.ItemSlot, { scope: __scopeToast, children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
            Root2,
            {
              asChild: true,
              onEscapeKeyDown: composeEventHandlers(onEscapeKeyDown, () => {
                if (!context2.isFocusedToastEscapeKeyDownRef.current) handleClose();
                context2.isFocusedToastEscapeKeyDownRef.current = false;
              }),
              children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
                Primitive2.li,
                {
                  tabIndex: 0,
                  "data-state": open ? "open" : "closed",
                  "data-swipe-direction": context2.swipeDirection,
                  ...toastProps,
                  ref: composedRefs,
                  style: { userSelect: "none", touchAction: "none", ...props.style },
                  onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
                    if (event.key !== "Escape") return;
                    onEscapeKeyDown?.(event.nativeEvent);
                    if (!event.nativeEvent.defaultPrevented) {
                      context2.isFocusedToastEscapeKeyDownRef.current = true;
                      handleClose();
                    }
                  }),
                  onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
                    if (event.button !== 0) return;
                    pointerStartRef.current = { x: event.clientX, y: event.clientY };
                  }),
                  onPointerMove: composeEventHandlers(props.onPointerMove, (event) => {
                    if (!pointerStartRef.current) return;
                    const x = event.clientX - pointerStartRef.current.x;
                    const y = event.clientY - pointerStartRef.current.y;
                    const hasSwipeMoveStarted = Boolean(swipeDeltaRef.current);
                    const isHorizontalSwipe = ["left", "right"].includes(context2.swipeDirection);
                    const clamp4 = ["left", "up"].includes(context2.swipeDirection) ? Math.min : Math.max;
                    const clampedX = isHorizontalSwipe ? clamp4(0, x) : 0;
                    const clampedY = !isHorizontalSwipe ? clamp4(0, y) : 0;
                    const moveStartBuffer = event.pointerType === "touch" ? 10 : 2;
                    const delta = { x: clampedX, y: clampedY };
                    const eventDetail = { originalEvent: event, delta };
                    if (hasSwipeMoveStarted) {
                      swipeDeltaRef.current = delta;
                      handleAndDispatchCustomEvent2(TOAST_SWIPE_MOVE, onSwipeMove, eventDetail, {
                        discrete: false
                      });
                    } else if (isDeltaInDirection(delta, context2.swipeDirection, moveStartBuffer)) {
                      swipeDeltaRef.current = delta;
                      handleAndDispatchCustomEvent2(TOAST_SWIPE_START, onSwipeStart, eventDetail, {
                        discrete: false
                      });
                      event.target.setPointerCapture(event.pointerId);
                    } else if (Math.abs(x) > moveStartBuffer || Math.abs(y) > moveStartBuffer) {
                      pointerStartRef.current = null;
                    }
                  }),
                  onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
                    const delta = swipeDeltaRef.current;
                    const target = event.target;
                    if (target.hasPointerCapture(event.pointerId)) {
                      target.releasePointerCapture(event.pointerId);
                    }
                    swipeDeltaRef.current = null;
                    pointerStartRef.current = null;
                    if (delta) {
                      const toast2 = event.currentTarget;
                      const eventDetail = { originalEvent: event, delta };
                      if (isDeltaInDirection(delta, context2.swipeDirection, context2.swipeThreshold)) {
                        handleAndDispatchCustomEvent2(TOAST_SWIPE_END, onSwipeEnd, eventDetail, {
                          discrete: true
                        });
                      } else {
                        handleAndDispatchCustomEvent2(
                          TOAST_SWIPE_CANCEL,
                          onSwipeCancel,
                          eventDetail,
                          {
                            discrete: true
                          }
                        );
                      }
                      toast2.addEventListener("click", (event2) => event2.preventDefault(), {
                        once: true
                      });
                    }
                  })
                }
              )
            }
          ) }),
          context2.viewport
        ) })
      ] });
    }
  );
  var ToastAnnounce = (props) => {
    const { __scopeToast, children, ...announceProps } = props;
    const context2 = useToastProviderContext(TOAST_NAME, __scopeToast);
    const [renderAnnounceText, setRenderAnnounceText] = React31.useState(false);
    const [isAnnounced, setIsAnnounced] = React31.useState(false);
    useNextFrame(() => setRenderAnnounceText(true));
    React31.useEffect(() => {
      const timer = window.setTimeout(() => setIsAnnounced(true), 1e3);
      return () => window.clearTimeout(timer);
    }, []);
    return isAnnounced ? null : /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(Portal, { asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(VisuallyHidden, { ...announceProps, children: renderAnnounceText && /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)(import_jsx_runtime14.Fragment, { children: [
      context2.label,
      " ",
      children
    ] }) }) });
  };
  var TITLE_NAME = "ToastTitle";
  var ToastTitle = React31.forwardRef(
    (props, forwardedRef) => {
      const { __scopeToast, ...titleProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(Primitive2.div, { ...titleProps, ref: forwardedRef });
    }
  );
  ToastTitle.displayName = TITLE_NAME;
  var DESCRIPTION_NAME = "ToastDescription";
  var ToastDescription = React31.forwardRef(
    (props, forwardedRef) => {
      const { __scopeToast, ...descriptionProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(Primitive2.div, { ...descriptionProps, ref: forwardedRef });
    }
  );
  ToastDescription.displayName = DESCRIPTION_NAME;
  var ACTION_NAME = "ToastAction";
  var ToastAction = React31.forwardRef(
    (props, forwardedRef) => {
      const { altText, ...actionProps } = props;
      if (!altText.trim()) {
        console.error(
          `Invalid prop \`altText\` supplied to \`${ACTION_NAME}\`. Expected non-empty \`string\`.`
        );
        return null;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(ToastAnnounceExclude, { altText, asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(ToastClose, { ...actionProps, ref: forwardedRef }) });
    }
  );
  ToastAction.displayName = ACTION_NAME;
  var CLOSE_NAME = "ToastClose";
  var ToastClose = React31.forwardRef(
    (props, forwardedRef) => {
      const { __scopeToast, ...closeProps } = props;
      const interactiveContext = useToastInteractiveContext(CLOSE_NAME, __scopeToast);
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(ToastAnnounceExclude, { asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
        Primitive2.button,
        {
          type: "button",
          ...closeProps,
          ref: forwardedRef,
          onClick: composeEventHandlers(props.onClick, interactiveContext.onClose)
        }
      ) });
    }
  );
  ToastClose.displayName = CLOSE_NAME;
  var ToastAnnounceExclude = React31.forwardRef((props, forwardedRef) => {
    const { __scopeToast, altText, ...announceExcludeProps } = props;
    return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
      Primitive2.div,
      {
        "data-radix-toast-announce-exclude": "",
        "data-radix-toast-announce-alt": altText || void 0,
        ...announceExcludeProps,
        ref: forwardedRef
      }
    );
  });
  function getAnnounceTextContent(container) {
    const textContent = [];
    const childNodes = Array.from(container.childNodes);
    childNodes.forEach((node) => {
      if (node.nodeType === node.TEXT_NODE && node.textContent) textContent.push(node.textContent);
      if (isHTMLElement2(node)) {
        const isHidden2 = node.ariaHidden || node.hidden || node.style.display === "none";
        const isExcluded = node.dataset.radixToastAnnounceExclude === "";
        if (!isHidden2) {
          if (isExcluded) {
            const altText = node.dataset.radixToastAnnounceAlt;
            if (altText) textContent.push(altText);
          } else {
            textContent.push(...getAnnounceTextContent(node));
          }
        }
      }
    });
    return textContent;
  }
  function handleAndDispatchCustomEvent2(name, handler, detail, { discrete }) {
    const currentTarget = detail.originalEvent.currentTarget;
    const event = new CustomEvent(name, { bubbles: true, cancelable: true, detail });
    if (handler) currentTarget.addEventListener(name, handler, { once: true });
    if (discrete) {
      dispatchDiscreteCustomEvent(currentTarget, event);
    } else {
      currentTarget.dispatchEvent(event);
    }
  }
  var isDeltaInDirection = (delta, direction, threshold = 0) => {
    const deltaX = Math.abs(delta.x);
    const deltaY = Math.abs(delta.y);
    const isDeltaX = deltaX > deltaY;
    if (direction === "left" || direction === "right") {
      return isDeltaX && deltaX > threshold;
    } else {
      return !isDeltaX && deltaY > threshold;
    }
  };
  function useNextFrame(callback = () => {
  }) {
    const fn = useCallbackRef(callback);
    useLayoutEffect22(() => {
      let raf1 = 0;
      let raf2 = 0;
      raf1 = window.requestAnimationFrame(() => raf2 = window.requestAnimationFrame(fn));
      return () => {
        window.cancelAnimationFrame(raf1);
        window.cancelAnimationFrame(raf2);
      };
    }, [fn]);
  }
  function isHTMLElement2(node) {
    return node.nodeType === node.ELEMENT_NODE;
  }
  function getTabbableCandidates(container) {
    const nodes = [];
    const walker = document.createTreeWalker(container, NodeFilter.SHOW_ELEMENT, {
      acceptNode: (node) => {
        const isHiddenInput = node.tagName === "INPUT" && node.type === "hidden";
        if (node.disabled || node.hidden || isHiddenInput) return NodeFilter.FILTER_SKIP;
        return node.tabIndex >= 0 ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
      }
    });
    while (walker.nextNode()) nodes.push(walker.currentNode);
    return nodes;
  }
  function focusFirst(candidates) {
    const previouslyFocusedElement = document.activeElement;
    return candidates.some((candidate) => {
      if (candidate === previouslyFocusedElement) return true;
      candidate.focus();
      return document.activeElement !== previouslyFocusedElement;
    });
  }
  var Provider = ToastProvider;
  var Viewport = ToastViewport;
  var Root22 = Toast;
  var Title6 = ToastTitle;
  var Description5 = ToastDescription;
  var Action3 = ToastAction;
  var Close = ToastClose;

  // src/components/ui/toast/Toast.tsx
  var viewportVariants = cva("fixed z-100 w-full md:max-w-max", {
    variants: {
      position: {
        "top-start": "top-20 inset-s-3",
        "top-center": "top-20 inset-s-1/2 -translate-x-1/2",
        "top-end": "top-20 inset-e-3",
        "bottom-start": "bottom-2 inset-s-3",
        "bottom-center": "bottom-2 inset-s-1/2 -translate-x-1/2",
        "bottom-end": "bottom-2 inset-e-3"
      }
    },
    defaultVariants: {
      position: "top-end"
    }
  });
  var toastVariants = cva(
    `
    group pointer-events-auto relative flex w-full items-center justify-between overflow-hidden
    rounded-md p-4 pe-10 shadow-boundary-ghost transition-all
    data-[swipe=cancel]:translate-x-0 data-[swipe=end]:translate-x-[var(--radix-toast-swipe-end-x)] data-[swipe=move]:translate-x-[var(--radix-toast-swipe-move-x)]
    data-[swipe=move]:transition-none data-[state=open]:animate-in data-[state=closed]:animate-out data-[swipe=end]:animate-out
    data-[state=closed]:fade-out-80
  `,
    {
      variants: {
        variant: {
          default: "bg-primary-400 text-primary-foreground",
          success: "bg-success-200 text-foreground",
          destructive: "bg-destructive-200 text-foreground",
          warning: "bg-warning-200 text-foreground",
          info: "bg-background text-foreground"
        },
        position: {
          "top-start": "data-[state=open]:slide-in-from-top-full data-[state=closed]:slide-out-to-start-full",
          "top-center": "data-[state=open]:slide-in-from-top-full data-[state=closed]:slide-out-to-top-full",
          "top-end": "data-[state=open]:slide-in-from-top-full data-[state=closed]:slide-out-to-end-full",
          "bottom-start": "data-[state=open]:slide-in-from-bottom-full data-[state=closed]:slide-out-to-start-full",
          "bottom-center": "data-[state=open]:slide-in-from-bottom-full data-[state=closed]:slide-out-to-bottom-full",
          "bottom-end": "data-[state=open]:slide-in-from-bottom-full data-[state=closed]:slide-out-to-end-full"
        }
      },
      defaultVariants: {
        variant: "default",
        position: "top-end"
      }
    }
  );
  var ToastContext = (0, import_react54.createContext)({ variant: "default", position: "top-end" });
  var ToastViewport2 = ({ className, position, ...props }) => /* @__PURE__ */ React.createElement(Viewport, { "data-slot": "toast-viewport", className: cn(viewportVariants({ position }), className), ...props });
  var ToastTitle2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Title6, { "data-slot": "toast-title", className: cn("text-sm font-semibold", className), ...props });
  var ToastDescription2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    Description5,
    {
      "data-slot": "toast-description",
      className: cn("text-sm whitespace-pre-line opacity-90 select-text md:max-w-[30dvw]", className),
      ...props
    }
  );
  var ToastClose2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    Close,
    {
      "aria-label": "Close",
      "data-slot": "toast-close",
      className: cn(
        "absolute inset-e-2 top-2 cursor-pointer rounded-md p-1 text-inherit opacity-0",
        "transition-opacity group-hover:opacity-100 focus:opacity-100 focus:outline-none",
        className
      ),
      "toast-close": "",
      ...props
    },
    /* @__PURE__ */ React.createElement(X, { size: 16 })
  );
  var ToastIcon = ({ variant }) => {
    const context2 = (0, import_react54.useContext)(ToastContext);
    const resolvedVariant = variant ?? context2.variant;
    const iconClass = "shrink-0 size-5";
    switch (resolvedVariant) {
      case "success":
        return /* @__PURE__ */ React.createElement(CircleCheck, { className: cn(iconClass, "text-success") });
      case "destructive":
        return /* @__PURE__ */ React.createElement(CircleX, { className: cn(iconClass, "text-destructive") });
      case "warning":
        return /* @__PURE__ */ React.createElement(CircleAlert, { className: cn(iconClass, "text-warning") });
      case "info":
        return /* @__PURE__ */ React.createElement(Info, { className: cn(iconClass, "text-primary") });
      default:
        return /* @__PURE__ */ React.createElement(Info, { className: cn(iconClass, "text-inherit") });
    }
  };
  var ToastAction2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Action3, { "data-slot": "toast-action", className: cn("ms-4", className), ...props });
  var Toast2 = ({ className, variant, position, children, ...props }) => {
    return /* @__PURE__ */ React.createElement(ToastContext.Provider, { value: { variant, position } }, /* @__PURE__ */ React.createElement(Root22, { "data-slot": "toast", className: cn("outline-none", toastVariants({ variant, position }), className), ...props }, children));
  };
  Toast2.Provider = Provider;
  Toast2.Viewport = ToastViewport2;
  Toast2.Title = ToastTitle2;
  Toast2.Description = ToastDescription2;
  Toast2.Close = ToastClose2;
  Toast2.Icon = ToastIcon;
  Toast2.Action = ToastAction2;
  Toast2.displayName = Root22.displayName;
  ToastViewport2.displayName = Viewport.displayName;
  ToastTitle2.displayName = Title6.displayName;
  ToastDescription2.displayName = Description5.displayName;
  ToastClose2.displayName = Close.displayName;
  ToastIcon.displayName = "ToastIcon";
  ToastAction2.displayName = Action3.displayName;
  var Toast_default = Toast2;

  // src/components/ui/avatar/index.ts
  init_define_import_meta_env();

  // src/components/ui/avatar/Avatar.tsx
  init_define_import_meta_env();

  // node_modules/@radix-ui/react-avatar/dist/index.mjs
  init_define_import_meta_env();
  var React34 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-avatar/node_modules/@radix-ui/react-context/dist/index.mjs
  init_define_import_meta_env();
  var React32 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime15 = __toESM(require_react_shim(), 1);
  function createContextScope2(scopeName, createContextScopeDeps = []) {
    let defaultContexts = [];
    function createContext32(rootComponentName, defaultContext2) {
      const BaseContext = React32.createContext(defaultContext2);
      BaseContext.displayName = rootComponentName + "Context";
      const index2 = defaultContexts.length;
      defaultContexts = [...defaultContexts, defaultContext2];
      const Provider4 = (props) => {
        const { scope, children, ...context2 } = props;
        const Context = scope?.[scopeName]?.[index2] || BaseContext;
        const value = React32.useMemo(() => context2, Object.values(context2));
        return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(Context.Provider, { value, children });
      };
      Provider4.displayName = rootComponentName + "Provider";
      function useContext210(consumerName, scope) {
        const Context = scope?.[scopeName]?.[index2] || BaseContext;
        const context2 = React32.useContext(Context);
        if (context2) return context2;
        if (defaultContext2 !== void 0) return defaultContext2;
        throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
      }
      return [Provider4, useContext210];
    }
    const createScope = () => {
      const scopeContexts = defaultContexts.map((defaultContext2) => {
        return React32.createContext(defaultContext2);
      });
      return function useScope(scope) {
        const contexts = scope?.[scopeName] || scopeContexts;
        return React32.useMemo(
          () => ({ [`__scope${scopeName}`]: { ...scope, [scopeName]: contexts } }),
          [scope, contexts]
        );
      };
    };
    createScope.scopeName = scopeName;
    return [createContext32, composeContextScopes2(createScope, ...createContextScopeDeps)];
  }
  function composeContextScopes2(...scopes) {
    const baseScope = scopes[0];
    if (scopes.length === 1) return baseScope;
    const createScope = () => {
      const scopeHooks = scopes.map((createScope2) => ({
        useScope: createScope2(),
        scopeName: createScope2.scopeName
      }));
      return function useComposedScopes(overrideScopes) {
        const nextScopes = scopeHooks.reduce((nextScopes2, { useScope, scopeName }) => {
          const scopeProps = useScope(overrideScopes);
          const currentScope = scopeProps[`__scope${scopeName}`];
          return { ...nextScopes2, ...currentScope };
        }, {});
        return React32.useMemo(() => ({ [`__scope${baseScope.scopeName}`]: nextScopes }), [nextScopes]);
      };
    };
    createScope.scopeName = baseScope.scopeName;
    return createScope;
  }

  // node_modules/@radix-ui/react-avatar/node_modules/@radix-ui/react-primitive/dist/index.mjs
  init_define_import_meta_env();
  var React33 = __toESM(require_react_shim(), 1);
  var ReactDOM6 = __toESM(require_react_dom_shim(), 1);
  var import_jsx_runtime16 = __toESM(require_react_shim(), 1);
  var NODES3 = [
    "a",
    "button",
    "div",
    "form",
    "h2",
    "h3",
    "img",
    "input",
    "label",
    "li",
    "nav",
    "ol",
    "p",
    "select",
    "span",
    "svg",
    "ul"
  ];
  var Primitive3 = NODES3.reduce((primitive, node) => {
    const Slot6 = createSlot(`Primitive.${node}`);
    const Node2 = React33.forwardRef((props, forwardedRef) => {
      const { asChild, ...primitiveProps } = props;
      const Comp = asChild ? Slot6 : node;
      if (typeof window !== "undefined") {
        window[/* @__PURE__ */ Symbol.for("radix-ui")] = true;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(Comp, { ...primitiveProps, ref: forwardedRef });
    });
    Node2.displayName = `Primitive.${node}`;
    return { ...primitive, [node]: Node2 };
  }, {});

  // node_modules/@radix-ui/react-use-is-hydrated/dist/index.mjs
  init_define_import_meta_env();
  var import_shim2 = __toESM(require_shim(), 1);
  function useIsHydrated() {
    return (0, import_shim2.useSyncExternalStore)(
      subscribe,
      () => true,
      () => false
    );
  }
  function subscribe() {
    return () => {
    };
  }

  // node_modules/@radix-ui/react-avatar/dist/index.mjs
  var import_jsx_runtime17 = __toESM(require_react_shim(), 1);
  var AVATAR_NAME = "Avatar";
  var [createAvatarContext, createAvatarScope] = createContextScope2(AVATAR_NAME);
  var [AvatarProvider, useAvatarContext] = createAvatarContext(AVATAR_NAME);
  var Avatar = React34.forwardRef(
    (props, forwardedRef) => {
      const { __scopeAvatar, ...avatarProps } = props;
      const [imageLoadingStatus, setImageLoadingStatus] = React34.useState("idle");
      return /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
        AvatarProvider,
        {
          scope: __scopeAvatar,
          imageLoadingStatus,
          onImageLoadingStatusChange: setImageLoadingStatus,
          children: /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(Primitive3.span, { ...avatarProps, ref: forwardedRef })
        }
      );
    }
  );
  Avatar.displayName = AVATAR_NAME;
  var IMAGE_NAME = "AvatarImage";
  var AvatarImage = React34.forwardRef(
    (props, forwardedRef) => {
      const { __scopeAvatar, src, onLoadingStatusChange = () => {
      }, ...imageProps } = props;
      const context2 = useAvatarContext(IMAGE_NAME, __scopeAvatar);
      const imageLoadingStatus = useImageLoadingStatus(src, imageProps);
      const handleLoadingStatusChange = useCallbackRef((status) => {
        onLoadingStatusChange(status);
        context2.onImageLoadingStatusChange(status);
      });
      useLayoutEffect22(() => {
        if (imageLoadingStatus !== "idle") {
          handleLoadingStatusChange(imageLoadingStatus);
        }
      }, [imageLoadingStatus, handleLoadingStatusChange]);
      return imageLoadingStatus === "loaded" ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(Primitive3.img, { ...imageProps, ref: forwardedRef, src }) : null;
    }
  );
  AvatarImage.displayName = IMAGE_NAME;
  var FALLBACK_NAME = "AvatarFallback";
  var AvatarFallback = React34.forwardRef(
    (props, forwardedRef) => {
      const { __scopeAvatar, delayMs, ...fallbackProps } = props;
      const context2 = useAvatarContext(FALLBACK_NAME, __scopeAvatar);
      const [canRender, setCanRender] = React34.useState(delayMs === void 0);
      React34.useEffect(() => {
        if (delayMs !== void 0) {
          const timerId = window.setTimeout(() => setCanRender(true), delayMs);
          return () => window.clearTimeout(timerId);
        }
      }, [delayMs]);
      return canRender && context2.imageLoadingStatus !== "loaded" ? /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(Primitive3.span, { ...fallbackProps, ref: forwardedRef }) : null;
    }
  );
  AvatarFallback.displayName = FALLBACK_NAME;
  function resolveLoadingStatus(image, src) {
    if (!image) {
      return "idle";
    }
    if (!src) {
      return "error";
    }
    if (image.src !== src) {
      image.src = src;
    }
    return image.complete && image.naturalWidth > 0 ? "loaded" : "loading";
  }
  function useImageLoadingStatus(src, { referrerPolicy, crossOrigin }) {
    const isHydrated = useIsHydrated();
    const imageRef = React34.useRef(null);
    const image = (() => {
      if (!isHydrated) return null;
      if (!imageRef.current) {
        imageRef.current = new window.Image();
      }
      return imageRef.current;
    })();
    const [loadingStatus, setLoadingStatus] = React34.useState(
      () => resolveLoadingStatus(image, src)
    );
    useLayoutEffect22(() => {
      setLoadingStatus(resolveLoadingStatus(image, src));
    }, [image, src]);
    useLayoutEffect22(() => {
      const updateStatus = (status) => () => {
        setLoadingStatus(status);
      };
      if (!image) return;
      const handleLoad = updateStatus("loaded");
      const handleError = updateStatus("error");
      image.addEventListener("load", handleLoad);
      image.addEventListener("error", handleError);
      if (referrerPolicy) {
        image.referrerPolicy = referrerPolicy;
      }
      if (typeof crossOrigin === "string") {
        image.crossOrigin = crossOrigin;
      }
      return () => {
        image.removeEventListener("load", handleLoad);
        image.removeEventListener("error", handleError);
      };
    }, [image, crossOrigin, referrerPolicy]);
    return loadingStatus;
  }
  var Root4 = Avatar;
  var Image = AvatarImage;
  var Fallback = AvatarFallback;

  // src/components/ui/avatar/Avatar.tsx
  var Image2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Image, { "data-slot": "avatar-iamge", className: cn("aspect-square h-full w-full", className), ...props });
  var Fallback2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    Fallback,
    {
      "data-slot": "avatar-fallback",
      className: cn("bg-background flex h-full w-full items-center justify-center rounded-full", className),
      ...props
    }
  );
  var Avatar2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    Root4,
    {
      "data-slot": "avatar",
      className: cn("relative flex h-10 w-10 shrink-0 overflow-hidden rounded-full", className),
      ...props
    }
  );
  Avatar2.Image = Image2;
  Avatar2.Fallback = Fallback2;
  var Avatar_default = Avatar2;

  // src/components/ui/pagination/index.ts
  init_define_import_meta_env();

  // src/components/ui/pagination/Pagination.tsx
  init_define_import_meta_env();
  var List4 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("ol", { "data-slot": "pagination-content", className: cn("flex items-center gap-2", className), ...props });
  var Item4 = ({ ...props }) => /* @__PURE__ */ React.createElement("li", { "data-slot": "pagination-item", ...props });
  var NavButton = ({ className, isActive, ...props }) => /* @__PURE__ */ React.createElement(
    "button",
    {
      type: "button",
      "aria-current": isActive ? "page" : void 0,
      "data-slot": "pagination-link",
      "data-active": isActive,
      className: cn(
        "inline-flex cursor-pointer items-center justify-center rounded-full px-3 py-2 text-xs font-medium transition-colors",
        "size-9 outline-none",
        "disabled:pointer-events-none disabled:opacity-50",
        isActive ? "bg-primary text-primary-foreground dark:bg-primary-300" : "text-muted-400 hover:text-primary-900 hover:bg-muted-50",
        className
      ),
      ...props
    }
  );
  var Previous = ({ className, ...props }) => /* @__PURE__ */ React.createElement(NavButton, { "aria-label": "Go to previous page", className: cn("text-primary size-6 rounded-lg p-0", className), ...props }, /* @__PURE__ */ React.createElement(ChevronLeft, { className: "size-6", strokeWidth: 1.5 }));
  var Next = ({ className, ...props }) => /* @__PURE__ */ React.createElement(NavButton, { "aria-label": "Go to next page", className: cn("text-primary size-6 rounded-lg p-0", className), ...props }, /* @__PURE__ */ React.createElement(ChevronRight, { className: "size-6", strokeWidth: 1.5 }));
  var Ellipsis2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    "span",
    {
      "aria-hidden": true,
      "data-slot": "pagination-ellipsis",
      className: cn("text-muted-400 flex size-5 items-center justify-center", className),
      ...props
    },
    /* @__PURE__ */ React.createElement(Ellipsis, { className: "size-4" }),
    /* @__PURE__ */ React.createElement("span", { className: "sr-only" }, "More pages")
  );
  var Pagination = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    "nav",
    {
      role: "navigation",
      "aria-label": "pagination",
      "data-slot": "pagination",
      className: cn("flex w-full items-center justify-end gap-3 rounded-2xl px-6 py-2", className),
      ...props
    }
  );
  Pagination.List = List4;
  Pagination.Item = Item4;
  Pagination.NavButton = NavButton;
  Pagination.Previous = Previous;
  Pagination.Next = Next;
  Pagination.Ellipsis = Ellipsis2;
  var Pagination_default = Pagination;

  // src/components/ui/popover/index.ts
  init_define_import_meta_env();

  // src/components/ui/popover/Popover.tsx
  init_define_import_meta_env();
  var import_react57 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-popover/dist/index.mjs
  init_define_import_meta_env();
  var React50 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-focus-guards/dist/index.mjs
  init_define_import_meta_env();
  var React35 = __toESM(require_react_shim(), 1);
  var count2 = 0;
  function useFocusGuards() {
    React35.useEffect(() => {
      const edgeGuards = document.querySelectorAll("[data-radix-focus-guard]");
      document.body.insertAdjacentElement("afterbegin", edgeGuards[0] ?? createFocusGuard());
      document.body.insertAdjacentElement("beforeend", edgeGuards[1] ?? createFocusGuard());
      count2++;
      return () => {
        if (count2 === 1) {
          document.querySelectorAll("[data-radix-focus-guard]").forEach((node) => node.remove());
        }
        count2--;
      };
    }, []);
  }
  function createFocusGuard() {
    const element = document.createElement("span");
    element.setAttribute("data-radix-focus-guard", "");
    element.tabIndex = 0;
    element.style.outline = "none";
    element.style.opacity = "0";
    element.style.position = "fixed";
    element.style.pointerEvents = "none";
    return element;
  }

  // node_modules/@radix-ui/react-focus-scope/dist/index.mjs
  init_define_import_meta_env();
  var React36 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime18 = __toESM(require_react_shim(), 1);
  var AUTOFOCUS_ON_MOUNT = "focusScope.autoFocusOnMount";
  var AUTOFOCUS_ON_UNMOUNT = "focusScope.autoFocusOnUnmount";
  var EVENT_OPTIONS = { bubbles: false, cancelable: true };
  var FOCUS_SCOPE_NAME = "FocusScope";
  var FocusScope = React36.forwardRef((props, forwardedRef) => {
    const {
      loop = false,
      trapped = false,
      onMountAutoFocus: onMountAutoFocusProp,
      onUnmountAutoFocus: onUnmountAutoFocusProp,
      ...scopeProps
    } = props;
    const [container, setContainer] = React36.useState(null);
    const onMountAutoFocus = useCallbackRef(onMountAutoFocusProp);
    const onUnmountAutoFocus = useCallbackRef(onUnmountAutoFocusProp);
    const lastFocusedElementRef = React36.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setContainer(node));
    const focusScope = React36.useRef({
      paused: false,
      pause() {
        this.paused = true;
      },
      resume() {
        this.paused = false;
      }
    }).current;
    React36.useEffect(() => {
      if (trapped) {
        let handleFocusIn2 = function(event) {
          if (focusScope.paused || !container) return;
          const target = event.target;
          if (container.contains(target)) {
            lastFocusedElementRef.current = target;
          } else {
            focus(lastFocusedElementRef.current, { select: true });
          }
        }, handleFocusOut2 = function(event) {
          if (focusScope.paused || !container) return;
          const relatedTarget = event.relatedTarget;
          if (relatedTarget === null) return;
          if (!container.contains(relatedTarget)) {
            focus(lastFocusedElementRef.current, { select: true });
          }
        }, handleMutations2 = function(mutations) {
          const focusedElement = document.activeElement;
          if (focusedElement !== document.body) return;
          for (const mutation of mutations) {
            if (mutation.removedNodes.length > 0) focus(container);
          }
        };
        var handleFocusIn = handleFocusIn2, handleFocusOut = handleFocusOut2, handleMutations = handleMutations2;
        document.addEventListener("focusin", handleFocusIn2);
        document.addEventListener("focusout", handleFocusOut2);
        const mutationObserver = new MutationObserver(handleMutations2);
        if (container) mutationObserver.observe(container, { childList: true, subtree: true });
        return () => {
          document.removeEventListener("focusin", handleFocusIn2);
          document.removeEventListener("focusout", handleFocusOut2);
          mutationObserver.disconnect();
        };
      }
    }, [trapped, container, focusScope.paused]);
    React36.useEffect(() => {
      if (container) {
        focusScopesStack.add(focusScope);
        const previouslyFocusedElement = document.activeElement;
        const hasFocusedCandidate = container.contains(previouslyFocusedElement);
        if (!hasFocusedCandidate) {
          const mountEvent = new CustomEvent(AUTOFOCUS_ON_MOUNT, EVENT_OPTIONS);
          container.addEventListener(AUTOFOCUS_ON_MOUNT, onMountAutoFocus);
          container.dispatchEvent(mountEvent);
          if (!mountEvent.defaultPrevented) {
            focusFirst2(removeLinks(getTabbableCandidates2(container)), { select: true });
            if (document.activeElement === previouslyFocusedElement) {
              focus(container);
            }
          }
        }
        return () => {
          container.removeEventListener(AUTOFOCUS_ON_MOUNT, onMountAutoFocus);
          setTimeout(() => {
            const unmountEvent = new CustomEvent(AUTOFOCUS_ON_UNMOUNT, EVENT_OPTIONS);
            container.addEventListener(AUTOFOCUS_ON_UNMOUNT, onUnmountAutoFocus);
            container.dispatchEvent(unmountEvent);
            if (!unmountEvent.defaultPrevented) {
              focus(previouslyFocusedElement ?? document.body, { select: true });
            }
            container.removeEventListener(AUTOFOCUS_ON_UNMOUNT, onUnmountAutoFocus);
            focusScopesStack.remove(focusScope);
          }, 0);
        };
      }
    }, [container, onMountAutoFocus, onUnmountAutoFocus, focusScope]);
    const handleKeyDown = React36.useCallback(
      (event) => {
        if (!loop && !trapped) return;
        if (focusScope.paused) return;
        const isTabKey = event.key === "Tab" && !event.altKey && !event.ctrlKey && !event.metaKey;
        const focusedElement = document.activeElement;
        if (isTabKey && focusedElement) {
          const container2 = event.currentTarget;
          const [first, last] = getTabbableEdges(container2);
          const hasTabbableElementsInside = first && last;
          if (!hasTabbableElementsInside) {
            if (focusedElement === container2) event.preventDefault();
          } else {
            if (!event.shiftKey && focusedElement === last) {
              event.preventDefault();
              if (loop) focus(first, { select: true });
            } else if (event.shiftKey && focusedElement === first) {
              event.preventDefault();
              if (loop) focus(last, { select: true });
            }
          }
        }
      },
      [loop, trapped, focusScope.paused]
    );
    return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Primitive2.div, { tabIndex: -1, ...scopeProps, ref: composedRefs, onKeyDown: handleKeyDown });
  });
  FocusScope.displayName = FOCUS_SCOPE_NAME;
  function focusFirst2(candidates, { select = false } = {}) {
    const previouslyFocusedElement = document.activeElement;
    for (const candidate of candidates) {
      focus(candidate, { select });
      if (document.activeElement !== previouslyFocusedElement) return;
    }
  }
  function getTabbableEdges(container) {
    const candidates = getTabbableCandidates2(container);
    const first = findVisible(candidates, container);
    const last = findVisible(candidates.reverse(), container);
    return [first, last];
  }
  function getTabbableCandidates2(container) {
    const nodes = [];
    const walker = document.createTreeWalker(container, NodeFilter.SHOW_ELEMENT, {
      acceptNode: (node) => {
        const isHiddenInput = node.tagName === "INPUT" && node.type === "hidden";
        if (node.disabled || node.hidden || isHiddenInput) return NodeFilter.FILTER_SKIP;
        return node.tabIndex >= 0 ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
      }
    });
    while (walker.nextNode()) nodes.push(walker.currentNode);
    return nodes;
  }
  function findVisible(elements, container) {
    for (const element of elements) {
      if (!isHidden(element, { upTo: container })) return element;
    }
  }
  function isHidden(node, { upTo }) {
    if (getComputedStyle(node).visibility === "hidden") return true;
    while (node) {
      if (upTo !== void 0 && node === upTo) return false;
      if (getComputedStyle(node).display === "none") return true;
      node = node.parentElement;
    }
    return false;
  }
  function isSelectableInput(element) {
    return element instanceof HTMLInputElement && "select" in element;
  }
  function focus(element, { select = false } = {}) {
    if (element && element.focus) {
      const previouslyFocusedElement = document.activeElement;
      element.focus({ preventScroll: true });
      if (element !== previouslyFocusedElement && isSelectableInput(element) && select)
        element.select();
    }
  }
  var focusScopesStack = createFocusScopesStack();
  function createFocusScopesStack() {
    let stack = [];
    return {
      add(focusScope) {
        const activeFocusScope = stack[0];
        if (focusScope !== activeFocusScope) {
          activeFocusScope?.pause();
        }
        stack = arrayRemove(stack, focusScope);
        stack.unshift(focusScope);
      },
      remove(focusScope) {
        stack = arrayRemove(stack, focusScope);
        stack[0]?.resume();
      }
    };
  }
  function arrayRemove(array, item) {
    const updatedArray = [...array];
    const index2 = updatedArray.indexOf(item);
    if (index2 !== -1) {
      updatedArray.splice(index2, 1);
    }
    return updatedArray;
  }
  function removeLinks(items) {
    return items.filter((item) => item.tagName !== "A");
  }

  // node_modules/@radix-ui/react-id/dist/index.mjs
  init_define_import_meta_env();
  var React37 = __toESM(require_react_shim(), 1);
  var useReactId = React37[" useId ".trim().toString()] || (() => void 0);
  var count3 = 0;
  function useId5(deterministicId) {
    const [id, setId] = React37.useState(useReactId());
    useLayoutEffect22(() => {
      if (!deterministicId) setId((reactId) => reactId ?? String(count3++));
    }, [deterministicId]);
    return deterministicId || (id ? `radix-${id}` : "");
  }

  // node_modules/@radix-ui/react-popper/dist/index.mjs
  init_define_import_meta_env();
  var React41 = __toESM(require_react_shim(), 1);

  // node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
  init_define_import_meta_env();

  // node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
  init_define_import_meta_env();

  // node_modules/@floating-ui/core/dist/floating-ui.core.mjs
  init_define_import_meta_env();

  // node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
  init_define_import_meta_env();
  var sides = ["top", "right", "bottom", "left"];
  var min2 = Math.min;
  var max2 = Math.max;
  var round = Math.round;
  var floor = Math.floor;
  var createCoords = (v) => ({
    x: v,
    y: v
  });
  var oppositeSideMap = {
    left: "right",
    right: "left",
    bottom: "top",
    top: "bottom"
  };
  function clamp(start, value, end) {
    return max2(start, min2(value, end));
  }
  function evaluate(value, param) {
    return typeof value === "function" ? value(param) : value;
  }
  function getSide(placement) {
    return placement.split("-")[0];
  }
  function getAlignment(placement) {
    return placement.split("-")[1];
  }
  function getOppositeAxis(axis) {
    return axis === "x" ? "y" : "x";
  }
  function getAxisLength(axis) {
    return axis === "y" ? "height" : "width";
  }
  function getSideAxis(placement) {
    const firstChar = placement[0];
    return firstChar === "t" || firstChar === "b" ? "y" : "x";
  }
  function getAlignmentAxis(placement) {
    return getOppositeAxis(getSideAxis(placement));
  }
  function getAlignmentSides(placement, rects, rtl) {
    if (rtl === void 0) {
      rtl = false;
    }
    const alignment = getAlignment(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const length = getAxisLength(alignmentAxis);
    let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
    if (rects.reference[length] > rects.floating[length]) {
      mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
    }
    return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
  }
  function getExpandedPlacements(placement) {
    const oppositePlacement = getOppositePlacement(placement);
    return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
  }
  function getOppositeAlignmentPlacement(placement) {
    return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
  }
  var lrPlacement = ["left", "right"];
  var rlPlacement = ["right", "left"];
  var tbPlacement = ["top", "bottom"];
  var btPlacement = ["bottom", "top"];
  function getSideList(side, isStart, rtl) {
    switch (side) {
      case "top":
      case "bottom":
        if (rtl) return isStart ? rlPlacement : lrPlacement;
        return isStart ? lrPlacement : rlPlacement;
      case "left":
      case "right":
        return isStart ? tbPlacement : btPlacement;
      default:
        return [];
    }
  }
  function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
    const alignment = getAlignment(placement);
    let list = getSideList(getSide(placement), direction === "start", rtl);
    if (alignment) {
      list = list.map((side) => side + "-" + alignment);
      if (flipAlignment) {
        list = list.concat(list.map(getOppositeAlignmentPlacement));
      }
    }
    return list;
  }
  function getOppositePlacement(placement) {
    const side = getSide(placement);
    return oppositeSideMap[side] + placement.slice(side.length);
  }
  function expandPaddingObject(padding) {
    return {
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      ...padding
    };
  }
  function getPaddingObject(padding) {
    return typeof padding !== "number" ? expandPaddingObject(padding) : {
      top: padding,
      right: padding,
      bottom: padding,
      left: padding
    };
  }
  function rectToClientRect(rect) {
    const {
      x,
      y,
      width,
      height
    } = rect;
    return {
      width,
      height,
      top: y,
      left: x,
      right: x + width,
      bottom: y + height,
      x,
      y
    };
  }

  // node_modules/@floating-ui/core/dist/floating-ui.core.mjs
  function computeCoordsFromPlacement(_ref, placement, rtl) {
    let {
      reference,
      floating
    } = _ref;
    const sideAxis = getSideAxis(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const alignLength = getAxisLength(alignmentAxis);
    const side = getSide(placement);
    const isVertical = sideAxis === "y";
    const commonX = reference.x + reference.width / 2 - floating.width / 2;
    const commonY = reference.y + reference.height / 2 - floating.height / 2;
    const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
    let coords;
    switch (side) {
      case "top":
        coords = {
          x: commonX,
          y: reference.y - floating.height
        };
        break;
      case "bottom":
        coords = {
          x: commonX,
          y: reference.y + reference.height
        };
        break;
      case "right":
        coords = {
          x: reference.x + reference.width,
          y: commonY
        };
        break;
      case "left":
        coords = {
          x: reference.x - floating.width,
          y: commonY
        };
        break;
      default:
        coords = {
          x: reference.x,
          y: reference.y
        };
    }
    switch (getAlignment(placement)) {
      case "start":
        coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
        break;
      case "end":
        coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
        break;
    }
    return coords;
  }
  async function detectOverflow(state, options3) {
    var _await$platform$isEle;
    if (options3 === void 0) {
      options3 = {};
    }
    const {
      x,
      y,
      platform: platform2,
      rects,
      elements,
      strategy
    } = state;
    const {
      boundary = "clippingAncestors",
      rootBoundary = "viewport",
      elementContext = "floating",
      altBoundary = false,
      padding = 0
    } = evaluate(options3, state);
    const paddingObject = getPaddingObject(padding);
    const altContext = elementContext === "floating" ? "reference" : "floating";
    const element = elements[altBoundary ? altContext : elementContext];
    const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
      element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
      boundary,
      rootBoundary,
      strategy
    }));
    const rect = elementContext === "floating" ? {
      x,
      y,
      width: rects.floating.width,
      height: rects.floating.height
    } : rects.reference;
    const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
    const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) ? await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
      x: 1,
      y: 1
    } : {
      x: 1,
      y: 1
    };
    const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
      elements,
      rect,
      offsetParent,
      strategy
    }) : rect);
    return {
      top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
      bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
      left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
      right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
    };
  }
  var MAX_RESET_COUNT = 50;
  var computePosition = async (reference, floating, config) => {
    const {
      placement = "bottom",
      strategy = "absolute",
      middleware = [],
      platform: platform2
    } = config;
    const platformWithDetectOverflow = platform2.detectOverflow ? platform2 : {
      ...platform2,
      detectOverflow
    };
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
    let rects = await platform2.getElementRects({
      reference,
      floating,
      strategy
    });
    let {
      x,
      y
    } = computeCoordsFromPlacement(rects, placement, rtl);
    let statefulPlacement = placement;
    let resetCount = 0;
    const middlewareData = {};
    for (let i = 0; i < middleware.length; i++) {
      const currentMiddleware = middleware[i];
      if (!currentMiddleware) {
        continue;
      }
      const {
        name,
        fn
      } = currentMiddleware;
      const {
        x: nextX,
        y: nextY,
        data,
        reset
      } = await fn({
        x,
        y,
        initialPlacement: placement,
        placement: statefulPlacement,
        strategy,
        middlewareData,
        rects,
        platform: platformWithDetectOverflow,
        elements: {
          reference,
          floating
        }
      });
      x = nextX != null ? nextX : x;
      y = nextY != null ? nextY : y;
      middlewareData[name] = {
        ...middlewareData[name],
        ...data
      };
      if (reset && resetCount < MAX_RESET_COUNT) {
        resetCount++;
        if (typeof reset === "object") {
          if (reset.placement) {
            statefulPlacement = reset.placement;
          }
          if (reset.rects) {
            rects = reset.rects === true ? await platform2.getElementRects({
              reference,
              floating,
              strategy
            }) : reset.rects;
          }
          ({
            x,
            y
          } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
        }
        i = -1;
      }
    }
    return {
      x,
      y,
      placement: statefulPlacement,
      strategy,
      middlewareData
    };
  };
  var arrow = (options3) => ({
    name: "arrow",
    options: options3,
    async fn(state) {
      const {
        x,
        y,
        placement,
        rects,
        platform: platform2,
        elements,
        middlewareData
      } = state;
      const {
        element,
        padding = 0
      } = evaluate(options3, state) || {};
      if (element == null) {
        return {};
      }
      const paddingObject = getPaddingObject(padding);
      const coords = {
        x,
        y
      };
      const axis = getAlignmentAxis(placement);
      const length = getAxisLength(axis);
      const arrowDimensions = await platform2.getDimensions(element);
      const isYAxis = axis === "y";
      const minProp = isYAxis ? "top" : "left";
      const maxProp = isYAxis ? "bottom" : "right";
      const clientProp = isYAxis ? "clientHeight" : "clientWidth";
      const endDiff = rects.reference[length] + rects.reference[axis] - coords[axis] - rects.floating[length];
      const startDiff = coords[axis] - rects.reference[axis];
      const arrowOffsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(element));
      let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
      if (!clientSize || !await (platform2.isElement == null ? void 0 : platform2.isElement(arrowOffsetParent))) {
        clientSize = elements.floating[clientProp] || rects.floating[length];
      }
      const centerToReference = endDiff / 2 - startDiff / 2;
      const largestPossiblePadding = clientSize / 2 - arrowDimensions[length] / 2 - 1;
      const minPadding = min2(paddingObject[minProp], largestPossiblePadding);
      const maxPadding = min2(paddingObject[maxProp], largestPossiblePadding);
      const min$1 = minPadding;
      const max3 = clientSize - arrowDimensions[length] - maxPadding;
      const center = clientSize / 2 - arrowDimensions[length] / 2 + centerToReference;
      const offset4 = clamp(min$1, center, max3);
      const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center !== offset4 && rects.reference[length] / 2 - (center < min$1 ? minPadding : maxPadding) - arrowDimensions[length] / 2 < 0;
      const alignmentOffset = shouldAddOffset ? center < min$1 ? center - min$1 : center - max3 : 0;
      return {
        [axis]: coords[axis] + alignmentOffset,
        data: {
          [axis]: offset4,
          centerOffset: center - offset4 - alignmentOffset,
          ...shouldAddOffset && {
            alignmentOffset
          }
        },
        reset: shouldAddOffset
      };
    }
  });
  var flip = function(options3) {
    if (options3 === void 0) {
      options3 = {};
    }
    return {
      name: "flip",
      options: options3,
      async fn(state) {
        var _middlewareData$arrow, _middlewareData$flip;
        const {
          placement,
          middlewareData,
          rects,
          initialPlacement,
          platform: platform2,
          elements
        } = state;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = true,
          fallbackPlacements: specifiedFallbackPlacements,
          fallbackStrategy = "bestFit",
          fallbackAxisSideDirection = "none",
          flipAlignment = true,
          ...detectOverflowOptions
        } = evaluate(options3, state);
        if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
          return {};
        }
        const side = getSide(placement);
        const initialSideAxis = getSideAxis(initialPlacement);
        const isBasePlacement = getSide(initialPlacement) === initialPlacement;
        const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
        const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
        const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
        if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
          fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
        }
        const placements2 = [initialPlacement, ...fallbackPlacements];
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const overflows = [];
        let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
        if (checkMainAxis) {
          overflows.push(overflow[side]);
        }
        if (checkCrossAxis) {
          const sides2 = getAlignmentSides(placement, rects, rtl);
          overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
        }
        overflowsData = [...overflowsData, {
          placement,
          overflows
        }];
        if (!overflows.every((side2) => side2 <= 0)) {
          var _middlewareData$flip2, _overflowsData$filter;
          const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
          const nextPlacement = placements2[nextIndex];
          if (nextPlacement) {
            const ignoreCrossAxisOverflow = checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false;
            if (!ignoreCrossAxisOverflow || // We leave the current main axis only if every placement on that axis
            // overflows the main axis.
            overflowsData.every((d) => getSideAxis(d.placement) === initialSideAxis ? d.overflows[0] > 0 : true)) {
              return {
                data: {
                  index: nextIndex,
                  overflows: overflowsData
                },
                reset: {
                  placement: nextPlacement
                }
              };
            }
          }
          let resetPlacement = (_overflowsData$filter = overflowsData.filter((d) => d.overflows[0] <= 0).sort((a, b) => a.overflows[1] - b.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
          if (!resetPlacement) {
            switch (fallbackStrategy) {
              case "bestFit": {
                var _overflowsData$filter2;
                const placement2 = (_overflowsData$filter2 = overflowsData.filter((d) => {
                  if (hasFallbackAxisSideDirection) {
                    const currentSideAxis = getSideAxis(d.placement);
                    return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                    // reading directions favoring greater width.
                    currentSideAxis === "y";
                  }
                  return true;
                }).map((d) => [d.placement, d.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a, b) => a[1] - b[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
                if (placement2) {
                  resetPlacement = placement2;
                }
                break;
              }
              case "initialPlacement":
                resetPlacement = initialPlacement;
                break;
            }
          }
          if (placement !== resetPlacement) {
            return {
              reset: {
                placement: resetPlacement
              }
            };
          }
        }
        return {};
      }
    };
  };
  function getSideOffsets(overflow, rect) {
    return {
      top: overflow.top - rect.height,
      right: overflow.right - rect.width,
      bottom: overflow.bottom - rect.height,
      left: overflow.left - rect.width
    };
  }
  function isAnySideFullyClipped(overflow) {
    return sides.some((side) => overflow[side] >= 0);
  }
  var hide = function(options3) {
    if (options3 === void 0) {
      options3 = {};
    }
    return {
      name: "hide",
      options: options3,
      async fn(state) {
        const {
          rects,
          platform: platform2
        } = state;
        const {
          strategy = "referenceHidden",
          ...detectOverflowOptions
        } = evaluate(options3, state);
        switch (strategy) {
          case "referenceHidden": {
            const overflow = await platform2.detectOverflow(state, {
              ...detectOverflowOptions,
              elementContext: "reference"
            });
            const offsets = getSideOffsets(overflow, rects.reference);
            return {
              data: {
                referenceHiddenOffsets: offsets,
                referenceHidden: isAnySideFullyClipped(offsets)
              }
            };
          }
          case "escaped": {
            const overflow = await platform2.detectOverflow(state, {
              ...detectOverflowOptions,
              altBoundary: true
            });
            const offsets = getSideOffsets(overflow, rects.floating);
            return {
              data: {
                escapedOffsets: offsets,
                escaped: isAnySideFullyClipped(offsets)
              }
            };
          }
          default: {
            return {};
          }
        }
      }
    };
  };
  var originSides = /* @__PURE__ */ new Set(["left", "top"]);
  async function convertValueToCoords(state, options3) {
    const {
      placement,
      platform: platform2,
      elements
    } = state;
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
    const side = getSide(placement);
    const alignment = getAlignment(placement);
    const isVertical = getSideAxis(placement) === "y";
    const mainAxisMulti = originSides.has(side) ? -1 : 1;
    const crossAxisMulti = rtl && isVertical ? -1 : 1;
    const rawValue = evaluate(options3, state);
    let {
      mainAxis,
      crossAxis,
      alignmentAxis
    } = typeof rawValue === "number" ? {
      mainAxis: rawValue,
      crossAxis: 0,
      alignmentAxis: null
    } : {
      mainAxis: rawValue.mainAxis || 0,
      crossAxis: rawValue.crossAxis || 0,
      alignmentAxis: rawValue.alignmentAxis
    };
    if (alignment && typeof alignmentAxis === "number") {
      crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
    }
    return isVertical ? {
      x: crossAxis * crossAxisMulti,
      y: mainAxis * mainAxisMulti
    } : {
      x: mainAxis * mainAxisMulti,
      y: crossAxis * crossAxisMulti
    };
  }
  var offset = function(options3) {
    if (options3 === void 0) {
      options3 = 0;
    }
    return {
      name: "offset",
      options: options3,
      async fn(state) {
        var _middlewareData$offse, _middlewareData$arrow;
        const {
          x,
          y,
          placement,
          middlewareData
        } = state;
        const diffCoords = await convertValueToCoords(state, options3);
        if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
          return {};
        }
        return {
          x: x + diffCoords.x,
          y: y + diffCoords.y,
          data: {
            ...diffCoords,
            placement
          }
        };
      }
    };
  };
  var shift = function(options3) {
    if (options3 === void 0) {
      options3 = {};
    }
    return {
      name: "shift",
      options: options3,
      async fn(state) {
        const {
          x,
          y,
          placement,
          platform: platform2
        } = state;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = false,
          limiter = {
            fn: (_ref) => {
              let {
                x: x2,
                y: y2
              } = _ref;
              return {
                x: x2,
                y: y2
              };
            }
          },
          ...detectOverflowOptions
        } = evaluate(options3, state);
        const coords = {
          x,
          y
        };
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const crossAxis = getSideAxis(getSide(placement));
        const mainAxis = getOppositeAxis(crossAxis);
        let mainAxisCoord = coords[mainAxis];
        let crossAxisCoord = coords[crossAxis];
        if (checkMainAxis) {
          const minSide = mainAxis === "y" ? "top" : "left";
          const maxSide = mainAxis === "y" ? "bottom" : "right";
          const min3 = mainAxisCoord + overflow[minSide];
          const max3 = mainAxisCoord - overflow[maxSide];
          mainAxisCoord = clamp(min3, mainAxisCoord, max3);
        }
        if (checkCrossAxis) {
          const minSide = crossAxis === "y" ? "top" : "left";
          const maxSide = crossAxis === "y" ? "bottom" : "right";
          const min3 = crossAxisCoord + overflow[minSide];
          const max3 = crossAxisCoord - overflow[maxSide];
          crossAxisCoord = clamp(min3, crossAxisCoord, max3);
        }
        const limitedCoords = limiter.fn({
          ...state,
          [mainAxis]: mainAxisCoord,
          [crossAxis]: crossAxisCoord
        });
        return {
          ...limitedCoords,
          data: {
            x: limitedCoords.x - x,
            y: limitedCoords.y - y,
            enabled: {
              [mainAxis]: checkMainAxis,
              [crossAxis]: checkCrossAxis
            }
          }
        };
      }
    };
  };
  var limitShift = function(options3) {
    if (options3 === void 0) {
      options3 = {};
    }
    return {
      options: options3,
      fn(state) {
        const {
          x,
          y,
          placement,
          rects,
          middlewareData
        } = state;
        const {
          offset: offset4 = 0,
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = true
        } = evaluate(options3, state);
        const coords = {
          x,
          y
        };
        const crossAxis = getSideAxis(placement);
        const mainAxis = getOppositeAxis(crossAxis);
        let mainAxisCoord = coords[mainAxis];
        let crossAxisCoord = coords[crossAxis];
        const rawOffset = evaluate(offset4, state);
        const computedOffset = typeof rawOffset === "number" ? {
          mainAxis: rawOffset,
          crossAxis: 0
        } : {
          mainAxis: 0,
          crossAxis: 0,
          ...rawOffset
        };
        if (checkMainAxis) {
          const len = mainAxis === "y" ? "height" : "width";
          const limitMin = rects.reference[mainAxis] - rects.floating[len] + computedOffset.mainAxis;
          const limitMax = rects.reference[mainAxis] + rects.reference[len] - computedOffset.mainAxis;
          if (mainAxisCoord < limitMin) {
            mainAxisCoord = limitMin;
          } else if (mainAxisCoord > limitMax) {
            mainAxisCoord = limitMax;
          }
        }
        if (checkCrossAxis) {
          var _middlewareData$offse, _middlewareData$offse2;
          const len = mainAxis === "y" ? "width" : "height";
          const isOriginSide = originSides.has(getSide(placement));
          const limitMin = rects.reference[crossAxis] - rects.floating[len] + (isOriginSide ? ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse[crossAxis]) || 0 : 0) + (isOriginSide ? 0 : computedOffset.crossAxis);
          const limitMax = rects.reference[crossAxis] + rects.reference[len] + (isOriginSide ? 0 : ((_middlewareData$offse2 = middlewareData.offset) == null ? void 0 : _middlewareData$offse2[crossAxis]) || 0) - (isOriginSide ? computedOffset.crossAxis : 0);
          if (crossAxisCoord < limitMin) {
            crossAxisCoord = limitMin;
          } else if (crossAxisCoord > limitMax) {
            crossAxisCoord = limitMax;
          }
        }
        return {
          [mainAxis]: mainAxisCoord,
          [crossAxis]: crossAxisCoord
        };
      }
    };
  };
  var size = function(options3) {
    if (options3 === void 0) {
      options3 = {};
    }
    return {
      name: "size",
      options: options3,
      async fn(state) {
        var _state$middlewareData, _state$middlewareData2;
        const {
          placement,
          rects,
          platform: platform2,
          elements
        } = state;
        const {
          apply = () => {
          },
          ...detectOverflowOptions
        } = evaluate(options3, state);
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const side = getSide(placement);
        const alignment = getAlignment(placement);
        const isYAxis = getSideAxis(placement) === "y";
        const {
          width,
          height
        } = rects.floating;
        let heightSide;
        let widthSide;
        if (side === "top" || side === "bottom") {
          heightSide = side;
          widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
        } else {
          widthSide = side;
          heightSide = alignment === "end" ? "top" : "bottom";
        }
        const maximumClippingHeight = height - overflow.top - overflow.bottom;
        const maximumClippingWidth = width - overflow.left - overflow.right;
        const overflowAvailableHeight = min2(height - overflow[heightSide], maximumClippingHeight);
        const overflowAvailableWidth = min2(width - overflow[widthSide], maximumClippingWidth);
        const noShift = !state.middlewareData.shift;
        let availableHeight = overflowAvailableHeight;
        let availableWidth = overflowAvailableWidth;
        if ((_state$middlewareData = state.middlewareData.shift) != null && _state$middlewareData.enabled.x) {
          availableWidth = maximumClippingWidth;
        }
        if ((_state$middlewareData2 = state.middlewareData.shift) != null && _state$middlewareData2.enabled.y) {
          availableHeight = maximumClippingHeight;
        }
        if (noShift && !alignment) {
          const xMin = max2(overflow.left, 0);
          const xMax = max2(overflow.right, 0);
          const yMin = max2(overflow.top, 0);
          const yMax = max2(overflow.bottom, 0);
          if (isYAxis) {
            availableWidth = width - 2 * (xMin !== 0 || xMax !== 0 ? xMin + xMax : max2(overflow.left, overflow.right));
          } else {
            availableHeight = height - 2 * (yMin !== 0 || yMax !== 0 ? yMin + yMax : max2(overflow.top, overflow.bottom));
          }
        }
        await apply({
          ...state,
          availableWidth,
          availableHeight
        });
        const nextDimensions = await platform2.getDimensions(elements.floating);
        if (width !== nextDimensions.width || height !== nextDimensions.height) {
          return {
            reset: {
              rects: true
            }
          };
        }
        return {};
      }
    };
  };

  // node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
  init_define_import_meta_env();
  function hasWindow() {
    return typeof window !== "undefined";
  }
  function getNodeName(node) {
    if (isNode(node)) {
      return (node.nodeName || "").toLowerCase();
    }
    return "#document";
  }
  function getWindow(node) {
    var _node$ownerDocument;
    return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
  }
  function getDocumentElement(node) {
    var _ref;
    return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
  }
  function isNode(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Node || value instanceof getWindow(value).Node;
  }
  function isElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Element || value instanceof getWindow(value).Element;
  }
  function isHTMLElement3(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
  }
  function isShadowRoot(value) {
    if (!hasWindow() || typeof ShadowRoot === "undefined") {
      return false;
    }
    return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
  }
  function isOverflowElement(element) {
    const {
      overflow,
      overflowX,
      overflowY,
      display
    } = getComputedStyle2(element);
    return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
  }
  function isTableElement(element) {
    return /^(table|td|th)$/.test(getNodeName(element));
  }
  function isTopLayer(element) {
    try {
      if (element.matches(":popover-open")) {
        return true;
      }
    } catch (_e2) {
    }
    try {
      return element.matches(":modal");
    } catch (_e2) {
      return false;
    }
  }
  var willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
  var containRe = /paint|layout|strict|content/;
  var isNotNone = (value) => !!value && value !== "none";
  var isWebKitValue;
  function isContainingBlock(elementOrCss) {
    const css = isElement(elementOrCss) ? getComputedStyle2(elementOrCss) : elementOrCss;
    return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
  }
  function getContainingBlock(element) {
    let currentNode = getParentNode(element);
    while (isHTMLElement3(currentNode) && !isLastTraversableNode(currentNode)) {
      if (isContainingBlock(currentNode)) {
        return currentNode;
      } else if (isTopLayer(currentNode)) {
        return null;
      }
      currentNode = getParentNode(currentNode);
    }
    return null;
  }
  function isWebKit() {
    if (isWebKitValue == null) {
      isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
    }
    return isWebKitValue;
  }
  function isLastTraversableNode(node) {
    return /^(html|body|#document)$/.test(getNodeName(node));
  }
  function getComputedStyle2(element) {
    return getWindow(element).getComputedStyle(element);
  }
  function getNodeScroll(element) {
    if (isElement(element)) {
      return {
        scrollLeft: element.scrollLeft,
        scrollTop: element.scrollTop
      };
    }
    return {
      scrollLeft: element.scrollX,
      scrollTop: element.scrollY
    };
  }
  function getParentNode(node) {
    if (getNodeName(node) === "html") {
      return node;
    }
    const result = (
      // Step into the shadow DOM of the parent of a slotted node.
      node.assignedSlot || // DOM Element detected.
      node.parentNode || // ShadowRoot detected.
      isShadowRoot(node) && node.host || // Fallback.
      getDocumentElement(node)
    );
    return isShadowRoot(result) ? result.host : result;
  }
  function getNearestOverflowAncestor(node) {
    const parentNode = getParentNode(node);
    if (isLastTraversableNode(parentNode)) {
      return node.ownerDocument ? node.ownerDocument.body : node.body;
    }
    if (isHTMLElement3(parentNode) && isOverflowElement(parentNode)) {
      return parentNode;
    }
    return getNearestOverflowAncestor(parentNode);
  }
  function getOverflowAncestors(node, list, traverseIframes) {
    var _node$ownerDocument2;
    if (list === void 0) {
      list = [];
    }
    if (traverseIframes === void 0) {
      traverseIframes = true;
    }
    const scrollableAncestor = getNearestOverflowAncestor(node);
    const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
    const win = getWindow(scrollableAncestor);
    if (isBody) {
      const frameElement = getFrameElement(win);
      return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
    } else {
      return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
    }
  }
  function getFrameElement(win) {
    return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
  }

  // node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
  function getCssDimensions(element) {
    const css = getComputedStyle2(element);
    let width = parseFloat(css.width) || 0;
    let height = parseFloat(css.height) || 0;
    const hasOffset = isHTMLElement3(element);
    const offsetWidth = hasOffset ? element.offsetWidth : width;
    const offsetHeight = hasOffset ? element.offsetHeight : height;
    const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
    if (shouldFallback) {
      width = offsetWidth;
      height = offsetHeight;
    }
    return {
      width,
      height,
      $: shouldFallback
    };
  }
  function unwrapElement(element) {
    return !isElement(element) ? element.contextElement : element;
  }
  function getScale(element) {
    const domElement = unwrapElement(element);
    if (!isHTMLElement3(domElement)) {
      return createCoords(1);
    }
    const rect = domElement.getBoundingClientRect();
    const {
      width,
      height,
      $: $2
    } = getCssDimensions(domElement);
    let x = ($2 ? round(rect.width) : rect.width) / width;
    let y = ($2 ? round(rect.height) : rect.height) / height;
    if (!x || !Number.isFinite(x)) {
      x = 1;
    }
    if (!y || !Number.isFinite(y)) {
      y = 1;
    }
    return {
      x,
      y
    };
  }
  var noOffsets = /* @__PURE__ */ createCoords(0);
  function getVisualOffsets(element) {
    const win = getWindow(element);
    if (!isWebKit() || !win.visualViewport) {
      return noOffsets;
    }
    return {
      x: win.visualViewport.offsetLeft,
      y: win.visualViewport.offsetTop
    };
  }
  function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
    if (isFixed === void 0) {
      isFixed = false;
    }
    if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element)) {
      return false;
    }
    return isFixed;
  }
  function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
    if (includeScale === void 0) {
      includeScale = false;
    }
    if (isFixedStrategy === void 0) {
      isFixedStrategy = false;
    }
    const clientRect = element.getBoundingClientRect();
    const domElement = unwrapElement(element);
    let scale = createCoords(1);
    if (includeScale) {
      if (offsetParent) {
        if (isElement(offsetParent)) {
          scale = getScale(offsetParent);
        }
      } else {
        scale = getScale(element);
      }
    }
    const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
    let x = (clientRect.left + visualOffsets.x) / scale.x;
    let y = (clientRect.top + visualOffsets.y) / scale.y;
    let width = clientRect.width / scale.x;
    let height = clientRect.height / scale.y;
    if (domElement) {
      const win = getWindow(domElement);
      const offsetWin = offsetParent && isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
      let currentWin = win;
      let currentIFrame = getFrameElement(currentWin);
      while (currentIFrame && offsetParent && offsetWin !== currentWin) {
        const iframeScale = getScale(currentIFrame);
        const iframeRect = currentIFrame.getBoundingClientRect();
        const css = getComputedStyle2(currentIFrame);
        const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
        const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
        x *= iframeScale.x;
        y *= iframeScale.y;
        width *= iframeScale.x;
        height *= iframeScale.y;
        x += left;
        y += top;
        currentWin = getWindow(currentIFrame);
        currentIFrame = getFrameElement(currentWin);
      }
    }
    return rectToClientRect({
      width,
      height,
      x,
      y
    });
  }
  function getWindowScrollBarX(element, rect) {
    const leftScroll = getNodeScroll(element).scrollLeft;
    if (!rect) {
      return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
    }
    return rect.left + leftScroll;
  }
  function getHTMLOffset(documentElement, scroll) {
    const htmlRect = documentElement.getBoundingClientRect();
    const x = htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect);
    const y = htmlRect.top + scroll.scrollTop;
    return {
      x,
      y
    };
  }
  function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
    let {
      elements,
      rect,
      offsetParent,
      strategy
    } = _ref;
    const isFixed = strategy === "fixed";
    const documentElement = getDocumentElement(offsetParent);
    const topLayer = elements ? isTopLayer(elements.floating) : false;
    if (offsetParent === documentElement || topLayer && isFixed) {
      return rect;
    }
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    let scale = createCoords(1);
    const offsets = createCoords(0);
    const isOffsetParentAnElement = isHTMLElement3(offsetParent);
    if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent);
        scale = getScale(offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      }
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    return {
      width: rect.width * scale.x,
      height: rect.height * scale.y,
      x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
      y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
    };
  }
  function getClientRects(element) {
    return Array.from(element.getClientRects());
  }
  function getDocumentRect(element) {
    const html = getDocumentElement(element);
    const scroll = getNodeScroll(element);
    const body = element.ownerDocument.body;
    const width = max2(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
    const height = max2(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
    let x = -scroll.scrollLeft + getWindowScrollBarX(element);
    const y = -scroll.scrollTop;
    if (getComputedStyle2(body).direction === "rtl") {
      x += max2(html.clientWidth, body.clientWidth) - width;
    }
    return {
      width,
      height,
      x,
      y
    };
  }
  var SCROLLBAR_MAX = 25;
  function getViewportRect(element, strategy) {
    const win = getWindow(element);
    const html = getDocumentElement(element);
    const visualViewport = win.visualViewport;
    let width = html.clientWidth;
    let height = html.clientHeight;
    let x = 0;
    let y = 0;
    if (visualViewport) {
      width = visualViewport.width;
      height = visualViewport.height;
      const visualViewportBased = isWebKit();
      if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
        x = visualViewport.offsetLeft;
        y = visualViewport.offsetTop;
      }
    }
    const windowScrollbarX = getWindowScrollBarX(html);
    if (windowScrollbarX <= 0) {
      const doc = html.ownerDocument;
      const body = doc.body;
      const bodyStyles = getComputedStyle(body);
      const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
      const clippingStableScrollbarWidth = Math.abs(html.clientWidth - body.clientWidth - bodyMarginInline);
      if (clippingStableScrollbarWidth <= SCROLLBAR_MAX) {
        width -= clippingStableScrollbarWidth;
      }
    } else if (windowScrollbarX <= SCROLLBAR_MAX) {
      width += windowScrollbarX;
    }
    return {
      width,
      height,
      x,
      y
    };
  }
  function getInnerBoundingClientRect(element, strategy) {
    const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
    const top = clientRect.top + element.clientTop;
    const left = clientRect.left + element.clientLeft;
    const scale = isHTMLElement3(element) ? getScale(element) : createCoords(1);
    const width = element.clientWidth * scale.x;
    const height = element.clientHeight * scale.y;
    const x = left * scale.x;
    const y = top * scale.y;
    return {
      width,
      height,
      x,
      y
    };
  }
  function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
    let rect;
    if (clippingAncestor === "viewport") {
      rect = getViewportRect(element, strategy);
    } else if (clippingAncestor === "document") {
      rect = getDocumentRect(getDocumentElement(element));
    } else if (isElement(clippingAncestor)) {
      rect = getInnerBoundingClientRect(clippingAncestor, strategy);
    } else {
      const visualOffsets = getVisualOffsets(element);
      rect = {
        x: clippingAncestor.x - visualOffsets.x,
        y: clippingAncestor.y - visualOffsets.y,
        width: clippingAncestor.width,
        height: clippingAncestor.height
      };
    }
    return rectToClientRect(rect);
  }
  function hasFixedPositionAncestor(element, stopNode) {
    const parentNode = getParentNode(element);
    if (parentNode === stopNode || !isElement(parentNode) || isLastTraversableNode(parentNode)) {
      return false;
    }
    return getComputedStyle2(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
  }
  function getClippingElementAncestors(element, cache) {
    const cachedResult = cache.get(element);
    if (cachedResult) {
      return cachedResult;
    }
    let result = getOverflowAncestors(element, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
    let currentContainingBlockComputedStyle = null;
    const elementIsFixed = getComputedStyle2(element).position === "fixed";
    let currentNode = elementIsFixed ? getParentNode(element) : element;
    while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
      const computedStyle = getComputedStyle2(currentNode);
      const currentNodeIsContaining = isContainingBlock(currentNode);
      if (!currentNodeIsContaining && computedStyle.position === "fixed") {
        currentContainingBlockComputedStyle = null;
      }
      const shouldDropCurrentNode = elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && (currentContainingBlockComputedStyle.position === "absolute" || currentContainingBlockComputedStyle.position === "fixed") || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element, currentNode);
      if (shouldDropCurrentNode) {
        result = result.filter((ancestor) => ancestor !== currentNode);
      } else {
        currentContainingBlockComputedStyle = computedStyle;
      }
      currentNode = getParentNode(currentNode);
    }
    cache.set(element, result);
    return result;
  }
  function getClippingRect(_ref) {
    let {
      element,
      boundary,
      rootBoundary,
      strategy
    } = _ref;
    const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary);
    const clippingAncestors = [...elementClippingAncestors, rootBoundary];
    const firstRect = getClientRectFromClippingAncestor(element, clippingAncestors[0], strategy);
    let top = firstRect.top;
    let right = firstRect.right;
    let bottom = firstRect.bottom;
    let left = firstRect.left;
    for (let i = 1; i < clippingAncestors.length; i++) {
      const rect = getClientRectFromClippingAncestor(element, clippingAncestors[i], strategy);
      top = max2(rect.top, top);
      right = min2(rect.right, right);
      bottom = min2(rect.bottom, bottom);
      left = max2(rect.left, left);
    }
    return {
      width: right - left,
      height: bottom - top,
      x: left,
      y: top
    };
  }
  function getDimensions(element) {
    const {
      width,
      height
    } = getCssDimensions(element);
    return {
      width,
      height
    };
  }
  function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
    const isOffsetParentAnElement = isHTMLElement3(offsetParent);
    const documentElement = getDocumentElement(offsetParent);
    const isFixed = strategy === "fixed";
    const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    const offsets = createCoords(0);
    function setLeftRTLScrollbarOffset() {
      offsets.x = getWindowScrollBarX(documentElement);
    }
    if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      } else if (documentElement) {
        setLeftRTLScrollbarOffset();
      }
    }
    if (isFixed && !isOffsetParentAnElement && documentElement) {
      setLeftRTLScrollbarOffset();
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    const x = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
    const y = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
    return {
      x,
      y,
      width: rect.width,
      height: rect.height
    };
  }
  function isStaticPositioned(element) {
    return getComputedStyle2(element).position === "static";
  }
  function getTrueOffsetParent(element, polyfill) {
    if (!isHTMLElement3(element) || getComputedStyle2(element).position === "fixed") {
      return null;
    }
    if (polyfill) {
      return polyfill(element);
    }
    let rawOffsetParent = element.offsetParent;
    if (getDocumentElement(element) === rawOffsetParent) {
      rawOffsetParent = rawOffsetParent.ownerDocument.body;
    }
    return rawOffsetParent;
  }
  function getOffsetParent(element, polyfill) {
    const win = getWindow(element);
    if (isTopLayer(element)) {
      return win;
    }
    if (!isHTMLElement3(element)) {
      let svgOffsetParent = getParentNode(element);
      while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
        if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
          return svgOffsetParent;
        }
        svgOffsetParent = getParentNode(svgOffsetParent);
      }
      return win;
    }
    let offsetParent = getTrueOffsetParent(element, polyfill);
    while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
      offsetParent = getTrueOffsetParent(offsetParent, polyfill);
    }
    if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
      return win;
    }
    return offsetParent || getContainingBlock(element) || win;
  }
  var getElementRects = async function(data) {
    const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
    const getDimensionsFn = this.getDimensions;
    const floatingDimensions = await getDimensionsFn(data.floating);
    return {
      reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
      floating: {
        x: 0,
        y: 0,
        width: floatingDimensions.width,
        height: floatingDimensions.height
      }
    };
  };
  function isRTL(element) {
    return getComputedStyle2(element).direction === "rtl";
  }
  var platform = {
    convertOffsetParentRelativeRectToViewportRelativeRect,
    getDocumentElement,
    getClippingRect,
    getOffsetParent,
    getElementRects,
    getClientRects,
    getDimensions,
    getScale,
    isElement,
    isRTL
  };
  function rectsAreEqual(a, b) {
    return a.x === b.x && a.y === b.y && a.width === b.width && a.height === b.height;
  }
  function observeMove(element, onMove) {
    let io = null;
    let timeoutId;
    const root = getDocumentElement(element);
    function cleanup() {
      var _io;
      clearTimeout(timeoutId);
      (_io = io) == null || _io.disconnect();
      io = null;
    }
    function refresh(skip, threshold) {
      if (skip === void 0) {
        skip = false;
      }
      if (threshold === void 0) {
        threshold = 1;
      }
      cleanup();
      const elementRectForRootMargin = element.getBoundingClientRect();
      const {
        left,
        top,
        width,
        height
      } = elementRectForRootMargin;
      if (!skip) {
        onMove();
      }
      if (!width || !height) {
        return;
      }
      const insetTop = floor(top);
      const insetRight = floor(root.clientWidth - (left + width));
      const insetBottom = floor(root.clientHeight - (top + height));
      const insetLeft = floor(left);
      const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
      const options3 = {
        rootMargin,
        threshold: max2(0, min2(1, threshold)) || 1
      };
      let isFirstUpdate = true;
      function handleObserve(entries) {
        const ratio = entries[0].intersectionRatio;
        if (ratio !== threshold) {
          if (!isFirstUpdate) {
            return refresh();
          }
          if (!ratio) {
            timeoutId = setTimeout(() => {
              refresh(false, 1e-7);
            }, 1e3);
          } else {
            refresh(false, ratio);
          }
        }
        if (ratio === 1 && !rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) {
          refresh();
        }
        isFirstUpdate = false;
      }
      try {
        io = new IntersectionObserver(handleObserve, {
          ...options3,
          // Handle <iframe>s
          root: root.ownerDocument
        });
      } catch (_e2) {
        io = new IntersectionObserver(handleObserve, options3);
      }
      io.observe(element);
    }
    refresh(true);
    return cleanup;
  }
  function autoUpdate(reference, floating, update, options3) {
    if (options3 === void 0) {
      options3 = {};
    }
    const {
      ancestorScroll = true,
      ancestorResize = true,
      elementResize = typeof ResizeObserver === "function",
      layoutShift = typeof IntersectionObserver === "function",
      animationFrame = false
    } = options3;
    const referenceEl = unwrapElement(reference);
    const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.addEventListener("scroll", update, {
        passive: true
      });
      ancestorResize && ancestor.addEventListener("resize", update);
    });
    const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update) : null;
    let reobserveFrame = -1;
    let resizeObserver = null;
    if (elementResize) {
      resizeObserver = new ResizeObserver((_ref) => {
        let [firstEntry] = _ref;
        if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
          resizeObserver.unobserve(floating);
          cancelAnimationFrame(reobserveFrame);
          reobserveFrame = requestAnimationFrame(() => {
            var _resizeObserver;
            (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
          });
        }
        update();
      });
      if (referenceEl && !animationFrame) {
        resizeObserver.observe(referenceEl);
      }
      if (floating) {
        resizeObserver.observe(floating);
      }
    }
    let frameId;
    let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
    if (animationFrame) {
      frameLoop();
    }
    function frameLoop() {
      const nextRefRect = getBoundingClientRect(reference);
      if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) {
        update();
      }
      prevRefRect = nextRefRect;
      frameId = requestAnimationFrame(frameLoop);
    }
    update();
    return () => {
      var _resizeObserver2;
      ancestors.forEach((ancestor) => {
        ancestorScroll && ancestor.removeEventListener("scroll", update);
        ancestorResize && ancestor.removeEventListener("resize", update);
      });
      cleanupIo == null || cleanupIo();
      (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
      resizeObserver = null;
      if (animationFrame) {
        cancelAnimationFrame(frameId);
      }
    };
  }
  var offset2 = offset;
  var shift2 = shift;
  var flip2 = flip;
  var size2 = size;
  var hide2 = hide;
  var arrow2 = arrow;
  var limitShift2 = limitShift;
  var computePosition2 = (reference, floating, options3) => {
    const cache = /* @__PURE__ */ new Map();
    const mergedOptions = {
      platform,
      ...options3
    };
    const platformWithCache = {
      ...mergedOptions.platform,
      _c: cache
    };
    return computePosition(reference, floating, {
      ...mergedOptions,
      platform: platformWithCache
    });
  };

  // node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
  var React38 = __toESM(require_react_shim(), 1);
  var import_react55 = __toESM(require_react_shim(), 1);
  var ReactDOM7 = __toESM(require_react_dom_shim(), 1);
  var isClient = typeof document !== "undefined";
  var noop3 = function noop4() {
  };
  var index = isClient ? import_react55.useLayoutEffect : noop3;
  function deepEqual2(a, b) {
    if (a === b) {
      return true;
    }
    if (typeof a !== typeof b) {
      return false;
    }
    if (typeof a === "function" && a.toString() === b.toString()) {
      return true;
    }
    let length;
    let i;
    let keys;
    if (a && b && typeof a === "object") {
      if (Array.isArray(a)) {
        length = a.length;
        if (length !== b.length) return false;
        for (i = length; i-- !== 0; ) {
          if (!deepEqual2(a[i], b[i])) {
            return false;
          }
        }
        return true;
      }
      keys = Object.keys(a);
      length = keys.length;
      if (length !== Object.keys(b).length) {
        return false;
      }
      for (i = length; i-- !== 0; ) {
        if (!{}.hasOwnProperty.call(b, keys[i])) {
          return false;
        }
      }
      for (i = length; i-- !== 0; ) {
        const key = keys[i];
        if (key === "_owner" && a.$$typeof) {
          continue;
        }
        if (!deepEqual2(a[key], b[key])) {
          return false;
        }
      }
      return true;
    }
    return a !== a && b !== b;
  }
  function getDPR(element) {
    if (typeof window === "undefined") {
      return 1;
    }
    const win = element.ownerDocument.defaultView || window;
    return win.devicePixelRatio || 1;
  }
  function roundByDPR(element, value) {
    const dpr = getDPR(element);
    return Math.round(value * dpr) / dpr;
  }
  function useLatestRef(value) {
    const ref = React38.useRef(value);
    index(() => {
      ref.current = value;
    });
    return ref;
  }
  function useFloating(options3) {
    if (options3 === void 0) {
      options3 = {};
    }
    const {
      placement = "bottom",
      strategy = "absolute",
      middleware = [],
      platform: platform2,
      elements: {
        reference: externalReference,
        floating: externalFloating
      } = {},
      transform = true,
      whileElementsMounted,
      open
    } = options3;
    const [data, setData] = React38.useState({
      x: 0,
      y: 0,
      strategy,
      placement,
      middlewareData: {},
      isPositioned: false
    });
    const [latestMiddleware, setLatestMiddleware] = React38.useState(middleware);
    if (!deepEqual2(latestMiddleware, middleware)) {
      setLatestMiddleware(middleware);
    }
    const [_reference, _setReference] = React38.useState(null);
    const [_floating, _setFloating] = React38.useState(null);
    const setReference = React38.useCallback((node) => {
      if (node !== referenceRef.current) {
        referenceRef.current = node;
        _setReference(node);
      }
    }, []);
    const setFloating = React38.useCallback((node) => {
      if (node !== floatingRef.current) {
        floatingRef.current = node;
        _setFloating(node);
      }
    }, []);
    const referenceEl = externalReference || _reference;
    const floatingEl = externalFloating || _floating;
    const referenceRef = React38.useRef(null);
    const floatingRef = React38.useRef(null);
    const dataRef = React38.useRef(data);
    const hasWhileElementsMounted = whileElementsMounted != null;
    const whileElementsMountedRef = useLatestRef(whileElementsMounted);
    const platformRef = useLatestRef(platform2);
    const openRef = useLatestRef(open);
    const update = React38.useCallback(() => {
      if (!referenceRef.current || !floatingRef.current) {
        return;
      }
      const config = {
        placement,
        strategy,
        middleware: latestMiddleware
      };
      if (platformRef.current) {
        config.platform = platformRef.current;
      }
      computePosition2(referenceRef.current, floatingRef.current, config).then((data2) => {
        const fullData = {
          ...data2,
          // The floating element's position may be recomputed while it's closed
          // but still mounted (such as when transitioning out). To ensure
          // `isPositioned` will be `false` initially on the next open, avoid
          // setting it to `true` when `open === false` (must be specified).
          isPositioned: openRef.current !== false
        };
        if (isMountedRef.current && !deepEqual2(dataRef.current, fullData)) {
          dataRef.current = fullData;
          ReactDOM7.flushSync(() => {
            setData(fullData);
          });
        }
      });
    }, [latestMiddleware, placement, strategy, platformRef, openRef]);
    index(() => {
      if (open === false && dataRef.current.isPositioned) {
        dataRef.current.isPositioned = false;
        setData((data2) => ({
          ...data2,
          isPositioned: false
        }));
      }
    }, [open]);
    const isMountedRef = React38.useRef(false);
    index(() => {
      isMountedRef.current = true;
      return () => {
        isMountedRef.current = false;
      };
    }, []);
    index(() => {
      if (referenceEl) referenceRef.current = referenceEl;
      if (floatingEl) floatingRef.current = floatingEl;
      if (referenceEl && floatingEl) {
        if (whileElementsMountedRef.current) {
          return whileElementsMountedRef.current(referenceEl, floatingEl, update);
        }
        update();
      }
    }, [referenceEl, floatingEl, update, whileElementsMountedRef, hasWhileElementsMounted]);
    const refs = React38.useMemo(() => ({
      reference: referenceRef,
      floating: floatingRef,
      setReference,
      setFloating
    }), [setReference, setFloating]);
    const elements = React38.useMemo(() => ({
      reference: referenceEl,
      floating: floatingEl
    }), [referenceEl, floatingEl]);
    const floatingStyles = React38.useMemo(() => {
      const initialStyles = {
        position: strategy,
        left: 0,
        top: 0
      };
      if (!elements.floating) {
        return initialStyles;
      }
      const x = roundByDPR(elements.floating, data.x);
      const y = roundByDPR(elements.floating, data.y);
      if (transform) {
        return {
          ...initialStyles,
          transform: "translate(" + x + "px, " + y + "px)",
          ...getDPR(elements.floating) >= 1.5 && {
            willChange: "transform"
          }
        };
      }
      return {
        position: strategy,
        left: x,
        top: y
      };
    }, [strategy, transform, elements.floating, data.x, data.y]);
    return React38.useMemo(() => ({
      ...data,
      update,
      refs,
      elements,
      floatingStyles
    }), [data, update, refs, elements, floatingStyles]);
  }
  var arrow$1 = (options3) => {
    function isRef(value) {
      return {}.hasOwnProperty.call(value, "current");
    }
    return {
      name: "arrow",
      options: options3,
      fn(state) {
        const {
          element,
          padding
        } = typeof options3 === "function" ? options3(state) : options3;
        if (element && isRef(element)) {
          if (element.current != null) {
            return arrow2({
              element: element.current,
              padding
            }).fn(state);
          }
          return {};
        }
        if (element) {
          return arrow2({
            element,
            padding
          }).fn(state);
        }
        return {};
      }
    };
  };
  var offset3 = (options3, deps) => {
    const result = offset2(options3);
    return {
      name: result.name,
      fn: result.fn,
      options: [options3, deps]
    };
  };
  var shift3 = (options3, deps) => {
    const result = shift2(options3);
    return {
      name: result.name,
      fn: result.fn,
      options: [options3, deps]
    };
  };
  var limitShift3 = (options3, deps) => {
    const result = limitShift2(options3);
    return {
      fn: result.fn,
      options: [options3, deps]
    };
  };
  var flip3 = (options3, deps) => {
    const result = flip2(options3);
    return {
      name: result.name,
      fn: result.fn,
      options: [options3, deps]
    };
  };
  var size3 = (options3, deps) => {
    const result = size2(options3);
    return {
      name: result.name,
      fn: result.fn,
      options: [options3, deps]
    };
  };
  var hide3 = (options3, deps) => {
    const result = hide2(options3);
    return {
      name: result.name,
      fn: result.fn,
      options: [options3, deps]
    };
  };
  var arrow3 = (options3, deps) => {
    const result = arrow$1(options3);
    return {
      name: result.name,
      fn: result.fn,
      options: [options3, deps]
    };
  };

  // node_modules/@radix-ui/react-arrow/dist/index.mjs
  init_define_import_meta_env();
  var React39 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime19 = __toESM(require_react_shim(), 1);
  var NAME3 = "Arrow";
  var Arrow = React39.forwardRef((props, forwardedRef) => {
    const { children, width = 10, height = 5, ...arrowProps } = props;
    return /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
      Primitive2.svg,
      {
        ...arrowProps,
        ref: forwardedRef,
        width,
        height,
        viewBox: "0 0 30 10",
        preserveAspectRatio: "none",
        children: props.asChild ? children : /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("polygon", { points: "0,0 30,0 15,10" })
      }
    );
  });
  Arrow.displayName = NAME3;
  var Root5 = Arrow;

  // node_modules/@radix-ui/react-use-size/dist/index.mjs
  init_define_import_meta_env();
  var React40 = __toESM(require_react_shim(), 1);
  function useSize(element) {
    const [size4, setSize] = React40.useState(void 0);
    useLayoutEffect22(() => {
      if (element) {
        setSize({ width: element.offsetWidth, height: element.offsetHeight });
        const resizeObserver = new ResizeObserver((entries) => {
          if (!Array.isArray(entries)) {
            return;
          }
          if (!entries.length) {
            return;
          }
          const entry = entries[0];
          let width;
          let height;
          if ("borderBoxSize" in entry) {
            const borderSizeEntry = entry["borderBoxSize"];
            const borderSize = Array.isArray(borderSizeEntry) ? borderSizeEntry[0] : borderSizeEntry;
            width = borderSize["inlineSize"];
            height = borderSize["blockSize"];
          } else {
            width = element.offsetWidth;
            height = element.offsetHeight;
          }
          setSize({ width, height });
        });
        resizeObserver.observe(element, { box: "border-box" });
        return () => resizeObserver.unobserve(element);
      } else {
        setSize(void 0);
      }
    }, [element]);
    return size4;
  }

  // node_modules/@radix-ui/react-popper/dist/index.mjs
  var import_jsx_runtime20 = __toESM(require_react_shim(), 1);
  var POPPER_NAME = "Popper";
  var [createPopperContext, createPopperScope] = createContextScope(POPPER_NAME);
  var [PopperProvider, usePopperContext] = createPopperContext(POPPER_NAME);
  var Popper = (props) => {
    const { __scopePopper, children } = props;
    const [anchor, setAnchor] = React41.useState(null);
    return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(PopperProvider, { scope: __scopePopper, anchor, onAnchorChange: setAnchor, children });
  };
  Popper.displayName = POPPER_NAME;
  var ANCHOR_NAME = "PopperAnchor";
  var PopperAnchor = React41.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopper, virtualRef, ...anchorProps } = props;
      const context2 = usePopperContext(ANCHOR_NAME, __scopePopper);
      const ref = React41.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      const anchorRef = React41.useRef(null);
      React41.useEffect(() => {
        const previousAnchor = anchorRef.current;
        anchorRef.current = virtualRef?.current || ref.current;
        if (previousAnchor !== anchorRef.current) {
          context2.onAnchorChange(anchorRef.current);
        }
      });
      return virtualRef ? null : /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(Primitive2.div, { ...anchorProps, ref: composedRefs });
    }
  );
  PopperAnchor.displayName = ANCHOR_NAME;
  var CONTENT_NAME = "PopperContent";
  var [PopperContentProvider, useContentContext] = createPopperContext(CONTENT_NAME);
  var PopperContent = React41.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopePopper,
        side = "bottom",
        sideOffset = 0,
        align = "center",
        alignOffset = 0,
        arrowPadding = 0,
        avoidCollisions = true,
        collisionBoundary = [],
        collisionPadding: collisionPaddingProp = 0,
        sticky = "partial",
        hideWhenDetached = false,
        updatePositionStrategy = "optimized",
        onPlaced,
        ...contentProps
      } = props;
      const context2 = usePopperContext(CONTENT_NAME, __scopePopper);
      const [content, setContent] = React41.useState(null);
      const composedRefs = useComposedRefs(forwardedRef, (node) => setContent(node));
      const [arrow4, setArrow] = React41.useState(null);
      const arrowSize = useSize(arrow4);
      const arrowWidth = arrowSize?.width ?? 0;
      const arrowHeight = arrowSize?.height ?? 0;
      const desiredPlacement = side + (align !== "center" ? "-" + align : "");
      const collisionPadding = typeof collisionPaddingProp === "number" ? collisionPaddingProp : { top: 0, right: 0, bottom: 0, left: 0, ...collisionPaddingProp };
      const boundary = Array.isArray(collisionBoundary) ? collisionBoundary : [collisionBoundary];
      const hasExplicitBoundaries = boundary.length > 0;
      const detectOverflowOptions = {
        padding: collisionPadding,
        boundary: boundary.filter(isNotNull),
        // with `strategy: 'fixed'`, this is the only way to get it to respect boundaries
        altBoundary: hasExplicitBoundaries
      };
      const { refs, floatingStyles, placement, isPositioned, middlewareData } = useFloating({
        // default to `fixed` strategy so users don't have to pick and we also avoid focus scroll issues
        strategy: "fixed",
        placement: desiredPlacement,
        whileElementsMounted: (...args) => {
          const cleanup = autoUpdate(...args, {
            animationFrame: updatePositionStrategy === "always"
          });
          return cleanup;
        },
        elements: {
          reference: context2.anchor
        },
        middleware: [
          offset3({ mainAxis: sideOffset + arrowHeight, alignmentAxis: alignOffset }),
          avoidCollisions && shift3({
            mainAxis: true,
            crossAxis: false,
            limiter: sticky === "partial" ? limitShift3() : void 0,
            ...detectOverflowOptions
          }),
          avoidCollisions && flip3({ ...detectOverflowOptions }),
          size3({
            ...detectOverflowOptions,
            apply: ({ elements, rects, availableWidth, availableHeight }) => {
              const { width: anchorWidth, height: anchorHeight } = rects.reference;
              const contentStyle = elements.floating.style;
              contentStyle.setProperty("--radix-popper-available-width", `${availableWidth}px`);
              contentStyle.setProperty("--radix-popper-available-height", `${availableHeight}px`);
              contentStyle.setProperty("--radix-popper-anchor-width", `${anchorWidth}px`);
              contentStyle.setProperty("--radix-popper-anchor-height", `${anchorHeight}px`);
            }
          }),
          arrow4 && arrow3({ element: arrow4, padding: arrowPadding }),
          transformOrigin({ arrowWidth, arrowHeight }),
          hideWhenDetached && hide3({ strategy: "referenceHidden", ...detectOverflowOptions })
        ]
      });
      const [placedSide, placedAlign] = getSideAndAlignFromPlacement(placement);
      const handlePlaced = useCallbackRef(onPlaced);
      useLayoutEffect22(() => {
        if (isPositioned) {
          handlePlaced?.();
        }
      }, [isPositioned, handlePlaced]);
      const arrowX = middlewareData.arrow?.x;
      const arrowY = middlewareData.arrow?.y;
      const cannotCenterArrow = middlewareData.arrow?.centerOffset !== 0;
      const [contentZIndex, setContentZIndex] = React41.useState();
      useLayoutEffect22(() => {
        if (content) setContentZIndex(window.getComputedStyle(content).zIndex);
      }, [content]);
      return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
        "div",
        {
          ref: refs.setFloating,
          "data-radix-popper-content-wrapper": "",
          style: {
            ...floatingStyles,
            transform: isPositioned ? floatingStyles.transform : "translate(0, -200%)",
            // keep off the page when measuring
            minWidth: "max-content",
            zIndex: contentZIndex,
            ["--radix-popper-transform-origin"]: [
              middlewareData.transformOrigin?.x,
              middlewareData.transformOrigin?.y
            ].join(" "),
            // hide the content if using the hide middleware and should be hidden
            // set visibility to hidden and disable pointer events so the UI behaves
            // as if the PopperContent isn't there at all
            ...middlewareData.hide?.referenceHidden && {
              visibility: "hidden",
              pointerEvents: "none"
            }
          },
          dir: props.dir,
          children: /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
            PopperContentProvider,
            {
              scope: __scopePopper,
              placedSide,
              onArrowChange: setArrow,
              arrowX,
              arrowY,
              shouldHideArrow: cannotCenterArrow,
              children: /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
                Primitive2.div,
                {
                  "data-side": placedSide,
                  "data-align": placedAlign,
                  ...contentProps,
                  ref: composedRefs,
                  style: {
                    ...contentProps.style,
                    // if the PopperContent hasn't been placed yet (not all measurements done)
                    // we prevent animations so that users's animation don't kick in too early referring wrong sides
                    animation: !isPositioned ? "none" : void 0
                  }
                }
              )
            }
          )
        }
      );
    }
  );
  PopperContent.displayName = CONTENT_NAME;
  var ARROW_NAME = "PopperArrow";
  var OPPOSITE_SIDE = {
    top: "bottom",
    right: "left",
    bottom: "top",
    left: "right"
  };
  var PopperArrow = React41.forwardRef(function PopperArrow2(props, forwardedRef) {
    const { __scopePopper, ...arrowProps } = props;
    const contentContext = useContentContext(ARROW_NAME, __scopePopper);
    const baseSide = OPPOSITE_SIDE[contentContext.placedSide];
    return (
      // we have to use an extra wrapper because `ResizeObserver` (used by `useSize`)
      // doesn't report size as we'd expect on SVG elements.
      // it reports their bounding box which is effectively the largest path inside the SVG.
      /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
        "span",
        {
          ref: contentContext.onArrowChange,
          style: {
            position: "absolute",
            left: contentContext.arrowX,
            top: contentContext.arrowY,
            [baseSide]: 0,
            transformOrigin: {
              top: "",
              right: "0 0",
              bottom: "center 0",
              left: "100% 0"
            }[contentContext.placedSide],
            transform: {
              top: "translateY(100%)",
              right: "translateY(50%) rotate(90deg) translateX(-50%)",
              bottom: `rotate(180deg)`,
              left: "translateY(50%) rotate(-90deg) translateX(50%)"
            }[contentContext.placedSide],
            visibility: contentContext.shouldHideArrow ? "hidden" : void 0
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
            Root5,
            {
              ...arrowProps,
              ref: forwardedRef,
              style: {
                ...arrowProps.style,
                // ensures the element can be measured correctly (mostly for if SVG)
                display: "block"
              }
            }
          )
        }
      )
    );
  });
  PopperArrow.displayName = ARROW_NAME;
  function isNotNull(value) {
    return value !== null;
  }
  var transformOrigin = (options3) => ({
    name: "transformOrigin",
    options: options3,
    fn(data) {
      const { placement, rects, middlewareData } = data;
      const cannotCenterArrow = middlewareData.arrow?.centerOffset !== 0;
      const isArrowHidden = cannotCenterArrow;
      const arrowWidth = isArrowHidden ? 0 : options3.arrowWidth;
      const arrowHeight = isArrowHidden ? 0 : options3.arrowHeight;
      const [placedSide, placedAlign] = getSideAndAlignFromPlacement(placement);
      const noArrowAlign = { start: "0%", center: "50%", end: "100%" }[placedAlign];
      const arrowXCenter = (middlewareData.arrow?.x ?? 0) + arrowWidth / 2;
      const arrowYCenter = (middlewareData.arrow?.y ?? 0) + arrowHeight / 2;
      let x = "";
      let y = "";
      if (placedSide === "bottom") {
        x = isArrowHidden ? noArrowAlign : `${arrowXCenter}px`;
        y = `${-arrowHeight}px`;
      } else if (placedSide === "top") {
        x = isArrowHidden ? noArrowAlign : `${arrowXCenter}px`;
        y = `${rects.floating.height + arrowHeight}px`;
      } else if (placedSide === "right") {
        x = `${-arrowHeight}px`;
        y = isArrowHidden ? noArrowAlign : `${arrowYCenter}px`;
      } else if (placedSide === "left") {
        x = `${rects.floating.width + arrowHeight}px`;
        y = isArrowHidden ? noArrowAlign : `${arrowYCenter}px`;
      }
      return { data: { x, y } };
    }
  });
  function getSideAndAlignFromPlacement(placement) {
    const [side, align = "center"] = placement.split("-");
    return [side, align];
  }
  var Root23 = Popper;
  var Anchor = PopperAnchor;
  var Content4 = PopperContent;
  var Arrow2 = PopperArrow;

  // node_modules/@radix-ui/react-popover/node_modules/@radix-ui/react-slot/dist/index.mjs
  init_define_import_meta_env();
  var React42 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime21 = __toESM(require_react_shim(), 1);
  // @__NO_SIDE_EFFECTS__
  function createSlot4(ownerName) {
    const SlotClone = /* @__PURE__ */ createSlotClone4(ownerName);
    const Slot22 = React42.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      const childrenArray = React42.Children.toArray(children);
      const slottable = childrenArray.find(isSlottable4);
      if (slottable) {
        const newElement = slottable.props.children;
        const newChildren = childrenArray.map((child) => {
          if (child === slottable) {
            if (React42.Children.count(newElement) > 1) return React42.Children.only(null);
            return React42.isValidElement(newElement) ? newElement.props.children : null;
          } else {
            return child;
          }
        });
        return /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children: React42.isValidElement(newElement) ? React42.cloneElement(newElement, void 0, newChildren) : null });
      }
      return /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children });
    });
    Slot22.displayName = `${ownerName}.Slot`;
    return Slot22;
  }
  // @__NO_SIDE_EFFECTS__
  function createSlotClone4(ownerName) {
    const SlotClone = React42.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      if (React42.isValidElement(children)) {
        const childrenRef = getElementRef5(children);
        const props2 = mergeProps4(slotProps, children.props);
        if (children.type !== React42.Fragment) {
          props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
        }
        return React42.cloneElement(children, props2);
      }
      return React42.Children.count(children) > 1 ? React42.Children.only(null) : null;
    });
    SlotClone.displayName = `${ownerName}.SlotClone`;
    return SlotClone;
  }
  var SLOTTABLE_IDENTIFIER4 = /* @__PURE__ */ Symbol("radix.slottable");
  function isSlottable4(child) {
    return React42.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER4;
  }
  function mergeProps4(slotProps, childProps) {
    const overrideProps = { ...childProps };
    for (const propName in childProps) {
      const slotPropValue = slotProps[propName];
      const childPropValue = childProps[propName];
      const isHandler = /^on[A-Z]/.test(propName);
      if (isHandler) {
        if (slotPropValue && childPropValue) {
          overrideProps[propName] = (...args) => {
            const result = childPropValue(...args);
            slotPropValue(...args);
            return result;
          };
        } else if (slotPropValue) {
          overrideProps[propName] = slotPropValue;
        }
      } else if (propName === "style") {
        overrideProps[propName] = { ...slotPropValue, ...childPropValue };
      } else if (propName === "className") {
        overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
      }
    }
    return { ...slotProps, ...overrideProps };
  }
  function getElementRef5(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }

  // node_modules/aria-hidden/dist/es2015/index.js
  init_define_import_meta_env();
  var getDefaultParent = function(originalTarget) {
    if (typeof document === "undefined") {
      return null;
    }
    var sampleTarget = Array.isArray(originalTarget) ? originalTarget[0] : originalTarget;
    return sampleTarget.ownerDocument.body;
  };
  var counterMap = /* @__PURE__ */ new WeakMap();
  var uncontrolledNodes = /* @__PURE__ */ new WeakMap();
  var markerMap = {};
  var lockCount = 0;
  var unwrapHost = function(node) {
    return node && (node.host || unwrapHost(node.parentNode));
  };
  var correctTargets = function(parent, targets) {
    return targets.map(function(target) {
      if (parent.contains(target)) {
        return target;
      }
      var correctedTarget = unwrapHost(target);
      if (correctedTarget && parent.contains(correctedTarget)) {
        return correctedTarget;
      }
      console.error("aria-hidden", target, "in not contained inside", parent, ". Doing nothing");
      return null;
    }).filter(function(x) {
      return Boolean(x);
    });
  };
  var applyAttributeToOthers = function(originalTarget, parentNode, markerName, controlAttribute) {
    var targets = correctTargets(parentNode, Array.isArray(originalTarget) ? originalTarget : [originalTarget]);
    if (!markerMap[markerName]) {
      markerMap[markerName] = /* @__PURE__ */ new WeakMap();
    }
    var markerCounter = markerMap[markerName];
    var hiddenNodes = [];
    var elementsToKeep = /* @__PURE__ */ new Set();
    var elementsToStop = new Set(targets);
    var keep = function(el) {
      if (!el || elementsToKeep.has(el)) {
        return;
      }
      elementsToKeep.add(el);
      keep(el.parentNode);
    };
    targets.forEach(keep);
    var deep = function(parent) {
      if (!parent || elementsToStop.has(parent)) {
        return;
      }
      Array.prototype.forEach.call(parent.children, function(node) {
        if (elementsToKeep.has(node)) {
          deep(node);
        } else {
          try {
            var attr = node.getAttribute(controlAttribute);
            var alreadyHidden = attr !== null && attr !== "false";
            var counterValue = (counterMap.get(node) || 0) + 1;
            var markerValue = (markerCounter.get(node) || 0) + 1;
            counterMap.set(node, counterValue);
            markerCounter.set(node, markerValue);
            hiddenNodes.push(node);
            if (counterValue === 1 && alreadyHidden) {
              uncontrolledNodes.set(node, true);
            }
            if (markerValue === 1) {
              node.setAttribute(markerName, "true");
            }
            if (!alreadyHidden) {
              node.setAttribute(controlAttribute, "true");
            }
          } catch (e2) {
            console.error("aria-hidden: cannot operate on ", node, e2);
          }
        }
      });
    };
    deep(parentNode);
    elementsToKeep.clear();
    lockCount++;
    return function() {
      hiddenNodes.forEach(function(node) {
        var counterValue = counterMap.get(node) - 1;
        var markerValue = markerCounter.get(node) - 1;
        counterMap.set(node, counterValue);
        markerCounter.set(node, markerValue);
        if (!counterValue) {
          if (!uncontrolledNodes.has(node)) {
            node.removeAttribute(controlAttribute);
          }
          uncontrolledNodes.delete(node);
        }
        if (!markerValue) {
          node.removeAttribute(markerName);
        }
      });
      lockCount--;
      if (!lockCount) {
        counterMap = /* @__PURE__ */ new WeakMap();
        counterMap = /* @__PURE__ */ new WeakMap();
        uncontrolledNodes = /* @__PURE__ */ new WeakMap();
        markerMap = {};
      }
    };
  };
  var hideOthers = function(originalTarget, parentNode, markerName) {
    if (markerName === void 0) {
      markerName = "data-aria-hidden";
    }
    var targets = Array.from(Array.isArray(originalTarget) ? originalTarget : [originalTarget]);
    var activeParentNode = parentNode || getDefaultParent(originalTarget);
    if (!activeParentNode) {
      return function() {
        return null;
      };
    }
    targets.push.apply(targets, Array.from(activeParentNode.querySelectorAll("[aria-live], script")));
    return applyAttributeToOthers(targets, activeParentNode, markerName, "aria-hidden");
  };

  // node_modules/react-remove-scroll/dist/es2015/index.js
  init_define_import_meta_env();

  // node_modules/react-remove-scroll/dist/es2015/Combination.js
  init_define_import_meta_env();

  // node_modules/tslib/tslib.es6.mjs
  init_define_import_meta_env();
  var __assign = function() {
    __assign = Object.assign || function __assign2(t3) {
      for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p2 in s) if (Object.prototype.hasOwnProperty.call(s, p2)) t3[p2] = s[p2];
      }
      return t3;
    };
    return __assign.apply(this, arguments);
  };
  function __rest(s, e2) {
    var t3 = {};
    for (var p2 in s) if (Object.prototype.hasOwnProperty.call(s, p2) && e2.indexOf(p2) < 0)
      t3[p2] = s[p2];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
      for (var i = 0, p2 = Object.getOwnPropertySymbols(s); i < p2.length; i++) {
        if (e2.indexOf(p2[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p2[i]))
          t3[p2[i]] = s[p2[i]];
      }
    return t3;
  }
  function __spreadArray(to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar2; i < l; i++) {
      if (ar2 || !(i in from)) {
        if (!ar2) ar2 = Array.prototype.slice.call(from, 0, i);
        ar2[i] = from[i];
      }
    }
    return to.concat(ar2 || Array.prototype.slice.call(from));
  }

  // node_modules/react-remove-scroll/dist/es2015/Combination.js
  var React49 = __toESM(require_react_shim());

  // node_modules/react-remove-scroll/dist/es2015/UI.js
  init_define_import_meta_env();
  var React45 = __toESM(require_react_shim());

  // node_modules/react-remove-scroll-bar/dist/es2015/constants.js
  init_define_import_meta_env();
  var zeroRightClassName = "right-scroll-bar-position";
  var fullWidthClassName = "width-before-scroll-bar";
  var noScrollbarsClassName = "with-scroll-bars-hidden";
  var removedBarSizeVariable = "--removed-body-scroll-bar-size";

  // node_modules/use-callback-ref/dist/es2015/index.js
  init_define_import_meta_env();

  // node_modules/use-callback-ref/dist/es2015/assignRef.js
  init_define_import_meta_env();
  function assignRef(ref, value) {
    if (typeof ref === "function") {
      ref(value);
    } else if (ref) {
      ref.current = value;
    }
    return ref;
  }

  // node_modules/use-callback-ref/dist/es2015/useRef.js
  init_define_import_meta_env();
  var import_react56 = __toESM(require_react_shim());
  function useCallbackRef2(initialValue, callback) {
    var ref = (0, import_react56.useState)(function() {
      return {
        // value
        value: initialValue,
        // last callback
        callback,
        // "memoized" public interface
        facade: {
          get current() {
            return ref.value;
          },
          set current(value) {
            var last = ref.value;
            if (last !== value) {
              ref.value = value;
              ref.callback(value, last);
            }
          }
        }
      };
    })[0];
    ref.callback = callback;
    return ref.facade;
  }

  // node_modules/use-callback-ref/dist/es2015/useMergeRef.js
  init_define_import_meta_env();
  var React43 = __toESM(require_react_shim());
  var useIsomorphicLayoutEffect3 = typeof window !== "undefined" ? React43.useLayoutEffect : React43.useEffect;
  var currentValues = /* @__PURE__ */ new WeakMap();
  function useMergeRefs(refs, defaultValue) {
    var callbackRef = useCallbackRef2(defaultValue || null, function(newValue) {
      return refs.forEach(function(ref) {
        return assignRef(ref, newValue);
      });
    });
    useIsomorphicLayoutEffect3(function() {
      var oldValue = currentValues.get(callbackRef);
      if (oldValue) {
        var prevRefs_1 = new Set(oldValue);
        var nextRefs_1 = new Set(refs);
        var current_1 = callbackRef.current;
        prevRefs_1.forEach(function(ref) {
          if (!nextRefs_1.has(ref)) {
            assignRef(ref, null);
          }
        });
        nextRefs_1.forEach(function(ref) {
          if (!prevRefs_1.has(ref)) {
            assignRef(ref, current_1);
          }
        });
      }
      currentValues.set(callbackRef, refs);
    }, [refs]);
    return callbackRef;
  }

  // node_modules/react-remove-scroll/dist/es2015/medium.js
  init_define_import_meta_env();

  // node_modules/use-sidecar/dist/es2015/index.js
  init_define_import_meta_env();

  // node_modules/use-sidecar/dist/es2015/medium.js
  init_define_import_meta_env();
  function ItoI(a) {
    return a;
  }
  function innerCreateMedium(defaults2, middleware) {
    if (middleware === void 0) {
      middleware = ItoI;
    }
    var buffer = [];
    var assigned = false;
    var medium = {
      read: function() {
        if (assigned) {
          throw new Error("Sidecar: could not `read` from an `assigned` medium. `read` could be used only with `useMedium`.");
        }
        if (buffer.length) {
          return buffer[buffer.length - 1];
        }
        return defaults2;
      },
      useMedium: function(data) {
        var item = middleware(data, assigned);
        buffer.push(item);
        return function() {
          buffer = buffer.filter(function(x) {
            return x !== item;
          });
        };
      },
      assignSyncMedium: function(cb) {
        assigned = true;
        while (buffer.length) {
          var cbs = buffer;
          buffer = [];
          cbs.forEach(cb);
        }
        buffer = {
          push: function(x) {
            return cb(x);
          },
          filter: function() {
            return buffer;
          }
        };
      },
      assignMedium: function(cb) {
        assigned = true;
        var pendingQueue = [];
        if (buffer.length) {
          var cbs = buffer;
          buffer = [];
          cbs.forEach(cb);
          pendingQueue = buffer;
        }
        var executeQueue = function() {
          var cbs2 = pendingQueue;
          pendingQueue = [];
          cbs2.forEach(cb);
        };
        var cycle = function() {
          return Promise.resolve().then(executeQueue);
        };
        cycle();
        buffer = {
          push: function(x) {
            pendingQueue.push(x);
            cycle();
          },
          filter: function(filter) {
            pendingQueue = pendingQueue.filter(filter);
            return buffer;
          }
        };
      }
    };
    return medium;
  }
  function createSidecarMedium(options3) {
    if (options3 === void 0) {
      options3 = {};
    }
    var medium = innerCreateMedium(null);
    medium.options = __assign({ async: true, ssr: false }, options3);
    return medium;
  }

  // node_modules/use-sidecar/dist/es2015/exports.js
  init_define_import_meta_env();
  var React44 = __toESM(require_react_shim());
  var SideCar = function(_a) {
    var sideCar = _a.sideCar, rest = __rest(_a, ["sideCar"]);
    if (!sideCar) {
      throw new Error("Sidecar: please provide `sideCar` property to import the right car");
    }
    var Target = sideCar.read();
    if (!Target) {
      throw new Error("Sidecar medium not found");
    }
    return React44.createElement(Target, __assign({}, rest));
  };
  SideCar.isSideCarExport = true;
  function exportSidecar(medium, exported) {
    medium.useMedium(exported);
    return SideCar;
  }

  // node_modules/react-remove-scroll/dist/es2015/medium.js
  var effectCar = createSidecarMedium();

  // node_modules/react-remove-scroll/dist/es2015/UI.js
  var nothing = function() {
    return;
  };
  var RemoveScroll = React45.forwardRef(function(props, parentRef) {
    var ref = React45.useRef(null);
    var _a = React45.useState({
      onScrollCapture: nothing,
      onWheelCapture: nothing,
      onTouchMoveCapture: nothing
    }), callbacks = _a[0], setCallbacks = _a[1];
    var forwardProps = props.forwardProps, children = props.children, className = props.className, removeScrollBar = props.removeScrollBar, enabled = props.enabled, shards = props.shards, sideCar = props.sideCar, noRelative = props.noRelative, noIsolation = props.noIsolation, inert = props.inert, allowPinchZoom = props.allowPinchZoom, _b = props.as, Container = _b === void 0 ? "div" : _b, gapMode = props.gapMode, rest = __rest(props, ["forwardProps", "children", "className", "removeScrollBar", "enabled", "shards", "sideCar", "noRelative", "noIsolation", "inert", "allowPinchZoom", "as", "gapMode"]);
    var SideCar2 = sideCar;
    var containerRef = useMergeRefs([ref, parentRef]);
    var containerProps = __assign(__assign({}, rest), callbacks);
    return React45.createElement(
      React45.Fragment,
      null,
      enabled && React45.createElement(SideCar2, { sideCar: effectCar, removeScrollBar, shards, noRelative, noIsolation, inert, setCallbacks, allowPinchZoom: !!allowPinchZoom, lockRef: ref, gapMode }),
      forwardProps ? React45.cloneElement(React45.Children.only(children), __assign(__assign({}, containerProps), { ref: containerRef })) : React45.createElement(Container, __assign({}, containerProps, { className, ref: containerRef }), children)
    );
  });
  RemoveScroll.defaultProps = {
    enabled: true,
    removeScrollBar: true,
    inert: false
  };
  RemoveScroll.classNames = {
    fullWidth: fullWidthClassName,
    zeroRight: zeroRightClassName
  };

  // node_modules/react-remove-scroll/dist/es2015/sidecar.js
  init_define_import_meta_env();

  // node_modules/react-remove-scroll/dist/es2015/SideEffect.js
  init_define_import_meta_env();
  var React48 = __toESM(require_react_shim());

  // node_modules/react-remove-scroll-bar/dist/es2015/index.js
  init_define_import_meta_env();

  // node_modules/react-remove-scroll-bar/dist/es2015/component.js
  init_define_import_meta_env();
  var React47 = __toESM(require_react_shim());

  // node_modules/react-style-singleton/dist/es2015/index.js
  init_define_import_meta_env();

  // node_modules/react-style-singleton/dist/es2015/component.js
  init_define_import_meta_env();

  // node_modules/react-style-singleton/dist/es2015/hook.js
  init_define_import_meta_env();
  var React46 = __toESM(require_react_shim());

  // node_modules/react-style-singleton/dist/es2015/singleton.js
  init_define_import_meta_env();

  // node_modules/get-nonce/dist/es2015/index.js
  init_define_import_meta_env();
  var currentNonce;
  var getNonce = function() {
    if (currentNonce) {
      return currentNonce;
    }
    if (typeof __webpack_nonce__ !== "undefined") {
      return __webpack_nonce__;
    }
    return void 0;
  };

  // node_modules/react-style-singleton/dist/es2015/singleton.js
  function makeStyleTag() {
    if (!document)
      return null;
    var tag = document.createElement("style");
    tag.type = "text/css";
    var nonce = getNonce();
    if (nonce) {
      tag.setAttribute("nonce", nonce);
    }
    return tag;
  }
  function injectStyles(tag, css) {
    if (tag.styleSheet) {
      tag.styleSheet.cssText = css;
    } else {
      tag.appendChild(document.createTextNode(css));
    }
  }
  function insertStyleTag(tag) {
    var head = document.head || document.getElementsByTagName("head")[0];
    head.appendChild(tag);
  }
  var stylesheetSingleton = function() {
    var counter = 0;
    var stylesheet = null;
    return {
      add: function(style) {
        if (counter == 0) {
          if (stylesheet = makeStyleTag()) {
            injectStyles(stylesheet, style);
            insertStyleTag(stylesheet);
          }
        }
        counter++;
      },
      remove: function() {
        counter--;
        if (!counter && stylesheet) {
          stylesheet.parentNode && stylesheet.parentNode.removeChild(stylesheet);
          stylesheet = null;
        }
      }
    };
  };

  // node_modules/react-style-singleton/dist/es2015/hook.js
  var styleHookSingleton = function() {
    var sheet = stylesheetSingleton();
    return function(styles, isDynamic) {
      React46.useEffect(function() {
        sheet.add(styles);
        return function() {
          sheet.remove();
        };
      }, [styles && isDynamic]);
    };
  };

  // node_modules/react-style-singleton/dist/es2015/component.js
  var styleSingleton = function() {
    var useStyle = styleHookSingleton();
    var Sheet = function(_a) {
      var styles = _a.styles, dynamic = _a.dynamic;
      useStyle(styles, dynamic);
      return null;
    };
    return Sheet;
  };

  // node_modules/react-remove-scroll-bar/dist/es2015/utils.js
  init_define_import_meta_env();
  var zeroGap = {
    left: 0,
    top: 0,
    right: 0,
    gap: 0
  };
  var parse2 = function(x) {
    return parseInt(x || "", 10) || 0;
  };
  var getOffset = function(gapMode) {
    var cs = window.getComputedStyle(document.body);
    var left = cs[gapMode === "padding" ? "paddingLeft" : "marginLeft"];
    var top = cs[gapMode === "padding" ? "paddingTop" : "marginTop"];
    var right = cs[gapMode === "padding" ? "paddingRight" : "marginRight"];
    return [parse2(left), parse2(top), parse2(right)];
  };
  var getGapWidth = function(gapMode) {
    if (gapMode === void 0) {
      gapMode = "margin";
    }
    if (typeof window === "undefined") {
      return zeroGap;
    }
    var offsets = getOffset(gapMode);
    var documentWidth = document.documentElement.clientWidth;
    var windowWidth = window.innerWidth;
    return {
      left: offsets[0],
      top: offsets[1],
      right: offsets[2],
      gap: Math.max(0, windowWidth - documentWidth + offsets[2] - offsets[0])
    };
  };

  // node_modules/react-remove-scroll-bar/dist/es2015/component.js
  var Style = styleSingleton();
  var lockAttribute = "data-scroll-locked";
  var getStyles = function(_a, allowRelative, gapMode, important) {
    var left = _a.left, top = _a.top, right = _a.right, gap = _a.gap;
    if (gapMode === void 0) {
      gapMode = "margin";
    }
    return "\n  .".concat(noScrollbarsClassName, " {\n   overflow: hidden ").concat(important, ";\n   padding-right: ").concat(gap, "px ").concat(important, ";\n  }\n  body[").concat(lockAttribute, "] {\n    overflow: hidden ").concat(important, ";\n    overscroll-behavior: contain;\n    ").concat([
      allowRelative && "position: relative ".concat(important, ";"),
      gapMode === "margin" && "\n    padding-left: ".concat(left, "px;\n    padding-top: ").concat(top, "px;\n    padding-right: ").concat(right, "px;\n    margin-left:0;\n    margin-top:0;\n    margin-right: ").concat(gap, "px ").concat(important, ";\n    "),
      gapMode === "padding" && "padding-right: ".concat(gap, "px ").concat(important, ";")
    ].filter(Boolean).join(""), "\n  }\n  \n  .").concat(zeroRightClassName, " {\n    right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(fullWidthClassName, " {\n    margin-right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(zeroRightClassName, " .").concat(zeroRightClassName, " {\n    right: 0 ").concat(important, ";\n  }\n  \n  .").concat(fullWidthClassName, " .").concat(fullWidthClassName, " {\n    margin-right: 0 ").concat(important, ";\n  }\n  \n  body[").concat(lockAttribute, "] {\n    ").concat(removedBarSizeVariable, ": ").concat(gap, "px;\n  }\n");
  };
  var getCurrentUseCounter = function() {
    var counter = parseInt(document.body.getAttribute(lockAttribute) || "0", 10);
    return isFinite(counter) ? counter : 0;
  };
  var useLockAttribute = function() {
    React47.useEffect(function() {
      document.body.setAttribute(lockAttribute, (getCurrentUseCounter() + 1).toString());
      return function() {
        var newCounter = getCurrentUseCounter() - 1;
        if (newCounter <= 0) {
          document.body.removeAttribute(lockAttribute);
        } else {
          document.body.setAttribute(lockAttribute, newCounter.toString());
        }
      };
    }, []);
  };
  var RemoveScrollBar = function(_a) {
    var noRelative = _a.noRelative, noImportant = _a.noImportant, _b = _a.gapMode, gapMode = _b === void 0 ? "margin" : _b;
    useLockAttribute();
    var gap = React47.useMemo(function() {
      return getGapWidth(gapMode);
    }, [gapMode]);
    return React47.createElement(Style, { styles: getStyles(gap, !noRelative, gapMode, !noImportant ? "!important" : "") });
  };

  // node_modules/react-remove-scroll/dist/es2015/aggresiveCapture.js
  init_define_import_meta_env();
  var passiveSupported = false;
  if (typeof window !== "undefined") {
    try {
      options2 = Object.defineProperty({}, "passive", {
        get: function() {
          passiveSupported = true;
          return true;
        }
      });
      window.addEventListener("test", options2, options2);
      window.removeEventListener("test", options2, options2);
    } catch (err) {
      passiveSupported = false;
    }
  }
  var options2;
  var nonPassive = passiveSupported ? { passive: false } : false;

  // node_modules/react-remove-scroll/dist/es2015/handleScroll.js
  init_define_import_meta_env();
  var alwaysContainsScroll = function(node) {
    return node.tagName === "TEXTAREA";
  };
  var elementCanBeScrolled = function(node, overflow) {
    if (!(node instanceof Element)) {
      return false;
    }
    var styles = window.getComputedStyle(node);
    return (
      // not-not-scrollable
      styles[overflow] !== "hidden" && // contains scroll inside self
      !(styles.overflowY === styles.overflowX && !alwaysContainsScroll(node) && styles[overflow] === "visible")
    );
  };
  var elementCouldBeVScrolled = function(node) {
    return elementCanBeScrolled(node, "overflowY");
  };
  var elementCouldBeHScrolled = function(node) {
    return elementCanBeScrolled(node, "overflowX");
  };
  var locationCouldBeScrolled = function(axis, node) {
    var ownerDocument = node.ownerDocument;
    var current = node;
    do {
      if (typeof ShadowRoot !== "undefined" && current instanceof ShadowRoot) {
        current = current.host;
      }
      var isScrollable = elementCouldBeScrolled(axis, current);
      if (isScrollable) {
        var _a = getScrollVariables(axis, current), scrollHeight = _a[1], clientHeight = _a[2];
        if (scrollHeight > clientHeight) {
          return true;
        }
      }
      current = current.parentNode;
    } while (current && current !== ownerDocument.body);
    return false;
  };
  var getVScrollVariables = function(_a) {
    var scrollTop = _a.scrollTop, scrollHeight = _a.scrollHeight, clientHeight = _a.clientHeight;
    return [
      scrollTop,
      scrollHeight,
      clientHeight
    ];
  };
  var getHScrollVariables = function(_a) {
    var scrollLeft = _a.scrollLeft, scrollWidth = _a.scrollWidth, clientWidth = _a.clientWidth;
    return [
      scrollLeft,
      scrollWidth,
      clientWidth
    ];
  };
  var elementCouldBeScrolled = function(axis, node) {
    return axis === "v" ? elementCouldBeVScrolled(node) : elementCouldBeHScrolled(node);
  };
  var getScrollVariables = function(axis, node) {
    return axis === "v" ? getVScrollVariables(node) : getHScrollVariables(node);
  };
  var getDirectionFactor = function(axis, direction) {
    return axis === "h" && direction === "rtl" ? -1 : 1;
  };
  var handleScroll = function(axis, endTarget, event, sourceDelta, noOverscroll) {
    var directionFactor = getDirectionFactor(axis, window.getComputedStyle(endTarget).direction);
    var delta = directionFactor * sourceDelta;
    var target = event.target;
    var targetInLock = endTarget.contains(target);
    var shouldCancelScroll = false;
    var isDeltaPositive = delta > 0;
    var availableScroll = 0;
    var availableScrollTop = 0;
    do {
      if (!target) {
        break;
      }
      var _a = getScrollVariables(axis, target), position = _a[0], scroll_1 = _a[1], capacity = _a[2];
      var elementScroll = scroll_1 - capacity - directionFactor * position;
      if (position || elementScroll) {
        if (elementCouldBeScrolled(axis, target)) {
          availableScroll += elementScroll;
          availableScrollTop += position;
        }
      }
      var parent_1 = target.parentNode;
      target = parent_1 && parent_1.nodeType === Node.DOCUMENT_FRAGMENT_NODE ? parent_1.host : parent_1;
    } while (
      // portaled content
      !targetInLock && target !== document.body || // self content
      targetInLock && (endTarget.contains(target) || endTarget === target)
    );
    if (isDeltaPositive && (noOverscroll && Math.abs(availableScroll) < 1 || !noOverscroll && delta > availableScroll)) {
      shouldCancelScroll = true;
    } else if (!isDeltaPositive && (noOverscroll && Math.abs(availableScrollTop) < 1 || !noOverscroll && -delta > availableScrollTop)) {
      shouldCancelScroll = true;
    }
    return shouldCancelScroll;
  };

  // node_modules/react-remove-scroll/dist/es2015/SideEffect.js
  var getTouchXY = function(event) {
    return "changedTouches" in event ? [event.changedTouches[0].clientX, event.changedTouches[0].clientY] : [0, 0];
  };
  var getDeltaXY = function(event) {
    return [event.deltaX, event.deltaY];
  };
  var extractRef = function(ref) {
    return ref && "current" in ref ? ref.current : ref;
  };
  var deltaCompare = function(x, y) {
    return x[0] === y[0] && x[1] === y[1];
  };
  var generateStyle = function(id) {
    return "\n  .block-interactivity-".concat(id, " {pointer-events: none;}\n  .allow-interactivity-").concat(id, " {pointer-events: all;}\n");
  };
  var idCounter = 0;
  var lockStack = [];
  function RemoveScrollSideCar(props) {
    var shouldPreventQueue = React48.useRef([]);
    var touchStartRef = React48.useRef([0, 0]);
    var activeAxis = React48.useRef();
    var id = React48.useState(idCounter++)[0];
    var Style2 = React48.useState(styleSingleton)[0];
    var lastProps = React48.useRef(props);
    React48.useEffect(function() {
      lastProps.current = props;
    }, [props]);
    React48.useEffect(function() {
      if (props.inert) {
        document.body.classList.add("block-interactivity-".concat(id));
        var allow_1 = __spreadArray([props.lockRef.current], (props.shards || []).map(extractRef), true).filter(Boolean);
        allow_1.forEach(function(el) {
          return el.classList.add("allow-interactivity-".concat(id));
        });
        return function() {
          document.body.classList.remove("block-interactivity-".concat(id));
          allow_1.forEach(function(el) {
            return el.classList.remove("allow-interactivity-".concat(id));
          });
        };
      }
      return;
    }, [props.inert, props.lockRef.current, props.shards]);
    var shouldCancelEvent = React48.useCallback(function(event, parent) {
      if ("touches" in event && event.touches.length === 2 || event.type === "wheel" && event.ctrlKey) {
        return !lastProps.current.allowPinchZoom;
      }
      var touch = getTouchXY(event);
      var touchStart = touchStartRef.current;
      var deltaX = "deltaX" in event ? event.deltaX : touchStart[0] - touch[0];
      var deltaY = "deltaY" in event ? event.deltaY : touchStart[1] - touch[1];
      var currentAxis;
      var target = event.target;
      var moveDirection = Math.abs(deltaX) > Math.abs(deltaY) ? "h" : "v";
      if ("touches" in event && moveDirection === "h" && target.type === "range") {
        return false;
      }
      var selection = window.getSelection();
      var anchorNode = selection && selection.anchorNode;
      var isTouchingSelection = anchorNode ? anchorNode === target || anchorNode.contains(target) : false;
      if (isTouchingSelection) {
        return false;
      }
      var canBeScrolledInMainDirection = locationCouldBeScrolled(moveDirection, target);
      if (!canBeScrolledInMainDirection) {
        return true;
      }
      if (canBeScrolledInMainDirection) {
        currentAxis = moveDirection;
      } else {
        currentAxis = moveDirection === "v" ? "h" : "v";
        canBeScrolledInMainDirection = locationCouldBeScrolled(moveDirection, target);
      }
      if (!canBeScrolledInMainDirection) {
        return false;
      }
      if (!activeAxis.current && "changedTouches" in event && (deltaX || deltaY)) {
        activeAxis.current = currentAxis;
      }
      if (!currentAxis) {
        return true;
      }
      var cancelingAxis = activeAxis.current || currentAxis;
      return handleScroll(cancelingAxis, parent, event, cancelingAxis === "h" ? deltaX : deltaY, true);
    }, []);
    var shouldPrevent = React48.useCallback(function(_event) {
      var event = _event;
      if (!lockStack.length || lockStack[lockStack.length - 1] !== Style2) {
        return;
      }
      var delta = "deltaY" in event ? getDeltaXY(event) : getTouchXY(event);
      var sourceEvent = shouldPreventQueue.current.filter(function(e2) {
        return e2.name === event.type && (e2.target === event.target || event.target === e2.shadowParent) && deltaCompare(e2.delta, delta);
      })[0];
      if (sourceEvent && sourceEvent.should) {
        if (event.cancelable) {
          event.preventDefault();
        }
        return;
      }
      if (!sourceEvent) {
        var shardNodes = (lastProps.current.shards || []).map(extractRef).filter(Boolean).filter(function(node) {
          return node.contains(event.target);
        });
        var shouldStop = shardNodes.length > 0 ? shouldCancelEvent(event, shardNodes[0]) : !lastProps.current.noIsolation;
        if (shouldStop) {
          if (event.cancelable) {
            event.preventDefault();
          }
        }
      }
    }, []);
    var shouldCancel = React48.useCallback(function(name, delta, target, should) {
      var event = { name, delta, target, should, shadowParent: getOutermostShadowParent(target) };
      shouldPreventQueue.current.push(event);
      setTimeout(function() {
        shouldPreventQueue.current = shouldPreventQueue.current.filter(function(e2) {
          return e2 !== event;
        });
      }, 1);
    }, []);
    var scrollTouchStart = React48.useCallback(function(event) {
      touchStartRef.current = getTouchXY(event);
      activeAxis.current = void 0;
    }, []);
    var scrollWheel = React48.useCallback(function(event) {
      shouldCancel(event.type, getDeltaXY(event), event.target, shouldCancelEvent(event, props.lockRef.current));
    }, []);
    var scrollTouchMove = React48.useCallback(function(event) {
      shouldCancel(event.type, getTouchXY(event), event.target, shouldCancelEvent(event, props.lockRef.current));
    }, []);
    React48.useEffect(function() {
      lockStack.push(Style2);
      props.setCallbacks({
        onScrollCapture: scrollWheel,
        onWheelCapture: scrollWheel,
        onTouchMoveCapture: scrollTouchMove
      });
      document.addEventListener("wheel", shouldPrevent, nonPassive);
      document.addEventListener("touchmove", shouldPrevent, nonPassive);
      document.addEventListener("touchstart", scrollTouchStart, nonPassive);
      return function() {
        lockStack = lockStack.filter(function(inst) {
          return inst !== Style2;
        });
        document.removeEventListener("wheel", shouldPrevent, nonPassive);
        document.removeEventListener("touchmove", shouldPrevent, nonPassive);
        document.removeEventListener("touchstart", scrollTouchStart, nonPassive);
      };
    }, []);
    var removeScrollBar = props.removeScrollBar, inert = props.inert;
    return React48.createElement(
      React48.Fragment,
      null,
      inert ? React48.createElement(Style2, { styles: generateStyle(id) }) : null,
      removeScrollBar ? React48.createElement(RemoveScrollBar, { noRelative: props.noRelative, gapMode: props.gapMode }) : null
    );
  }
  function getOutermostShadowParent(node) {
    var shadowParent = null;
    while (node !== null) {
      if (node instanceof ShadowRoot) {
        shadowParent = node.host;
        node = node.host;
      }
      node = node.parentNode;
    }
    return shadowParent;
  }

  // node_modules/react-remove-scroll/dist/es2015/sidecar.js
  var sidecar_default = exportSidecar(effectCar, RemoveScrollSideCar);

  // node_modules/react-remove-scroll/dist/es2015/Combination.js
  var ReactRemoveScroll = React49.forwardRef(function(props, ref) {
    return React49.createElement(RemoveScroll, __assign({}, props, { ref, sideCar: sidecar_default }));
  });
  ReactRemoveScroll.classNames = RemoveScroll.classNames;
  var Combination_default = ReactRemoveScroll;

  // node_modules/@radix-ui/react-popover/dist/index.mjs
  var import_jsx_runtime22 = __toESM(require_react_shim(), 1);
  var POPOVER_NAME = "Popover";
  var [createPopoverContext, createPopoverScope] = createContextScope(POPOVER_NAME, [
    createPopperScope
  ]);
  var usePopperScope = createPopperScope();
  var [PopoverProvider, usePopoverContext] = createPopoverContext(POPOVER_NAME);
  var Popover = (props) => {
    const {
      __scopePopover,
      children,
      open: openProp,
      defaultOpen,
      onOpenChange,
      modal = false
    } = props;
    const popperScope = usePopperScope(__scopePopover);
    const triggerRef = React50.useRef(null);
    const [hasCustomAnchor, setHasCustomAnchor] = React50.useState(false);
    const [open, setOpen] = useControllableState({
      prop: openProp,
      defaultProp: defaultOpen ?? false,
      onChange: onOpenChange,
      caller: POPOVER_NAME
    });
    return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Root23, { ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
      PopoverProvider,
      {
        scope: __scopePopover,
        contentId: useId5(),
        triggerRef,
        open,
        onOpenChange: setOpen,
        onOpenToggle: React50.useCallback(() => setOpen((prevOpen) => !prevOpen), [setOpen]),
        hasCustomAnchor,
        onCustomAnchorAdd: React50.useCallback(() => setHasCustomAnchor(true), []),
        onCustomAnchorRemove: React50.useCallback(() => setHasCustomAnchor(false), []),
        modal,
        children
      }
    ) });
  };
  Popover.displayName = POPOVER_NAME;
  var ANCHOR_NAME2 = "PopoverAnchor";
  var PopoverAnchor = React50.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopover, ...anchorProps } = props;
      const context2 = usePopoverContext(ANCHOR_NAME2, __scopePopover);
      const popperScope = usePopperScope(__scopePopover);
      const { onCustomAnchorAdd, onCustomAnchorRemove } = context2;
      React50.useEffect(() => {
        onCustomAnchorAdd();
        return () => onCustomAnchorRemove();
      }, [onCustomAnchorAdd, onCustomAnchorRemove]);
      return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Anchor, { ...popperScope, ...anchorProps, ref: forwardedRef });
    }
  );
  PopoverAnchor.displayName = ANCHOR_NAME2;
  var TRIGGER_NAME = "PopoverTrigger";
  var PopoverTrigger = React50.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopover, ...triggerProps } = props;
      const context2 = usePopoverContext(TRIGGER_NAME, __scopePopover);
      const popperScope = usePopperScope(__scopePopover);
      const composedTriggerRef = useComposedRefs(forwardedRef, context2.triggerRef);
      const trigger = /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        Primitive2.button,
        {
          type: "button",
          "aria-haspopup": "dialog",
          "aria-expanded": context2.open,
          "aria-controls": context2.contentId,
          "data-state": getState(context2.open),
          ...triggerProps,
          ref: composedTriggerRef,
          onClick: composeEventHandlers(props.onClick, context2.onOpenToggle)
        }
      );
      return context2.hasCustomAnchor ? trigger : /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Anchor, { asChild: true, ...popperScope, children: trigger });
    }
  );
  PopoverTrigger.displayName = TRIGGER_NAME;
  var PORTAL_NAME2 = "PopoverPortal";
  var [PortalProvider, usePortalContext] = createPopoverContext(PORTAL_NAME2, {
    forceMount: void 0
  });
  var PopoverPortal = (props) => {
    const { __scopePopover, forceMount, children, container } = props;
    const context2 = usePopoverContext(PORTAL_NAME2, __scopePopover);
    return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(PortalProvider, { scope: __scopePopover, forceMount, children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Presence, { present: forceMount || context2.open, children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Portal, { asChild: true, container, children }) }) });
  };
  PopoverPortal.displayName = PORTAL_NAME2;
  var CONTENT_NAME2 = "PopoverContent";
  var PopoverContent = React50.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext(CONTENT_NAME2, props.__scopePopover);
      const { forceMount = portalContext.forceMount, ...contentProps } = props;
      const context2 = usePopoverContext(CONTENT_NAME2, props.__scopePopover);
      return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Presence, { present: forceMount || context2.open, children: context2.modal ? /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(PopoverContentModal, { ...contentProps, ref: forwardedRef }) : /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(PopoverContentNonModal, { ...contentProps, ref: forwardedRef }) });
    }
  );
  PopoverContent.displayName = CONTENT_NAME2;
  var Slot2 = createSlot4("PopoverContent.RemoveScroll");
  var PopoverContentModal = React50.forwardRef(
    (props, forwardedRef) => {
      const context2 = usePopoverContext(CONTENT_NAME2, props.__scopePopover);
      const contentRef = React50.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, contentRef);
      const isRightClickOutsideRef = React50.useRef(false);
      React50.useEffect(() => {
        const content = contentRef.current;
        if (content) return hideOthers(content);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Combination_default, { as: Slot2, allowPinchZoom: true, children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        PopoverContentImpl,
        {
          ...props,
          ref: composedRefs,
          trapFocus: context2.open,
          disableOutsidePointerEvents: true,
          onCloseAutoFocus: composeEventHandlers(props.onCloseAutoFocus, (event) => {
            event.preventDefault();
            if (!isRightClickOutsideRef.current) context2.triggerRef.current?.focus();
          }),
          onPointerDownOutside: composeEventHandlers(
            props.onPointerDownOutside,
            (event) => {
              const originalEvent = event.detail.originalEvent;
              const ctrlLeftClick = originalEvent.button === 0 && originalEvent.ctrlKey === true;
              const isRightClick = originalEvent.button === 2 || ctrlLeftClick;
              isRightClickOutsideRef.current = isRightClick;
            },
            { checkForDefaultPrevented: false }
          ),
          onFocusOutside: composeEventHandlers(
            props.onFocusOutside,
            (event) => event.preventDefault(),
            { checkForDefaultPrevented: false }
          )
        }
      ) });
    }
  );
  var PopoverContentNonModal = React50.forwardRef(
    (props, forwardedRef) => {
      const context2 = usePopoverContext(CONTENT_NAME2, props.__scopePopover);
      const hasInteractedOutsideRef = React50.useRef(false);
      const hasPointerDownOutsideRef = React50.useRef(false);
      return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        PopoverContentImpl,
        {
          ...props,
          ref: forwardedRef,
          trapFocus: false,
          disableOutsidePointerEvents: false,
          onCloseAutoFocus: (event) => {
            props.onCloseAutoFocus?.(event);
            if (!event.defaultPrevented) {
              if (!hasInteractedOutsideRef.current) context2.triggerRef.current?.focus();
              event.preventDefault();
            }
            hasInteractedOutsideRef.current = false;
            hasPointerDownOutsideRef.current = false;
          },
          onInteractOutside: (event) => {
            props.onInteractOutside?.(event);
            if (!event.defaultPrevented) {
              hasInteractedOutsideRef.current = true;
              if (event.detail.originalEvent.type === "pointerdown") {
                hasPointerDownOutsideRef.current = true;
              }
            }
            const target = event.target;
            const targetIsTrigger = context2.triggerRef.current?.contains(target);
            if (targetIsTrigger) event.preventDefault();
            if (event.detail.originalEvent.type === "focusin" && hasPointerDownOutsideRef.current) {
              event.preventDefault();
            }
          }
        }
      );
    }
  );
  var PopoverContentImpl = React50.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopePopover,
        trapFocus,
        onOpenAutoFocus,
        onCloseAutoFocus,
        disableOutsidePointerEvents,
        onEscapeKeyDown,
        onPointerDownOutside,
        onFocusOutside,
        onInteractOutside,
        ...contentProps
      } = props;
      const context2 = usePopoverContext(CONTENT_NAME2, __scopePopover);
      const popperScope = usePopperScope(__scopePopover);
      useFocusGuards();
      return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        FocusScope,
        {
          asChild: true,
          loop: true,
          trapped: trapFocus,
          onMountAutoFocus: onOpenAutoFocus,
          onUnmountAutoFocus: onCloseAutoFocus,
          children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
            DismissableLayer,
            {
              asChild: true,
              disableOutsidePointerEvents,
              onInteractOutside,
              onEscapeKeyDown,
              onPointerDownOutside,
              onFocusOutside,
              onDismiss: () => context2.onOpenChange(false),
              children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
                Content4,
                {
                  "data-state": getState(context2.open),
                  role: "dialog",
                  id: context2.contentId,
                  ...popperScope,
                  ...contentProps,
                  ref: forwardedRef,
                  style: {
                    ...contentProps.style,
                    // re-namespace exposed content custom properties
                    ...{
                      "--radix-popover-content-transform-origin": "var(--radix-popper-transform-origin)",
                      "--radix-popover-content-available-width": "var(--radix-popper-available-width)",
                      "--radix-popover-content-available-height": "var(--radix-popper-available-height)",
                      "--radix-popover-trigger-width": "var(--radix-popper-anchor-width)",
                      "--radix-popover-trigger-height": "var(--radix-popper-anchor-height)"
                    }
                  }
                }
              )
            }
          )
        }
      );
    }
  );
  var CLOSE_NAME2 = "PopoverClose";
  var PopoverClose = React50.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopover, ...closeProps } = props;
      const context2 = usePopoverContext(CLOSE_NAME2, __scopePopover);
      return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        Primitive2.button,
        {
          type: "button",
          ...closeProps,
          ref: forwardedRef,
          onClick: composeEventHandlers(props.onClick, () => context2.onOpenChange(false))
        }
      );
    }
  );
  PopoverClose.displayName = CLOSE_NAME2;
  var ARROW_NAME2 = "PopoverArrow";
  var PopoverArrow = React50.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopover, ...arrowProps } = props;
      const popperScope = usePopperScope(__scopePopover);
      return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Arrow2, { ...popperScope, ...arrowProps, ref: forwardedRef });
    }
  );
  PopoverArrow.displayName = ARROW_NAME2;
  function getState(open) {
    return open ? "open" : "closed";
  }
  var Root24 = Popover;
  var Trigger5 = PopoverTrigger;
  var Portal2 = PopoverPortal;
  var Content22 = PopoverContent;

  // src/components/ui/popover/Popover.tsx
  var PopoverTrigger2 = (props) => /* @__PURE__ */ React.createElement(Trigger5, { "data-slot": "popover-trigger", ...props });
  var PopoverContent2 = ({ className, align = "center", sideOffset = 4, ...props }) => /* @__PURE__ */ React.createElement(Portal2, null, /* @__PURE__ */ React.createElement(
    Content22,
    {
      "data-slot": "popover-content",
      align,
      sideOffset,
      className: cn(
        "border-muted-200 bg-popover text-popover-foreground z-50 rounded-md border p-4 shadow-md outline-none",
        "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0",
        "data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2",
        "data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2",
        className
      ),
      ...props
    }
  ));
  var Popover2 = (props) => /* @__PURE__ */ React.createElement(Root24, { "data-slot": "popover", ...props });
  Popover2.Trigger = (0, import_react57.forwardRef)((props, ref) => PopoverTrigger2({ ...props, ref }));
  Popover2.Content = PopoverContent2;
  PopoverContent2.displayName = Content22.displayName;
  var Popover_default = Popover2;

  // src/components/ui/dropdown-menu/index.ts
  init_define_import_meta_env();

  // src/components/ui/dropdown-menu/DropdownMenu.tsx
  init_define_import_meta_env();

  // node_modules/@radix-ui/react-dropdown-menu/dist/index.mjs
  init_define_import_meta_env();
  var React54 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-menu/dist/index.mjs
  init_define_import_meta_env();
  var React53 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-roving-focus/dist/index.mjs
  init_define_import_meta_env();
  var React51 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime23 = __toESM(require_react_shim(), 1);
  var ENTRY_FOCUS = "rovingFocusGroup.onEntryFocus";
  var EVENT_OPTIONS2 = { bubbles: false, cancelable: true };
  var GROUP_NAME = "RovingFocusGroup";
  var [Collection2, useCollection2, createCollectionScope2] = createCollection(GROUP_NAME);
  var [createRovingFocusGroupContext, createRovingFocusGroupScope] = createContextScope(
    GROUP_NAME,
    [createCollectionScope2]
  );
  var [RovingFocusProvider, useRovingFocusContext] = createRovingFocusGroupContext(GROUP_NAME);
  var RovingFocusGroup = React51.forwardRef(
    (props, forwardedRef) => {
      return /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(Collection2.Provider, { scope: props.__scopeRovingFocusGroup, children: /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(Collection2.Slot, { scope: props.__scopeRovingFocusGroup, children: /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(RovingFocusGroupImpl, { ...props, ref: forwardedRef }) }) });
    }
  );
  RovingFocusGroup.displayName = GROUP_NAME;
  var RovingFocusGroupImpl = React51.forwardRef((props, forwardedRef) => {
    const {
      __scopeRovingFocusGroup,
      orientation,
      loop = false,
      dir: dir2,
      currentTabStopId: currentTabStopIdProp,
      defaultCurrentTabStopId,
      onCurrentTabStopIdChange,
      onEntryFocus,
      preventScrollOnEntryFocus = false,
      ...groupProps
    } = props;
    const ref = React51.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    const direction = useDirection(dir2);
    const [currentTabStopId, setCurrentTabStopId] = useControllableState({
      prop: currentTabStopIdProp,
      defaultProp: defaultCurrentTabStopId ?? null,
      onChange: onCurrentTabStopIdChange,
      caller: GROUP_NAME
    });
    const [isTabbingBackOut, setIsTabbingBackOut] = React51.useState(false);
    const handleEntryFocus = useCallbackRef(onEntryFocus);
    const getItems = useCollection2(__scopeRovingFocusGroup);
    const isClickFocusRef = React51.useRef(false);
    const [focusableItemsCount, setFocusableItemsCount] = React51.useState(0);
    React51.useEffect(() => {
      const node = ref.current;
      if (node) {
        node.addEventListener(ENTRY_FOCUS, handleEntryFocus);
        return () => node.removeEventListener(ENTRY_FOCUS, handleEntryFocus);
      }
    }, [handleEntryFocus]);
    return /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
      RovingFocusProvider,
      {
        scope: __scopeRovingFocusGroup,
        orientation,
        dir: direction,
        loop,
        currentTabStopId,
        onItemFocus: React51.useCallback(
          (tabStopId) => setCurrentTabStopId(tabStopId),
          [setCurrentTabStopId]
        ),
        onItemShiftTab: React51.useCallback(() => setIsTabbingBackOut(true), []),
        onFocusableItemAdd: React51.useCallback(
          () => setFocusableItemsCount((prevCount) => prevCount + 1),
          []
        ),
        onFocusableItemRemove: React51.useCallback(
          () => setFocusableItemsCount((prevCount) => prevCount - 1),
          []
        ),
        children: /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
          Primitive2.div,
          {
            tabIndex: isTabbingBackOut || focusableItemsCount === 0 ? -1 : 0,
            "data-orientation": orientation,
            ...groupProps,
            ref: composedRefs,
            style: { outline: "none", ...props.style },
            onMouseDown: composeEventHandlers(props.onMouseDown, () => {
              isClickFocusRef.current = true;
            }),
            onFocus: composeEventHandlers(props.onFocus, (event) => {
              const isKeyboardFocus = !isClickFocusRef.current;
              if (event.target === event.currentTarget && isKeyboardFocus && !isTabbingBackOut) {
                const entryFocusEvent = new CustomEvent(ENTRY_FOCUS, EVENT_OPTIONS2);
                event.currentTarget.dispatchEvent(entryFocusEvent);
                if (!entryFocusEvent.defaultPrevented) {
                  const items = getItems().filter((item) => item.focusable);
                  const activeItem = items.find((item) => item.active);
                  const currentItem = items.find((item) => item.id === currentTabStopId);
                  const candidateItems = [activeItem, currentItem, ...items].filter(
                    Boolean
                  );
                  const candidateNodes = candidateItems.map((item) => item.ref.current);
                  focusFirst3(candidateNodes, preventScrollOnEntryFocus);
                }
              }
              isClickFocusRef.current = false;
            }),
            onBlur: composeEventHandlers(props.onBlur, () => setIsTabbingBackOut(false))
          }
        )
      }
    );
  });
  var ITEM_NAME = "RovingFocusGroupItem";
  var RovingFocusGroupItem = React51.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeRovingFocusGroup,
        focusable = true,
        active = false,
        tabStopId,
        children,
        ...itemProps
      } = props;
      const autoId = useId5();
      const id = tabStopId || autoId;
      const context2 = useRovingFocusContext(ITEM_NAME, __scopeRovingFocusGroup);
      const isCurrentTabStop = context2.currentTabStopId === id;
      const getItems = useCollection2(__scopeRovingFocusGroup);
      const { onFocusableItemAdd, onFocusableItemRemove, currentTabStopId } = context2;
      React51.useEffect(() => {
        if (focusable) {
          onFocusableItemAdd();
          return () => onFocusableItemRemove();
        }
      }, [focusable, onFocusableItemAdd, onFocusableItemRemove]);
      return /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
        Collection2.ItemSlot,
        {
          scope: __scopeRovingFocusGroup,
          id,
          focusable,
          active,
          children: /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
            Primitive2.span,
            {
              tabIndex: isCurrentTabStop ? 0 : -1,
              "data-orientation": context2.orientation,
              ...itemProps,
              ref: forwardedRef,
              onMouseDown: composeEventHandlers(props.onMouseDown, (event) => {
                if (!focusable) event.preventDefault();
                else context2.onItemFocus(id);
              }),
              onFocus: composeEventHandlers(props.onFocus, () => context2.onItemFocus(id)),
              onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
                if (event.key === "Tab" && event.shiftKey) {
                  context2.onItemShiftTab();
                  return;
                }
                if (event.target !== event.currentTarget) return;
                const focusIntent = getFocusIntent(event, context2.orientation, context2.dir);
                if (focusIntent !== void 0) {
                  if (event.metaKey || event.ctrlKey || event.altKey || event.shiftKey) return;
                  event.preventDefault();
                  const items = getItems().filter((item) => item.focusable);
                  let candidateNodes = items.map((item) => item.ref.current);
                  if (focusIntent === "last") candidateNodes.reverse();
                  else if (focusIntent === "prev" || focusIntent === "next") {
                    if (focusIntent === "prev") candidateNodes.reverse();
                    const currentIndex = candidateNodes.indexOf(event.currentTarget);
                    candidateNodes = context2.loop ? wrapArray(candidateNodes, currentIndex + 1) : candidateNodes.slice(currentIndex + 1);
                  }
                  setTimeout(() => focusFirst3(candidateNodes));
                }
              }),
              children: typeof children === "function" ? children({ isCurrentTabStop, hasTabStop: currentTabStopId != null }) : children
            }
          )
        }
      );
    }
  );
  RovingFocusGroupItem.displayName = ITEM_NAME;
  var MAP_KEY_TO_FOCUS_INTENT = {
    ArrowLeft: "prev",
    ArrowUp: "prev",
    ArrowRight: "next",
    ArrowDown: "next",
    PageUp: "first",
    Home: "first",
    PageDown: "last",
    End: "last"
  };
  function getDirectionAwareKey(key, dir2) {
    if (dir2 !== "rtl") return key;
    return key === "ArrowLeft" ? "ArrowRight" : key === "ArrowRight" ? "ArrowLeft" : key;
  }
  function getFocusIntent(event, orientation, dir2) {
    const key = getDirectionAwareKey(event.key, dir2);
    if (orientation === "vertical" && ["ArrowLeft", "ArrowRight"].includes(key)) return void 0;
    if (orientation === "horizontal" && ["ArrowUp", "ArrowDown"].includes(key)) return void 0;
    return MAP_KEY_TO_FOCUS_INTENT[key];
  }
  function focusFirst3(candidates, preventScroll = false) {
    const PREVIOUSLY_FOCUSED_ELEMENT = document.activeElement;
    for (const candidate of candidates) {
      if (candidate === PREVIOUSLY_FOCUSED_ELEMENT) return;
      candidate.focus({ preventScroll });
      if (document.activeElement !== PREVIOUSLY_FOCUSED_ELEMENT) return;
    }
  }
  function wrapArray(array, startIndex) {
    return array.map((_, index2) => array[(startIndex + index2) % array.length]);
  }
  var Root6 = RovingFocusGroup;
  var Item5 = RovingFocusGroupItem;

  // node_modules/@radix-ui/react-menu/node_modules/@radix-ui/react-slot/dist/index.mjs
  init_define_import_meta_env();
  var React52 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime24 = __toESM(require_react_shim(), 1);
  // @__NO_SIDE_EFFECTS__
  function createSlot5(ownerName) {
    const SlotClone = /* @__PURE__ */ createSlotClone5(ownerName);
    const Slot22 = React52.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      const childrenArray = React52.Children.toArray(children);
      const slottable = childrenArray.find(isSlottable5);
      if (slottable) {
        const newElement = slottable.props.children;
        const newChildren = childrenArray.map((child) => {
          if (child === slottable) {
            if (React52.Children.count(newElement) > 1) return React52.Children.only(null);
            return React52.isValidElement(newElement) ? newElement.props.children : null;
          } else {
            return child;
          }
        });
        return /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children: React52.isValidElement(newElement) ? React52.cloneElement(newElement, void 0, newChildren) : null });
      }
      return /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children });
    });
    Slot22.displayName = `${ownerName}.Slot`;
    return Slot22;
  }
  // @__NO_SIDE_EFFECTS__
  function createSlotClone5(ownerName) {
    const SlotClone = React52.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      if (React52.isValidElement(children)) {
        const childrenRef = getElementRef6(children);
        const props2 = mergeProps5(slotProps, children.props);
        if (children.type !== React52.Fragment) {
          props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
        }
        return React52.cloneElement(children, props2);
      }
      return React52.Children.count(children) > 1 ? React52.Children.only(null) : null;
    });
    SlotClone.displayName = `${ownerName}.SlotClone`;
    return SlotClone;
  }
  var SLOTTABLE_IDENTIFIER5 = /* @__PURE__ */ Symbol("radix.slottable");
  function isSlottable5(child) {
    return React52.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER5;
  }
  function mergeProps5(slotProps, childProps) {
    const overrideProps = { ...childProps };
    for (const propName in childProps) {
      const slotPropValue = slotProps[propName];
      const childPropValue = childProps[propName];
      const isHandler = /^on[A-Z]/.test(propName);
      if (isHandler) {
        if (slotPropValue && childPropValue) {
          overrideProps[propName] = (...args) => {
            const result = childPropValue(...args);
            slotPropValue(...args);
            return result;
          };
        } else if (slotPropValue) {
          overrideProps[propName] = slotPropValue;
        }
      } else if (propName === "style") {
        overrideProps[propName] = { ...slotPropValue, ...childPropValue };
      } else if (propName === "className") {
        overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
      }
    }
    return { ...slotProps, ...overrideProps };
  }
  function getElementRef6(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }

  // node_modules/@radix-ui/react-menu/dist/index.mjs
  var import_jsx_runtime25 = __toESM(require_react_shim(), 1);
  var SELECTION_KEYS = ["Enter", " "];
  var FIRST_KEYS = ["ArrowDown", "PageUp", "Home"];
  var LAST_KEYS = ["ArrowUp", "PageDown", "End"];
  var FIRST_LAST_KEYS = [...FIRST_KEYS, ...LAST_KEYS];
  var SUB_OPEN_KEYS = {
    ltr: [...SELECTION_KEYS, "ArrowRight"],
    rtl: [...SELECTION_KEYS, "ArrowLeft"]
  };
  var SUB_CLOSE_KEYS = {
    ltr: ["ArrowLeft"],
    rtl: ["ArrowRight"]
  };
  var MENU_NAME = "Menu";
  var [Collection3, useCollection3, createCollectionScope3] = createCollection(MENU_NAME);
  var [createMenuContext, createMenuScope] = createContextScope(MENU_NAME, [
    createCollectionScope3,
    createPopperScope,
    createRovingFocusGroupScope
  ]);
  var usePopperScope2 = createPopperScope();
  var useRovingFocusGroupScope = createRovingFocusGroupScope();
  var [MenuProvider, useMenuContext] = createMenuContext(MENU_NAME);
  var [MenuRootProvider, useMenuRootContext] = createMenuContext(MENU_NAME);
  var Menu = (props) => {
    const { __scopeMenu, open = false, children, dir: dir2, onOpenChange, modal = true } = props;
    const popperScope = usePopperScope2(__scopeMenu);
    const [content, setContent] = React53.useState(null);
    const isUsingKeyboardRef = React53.useRef(false);
    const handleOpenChange = useCallbackRef(onOpenChange);
    const direction = useDirection(dir2);
    React53.useEffect(() => {
      const handleKeyDown = () => {
        isUsingKeyboardRef.current = true;
        document.addEventListener("pointerdown", handlePointer, { capture: true, once: true });
        document.addEventListener("pointermove", handlePointer, { capture: true, once: true });
      };
      const handlePointer = () => isUsingKeyboardRef.current = false;
      document.addEventListener("keydown", handleKeyDown, { capture: true });
      return () => {
        document.removeEventListener("keydown", handleKeyDown, { capture: true });
        document.removeEventListener("pointerdown", handlePointer, { capture: true });
        document.removeEventListener("pointermove", handlePointer, { capture: true });
      };
    }, []);
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Root23, { ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
      MenuProvider,
      {
        scope: __scopeMenu,
        open,
        onOpenChange: handleOpenChange,
        content,
        onContentChange: setContent,
        children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
          MenuRootProvider,
          {
            scope: __scopeMenu,
            onClose: React53.useCallback(() => handleOpenChange(false), [handleOpenChange]),
            isUsingKeyboardRef,
            dir: direction,
            modal,
            children
          }
        )
      }
    ) });
  };
  Menu.displayName = MENU_NAME;
  var ANCHOR_NAME3 = "MenuAnchor";
  var MenuAnchor = React53.forwardRef(
    (props, forwardedRef) => {
      const { __scopeMenu, ...anchorProps } = props;
      const popperScope = usePopperScope2(__scopeMenu);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Anchor, { ...popperScope, ...anchorProps, ref: forwardedRef });
    }
  );
  MenuAnchor.displayName = ANCHOR_NAME3;
  var PORTAL_NAME3 = "MenuPortal";
  var [PortalProvider2, usePortalContext2] = createMenuContext(PORTAL_NAME3, {
    forceMount: void 0
  });
  var MenuPortal = (props) => {
    const { __scopeMenu, forceMount, children, container } = props;
    const context2 = useMenuContext(PORTAL_NAME3, __scopeMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(PortalProvider2, { scope: __scopeMenu, forceMount, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Presence, { present: forceMount || context2.open, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Portal, { asChild: true, container, children }) }) });
  };
  MenuPortal.displayName = PORTAL_NAME3;
  var CONTENT_NAME3 = "MenuContent";
  var [MenuContentProvider, useMenuContentContext] = createMenuContext(CONTENT_NAME3);
  var MenuContent = React53.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext2(CONTENT_NAME3, props.__scopeMenu);
      const { forceMount = portalContext.forceMount, ...contentProps } = props;
      const context2 = useMenuContext(CONTENT_NAME3, props.__scopeMenu);
      const rootContext = useMenuRootContext(CONTENT_NAME3, props.__scopeMenu);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Collection3.Provider, { scope: props.__scopeMenu, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Presence, { present: forceMount || context2.open, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Collection3.Slot, { scope: props.__scopeMenu, children: rootContext.modal ? /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(MenuRootContentModal, { ...contentProps, ref: forwardedRef }) : /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(MenuRootContentNonModal, { ...contentProps, ref: forwardedRef }) }) }) });
    }
  );
  var MenuRootContentModal = React53.forwardRef(
    (props, forwardedRef) => {
      const context2 = useMenuContext(CONTENT_NAME3, props.__scopeMenu);
      const ref = React53.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      React53.useEffect(() => {
        const content = ref.current;
        if (content) return hideOthers(content);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        MenuContentImpl,
        {
          ...props,
          ref: composedRefs,
          trapFocus: context2.open,
          disableOutsidePointerEvents: context2.open,
          disableOutsideScroll: true,
          onFocusOutside: composeEventHandlers(
            props.onFocusOutside,
            (event) => event.preventDefault(),
            { checkForDefaultPrevented: false }
          ),
          onDismiss: () => context2.onOpenChange(false)
        }
      );
    }
  );
  var MenuRootContentNonModal = React53.forwardRef((props, forwardedRef) => {
    const context2 = useMenuContext(CONTENT_NAME3, props.__scopeMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
      MenuContentImpl,
      {
        ...props,
        ref: forwardedRef,
        trapFocus: false,
        disableOutsidePointerEvents: false,
        disableOutsideScroll: false,
        onDismiss: () => context2.onOpenChange(false)
      }
    );
  });
  var Slot3 = createSlot5("MenuContent.ScrollLock");
  var MenuContentImpl = React53.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeMenu,
        loop = false,
        trapFocus,
        onOpenAutoFocus,
        onCloseAutoFocus,
        disableOutsidePointerEvents,
        onEntryFocus,
        onEscapeKeyDown,
        onPointerDownOutside,
        onFocusOutside,
        onInteractOutside,
        onDismiss,
        disableOutsideScroll,
        ...contentProps
      } = props;
      const context2 = useMenuContext(CONTENT_NAME3, __scopeMenu);
      const rootContext = useMenuRootContext(CONTENT_NAME3, __scopeMenu);
      const popperScope = usePopperScope2(__scopeMenu);
      const rovingFocusGroupScope = useRovingFocusGroupScope(__scopeMenu);
      const getItems = useCollection3(__scopeMenu);
      const [currentItemId, setCurrentItemId] = React53.useState(null);
      const contentRef = React53.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, contentRef, context2.onContentChange);
      const timerRef = React53.useRef(0);
      const searchRef = React53.useRef("");
      const pointerGraceTimerRef = React53.useRef(0);
      const pointerGraceIntentRef = React53.useRef(null);
      const pointerDirRef = React53.useRef("right");
      const lastPointerXRef = React53.useRef(0);
      const ScrollLockWrapper = disableOutsideScroll ? Combination_default : React53.Fragment;
      const scrollLockWrapperProps = disableOutsideScroll ? { as: Slot3, allowPinchZoom: true } : void 0;
      const handleTypeaheadSearch = (key) => {
        const search = searchRef.current + key;
        const items = getItems().filter((item) => !item.disabled);
        const currentItem = document.activeElement;
        const currentMatch = items.find((item) => item.ref.current === currentItem)?.textValue;
        const values = items.map((item) => item.textValue);
        const nextMatch = getNextMatch(values, search, currentMatch);
        const newItem = items.find((item) => item.textValue === nextMatch)?.ref.current;
        (function updateSearch(value) {
          searchRef.current = value;
          window.clearTimeout(timerRef.current);
          if (value !== "") timerRef.current = window.setTimeout(() => updateSearch(""), 1e3);
        })(search);
        if (newItem) {
          setTimeout(() => newItem.focus());
        }
      };
      React53.useEffect(() => {
        return () => window.clearTimeout(timerRef.current);
      }, []);
      useFocusGuards();
      const isPointerMovingToSubmenu = React53.useCallback((event) => {
        const isMovingTowards = pointerDirRef.current === pointerGraceIntentRef.current?.side;
        return isMovingTowards && isPointerInGraceArea(event, pointerGraceIntentRef.current?.area);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        MenuContentProvider,
        {
          scope: __scopeMenu,
          searchRef,
          onItemEnter: React53.useCallback(
            (event) => {
              if (isPointerMovingToSubmenu(event)) event.preventDefault();
            },
            [isPointerMovingToSubmenu]
          ),
          onItemLeave: React53.useCallback(
            (event) => {
              if (isPointerMovingToSubmenu(event)) return;
              contentRef.current?.focus();
              setCurrentItemId(null);
            },
            [isPointerMovingToSubmenu]
          ),
          onTriggerLeave: React53.useCallback(
            (event) => {
              if (isPointerMovingToSubmenu(event)) event.preventDefault();
            },
            [isPointerMovingToSubmenu]
          ),
          pointerGraceTimerRef,
          onPointerGraceIntentChange: React53.useCallback((intent) => {
            pointerGraceIntentRef.current = intent;
          }, []),
          children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(ScrollLockWrapper, { ...scrollLockWrapperProps, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
            FocusScope,
            {
              asChild: true,
              trapped: trapFocus,
              onMountAutoFocus: composeEventHandlers(onOpenAutoFocus, (event) => {
                event.preventDefault();
                contentRef.current?.focus({ preventScroll: true });
              }),
              onUnmountAutoFocus: onCloseAutoFocus,
              children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
                DismissableLayer,
                {
                  asChild: true,
                  disableOutsidePointerEvents,
                  onEscapeKeyDown,
                  onPointerDownOutside,
                  onFocusOutside,
                  onInteractOutside,
                  onDismiss,
                  children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
                    Root6,
                    {
                      asChild: true,
                      ...rovingFocusGroupScope,
                      dir: rootContext.dir,
                      orientation: "vertical",
                      loop,
                      currentTabStopId: currentItemId,
                      onCurrentTabStopIdChange: setCurrentItemId,
                      onEntryFocus: composeEventHandlers(onEntryFocus, (event) => {
                        if (!rootContext.isUsingKeyboardRef.current) event.preventDefault();
                      }),
                      preventScrollOnEntryFocus: true,
                      children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
                        Content4,
                        {
                          role: "menu",
                          "aria-orientation": "vertical",
                          "data-state": getOpenState(context2.open),
                          "data-radix-menu-content": "",
                          dir: rootContext.dir,
                          ...popperScope,
                          ...contentProps,
                          ref: composedRefs,
                          style: { outline: "none", ...contentProps.style },
                          onKeyDown: composeEventHandlers(contentProps.onKeyDown, (event) => {
                            const target = event.target;
                            const isKeyDownInside = target.closest("[data-radix-menu-content]") === event.currentTarget;
                            const isModifierKey = event.ctrlKey || event.altKey || event.metaKey;
                            const isCharacterKey = event.key.length === 1;
                            if (isKeyDownInside) {
                              if (event.key === "Tab") event.preventDefault();
                              if (!isModifierKey && isCharacterKey) handleTypeaheadSearch(event.key);
                            }
                            const content = contentRef.current;
                            if (event.target !== content) return;
                            if (!FIRST_LAST_KEYS.includes(event.key)) return;
                            event.preventDefault();
                            const items = getItems().filter((item) => !item.disabled);
                            const candidateNodes = items.map((item) => item.ref.current);
                            if (LAST_KEYS.includes(event.key)) candidateNodes.reverse();
                            focusFirst4(candidateNodes);
                          }),
                          onBlur: composeEventHandlers(props.onBlur, (event) => {
                            if (!event.currentTarget.contains(event.target)) {
                              window.clearTimeout(timerRef.current);
                              searchRef.current = "";
                            }
                          }),
                          onPointerMove: composeEventHandlers(
                            props.onPointerMove,
                            whenMouse((event) => {
                              const target = event.target;
                              const pointerXHasChanged = lastPointerXRef.current !== event.clientX;
                              if (event.currentTarget.contains(target) && pointerXHasChanged) {
                                const newDir = event.clientX > lastPointerXRef.current ? "right" : "left";
                                pointerDirRef.current = newDir;
                                lastPointerXRef.current = event.clientX;
                              }
                            })
                          )
                        }
                      )
                    }
                  )
                }
              )
            }
          ) })
        }
      );
    }
  );
  MenuContent.displayName = CONTENT_NAME3;
  var GROUP_NAME2 = "MenuGroup";
  var MenuGroup = React53.forwardRef(
    (props, forwardedRef) => {
      const { __scopeMenu, ...groupProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Primitive2.div, { role: "group", ...groupProps, ref: forwardedRef });
    }
  );
  MenuGroup.displayName = GROUP_NAME2;
  var LABEL_NAME = "MenuLabel";
  var MenuLabel = React53.forwardRef(
    (props, forwardedRef) => {
      const { __scopeMenu, ...labelProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Primitive2.div, { ...labelProps, ref: forwardedRef });
    }
  );
  MenuLabel.displayName = LABEL_NAME;
  var ITEM_NAME2 = "MenuItem";
  var ITEM_SELECT = "menu.itemSelect";
  var MenuItem = React53.forwardRef(
    (props, forwardedRef) => {
      const { disabled = false, onSelect, ...itemProps } = props;
      const ref = React53.useRef(null);
      const rootContext = useMenuRootContext(ITEM_NAME2, props.__scopeMenu);
      const contentContext = useMenuContentContext(ITEM_NAME2, props.__scopeMenu);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      const isPointerDownRef = React53.useRef(false);
      const handleSelect = () => {
        const menuItem = ref.current;
        if (!disabled && menuItem) {
          const itemSelectEvent = new CustomEvent(ITEM_SELECT, { bubbles: true, cancelable: true });
          menuItem.addEventListener(ITEM_SELECT, (event) => onSelect?.(event), { once: true });
          dispatchDiscreteCustomEvent(menuItem, itemSelectEvent);
          if (itemSelectEvent.defaultPrevented) {
            isPointerDownRef.current = false;
          } else {
            rootContext.onClose();
          }
        }
      };
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        MenuItemImpl,
        {
          ...itemProps,
          ref: composedRefs,
          disabled,
          onClick: composeEventHandlers(props.onClick, handleSelect),
          onPointerDown: (event) => {
            props.onPointerDown?.(event);
            isPointerDownRef.current = true;
          },
          onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
            if (!isPointerDownRef.current) event.currentTarget?.click();
          }),
          onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
            const isTypingAhead = contentContext.searchRef.current !== "";
            if (disabled || isTypingAhead && event.key === " ") return;
            if (SELECTION_KEYS.includes(event.key)) {
              event.currentTarget.click();
              event.preventDefault();
            }
          })
        }
      );
    }
  );
  MenuItem.displayName = ITEM_NAME2;
  var MenuItemImpl = React53.forwardRef(
    (props, forwardedRef) => {
      const { __scopeMenu, disabled = false, textValue, ...itemProps } = props;
      const contentContext = useMenuContentContext(ITEM_NAME2, __scopeMenu);
      const rovingFocusGroupScope = useRovingFocusGroupScope(__scopeMenu);
      const ref = React53.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      const [isFocused, setIsFocused] = React53.useState(false);
      const [textContent, setTextContent] = React53.useState("");
      React53.useEffect(() => {
        const menuItem = ref.current;
        if (menuItem) {
          setTextContent((menuItem.textContent ?? "").trim());
        }
      }, [itemProps.children]);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        Collection3.ItemSlot,
        {
          scope: __scopeMenu,
          disabled,
          textValue: textValue ?? textContent,
          children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Item5, { asChild: true, ...rovingFocusGroupScope, focusable: !disabled, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
            Primitive2.div,
            {
              role: "menuitem",
              "data-highlighted": isFocused ? "" : void 0,
              "aria-disabled": disabled || void 0,
              "data-disabled": disabled ? "" : void 0,
              ...itemProps,
              ref: composedRefs,
              onPointerMove: composeEventHandlers(
                props.onPointerMove,
                whenMouse((event) => {
                  if (disabled) {
                    contentContext.onItemLeave(event);
                  } else {
                    contentContext.onItemEnter(event);
                    if (!event.defaultPrevented) {
                      const item = event.currentTarget;
                      item.focus({ preventScroll: true });
                    }
                  }
                })
              ),
              onPointerLeave: composeEventHandlers(
                props.onPointerLeave,
                whenMouse((event) => contentContext.onItemLeave(event))
              ),
              onFocus: composeEventHandlers(props.onFocus, () => setIsFocused(true)),
              onBlur: composeEventHandlers(props.onBlur, () => setIsFocused(false))
            }
          ) })
        }
      );
    }
  );
  var CHECKBOX_ITEM_NAME = "MenuCheckboxItem";
  var MenuCheckboxItem = React53.forwardRef(
    (props, forwardedRef) => {
      const { checked = false, onCheckedChange, ...checkboxItemProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(ItemIndicatorProvider, { scope: props.__scopeMenu, checked, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        MenuItem,
        {
          role: "menuitemcheckbox",
          "aria-checked": isIndeterminate(checked) ? "mixed" : checked,
          ...checkboxItemProps,
          ref: forwardedRef,
          "data-state": getCheckedState(checked),
          onSelect: composeEventHandlers(
            checkboxItemProps.onSelect,
            () => onCheckedChange?.(isIndeterminate(checked) ? true : !checked),
            { checkForDefaultPrevented: false }
          )
        }
      ) });
    }
  );
  MenuCheckboxItem.displayName = CHECKBOX_ITEM_NAME;
  var RADIO_GROUP_NAME = "MenuRadioGroup";
  var [RadioGroupProvider, useRadioGroupContext] = createMenuContext(
    RADIO_GROUP_NAME,
    { value: void 0, onValueChange: () => {
    } }
  );
  var MenuRadioGroup = React53.forwardRef(
    (props, forwardedRef) => {
      const { value, onValueChange, ...groupProps } = props;
      const handleValueChange = useCallbackRef(onValueChange);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(RadioGroupProvider, { scope: props.__scopeMenu, value, onValueChange: handleValueChange, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(MenuGroup, { ...groupProps, ref: forwardedRef }) });
    }
  );
  MenuRadioGroup.displayName = RADIO_GROUP_NAME;
  var RADIO_ITEM_NAME = "MenuRadioItem";
  var MenuRadioItem = React53.forwardRef(
    (props, forwardedRef) => {
      const { value, ...radioItemProps } = props;
      const context2 = useRadioGroupContext(RADIO_ITEM_NAME, props.__scopeMenu);
      const checked = value === context2.value;
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(ItemIndicatorProvider, { scope: props.__scopeMenu, checked, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        MenuItem,
        {
          role: "menuitemradio",
          "aria-checked": checked,
          ...radioItemProps,
          ref: forwardedRef,
          "data-state": getCheckedState(checked),
          onSelect: composeEventHandlers(
            radioItemProps.onSelect,
            () => context2.onValueChange?.(value),
            { checkForDefaultPrevented: false }
          )
        }
      ) });
    }
  );
  MenuRadioItem.displayName = RADIO_ITEM_NAME;
  var ITEM_INDICATOR_NAME = "MenuItemIndicator";
  var [ItemIndicatorProvider, useItemIndicatorContext] = createMenuContext(
    ITEM_INDICATOR_NAME,
    { checked: false }
  );
  var MenuItemIndicator = React53.forwardRef(
    (props, forwardedRef) => {
      const { __scopeMenu, forceMount, ...itemIndicatorProps } = props;
      const indicatorContext = useItemIndicatorContext(ITEM_INDICATOR_NAME, __scopeMenu);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        Presence,
        {
          present: forceMount || isIndeterminate(indicatorContext.checked) || indicatorContext.checked === true,
          children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
            Primitive2.span,
            {
              ...itemIndicatorProps,
              ref: forwardedRef,
              "data-state": getCheckedState(indicatorContext.checked)
            }
          )
        }
      );
    }
  );
  MenuItemIndicator.displayName = ITEM_INDICATOR_NAME;
  var SEPARATOR_NAME = "MenuSeparator";
  var MenuSeparator = React53.forwardRef(
    (props, forwardedRef) => {
      const { __scopeMenu, ...separatorProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        Primitive2.div,
        {
          role: "separator",
          "aria-orientation": "horizontal",
          ...separatorProps,
          ref: forwardedRef
        }
      );
    }
  );
  MenuSeparator.displayName = SEPARATOR_NAME;
  var ARROW_NAME3 = "MenuArrow";
  var MenuArrow = React53.forwardRef(
    (props, forwardedRef) => {
      const { __scopeMenu, ...arrowProps } = props;
      const popperScope = usePopperScope2(__scopeMenu);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Arrow2, { ...popperScope, ...arrowProps, ref: forwardedRef });
    }
  );
  MenuArrow.displayName = ARROW_NAME3;
  var SUB_NAME = "MenuSub";
  var [MenuSubProvider, useMenuSubContext] = createMenuContext(SUB_NAME);
  var MenuSub = (props) => {
    const { __scopeMenu, children, open = false, onOpenChange } = props;
    const parentMenuContext = useMenuContext(SUB_NAME, __scopeMenu);
    const popperScope = usePopperScope2(__scopeMenu);
    const [trigger, setTrigger] = React53.useState(null);
    const [content, setContent] = React53.useState(null);
    const handleOpenChange = useCallbackRef(onOpenChange);
    React53.useEffect(() => {
      if (parentMenuContext.open === false) handleOpenChange(false);
      return () => handleOpenChange(false);
    }, [parentMenuContext.open, handleOpenChange]);
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Root23, { ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
      MenuProvider,
      {
        scope: __scopeMenu,
        open,
        onOpenChange: handleOpenChange,
        content,
        onContentChange: setContent,
        children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
          MenuSubProvider,
          {
            scope: __scopeMenu,
            contentId: useId5(),
            triggerId: useId5(),
            trigger,
            onTriggerChange: setTrigger,
            children
          }
        )
      }
    ) });
  };
  MenuSub.displayName = SUB_NAME;
  var SUB_TRIGGER_NAME = "MenuSubTrigger";
  var MenuSubTrigger = React53.forwardRef(
    (props, forwardedRef) => {
      const context2 = useMenuContext(SUB_TRIGGER_NAME, props.__scopeMenu);
      const rootContext = useMenuRootContext(SUB_TRIGGER_NAME, props.__scopeMenu);
      const subContext = useMenuSubContext(SUB_TRIGGER_NAME, props.__scopeMenu);
      const contentContext = useMenuContentContext(SUB_TRIGGER_NAME, props.__scopeMenu);
      const openTimerRef = React53.useRef(null);
      const { pointerGraceTimerRef, onPointerGraceIntentChange } = contentContext;
      const scope = { __scopeMenu: props.__scopeMenu };
      const clearOpenTimer = React53.useCallback(() => {
        if (openTimerRef.current) window.clearTimeout(openTimerRef.current);
        openTimerRef.current = null;
      }, []);
      React53.useEffect(() => clearOpenTimer, [clearOpenTimer]);
      React53.useEffect(() => {
        const pointerGraceTimer = pointerGraceTimerRef.current;
        return () => {
          window.clearTimeout(pointerGraceTimer);
          onPointerGraceIntentChange(null);
        };
      }, [pointerGraceTimerRef, onPointerGraceIntentChange]);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(MenuAnchor, { asChild: true, ...scope, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        MenuItemImpl,
        {
          id: subContext.triggerId,
          "aria-haspopup": "menu",
          "aria-expanded": context2.open,
          "aria-controls": subContext.contentId,
          "data-state": getOpenState(context2.open),
          ...props,
          ref: composeRefs(forwardedRef, subContext.onTriggerChange),
          onClick: (event) => {
            props.onClick?.(event);
            if (props.disabled || event.defaultPrevented) return;
            event.currentTarget.focus();
            if (!context2.open) context2.onOpenChange(true);
          },
          onPointerMove: composeEventHandlers(
            props.onPointerMove,
            whenMouse((event) => {
              contentContext.onItemEnter(event);
              if (event.defaultPrevented) return;
              if (!props.disabled && !context2.open && !openTimerRef.current) {
                contentContext.onPointerGraceIntentChange(null);
                openTimerRef.current = window.setTimeout(() => {
                  context2.onOpenChange(true);
                  clearOpenTimer();
                }, 100);
              }
            })
          ),
          onPointerLeave: composeEventHandlers(
            props.onPointerLeave,
            whenMouse((event) => {
              clearOpenTimer();
              const contentRect = context2.content?.getBoundingClientRect();
              if (contentRect) {
                const side = context2.content?.dataset.side;
                const rightSide = side === "right";
                const bleed = rightSide ? -5 : 5;
                const contentNearEdge = contentRect[rightSide ? "left" : "right"];
                const contentFarEdge = contentRect[rightSide ? "right" : "left"];
                contentContext.onPointerGraceIntentChange({
                  area: [
                    // Apply a bleed on clientX to ensure that our exit point is
                    // consistently within polygon bounds
                    { x: event.clientX + bleed, y: event.clientY },
                    { x: contentNearEdge, y: contentRect.top },
                    { x: contentFarEdge, y: contentRect.top },
                    { x: contentFarEdge, y: contentRect.bottom },
                    { x: contentNearEdge, y: contentRect.bottom }
                  ],
                  side
                });
                window.clearTimeout(pointerGraceTimerRef.current);
                pointerGraceTimerRef.current = window.setTimeout(
                  () => contentContext.onPointerGraceIntentChange(null),
                  300
                );
              } else {
                contentContext.onTriggerLeave(event);
                if (event.defaultPrevented) return;
                contentContext.onPointerGraceIntentChange(null);
              }
            })
          ),
          onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
            const isTypingAhead = contentContext.searchRef.current !== "";
            if (props.disabled || isTypingAhead && event.key === " ") return;
            if (SUB_OPEN_KEYS[rootContext.dir].includes(event.key)) {
              context2.onOpenChange(true);
              context2.content?.focus();
              event.preventDefault();
            }
          })
        }
      ) });
    }
  );
  MenuSubTrigger.displayName = SUB_TRIGGER_NAME;
  var SUB_CONTENT_NAME = "MenuSubContent";
  var MenuSubContent = React53.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext2(CONTENT_NAME3, props.__scopeMenu);
      const { forceMount = portalContext.forceMount, ...subContentProps } = props;
      const context2 = useMenuContext(CONTENT_NAME3, props.__scopeMenu);
      const rootContext = useMenuRootContext(CONTENT_NAME3, props.__scopeMenu);
      const subContext = useMenuSubContext(SUB_CONTENT_NAME, props.__scopeMenu);
      const ref = React53.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Collection3.Provider, { scope: props.__scopeMenu, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Presence, { present: forceMount || context2.open, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Collection3.Slot, { scope: props.__scopeMenu, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        MenuContentImpl,
        {
          id: subContext.contentId,
          "aria-labelledby": subContext.triggerId,
          ...subContentProps,
          ref: composedRefs,
          align: "start",
          side: rootContext.dir === "rtl" ? "left" : "right",
          disableOutsidePointerEvents: false,
          disableOutsideScroll: false,
          trapFocus: false,
          onOpenAutoFocus: (event) => {
            if (rootContext.isUsingKeyboardRef.current) ref.current?.focus();
            event.preventDefault();
          },
          onCloseAutoFocus: (event) => event.preventDefault(),
          onFocusOutside: composeEventHandlers(props.onFocusOutside, (event) => {
            if (event.target !== subContext.trigger) context2.onOpenChange(false);
          }),
          onEscapeKeyDown: composeEventHandlers(props.onEscapeKeyDown, (event) => {
            rootContext.onClose();
            event.preventDefault();
          }),
          onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
            const isKeyDownInside = event.currentTarget.contains(event.target);
            const isCloseKey = SUB_CLOSE_KEYS[rootContext.dir].includes(event.key);
            if (isKeyDownInside && isCloseKey) {
              context2.onOpenChange(false);
              subContext.trigger?.focus();
              event.preventDefault();
            }
          })
        }
      ) }) }) });
    }
  );
  MenuSubContent.displayName = SUB_CONTENT_NAME;
  function getOpenState(open) {
    return open ? "open" : "closed";
  }
  function isIndeterminate(checked) {
    return checked === "indeterminate";
  }
  function getCheckedState(checked) {
    return isIndeterminate(checked) ? "indeterminate" : checked ? "checked" : "unchecked";
  }
  function focusFirst4(candidates) {
    const PREVIOUSLY_FOCUSED_ELEMENT = document.activeElement;
    for (const candidate of candidates) {
      if (candidate === PREVIOUSLY_FOCUSED_ELEMENT) return;
      candidate.focus();
      if (document.activeElement !== PREVIOUSLY_FOCUSED_ELEMENT) return;
    }
  }
  function wrapArray2(array, startIndex) {
    return array.map((_, index2) => array[(startIndex + index2) % array.length]);
  }
  function getNextMatch(values, search, currentMatch) {
    const isRepeated = search.length > 1 && Array.from(search).every((char) => char === search[0]);
    const normalizedSearch = isRepeated ? search[0] : search;
    const currentMatchIndex = currentMatch ? values.indexOf(currentMatch) : -1;
    let wrappedValues = wrapArray2(values, Math.max(currentMatchIndex, 0));
    const excludeCurrentMatch = normalizedSearch.length === 1;
    if (excludeCurrentMatch) wrappedValues = wrappedValues.filter((v) => v !== currentMatch);
    const nextMatch = wrappedValues.find(
      (value) => value.toLowerCase().startsWith(normalizedSearch.toLowerCase())
    );
    return nextMatch !== currentMatch ? nextMatch : void 0;
  }
  function isPointInPolygon(point, polygon) {
    const { x, y } = point;
    let inside = false;
    for (let i = 0, j = polygon.length - 1; i < polygon.length; j = i++) {
      const ii = polygon[i];
      const jj = polygon[j];
      const xi = ii.x;
      const yi = ii.y;
      const xj = jj.x;
      const yj = jj.y;
      const intersect = yi > y !== yj > y && x < (xj - xi) * (y - yi) / (yj - yi) + xi;
      if (intersect) inside = !inside;
    }
    return inside;
  }
  function isPointerInGraceArea(event, area) {
    if (!area) return false;
    const cursorPos = { x: event.clientX, y: event.clientY };
    return isPointInPolygon(cursorPos, area);
  }
  function whenMouse(handler) {
    return (event) => event.pointerType === "mouse" ? handler(event) : void 0;
  }
  var Root32 = Menu;
  var Anchor2 = MenuAnchor;
  var Portal3 = MenuPortal;
  var Content23 = MenuContent;
  var Group = MenuGroup;
  var Label4 = MenuLabel;
  var Item22 = MenuItem;
  var CheckboxItem = MenuCheckboxItem;
  var RadioGroup = MenuRadioGroup;
  var RadioItem = MenuRadioItem;
  var ItemIndicator = MenuItemIndicator;
  var Separator2 = MenuSeparator;
  var Arrow22 = MenuArrow;
  var Sub = MenuSub;
  var SubTrigger = MenuSubTrigger;
  var SubContent = MenuSubContent;

  // node_modules/@radix-ui/react-dropdown-menu/dist/index.mjs
  var import_jsx_runtime26 = __toESM(require_react_shim(), 1);
  var DROPDOWN_MENU_NAME = "DropdownMenu";
  var [createDropdownMenuContext, createDropdownMenuScope] = createContextScope(
    DROPDOWN_MENU_NAME,
    [createMenuScope]
  );
  var useMenuScope = createMenuScope();
  var [DropdownMenuProvider, useDropdownMenuContext] = createDropdownMenuContext(DROPDOWN_MENU_NAME);
  var DropdownMenu = (props) => {
    const {
      __scopeDropdownMenu,
      children,
      dir: dir2,
      open: openProp,
      defaultOpen,
      onOpenChange,
      modal = true
    } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    const triggerRef = React54.useRef(null);
    const [open, setOpen] = useControllableState({
      prop: openProp,
      defaultProp: defaultOpen ?? false,
      onChange: onOpenChange,
      caller: DROPDOWN_MENU_NAME
    });
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
      DropdownMenuProvider,
      {
        scope: __scopeDropdownMenu,
        triggerId: useId5(),
        triggerRef,
        contentId: useId5(),
        open,
        onOpenChange: setOpen,
        onOpenToggle: React54.useCallback(() => setOpen((prevOpen) => !prevOpen), [setOpen]),
        modal,
        children: /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Root32, { ...menuScope, open, onOpenChange: setOpen, dir: dir2, modal, children })
      }
    );
  };
  DropdownMenu.displayName = DROPDOWN_MENU_NAME;
  var TRIGGER_NAME2 = "DropdownMenuTrigger";
  var DropdownMenuTrigger = React54.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDropdownMenu, disabled = false, ...triggerProps } = props;
      const context2 = useDropdownMenuContext(TRIGGER_NAME2, __scopeDropdownMenu);
      const menuScope = useMenuScope(__scopeDropdownMenu);
      return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Anchor2, { asChild: true, ...menuScope, children: /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
        Primitive2.button,
        {
          type: "button",
          id: context2.triggerId,
          "aria-haspopup": "menu",
          "aria-expanded": context2.open,
          "aria-controls": context2.open ? context2.contentId : void 0,
          "data-state": context2.open ? "open" : "closed",
          "data-disabled": disabled ? "" : void 0,
          disabled,
          ...triggerProps,
          ref: composeRefs(forwardedRef, context2.triggerRef),
          onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
            if (!disabled && event.button === 0 && event.ctrlKey === false) {
              context2.onOpenToggle();
              if (!context2.open) event.preventDefault();
            }
          }),
          onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
            if (disabled) return;
            if (["Enter", " "].includes(event.key)) context2.onOpenToggle();
            if (event.key === "ArrowDown") context2.onOpenChange(true);
            if (["Enter", " ", "ArrowDown"].includes(event.key)) event.preventDefault();
          })
        }
      ) });
    }
  );
  DropdownMenuTrigger.displayName = TRIGGER_NAME2;
  var PORTAL_NAME4 = "DropdownMenuPortal";
  var DropdownMenuPortal = (props) => {
    const { __scopeDropdownMenu, ...portalProps } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Portal3, { ...menuScope, ...portalProps });
  };
  DropdownMenuPortal.displayName = PORTAL_NAME4;
  var CONTENT_NAME4 = "DropdownMenuContent";
  var DropdownMenuContent = React54.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDropdownMenu, ...contentProps } = props;
      const context2 = useDropdownMenuContext(CONTENT_NAME4, __scopeDropdownMenu);
      const menuScope = useMenuScope(__scopeDropdownMenu);
      const hasInteractedOutsideRef = React54.useRef(false);
      return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
        Content23,
        {
          id: context2.contentId,
          "aria-labelledby": context2.triggerId,
          ...menuScope,
          ...contentProps,
          ref: forwardedRef,
          onCloseAutoFocus: composeEventHandlers(props.onCloseAutoFocus, (event) => {
            if (!hasInteractedOutsideRef.current) context2.triggerRef.current?.focus();
            hasInteractedOutsideRef.current = false;
            event.preventDefault();
          }),
          onInteractOutside: composeEventHandlers(props.onInteractOutside, (event) => {
            const originalEvent = event.detail.originalEvent;
            const ctrlLeftClick = originalEvent.button === 0 && originalEvent.ctrlKey === true;
            const isRightClick = originalEvent.button === 2 || ctrlLeftClick;
            if (!context2.modal || isRightClick) hasInteractedOutsideRef.current = true;
          }),
          style: {
            ...props.style,
            // re-namespace exposed content custom properties
            ...{
              "--radix-dropdown-menu-content-transform-origin": "var(--radix-popper-transform-origin)",
              "--radix-dropdown-menu-content-available-width": "var(--radix-popper-available-width)",
              "--radix-dropdown-menu-content-available-height": "var(--radix-popper-available-height)",
              "--radix-dropdown-menu-trigger-width": "var(--radix-popper-anchor-width)",
              "--radix-dropdown-menu-trigger-height": "var(--radix-popper-anchor-height)"
            }
          }
        }
      );
    }
  );
  DropdownMenuContent.displayName = CONTENT_NAME4;
  var GROUP_NAME3 = "DropdownMenuGroup";
  var DropdownMenuGroup = React54.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDropdownMenu, ...groupProps } = props;
      const menuScope = useMenuScope(__scopeDropdownMenu);
      return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Group, { ...menuScope, ...groupProps, ref: forwardedRef });
    }
  );
  DropdownMenuGroup.displayName = GROUP_NAME3;
  var LABEL_NAME2 = "DropdownMenuLabel";
  var DropdownMenuLabel = React54.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDropdownMenu, ...labelProps } = props;
      const menuScope = useMenuScope(__scopeDropdownMenu);
      return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Label4, { ...menuScope, ...labelProps, ref: forwardedRef });
    }
  );
  DropdownMenuLabel.displayName = LABEL_NAME2;
  var ITEM_NAME3 = "DropdownMenuItem";
  var DropdownMenuItem = React54.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDropdownMenu, ...itemProps } = props;
      const menuScope = useMenuScope(__scopeDropdownMenu);
      return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Item22, { ...menuScope, ...itemProps, ref: forwardedRef });
    }
  );
  DropdownMenuItem.displayName = ITEM_NAME3;
  var CHECKBOX_ITEM_NAME2 = "DropdownMenuCheckboxItem";
  var DropdownMenuCheckboxItem = React54.forwardRef((props, forwardedRef) => {
    const { __scopeDropdownMenu, ...checkboxItemProps } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(CheckboxItem, { ...menuScope, ...checkboxItemProps, ref: forwardedRef });
  });
  DropdownMenuCheckboxItem.displayName = CHECKBOX_ITEM_NAME2;
  var RADIO_GROUP_NAME2 = "DropdownMenuRadioGroup";
  var DropdownMenuRadioGroup = React54.forwardRef((props, forwardedRef) => {
    const { __scopeDropdownMenu, ...radioGroupProps } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(RadioGroup, { ...menuScope, ...radioGroupProps, ref: forwardedRef });
  });
  DropdownMenuRadioGroup.displayName = RADIO_GROUP_NAME2;
  var RADIO_ITEM_NAME2 = "DropdownMenuRadioItem";
  var DropdownMenuRadioItem = React54.forwardRef((props, forwardedRef) => {
    const { __scopeDropdownMenu, ...radioItemProps } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(RadioItem, { ...menuScope, ...radioItemProps, ref: forwardedRef });
  });
  DropdownMenuRadioItem.displayName = RADIO_ITEM_NAME2;
  var INDICATOR_NAME = "DropdownMenuItemIndicator";
  var DropdownMenuItemIndicator = React54.forwardRef((props, forwardedRef) => {
    const { __scopeDropdownMenu, ...itemIndicatorProps } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(ItemIndicator, { ...menuScope, ...itemIndicatorProps, ref: forwardedRef });
  });
  DropdownMenuItemIndicator.displayName = INDICATOR_NAME;
  var SEPARATOR_NAME2 = "DropdownMenuSeparator";
  var DropdownMenuSeparator = React54.forwardRef((props, forwardedRef) => {
    const { __scopeDropdownMenu, ...separatorProps } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Separator2, { ...menuScope, ...separatorProps, ref: forwardedRef });
  });
  DropdownMenuSeparator.displayName = SEPARATOR_NAME2;
  var ARROW_NAME4 = "DropdownMenuArrow";
  var DropdownMenuArrow = React54.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDropdownMenu, ...arrowProps } = props;
      const menuScope = useMenuScope(__scopeDropdownMenu);
      return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Arrow22, { ...menuScope, ...arrowProps, ref: forwardedRef });
    }
  );
  DropdownMenuArrow.displayName = ARROW_NAME4;
  var DropdownMenuSub = (props) => {
    const { __scopeDropdownMenu, children, open: openProp, onOpenChange, defaultOpen } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    const [open, setOpen] = useControllableState({
      prop: openProp,
      defaultProp: defaultOpen ?? false,
      onChange: onOpenChange,
      caller: "DropdownMenuSub"
    });
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Sub, { ...menuScope, open, onOpenChange: setOpen, children });
  };
  var SUB_TRIGGER_NAME2 = "DropdownMenuSubTrigger";
  var DropdownMenuSubTrigger = React54.forwardRef((props, forwardedRef) => {
    const { __scopeDropdownMenu, ...subTriggerProps } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(SubTrigger, { ...menuScope, ...subTriggerProps, ref: forwardedRef });
  });
  DropdownMenuSubTrigger.displayName = SUB_TRIGGER_NAME2;
  var SUB_CONTENT_NAME2 = "DropdownMenuSubContent";
  var DropdownMenuSubContent = React54.forwardRef((props, forwardedRef) => {
    const { __scopeDropdownMenu, ...subContentProps } = props;
    const menuScope = useMenuScope(__scopeDropdownMenu);
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
      SubContent,
      {
        ...menuScope,
        ...subContentProps,
        ref: forwardedRef,
        style: {
          ...props.style,
          // re-namespace exposed content custom properties
          ...{
            "--radix-dropdown-menu-content-transform-origin": "var(--radix-popper-transform-origin)",
            "--radix-dropdown-menu-content-available-width": "var(--radix-popper-available-width)",
            "--radix-dropdown-menu-content-available-height": "var(--radix-popper-available-height)",
            "--radix-dropdown-menu-trigger-width": "var(--radix-popper-anchor-width)",
            "--radix-dropdown-menu-trigger-height": "var(--radix-popper-anchor-height)"
          }
        }
      }
    );
  });
  DropdownMenuSubContent.displayName = SUB_CONTENT_NAME2;
  var Root25 = DropdownMenu;
  var Trigger6 = DropdownMenuTrigger;
  var Portal22 = DropdownMenuPortal;
  var Content24 = DropdownMenuContent;
  var Group2 = DropdownMenuGroup;
  var Label22 = DropdownMenuLabel;
  var Item23 = DropdownMenuItem;
  var CheckboxItem2 = DropdownMenuCheckboxItem;
  var RadioGroup2 = DropdownMenuRadioGroup;
  var RadioItem2 = DropdownMenuRadioItem;
  var ItemIndicator2 = DropdownMenuItemIndicator;
  var Separator22 = DropdownMenuSeparator;
  var Sub2 = DropdownMenuSub;
  var SubTrigger2 = DropdownMenuSubTrigger;
  var SubContent2 = DropdownMenuSubContent;

  // src/components/ui/dropdown-menu/DropdownMenu.tsx
  var DropdownMenuTrigger2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    Trigger6,
    {
      "data-slot": "dropdown-trigger",
      className: cn("flex items-center justify-center text-sm font-medium outline-none", className),
      ...props
    }
  );
  var DropdownMenuPortal2 = (...props) => /* @__PURE__ */ React.createElement(Portal22, { "data-slot": "dropdown-portal", ...props });
  var DropdownMenuGroup2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Group2, { "data-slot": "dropdown-group", className: cn(className), ...props });
  var DropdownMenuSub2 = (props) => /* @__PURE__ */ React.createElement(Sub2, { "data-slot": "dropdown-sub", ...props });
  var DropdownMenuRadioGroup2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(RadioGroup2, { "data-slot": "dropdown-radio-group", className: cn(className), ...props });
  var SubTrigger3 = ({ className, inset, children, ...props }) => /* @__PURE__ */ React.createElement(
    SubTrigger2,
    {
      "data-slot": "dropdown-subtrigger",
      className: cn(
        "focus:bg-accent data-[state=open]:bg-accent flex cursor-default items-center rounded-sm px-2 py-1.5 text-sm outline-none select-none",
        inset && "ps-8",
        className
      ),
      ...props
    },
    children,
    /* @__PURE__ */ React.createElement(ChevronRight, { className: "ms-auto h-4 w-4" })
  );
  var SubContent3 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    SubContent2,
    {
      "data-slot": "dropdown-subcontent",
      className: cn(
        "border-primary bg-popover text-popover-foreground data-[state=open]:animate-in data-[state=closed]:animate-out",
        "data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95",
        "data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2",
        "data-[side=top]:slide-in-from-bottom-2 z-50 min-w-32 overflow-hidden rounded-md border p-1 shadow-lg",
        className
      ),
      ...props
    }
  );
  var Content5 = ({ className, sideOffset = 4, ...props }) => /* @__PURE__ */ React.createElement(Portal22, null, /* @__PURE__ */ React.createElement(
    Content24,
    {
      "data-slot": "dropdown-content",
      sideOffset,
      className: cn(
        "bg-popover text-popover-foreground shadow-boundary-ghost z-50 min-w-32 overflow-hidden rounded-md",
        "data-[state=open]:animate-in data-[state=closed]:animate-out",
        "data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95",
        "data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2",
        "data-[side=top]:slide-in-from-bottom-2",
        className
      ),
      ...props
    }
  ));
  var Item6 = ({ className, inset, ...props }) => /* @__PURE__ */ React.createElement(
    Item23,
    {
      "data-slot": "dropdown-item",
      className: cn(
        "hover:bg-primary hover:text-primary-foreground dark:hover:bg-primary-300 text-foreground relative flex cursor-pointer items-center gap-2 rounded-sm px-2 py-1.5 text-xs transition-colors",
        "outline-none select-none data-disabled:pointer-events-none data-disabled:opacity-50",
        inset && "ps-8",
        className
      ),
      ...props
    }
  );
  var CheckboxItem3 = ({ className, children, checked, ...props }) => /* @__PURE__ */ React.createElement(
    CheckboxItem2,
    {
      "data-slot": "dropdown-checkbox-item",
      className: cn(
        "focus:bg-accent focus:text-accent-foreground relative flex cursor-default items-center rounded-sm py-1.5",
        "ps-8 pe-2 text-sm transition-colors outline-none select-none data-disabled:pointer-events-none data-disabled:opacity-50",
        className
      ),
      checked,
      ...props
    },
    /* @__PURE__ */ React.createElement("span", { className: "absolute inset-s-2 flex h-3.5 w-3.5 items-center justify-center" }, /* @__PURE__ */ React.createElement(ItemIndicator2, { "data-slot": "dropdown-item-indicator" }, /* @__PURE__ */ React.createElement(Check, { size: 16 }))),
    children
  );
  var RadioItem3 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    RadioItem2,
    {
      "data-slot": "dropdown-radio-item",
      className: cn(
        "focus:bg-accent focus:text-accent-foreground relative flex cursor-default items-center rounded-sm py-1.5",
        "ps-8 pe-2 text-sm transition-colors outline-none select-none data-disabled:pointer-events-none data-disabled:opacity-50",
        className
      ),
      ...props
    },
    /* @__PURE__ */ React.createElement("span", { className: "absolute inset-s-2 flex h-3.5 w-3.5 items-center justify-center" }, /* @__PURE__ */ React.createElement(ItemIndicator2, { "data-slot": "dropdown-item-indicator" }, /* @__PURE__ */ React.createElement(Circle, { className: "h-2 w-2 fill-current" }))),
    children
  );
  var Label5 = ({ className, inset, ...props }) => /* @__PURE__ */ React.createElement(
    Label22,
    {
      "data-slot": "dropdown-label",
      className: cn("px-2 py-1.5 text-sm font-semibold", inset && "ps-8", className),
      ...props
    }
  );
  var Separator3 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Separator22, { "data-slot": "dropdown-separator", className: cn("bg-muted-200 -mx-1 my-1 h-px", className), ...props });
  var Shortcut = ({ className, ...props }) => /* @__PURE__ */ React.createElement("span", { "data-slot": "dropdown-shortcut", className: cn("ms-auto text-xs tracking-widest opacity-60", className), ...props });
  var DropdownMenu2 = (props) => /* @__PURE__ */ React.createElement(Root25, { "data-slot": "dropdown-menu", ...props });
  DropdownMenu2.Trigger = DropdownMenuTrigger2;
  DropdownMenu2.Group = DropdownMenuGroup2;
  DropdownMenu2.Portal = DropdownMenuPortal2;
  DropdownMenu2.Sub = DropdownMenuSub2;
  DropdownMenu2.RadioGroup = DropdownMenuRadioGroup2;
  DropdownMenu2.SubTrigger = SubTrigger3;
  DropdownMenu2.SubContent = SubContent3;
  DropdownMenu2.Content = Content5;
  DropdownMenu2.Item = Item6;
  DropdownMenu2.CheckboxItem = CheckboxItem3;
  DropdownMenu2.RadioItem = RadioItem3;
  DropdownMenu2.Label = Label5;
  DropdownMenu2.Separator = Separator3;
  DropdownMenu2.Shortcut = Shortcut;
  SubTrigger3.displayName = SubTrigger2.displayName;
  SubContent3.displayName = SubContent2.displayName;
  Content5.displayName = Content24.displayName;
  Item6.displayName = Item23.displayName;
  CheckboxItem3.displayName = CheckboxItem2.displayName;
  RadioItem3.displayName = RadioItem2.displayName;
  Label5.displayName = Label22.displayName;
  Separator3.displayName = Separator22.displayName;
  Shortcut.displayName = "DropdownMenuShortcut";
  var DropdownMenu_default = DropdownMenu2;

  // src/components/ui/card/index.ts
  init_define_import_meta_env();

  // src/components/ui/card/Card.tsx
  init_define_import_meta_env();
  var cardVariants = cva("rounded-2xl bg-background", {
    variants: {
      shadow: {
        none: "shadow-none",
        default: "shadow",
        sm: "shadow-sm",
        md: "shadow-md",
        lg: "shadow-lg",
        xl: "shadow-xl",
        deep: "shadow-deep",
        "shadow-boundary": "shadow-boundary",
        "shadow-boundary-ghost": "shadow-boundary-ghost",
        "shadow-spread": "shadow-spread",
        "shadow-inner": "shadow-inner"
      }
    },
    defaultVariants: {
      shadow: "default"
    }
  });
  var Header5 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("header", { "data-slot": "card-header", className: cn("px-6 py-4", className), ...props });
  var Title7 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("h3", { "data-slot": "card-title", className: cn("text-xl leading-none font-bold", className), ...props });
  var Description6 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("p", { "data-slot": "card-description", className: cn("text-muted-400 text-xs", className), ...props });
  var Content6 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("section", { "data-slot": "card-content", className: cn("px-6 py-4", className), ...props });
  var Footer4 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("footer", { "data-slot": "card-footer", className: cn("px-6 py-4", className), ...props });
  var Card = ({ className, shadow, ...props }) => /* @__PURE__ */ React.createElement("article", { "data-slot": "card", className: cn(cardVariants({ shadow }), className), ...props });
  Card.Header = Header5;
  Card.Title = Title7;
  Card.Description = Description6;
  Card.Content = Content6;
  Card.Footer = Footer4;
  var Card_default = Card;

  // src/components/ui/tooltip/index.ts
  init_define_import_meta_env();

  // src/components/ui/tooltip/Tooltip.tsx
  init_define_import_meta_env();
  var import_react58 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-tooltip/dist/index.mjs
  init_define_import_meta_env();
  var React56 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-tooltip/node_modules/@radix-ui/react-slot/dist/index.mjs
  init_define_import_meta_env();
  var React55 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime27 = __toESM(require_react_shim(), 1);
  var SLOTTABLE_IDENTIFIER6 = /* @__PURE__ */ Symbol("radix.slottable");
  // @__NO_SIDE_EFFECTS__
  function createSlottable(ownerName) {
    const Slottable2 = ({ children }) => {
      return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(import_jsx_runtime27.Fragment, { children });
    };
    Slottable2.displayName = `${ownerName}.Slottable`;
    Slottable2.__radixId = SLOTTABLE_IDENTIFIER6;
    return Slottable2;
  }

  // node_modules/@radix-ui/react-tooltip/dist/index.mjs
  var import_jsx_runtime28 = __toESM(require_react_shim(), 1);
  var [createTooltipContext, createTooltipScope] = createContextScope("Tooltip", [
    createPopperScope
  ]);
  var usePopperScope3 = createPopperScope();
  var PROVIDER_NAME2 = "TooltipProvider";
  var DEFAULT_DELAY_DURATION = 700;
  var TOOLTIP_OPEN = "tooltip.open";
  var [TooltipProviderContextProvider, useTooltipProviderContext] = createTooltipContext(PROVIDER_NAME2);
  var TooltipProvider = (props) => {
    const {
      __scopeTooltip,
      delayDuration = DEFAULT_DELAY_DURATION,
      skipDelayDuration = 300,
      disableHoverableContent = false,
      children
    } = props;
    const isOpenDelayedRef = React56.useRef(true);
    const isPointerInTransitRef = React56.useRef(false);
    const skipDelayTimerRef = React56.useRef(0);
    React56.useEffect(() => {
      const skipDelayTimer = skipDelayTimerRef.current;
      return () => window.clearTimeout(skipDelayTimer);
    }, []);
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
      TooltipProviderContextProvider,
      {
        scope: __scopeTooltip,
        isOpenDelayedRef,
        delayDuration,
        onOpen: React56.useCallback(() => {
          window.clearTimeout(skipDelayTimerRef.current);
          isOpenDelayedRef.current = false;
        }, []),
        onClose: React56.useCallback(() => {
          window.clearTimeout(skipDelayTimerRef.current);
          skipDelayTimerRef.current = window.setTimeout(
            () => isOpenDelayedRef.current = true,
            skipDelayDuration
          );
        }, [skipDelayDuration]),
        isPointerInTransitRef,
        onPointerInTransitChange: React56.useCallback((inTransit) => {
          isPointerInTransitRef.current = inTransit;
        }, []),
        disableHoverableContent,
        children
      }
    );
  };
  TooltipProvider.displayName = PROVIDER_NAME2;
  var TOOLTIP_NAME = "Tooltip";
  var [TooltipContextProvider, useTooltipContext] = createTooltipContext(TOOLTIP_NAME);
  var Tooltip = (props) => {
    const {
      __scopeTooltip,
      children,
      open: openProp,
      defaultOpen,
      onOpenChange,
      disableHoverableContent: disableHoverableContentProp,
      delayDuration: delayDurationProp
    } = props;
    const providerContext = useTooltipProviderContext(TOOLTIP_NAME, props.__scopeTooltip);
    const popperScope = usePopperScope3(__scopeTooltip);
    const [trigger, setTrigger] = React56.useState(null);
    const contentId = useId5();
    const openTimerRef = React56.useRef(0);
    const disableHoverableContent = disableHoverableContentProp ?? providerContext.disableHoverableContent;
    const delayDuration = delayDurationProp ?? providerContext.delayDuration;
    const wasOpenDelayedRef = React56.useRef(false);
    const [open, setOpen] = useControllableState({
      prop: openProp,
      defaultProp: defaultOpen ?? false,
      onChange: (open2) => {
        if (open2) {
          providerContext.onOpen();
          document.dispatchEvent(new CustomEvent(TOOLTIP_OPEN));
        } else {
          providerContext.onClose();
        }
        onOpenChange?.(open2);
      },
      caller: TOOLTIP_NAME
    });
    const stateAttribute = React56.useMemo(() => {
      return open ? wasOpenDelayedRef.current ? "delayed-open" : "instant-open" : "closed";
    }, [open]);
    const handleOpen = React56.useCallback(() => {
      window.clearTimeout(openTimerRef.current);
      openTimerRef.current = 0;
      wasOpenDelayedRef.current = false;
      setOpen(true);
    }, [setOpen]);
    const handleClose = React56.useCallback(() => {
      window.clearTimeout(openTimerRef.current);
      openTimerRef.current = 0;
      setOpen(false);
    }, [setOpen]);
    const handleDelayedOpen = React56.useCallback(() => {
      window.clearTimeout(openTimerRef.current);
      openTimerRef.current = window.setTimeout(() => {
        wasOpenDelayedRef.current = true;
        setOpen(true);
        openTimerRef.current = 0;
      }, delayDuration);
    }, [delayDuration, setOpen]);
    React56.useEffect(() => {
      return () => {
        if (openTimerRef.current) {
          window.clearTimeout(openTimerRef.current);
          openTimerRef.current = 0;
        }
      };
    }, []);
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Root23, { ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
      TooltipContextProvider,
      {
        scope: __scopeTooltip,
        contentId,
        open,
        stateAttribute,
        trigger,
        onTriggerChange: setTrigger,
        onTriggerEnter: React56.useCallback(() => {
          if (providerContext.isOpenDelayedRef.current) handleDelayedOpen();
          else handleOpen();
        }, [providerContext.isOpenDelayedRef, handleDelayedOpen, handleOpen]),
        onTriggerLeave: React56.useCallback(() => {
          if (disableHoverableContent) {
            handleClose();
          } else {
            window.clearTimeout(openTimerRef.current);
            openTimerRef.current = 0;
          }
        }, [handleClose, disableHoverableContent]),
        onOpen: handleOpen,
        onClose: handleClose,
        disableHoverableContent,
        children
      }
    ) });
  };
  Tooltip.displayName = TOOLTIP_NAME;
  var TRIGGER_NAME3 = "TooltipTrigger";
  var TooltipTrigger = React56.forwardRef(
    (props, forwardedRef) => {
      const { __scopeTooltip, ...triggerProps } = props;
      const context2 = useTooltipContext(TRIGGER_NAME3, __scopeTooltip);
      const providerContext = useTooltipProviderContext(TRIGGER_NAME3, __scopeTooltip);
      const popperScope = usePopperScope3(__scopeTooltip);
      const ref = React56.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref, context2.onTriggerChange);
      const isPointerDownRef = React56.useRef(false);
      const hasPointerMoveOpenedRef = React56.useRef(false);
      const handlePointerUp = React56.useCallback(() => isPointerDownRef.current = false, []);
      React56.useEffect(() => {
        return () => document.removeEventListener("pointerup", handlePointerUp);
      }, [handlePointerUp]);
      return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Anchor, { asChild: true, ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
        Primitive2.button,
        {
          "aria-describedby": context2.open ? context2.contentId : void 0,
          "data-state": context2.stateAttribute,
          ...triggerProps,
          ref: composedRefs,
          onPointerMove: composeEventHandlers(props.onPointerMove, (event) => {
            if (event.pointerType === "touch") return;
            if (!hasPointerMoveOpenedRef.current && !providerContext.isPointerInTransitRef.current) {
              context2.onTriggerEnter();
              hasPointerMoveOpenedRef.current = true;
            }
          }),
          onPointerLeave: composeEventHandlers(props.onPointerLeave, () => {
            context2.onTriggerLeave();
            hasPointerMoveOpenedRef.current = false;
          }),
          onPointerDown: composeEventHandlers(props.onPointerDown, () => {
            if (context2.open) {
              context2.onClose();
            }
            isPointerDownRef.current = true;
            document.addEventListener("pointerup", handlePointerUp, { once: true });
          }),
          onFocus: composeEventHandlers(props.onFocus, () => {
            if (!isPointerDownRef.current) context2.onOpen();
          }),
          onBlur: composeEventHandlers(props.onBlur, context2.onClose),
          onClick: composeEventHandlers(props.onClick, context2.onClose)
        }
      ) });
    }
  );
  TooltipTrigger.displayName = TRIGGER_NAME3;
  var PORTAL_NAME5 = "TooltipPortal";
  var [PortalProvider3, usePortalContext3] = createTooltipContext(PORTAL_NAME5, {
    forceMount: void 0
  });
  var TooltipPortal = (props) => {
    const { __scopeTooltip, forceMount, children, container } = props;
    const context2 = useTooltipContext(PORTAL_NAME5, __scopeTooltip);
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(PortalProvider3, { scope: __scopeTooltip, forceMount, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Presence, { present: forceMount || context2.open, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Portal, { asChild: true, container, children }) }) });
  };
  TooltipPortal.displayName = PORTAL_NAME5;
  var CONTENT_NAME5 = "TooltipContent";
  var TooltipContent = React56.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext3(CONTENT_NAME5, props.__scopeTooltip);
      const { forceMount = portalContext.forceMount, side = "top", ...contentProps } = props;
      const context2 = useTooltipContext(CONTENT_NAME5, props.__scopeTooltip);
      return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Presence, { present: forceMount || context2.open, children: context2.disableHoverableContent ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(TooltipContentImpl, { side, ...contentProps, ref: forwardedRef }) : /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(TooltipContentHoverable, { side, ...contentProps, ref: forwardedRef }) });
    }
  );
  var TooltipContentHoverable = React56.forwardRef((props, forwardedRef) => {
    const context2 = useTooltipContext(CONTENT_NAME5, props.__scopeTooltip);
    const providerContext = useTooltipProviderContext(CONTENT_NAME5, props.__scopeTooltip);
    const ref = React56.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    const [pointerGraceArea, setPointerGraceArea] = React56.useState(null);
    const { trigger, onClose } = context2;
    const content = ref.current;
    const { onPointerInTransitChange } = providerContext;
    const handleRemoveGraceArea = React56.useCallback(() => {
      setPointerGraceArea(null);
      onPointerInTransitChange(false);
    }, [onPointerInTransitChange]);
    const handleCreateGraceArea = React56.useCallback(
      (event, hoverTarget) => {
        const currentTarget = event.currentTarget;
        const exitPoint = { x: event.clientX, y: event.clientY };
        const exitSide = getExitSideFromRect(exitPoint, currentTarget.getBoundingClientRect());
        const paddedExitPoints = getPaddedExitPoints(exitPoint, exitSide);
        const hoverTargetPoints = getPointsFromRect(hoverTarget.getBoundingClientRect());
        const graceArea = getHull([...paddedExitPoints, ...hoverTargetPoints]);
        setPointerGraceArea(graceArea);
        onPointerInTransitChange(true);
      },
      [onPointerInTransitChange]
    );
    React56.useEffect(() => {
      return () => handleRemoveGraceArea();
    }, [handleRemoveGraceArea]);
    React56.useEffect(() => {
      if (trigger && content) {
        const handleTriggerLeave = (event) => handleCreateGraceArea(event, content);
        const handleContentLeave = (event) => handleCreateGraceArea(event, trigger);
        trigger.addEventListener("pointerleave", handleTriggerLeave);
        content.addEventListener("pointerleave", handleContentLeave);
        return () => {
          trigger.removeEventListener("pointerleave", handleTriggerLeave);
          content.removeEventListener("pointerleave", handleContentLeave);
        };
      }
    }, [trigger, content, handleCreateGraceArea, handleRemoveGraceArea]);
    React56.useEffect(() => {
      if (pointerGraceArea) {
        const handleTrackPointerGrace = (event) => {
          const target = event.target;
          const pointerPosition = { x: event.clientX, y: event.clientY };
          const hasEnteredTarget = trigger?.contains(target) || content?.contains(target);
          const isPointerOutsideGraceArea = !isPointInPolygon2(pointerPosition, pointerGraceArea);
          if (hasEnteredTarget) {
            handleRemoveGraceArea();
          } else if (isPointerOutsideGraceArea) {
            handleRemoveGraceArea();
            onClose();
          }
        };
        document.addEventListener("pointermove", handleTrackPointerGrace);
        return () => document.removeEventListener("pointermove", handleTrackPointerGrace);
      }
    }, [trigger, content, pointerGraceArea, onClose, handleRemoveGraceArea]);
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(TooltipContentImpl, { ...props, ref: composedRefs });
  });
  var [VisuallyHiddenContentContextProvider, useVisuallyHiddenContentContext] = createTooltipContext(TOOLTIP_NAME, { isInside: false });
  var Slottable = createSlottable("TooltipContent");
  var TooltipContentImpl = React56.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeTooltip,
        children,
        "aria-label": ariaLabel,
        onEscapeKeyDown,
        onPointerDownOutside,
        ...contentProps
      } = props;
      const context2 = useTooltipContext(CONTENT_NAME5, __scopeTooltip);
      const popperScope = usePopperScope3(__scopeTooltip);
      const { onClose } = context2;
      React56.useEffect(() => {
        document.addEventListener(TOOLTIP_OPEN, onClose);
        return () => document.removeEventListener(TOOLTIP_OPEN, onClose);
      }, [onClose]);
      React56.useEffect(() => {
        if (context2.trigger) {
          const handleScroll2 = (event) => {
            const target = event.target;
            if (target?.contains(context2.trigger)) onClose();
          };
          window.addEventListener("scroll", handleScroll2, { capture: true });
          return () => window.removeEventListener("scroll", handleScroll2, { capture: true });
        }
      }, [context2.trigger, onClose]);
      return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
        DismissableLayer,
        {
          asChild: true,
          disableOutsidePointerEvents: false,
          onEscapeKeyDown,
          onPointerDownOutside,
          onFocusOutside: (event) => event.preventDefault(),
          onDismiss: onClose,
          children: /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(
            Content4,
            {
              "data-state": context2.stateAttribute,
              ...popperScope,
              ...contentProps,
              ref: forwardedRef,
              style: {
                ...contentProps.style,
                // re-namespace exposed content custom properties
                ...{
                  "--radix-tooltip-content-transform-origin": "var(--radix-popper-transform-origin)",
                  "--radix-tooltip-content-available-width": "var(--radix-popper-available-width)",
                  "--radix-tooltip-content-available-height": "var(--radix-popper-available-height)",
                  "--radix-tooltip-trigger-width": "var(--radix-popper-anchor-width)",
                  "--radix-tooltip-trigger-height": "var(--radix-popper-anchor-height)"
                }
              },
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Slottable, { children }),
                /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(VisuallyHiddenContentContextProvider, { scope: __scopeTooltip, isInside: true, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Root3, { id: context2.contentId, role: "tooltip", children: ariaLabel || children }) })
              ]
            }
          )
        }
      );
    }
  );
  TooltipContent.displayName = CONTENT_NAME5;
  var ARROW_NAME5 = "TooltipArrow";
  var TooltipArrow = React56.forwardRef(
    (props, forwardedRef) => {
      const { __scopeTooltip, ...arrowProps } = props;
      const popperScope = usePopperScope3(__scopeTooltip);
      const visuallyHiddenContentContext = useVisuallyHiddenContentContext(
        ARROW_NAME5,
        __scopeTooltip
      );
      return visuallyHiddenContentContext.isInside ? null : /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Arrow2, { ...popperScope, ...arrowProps, ref: forwardedRef });
    }
  );
  TooltipArrow.displayName = ARROW_NAME5;
  function getExitSideFromRect(point, rect) {
    const top = Math.abs(rect.top - point.y);
    const bottom = Math.abs(rect.bottom - point.y);
    const right = Math.abs(rect.right - point.x);
    const left = Math.abs(rect.left - point.x);
    switch (Math.min(top, bottom, right, left)) {
      case left:
        return "left";
      case right:
        return "right";
      case top:
        return "top";
      case bottom:
        return "bottom";
      default:
        throw new Error("unreachable");
    }
  }
  function getPaddedExitPoints(exitPoint, exitSide, padding = 5) {
    const paddedExitPoints = [];
    switch (exitSide) {
      case "top":
        paddedExitPoints.push(
          { x: exitPoint.x - padding, y: exitPoint.y + padding },
          { x: exitPoint.x + padding, y: exitPoint.y + padding }
        );
        break;
      case "bottom":
        paddedExitPoints.push(
          { x: exitPoint.x - padding, y: exitPoint.y - padding },
          { x: exitPoint.x + padding, y: exitPoint.y - padding }
        );
        break;
      case "left":
        paddedExitPoints.push(
          { x: exitPoint.x + padding, y: exitPoint.y - padding },
          { x: exitPoint.x + padding, y: exitPoint.y + padding }
        );
        break;
      case "right":
        paddedExitPoints.push(
          { x: exitPoint.x - padding, y: exitPoint.y - padding },
          { x: exitPoint.x - padding, y: exitPoint.y + padding }
        );
        break;
    }
    return paddedExitPoints;
  }
  function getPointsFromRect(rect) {
    const { top, right, bottom, left } = rect;
    return [
      { x: left, y: top },
      { x: right, y: top },
      { x: right, y: bottom },
      { x: left, y: bottom }
    ];
  }
  function isPointInPolygon2(point, polygon) {
    const { x, y } = point;
    let inside = false;
    for (let i = 0, j = polygon.length - 1; i < polygon.length; j = i++) {
      const ii = polygon[i];
      const jj = polygon[j];
      const xi = ii.x;
      const yi = ii.y;
      const xj = jj.x;
      const yj = jj.y;
      const intersect = yi > y !== yj > y && x < (xj - xi) * (y - yi) / (yj - yi) + xi;
      if (intersect) inside = !inside;
    }
    return inside;
  }
  function getHull(points) {
    const newPoints = points.slice();
    newPoints.sort((a, b) => {
      if (a.x < b.x) return -1;
      else if (a.x > b.x) return 1;
      else if (a.y < b.y) return -1;
      else if (a.y > b.y) return 1;
      else return 0;
    });
    return getHullPresorted(newPoints);
  }
  function getHullPresorted(points) {
    if (points.length <= 1) return points.slice();
    const upperHull = [];
    for (let i = 0; i < points.length; i++) {
      const p2 = points[i];
      while (upperHull.length >= 2) {
        const q = upperHull[upperHull.length - 1];
        const r2 = upperHull[upperHull.length - 2];
        if ((q.x - r2.x) * (p2.y - r2.y) >= (q.y - r2.y) * (p2.x - r2.x)) upperHull.pop();
        else break;
      }
      upperHull.push(p2);
    }
    upperHull.pop();
    const lowerHull = [];
    for (let i = points.length - 1; i >= 0; i--) {
      const p2 = points[i];
      while (lowerHull.length >= 2) {
        const q = lowerHull[lowerHull.length - 1];
        const r2 = lowerHull[lowerHull.length - 2];
        if ((q.x - r2.x) * (p2.y - r2.y) >= (q.y - r2.y) * (p2.x - r2.x)) lowerHull.pop();
        else break;
      }
      lowerHull.push(p2);
    }
    lowerHull.pop();
    if (upperHull.length === 1 && lowerHull.length === 1 && upperHull[0].x === lowerHull[0].x && upperHull[0].y === lowerHull[0].y) {
      return upperHull;
    } else {
      return upperHull.concat(lowerHull);
    }
  }
  var Provider2 = TooltipProvider;
  var Root33 = Tooltip;
  var Trigger7 = TooltipTrigger;
  var Content25 = TooltipContent;

  // src/components/ui/tooltip/Tooltip.tsx
  var Provider3 = (props) => /* @__PURE__ */ React.createElement(Provider2, { "data-slot": "tooltip-provider", ...props });
  var Trigger8 = ({ ref, ...props }) => /* @__PURE__ */ React.createElement(Trigger7, { ref, "data-slot": "tooltip-trigger", ...props });
  var Content7 = ({ className, sideOffset = 4, ...props }) => /* @__PURE__ */ React.createElement(
    Content25,
    {
      "data-slot": "tooltip-content",
      sideOffset,
      className: cn(
        "bg-background text-foreground border-muted-200 z-50 overflow-hidden rounded-md border px-3 py-1.5 text-xs shadow",
        "animate-in fade-in-0 zoom-in-95",
        "data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95",
        "data-[side=top]:slide-in-from-bottom-2 data-[side=bottom]:slide-in-from-top-2",
        "data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2",
        className
      ),
      ...props
    }
  );
  var Tooltip2 = Root33;
  Tooltip2.Provider = Provider3;
  Tooltip2.Trigger = (0, import_react58.forwardRef)((props, ref) => Trigger8({ ...props, ref }));
  Tooltip2.Content = Content7;
  Tooltip2.Trigger.displayName = "TooltipTrigger";
  Content7.displayName = Content25.displayName;
  var Tooltip_default = Tooltip2;

  // src/components/ui/collapsible/index.ts
  init_define_import_meta_env();

  // src/components/ui/collapsible/Collapsible.tsx
  init_define_import_meta_env();
  var import_react59 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-collapsible/dist/index.mjs
  init_define_import_meta_env();
  var React57 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime29 = __toESM(require_react_shim(), 1);
  var COLLAPSIBLE_NAME = "Collapsible";
  var [createCollapsibleContext, createCollapsibleScope] = createContextScope(COLLAPSIBLE_NAME);
  var [CollapsibleProvider, useCollapsibleContext] = createCollapsibleContext(COLLAPSIBLE_NAME);
  var Collapsible = React57.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeCollapsible,
        open: openProp,
        defaultOpen,
        disabled,
        onOpenChange,
        ...collapsibleProps
      } = props;
      const [open, setOpen] = useControllableState({
        prop: openProp,
        defaultProp: defaultOpen ?? false,
        onChange: onOpenChange,
        caller: COLLAPSIBLE_NAME
      });
      return /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(
        CollapsibleProvider,
        {
          scope: __scopeCollapsible,
          disabled,
          contentId: useId5(),
          open,
          onOpenToggle: React57.useCallback(() => setOpen((prevOpen) => !prevOpen), [setOpen]),
          children: /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(
            Primitive2.div,
            {
              "data-state": getState2(open),
              "data-disabled": disabled ? "" : void 0,
              ...collapsibleProps,
              ref: forwardedRef
            }
          )
        }
      );
    }
  );
  Collapsible.displayName = COLLAPSIBLE_NAME;
  var TRIGGER_NAME4 = "CollapsibleTrigger";
  var CollapsibleTrigger = React57.forwardRef(
    (props, forwardedRef) => {
      const { __scopeCollapsible, ...triggerProps } = props;
      const context2 = useCollapsibleContext(TRIGGER_NAME4, __scopeCollapsible);
      return /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(
        Primitive2.button,
        {
          type: "button",
          "aria-controls": context2.contentId,
          "aria-expanded": context2.open || false,
          "data-state": getState2(context2.open),
          "data-disabled": context2.disabled ? "" : void 0,
          disabled: context2.disabled,
          ...triggerProps,
          ref: forwardedRef,
          onClick: composeEventHandlers(props.onClick, context2.onOpenToggle)
        }
      );
    }
  );
  CollapsibleTrigger.displayName = TRIGGER_NAME4;
  var CONTENT_NAME6 = "CollapsibleContent";
  var CollapsibleContent = React57.forwardRef(
    (props, forwardedRef) => {
      const { forceMount, ...contentProps } = props;
      const context2 = useCollapsibleContext(CONTENT_NAME6, props.__scopeCollapsible);
      return /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(Presence, { present: forceMount || context2.open, children: ({ present }) => /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(CollapsibleContentImpl, { ...contentProps, ref: forwardedRef, present }) });
    }
  );
  CollapsibleContent.displayName = CONTENT_NAME6;
  var CollapsibleContentImpl = React57.forwardRef((props, forwardedRef) => {
    const { __scopeCollapsible, present, children, ...contentProps } = props;
    const context2 = useCollapsibleContext(CONTENT_NAME6, __scopeCollapsible);
    const [isPresent, setIsPresent] = React57.useState(present);
    const ref = React57.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    const heightRef = React57.useRef(0);
    const height = heightRef.current;
    const widthRef = React57.useRef(0);
    const width = widthRef.current;
    const isOpen = context2.open || isPresent;
    const isMountAnimationPreventedRef = React57.useRef(isOpen);
    const originalStylesRef = React57.useRef(void 0);
    React57.useEffect(() => {
      const rAF = requestAnimationFrame(() => isMountAnimationPreventedRef.current = false);
      return () => cancelAnimationFrame(rAF);
    }, []);
    useLayoutEffect22(() => {
      const node = ref.current;
      if (node) {
        originalStylesRef.current = originalStylesRef.current || {
          transitionDuration: node.style.transitionDuration,
          animationName: node.style.animationName
        };
        node.style.transitionDuration = "0s";
        node.style.animationName = "none";
        const rect = node.getBoundingClientRect();
        heightRef.current = rect.height;
        widthRef.current = rect.width;
        if (!isMountAnimationPreventedRef.current) {
          node.style.transitionDuration = originalStylesRef.current.transitionDuration;
          node.style.animationName = originalStylesRef.current.animationName;
        }
        setIsPresent(present);
      }
    }, [context2.open, present]);
    return /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(
      Primitive2.div,
      {
        "data-state": getState2(context2.open),
        "data-disabled": context2.disabled ? "" : void 0,
        id: context2.contentId,
        hidden: !isOpen,
        ...contentProps,
        ref: composedRefs,
        style: {
          [`--radix-collapsible-content-height`]: height ? `${height}px` : void 0,
          [`--radix-collapsible-content-width`]: width ? `${width}px` : void 0,
          ...props.style
        },
        children: isOpen && children
      }
    );
  });
  function getState2(open) {
    return open ? "open" : "closed";
  }
  var Root7 = Collapsible;
  var Trigger9 = CollapsibleTrigger;
  var Content8 = CollapsibleContent;

  // src/components/ui/collapsible/Collapsible.tsx
  var CollapsibleTrigger2 = ({ ref, className, ...props }) => /* @__PURE__ */ React.createElement(Trigger9, { ref, "data-slot": "collapsible-trigger", className: cn("cursor-pointer", className), ...props });
  var CollapsibleContent2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    Content8,
    {
      "data-slot": "collapsible-content",
      className: cn(
        "data-[state=closed]:animate-collapsible-up data-[state=open]:animate-collapsible-down overflow-hidden transition-all",
        className
      ),
      ...props
    }
  );
  var Collapsible2 = (props) => /* @__PURE__ */ React.createElement(Root7, { "data-slot": "collapsible", ...props });
  Collapsible2.Trigger = (0, import_react59.forwardRef)((props, ref) => CollapsibleTrigger2({ ...props, ref }));
  Collapsible2.Content = CollapsibleContent2;
  Collapsible2.Trigger.displayName = "CollapsibleTrigger";
  Collapsible2.Content.displayName = "CollapsibleContent";
  Collapsible2.displayName = "Collapsible";
  var Collapsible_default = Collapsible2;

  // src/components/ui/accordion/index.ts
  init_define_import_meta_env();

  // src/components/ui/accordion/Accordion.tsx
  init_define_import_meta_env();

  // node_modules/@radix-ui/react-accordion/dist/index.mjs
  init_define_import_meta_env();
  var import_react60 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime30 = __toESM(require_react_shim(), 1);
  var ACCORDION_NAME = "Accordion";
  var ACCORDION_KEYS = ["Home", "End", "ArrowDown", "ArrowUp", "ArrowLeft", "ArrowRight"];
  var [Collection4, useCollection4, createCollectionScope4] = createCollection(ACCORDION_NAME);
  var [createAccordionContext, createAccordionScope] = createContextScope(ACCORDION_NAME, [
    createCollectionScope4,
    createCollapsibleScope
  ]);
  var useCollapsibleScope = createCollapsibleScope();
  var Accordion = import_react60.default.forwardRef(
    (props, forwardedRef) => {
      const { type, ...accordionProps } = props;
      const singleProps = accordionProps;
      const multipleProps = accordionProps;
      return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(Collection4.Provider, { scope: props.__scopeAccordion, children: type === "multiple" ? /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(AccordionImplMultiple, { ...multipleProps, ref: forwardedRef }) : /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(AccordionImplSingle, { ...singleProps, ref: forwardedRef }) });
    }
  );
  Accordion.displayName = ACCORDION_NAME;
  var [AccordionValueProvider, useAccordionValueContext] = createAccordionContext(ACCORDION_NAME);
  var [AccordionCollapsibleProvider, useAccordionCollapsibleContext] = createAccordionContext(
    ACCORDION_NAME,
    { collapsible: false }
  );
  var AccordionImplSingle = import_react60.default.forwardRef(
    (props, forwardedRef) => {
      const {
        value: valueProp,
        defaultValue,
        onValueChange = () => {
        },
        collapsible = false,
        ...accordionSingleProps
      } = props;
      const [value, setValue] = useControllableState({
        prop: valueProp,
        defaultProp: defaultValue ?? "",
        onChange: onValueChange,
        caller: ACCORDION_NAME
      });
      return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
        AccordionValueProvider,
        {
          scope: props.__scopeAccordion,
          value: import_react60.default.useMemo(() => value ? [value] : [], [value]),
          onItemOpen: setValue,
          onItemClose: import_react60.default.useCallback(() => collapsible && setValue(""), [collapsible, setValue]),
          children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(AccordionCollapsibleProvider, { scope: props.__scopeAccordion, collapsible, children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(AccordionImpl, { ...accordionSingleProps, ref: forwardedRef }) })
        }
      );
    }
  );
  var AccordionImplMultiple = import_react60.default.forwardRef((props, forwardedRef) => {
    const {
      value: valueProp,
      defaultValue,
      onValueChange = () => {
      },
      ...accordionMultipleProps
    } = props;
    const [value, setValue] = useControllableState({
      prop: valueProp,
      defaultProp: defaultValue ?? [],
      onChange: onValueChange,
      caller: ACCORDION_NAME
    });
    const handleItemOpen = import_react60.default.useCallback(
      (itemValue) => setValue((prevValue = []) => [...prevValue, itemValue]),
      [setValue]
    );
    const handleItemClose = import_react60.default.useCallback(
      (itemValue) => setValue((prevValue = []) => prevValue.filter((value2) => value2 !== itemValue)),
      [setValue]
    );
    return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
      AccordionValueProvider,
      {
        scope: props.__scopeAccordion,
        value,
        onItemOpen: handleItemOpen,
        onItemClose: handleItemClose,
        children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(AccordionCollapsibleProvider, { scope: props.__scopeAccordion, collapsible: true, children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(AccordionImpl, { ...accordionMultipleProps, ref: forwardedRef }) })
      }
    );
  });
  var [AccordionImplProvider, useAccordionContext] = createAccordionContext(ACCORDION_NAME);
  var AccordionImpl = import_react60.default.forwardRef(
    (props, forwardedRef) => {
      const { __scopeAccordion, disabled, dir: dir2, orientation = "vertical", ...accordionProps } = props;
      const accordionRef = import_react60.default.useRef(null);
      const composedRefs = useComposedRefs(accordionRef, forwardedRef);
      const getItems = useCollection4(__scopeAccordion);
      const direction = useDirection(dir2);
      const isDirectionLTR = direction === "ltr";
      const handleKeyDown = composeEventHandlers(props.onKeyDown, (event) => {
        if (!ACCORDION_KEYS.includes(event.key)) return;
        const target = event.target;
        const triggerCollection = getItems().filter((item) => !item.ref.current?.disabled);
        const triggerIndex = triggerCollection.findIndex((item) => item.ref.current === target);
        const triggerCount = triggerCollection.length;
        if (triggerIndex === -1) return;
        event.preventDefault();
        let nextIndex = triggerIndex;
        const homeIndex = 0;
        const endIndex = triggerCount - 1;
        const moveNext = () => {
          nextIndex = triggerIndex + 1;
          if (nextIndex > endIndex) {
            nextIndex = homeIndex;
          }
        };
        const movePrev = () => {
          nextIndex = triggerIndex - 1;
          if (nextIndex < homeIndex) {
            nextIndex = endIndex;
          }
        };
        switch (event.key) {
          case "Home":
            nextIndex = homeIndex;
            break;
          case "End":
            nextIndex = endIndex;
            break;
          case "ArrowRight":
            if (orientation === "horizontal") {
              if (isDirectionLTR) {
                moveNext();
              } else {
                movePrev();
              }
            }
            break;
          case "ArrowDown":
            if (orientation === "vertical") {
              moveNext();
            }
            break;
          case "ArrowLeft":
            if (orientation === "horizontal") {
              if (isDirectionLTR) {
                movePrev();
              } else {
                moveNext();
              }
            }
            break;
          case "ArrowUp":
            if (orientation === "vertical") {
              movePrev();
            }
            break;
        }
        const clampedIndex = nextIndex % triggerCount;
        triggerCollection[clampedIndex].ref.current?.focus();
      });
      return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
        AccordionImplProvider,
        {
          scope: __scopeAccordion,
          disabled,
          direction: dir2,
          orientation,
          children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(Collection4.Slot, { scope: __scopeAccordion, children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
            Primitive2.div,
            {
              ...accordionProps,
              "data-orientation": orientation,
              ref: composedRefs,
              onKeyDown: disabled ? void 0 : handleKeyDown
            }
          ) })
        }
      );
    }
  );
  var ITEM_NAME4 = "AccordionItem";
  var [AccordionItemProvider, useAccordionItemContext] = createAccordionContext(ITEM_NAME4);
  var AccordionItem = import_react60.default.forwardRef(
    (props, forwardedRef) => {
      const { __scopeAccordion, value, ...accordionItemProps } = props;
      const accordionContext = useAccordionContext(ITEM_NAME4, __scopeAccordion);
      const valueContext = useAccordionValueContext(ITEM_NAME4, __scopeAccordion);
      const collapsibleScope = useCollapsibleScope(__scopeAccordion);
      const triggerId = useId5();
      const open = value && valueContext.value.includes(value) || false;
      const disabled = accordionContext.disabled || props.disabled;
      return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
        AccordionItemProvider,
        {
          scope: __scopeAccordion,
          open,
          disabled,
          triggerId,
          children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
            Root7,
            {
              "data-orientation": accordionContext.orientation,
              "data-state": getState3(open),
              ...collapsibleScope,
              ...accordionItemProps,
              ref: forwardedRef,
              disabled,
              open,
              onOpenChange: (open2) => {
                if (open2) {
                  valueContext.onItemOpen(value);
                } else {
                  valueContext.onItemClose(value);
                }
              }
            }
          )
        }
      );
    }
  );
  AccordionItem.displayName = ITEM_NAME4;
  var HEADER_NAME = "AccordionHeader";
  var AccordionHeader = import_react60.default.forwardRef(
    (props, forwardedRef) => {
      const { __scopeAccordion, ...headerProps } = props;
      const accordionContext = useAccordionContext(ACCORDION_NAME, __scopeAccordion);
      const itemContext = useAccordionItemContext(HEADER_NAME, __scopeAccordion);
      return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
        Primitive2.h3,
        {
          "data-orientation": accordionContext.orientation,
          "data-state": getState3(itemContext.open),
          "data-disabled": itemContext.disabled ? "" : void 0,
          ...headerProps,
          ref: forwardedRef
        }
      );
    }
  );
  AccordionHeader.displayName = HEADER_NAME;
  var TRIGGER_NAME5 = "AccordionTrigger";
  var AccordionTrigger = import_react60.default.forwardRef(
    (props, forwardedRef) => {
      const { __scopeAccordion, ...triggerProps } = props;
      const accordionContext = useAccordionContext(ACCORDION_NAME, __scopeAccordion);
      const itemContext = useAccordionItemContext(TRIGGER_NAME5, __scopeAccordion);
      const collapsibleContext = useAccordionCollapsibleContext(TRIGGER_NAME5, __scopeAccordion);
      const collapsibleScope = useCollapsibleScope(__scopeAccordion);
      return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(Collection4.ItemSlot, { scope: __scopeAccordion, children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
        Trigger9,
        {
          "aria-disabled": itemContext.open && !collapsibleContext.collapsible || void 0,
          "data-orientation": accordionContext.orientation,
          id: itemContext.triggerId,
          ...collapsibleScope,
          ...triggerProps,
          ref: forwardedRef
        }
      ) });
    }
  );
  AccordionTrigger.displayName = TRIGGER_NAME5;
  var CONTENT_NAME7 = "AccordionContent";
  var AccordionContent = import_react60.default.forwardRef(
    (props, forwardedRef) => {
      const { __scopeAccordion, ...contentProps } = props;
      const accordionContext = useAccordionContext(ACCORDION_NAME, __scopeAccordion);
      const itemContext = useAccordionItemContext(CONTENT_NAME7, __scopeAccordion);
      const collapsibleScope = useCollapsibleScope(__scopeAccordion);
      return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
        Content8,
        {
          role: "region",
          "aria-labelledby": itemContext.triggerId,
          "data-orientation": accordionContext.orientation,
          ...collapsibleScope,
          ...contentProps,
          ref: forwardedRef,
          style: {
            ["--radix-accordion-content-height"]: "var(--radix-collapsible-content-height)",
            ["--radix-accordion-content-width"]: "var(--radix-collapsible-content-width)",
            ...props.style
          }
        }
      );
    }
  );
  AccordionContent.displayName = CONTENT_NAME7;
  function getState3(open) {
    return open ? "open" : "closed";
  }
  var Root26 = Accordion;
  var Item7 = AccordionItem;
  var Header6 = AccordionHeader;
  var Trigger22 = AccordionTrigger;
  var Content26 = AccordionContent;

  // src/components/ui/accordion/Accordion.tsx
  var Trigger10 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Header6, { className: "flex" }, /* @__PURE__ */ React.createElement(
    Trigger22,
    {
      "data-slot": "accordion-trigger",
      className: cn(
        "flex flex-1 cursor-pointer items-center justify-between py-4 font-medium transition-all hover:underline [&[data-state=open]>svg]:rotate-180",
        className
      ),
      ...props
    },
    children,
    /* @__PURE__ */ React.createElement(ChevronDown, { className: "bg-primary-400 text-background h-6 w-6 shrink-0 rounded-sm transition-transform duration-200" })
  ));
  var Item8 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    Item7,
    {
      "data-slot": "accordion-item",
      className: cn(
        "bg-background border-b-muted border-b px-4 first:rounded-ss-md first:rounded-se-md last:rounded-ee-md last:rounded-es-md last:border-b-0",
        className
      ),
      ...props
    }
  );
  var Content9 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    Content26,
    {
      "data-slot": "accordion-content",
      className: "data-[state=closed]:animate-accordion-up data-[state=open]:animate-accordion-down overflow-hidden text-sm transition-all",
      ...props
    },
    /* @__PURE__ */ React.createElement("div", { className: cn("pt-0 pb-4", className) }, children)
  );
  var Accordion2 = Root26;
  Accordion2.Trigger = Trigger10;
  Accordion2.Item = Item8;
  Accordion2.Content = Content9;
  Item8.displayName = "AccordionItem";
  Trigger10.displayName = Trigger22.displayName;
  Content9.displayName = Content26.displayName;
  var Accordion_default = Accordion2;

  // src/components/ui/dialog/index.ts
  init_define_import_meta_env();

  // src/components/ui/dialog/Dialog.tsx
  init_define_import_meta_env();
  var import_react61 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-dialog/dist/index.mjs
  init_define_import_meta_env();
  var React60 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-dialog/node_modules/@radix-ui/react-slot/dist/index.mjs
  init_define_import_meta_env();
  var React59 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime31 = __toESM(require_react_shim(), 1);
  // @__NO_SIDE_EFFECTS__
  function createSlot6(ownerName) {
    const SlotClone = /* @__PURE__ */ createSlotClone6(ownerName);
    const Slot22 = React59.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      const childrenArray = React59.Children.toArray(children);
      const slottable = childrenArray.find(isSlottable6);
      if (slottable) {
        const newElement = slottable.props.children;
        const newChildren = childrenArray.map((child) => {
          if (child === slottable) {
            if (React59.Children.count(newElement) > 1) return React59.Children.only(null);
            return React59.isValidElement(newElement) ? newElement.props.children : null;
          } else {
            return child;
          }
        });
        return /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children: React59.isValidElement(newElement) ? React59.cloneElement(newElement, void 0, newChildren) : null });
      }
      return /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children });
    });
    Slot22.displayName = `${ownerName}.Slot`;
    return Slot22;
  }
  // @__NO_SIDE_EFFECTS__
  function createSlotClone6(ownerName) {
    const SlotClone = React59.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      if (React59.isValidElement(children)) {
        const childrenRef = getElementRef7(children);
        const props2 = mergeProps6(slotProps, children.props);
        if (children.type !== React59.Fragment) {
          props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
        }
        return React59.cloneElement(children, props2);
      }
      return React59.Children.count(children) > 1 ? React59.Children.only(null) : null;
    });
    SlotClone.displayName = `${ownerName}.SlotClone`;
    return SlotClone;
  }
  var SLOTTABLE_IDENTIFIER7 = /* @__PURE__ */ Symbol("radix.slottable");
  function isSlottable6(child) {
    return React59.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER7;
  }
  function mergeProps6(slotProps, childProps) {
    const overrideProps = { ...childProps };
    for (const propName in childProps) {
      const slotPropValue = slotProps[propName];
      const childPropValue = childProps[propName];
      const isHandler = /^on[A-Z]/.test(propName);
      if (isHandler) {
        if (slotPropValue && childPropValue) {
          overrideProps[propName] = (...args) => {
            const result = childPropValue(...args);
            slotPropValue(...args);
            return result;
          };
        } else if (slotPropValue) {
          overrideProps[propName] = slotPropValue;
        }
      } else if (propName === "style") {
        overrideProps[propName] = { ...slotPropValue, ...childPropValue };
      } else if (propName === "className") {
        overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
      }
    }
    return { ...slotProps, ...overrideProps };
  }
  function getElementRef7(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }

  // node_modules/@radix-ui/react-dialog/dist/index.mjs
  var import_jsx_runtime32 = __toESM(require_react_shim(), 1);
  var DIALOG_NAME = "Dialog";
  var [createDialogContext, createDialogScope] = createContextScope(DIALOG_NAME);
  var [DialogProvider, useDialogContext] = createDialogContext(DIALOG_NAME);
  var Dialog = (props) => {
    const {
      __scopeDialog,
      children,
      open: openProp,
      defaultOpen,
      onOpenChange,
      modal = true
    } = props;
    const triggerRef = React60.useRef(null);
    const contentRef = React60.useRef(null);
    const [open, setOpen] = useControllableState({
      prop: openProp,
      defaultProp: defaultOpen ?? false,
      onChange: onOpenChange,
      caller: DIALOG_NAME
    });
    return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
      DialogProvider,
      {
        scope: __scopeDialog,
        triggerRef,
        contentRef,
        contentId: useId5(),
        titleId: useId5(),
        descriptionId: useId5(),
        open,
        onOpenChange: setOpen,
        onOpenToggle: React60.useCallback(() => setOpen((prevOpen) => !prevOpen), [setOpen]),
        modal,
        children
      }
    );
  };
  Dialog.displayName = DIALOG_NAME;
  var TRIGGER_NAME6 = "DialogTrigger";
  var DialogTrigger = React60.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...triggerProps } = props;
      const context2 = useDialogContext(TRIGGER_NAME6, __scopeDialog);
      const composedTriggerRef = useComposedRefs(forwardedRef, context2.triggerRef);
      return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
        Primitive2.button,
        {
          type: "button",
          "aria-haspopup": "dialog",
          "aria-expanded": context2.open,
          "aria-controls": context2.contentId,
          "data-state": getState4(context2.open),
          ...triggerProps,
          ref: composedTriggerRef,
          onClick: composeEventHandlers(props.onClick, context2.onOpenToggle)
        }
      );
    }
  );
  DialogTrigger.displayName = TRIGGER_NAME6;
  var PORTAL_NAME6 = "DialogPortal";
  var [PortalProvider4, usePortalContext4] = createDialogContext(PORTAL_NAME6, {
    forceMount: void 0
  });
  var DialogPortal = (props) => {
    const { __scopeDialog, forceMount, children, container } = props;
    const context2 = useDialogContext(PORTAL_NAME6, __scopeDialog);
    return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(PortalProvider4, { scope: __scopeDialog, forceMount, children: React60.Children.map(children, (child) => /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(Presence, { present: forceMount || context2.open, children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(Portal, { asChild: true, container, children: child }) })) });
  };
  DialogPortal.displayName = PORTAL_NAME6;
  var OVERLAY_NAME = "DialogOverlay";
  var DialogOverlay = React60.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext4(OVERLAY_NAME, props.__scopeDialog);
      const { forceMount = portalContext.forceMount, ...overlayProps } = props;
      const context2 = useDialogContext(OVERLAY_NAME, props.__scopeDialog);
      return context2.modal ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(Presence, { present: forceMount || context2.open, children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(DialogOverlayImpl, { ...overlayProps, ref: forwardedRef }) }) : null;
    }
  );
  DialogOverlay.displayName = OVERLAY_NAME;
  var Slot4 = createSlot6("DialogOverlay.RemoveScroll");
  var DialogOverlayImpl = React60.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...overlayProps } = props;
      const context2 = useDialogContext(OVERLAY_NAME, __scopeDialog);
      return (
        // Make sure `Content` is scrollable even when it doesn't live inside `RemoveScroll`
        // ie. when `Overlay` and `Content` are siblings
        /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(Combination_default, { as: Slot4, allowPinchZoom: true, shards: [context2.contentRef], children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
          Primitive2.div,
          {
            "data-state": getState4(context2.open),
            ...overlayProps,
            ref: forwardedRef,
            style: { pointerEvents: "auto", ...overlayProps.style }
          }
        ) })
      );
    }
  );
  var CONTENT_NAME8 = "DialogContent";
  var DialogContent = React60.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext4(CONTENT_NAME8, props.__scopeDialog);
      const { forceMount = portalContext.forceMount, ...contentProps } = props;
      const context2 = useDialogContext(CONTENT_NAME8, props.__scopeDialog);
      return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(Presence, { present: forceMount || context2.open, children: context2.modal ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(DialogContentModal, { ...contentProps, ref: forwardedRef }) : /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(DialogContentNonModal, { ...contentProps, ref: forwardedRef }) });
    }
  );
  DialogContent.displayName = CONTENT_NAME8;
  var DialogContentModal = React60.forwardRef(
    (props, forwardedRef) => {
      const context2 = useDialogContext(CONTENT_NAME8, props.__scopeDialog);
      const contentRef = React60.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, context2.contentRef, contentRef);
      React60.useEffect(() => {
        const content = contentRef.current;
        if (content) return hideOthers(content);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
        DialogContentImpl,
        {
          ...props,
          ref: composedRefs,
          trapFocus: context2.open,
          disableOutsidePointerEvents: true,
          onCloseAutoFocus: composeEventHandlers(props.onCloseAutoFocus, (event) => {
            event.preventDefault();
            context2.triggerRef.current?.focus();
          }),
          onPointerDownOutside: composeEventHandlers(props.onPointerDownOutside, (event) => {
            const originalEvent = event.detail.originalEvent;
            const ctrlLeftClick = originalEvent.button === 0 && originalEvent.ctrlKey === true;
            const isRightClick = originalEvent.button === 2 || ctrlLeftClick;
            if (isRightClick) event.preventDefault();
          }),
          onFocusOutside: composeEventHandlers(
            props.onFocusOutside,
            (event) => event.preventDefault()
          )
        }
      );
    }
  );
  var DialogContentNonModal = React60.forwardRef(
    (props, forwardedRef) => {
      const context2 = useDialogContext(CONTENT_NAME8, props.__scopeDialog);
      const hasInteractedOutsideRef = React60.useRef(false);
      const hasPointerDownOutsideRef = React60.useRef(false);
      return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
        DialogContentImpl,
        {
          ...props,
          ref: forwardedRef,
          trapFocus: false,
          disableOutsidePointerEvents: false,
          onCloseAutoFocus: (event) => {
            props.onCloseAutoFocus?.(event);
            if (!event.defaultPrevented) {
              if (!hasInteractedOutsideRef.current) context2.triggerRef.current?.focus();
              event.preventDefault();
            }
            hasInteractedOutsideRef.current = false;
            hasPointerDownOutsideRef.current = false;
          },
          onInteractOutside: (event) => {
            props.onInteractOutside?.(event);
            if (!event.defaultPrevented) {
              hasInteractedOutsideRef.current = true;
              if (event.detail.originalEvent.type === "pointerdown") {
                hasPointerDownOutsideRef.current = true;
              }
            }
            const target = event.target;
            const targetIsTrigger = context2.triggerRef.current?.contains(target);
            if (targetIsTrigger) event.preventDefault();
            if (event.detail.originalEvent.type === "focusin" && hasPointerDownOutsideRef.current) {
              event.preventDefault();
            }
          }
        }
      );
    }
  );
  var DialogContentImpl = React60.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, trapFocus, onOpenAutoFocus, onCloseAutoFocus, ...contentProps } = props;
      const context2 = useDialogContext(CONTENT_NAME8, __scopeDialog);
      const contentRef = React60.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, contentRef);
      useFocusGuards();
      return /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)(import_jsx_runtime32.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
          FocusScope,
          {
            asChild: true,
            loop: true,
            trapped: trapFocus,
            onMountAutoFocus: onOpenAutoFocus,
            onUnmountAutoFocus: onCloseAutoFocus,
            children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
              DismissableLayer,
              {
                role: "dialog",
                id: context2.contentId,
                "aria-describedby": context2.descriptionId,
                "aria-labelledby": context2.titleId,
                "data-state": getState4(context2.open),
                ...contentProps,
                ref: composedRefs,
                onDismiss: () => context2.onOpenChange(false)
              }
            )
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)(import_jsx_runtime32.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(TitleWarning, { titleId: context2.titleId }),
          /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(DescriptionWarning, { contentRef, descriptionId: context2.descriptionId })
        ] })
      ] });
    }
  );
  var TITLE_NAME2 = "DialogTitle";
  var DialogTitle = React60.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...titleProps } = props;
      const context2 = useDialogContext(TITLE_NAME2, __scopeDialog);
      return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(Primitive2.h2, { id: context2.titleId, ...titleProps, ref: forwardedRef });
    }
  );
  DialogTitle.displayName = TITLE_NAME2;
  var DESCRIPTION_NAME2 = "DialogDescription";
  var DialogDescription = React60.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...descriptionProps } = props;
      const context2 = useDialogContext(DESCRIPTION_NAME2, __scopeDialog);
      return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(Primitive2.p, { id: context2.descriptionId, ...descriptionProps, ref: forwardedRef });
    }
  );
  DialogDescription.displayName = DESCRIPTION_NAME2;
  var CLOSE_NAME3 = "DialogClose";
  var DialogClose = React60.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...closeProps } = props;
      const context2 = useDialogContext(CLOSE_NAME3, __scopeDialog);
      return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
        Primitive2.button,
        {
          type: "button",
          ...closeProps,
          ref: forwardedRef,
          onClick: composeEventHandlers(props.onClick, () => context2.onOpenChange(false))
        }
      );
    }
  );
  DialogClose.displayName = CLOSE_NAME3;
  function getState4(open) {
    return open ? "open" : "closed";
  }
  var TITLE_WARNING_NAME = "DialogTitleWarning";
  var [WarningProvider, useWarningContext] = createContext22(TITLE_WARNING_NAME, {
    contentName: CONTENT_NAME8,
    titleName: TITLE_NAME2,
    docsSlug: "dialog"
  });
  var TitleWarning = ({ titleId }) => {
    const titleWarningContext = useWarningContext(TITLE_WARNING_NAME);
    const MESSAGE = `\`${titleWarningContext.contentName}\` requires a \`${titleWarningContext.titleName}\` for the component to be accessible for screen reader users.

If you want to hide the \`${titleWarningContext.titleName}\`, you can wrap it with our VisuallyHidden component.

For more information, see https://radix-ui.com/primitives/docs/components/${titleWarningContext.docsSlug}`;
    React60.useEffect(() => {
      if (titleId) {
        const hasTitle = document.getElementById(titleId);
        if (!hasTitle) console.error(MESSAGE);
      }
    }, [MESSAGE, titleId]);
    return null;
  };
  var DESCRIPTION_WARNING_NAME = "DialogDescriptionWarning";
  var DescriptionWarning = ({ contentRef, descriptionId }) => {
    const descriptionWarningContext = useWarningContext(DESCRIPTION_WARNING_NAME);
    const MESSAGE = `Warning: Missing \`Description\` or \`aria-describedby={undefined}\` for {${descriptionWarningContext.contentName}}.`;
    React60.useEffect(() => {
      const describedById = contentRef.current?.getAttribute("aria-describedby");
      if (descriptionId && describedById) {
        const hasDescription = document.getElementById(descriptionId);
        if (!hasDescription) console.warn(MESSAGE);
      }
    }, [MESSAGE, contentRef, descriptionId]);
    return null;
  };
  var Root8 = Dialog;
  var Trigger11 = DialogTrigger;
  var Portal4 = DialogPortal;
  var Overlay = DialogOverlay;
  var Content10 = DialogContent;
  var Title8 = DialogTitle;
  var Description7 = DialogDescription;
  var Close2 = DialogClose;

  // src/components/ui/dialog/Dialog.tsx
  var DialogOverlay2 = ({ ref, className, ...props }) => /* @__PURE__ */ React.createElement(
    Overlay,
    {
      ref,
      "data-slot": "dialog-overlay",
      className: cn(
        "bg-foreground/20 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 fixed inset-0 z-50",
        className
      ),
      ...props
    }
  );
  var DialogTrigger2 = ({ ref, className, ...props }) => /* @__PURE__ */ React.createElement(Trigger11, { ref, "data-slot": "dialog-trigger", className: cn(className), ...props });
  var DialogPanel = ({ ref, className, children, ...props }) => /* @__PURE__ */ React.createElement(Portal4, null, /* @__PURE__ */ React.createElement(Dialog2.Overlay, null), /* @__PURE__ */ React.createElement(
    Content10,
    {
      ref,
      onWheel: (e2) => e2.stopPropagation(),
      "data-slot": "dialog-panel",
      className: cn(
        "bg-background grid w-full max-w-lg gap-6 rounded-2xl py-6 shadow-lg",
        "fixed left-1/2 top-1/2 z-50 translate-x-[-50%] translate-y-[-50%] duration-200",
        "focus:outline-none",
        "data-[state=open]:animate-in data-[state=closed]:animate-out",
        "data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0",
        "data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95",
        "data-[state=closed]:slide-out-to-bottom data-[state=open]:slide-in-from-top",
        className
      ),
      ...props
    },
    children
  ));
  var DialogClose2 = ({ ref, className, ...props }) => /* @__PURE__ */ React.createElement(Close2, { ref, "data-slot": "dialog-close", className: cn(className), ...props });
  var DialogHeader = ({ className, ...props }) => /* @__PURE__ */ React.createElement("header", { "data-slot": "dialog-header", className: cn("px-6", className), ...props });
  var DialogHeaderTitle = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    Title8,
    {
      "data-slot": "dialog-title",
      className: cn("text-foreground text-xl leading-none font-semibold", className),
      ...props
    }
  );
  var DialogHeaderDescription = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Description7, { "data-slot": "dialog-description", className: cn("text-muted-foreground text-sm", className), ...props });
  var DialogContent2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("section", { "data-slot": "dialog-content", className: cn("px-6", className), ...props });
  var DialogFooter = ({ className, ...props }) => /* @__PURE__ */ React.createElement("footer", { "data-slot": "dialog-footer", className: cn("px-6", className), ...props });
  var Dialog2 = Root8;
  Dialog2.Portal = Portal4;
  Dialog2.Overlay = (0, import_react61.forwardRef)((props, ref) => DialogOverlay2({ ...props, ref }));
  Dialog2.Trigger = (0, import_react61.forwardRef)((props, ref) => DialogTrigger2({ ...props, ref }));
  Dialog2.Panel = (0, import_react61.forwardRef)((props, ref) => DialogPanel({ ...props, ref }));
  Dialog2.Close = (0, import_react61.forwardRef)((props, ref) => DialogClose2({ ...props, ref }));
  Dialog2.Header = DialogHeader;
  Dialog2.Title = DialogHeaderTitle;
  Dialog2.Description = DialogHeaderDescription;
  Dialog2.Content = DialogContent2;
  Dialog2.Footer = DialogFooter;
  Dialog2.Overlay.displayName = Overlay.displayName;
  Dialog2.Trigger.displayName = "DialogTrigger";
  Dialog2.Panel.displayName = "DialogPanel";
  Dialog2.Close.displayName = "DialogClose";
  DialogHeader.displayName = "DialogHeader";
  DialogHeaderTitle.displayName = Title8.displayName;
  DialogHeaderDescription.displayName = Description7.displayName;
  DialogContent2.displayName = "DialogContent";
  DialogFooter.displayName = "DialogFooter";
  var Dialog_default = Dialog2;

  // src/components/ui/tabs/index.ts
  init_define_import_meta_env();

  // src/components/ui/tabs/Tabs.tsx
  init_define_import_meta_env();

  // node_modules/@radix-ui/react-tabs/dist/index.mjs
  init_define_import_meta_env();
  var React61 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime33 = __toESM(require_react_shim(), 1);
  var TABS_NAME = "Tabs";
  var [createTabsContext, createTabsScope] = createContextScope(TABS_NAME, [
    createRovingFocusGroupScope
  ]);
  var useRovingFocusGroupScope2 = createRovingFocusGroupScope();
  var [TabsProvider, useTabsContext] = createTabsContext(TABS_NAME);
  var Tabs = React61.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeTabs,
        value: valueProp,
        onValueChange,
        defaultValue,
        orientation = "horizontal",
        dir: dir2,
        activationMode = "automatic",
        ...tabsProps
      } = props;
      const direction = useDirection(dir2);
      const [value, setValue] = useControllableState({
        prop: valueProp,
        onChange: onValueChange,
        defaultProp: defaultValue ?? "",
        caller: TABS_NAME
      });
      return /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
        TabsProvider,
        {
          scope: __scopeTabs,
          baseId: useId5(),
          value,
          onValueChange: setValue,
          orientation,
          dir: direction,
          activationMode,
          children: /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
            Primitive2.div,
            {
              dir: direction,
              "data-orientation": orientation,
              ...tabsProps,
              ref: forwardedRef
            }
          )
        }
      );
    }
  );
  Tabs.displayName = TABS_NAME;
  var TAB_LIST_NAME = "TabsList";
  var TabsList = React61.forwardRef(
    (props, forwardedRef) => {
      const { __scopeTabs, loop = true, ...listProps } = props;
      const context2 = useTabsContext(TAB_LIST_NAME, __scopeTabs);
      const rovingFocusGroupScope = useRovingFocusGroupScope2(__scopeTabs);
      return /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
        Root6,
        {
          asChild: true,
          ...rovingFocusGroupScope,
          orientation: context2.orientation,
          dir: context2.dir,
          loop,
          children: /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
            Primitive2.div,
            {
              role: "tablist",
              "aria-orientation": context2.orientation,
              ...listProps,
              ref: forwardedRef
            }
          )
        }
      );
    }
  );
  TabsList.displayName = TAB_LIST_NAME;
  var TRIGGER_NAME7 = "TabsTrigger";
  var TabsTrigger = React61.forwardRef(
    (props, forwardedRef) => {
      const { __scopeTabs, value, disabled = false, ...triggerProps } = props;
      const context2 = useTabsContext(TRIGGER_NAME7, __scopeTabs);
      const rovingFocusGroupScope = useRovingFocusGroupScope2(__scopeTabs);
      const triggerId = makeTriggerId(context2.baseId, value);
      const contentId = makeContentId(context2.baseId, value);
      const isSelected = value === context2.value;
      return /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
        Item5,
        {
          asChild: true,
          ...rovingFocusGroupScope,
          focusable: !disabled,
          active: isSelected,
          children: /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
            Primitive2.button,
            {
              type: "button",
              role: "tab",
              "aria-selected": isSelected,
              "aria-controls": contentId,
              "data-state": isSelected ? "active" : "inactive",
              "data-disabled": disabled ? "" : void 0,
              disabled,
              id: triggerId,
              ...triggerProps,
              ref: forwardedRef,
              onMouseDown: composeEventHandlers(props.onMouseDown, (event) => {
                if (!disabled && event.button === 0 && event.ctrlKey === false) {
                  context2.onValueChange(value);
                } else {
                  event.preventDefault();
                }
              }),
              onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
                if ([" ", "Enter"].includes(event.key)) context2.onValueChange(value);
              }),
              onFocus: composeEventHandlers(props.onFocus, () => {
                const isAutomaticActivation = context2.activationMode !== "manual";
                if (!isSelected && !disabled && isAutomaticActivation) {
                  context2.onValueChange(value);
                }
              })
            }
          )
        }
      );
    }
  );
  TabsTrigger.displayName = TRIGGER_NAME7;
  var CONTENT_NAME9 = "TabsContent";
  var TabsContent = React61.forwardRef(
    (props, forwardedRef) => {
      const { __scopeTabs, value, forceMount, children, ...contentProps } = props;
      const context2 = useTabsContext(CONTENT_NAME9, __scopeTabs);
      const triggerId = makeTriggerId(context2.baseId, value);
      const contentId = makeContentId(context2.baseId, value);
      const isSelected = value === context2.value;
      const isMountAnimationPreventedRef = React61.useRef(isSelected);
      React61.useEffect(() => {
        const rAF = requestAnimationFrame(() => isMountAnimationPreventedRef.current = false);
        return () => cancelAnimationFrame(rAF);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(Presence, { present: forceMount || isSelected, children: ({ present }) => /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
        Primitive2.div,
        {
          "data-state": isSelected ? "active" : "inactive",
          "data-orientation": context2.orientation,
          role: "tabpanel",
          "aria-labelledby": triggerId,
          hidden: !present,
          id: contentId,
          tabIndex: 0,
          ...contentProps,
          ref: forwardedRef,
          style: {
            ...props.style,
            animationDuration: isMountAnimationPreventedRef.current ? "0s" : void 0
          },
          children: present && children
        }
      ) });
    }
  );
  TabsContent.displayName = CONTENT_NAME9;
  function makeTriggerId(baseId, value) {
    return `${baseId}-trigger-${value}`;
  }
  function makeContentId(baseId, value) {
    return `${baseId}-content-${value}`;
  }
  var Root27 = Tabs;
  var List5 = TabsList;
  var Trigger12 = TabsTrigger;
  var Content11 = TabsContent;

  // src/components/ui/tabs/Tabs.tsx
  var tabsListVariants = cva("flex items-center rounded-2xl p-1 gap-1 min-w-0 overflow-x-auto", {
    variants: {
      variant: {
        default: "bg-background text-foreground w-full border border-muted-200",
        compact: "w-fit bg-background border border-muted-200 text-foreground",
        plain: "text-foreground bg-transparent",
        compactPlain: "w-fit text-foreground bg-transparent"
      }
    },
    defaultVariants: {
      variant: "default"
    }
  });
  var tabsTriggerVariants = cva(
    `
  inline-flex cursor-pointer items-center justify-center rounded-[inherit] px-3 py-2 text-sm font-medium whitespace-nowrap transition-colors
  focus-visible:outline-hidden disabled:pointer-events-none disabled:opacity-50
  data-[state=active]:bg-primary data-[state=active]:font-bold data-[state=active]:text-surface dark:data-[state=active]:bg-primary-300 hover:bg-primary-15 hover:text-foreground
  `,
    {
      variants: {
        variant: {
          default: "grow shrink-0 flex gap-1",
          fitContent: ""
        }
      },
      defaultVariants: {
        variant: "default"
      }
    }
  );
  var List6 = ({ className, variant, ...props }) => /* @__PURE__ */ React.createElement(List5, { "data-slot": "tabs-list", className: cn(tabsListVariants({ variant }), className), ...props });
  var Trigger13 = ({ className, variant, ...props }) => /* @__PURE__ */ React.createElement(Trigger12, { "data-slot": "tabs-trigger", className: cn(tabsTriggerVariants({ variant }), className), ...props });
  var Content12 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Content11, { "data-slot": "tabs-content", className: cn("mt-6 focus-visible:outline-none", className), ...props });
  var Tabs2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Root27, { "data-slot": "tabs", className: cn("min-w-0", className), ...props });
  Tabs2.List = List6;
  Tabs2.Trigger = Trigger13;
  Tabs2.Content = Content12;
  var Tabs_default = Tabs2;

  // src/components/ui/divider/index.ts
  init_define_import_meta_env();

  // src/components/ui/divider/Divider.tsx
  init_define_import_meta_env();
  var dividerVariants = cva("border-0 m-0", {
    variants: {
      orientation: {
        horizontal: "h-px w-full",
        vertical: "w-px h-[stretch]"
      },
      variant: {
        solid: "bg-muted-200",
        dashed: "bg-transparent border-t border-muted-200 border-dashed"
      }
    },
    compoundVariants: [
      {
        orientation: "vertical",
        variant: "dashed",
        className: "border-t-0 border-s border-muted-200 border-dashed"
      }
    ],
    defaultVariants: {
      orientation: "horizontal",
      variant: "solid"
    }
  });
  var Divider = ({ className, orientation, variant, ...props }) => /* @__PURE__ */ React.createElement("hr", { "data-slot": "divider", className: cn(dividerVariants({ orientation, variant }), className), ...props });
  var Divider_default = Divider;

  // src/components/ui/table/index.ts
  init_define_import_meta_env();

  // src/components/ui/table/Table.tsx
  init_define_import_meta_env();
  var Header7 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("thead", { "data-slot": "table-header", className: cn("bg-primary-15 [&_tr]:border-muted-200 [&_tr]:border-b", className), ...props });
  var Body = ({ className, ...props }) => /* @__PURE__ */ React.createElement("tbody", { "data-slot": "table-body", className: cn("[&_tr:last-child]:border-0", className), ...props });
  var Footer5 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("tfoot", { "data-slot": "table-footer", className: cn("bg-secondary text-primary-900 font-medium", className), ...props });
  var Row = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    "tr",
    {
      "data-slot": "table-row",
      className: cn(
        "hover:bg-primary-15 data-[state=selected]:bg-primary-15 border-muted-200 bg-background border-b transition-colors",
        className
      ),
      ...props
    }
  );
  var Head = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    "th",
    {
      "data-slot": "table-head",
      className: cn(
        "bg-primary-25 text-primary-700 px-6 py-3 text-start align-middle text-xs font-bold [&:has([role=checkbox])]:pe-0",
        className
      ),
      ...props
    }
  );
  var Cell = ({ className, ...props }) => /* @__PURE__ */ React.createElement("td", { "data-slot": "table-cell", className: cn("h-18 px-6 py-3 align-middle text-xs [&:has([role=checkbox])]:pe-0", className), ...props });
  var Caption = ({ className, ...props }) => /* @__PURE__ */ React.createElement("caption", { "data-slot": "table-caption", className: cn("text-muted-foreground mt-4 text-sm", className), ...props });
  var Empty = ({
    className,
    colSpan = 1,
    title = "No Results Found",
    description = "No results found for your search. Please try again with different keywords.",
    when,
    ...props
  }) => {
    if (!when) return null;
    return /* @__PURE__ */ React.createElement("tr", { "data-slot": "table-empty", className: cn("h-full hover:bg-transparent", className), ...props }, /* @__PURE__ */ React.createElement("td", { colSpan, className: "bg-muted-50 text-center" }, /* @__PURE__ */ React.createElement("div", { className: "relative inline-flex h-30 flex-col items-center justify-center" }, /* @__PURE__ */ React.createElement(Search, { strokeWidth: 1.5, className: "text-muted-200 absolute top-1/2 left-1/2 size-30 -translate-x-1/2 -translate-y-1/2" }), /* @__PURE__ */ React.createElement("div", { className: "relative flex flex-col items-center gap-1" }, /* @__PURE__ */ React.createElement("span", { className: "text-muted-400 block text-lg font-medium capitalize" }, title), /* @__PURE__ */ React.createElement("span", { className: "text-muted-400 block text-sm font-normal" }, description)))));
  };
  var TableLoader = ({ colSpan, loaderType = "skeleton", isLoading = false, rows = 10 }) => {
    if (!isLoading) return null;
    if (loaderType === "spinner") return /* @__PURE__ */ React.createElement(PrimeLoader_default, { "data-slot": "table-loader", displayLogo: false, withOverlay: false });
    return /* @__PURE__ */ React.createElement(React.Fragment, null, Array.from({ length: rows }, (_, idx) => /* @__PURE__ */ React.createElement(Row, { key: `skeleton-row-${idx}`, "data-slot": "table-loader" }, Array.from({ length: colSpan }, (_2, colIndex) => /* @__PURE__ */ React.createElement(Cell, { key: `skeleton-cell-${idx}-${colIndex}` }, /* @__PURE__ */ React.createElement(Skeleton_default, null))))));
  };
  var Table = ({ className, ...props }) => /* @__PURE__ */ React.createElement("table", { "data-slot": "table", className: cn("w-full caption-bottom text-sm", className), ...props });
  Table.Header = Header7;
  Table.Body = Body;
  Table.Footer = Footer5;
  Table.Row = Row;
  Table.Head = Head;
  Table.Cell = Cell;
  Table.Caption = Caption;
  Table.Empty = Empty;
  Table.Loader = TableLoader;
  var Table_default = Table;

  // src/components/ui/badge/index.ts
  init_define_import_meta_env();

  // src/components/ui/badge/Badge.tsx
  init_define_import_meta_env();
  var badgeVariants = cva("inline-flex items-center gap-1 font-normal text-muted-foreground", {
    variants: {
      size: {
        default: "p-0 text-xs",
        xs: "px-2 py-0.5 text-xs",
        sm: "px-2.5 py-1 text-sm",
        md: "px-3 py-1.5 text-md",
        lg: "px-3.5 py-2 text-lg"
      },
      variant: {
        default: `**:data-[slot=badge-dot]:text-primary`,
        success: `**:data-[slot=badge-dot]:text-success`,
        destructive: `**:data-[slot=badge-dot]:text-destructive`,
        warning: `**:data-[slot=badge-dot]:text-warning`,
        muted: `**:data-[slot=badge-dot]:text-muted-foreground`,
        secondary: `**:data-[slot=badge-dot]:text-secondary-600`,
        neutral: `**:data-[slot=badge-dot]:text-primary-900`,
        accent: `**:data-[slot=badge-dot]:text-primary-400`
      }
    },
    defaultVariants: {
      size: "default",
      variant: "default"
    }
  });
  var Badge = ({ className, variant, size: size4, children, ...props }) => {
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "badge", className: cn(badgeVariants({ variant, size: size4 }), className), ...props }, /* @__PURE__ */ React.createElement("span", { "data-slot": "badge-dot", className: "h-2 w-2 shrink-0 rounded-full bg-current" }), children);
  };
  var Badge_default = Badge;

  // src/components/ui/chip/index.ts
  init_define_import_meta_env();

  // src/components/ui/calendar/index.ts
  init_define_import_meta_env();

  // src/components/ui/calendar/Calendar.tsx
  init_define_import_meta_env();

  // node_modules/@date-fns/tz/index.js
  init_define_import_meta_env();

  // node_modules/@date-fns/tz/constants/index.js
  init_define_import_meta_env();

  // node_modules/@date-fns/tz/date/index.js
  init_define_import_meta_env();

  // node_modules/@date-fns/tz/tzName/index.js
  init_define_import_meta_env();
  function tzName(timeZone, date, format3 = "long") {
    return new Intl.DateTimeFormat("en-US", {
      // Enforces engine to render the time. Without the option JavaScriptCore omits it.
      hour: "numeric",
      timeZone,
      timeZoneName: format3
    }).format(date).split(/\s/g).slice(2).join(" ");
  }

  // node_modules/@date-fns/tz/date/mini.js
  init_define_import_meta_env();

  // node_modules/@date-fns/tz/tzOffset/index.js
  init_define_import_meta_env();
  var offsetFormatCache = {};
  var offsetCache = {};
  function tzOffset(timeZone, date) {
    try {
      const format3 = offsetFormatCache[timeZone] || (offsetFormatCache[timeZone] = new Intl.DateTimeFormat("en-US", {
        timeZone,
        timeZoneName: "longOffset"
      }).format);
      const offsetStr = format3(date).split("GMT")[1];
      if (offsetStr in offsetCache) return offsetCache[offsetStr];
      return calcOffset(offsetStr, offsetStr.split(":"));
    } catch {
      if (timeZone in offsetCache) return offsetCache[timeZone];
      const captures = timeZone?.match(offsetRe);
      if (captures) return calcOffset(timeZone, captures.slice(1));
      return NaN;
    }
  }
  var offsetRe = /([+-]\d\d):?(\d\d)?/;
  function calcOffset(cacheStr, values) {
    const hours = +(values[0] || 0);
    const minutes = +(values[1] || 0);
    const seconds = +(values[2] || 0) / 60;
    return offsetCache[cacheStr] = hours * 60 + minutes > 0 ? hours * 60 + minutes + seconds : hours * 60 - minutes - seconds;
  }

  // node_modules/@date-fns/tz/date/mini.js
  var TZDateMini = class _TZDateMini extends Date {
    //#region static
    constructor(...args) {
      super();
      if (args.length > 1 && typeof args[args.length - 1] === "string") {
        this.timeZone = args.pop();
      }
      this.internal = /* @__PURE__ */ new Date();
      if (isNaN(tzOffset(this.timeZone, this))) {
        this.setTime(NaN);
      } else {
        if (!args.length) {
          this.setTime(Date.now());
        } else if (typeof args[0] === "number" && (args.length === 1 || args.length === 2 && typeof args[1] !== "number")) {
          this.setTime(args[0]);
        } else if (typeof args[0] === "string") {
          this.setTime(+new Date(args[0]));
        } else if (args[0] instanceof Date) {
          this.setTime(+args[0]);
        } else {
          this.setTime(+new Date(...args));
          adjustToSystemTZ(this, NaN);
          syncToInternal(this);
        }
      }
    }
    static tz(tz, ...args) {
      return args.length ? new _TZDateMini(...args, tz) : new _TZDateMini(Date.now(), tz);
    }
    //#endregion
    //#region time zone
    withTimeZone(timeZone) {
      return new _TZDateMini(+this, timeZone);
    }
    getTimezoneOffset() {
      const offset4 = -tzOffset(this.timeZone, this);
      return offset4 > 0 ? Math.floor(offset4) : Math.ceil(offset4);
    }
    //#endregion
    //#region time
    setTime(time) {
      Date.prototype.setTime.apply(this, arguments);
      syncToInternal(this);
      return +this;
    }
    //#endregion
    //#region date-fns integration
    [/* @__PURE__ */ Symbol.for("constructDateFrom")](date) {
      return new _TZDateMini(+new Date(date), this.timeZone);
    }
    //#endregion
  };
  var re = /^(get|set)(?!UTC)/;
  Object.getOwnPropertyNames(Date.prototype).forEach((method) => {
    if (!re.test(method)) return;
    const utcMethod = method.replace(re, "$1UTC");
    if (!TZDateMini.prototype[utcMethod]) return;
    if (method.startsWith("get")) {
      TZDateMini.prototype[method] = function() {
        return this.internal[utcMethod]();
      };
    } else {
      TZDateMini.prototype[method] = function() {
        Date.prototype[utcMethod].apply(this.internal, arguments);
        syncFromInternal(this);
        return +this;
      };
      TZDateMini.prototype[utcMethod] = function() {
        Date.prototype[utcMethod].apply(this, arguments);
        syncToInternal(this);
        return +this;
      };
    }
  });
  function syncToInternal(date) {
    date.internal.setTime(+date);
    date.internal.setUTCSeconds(date.internal.getUTCSeconds() - Math.round(-tzOffset(date.timeZone, date) * 60));
  }
  function syncFromInternal(date) {
    Date.prototype.setFullYear.call(date, date.internal.getUTCFullYear(), date.internal.getUTCMonth(), date.internal.getUTCDate());
    Date.prototype.setHours.call(date, date.internal.getUTCHours(), date.internal.getUTCMinutes(), date.internal.getUTCSeconds(), date.internal.getUTCMilliseconds());
    adjustToSystemTZ(date);
  }
  function adjustToSystemTZ(date) {
    const baseOffset = tzOffset(date.timeZone, date);
    const offset4 = baseOffset > 0 ? Math.floor(baseOffset) : Math.ceil(baseOffset);
    const prevHour = /* @__PURE__ */ new Date(+date);
    prevHour.setUTCHours(prevHour.getUTCHours() - 1);
    const systemOffset = -(/* @__PURE__ */ new Date(+date)).getTimezoneOffset();
    const prevHourSystemOffset = -(/* @__PURE__ */ new Date(+prevHour)).getTimezoneOffset();
    const systemDSTChange = systemOffset - prevHourSystemOffset;
    const dstShift = Date.prototype.getHours.apply(date) !== date.internal.getUTCHours();
    if (systemDSTChange && dstShift) date.internal.setUTCMinutes(date.internal.getUTCMinutes() + systemDSTChange);
    const offsetDiff = systemOffset - offset4;
    if (offsetDiff) Date.prototype.setUTCMinutes.call(date, Date.prototype.getUTCMinutes.call(date) + offsetDiff);
    const systemDate = /* @__PURE__ */ new Date(+date);
    systemDate.setUTCSeconds(0);
    const systemSecondsOffset = systemOffset > 0 ? systemDate.getSeconds() : (systemDate.getSeconds() - 60) % 60;
    const secondsOffset = Math.round(-(tzOffset(date.timeZone, date) * 60)) % 60;
    if (secondsOffset || systemSecondsOffset) {
      date.internal.setUTCSeconds(date.internal.getUTCSeconds() + secondsOffset);
      Date.prototype.setUTCSeconds.call(date, Date.prototype.getUTCSeconds.call(date) + secondsOffset + systemSecondsOffset);
    }
    const postBaseOffset = tzOffset(date.timeZone, date);
    const postOffset = postBaseOffset > 0 ? Math.floor(postBaseOffset) : Math.ceil(postBaseOffset);
    const postSystemOffset = -(/* @__PURE__ */ new Date(+date)).getTimezoneOffset();
    const postOffsetDiff = postSystemOffset - postOffset;
    const offsetChanged = postOffset !== offset4;
    const postDiff = postOffsetDiff - offsetDiff;
    if (offsetChanged && postDiff) {
      Date.prototype.setUTCMinutes.call(date, Date.prototype.getUTCMinutes.call(date) + postDiff);
      const newBaseOffset = tzOffset(date.timeZone, date);
      const newOffset = newBaseOffset > 0 ? Math.floor(newBaseOffset) : Math.ceil(newBaseOffset);
      const offsetChange = postOffset - newOffset;
      if (offsetChange) {
        date.internal.setUTCMinutes(date.internal.getUTCMinutes() + offsetChange);
        Date.prototype.setUTCMinutes.call(date, Date.prototype.getUTCMinutes.call(date) + offsetChange);
      }
    }
  }

  // node_modules/@date-fns/tz/date/index.js
  var TZDate = class _TZDate extends TZDateMini {
    //#region static
    static tz(tz, ...args) {
      return args.length ? new _TZDate(...args, tz) : new _TZDate(Date.now(), tz);
    }
    //#endregion
    //#region representation
    toISOString() {
      const [sign, hours, minutes] = this.tzComponents();
      const tz = `${sign}${hours}:${minutes}`;
      return this.internal.toISOString().slice(0, -1) + tz;
    }
    toString() {
      return `${this.toDateString()} ${this.toTimeString()}`;
    }
    toDateString() {
      const [day, date, month, year] = this.internal.toUTCString().split(" ");
      return `${day?.slice(0, -1)} ${month} ${date} ${year}`;
    }
    toTimeString() {
      const time = this.internal.toUTCString().split(" ")[4];
      const [sign, hours, minutes] = this.tzComponents();
      return `${time} GMT${sign}${hours}${minutes} (${tzName(this.timeZone, this)})`;
    }
    toLocaleString(locales, options3) {
      return Date.prototype.toLocaleString.call(this, locales, {
        ...options3,
        timeZone: options3?.timeZone || this.timeZone
      });
    }
    toLocaleDateString(locales, options3) {
      return Date.prototype.toLocaleDateString.call(this, locales, {
        ...options3,
        timeZone: options3?.timeZone || this.timeZone
      });
    }
    toLocaleTimeString(locales, options3) {
      return Date.prototype.toLocaleTimeString.call(this, locales, {
        ...options3,
        timeZone: options3?.timeZone || this.timeZone
      });
    }
    //#endregion
    //#region private
    tzComponents() {
      const offset4 = this.getTimezoneOffset();
      const sign = offset4 > 0 ? "-" : "+";
      const hours = String(Math.floor(Math.abs(offset4) / 60)).padStart(2, "0");
      const minutes = String(Math.abs(offset4) % 60).padStart(2, "0");
      return [sign, hours, minutes];
    }
    //#endregion
    withTimeZone(timeZone) {
      return new _TZDate(+this, timeZone);
    }
    //#region date-fns integration
    [/* @__PURE__ */ Symbol.for("constructDateFrom")](date) {
      return new _TZDate(+new Date(date), this.timeZone);
    }
    //#endregion
  };

  // node_modules/@date-fns/tz/tz/index.js
  init_define_import_meta_env();

  // node_modules/@date-fns/tz/tzScan/index.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/classes/CalendarDay.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/classes/DateLib.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/helpers/endOfBroadcastWeek.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/helpers/getBroadcastWeeksInMonth.js
  init_define_import_meta_env();
  var FIVE_WEEKS = 5;
  var FOUR_WEEKS = 4;
  function getBroadcastWeeksInMonth(month, dateLib) {
    const firstDayOfMonth = dateLib.startOfMonth(month);
    const firstDayOfWeek = firstDayOfMonth.getDay() > 0 ? firstDayOfMonth.getDay() : 7;
    const broadcastStartDate = dateLib.addDays(month, -firstDayOfWeek + 1);
    const lastDateOfLastWeek = dateLib.addDays(broadcastStartDate, FIVE_WEEKS * 7 - 1);
    const numberOfWeeks = dateLib.getMonth(month) === dateLib.getMonth(lastDateOfLastWeek) ? FIVE_WEEKS : FOUR_WEEKS;
    return numberOfWeeks;
  }

  // node_modules/react-day-picker/dist/esm/helpers/startOfBroadcastWeek.js
  init_define_import_meta_env();
  function startOfBroadcastWeek(date, dateLib) {
    const firstOfMonth = dateLib.startOfMonth(date);
    const dayOfWeek = firstOfMonth.getDay();
    if (dayOfWeek === 1) {
      return firstOfMonth;
    } else if (dayOfWeek === 0) {
      return dateLib.addDays(firstOfMonth, -1 * 6);
    } else {
      return dateLib.addDays(firstOfMonth, -1 * (dayOfWeek - 1));
    }
  }

  // node_modules/react-day-picker/dist/esm/helpers/endOfBroadcastWeek.js
  function endOfBroadcastWeek(date, dateLib) {
    const startDate = startOfBroadcastWeek(date, dateLib);
    const numberOfWeeks = getBroadcastWeeksInMonth(date, dateLib);
    const endDate = dateLib.addDays(startDate, numberOfWeeks * 7 - 1);
    return endDate;
  }

  // node_modules/react-day-picker/dist/esm/locale/en-US.js
  init_define_import_meta_env();
  var enUS2 = {
    ...enUS,
    labels: {
      labelDayButton: (date, modifiers, options3, dateLib) => {
        let formatDate;
        if (dateLib && typeof dateLib.format === "function") {
          formatDate = dateLib.format.bind(dateLib);
        } else {
          formatDate = (d, pattern) => format(d, pattern, { locale: enUS, ...options3 });
        }
        let label = formatDate(date, "PPPP");
        if (modifiers.today)
          label = `Today, ${label}`;
        if (modifiers.selected)
          label = `${label}, selected`;
        return label;
      },
      labelMonthDropdown: "Choose the Month",
      labelNext: "Go to the Next Month",
      labelPrevious: "Go to the Previous Month",
      labelWeekNumber: (weekNumber) => `Week ${weekNumber}`,
      labelYearDropdown: "Choose the Year",
      labelGrid: (date, options3, dateLib) => {
        let formatDate;
        if (dateLib && typeof dateLib.format === "function") {
          formatDate = dateLib.format.bind(dateLib);
        } else {
          formatDate = (d, pattern) => format(d, pattern, { locale: enUS, ...options3 });
        }
        return formatDate(date, "LLLL yyyy");
      },
      labelGridcell: (date, modifiers, options3, dateLib) => {
        let formatDate;
        if (dateLib && typeof dateLib.format === "function") {
          formatDate = dateLib.format.bind(dateLib);
        } else {
          formatDate = (d, pattern) => format(d, pattern, { locale: enUS, ...options3 });
        }
        let label = formatDate(date, "PPPP");
        if (modifiers?.today) {
          label = `Today, ${label}`;
        }
        return label;
      },
      labelNav: "Navigation bar",
      labelWeekNumberHeader: "Week Number",
      labelWeekday: (date, options3, dateLib) => {
        let formatDate;
        if (dateLib && typeof dateLib.format === "function") {
          formatDate = dateLib.format.bind(dateLib);
        } else {
          formatDate = (d, pattern) => format(d, pattern, { locale: enUS, ...options3 });
        }
        return formatDate(date, "cccc");
      }
    }
  };

  // node_modules/react-day-picker/dist/esm/classes/DateLib.js
  var DateLib = class _DateLib {
    /**
     * Creates an instance of `DateLib`.
     *
     * @param options Configuration options for the date library.
     * @param overrides Custom overrides for the date library functions.
     */
    constructor(options3, overrides) {
      this.Date = Date;
      this.today = () => {
        if (this.overrides?.today) {
          return this.overrides.today();
        }
        if (this.options.timeZone) {
          return TZDate.tz(this.options.timeZone);
        }
        return new this.Date();
      };
      this.newDate = (year, monthIndex, date) => {
        if (this.overrides?.newDate) {
          return this.overrides.newDate(year, monthIndex, date);
        }
        if (this.options.timeZone) {
          return new TZDate(year, monthIndex, date, this.options.timeZone);
        }
        return new Date(year, monthIndex, date);
      };
      this.addDays = (date, amount) => {
        return this.overrides?.addDays ? this.overrides.addDays(date, amount) : addDays(date, amount);
      };
      this.addMonths = (date, amount) => {
        return this.overrides?.addMonths ? this.overrides.addMonths(date, amount) : addMonths(date, amount);
      };
      this.addWeeks = (date, amount) => {
        return this.overrides?.addWeeks ? this.overrides.addWeeks(date, amount) : addWeeks(date, amount);
      };
      this.addYears = (date, amount) => {
        return this.overrides?.addYears ? this.overrides.addYears(date, amount) : addYears(date, amount);
      };
      this.differenceInCalendarDays = (dateLeft, dateRight) => {
        return this.overrides?.differenceInCalendarDays ? this.overrides.differenceInCalendarDays(dateLeft, dateRight) : differenceInCalendarDays(dateLeft, dateRight);
      };
      this.differenceInCalendarMonths = (dateLeft, dateRight) => {
        return this.overrides?.differenceInCalendarMonths ? this.overrides.differenceInCalendarMonths(dateLeft, dateRight) : differenceInCalendarMonths(dateLeft, dateRight);
      };
      this.eachMonthOfInterval = (interval) => {
        return this.overrides?.eachMonthOfInterval ? this.overrides.eachMonthOfInterval(interval) : eachMonthOfInterval(interval);
      };
      this.eachYearOfInterval = (interval) => {
        const years = this.overrides?.eachYearOfInterval ? this.overrides.eachYearOfInterval(interval) : eachYearOfInterval(interval);
        const uniqueYears = new Set(years.map((d) => this.getYear(d)));
        if (uniqueYears.size === years.length) {
          return years;
        }
        const yearsArray = [];
        uniqueYears.forEach((y) => {
          yearsArray.push(new Date(y, 0, 1));
        });
        return yearsArray;
      };
      this.endOfBroadcastWeek = (date) => {
        return this.overrides?.endOfBroadcastWeek ? this.overrides.endOfBroadcastWeek(date) : endOfBroadcastWeek(date, this);
      };
      this.endOfISOWeek = (date) => {
        return this.overrides?.endOfISOWeek ? this.overrides.endOfISOWeek(date) : endOfISOWeek(date);
      };
      this.endOfMonth = (date) => {
        return this.overrides?.endOfMonth ? this.overrides.endOfMonth(date) : endOfMonth(date);
      };
      this.endOfWeek = (date, options4) => {
        return this.overrides?.endOfWeek ? this.overrides.endOfWeek(date, options4) : endOfWeek(date, this.options);
      };
      this.endOfYear = (date) => {
        return this.overrides?.endOfYear ? this.overrides.endOfYear(date) : endOfYear(date);
      };
      this.format = (date, formatStr, _options) => {
        const formatted = this.overrides?.format ? this.overrides.format(date, formatStr, this.options) : format(date, formatStr, this.options);
        if (this.options.numerals && this.options.numerals !== "latn") {
          return this.replaceDigits(formatted);
        }
        return formatted;
      };
      this.getISOWeek = (date) => {
        return this.overrides?.getISOWeek ? this.overrides.getISOWeek(date) : getISOWeek(date);
      };
      this.getMonth = (date, _options) => {
        return this.overrides?.getMonth ? this.overrides.getMonth(date, this.options) : getMonth(date, this.options);
      };
      this.getYear = (date, _options) => {
        return this.overrides?.getYear ? this.overrides.getYear(date, this.options) : getYear(date, this.options);
      };
      this.getWeek = (date, _options) => {
        return this.overrides?.getWeek ? this.overrides.getWeek(date, this.options) : getWeek(date, this.options);
      };
      this.isAfter = (date, dateToCompare) => {
        return this.overrides?.isAfter ? this.overrides.isAfter(date, dateToCompare) : isAfter(date, dateToCompare);
      };
      this.isBefore = (date, dateToCompare) => {
        return this.overrides?.isBefore ? this.overrides.isBefore(date, dateToCompare) : isBefore(date, dateToCompare);
      };
      this.isDate = (value) => {
        return this.overrides?.isDate ? this.overrides.isDate(value) : isDate(value);
      };
      this.isSameDay = (dateLeft, dateRight) => {
        return this.overrides?.isSameDay ? this.overrides.isSameDay(dateLeft, dateRight) : isSameDay(dateLeft, dateRight);
      };
      this.isSameMonth = (dateLeft, dateRight) => {
        return this.overrides?.isSameMonth ? this.overrides.isSameMonth(dateLeft, dateRight) : isSameMonth(dateLeft, dateRight);
      };
      this.isSameYear = (dateLeft, dateRight) => {
        return this.overrides?.isSameYear ? this.overrides.isSameYear(dateLeft, dateRight) : isSameYear(dateLeft, dateRight);
      };
      this.max = (dates) => {
        return this.overrides?.max ? this.overrides.max(dates) : max(dates);
      };
      this.min = (dates) => {
        return this.overrides?.min ? this.overrides.min(dates) : min(dates);
      };
      this.setMonth = (date, month) => {
        return this.overrides?.setMonth ? this.overrides.setMonth(date, month) : setMonth(date, month);
      };
      this.setYear = (date, year) => {
        return this.overrides?.setYear ? this.overrides.setYear(date, year) : setYear(date, year);
      };
      this.startOfBroadcastWeek = (date, _dateLib) => {
        return this.overrides?.startOfBroadcastWeek ? this.overrides.startOfBroadcastWeek(date, this) : startOfBroadcastWeek(date, this);
      };
      this.startOfDay = (date) => {
        return this.overrides?.startOfDay ? this.overrides.startOfDay(date) : startOfDay(date);
      };
      this.startOfISOWeek = (date) => {
        return this.overrides?.startOfISOWeek ? this.overrides.startOfISOWeek(date) : startOfISOWeek(date);
      };
      this.startOfMonth = (date) => {
        return this.overrides?.startOfMonth ? this.overrides.startOfMonth(date) : startOfMonth(date);
      };
      this.startOfWeek = (date, _options) => {
        return this.overrides?.startOfWeek ? this.overrides.startOfWeek(date, this.options) : startOfWeek(date, this.options);
      };
      this.startOfYear = (date) => {
        return this.overrides?.startOfYear ? this.overrides.startOfYear(date) : startOfYear(date);
      };
      this.options = { locale: enUS2, ...options3 };
      this.overrides = overrides;
    }
    /**
     * Generates a mapping of Arabic digits (0-9) to the target numbering system
     * digits.
     *
     * @since 9.5.0
     * @returns A record mapping Arabic digits to the target numerals.
     */
    getDigitMap() {
      const { numerals = "latn" } = this.options;
      const formatter = new Intl.NumberFormat("en-US", {
        numberingSystem: numerals
      });
      const digitMap = {};
      for (let i = 0; i < 10; i++) {
        digitMap[i.toString()] = formatter.format(i);
      }
      return digitMap;
    }
    /**
     * Replaces Arabic digits in a string with the target numbering system digits.
     *
     * @since 9.5.0
     * @param input The string containing Arabic digits.
     * @returns The string with digits replaced.
     */
    replaceDigits(input) {
      const digitMap = this.getDigitMap();
      return input.replace(/\d/g, (digit) => digitMap[digit] || digit);
    }
    /**
     * Formats a number using the configured numbering system.
     *
     * @since 9.5.0
     * @param value The number to format.
     * @returns The formatted number as a string.
     */
    formatNumber(value) {
      return this.replaceDigits(value.toString());
    }
    /**
     * Returns the preferred ordering for month and year labels for the current
     * locale.
     */
    getMonthYearOrder() {
      const code = this.options.locale?.code;
      if (!code) {
        return "month-first";
      }
      return _DateLib.yearFirstLocales.has(code) ? "year-first" : "month-first";
    }
    /**
     * Formats the month/year pair respecting locale conventions.
     *
     * @since 9.11.0
     */
    formatMonthYear(date) {
      const { locale, timeZone, numerals } = this.options;
      const localeCode = locale?.code;
      if (localeCode && _DateLib.yearFirstLocales.has(localeCode)) {
        try {
          const intl = new Intl.DateTimeFormat(localeCode, {
            month: "long",
            year: "numeric",
            timeZone,
            numberingSystem: numerals
          });
          const formatted = intl.format(date);
          return formatted;
        } catch {
        }
      }
      const pattern = this.getMonthYearOrder() === "year-first" ? "y LLLL" : "LLLL y";
      return this.format(date, pattern);
    }
  };
  DateLib.yearFirstLocales = /* @__PURE__ */ new Set([
    "eu",
    "hu",
    "ja",
    "ja-Hira",
    "ja-JP",
    "ko",
    "ko-KR",
    "lt",
    "lt-LT",
    "lv",
    "lv-LV",
    "mn",
    "mn-MN",
    "zh",
    "zh-CN",
    "zh-HK",
    "zh-TW"
  ]);
  var defaultDateLib = new DateLib();

  // node_modules/react-day-picker/dist/esm/classes/CalendarDay.js
  var CalendarDay = class {
    constructor(date, displayMonth, dateLib = defaultDateLib) {
      this.date = date;
      this.displayMonth = displayMonth;
      this.outside = Boolean(displayMonth && !dateLib.isSameMonth(date, displayMonth));
      this.dateLib = dateLib;
      this.isoDate = dateLib.format(date, "yyyy-MM-dd");
      this.displayMonthId = dateLib.format(displayMonth, "yyyy-MM");
      this.dateMonthId = dateLib.format(date, "yyyy-MM");
    }
    /**
     * Checks if this day is equal to another `CalendarDay`, considering both the
     * date and the displayed month.
     *
     * @param day The `CalendarDay` to compare with.
     * @returns `true` if the days are equal, otherwise `false`.
     */
    isEqualTo(day) {
      return this.dateLib.isSameDay(day.date, this.date) && this.dateLib.isSameMonth(day.displayMonth, this.displayMonth);
    }
  };

  // node_modules/react-day-picker/dist/esm/classes/CalendarMonth.js
  init_define_import_meta_env();
  var CalendarMonth = class {
    constructor(month, weeks) {
      this.date = month;
      this.weeks = weeks;
    }
  };

  // node_modules/react-day-picker/dist/esm/classes/CalendarWeek.js
  init_define_import_meta_env();
  var CalendarWeek = class {
    constructor(weekNumber, days) {
      this.days = days;
      this.weekNumber = weekNumber;
    }
  };

  // node_modules/react-day-picker/dist/esm/components/custom-components.js
  var custom_components_exports = {};
  __export(custom_components_exports, {
    Button: () => Button2,
    CaptionLabel: () => CaptionLabel,
    Chevron: () => Chevron,
    Day: () => Day,
    DayButton: () => DayButton,
    Dropdown: () => Dropdown,
    DropdownNav: () => DropdownNav,
    Footer: () => Footer6,
    Month: () => Month,
    MonthCaption: () => MonthCaption,
    MonthGrid: () => MonthGrid,
    Months: () => Months,
    MonthsDropdown: () => MonthsDropdown,
    Nav: () => Nav,
    NextMonthButton: () => NextMonthButton,
    Option: () => Option,
    PreviousMonthButton: () => PreviousMonthButton,
    Root: () => Root9,
    Select: () => Select,
    Week: () => Week,
    WeekNumber: () => WeekNumber,
    WeekNumberHeader: () => WeekNumberHeader,
    Weekday: () => Weekday,
    Weekdays: () => Weekdays,
    Weeks: () => Weeks,
    YearsDropdown: () => YearsDropdown
  });
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/components/Button.js
  init_define_import_meta_env();
  var import_react62 = __toESM(require_react_shim(), 1);
  function Button2(props) {
    return import_react62.default.createElement("button", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/CaptionLabel.js
  init_define_import_meta_env();
  var import_react63 = __toESM(require_react_shim(), 1);
  function CaptionLabel(props) {
    return import_react63.default.createElement("span", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Chevron.js
  init_define_import_meta_env();
  var import_react64 = __toESM(require_react_shim(), 1);
  function Chevron(props) {
    const { size: size4 = 24, orientation = "left", className } = props;
    return (
      // biome-ignore lint/a11y/noSvgWithoutTitle: handled by the parent component
      import_react64.default.createElement(
        "svg",
        { className, width: size4, height: size4, viewBox: "0 0 24 24" },
        orientation === "up" && import_react64.default.createElement("polygon", { points: "6.77 17 12.5 11.43 18.24 17 20 15.28 12.5 8 5 15.28" }),
        orientation === "down" && import_react64.default.createElement("polygon", { points: "6.77 8 12.5 13.57 18.24 8 20 9.72 12.5 17 5 9.72" }),
        orientation === "left" && import_react64.default.createElement("polygon", { points: "16 18.112 9.81111111 12 16 5.87733333 14.0888889 4 6 12 14.0888889 20" }),
        orientation === "right" && import_react64.default.createElement("polygon", { points: "8 18.112 14.18888889 12 8 5.87733333 9.91111111 4 18 12 9.91111111 20" })
      )
    );
  }

  // node_modules/react-day-picker/dist/esm/components/Day.js
  init_define_import_meta_env();
  var import_react65 = __toESM(require_react_shim(), 1);
  function Day(props) {
    const { day, modifiers, ...tdProps } = props;
    return import_react65.default.createElement("td", { ...tdProps });
  }

  // node_modules/react-day-picker/dist/esm/components/DayButton.js
  init_define_import_meta_env();
  var import_react66 = __toESM(require_react_shim(), 1);
  function DayButton(props) {
    const { day, modifiers, ...buttonProps } = props;
    const ref = import_react66.default.useRef(null);
    import_react66.default.useEffect(() => {
      if (modifiers.focused)
        ref.current?.focus();
    }, [modifiers.focused]);
    return import_react66.default.createElement("button", { ref, ...buttonProps });
  }

  // node_modules/react-day-picker/dist/esm/components/Dropdown.js
  init_define_import_meta_env();
  var import_react67 = __toESM(require_react_shim(), 1);

  // node_modules/react-day-picker/dist/esm/UI.js
  init_define_import_meta_env();
  var UI;
  (function(UI2) {
    UI2["Root"] = "root";
    UI2["Chevron"] = "chevron";
    UI2["Day"] = "day";
    UI2["DayButton"] = "day_button";
    UI2["CaptionLabel"] = "caption_label";
    UI2["Dropdowns"] = "dropdowns";
    UI2["Dropdown"] = "dropdown";
    UI2["DropdownRoot"] = "dropdown_root";
    UI2["Footer"] = "footer";
    UI2["MonthGrid"] = "month_grid";
    UI2["MonthCaption"] = "month_caption";
    UI2["MonthsDropdown"] = "months_dropdown";
    UI2["Month"] = "month";
    UI2["Months"] = "months";
    UI2["Nav"] = "nav";
    UI2["NextMonthButton"] = "button_next";
    UI2["PreviousMonthButton"] = "button_previous";
    UI2["Week"] = "week";
    UI2["Weeks"] = "weeks";
    UI2["Weekday"] = "weekday";
    UI2["Weekdays"] = "weekdays";
    UI2["WeekNumber"] = "week_number";
    UI2["WeekNumberHeader"] = "week_number_header";
    UI2["YearsDropdown"] = "years_dropdown";
  })(UI || (UI = {}));
  var DayFlag;
  (function(DayFlag2) {
    DayFlag2["disabled"] = "disabled";
    DayFlag2["hidden"] = "hidden";
    DayFlag2["outside"] = "outside";
    DayFlag2["focused"] = "focused";
    DayFlag2["today"] = "today";
  })(DayFlag || (DayFlag = {}));
  var SelectionState;
  (function(SelectionState2) {
    SelectionState2["range_end"] = "range_end";
    SelectionState2["range_middle"] = "range_middle";
    SelectionState2["range_start"] = "range_start";
    SelectionState2["selected"] = "selected";
  })(SelectionState || (SelectionState = {}));
  var Animation;
  (function(Animation2) {
    Animation2["weeks_before_enter"] = "weeks_before_enter";
    Animation2["weeks_before_exit"] = "weeks_before_exit";
    Animation2["weeks_after_enter"] = "weeks_after_enter";
    Animation2["weeks_after_exit"] = "weeks_after_exit";
    Animation2["caption_after_enter"] = "caption_after_enter";
    Animation2["caption_after_exit"] = "caption_after_exit";
    Animation2["caption_before_enter"] = "caption_before_enter";
    Animation2["caption_before_exit"] = "caption_before_exit";
  })(Animation || (Animation = {}));

  // node_modules/react-day-picker/dist/esm/components/Dropdown.js
  function Dropdown(props) {
    const { options: options3, className, components, classNames, ...selectProps } = props;
    const cssClassSelect = [classNames[UI.Dropdown], className].join(" ");
    const selectedOption = options3?.find(({ value }) => value === selectProps.value);
    return import_react67.default.createElement(
      "span",
      { "data-disabled": selectProps.disabled, className: classNames[UI.DropdownRoot] },
      import_react67.default.createElement(components.Select, { className: cssClassSelect, ...selectProps }, options3?.map(({ value, label, disabled }) => import_react67.default.createElement(components.Option, { key: value, value, disabled }, label))),
      import_react67.default.createElement(
        "span",
        { className: classNames[UI.CaptionLabel], "aria-hidden": true },
        selectedOption?.label,
        import_react67.default.createElement(components.Chevron, { orientation: "down", size: 18, className: classNames[UI.Chevron] })
      )
    );
  }

  // node_modules/react-day-picker/dist/esm/components/DropdownNav.js
  init_define_import_meta_env();
  var import_react68 = __toESM(require_react_shim(), 1);
  function DropdownNav(props) {
    return import_react68.default.createElement("div", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Footer.js
  init_define_import_meta_env();
  var import_react69 = __toESM(require_react_shim(), 1);
  function Footer6(props) {
    return import_react69.default.createElement("div", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Month.js
  init_define_import_meta_env();
  var import_react70 = __toESM(require_react_shim(), 1);
  function Month(props) {
    const { calendarMonth, displayIndex, ...divProps } = props;
    return import_react70.default.createElement("div", { ...divProps }, props.children);
  }

  // node_modules/react-day-picker/dist/esm/components/MonthCaption.js
  init_define_import_meta_env();
  var import_react71 = __toESM(require_react_shim(), 1);
  function MonthCaption(props) {
    const { calendarMonth, displayIndex, ...divProps } = props;
    return import_react71.default.createElement("div", { ...divProps });
  }

  // node_modules/react-day-picker/dist/esm/components/MonthGrid.js
  init_define_import_meta_env();
  var import_react72 = __toESM(require_react_shim(), 1);
  function MonthGrid(props) {
    return import_react72.default.createElement("table", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Months.js
  init_define_import_meta_env();
  var import_react73 = __toESM(require_react_shim(), 1);
  function Months(props) {
    return import_react73.default.createElement("div", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/MonthsDropdown.js
  init_define_import_meta_env();
  var import_react75 = __toESM(require_react_shim(), 1);

  // node_modules/react-day-picker/dist/esm/useDayPicker.js
  init_define_import_meta_env();
  var import_react74 = __toESM(require_react_shim(), 1);
  var dayPickerContext = (0, import_react74.createContext)(void 0);
  function useDayPicker() {
    const context2 = (0, import_react74.useContext)(dayPickerContext);
    if (context2 === void 0) {
      throw new Error("useDayPicker() must be used within a custom component.");
    }
    return context2;
  }

  // node_modules/react-day-picker/dist/esm/components/MonthsDropdown.js
  function MonthsDropdown(props) {
    const { components } = useDayPicker();
    return import_react75.default.createElement(components.Dropdown, { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Nav.js
  init_define_import_meta_env();
  var import_react76 = __toESM(require_react_shim(), 1);
  function Nav(props) {
    const { onPreviousClick, onNextClick, previousMonth, nextMonth, ...navProps } = props;
    const { components, classNames, labels: { labelPrevious: labelPrevious2, labelNext: labelNext2 } } = useDayPicker();
    const handleNextClick = (0, import_react76.useCallback)((e2) => {
      if (nextMonth) {
        onNextClick?.(e2);
      }
    }, [nextMonth, onNextClick]);
    const handlePreviousClick = (0, import_react76.useCallback)((e2) => {
      if (previousMonth) {
        onPreviousClick?.(e2);
      }
    }, [previousMonth, onPreviousClick]);
    return import_react76.default.createElement(
      "nav",
      { ...navProps },
      import_react76.default.createElement(
        components.PreviousMonthButton,
        { type: "button", className: classNames[UI.PreviousMonthButton], tabIndex: previousMonth ? void 0 : -1, "aria-disabled": previousMonth ? void 0 : true, "aria-label": labelPrevious2(previousMonth), onClick: handlePreviousClick },
        import_react76.default.createElement(components.Chevron, { disabled: previousMonth ? void 0 : true, className: classNames[UI.Chevron], orientation: "left" })
      ),
      import_react76.default.createElement(
        components.NextMonthButton,
        { type: "button", className: classNames[UI.NextMonthButton], tabIndex: nextMonth ? void 0 : -1, "aria-disabled": nextMonth ? void 0 : true, "aria-label": labelNext2(nextMonth), onClick: handleNextClick },
        import_react76.default.createElement(components.Chevron, { disabled: nextMonth ? void 0 : true, orientation: "right", className: classNames[UI.Chevron] })
      )
    );
  }

  // node_modules/react-day-picker/dist/esm/components/NextMonthButton.js
  init_define_import_meta_env();
  var import_react77 = __toESM(require_react_shim(), 1);
  function NextMonthButton(props) {
    const { components } = useDayPicker();
    return import_react77.default.createElement(components.Button, { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Option.js
  init_define_import_meta_env();
  var import_react78 = __toESM(require_react_shim(), 1);
  function Option(props) {
    return import_react78.default.createElement("option", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/PreviousMonthButton.js
  init_define_import_meta_env();
  var import_react79 = __toESM(require_react_shim(), 1);
  function PreviousMonthButton(props) {
    const { components } = useDayPicker();
    return import_react79.default.createElement(components.Button, { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Root.js
  init_define_import_meta_env();
  var import_react80 = __toESM(require_react_shim(), 1);
  function Root9(props) {
    const { rootRef, ...rest } = props;
    return import_react80.default.createElement("div", { ...rest, ref: rootRef });
  }

  // node_modules/react-day-picker/dist/esm/components/Select.js
  init_define_import_meta_env();
  var import_react81 = __toESM(require_react_shim(), 1);
  function Select(props) {
    return import_react81.default.createElement("select", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Week.js
  init_define_import_meta_env();
  var import_react82 = __toESM(require_react_shim(), 1);
  function Week(props) {
    const { week, ...trProps } = props;
    return import_react82.default.createElement("tr", { ...trProps });
  }

  // node_modules/react-day-picker/dist/esm/components/Weekday.js
  init_define_import_meta_env();
  var import_react83 = __toESM(require_react_shim(), 1);
  function Weekday(props) {
    return import_react83.default.createElement("th", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Weekdays.js
  init_define_import_meta_env();
  var import_react84 = __toESM(require_react_shim(), 1);
  function Weekdays(props) {
    return import_react84.default.createElement(
      "thead",
      { "aria-hidden": true },
      import_react84.default.createElement("tr", { ...props })
    );
  }

  // node_modules/react-day-picker/dist/esm/components/WeekNumber.js
  init_define_import_meta_env();
  var import_react85 = __toESM(require_react_shim(), 1);
  function WeekNumber(props) {
    const { week, ...thProps } = props;
    return import_react85.default.createElement("th", { ...thProps });
  }

  // node_modules/react-day-picker/dist/esm/components/WeekNumberHeader.js
  init_define_import_meta_env();
  var import_react86 = __toESM(require_react_shim(), 1);
  function WeekNumberHeader(props) {
    return import_react86.default.createElement("th", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/Weeks.js
  init_define_import_meta_env();
  var import_react87 = __toESM(require_react_shim(), 1);
  function Weeks(props) {
    return import_react87.default.createElement("tbody", { ...props });
  }

  // node_modules/react-day-picker/dist/esm/components/YearsDropdown.js
  init_define_import_meta_env();
  var import_react88 = __toESM(require_react_shim(), 1);
  function YearsDropdown(props) {
    const { components } = useDayPicker();
    return import_react88.default.createElement(components.Dropdown, { ...props });
  }

  // node_modules/react-day-picker/dist/esm/DayPicker.js
  init_define_import_meta_env();
  var import_react93 = __toESM(require_react_shim(), 1);

  // node_modules/react-day-picker/dist/esm/helpers/createGetModifiers.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/utils/dateMatchModifiers.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/utils/rangeIncludesDate.js
  init_define_import_meta_env();
  function rangeIncludesDate(range, date, excludeEnds = false, dateLib = defaultDateLib) {
    let { from, to } = range;
    const { differenceInCalendarDays: differenceInCalendarDays2, isSameDay: isSameDay2 } = dateLib;
    if (from && to) {
      const isRangeInverted = differenceInCalendarDays2(to, from) < 0;
      if (isRangeInverted) {
        [from, to] = [to, from];
      }
      const isInRange = differenceInCalendarDays2(date, from) >= (excludeEnds ? 1 : 0) && differenceInCalendarDays2(to, date) >= (excludeEnds ? 1 : 0);
      return isInRange;
    }
    if (!excludeEnds && to) {
      return isSameDay2(to, date);
    }
    if (!excludeEnds && from) {
      return isSameDay2(from, date);
    }
    return false;
  }

  // node_modules/react-day-picker/dist/esm/utils/typeguards.js
  init_define_import_meta_env();
  function isDateInterval(matcher) {
    return Boolean(matcher && typeof matcher === "object" && "before" in matcher && "after" in matcher);
  }
  function isDateRange(value) {
    return Boolean(value && typeof value === "object" && "from" in value);
  }
  function isDateAfterType(value) {
    return Boolean(value && typeof value === "object" && "after" in value);
  }
  function isDateBeforeType(value) {
    return Boolean(value && typeof value === "object" && "before" in value);
  }
  function isDayOfWeekType(value) {
    return Boolean(value && typeof value === "object" && "dayOfWeek" in value);
  }
  function isDatesArray(value, dateLib) {
    return Array.isArray(value) && value.every(dateLib.isDate);
  }

  // node_modules/react-day-picker/dist/esm/utils/dateMatchModifiers.js
  function dateMatchModifiers(date, matchers, dateLib = defaultDateLib) {
    const matchersArr = !Array.isArray(matchers) ? [matchers] : matchers;
    const { isSameDay: isSameDay2, differenceInCalendarDays: differenceInCalendarDays2, isAfter: isAfter2 } = dateLib;
    return matchersArr.some((matcher) => {
      if (typeof matcher === "boolean") {
        return matcher;
      }
      if (dateLib.isDate(matcher)) {
        return isSameDay2(date, matcher);
      }
      if (isDatesArray(matcher, dateLib)) {
        return matcher.some((matcherDate) => isSameDay2(date, matcherDate));
      }
      if (isDateRange(matcher)) {
        return rangeIncludesDate(matcher, date, false, dateLib);
      }
      if (isDayOfWeekType(matcher)) {
        if (!Array.isArray(matcher.dayOfWeek)) {
          return matcher.dayOfWeek === date.getDay();
        }
        return matcher.dayOfWeek.includes(date.getDay());
      }
      if (isDateInterval(matcher)) {
        const diffBefore = differenceInCalendarDays2(matcher.before, date);
        const diffAfter = differenceInCalendarDays2(matcher.after, date);
        const isDayBefore = diffBefore > 0;
        const isDayAfter = diffAfter < 0;
        const isClosedInterval = isAfter2(matcher.before, matcher.after);
        if (isClosedInterval) {
          return isDayAfter && isDayBefore;
        } else {
          return isDayBefore || isDayAfter;
        }
      }
      if (isDateAfterType(matcher)) {
        return differenceInCalendarDays2(date, matcher.after) > 0;
      }
      if (isDateBeforeType(matcher)) {
        return differenceInCalendarDays2(matcher.before, date) > 0;
      }
      if (typeof matcher === "function") {
        return matcher(date);
      }
      return false;
    });
  }

  // node_modules/react-day-picker/dist/esm/helpers/createGetModifiers.js
  function createGetModifiers(days, props, navStart, navEnd, dateLib) {
    const { disabled, hidden, modifiers, showOutsideDays, broadcastCalendar, today = dateLib.today() } = props;
    const { isSameDay: isSameDay2, isSameMonth: isSameMonth2, startOfMonth: startOfMonth2, isBefore: isBefore2, endOfMonth: endOfMonth2, isAfter: isAfter2 } = dateLib;
    const computedNavStart = navStart && startOfMonth2(navStart);
    const computedNavEnd = navEnd && endOfMonth2(navEnd);
    const internalModifiersMap = {
      [DayFlag.focused]: [],
      [DayFlag.outside]: [],
      [DayFlag.disabled]: [],
      [DayFlag.hidden]: [],
      [DayFlag.today]: []
    };
    const customModifiersMap = {};
    for (const day of days) {
      const { date, displayMonth } = day;
      const isOutside = Boolean(displayMonth && !isSameMonth2(date, displayMonth));
      const isBeforeNavStart = Boolean(computedNavStart && isBefore2(date, computedNavStart));
      const isAfterNavEnd = Boolean(computedNavEnd && isAfter2(date, computedNavEnd));
      const isDisabled = Boolean(disabled && dateMatchModifiers(date, disabled, dateLib));
      const isHidden2 = Boolean(hidden && dateMatchModifiers(date, hidden, dateLib)) || isBeforeNavStart || isAfterNavEnd || // Broadcast calendar will show outside days as default
      !broadcastCalendar && !showOutsideDays && isOutside || broadcastCalendar && showOutsideDays === false && isOutside;
      const isToday = isSameDay2(date, today);
      if (isOutside)
        internalModifiersMap.outside.push(day);
      if (isDisabled)
        internalModifiersMap.disabled.push(day);
      if (isHidden2)
        internalModifiersMap.hidden.push(day);
      if (isToday)
        internalModifiersMap.today.push(day);
      if (modifiers) {
        Object.keys(modifiers).forEach((name) => {
          const modifierValue = modifiers?.[name];
          const isMatch = modifierValue ? dateMatchModifiers(date, modifierValue, dateLib) : false;
          if (!isMatch)
            return;
          if (customModifiersMap[name]) {
            customModifiersMap[name].push(day);
          } else {
            customModifiersMap[name] = [day];
          }
        });
      }
    }
    return (day) => {
      const dayFlags = {
        [DayFlag.focused]: false,
        [DayFlag.disabled]: false,
        [DayFlag.hidden]: false,
        [DayFlag.outside]: false,
        [DayFlag.today]: false
      };
      const customModifiers = {};
      for (const name in internalModifiersMap) {
        const days2 = internalModifiersMap[name];
        dayFlags[name] = days2.some((d) => d === day);
      }
      for (const name in customModifiersMap) {
        customModifiers[name] = customModifiersMap[name].some((d) => d === day);
      }
      return {
        ...dayFlags,
        // custom modifiers should override all the previous ones
        ...customModifiers
      };
    };
  }

  // node_modules/react-day-picker/dist/esm/helpers/getClassNamesForModifiers.js
  init_define_import_meta_env();
  function getClassNamesForModifiers(modifiers, classNames, modifiersClassNames = {}) {
    const modifierClassNames = Object.entries(modifiers).filter(([, active]) => active === true).reduce((previousValue, [key]) => {
      if (modifiersClassNames[key]) {
        previousValue.push(modifiersClassNames[key]);
      } else if (classNames[DayFlag[key]]) {
        previousValue.push(classNames[DayFlag[key]]);
      } else if (classNames[SelectionState[key]]) {
        previousValue.push(classNames[SelectionState[key]]);
      }
      return previousValue;
    }, [classNames[UI.Day]]);
    return modifierClassNames;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getComponents.js
  init_define_import_meta_env();
  function getComponents(customComponents) {
    return {
      ...custom_components_exports,
      ...customComponents
    };
  }

  // node_modules/react-day-picker/dist/esm/helpers/getDataAttributes.js
  init_define_import_meta_env();
  function getDataAttributes(props) {
    const dataAttributes = {
      "data-mode": props.mode ?? void 0,
      "data-required": "required" in props ? props.required : void 0,
      "data-multiple-months": props.numberOfMonths && props.numberOfMonths > 1 || void 0,
      "data-week-numbers": props.showWeekNumber || void 0,
      "data-broadcast-calendar": props.broadcastCalendar || void 0,
      "data-nav-layout": props.navLayout || void 0
    };
    Object.entries(props).forEach(([key, val]) => {
      if (key.startsWith("data-")) {
        dataAttributes[key] = val;
      }
    });
    return dataAttributes;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getDefaultClassNames.js
  init_define_import_meta_env();
  function getDefaultClassNames() {
    const classNames = {};
    for (const key in UI) {
      classNames[UI[key]] = `rdp-${UI[key]}`;
    }
    for (const key in DayFlag) {
      classNames[DayFlag[key]] = `rdp-${DayFlag[key]}`;
    }
    for (const key in SelectionState) {
      classNames[SelectionState[key]] = `rdp-${SelectionState[key]}`;
    }
    for (const key in Animation) {
      classNames[Animation[key]] = `rdp-${Animation[key]}`;
    }
    return classNames;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getFormatters.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/formatters/index.js
  var formatters_exports = {};
  __export(formatters_exports, {
    formatCaption: () => formatCaption,
    formatDay: () => formatDay,
    formatMonthCaption: () => formatMonthCaption,
    formatMonthDropdown: () => formatMonthDropdown,
    formatWeekNumber: () => formatWeekNumber,
    formatWeekNumberHeader: () => formatWeekNumberHeader,
    formatWeekdayName: () => formatWeekdayName,
    formatYearCaption: () => formatYearCaption,
    formatYearDropdown: () => formatYearDropdown
  });
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/formatters/formatCaption.js
  init_define_import_meta_env();
  function formatCaption(month, options3, dateLib) {
    const lib = dateLib ?? new DateLib(options3);
    return lib.formatMonthYear(month);
  }
  var formatMonthCaption = formatCaption;

  // node_modules/react-day-picker/dist/esm/formatters/formatDay.js
  init_define_import_meta_env();
  function formatDay(date, options3, dateLib) {
    return (dateLib ?? new DateLib(options3)).format(date, "d");
  }

  // node_modules/react-day-picker/dist/esm/formatters/formatMonthDropdown.js
  init_define_import_meta_env();
  function formatMonthDropdown(month, dateLib = defaultDateLib) {
    return dateLib.format(month, "LLLL");
  }

  // node_modules/react-day-picker/dist/esm/formatters/formatWeekdayName.js
  init_define_import_meta_env();
  function formatWeekdayName(weekday, options3, dateLib) {
    return (dateLib ?? new DateLib(options3)).format(weekday, "cccccc");
  }

  // node_modules/react-day-picker/dist/esm/formatters/formatWeekNumber.js
  init_define_import_meta_env();
  function formatWeekNumber(weekNumber, dateLib = defaultDateLib) {
    if (weekNumber < 10) {
      return dateLib.formatNumber(`0${weekNumber.toLocaleString()}`);
    }
    return dateLib.formatNumber(`${weekNumber.toLocaleString()}`);
  }

  // node_modules/react-day-picker/dist/esm/formatters/formatWeekNumberHeader.js
  init_define_import_meta_env();
  function formatWeekNumberHeader() {
    return ``;
  }

  // node_modules/react-day-picker/dist/esm/formatters/formatYearDropdown.js
  init_define_import_meta_env();
  function formatYearDropdown(year, dateLib = defaultDateLib) {
    return dateLib.format(year, "yyyy");
  }
  var formatYearCaption = formatYearDropdown;

  // node_modules/react-day-picker/dist/esm/helpers/getFormatters.js
  function getFormatters(customFormatters) {
    if (customFormatters?.formatMonthCaption && !customFormatters.formatCaption) {
      customFormatters.formatCaption = customFormatters.formatMonthCaption;
    }
    if (customFormatters?.formatYearCaption && !customFormatters.formatYearDropdown) {
      customFormatters.formatYearDropdown = customFormatters.formatYearCaption;
    }
    return {
      ...formatters_exports,
      ...customFormatters
    };
  }

  // node_modules/react-day-picker/dist/esm/helpers/getLabels.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/labels/index.js
  var labels_exports = {};
  __export(labels_exports, {
    labelCaption: () => labelCaption,
    labelDay: () => labelDay,
    labelDayButton: () => labelDayButton,
    labelGrid: () => labelGrid,
    labelGridcell: () => labelGridcell,
    labelMonthDropdown: () => labelMonthDropdown,
    labelNav: () => labelNav,
    labelNext: () => labelNext,
    labelPrevious: () => labelPrevious,
    labelWeekNumber: () => labelWeekNumber,
    labelWeekNumberHeader: () => labelWeekNumberHeader,
    labelWeekday: () => labelWeekday,
    labelYearDropdown: () => labelYearDropdown
  });
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/labels/labelDayButton.js
  init_define_import_meta_env();
  function labelDayButton(date, modifiers, options3, dateLib) {
    let label = (dateLib ?? new DateLib(options3)).format(date, "PPPP");
    if (modifiers.today)
      label = `Today, ${label}`;
    if (modifiers.selected)
      label = `${label}, selected`;
    return label;
  }
  var labelDay = labelDayButton;

  // node_modules/react-day-picker/dist/esm/labels/labelGrid.js
  init_define_import_meta_env();
  function labelGrid(date, options3, dateLib) {
    const lib = dateLib ?? new DateLib(options3);
    return lib.formatMonthYear(date);
  }
  var labelCaption = labelGrid;

  // node_modules/react-day-picker/dist/esm/labels/labelGridcell.js
  init_define_import_meta_env();
  function labelGridcell(date, modifiers, options3, dateLib) {
    let label = (dateLib ?? new DateLib(options3)).format(date, "PPPP");
    if (modifiers?.today) {
      label = `Today, ${label}`;
    }
    return label;
  }

  // node_modules/react-day-picker/dist/esm/labels/labelMonthDropdown.js
  init_define_import_meta_env();
  function labelMonthDropdown(_options) {
    return "Choose the Month";
  }

  // node_modules/react-day-picker/dist/esm/labels/labelNav.js
  init_define_import_meta_env();
  function labelNav() {
    return "";
  }

  // node_modules/react-day-picker/dist/esm/labels/labelNext.js
  init_define_import_meta_env();
  var defaultLabel = "Go to the Next Month";
  function labelNext(_month, _options) {
    return defaultLabel;
  }

  // node_modules/react-day-picker/dist/esm/labels/labelPrevious.js
  init_define_import_meta_env();
  function labelPrevious(_month) {
    return "Go to the Previous Month";
  }

  // node_modules/react-day-picker/dist/esm/labels/labelWeekday.js
  init_define_import_meta_env();
  function labelWeekday(date, options3, dateLib) {
    return (dateLib ?? new DateLib(options3)).format(date, "cccc");
  }

  // node_modules/react-day-picker/dist/esm/labels/labelWeekNumber.js
  init_define_import_meta_env();
  function labelWeekNumber(weekNumber, _options) {
    return `Week ${weekNumber}`;
  }

  // node_modules/react-day-picker/dist/esm/labels/labelWeekNumberHeader.js
  init_define_import_meta_env();
  function labelWeekNumberHeader(_options) {
    return "Week Number";
  }

  // node_modules/react-day-picker/dist/esm/labels/labelYearDropdown.js
  init_define_import_meta_env();
  function labelYearDropdown(_options) {
    return "Choose the Year";
  }

  // node_modules/react-day-picker/dist/esm/helpers/getLabels.js
  var resolveLabel = (defaultLabel2, customLabel, localeLabel) => {
    if (customLabel)
      return customLabel;
    if (localeLabel) {
      return typeof localeLabel === "function" ? localeLabel : (..._args) => localeLabel;
    }
    return defaultLabel2;
  };
  function getLabels(customLabels, options3) {
    const localeLabels = options3.locale?.labels ?? {};
    return {
      ...labels_exports,
      ...customLabels ?? {},
      labelDayButton: resolveLabel(labelDayButton, customLabels?.labelDayButton, localeLabels.labelDayButton),
      labelMonthDropdown: resolveLabel(labelMonthDropdown, customLabels?.labelMonthDropdown, localeLabels.labelMonthDropdown),
      labelNext: resolveLabel(labelNext, customLabels?.labelNext, localeLabels.labelNext),
      labelPrevious: resolveLabel(labelPrevious, customLabels?.labelPrevious, localeLabels.labelPrevious),
      labelWeekNumber: resolveLabel(labelWeekNumber, customLabels?.labelWeekNumber, localeLabels.labelWeekNumber),
      labelYearDropdown: resolveLabel(labelYearDropdown, customLabels?.labelYearDropdown, localeLabels.labelYearDropdown),
      labelGrid: resolveLabel(labelGrid, customLabels?.labelGrid, localeLabels.labelGrid),
      labelGridcell: resolveLabel(labelGridcell, customLabels?.labelGridcell, localeLabels.labelGridcell),
      labelNav: resolveLabel(labelNav, customLabels?.labelNav, localeLabels.labelNav),
      labelWeekNumberHeader: resolveLabel(labelWeekNumberHeader, customLabels?.labelWeekNumberHeader, localeLabels.labelWeekNumberHeader),
      labelWeekday: resolveLabel(labelWeekday, customLabels?.labelWeekday, localeLabels.labelWeekday)
    };
  }

  // node_modules/react-day-picker/dist/esm/helpers/getMonthOptions.js
  init_define_import_meta_env();
  function getMonthOptions(displayMonth, navStart, navEnd, formatters2, dateLib) {
    const { startOfMonth: startOfMonth2, startOfYear: startOfYear2, endOfYear: endOfYear2, eachMonthOfInterval: eachMonthOfInterval2, getMonth: getMonth2 } = dateLib;
    const months = eachMonthOfInterval2({
      start: startOfYear2(displayMonth),
      end: endOfYear2(displayMonth)
    });
    const options3 = months.map((month) => {
      const label = formatters2.formatMonthDropdown(month, dateLib);
      const value = getMonth2(month);
      const disabled = navStart && month < startOfMonth2(navStart) || navEnd && month > startOfMonth2(navEnd) || false;
      return { value, label, disabled };
    });
    return options3;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getStyleForModifiers.js
  init_define_import_meta_env();
  function getStyleForModifiers(dayModifiers, styles = {}, modifiersStyles = {}) {
    let style = { ...styles?.[UI.Day] };
    Object.entries(dayModifiers).filter(([, active]) => active === true).forEach(([modifier]) => {
      style = {
        ...style,
        ...modifiersStyles?.[modifier]
      };
    });
    return style;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getWeekdays.js
  init_define_import_meta_env();
  function getWeekdays(dateLib, ISOWeek, broadcastCalendar, today) {
    const referenceToday = today ?? dateLib.today();
    const start = broadcastCalendar ? dateLib.startOfBroadcastWeek(referenceToday, dateLib) : ISOWeek ? dateLib.startOfISOWeek(referenceToday) : dateLib.startOfWeek(referenceToday);
    const days = [];
    for (let i = 0; i < 7; i++) {
      const day = dateLib.addDays(start, i);
      days.push(day);
    }
    return days;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getYearOptions.js
  init_define_import_meta_env();
  function getYearOptions(navStart, navEnd, formatters2, dateLib, reverse = false) {
    if (!navStart)
      return void 0;
    if (!navEnd)
      return void 0;
    const { startOfYear: startOfYear2, endOfYear: endOfYear2, eachYearOfInterval: eachYearOfInterval2, getYear: getYear2 } = dateLib;
    const firstNavYear = startOfYear2(navStart);
    const lastNavYear = endOfYear2(navEnd);
    const years = eachYearOfInterval2({ start: firstNavYear, end: lastNavYear });
    if (reverse)
      years.reverse();
    return years.map((year) => {
      const label = formatters2.formatYearDropdown(year, dateLib);
      return {
        value: getYear2(year),
        label,
        disabled: false
      };
    });
  }

  // node_modules/react-day-picker/dist/esm/noonDateLib.js
  init_define_import_meta_env();
  function createNoonOverrides(timeZone, options3 = {}) {
    const { weekStartsOn, locale } = options3;
    const fallbackWeekStartsOn = weekStartsOn ?? locale?.options?.weekStartsOn ?? 0;
    const toNoonTZDate = (date) => {
      const normalizedDate = typeof date === "number" || typeof date === "string" ? new Date(date) : date;
      return new TZDate(normalizedDate.getFullYear(), normalizedDate.getMonth(), normalizedDate.getDate(), 12, 0, 0, timeZone);
    };
    const toCalendarDate = (date) => {
      const zoned = toNoonTZDate(date);
      return new Date(zoned.getFullYear(), zoned.getMonth(), zoned.getDate(), 0, 0, 0, 0);
    };
    return {
      today: () => {
        return toNoonTZDate(TZDate.tz(timeZone));
      },
      newDate: (year, monthIndex, date) => {
        return new TZDate(year, monthIndex, date, 12, 0, 0, timeZone);
      },
      startOfDay: (date) => {
        return toNoonTZDate(date);
      },
      startOfWeek: (date, options4) => {
        const base = toNoonTZDate(date);
        const weekStartsOnValue = options4?.weekStartsOn ?? fallbackWeekStartsOn;
        const diff = (base.getDay() - weekStartsOnValue + 7) % 7;
        base.setDate(base.getDate() - diff);
        return base;
      },
      startOfISOWeek: (date) => {
        const base = toNoonTZDate(date);
        const diff = (base.getDay() - 1 + 7) % 7;
        base.setDate(base.getDate() - diff);
        return base;
      },
      startOfMonth: (date) => {
        const base = toNoonTZDate(date);
        base.setDate(1);
        return base;
      },
      startOfYear: (date) => {
        const base = toNoonTZDate(date);
        base.setMonth(0, 1);
        return base;
      },
      endOfWeek: (date, options4) => {
        const base = toNoonTZDate(date);
        const weekStartsOnValue = options4?.weekStartsOn ?? fallbackWeekStartsOn;
        const endDow = (weekStartsOnValue + 6) % 7;
        const diff = (endDow - base.getDay() + 7) % 7;
        base.setDate(base.getDate() + diff);
        return base;
      },
      endOfISOWeek: (date) => {
        const base = toNoonTZDate(date);
        const diff = (7 - base.getDay()) % 7;
        base.setDate(base.getDate() + diff);
        return base;
      },
      endOfMonth: (date) => {
        const base = toNoonTZDate(date);
        base.setMonth(base.getMonth() + 1, 0);
        return base;
      },
      endOfYear: (date) => {
        const base = toNoonTZDate(date);
        base.setMonth(11, 31);
        return base;
      },
      eachMonthOfInterval: (interval) => {
        const start = toNoonTZDate(interval.start);
        const end = toNoonTZDate(interval.end);
        const result = [];
        const cursor = new TZDate(start.getFullYear(), start.getMonth(), 1, 12, 0, 0, timeZone);
        const endKey = end.getFullYear() * 12 + end.getMonth();
        while (cursor.getFullYear() * 12 + cursor.getMonth() <= endKey) {
          result.push(new TZDate(cursor, timeZone));
          cursor.setMonth(cursor.getMonth() + 1, 1);
        }
        return result;
      },
      // Normalize to noon once before arithmetic (avoid DST/midnight edge cases),
      // mutate the same TZDate, and return it.
      addDays: (date, amount) => {
        const base = toNoonTZDate(date);
        base.setDate(base.getDate() + amount);
        return base;
      },
      addWeeks: (date, amount) => {
        const base = toNoonTZDate(date);
        base.setDate(base.getDate() + amount * 7);
        return base;
      },
      addMonths: (date, amount) => {
        const base = toNoonTZDate(date);
        base.setMonth(base.getMonth() + amount);
        return base;
      },
      addYears: (date, amount) => {
        const base = toNoonTZDate(date);
        base.setFullYear(base.getFullYear() + amount);
        return base;
      },
      eachYearOfInterval: (interval) => {
        const start = toNoonTZDate(interval.start);
        const end = toNoonTZDate(interval.end);
        const years = [];
        const cursor = new TZDate(start.getFullYear(), 0, 1, 12, 0, 0, timeZone);
        while (cursor.getFullYear() <= end.getFullYear()) {
          years.push(new TZDate(cursor, timeZone));
          cursor.setFullYear(cursor.getFullYear() + 1, 0, 1);
        }
        return years;
      },
      getWeek: (date, options4) => {
        const base = toCalendarDate(date);
        return getWeek(base, {
          weekStartsOn: options4?.weekStartsOn ?? fallbackWeekStartsOn,
          firstWeekContainsDate: options4?.firstWeekContainsDate ?? locale?.options?.firstWeekContainsDate ?? 1
        });
      },
      getISOWeek: (date) => {
        const base = toCalendarDate(date);
        return getISOWeek(base);
      },
      differenceInCalendarDays: (dateLeft, dateRight) => {
        const left = toCalendarDate(dateLeft);
        const right = toCalendarDate(dateRight);
        return differenceInCalendarDays(left, right);
      },
      differenceInCalendarMonths: (dateLeft, dateRight) => {
        const left = toCalendarDate(dateLeft);
        const right = toCalendarDate(dateRight);
        return differenceInCalendarMonths(left, right);
      }
    };
  }

  // node_modules/react-day-picker/dist/esm/useAnimation.js
  init_define_import_meta_env();
  var import_react89 = __toESM(require_react_shim(), 1);
  var asHtmlElement = (element) => {
    if (element instanceof HTMLElement)
      return element;
    return null;
  };
  var queryMonthEls = (element) => [
    ...element.querySelectorAll("[data-animated-month]") ?? []
  ];
  var queryMonthEl = (element) => asHtmlElement(element.querySelector("[data-animated-month]"));
  var queryCaptionEl = (element) => asHtmlElement(element.querySelector("[data-animated-caption]"));
  var queryWeeksEl = (element) => asHtmlElement(element.querySelector("[data-animated-weeks]"));
  var queryNavEl = (element) => asHtmlElement(element.querySelector("[data-animated-nav]"));
  var queryWeekdaysEl = (element) => asHtmlElement(element.querySelector("[data-animated-weekdays]"));
  function useAnimation(rootElRef, enabled, { classNames, months, focused, dateLib }) {
    const previousRootElSnapshotRef = (0, import_react89.useRef)(null);
    const previousMonthsRef = (0, import_react89.useRef)(months);
    const animatingRef = (0, import_react89.useRef)(false);
    (0, import_react89.useLayoutEffect)(() => {
      const previousMonths = previousMonthsRef.current;
      previousMonthsRef.current = months;
      if (!enabled || !rootElRef.current || // safety check because the ref can be set to anything by consumers
      !(rootElRef.current instanceof HTMLElement) || // validation required for the animation to work as expected
      months.length === 0 || previousMonths.length === 0 || months.length !== previousMonths.length) {
        return;
      }
      const isSameMonth2 = dateLib.isSameMonth(months[0].date, previousMonths[0].date);
      const isAfterPreviousMonth = dateLib.isAfter(months[0].date, previousMonths[0].date);
      const captionAnimationClass = isAfterPreviousMonth ? classNames[Animation.caption_after_enter] : classNames[Animation.caption_before_enter];
      const weeksAnimationClass = isAfterPreviousMonth ? classNames[Animation.weeks_after_enter] : classNames[Animation.weeks_before_enter];
      const previousRootElSnapshot = previousRootElSnapshotRef.current;
      const rootElSnapshot = rootElRef.current.cloneNode(true);
      if (rootElSnapshot instanceof HTMLElement) {
        const currentMonthElsSnapshot = queryMonthEls(rootElSnapshot);
        currentMonthElsSnapshot.forEach((currentMonthElSnapshot) => {
          if (!(currentMonthElSnapshot instanceof HTMLElement))
            return;
          const previousMonthElSnapshot = queryMonthEl(currentMonthElSnapshot);
          if (previousMonthElSnapshot && currentMonthElSnapshot.contains(previousMonthElSnapshot)) {
            currentMonthElSnapshot.removeChild(previousMonthElSnapshot);
          }
          const captionEl = queryCaptionEl(currentMonthElSnapshot);
          if (captionEl) {
            captionEl.classList.remove(captionAnimationClass);
          }
          const weeksEl = queryWeeksEl(currentMonthElSnapshot);
          if (weeksEl) {
            weeksEl.classList.remove(weeksAnimationClass);
          }
        });
        previousRootElSnapshotRef.current = rootElSnapshot;
      } else {
        previousRootElSnapshotRef.current = null;
      }
      if (animatingRef.current || isSameMonth2 || // skip animation if a day is focused because it can cause issues to the animation and is better for a11y
      focused) {
        return;
      }
      const previousMonthEls = previousRootElSnapshot instanceof HTMLElement ? queryMonthEls(previousRootElSnapshot) : [];
      const currentMonthEls = queryMonthEls(rootElRef.current);
      if (currentMonthEls?.every((el) => el instanceof HTMLElement) && previousMonthEls && previousMonthEls.every((el) => el instanceof HTMLElement)) {
        animatingRef.current = true;
        const cleanUpFunctions = [];
        rootElRef.current.style.isolation = "isolate";
        const navEl = queryNavEl(rootElRef.current);
        if (navEl) {
          navEl.style.zIndex = "1";
        }
        currentMonthEls.forEach((currentMonthEl, index2) => {
          const previousMonthEl = previousMonthEls[index2];
          if (!previousMonthEl) {
            return;
          }
          currentMonthEl.style.position = "relative";
          currentMonthEl.style.overflow = "hidden";
          const captionEl = queryCaptionEl(currentMonthEl);
          if (captionEl) {
            captionEl.classList.add(captionAnimationClass);
          }
          const weeksEl = queryWeeksEl(currentMonthEl);
          if (weeksEl) {
            weeksEl.classList.add(weeksAnimationClass);
          }
          const cleanUp = () => {
            animatingRef.current = false;
            if (rootElRef.current) {
              rootElRef.current.style.isolation = "";
            }
            if (navEl) {
              navEl.style.zIndex = "";
            }
            if (captionEl) {
              captionEl.classList.remove(captionAnimationClass);
            }
            if (weeksEl) {
              weeksEl.classList.remove(weeksAnimationClass);
            }
            currentMonthEl.style.position = "";
            currentMonthEl.style.overflow = "";
            if (currentMonthEl.contains(previousMonthEl)) {
              currentMonthEl.removeChild(previousMonthEl);
            }
          };
          cleanUpFunctions.push(cleanUp);
          previousMonthEl.style.pointerEvents = "none";
          previousMonthEl.style.position = "absolute";
          previousMonthEl.style.overflow = "hidden";
          previousMonthEl.setAttribute("aria-hidden", "true");
          const previousWeekdaysEl = queryWeekdaysEl(previousMonthEl);
          if (previousWeekdaysEl) {
            previousWeekdaysEl.style.opacity = "0";
          }
          const previousCaptionEl = queryCaptionEl(previousMonthEl);
          if (previousCaptionEl) {
            previousCaptionEl.classList.add(isAfterPreviousMonth ? classNames[Animation.caption_before_exit] : classNames[Animation.caption_after_exit]);
            previousCaptionEl.addEventListener("animationend", cleanUp);
          }
          const previousWeeksEl = queryWeeksEl(previousMonthEl);
          if (previousWeeksEl) {
            previousWeeksEl.classList.add(isAfterPreviousMonth ? classNames[Animation.weeks_before_exit] : classNames[Animation.weeks_after_exit]);
          }
          currentMonthEl.insertBefore(previousMonthEl, currentMonthEl.firstChild);
        });
      }
    });
  }

  // node_modules/react-day-picker/dist/esm/useCalendar.js
  init_define_import_meta_env();
  var import_react91 = __toESM(require_react_shim(), 1);

  // node_modules/react-day-picker/dist/esm/helpers/getDates.js
  init_define_import_meta_env();
  function getDates(displayMonths, maxDate, props, dateLib) {
    const firstMonth = displayMonths[0];
    const lastMonth = displayMonths[displayMonths.length - 1];
    const { ISOWeek, fixedWeeks, broadcastCalendar } = props ?? {};
    const { addDays: addDays2, differenceInCalendarDays: differenceInCalendarDays2, differenceInCalendarMonths: differenceInCalendarMonths2, endOfBroadcastWeek: endOfBroadcastWeek2, endOfISOWeek: endOfISOWeek2, endOfMonth: endOfMonth2, endOfWeek: endOfWeek2, isAfter: isAfter2, startOfBroadcastWeek: startOfBroadcastWeek2, startOfISOWeek: startOfISOWeek2, startOfWeek: startOfWeek2 } = dateLib;
    const startWeekFirstDate = broadcastCalendar ? startOfBroadcastWeek2(firstMonth, dateLib) : ISOWeek ? startOfISOWeek2(firstMonth) : startOfWeek2(firstMonth);
    const displayMonthsWeekEnd = broadcastCalendar ? endOfBroadcastWeek2(lastMonth) : ISOWeek ? endOfISOWeek2(endOfMonth2(lastMonth)) : endOfWeek2(endOfMonth2(lastMonth));
    const constraintWeekEnd = maxDate && (broadcastCalendar ? endOfBroadcastWeek2(maxDate) : ISOWeek ? endOfISOWeek2(maxDate) : endOfWeek2(maxDate));
    const gridEndDate = constraintWeekEnd && isAfter2(displayMonthsWeekEnd, constraintWeekEnd) ? constraintWeekEnd : displayMonthsWeekEnd;
    const nOfDays = differenceInCalendarDays2(gridEndDate, startWeekFirstDate);
    const nOfMonths = differenceInCalendarMonths2(lastMonth, firstMonth) + 1;
    const dates = [];
    for (let i = 0; i <= nOfDays; i++) {
      const date = addDays2(startWeekFirstDate, i);
      dates.push(date);
    }
    const nrOfDaysWithFixedWeeks = broadcastCalendar ? 35 : 42;
    const extraDates = nrOfDaysWithFixedWeeks * nOfMonths;
    if (fixedWeeks && dates.length < extraDates) {
      const daysToAdd = extraDates - dates.length;
      for (let i = 0; i < daysToAdd; i++) {
        const date = addDays2(dates[dates.length - 1], 1);
        dates.push(date);
      }
    }
    return dates;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getDays.js
  init_define_import_meta_env();
  function getDays(calendarMonths) {
    const initialDays = [];
    return calendarMonths.reduce((days, month) => {
      const weekDays = month.weeks.reduce((weekDays2, week) => {
        return weekDays2.concat(week.days.slice());
      }, initialDays.slice());
      return days.concat(weekDays.slice());
    }, initialDays.slice());
  }

  // node_modules/react-day-picker/dist/esm/helpers/getDisplayMonths.js
  init_define_import_meta_env();
  function getDisplayMonths(firstDisplayedMonth, calendarEndMonth, props, dateLib) {
    const { numberOfMonths = 1 } = props;
    const months = [];
    for (let i = 0; i < numberOfMonths; i++) {
      const month = dateLib.addMonths(firstDisplayedMonth, i);
      if (calendarEndMonth && month > calendarEndMonth) {
        break;
      }
      months.push(month);
    }
    return months;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getInitialMonth.js
  init_define_import_meta_env();
  function getInitialMonth(props, navStart, navEnd, dateLib) {
    const { month, defaultMonth, today = dateLib.today(), numberOfMonths = 1 } = props;
    let initialMonth = month || defaultMonth || today;
    const { differenceInCalendarMonths: differenceInCalendarMonths2, addMonths: addMonths2, startOfMonth: startOfMonth2 } = dateLib;
    if (navEnd && differenceInCalendarMonths2(navEnd, initialMonth) < numberOfMonths - 1) {
      const offset4 = -1 * (numberOfMonths - 1);
      initialMonth = addMonths2(navEnd, offset4);
    }
    if (navStart && differenceInCalendarMonths2(initialMonth, navStart) < 0) {
      initialMonth = navStart;
    }
    return startOfMonth2(initialMonth);
  }

  // node_modules/react-day-picker/dist/esm/helpers/getMonths.js
  init_define_import_meta_env();
  function getMonths(displayMonths, dates, props, dateLib) {
    const { addDays: addDays2, endOfBroadcastWeek: endOfBroadcastWeek2, endOfISOWeek: endOfISOWeek2, endOfMonth: endOfMonth2, endOfWeek: endOfWeek2, getISOWeek: getISOWeek2, getWeek: getWeek2, startOfBroadcastWeek: startOfBroadcastWeek2, startOfISOWeek: startOfISOWeek2, startOfWeek: startOfWeek2 } = dateLib;
    const dayPickerMonths = displayMonths.reduce((months, month) => {
      const firstDateOfFirstWeek = props.broadcastCalendar ? startOfBroadcastWeek2(month, dateLib) : props.ISOWeek ? startOfISOWeek2(month) : startOfWeek2(month);
      const lastDateOfLastWeek = props.broadcastCalendar ? endOfBroadcastWeek2(month) : props.ISOWeek ? endOfISOWeek2(endOfMonth2(month)) : endOfWeek2(endOfMonth2(month));
      const monthDates = dates.filter((date) => {
        return date >= firstDateOfFirstWeek && date <= lastDateOfLastWeek;
      });
      const nrOfDaysWithFixedWeeks = props.broadcastCalendar ? 35 : 42;
      if (props.fixedWeeks && monthDates.length < nrOfDaysWithFixedWeeks) {
        const extraDates = dates.filter((date) => {
          const daysToAdd = nrOfDaysWithFixedWeeks - monthDates.length;
          return date > lastDateOfLastWeek && date <= addDays2(lastDateOfLastWeek, daysToAdd);
        });
        monthDates.push(...extraDates);
      }
      const weeks = monthDates.reduce((weeks2, date) => {
        const weekNumber = props.ISOWeek ? getISOWeek2(date) : getWeek2(date);
        const week = weeks2.find((week2) => week2.weekNumber === weekNumber);
        const day = new CalendarDay(date, month, dateLib);
        if (!week) {
          weeks2.push(new CalendarWeek(weekNumber, [day]));
        } else {
          week.days.push(day);
        }
        return weeks2;
      }, []);
      const dayPickerMonth = new CalendarMonth(month, weeks);
      months.push(dayPickerMonth);
      return months;
    }, []);
    if (!props.reverseMonths) {
      return dayPickerMonths;
    } else {
      return dayPickerMonths.reverse();
    }
  }

  // node_modules/react-day-picker/dist/esm/helpers/getNavMonth.js
  init_define_import_meta_env();
  function getNavMonths(props, dateLib) {
    let { startMonth, endMonth } = props;
    const { startOfYear: startOfYear2, startOfDay: startOfDay2, startOfMonth: startOfMonth2, endOfMonth: endOfMonth2, addYears: addYears2, endOfYear: endOfYear2, newDate, today } = dateLib;
    const { fromYear, toYear, fromMonth, toMonth } = props;
    if (!startMonth && fromMonth) {
      startMonth = fromMonth;
    }
    if (!startMonth && fromYear) {
      startMonth = dateLib.newDate(fromYear, 0, 1);
    }
    if (!endMonth && toMonth) {
      endMonth = toMonth;
    }
    if (!endMonth && toYear) {
      endMonth = newDate(toYear, 11, 31);
    }
    const hasYearDropdown = props.captionLayout === "dropdown" || props.captionLayout === "dropdown-years";
    if (startMonth) {
      startMonth = startOfMonth2(startMonth);
    } else if (fromYear) {
      startMonth = newDate(fromYear, 0, 1);
    } else if (!startMonth && hasYearDropdown) {
      startMonth = startOfYear2(addYears2(props.today ?? today(), -100));
    }
    if (endMonth) {
      endMonth = endOfMonth2(endMonth);
    } else if (toYear) {
      endMonth = newDate(toYear, 11, 31);
    } else if (!endMonth && hasYearDropdown) {
      endMonth = endOfYear2(props.today ?? today());
    }
    return [
      startMonth ? startOfDay2(startMonth) : startMonth,
      endMonth ? startOfDay2(endMonth) : endMonth
    ];
  }

  // node_modules/react-day-picker/dist/esm/helpers/getNextMonth.js
  init_define_import_meta_env();
  function getNextMonth(firstDisplayedMonth, calendarEndMonth, options3, dateLib) {
    if (options3.disableNavigation) {
      return void 0;
    }
    const { pagedNavigation, numberOfMonths = 1 } = options3;
    const { startOfMonth: startOfMonth2, addMonths: addMonths2, differenceInCalendarMonths: differenceInCalendarMonths2 } = dateLib;
    const offset4 = pagedNavigation ? numberOfMonths : 1;
    const month = startOfMonth2(firstDisplayedMonth);
    if (!calendarEndMonth) {
      return addMonths2(month, offset4);
    }
    const monthsDiff = differenceInCalendarMonths2(calendarEndMonth, firstDisplayedMonth);
    if (monthsDiff < numberOfMonths) {
      return void 0;
    }
    return addMonths2(month, offset4);
  }

  // node_modules/react-day-picker/dist/esm/helpers/getPreviousMonth.js
  init_define_import_meta_env();
  function getPreviousMonth(firstDisplayedMonth, calendarStartMonth, options3, dateLib) {
    if (options3.disableNavigation) {
      return void 0;
    }
    const { pagedNavigation, numberOfMonths } = options3;
    const { startOfMonth: startOfMonth2, addMonths: addMonths2, differenceInCalendarMonths: differenceInCalendarMonths2 } = dateLib;
    const offset4 = pagedNavigation ? numberOfMonths ?? 1 : 1;
    const month = startOfMonth2(firstDisplayedMonth);
    if (!calendarStartMonth) {
      return addMonths2(month, -offset4);
    }
    const monthsDiff = differenceInCalendarMonths2(month, calendarStartMonth);
    if (monthsDiff <= 0) {
      return void 0;
    }
    return addMonths2(month, -offset4);
  }

  // node_modules/react-day-picker/dist/esm/helpers/getWeeks.js
  init_define_import_meta_env();
  function getWeeks(months) {
    const initialWeeks = [];
    return months.reduce((weeks, month) => {
      return weeks.concat(month.weeks.slice());
    }, initialWeeks.slice());
  }

  // node_modules/react-day-picker/dist/esm/helpers/useControlledValue.js
  init_define_import_meta_env();
  var import_react90 = __toESM(require_react_shim(), 1);
  function useControlledValue(defaultValue, controlledValue) {
    const [uncontrolledValue, setValue] = (0, import_react90.useState)(defaultValue);
    const value = controlledValue === void 0 ? uncontrolledValue : controlledValue;
    return [value, setValue];
  }

  // node_modules/react-day-picker/dist/esm/useCalendar.js
  function useCalendar(props, dateLib) {
    const [navStart, navEnd] = getNavMonths(props, dateLib);
    const { startOfMonth: startOfMonth2, endOfMonth: endOfMonth2 } = dateLib;
    const initialMonth = getInitialMonth(props, navStart, navEnd, dateLib);
    const [firstMonth, setFirstMonth] = useControlledValue(
      initialMonth,
      // initialMonth is always computed from props.month if provided
      props.month ? initialMonth : void 0
    );
    (0, import_react91.useEffect)(() => {
      const newInitialMonth = getInitialMonth(props, navStart, navEnd, dateLib);
      setFirstMonth(newInitialMonth);
    }, [props.timeZone]);
    const { months, weeks, days, previousMonth, nextMonth } = (0, import_react91.useMemo)(() => {
      const displayMonths = getDisplayMonths(firstMonth, navEnd, { numberOfMonths: props.numberOfMonths }, dateLib);
      const dates = getDates(displayMonths, props.endMonth ? endOfMonth2(props.endMonth) : void 0, {
        ISOWeek: props.ISOWeek,
        fixedWeeks: props.fixedWeeks,
        broadcastCalendar: props.broadcastCalendar
      }, dateLib);
      const months2 = getMonths(displayMonths, dates, {
        broadcastCalendar: props.broadcastCalendar,
        fixedWeeks: props.fixedWeeks,
        ISOWeek: props.ISOWeek,
        reverseMonths: props.reverseMonths
      }, dateLib);
      const weeks2 = getWeeks(months2);
      const days2 = getDays(months2);
      const previousMonth2 = getPreviousMonth(firstMonth, navStart, props, dateLib);
      const nextMonth2 = getNextMonth(firstMonth, navEnd, props, dateLib);
      return {
        months: months2,
        weeks: weeks2,
        days: days2,
        previousMonth: previousMonth2,
        nextMonth: nextMonth2
      };
    }, [
      dateLib,
      firstMonth.getTime(),
      navEnd?.getTime(),
      navStart?.getTime(),
      props.disableNavigation,
      props.broadcastCalendar,
      props.endMonth?.getTime(),
      props.fixedWeeks,
      props.ISOWeek,
      props.numberOfMonths,
      props.pagedNavigation,
      props.reverseMonths
    ]);
    const { disableNavigation, onMonthChange } = props;
    const isDayInCalendar = (day) => weeks.some((week) => week.days.some((d) => d.isEqualTo(day)));
    const goToMonth = (date) => {
      if (disableNavigation) {
        return;
      }
      let newMonth = startOfMonth2(date);
      if (navStart && newMonth < startOfMonth2(navStart)) {
        newMonth = startOfMonth2(navStart);
      }
      if (navEnd && newMonth > startOfMonth2(navEnd)) {
        newMonth = startOfMonth2(navEnd);
      }
      setFirstMonth(newMonth);
      onMonthChange?.(newMonth);
    };
    const goToDay = (day) => {
      if (isDayInCalendar(day)) {
        return;
      }
      goToMonth(day.date);
    };
    const calendar = {
      months,
      weeks,
      days,
      navStart,
      navEnd,
      previousMonth,
      nextMonth,
      goToMonth,
      goToDay
    };
    return calendar;
  }

  // node_modules/react-day-picker/dist/esm/useFocus.js
  init_define_import_meta_env();
  var import_react92 = __toESM(require_react_shim(), 1);

  // node_modules/react-day-picker/dist/esm/helpers/calculateFocusTarget.js
  init_define_import_meta_env();
  var FocusTargetPriority;
  (function(FocusTargetPriority2) {
    FocusTargetPriority2[FocusTargetPriority2["Today"] = 0] = "Today";
    FocusTargetPriority2[FocusTargetPriority2["Selected"] = 1] = "Selected";
    FocusTargetPriority2[FocusTargetPriority2["LastFocused"] = 2] = "LastFocused";
    FocusTargetPriority2[FocusTargetPriority2["FocusedModifier"] = 3] = "FocusedModifier";
  })(FocusTargetPriority || (FocusTargetPriority = {}));
  function isFocusableDay(modifiers) {
    return !modifiers[DayFlag.disabled] && !modifiers[DayFlag.hidden] && !modifiers[DayFlag.outside];
  }
  function calculateFocusTarget(days, getModifiers, isSelected, lastFocused) {
    let focusTarget;
    let foundFocusTargetPriority = -1;
    for (const day of days) {
      const modifiers = getModifiers(day);
      if (isFocusableDay(modifiers)) {
        if (modifiers[DayFlag.focused] && foundFocusTargetPriority < FocusTargetPriority.FocusedModifier) {
          focusTarget = day;
          foundFocusTargetPriority = FocusTargetPriority.FocusedModifier;
        } else if (lastFocused?.isEqualTo(day) && foundFocusTargetPriority < FocusTargetPriority.LastFocused) {
          focusTarget = day;
          foundFocusTargetPriority = FocusTargetPriority.LastFocused;
        } else if (isSelected(day.date) && foundFocusTargetPriority < FocusTargetPriority.Selected) {
          focusTarget = day;
          foundFocusTargetPriority = FocusTargetPriority.Selected;
        } else if (modifiers[DayFlag.today] && foundFocusTargetPriority < FocusTargetPriority.Today) {
          focusTarget = day;
          foundFocusTargetPriority = FocusTargetPriority.Today;
        }
      }
    }
    if (!focusTarget) {
      focusTarget = days.find((day) => isFocusableDay(getModifiers(day)));
    }
    return focusTarget;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getNextFocus.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/helpers/getFocusableDate.js
  init_define_import_meta_env();
  function getFocusableDate(moveBy, moveDir, refDate, navStart, navEnd, props, dateLib) {
    const { ISOWeek, broadcastCalendar } = props;
    const { addDays: addDays2, addMonths: addMonths2, addWeeks: addWeeks2, addYears: addYears2, endOfBroadcastWeek: endOfBroadcastWeek2, endOfISOWeek: endOfISOWeek2, endOfWeek: endOfWeek2, max: max3, min: min3, startOfBroadcastWeek: startOfBroadcastWeek2, startOfISOWeek: startOfISOWeek2, startOfWeek: startOfWeek2 } = dateLib;
    const moveFns = {
      day: addDays2,
      week: addWeeks2,
      month: addMonths2,
      year: addYears2,
      startOfWeek: (date) => broadcastCalendar ? startOfBroadcastWeek2(date, dateLib) : ISOWeek ? startOfISOWeek2(date) : startOfWeek2(date),
      endOfWeek: (date) => broadcastCalendar ? endOfBroadcastWeek2(date) : ISOWeek ? endOfISOWeek2(date) : endOfWeek2(date)
    };
    let focusableDate = moveFns[moveBy](refDate, moveDir === "after" ? 1 : -1);
    if (moveDir === "before" && navStart) {
      focusableDate = max3([navStart, focusableDate]);
    } else if (moveDir === "after" && navEnd) {
      focusableDate = min3([navEnd, focusableDate]);
    }
    return focusableDate;
  }

  // node_modules/react-day-picker/dist/esm/helpers/getNextFocus.js
  function getNextFocus(moveBy, moveDir, refDay, calendarStartMonth, calendarEndMonth, props, dateLib, attempt = 0) {
    if (attempt > 365) {
      return void 0;
    }
    const focusableDate = getFocusableDate(moveBy, moveDir, refDay.date, calendarStartMonth, calendarEndMonth, props, dateLib);
    const isDisabled = Boolean(props.disabled && dateMatchModifiers(focusableDate, props.disabled, dateLib));
    const isHidden2 = Boolean(props.hidden && dateMatchModifiers(focusableDate, props.hidden, dateLib));
    const targetMonth = focusableDate;
    const focusDay = new CalendarDay(focusableDate, targetMonth, dateLib);
    if (!isDisabled && !isHidden2) {
      return focusDay;
    }
    return getNextFocus(moveBy, moveDir, focusDay, calendarStartMonth, calendarEndMonth, props, dateLib, attempt + 1);
  }

  // node_modules/react-day-picker/dist/esm/useFocus.js
  function useFocus(props, calendar, getModifiers, isSelected, dateLib) {
    const { autoFocus } = props;
    const [lastFocused, setLastFocused] = (0, import_react92.useState)();
    const focusTarget = calculateFocusTarget(calendar.days, getModifiers, isSelected || (() => false), lastFocused);
    const [focusedDay, setFocused] = (0, import_react92.useState)(autoFocus ? focusTarget : void 0);
    const blur = () => {
      setLastFocused(focusedDay);
      setFocused(void 0);
    };
    const moveFocus = (moveBy, moveDir) => {
      if (!focusedDay)
        return;
      const nextFocus = getNextFocus(moveBy, moveDir, focusedDay, calendar.navStart, calendar.navEnd, props, dateLib);
      if (!nextFocus)
        return;
      if (props.disableNavigation) {
        const isNextInCalendar = calendar.days.some((day) => day.isEqualTo(nextFocus));
        if (!isNextInCalendar) {
          return;
        }
      }
      calendar.goToDay(nextFocus);
      setFocused(nextFocus);
    };
    const isFocusTarget = (day) => {
      return Boolean(focusTarget?.isEqualTo(day));
    };
    const useFocus2 = {
      isFocusTarget,
      setFocused,
      focused: focusedDay,
      blur,
      moveFocus
    };
    return useFocus2;
  }

  // node_modules/react-day-picker/dist/esm/useSelection.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/selection/useMulti.js
  init_define_import_meta_env();
  function useMulti(props, dateLib) {
    const { selected: initiallySelected, required, onSelect } = props;
    const [internallySelected, setSelected] = useControlledValue(initiallySelected, onSelect ? initiallySelected : void 0);
    const selected = !onSelect ? internallySelected : initiallySelected;
    const { isSameDay: isSameDay2 } = dateLib;
    const isSelected = (date) => {
      return selected?.some((d) => isSameDay2(d, date)) ?? false;
    };
    const { min: min3, max: max3 } = props;
    const select = (triggerDate, modifiers, e2) => {
      let newDates = [...selected ?? []];
      if (isSelected(triggerDate)) {
        if (selected?.length === min3) {
          return;
        }
        if (required && selected?.length === 1) {
          return;
        }
        newDates = selected?.filter((d) => !isSameDay2(d, triggerDate));
      } else {
        if (selected?.length === max3) {
          newDates = [triggerDate];
        } else {
          newDates = [...newDates, triggerDate];
        }
      }
      if (!onSelect) {
        setSelected(newDates);
      }
      onSelect?.(newDates, triggerDate, modifiers, e2);
      return newDates;
    };
    return {
      selected,
      select,
      isSelected
    };
  }

  // node_modules/react-day-picker/dist/esm/selection/useRange.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/utils/addToRange.js
  init_define_import_meta_env();
  function addToRange(date, initialRange, min3 = 0, max3 = 0, required = false, dateLib = defaultDateLib) {
    const { from, to } = initialRange || {};
    const { isSameDay: isSameDay2, isAfter: isAfter2, isBefore: isBefore2 } = dateLib;
    let range;
    if (!from && !to) {
      range = { from: date, to: min3 > 0 ? void 0 : date };
    } else if (from && !to) {
      if (isSameDay2(from, date)) {
        if (min3 === 0) {
          range = { from, to: date };
        } else if (required) {
          range = { from, to: void 0 };
        } else {
          range = void 0;
        }
      } else if (isBefore2(date, from)) {
        range = { from: date, to: from };
      } else {
        range = { from, to: date };
      }
    } else if (from && to) {
      if (isSameDay2(from, date) && isSameDay2(to, date)) {
        if (required) {
          range = { from, to };
        } else {
          range = void 0;
        }
      } else if (isSameDay2(from, date)) {
        range = { from, to: min3 > 0 ? void 0 : date };
      } else if (isSameDay2(to, date)) {
        range = { from: date, to: min3 > 0 ? void 0 : date };
      } else if (isBefore2(date, from)) {
        range = { from: date, to };
      } else if (isAfter2(date, from)) {
        range = { from, to: date };
      } else if (isAfter2(date, to)) {
        range = { from, to: date };
      } else {
        throw new Error("Invalid range");
      }
    }
    if (range?.from && range?.to) {
      const diff = dateLib.differenceInCalendarDays(range.to, range.from);
      if (max3 > 0 && diff > max3) {
        range = { from: date, to: void 0 };
      } else if (min3 > 1 && diff < min3) {
        range = { from: date, to: void 0 };
      }
    }
    return range;
  }

  // node_modules/react-day-picker/dist/esm/utils/rangeContainsDayOfWeek.js
  init_define_import_meta_env();
  function rangeContainsDayOfWeek(range, dayOfWeek, dateLib = defaultDateLib) {
    const dayOfWeekArr = !Array.isArray(dayOfWeek) ? [dayOfWeek] : dayOfWeek;
    let date = range.from;
    const totalDays = dateLib.differenceInCalendarDays(range.to, range.from);
    const totalDaysLimit = Math.min(totalDays, 6);
    for (let i = 0; i <= totalDaysLimit; i++) {
      if (dayOfWeekArr.includes(date.getDay())) {
        return true;
      }
      date = dateLib.addDays(date, 1);
    }
    return false;
  }

  // node_modules/react-day-picker/dist/esm/utils/rangeContainsModifiers.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/utils/rangeOverlaps.js
  init_define_import_meta_env();
  function rangeOverlaps(rangeLeft, rangeRight, dateLib = defaultDateLib) {
    return rangeIncludesDate(rangeLeft, rangeRight.from, false, dateLib) || rangeIncludesDate(rangeLeft, rangeRight.to, false, dateLib) || rangeIncludesDate(rangeRight, rangeLeft.from, false, dateLib) || rangeIncludesDate(rangeRight, rangeLeft.to, false, dateLib);
  }

  // node_modules/react-day-picker/dist/esm/utils/rangeContainsModifiers.js
  function rangeContainsModifiers(range, modifiers, dateLib = defaultDateLib) {
    const matchers = Array.isArray(modifiers) ? modifiers : [modifiers];
    const nonFunctionMatchers = matchers.filter((matcher) => typeof matcher !== "function");
    const nonFunctionMatchersResult = nonFunctionMatchers.some((matcher) => {
      if (typeof matcher === "boolean")
        return matcher;
      if (dateLib.isDate(matcher)) {
        return rangeIncludesDate(range, matcher, false, dateLib);
      }
      if (isDatesArray(matcher, dateLib)) {
        return matcher.some((date) => rangeIncludesDate(range, date, false, dateLib));
      }
      if (isDateRange(matcher)) {
        if (matcher.from && matcher.to) {
          return rangeOverlaps(range, { from: matcher.from, to: matcher.to }, dateLib);
        }
        return false;
      }
      if (isDayOfWeekType(matcher)) {
        return rangeContainsDayOfWeek(range, matcher.dayOfWeek, dateLib);
      }
      if (isDateInterval(matcher)) {
        const isClosedInterval = dateLib.isAfter(matcher.before, matcher.after);
        if (isClosedInterval) {
          return rangeOverlaps(range, {
            from: dateLib.addDays(matcher.after, 1),
            to: dateLib.addDays(matcher.before, -1)
          }, dateLib);
        }
        return dateMatchModifiers(range.from, matcher, dateLib) || dateMatchModifiers(range.to, matcher, dateLib);
      }
      if (isDateAfterType(matcher) || isDateBeforeType(matcher)) {
        return dateMatchModifiers(range.from, matcher, dateLib) || dateMatchModifiers(range.to, matcher, dateLib);
      }
      return false;
    });
    if (nonFunctionMatchersResult) {
      return true;
    }
    const functionMatchers = matchers.filter((matcher) => typeof matcher === "function");
    if (functionMatchers.length) {
      let date = range.from;
      const totalDays = dateLib.differenceInCalendarDays(range.to, range.from);
      for (let i = 0; i <= totalDays; i++) {
        if (functionMatchers.some((matcher) => matcher(date))) {
          return true;
        }
        date = dateLib.addDays(date, 1);
      }
    }
    return false;
  }

  // node_modules/react-day-picker/dist/esm/selection/useRange.js
  function useRange(props, dateLib) {
    const { disabled, excludeDisabled, resetOnSelect, selected: initiallySelected, required, onSelect } = props;
    const [internallySelected, setSelected] = useControlledValue(initiallySelected, onSelect ? initiallySelected : void 0);
    const selected = !onSelect ? internallySelected : initiallySelected;
    const isSelected = (date) => selected && rangeIncludesDate(selected, date, false, dateLib);
    const select = (triggerDate, modifiers, e2) => {
      const { min: min3, max: max3 } = props;
      let newRange;
      if (triggerDate) {
        const selectedFrom = selected?.from;
        const selectedTo = selected?.to;
        const hasFullRange = !!selectedFrom && !!selectedTo;
        const isClickingSingleDayRange = !!selectedFrom && !!selectedTo && dateLib.isSameDay(selectedFrom, selectedTo) && dateLib.isSameDay(triggerDate, selectedFrom);
        if (resetOnSelect && (hasFullRange || !selected?.from)) {
          if (!required && isClickingSingleDayRange) {
            newRange = void 0;
          } else {
            newRange = { from: triggerDate, to: void 0 };
          }
        } else {
          newRange = addToRange(triggerDate, selected, min3, max3, required, dateLib);
        }
      }
      if (excludeDisabled && disabled && newRange?.from && newRange.to) {
        if (rangeContainsModifiers({ from: newRange.from, to: newRange.to }, disabled, dateLib)) {
          newRange.from = triggerDate;
          newRange.to = void 0;
        }
      }
      if (!onSelect) {
        setSelected(newRange);
      }
      onSelect?.(newRange, triggerDate, modifiers, e2);
      return newRange;
    };
    return {
      selected,
      select,
      isSelected
    };
  }

  // node_modules/react-day-picker/dist/esm/selection/useSingle.js
  init_define_import_meta_env();
  function useSingle(props, dateLib) {
    const { selected: initiallySelected, required, onSelect } = props;
    const [internallySelected, setSelected] = useControlledValue(initiallySelected, onSelect ? initiallySelected : void 0);
    const selected = !onSelect ? internallySelected : initiallySelected;
    const { isSameDay: isSameDay2 } = dateLib;
    const isSelected = (compareDate) => {
      return selected ? isSameDay2(selected, compareDate) : false;
    };
    const select = (triggerDate, modifiers, e2) => {
      let newDate = triggerDate;
      if (!required && selected && selected && isSameDay2(triggerDate, selected)) {
        newDate = void 0;
      }
      if (!onSelect) {
        setSelected(newDate);
      }
      if (required) {
        onSelect?.(newDate, triggerDate, modifiers, e2);
      } else {
        onSelect?.(newDate, triggerDate, modifiers, e2);
      }
      return newDate;
    };
    return {
      selected,
      select,
      isSelected
    };
  }

  // node_modules/react-day-picker/dist/esm/useSelection.js
  function useSelection(props, dateLib) {
    const single = useSingle(props, dateLib);
    const multi = useMulti(props, dateLib);
    const range = useRange(props, dateLib);
    switch (props.mode) {
      case "single":
        return single;
      case "multiple":
        return multi;
      case "range":
        return range;
      default:
        return void 0;
    }
  }

  // node_modules/react-day-picker/dist/esm/utils/convertMatchersToTimeZone.js
  init_define_import_meta_env();

  // node_modules/react-day-picker/dist/esm/utils/toTimeZone.js
  init_define_import_meta_env();
  function toTimeZone(date, timeZone) {
    if (date instanceof TZDate && date.timeZone === timeZone) {
      return date;
    }
    return new TZDate(date, timeZone);
  }

  // node_modules/react-day-picker/dist/esm/utils/convertMatchersToTimeZone.js
  function toZoneNoon(date, timeZone, noonSafe) {
    if (!noonSafe)
      return toTimeZone(date, timeZone);
    const zoned = toTimeZone(date, timeZone);
    const noonZoned = new TZDate(zoned.getFullYear(), zoned.getMonth(), zoned.getDate(), 12, 0, 0, timeZone);
    return new Date(noonZoned.getTime());
  }
  function convertMatcher(matcher, timeZone, noonSafe) {
    if (typeof matcher === "boolean" || typeof matcher === "function") {
      return matcher;
    }
    if (matcher instanceof Date) {
      return toZoneNoon(matcher, timeZone, noonSafe);
    }
    if (Array.isArray(matcher)) {
      return matcher.map((value) => value instanceof Date ? toZoneNoon(value, timeZone, noonSafe) : value);
    }
    if (isDateRange(matcher)) {
      return {
        ...matcher,
        from: matcher.from ? toTimeZone(matcher.from, timeZone) : matcher.from,
        to: matcher.to ? toTimeZone(matcher.to, timeZone) : matcher.to
      };
    }
    if (isDateInterval(matcher)) {
      return {
        before: toZoneNoon(matcher.before, timeZone, noonSafe),
        after: toZoneNoon(matcher.after, timeZone, noonSafe)
      };
    }
    if (isDateAfterType(matcher)) {
      return {
        after: toZoneNoon(matcher.after, timeZone, noonSafe)
      };
    }
    if (isDateBeforeType(matcher)) {
      return {
        before: toZoneNoon(matcher.before, timeZone, noonSafe)
      };
    }
    return matcher;
  }
  function convertMatchersToTimeZone(matchers, timeZone, noonSafe) {
    if (!matchers) {
      return matchers;
    }
    if (Array.isArray(matchers)) {
      return matchers.map((matcher) => convertMatcher(matcher, timeZone, noonSafe));
    }
    return convertMatcher(matchers, timeZone, noonSafe);
  }

  // node_modules/react-day-picker/dist/esm/DayPicker.js
  function DayPicker(initialProps) {
    let props = initialProps;
    const timeZone = props.timeZone;
    if (timeZone) {
      props = {
        ...initialProps,
        timeZone
      };
      if (props.today) {
        props.today = toTimeZone(props.today, timeZone);
      }
      if (props.month) {
        props.month = toTimeZone(props.month, timeZone);
      }
      if (props.defaultMonth) {
        props.defaultMonth = toTimeZone(props.defaultMonth, timeZone);
      }
      if (props.startMonth) {
        props.startMonth = toTimeZone(props.startMonth, timeZone);
      }
      if (props.endMonth) {
        props.endMonth = toTimeZone(props.endMonth, timeZone);
      }
      if (props.mode === "single" && props.selected) {
        props.selected = toTimeZone(props.selected, timeZone);
      } else if (props.mode === "multiple" && props.selected) {
        props.selected = props.selected?.map((date) => toTimeZone(date, timeZone));
      } else if (props.mode === "range" && props.selected) {
        props.selected = {
          from: props.selected.from ? toTimeZone(props.selected.from, timeZone) : props.selected.from,
          to: props.selected.to ? toTimeZone(props.selected.to, timeZone) : props.selected.to
        };
      }
      if (props.disabled !== void 0) {
        props.disabled = convertMatchersToTimeZone(props.disabled, timeZone);
      }
      if (props.hidden !== void 0) {
        props.hidden = convertMatchersToTimeZone(props.hidden, timeZone);
      }
      if (props.modifiers) {
        const nextModifiers = {};
        Object.keys(props.modifiers).forEach((key) => {
          nextModifiers[key] = convertMatchersToTimeZone(props.modifiers?.[key], timeZone);
        });
        props.modifiers = nextModifiers;
      }
    }
    const { components, formatters: formatters2, labels, dateLib, locale, classNames } = (0, import_react93.useMemo)(() => {
      const locale2 = { ...enUS2, ...props.locale };
      const weekStartsOn = props.broadcastCalendar ? 1 : props.weekStartsOn;
      const noonOverrides = props.noonSafe && props.timeZone ? createNoonOverrides(props.timeZone, {
        weekStartsOn,
        locale: locale2
      }) : void 0;
      const overrides = props.dateLib && noonOverrides ? { ...noonOverrides, ...props.dateLib } : props.dateLib ?? noonOverrides;
      const dateLib2 = new DateLib({
        locale: locale2,
        weekStartsOn,
        firstWeekContainsDate: props.firstWeekContainsDate,
        useAdditionalWeekYearTokens: props.useAdditionalWeekYearTokens,
        useAdditionalDayOfYearTokens: props.useAdditionalDayOfYearTokens,
        timeZone: props.timeZone,
        numerals: props.numerals
      }, overrides);
      return {
        dateLib: dateLib2,
        components: getComponents(props.components),
        formatters: getFormatters(props.formatters),
        labels: getLabels(props.labels, dateLib2.options),
        locale: locale2,
        classNames: { ...getDefaultClassNames(), ...props.classNames }
      };
    }, [
      props.locale,
      props.broadcastCalendar,
      props.weekStartsOn,
      props.firstWeekContainsDate,
      props.useAdditionalWeekYearTokens,
      props.useAdditionalDayOfYearTokens,
      props.timeZone,
      props.numerals,
      props.dateLib,
      props.noonSafe,
      props.components,
      props.formatters,
      props.labels,
      props.classNames
    ]);
    if (!props.today) {
      props = { ...props, today: dateLib.today() };
    }
    const { captionLayout, mode, navLayout, numberOfMonths = 1, onDayBlur, onDayClick, onDayFocus, onDayKeyDown, onDayMouseEnter, onDayMouseLeave, onNextClick, onPrevClick, showWeekNumber, styles } = props;
    const { formatCaption: formatCaption2, formatDay: formatDay2, formatMonthDropdown: formatMonthDropdown2, formatWeekNumber: formatWeekNumber2, formatWeekNumberHeader: formatWeekNumberHeader2, formatWeekdayName: formatWeekdayName2, formatYearDropdown: formatYearDropdown2 } = formatters2;
    const calendar = useCalendar(props, dateLib);
    const { days, months, navStart, navEnd, previousMonth, nextMonth, goToMonth } = calendar;
    const getModifiers = createGetModifiers(days, props, navStart, navEnd, dateLib);
    const { isSelected, select, selected: selectedValue } = useSelection(props, dateLib) ?? {};
    const { blur, focused, isFocusTarget, moveFocus, setFocused } = useFocus(props, calendar, getModifiers, isSelected ?? (() => false), dateLib);
    const { labelDayButton: labelDayButton2, labelGridcell: labelGridcell2, labelGrid: labelGrid2, labelMonthDropdown: labelMonthDropdown2, labelNav: labelNav2, labelPrevious: labelPrevious2, labelNext: labelNext2, labelWeekday: labelWeekday2, labelWeekNumber: labelWeekNumber2, labelWeekNumberHeader: labelWeekNumberHeader2, labelYearDropdown: labelYearDropdown2 } = labels;
    const weekdays = (0, import_react93.useMemo)(() => getWeekdays(dateLib, props.ISOWeek, props.broadcastCalendar, props.today), [dateLib, props.ISOWeek, props.broadcastCalendar, props.today]);
    const isInteractive = mode !== void 0 || onDayClick !== void 0;
    const handlePreviousClick = (0, import_react93.useCallback)(() => {
      if (!previousMonth)
        return;
      goToMonth(previousMonth);
      onPrevClick?.(previousMonth);
    }, [previousMonth, goToMonth, onPrevClick]);
    const handleNextClick = (0, import_react93.useCallback)(() => {
      if (!nextMonth)
        return;
      goToMonth(nextMonth);
      onNextClick?.(nextMonth);
    }, [goToMonth, nextMonth, onNextClick]);
    const handleDayClick = (0, import_react93.useCallback)((day, m2) => (e2) => {
      e2.preventDefault();
      e2.stopPropagation();
      setFocused(day);
      if (m2.disabled) {
        return;
      }
      select?.(day.date, m2, e2);
      onDayClick?.(day.date, m2, e2);
    }, [select, onDayClick, setFocused]);
    const handleDayFocus = (0, import_react93.useCallback)((day, m2) => (e2) => {
      setFocused(day);
      onDayFocus?.(day.date, m2, e2);
    }, [onDayFocus, setFocused]);
    const handleDayBlur = (0, import_react93.useCallback)((day, m2) => (e2) => {
      blur();
      onDayBlur?.(day.date, m2, e2);
    }, [blur, onDayBlur]);
    const handleDayKeyDown = (0, import_react93.useCallback)((day, modifiers) => (e2) => {
      const keyMap = {
        ArrowLeft: [
          e2.shiftKey ? "month" : "day",
          props.dir === "rtl" ? "after" : "before"
        ],
        ArrowRight: [
          e2.shiftKey ? "month" : "day",
          props.dir === "rtl" ? "before" : "after"
        ],
        ArrowDown: [e2.shiftKey ? "year" : "week", "after"],
        ArrowUp: [e2.shiftKey ? "year" : "week", "before"],
        PageUp: [e2.shiftKey ? "year" : "month", "before"],
        PageDown: [e2.shiftKey ? "year" : "month", "after"],
        Home: ["startOfWeek", "before"],
        End: ["endOfWeek", "after"]
      };
      if (keyMap[e2.key]) {
        e2.preventDefault();
        e2.stopPropagation();
        const [moveBy, moveDir] = keyMap[e2.key];
        moveFocus(moveBy, moveDir);
      }
      onDayKeyDown?.(day.date, modifiers, e2);
    }, [moveFocus, onDayKeyDown, props.dir]);
    const handleDayMouseEnter = (0, import_react93.useCallback)((day, modifiers) => (e2) => {
      onDayMouseEnter?.(day.date, modifiers, e2);
    }, [onDayMouseEnter]);
    const handleDayMouseLeave = (0, import_react93.useCallback)((day, modifiers) => (e2) => {
      onDayMouseLeave?.(day.date, modifiers, e2);
    }, [onDayMouseLeave]);
    const handleMonthChange = (0, import_react93.useCallback)((date) => (e2) => {
      const selectedMonth = Number(e2.target.value);
      const month = dateLib.setMonth(dateLib.startOfMonth(date), selectedMonth);
      goToMonth(month);
    }, [dateLib, goToMonth]);
    const handleYearChange = (0, import_react93.useCallback)((date) => (e2) => {
      const selectedYear = Number(e2.target.value);
      const month = dateLib.setYear(dateLib.startOfMonth(date), selectedYear);
      goToMonth(month);
    }, [dateLib, goToMonth]);
    const { className, style } = (0, import_react93.useMemo)(() => ({
      className: [classNames[UI.Root], props.className].filter(Boolean).join(" "),
      style: { ...styles?.[UI.Root], ...props.style }
    }), [classNames, props.className, props.style, styles]);
    const dataAttributes = getDataAttributes(props);
    const rootElRef = (0, import_react93.useRef)(null);
    useAnimation(rootElRef, Boolean(props.animate), {
      classNames,
      months,
      focused,
      dateLib
    });
    const contextValue = {
      dayPickerProps: props,
      selected: selectedValue,
      select,
      isSelected,
      months,
      nextMonth,
      previousMonth,
      goToMonth,
      getModifiers,
      components,
      classNames,
      styles,
      labels,
      formatters: formatters2
    };
    return import_react93.default.createElement(
      dayPickerContext.Provider,
      { value: contextValue },
      import_react93.default.createElement(
        components.Root,
        { rootRef: props.animate ? rootElRef : void 0, className, style, dir: props.dir, id: props.id, lang: props.lang ?? locale.code, nonce: props.nonce, title: props.title, role: props.role, "aria-label": props["aria-label"], "aria-labelledby": props["aria-labelledby"], ...dataAttributes },
        import_react93.default.createElement(
          components.Months,
          { className: classNames[UI.Months], style: styles?.[UI.Months] },
          !props.hideNavigation && !navLayout && import_react93.default.createElement(components.Nav, { "data-animated-nav": props.animate ? "true" : void 0, className: classNames[UI.Nav], style: styles?.[UI.Nav], "aria-label": labelNav2(), onPreviousClick: handlePreviousClick, onNextClick: handleNextClick, previousMonth, nextMonth }),
          months.map((calendarMonth, displayIndex) => {
            return import_react93.default.createElement(
              components.Month,
              {
                "data-animated-month": props.animate ? "true" : void 0,
                className: classNames[UI.Month],
                style: styles?.[UI.Month],
                // biome-ignore lint/suspicious/noArrayIndexKey: breaks animation
                key: displayIndex,
                displayIndex,
                calendarMonth
              },
              navLayout === "around" && !props.hideNavigation && displayIndex === 0 && import_react93.default.createElement(
                components.PreviousMonthButton,
                { type: "button", className: classNames[UI.PreviousMonthButton], tabIndex: previousMonth ? void 0 : -1, "aria-disabled": previousMonth ? void 0 : true, "aria-label": labelPrevious2(previousMonth), onClick: handlePreviousClick, "data-animated-button": props.animate ? "true" : void 0 },
                import_react93.default.createElement(components.Chevron, { disabled: previousMonth ? void 0 : true, className: classNames[UI.Chevron], orientation: props.dir === "rtl" ? "right" : "left" })
              ),
              import_react93.default.createElement(components.MonthCaption, { "data-animated-caption": props.animate ? "true" : void 0, className: classNames[UI.MonthCaption], style: styles?.[UI.MonthCaption], calendarMonth, displayIndex }, captionLayout?.startsWith("dropdown") ? import_react93.default.createElement(
                components.DropdownNav,
                { className: classNames[UI.Dropdowns], style: styles?.[UI.Dropdowns] },
                (() => {
                  const monthControl = captionLayout === "dropdown" || captionLayout === "dropdown-months" ? import_react93.default.createElement(components.MonthsDropdown, { key: "month", className: classNames[UI.MonthsDropdown], "aria-label": labelMonthDropdown2(), classNames, components, disabled: Boolean(props.disableNavigation), onChange: handleMonthChange(calendarMonth.date), options: getMonthOptions(calendarMonth.date, navStart, navEnd, formatters2, dateLib), style: styles?.[UI.Dropdown], value: dateLib.getMonth(calendarMonth.date) }) : import_react93.default.createElement("span", { key: "month" }, formatMonthDropdown2(calendarMonth.date, dateLib));
                  const yearControl = captionLayout === "dropdown" || captionLayout === "dropdown-years" ? import_react93.default.createElement(components.YearsDropdown, { key: "year", className: classNames[UI.YearsDropdown], "aria-label": labelYearDropdown2(dateLib.options), classNames, components, disabled: Boolean(props.disableNavigation), onChange: handleYearChange(calendarMonth.date), options: getYearOptions(navStart, navEnd, formatters2, dateLib, Boolean(props.reverseYears)), style: styles?.[UI.Dropdown], value: dateLib.getYear(calendarMonth.date) }) : import_react93.default.createElement("span", { key: "year" }, formatYearDropdown2(calendarMonth.date, dateLib));
                  const controls = dateLib.getMonthYearOrder() === "year-first" ? [yearControl, monthControl] : [monthControl, yearControl];
                  return controls;
                })(),
                import_react93.default.createElement("span", { role: "status", "aria-live": "polite", style: {
                  border: 0,
                  clip: "rect(0 0 0 0)",
                  height: "1px",
                  margin: "-1px",
                  overflow: "hidden",
                  padding: 0,
                  position: "absolute",
                  width: "1px",
                  whiteSpace: "nowrap",
                  wordWrap: "normal"
                } }, formatCaption2(calendarMonth.date, dateLib.options, dateLib))
              ) : import_react93.default.createElement(components.CaptionLabel, { className: classNames[UI.CaptionLabel], role: "status", "aria-live": "polite" }, formatCaption2(calendarMonth.date, dateLib.options, dateLib))),
              navLayout === "around" && !props.hideNavigation && displayIndex === numberOfMonths - 1 && import_react93.default.createElement(
                components.NextMonthButton,
                { type: "button", className: classNames[UI.NextMonthButton], tabIndex: nextMonth ? void 0 : -1, "aria-disabled": nextMonth ? void 0 : true, "aria-label": labelNext2(nextMonth), onClick: handleNextClick, "data-animated-button": props.animate ? "true" : void 0 },
                import_react93.default.createElement(components.Chevron, { disabled: nextMonth ? void 0 : true, className: classNames[UI.Chevron], orientation: props.dir === "rtl" ? "left" : "right" })
              ),
              displayIndex === numberOfMonths - 1 && navLayout === "after" && !props.hideNavigation && import_react93.default.createElement(components.Nav, { "data-animated-nav": props.animate ? "true" : void 0, className: classNames[UI.Nav], style: styles?.[UI.Nav], "aria-label": labelNav2(), onPreviousClick: handlePreviousClick, onNextClick: handleNextClick, previousMonth, nextMonth }),
              import_react93.default.createElement(
                components.MonthGrid,
                { role: "grid", "aria-multiselectable": mode === "multiple" || mode === "range", "aria-label": labelGrid2(calendarMonth.date, dateLib.options, dateLib) || void 0, className: classNames[UI.MonthGrid], style: styles?.[UI.MonthGrid] },
                !props.hideWeekdays && import_react93.default.createElement(
                  components.Weekdays,
                  { "data-animated-weekdays": props.animate ? "true" : void 0, className: classNames[UI.Weekdays], style: styles?.[UI.Weekdays] },
                  showWeekNumber && import_react93.default.createElement(components.WeekNumberHeader, { "aria-label": labelWeekNumberHeader2(dateLib.options), className: classNames[UI.WeekNumberHeader], style: styles?.[UI.WeekNumberHeader], scope: "col" }, formatWeekNumberHeader2()),
                  weekdays.map((weekday) => import_react93.default.createElement(components.Weekday, { "aria-label": labelWeekday2(weekday, dateLib.options, dateLib), className: classNames[UI.Weekday], key: String(weekday), style: styles?.[UI.Weekday], scope: "col" }, formatWeekdayName2(weekday, dateLib.options, dateLib)))
                ),
                import_react93.default.createElement(components.Weeks, { "data-animated-weeks": props.animate ? "true" : void 0, className: classNames[UI.Weeks], style: styles?.[UI.Weeks] }, calendarMonth.weeks.map((week) => {
                  return import_react93.default.createElement(
                    components.Week,
                    { className: classNames[UI.Week], key: week.weekNumber, style: styles?.[UI.Week], week },
                    showWeekNumber && import_react93.default.createElement(components.WeekNumber, { week, style: styles?.[UI.WeekNumber], "aria-label": labelWeekNumber2(week.weekNumber, {
                      locale
                    }), className: classNames[UI.WeekNumber], scope: "row", role: "rowheader" }, formatWeekNumber2(week.weekNumber, dateLib)),
                    week.days.map((day) => {
                      const { date } = day;
                      const modifiers = getModifiers(day);
                      modifiers[DayFlag.focused] = !modifiers.hidden && Boolean(focused?.isEqualTo(day));
                      modifiers[SelectionState.selected] = isSelected?.(date) || modifiers.selected;
                      if (isDateRange(selectedValue)) {
                        const { from, to } = selectedValue;
                        modifiers[SelectionState.range_start] = Boolean(from && to && dateLib.isSameDay(date, from));
                        modifiers[SelectionState.range_end] = Boolean(from && to && dateLib.isSameDay(date, to));
                        modifiers[SelectionState.range_middle] = rangeIncludesDate(selectedValue, date, true, dateLib);
                      }
                      const style2 = getStyleForModifiers(modifiers, styles, props.modifiersStyles);
                      const className2 = getClassNamesForModifiers(modifiers, classNames, props.modifiersClassNames);
                      const ariaLabel = !isInteractive && !modifiers.hidden ? labelGridcell2(date, modifiers, dateLib.options, dateLib) : void 0;
                      return import_react93.default.createElement(components.Day, { key: `${day.isoDate}_${day.displayMonthId}`, day, modifiers, className: className2.join(" "), style: style2, role: "gridcell", "aria-selected": modifiers.selected || void 0, "aria-label": ariaLabel, "data-day": day.isoDate, "data-month": day.outside ? day.dateMonthId : void 0, "data-selected": modifiers.selected || void 0, "data-disabled": modifiers.disabled || void 0, "data-hidden": modifiers.hidden || void 0, "data-outside": day.outside || void 0, "data-focused": modifiers.focused || void 0, "data-today": modifiers.today || void 0 }, !modifiers.hidden && isInteractive ? import_react93.default.createElement(components.DayButton, { className: classNames[UI.DayButton], style: styles?.[UI.DayButton], type: "button", day, modifiers, disabled: !modifiers.focused && modifiers.disabled || void 0, "aria-disabled": modifiers.focused && modifiers.disabled || void 0, tabIndex: isFocusTarget(day) ? 0 : -1, "aria-label": labelDayButton2(date, modifiers, dateLib.options, dateLib), onClick: handleDayClick(day, modifiers), onBlur: handleDayBlur(day, modifiers), onFocus: handleDayFocus(day, modifiers), onKeyDown: handleDayKeyDown(day, modifiers), onMouseEnter: handleDayMouseEnter(day, modifiers), onMouseLeave: handleDayMouseLeave(day, modifiers) }, formatDay2(date, dateLib.options, dateLib)) : !modifiers.hidden && formatDay2(day.date, dateLib.options, dateLib));
                    })
                  );
                }))
              )
            );
          })
        ),
        props.footer && import_react93.default.createElement(components.Footer, { className: classNames[UI.Footer], style: styles?.[UI.Footer], role: "status", "aria-live": "polite" }, props.footer)
      )
    );
  }

  // src/components/ui/calendar/Calendar.tsx
  var import_react94 = __toESM(require_react_shim(), 1);
  function CalendarDayButton({ className, day, modifiers, ...props }) {
    const defaultClassNames = getDefaultClassNames();
    const ref = (0, import_react94.useRef)(null);
    (0, import_react94.useEffect)(() => {
      if (modifiers.focused) ref.current?.focus();
    }, [modifiers.focused]);
    const isToday = modifiers.today;
    const isSelected = modifiers.selected && !modifiers.range_start && !modifiers.range_end && !modifiers.range_middle;
    const isTodayOnly = isToday && !isSelected;
    return /* @__PURE__ */ React.createElement(
      Button_default,
      {
        variant: "unstyled",
        size: "icon",
        "data-day": day.date.toLocaleDateString(),
        "data-selected-single": isSelected,
        "data-today": isToday,
        "data-today-only": isTodayOnly,
        "data-range-start": modifiers.range_start,
        "data-range-end": modifiers.range_end,
        "data-range-middle": modifiers.range_middle,
        className: cn(
          // Today styling (only when not selected) — soft highlight, readable + distinct from selected
          isTodayOnly && "bg-primary-15 text-primary font-semibold rounded-md",
          // Selected date styling
          isSelected && "bg-primary text-primary-foreground rounded-md dark:bg-primary-300",
          // Range styling
          "data-[range-middle=true]:bg-accent data-[range-middle=true]:text-accent-foreground data-[range-start=true]:bg-primary dark:data-[range-start=true]:bg-primary-300 data-[range-start=true]:text-primary-foreground data-[range-end=true]:bg-primary dark:data-[range-end=true]:bg-primary-300 data-[range-end=true]:text-primary-foreground",
          // Focus and layout
          "group-data-[focused=true]/day:border-ring group-data-[focused=true]/day:ring-ring/50 flex aspect-square size-auto w-full min-w-(--cell-size) flex-col gap-1 leading-none font-normal group-data-[focused=true]/day:relative group-data-[focused=true]/day:z-10 group-data-[focused=true]/day:ring-[3px]",
          // Range border radius
          "data-[range-end=true]:rounded-md data-[range-end=true]:rounded-e-md data-[range-middle=true]:rounded-none data-[range-start=true]:rounded-md data-[range-start=true]:rounded-s-md",
          "[&>span]:text-xs [&>span]:opacity-70",
          defaultClassNames.day,
          className
        ),
        ...props
      }
    );
  }
  function CalendarMonthsDropdown({ options: options3, value, onChange, disabled }) {
    if (!options3 || options3.length === 0) return /* @__PURE__ */ React.createElement(React.Fragment, null);
    const selectOptions = options3.map((option) => ({
      id: String(option.value),
      name: option.label
    }));
    const handleValueChange = (newValue) => {
      const numericValue = Number(newValue);
      if (onChange) {
        onChange({ target: { value: String(numericValue) } });
      }
    };
    return /* @__PURE__ */ React.createElement(Select_default, { value: value !== null ? String(value) : "", onValueChange: handleValueChange, disabled }, /* @__PURE__ */ React.createElement(Select_default.Trigger, { className: "h-8 justify-center text-xs" }, /* @__PURE__ */ React.createElement(Select_default.Value, { className: "text-center" }), /* @__PURE__ */ React.createElement(Select_default.Icon, null)), /* @__PURE__ */ React.createElement(Select_default.Content, { position: "popper" }, selectOptions.map((option) => /* @__PURE__ */ React.createElement(
      Select_default.Item,
      {
        key: option.id,
        value: option.id,
        disabled: options3.find((o) => String(o.value) === option.id)?.disabled,
        className: "justify-center text-center"
      },
      /* @__PURE__ */ React.createElement(Select_default.Text, null, option.name)
    ))));
  }
  function CalendarYearsDropdown({ options: options3, value, onChange, disabled }) {
    if (!options3 || options3.length === 0) return /* @__PURE__ */ React.createElement(React.Fragment, null);
    const selectOptions = options3.map((option) => ({
      id: String(option.value),
      name: option.label
    }));
    const handleValueChange = (newValue) => {
      const numericValue = Number(newValue);
      if (onChange) {
        onChange({ target: { value: String(numericValue) } });
      }
    };
    return /* @__PURE__ */ React.createElement(Select_default, { value: value !== null ? String(value) : "", onValueChange: handleValueChange, disabled }, /* @__PURE__ */ React.createElement(Select_default.Trigger, { className: "h-8 justify-center text-xs" }, /* @__PURE__ */ React.createElement(Select_default.Value, { className: "text-center" }), /* @__PURE__ */ React.createElement(Select_default.Icon, null)), /* @__PURE__ */ React.createElement(Select_default.Content, { position: "popper" }, selectOptions.map((option) => /* @__PURE__ */ React.createElement(
      Select_default.Item,
      {
        key: option.id,
        value: option.id,
        disabled: options3.find((o) => String(o.value) === option.id)?.disabled,
        className: "justify-center text-center"
      },
      /* @__PURE__ */ React.createElement(Select_default.Text, null, option.name)
    ))));
  }
  function Calendar2({
    className,
    classNames,
    showOutsideDays = true,
    captionLayout = "label",
    buttonVariant = "ghost",
    formatters: formatters2,
    components,
    ...props
  }) {
    const defaultClassNames = getDefaultClassNames();
    return /* @__PURE__ */ React.createElement(
      DayPicker,
      {
        showOutsideDays,
        className: cn(
          "bg-background group/calendar p-3 [--cell-size:--spacing(8)] in-data-[slot=card-content]:bg-transparent in-data-[slot=popover-content]:bg-transparent",
          String.raw`rtl:**:[.rdp-button\_next>svg]:rotate-180`,
          String.raw`rtl:**:[.rdp-button\_previous>svg]:rotate-180`,
          className
        ),
        captionLayout,
        formatters: {
          formatMonthDropdown: (date) => date.toLocaleString("default", { month: "short" }),
          ...formatters2
        },
        classNames: {
          root: cn("w-fit", defaultClassNames.root),
          months: cn("flex gap-4 flex-col md:flex-row relative", defaultClassNames.months),
          month: cn("flex flex-col w-full gap-4", defaultClassNames.month),
          nav: cn("flex items-center gap-1 w-full absolute top-0 inset-x-0 justify-between", defaultClassNames.nav),
          button_previous: cn(
            buttonVariants({ variant: buttonVariant }),
            "size-(--cell-size) aria-disabled:opacity-50 p-0 select-none",
            defaultClassNames.button_previous
          ),
          button_next: cn(
            buttonVariants({ variant: buttonVariant }),
            "size-(--cell-size) aria-disabled:opacity-50 p-0 select-none",
            defaultClassNames.button_next
          ),
          month_caption: cn("flex items-center justify-center h-(--cell-size) w-full px-(--cell-size)", defaultClassNames.month_caption),
          dropdowns: cn("w-full flex items-center text-sm font-medium justify-center h-(--cell-size) gap-1.5", defaultClassNames.dropdowns),
          dropdown_root: cn(
            "relative has-focus:border-ring border border-input shadow-xs has-focus:ring-ring/50 has-focus:ring-[3px] rounded-md",
            defaultClassNames.dropdown_root
          ),
          dropdown: cn("absolute bg-popover inset-0 opacity-0", defaultClassNames.dropdown),
          caption_label: cn(
            "select-none font-medium",
            captionLayout === "label" ? "text-sm" : "rounded-md ps-2 pe-1 flex items-center gap-1 text-sm h-8 [&>svg]:text-muted-foreground [&>svg]:size-3.5",
            defaultClassNames.caption_label
          ),
          table: "w-full border-collapse",
          weekdays: cn("flex", defaultClassNames.weekdays),
          weekday: cn("text-muted-foreground rounded-md flex-1 font-normal text-[0.8rem] select-none", defaultClassNames.weekday),
          week: cn("flex w-full mt-2", defaultClassNames.week),
          week_number_header: cn("select-none w-(--cell-size)", defaultClassNames.week_number_header),
          week_number: cn("text-[0.8rem] select-none text-muted-foreground", defaultClassNames.week_number),
          day: cn(
            "relative w-full h-full p-0 text-center [&:last-child[data-selected=true]_button]:rounded-e-md group/day aspect-square select-none",
            props.showWeekNumber ? "[&:nth-child(2)[data-selected=true]_button]:rounded-s-md" : "[&:first-child[data-selected=true]_button]:rounded-s-md",
            defaultClassNames.day
          ),
          range_start: cn("rounded-s-md bg-accent", defaultClassNames.range_start),
          range_middle: cn("rounded-none", defaultClassNames.range_middle),
          range_end: cn("rounded-e-md bg-accent", defaultClassNames.range_end),
          today: cn("bg-accent text-accent-foreground rounded-md", defaultClassNames.today),
          outside: cn("text-muted-foreground aria-selected:text-muted-foreground", defaultClassNames.outside),
          disabled: cn("text-muted-foreground opacity-50", defaultClassNames.disabled),
          hidden: cn("invisible", defaultClassNames.hidden),
          ...classNames
        },
        components: {
          Root: ({ className: className2, rootRef, ...props2 }) => {
            return /* @__PURE__ */ React.createElement("div", { "data-slot": "calendar", ref: rootRef, className: cn(className2), ...props2 });
          },
          Chevron: ({ className: className2, orientation, ...props2 }) => {
            if (orientation === "left") {
              return /* @__PURE__ */ React.createElement(ChevronLeft, { className: cn("size-4", className2), ...props2 });
            }
            if (orientation === "right") {
              return /* @__PURE__ */ React.createElement(ChevronRight, { className: cn("size-4", className2), ...props2 });
            }
            return /* @__PURE__ */ React.createElement(ChevronDown, { className: cn("size-4", className2), ...props2 });
          },
          DayButton: CalendarDayButton,
          WeekNumber: ({ children, ...props2 }) => {
            return /* @__PURE__ */ React.createElement("td", { ...props2 }, /* @__PURE__ */ React.createElement("div", { className: "flex size-(--cell-size) items-center justify-center text-center" }, children));
          },
          MonthsDropdown: CalendarMonthsDropdown,
          YearsDropdown: CalendarYearsDropdown,
          ...components
        },
        ...props
      }
    );
  }
  var Calendar_default = Calendar2;

  // src/components/ui/checkbox/index.ts
  init_define_import_meta_env();

  // src/components/ui/checkbox/Checkbox.tsx
  init_define_import_meta_env();
  var import_react95 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-checkbox/dist/index.mjs
  init_define_import_meta_env();
  var React90 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-use-previous/dist/index.mjs
  init_define_import_meta_env();
  var React89 = __toESM(require_react_shim(), 1);
  function usePrevious2(value) {
    const ref = React89.useRef({ value, previous: value });
    return React89.useMemo(() => {
      if (ref.current.value !== value) {
        ref.current.previous = ref.current.value;
        ref.current.value = value;
      }
      return ref.current.previous;
    }, [value]);
  }

  // node_modules/@radix-ui/react-checkbox/dist/index.mjs
  var import_jsx_runtime34 = __toESM(require_react_shim(), 1);
  var CHECKBOX_NAME = "Checkbox";
  var [createCheckboxContext, createCheckboxScope] = createContextScope(CHECKBOX_NAME);
  var [CheckboxProviderImpl, useCheckboxContext] = createCheckboxContext(CHECKBOX_NAME);
  function CheckboxProvider(props) {
    const {
      __scopeCheckbox,
      checked: checkedProp,
      children,
      defaultChecked,
      disabled,
      form,
      name,
      onCheckedChange,
      required,
      value = "on",
      // @ts-expect-error
      internal_do_not_use_render
    } = props;
    const [checked, setChecked] = useControllableState({
      prop: checkedProp,
      defaultProp: defaultChecked ?? false,
      onChange: onCheckedChange,
      caller: CHECKBOX_NAME
    });
    const [control, setControl] = React90.useState(null);
    const [bubbleInput, setBubbleInput] = React90.useState(null);
    const hasConsumerStoppedPropagationRef = React90.useRef(false);
    const isFormControl = control ? !!form || !!control.closest("form") : (
      // We set this to true by default so that events bubble to forms without JS (SSR)
      true
    );
    const context2 = {
      checked,
      disabled,
      setChecked,
      control,
      setControl,
      name,
      form,
      value,
      hasConsumerStoppedPropagationRef,
      required,
      defaultChecked: isIndeterminate2(defaultChecked) ? false : defaultChecked,
      isFormControl,
      bubbleInput,
      setBubbleInput
    };
    return /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
      CheckboxProviderImpl,
      {
        scope: __scopeCheckbox,
        ...context2,
        children: isFunction3(internal_do_not_use_render) ? internal_do_not_use_render(context2) : children
      }
    );
  }
  var TRIGGER_NAME8 = "CheckboxTrigger";
  var CheckboxTrigger = React90.forwardRef(
    ({ __scopeCheckbox, onKeyDown, onClick, ...checkboxProps }, forwardedRef) => {
      const {
        control,
        value,
        disabled,
        checked,
        required,
        setControl,
        setChecked,
        hasConsumerStoppedPropagationRef,
        isFormControl,
        bubbleInput
      } = useCheckboxContext(TRIGGER_NAME8, __scopeCheckbox);
      const composedRefs = useComposedRefs(forwardedRef, setControl);
      const initialCheckedStateRef = React90.useRef(checked);
      React90.useEffect(() => {
        const form = control?.form;
        if (form) {
          const reset = () => setChecked(initialCheckedStateRef.current);
          form.addEventListener("reset", reset);
          return () => form.removeEventListener("reset", reset);
        }
      }, [control, setChecked]);
      return /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
        Primitive2.button,
        {
          type: "button",
          role: "checkbox",
          "aria-checked": isIndeterminate2(checked) ? "mixed" : checked,
          "aria-required": required,
          "data-state": getState5(checked),
          "data-disabled": disabled ? "" : void 0,
          disabled,
          value,
          ...checkboxProps,
          ref: composedRefs,
          onKeyDown: composeEventHandlers(onKeyDown, (event) => {
            if (event.key === "Enter") event.preventDefault();
          }),
          onClick: composeEventHandlers(onClick, (event) => {
            setChecked((prevChecked) => isIndeterminate2(prevChecked) ? true : !prevChecked);
            if (bubbleInput && isFormControl) {
              hasConsumerStoppedPropagationRef.current = event.isPropagationStopped();
              if (!hasConsumerStoppedPropagationRef.current) event.stopPropagation();
            }
          })
        }
      );
    }
  );
  CheckboxTrigger.displayName = TRIGGER_NAME8;
  var Checkbox = React90.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeCheckbox,
        name,
        checked,
        defaultChecked,
        required,
        disabled,
        value,
        onCheckedChange,
        form,
        ...checkboxProps
      } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
        CheckboxProvider,
        {
          __scopeCheckbox,
          checked,
          defaultChecked,
          disabled,
          required,
          onCheckedChange,
          name,
          form,
          value,
          internal_do_not_use_render: ({ isFormControl }) => /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)(import_jsx_runtime34.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
              CheckboxTrigger,
              {
                ...checkboxProps,
                ref: forwardedRef,
                __scopeCheckbox
              }
            ),
            isFormControl && /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
              CheckboxBubbleInput,
              {
                __scopeCheckbox
              }
            )
          ] })
        }
      );
    }
  );
  Checkbox.displayName = CHECKBOX_NAME;
  var INDICATOR_NAME2 = "CheckboxIndicator";
  var CheckboxIndicator = React90.forwardRef(
    (props, forwardedRef) => {
      const { __scopeCheckbox, forceMount, ...indicatorProps } = props;
      const context2 = useCheckboxContext(INDICATOR_NAME2, __scopeCheckbox);
      return /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
        Presence,
        {
          present: forceMount || isIndeterminate2(context2.checked) || context2.checked === true,
          children: /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
            Primitive2.span,
            {
              "data-state": getState5(context2.checked),
              "data-disabled": context2.disabled ? "" : void 0,
              ...indicatorProps,
              ref: forwardedRef,
              style: { pointerEvents: "none", ...props.style }
            }
          )
        }
      );
    }
  );
  CheckboxIndicator.displayName = INDICATOR_NAME2;
  var BUBBLE_INPUT_NAME = "CheckboxBubbleInput";
  var CheckboxBubbleInput = React90.forwardRef(
    ({ __scopeCheckbox, ...props }, forwardedRef) => {
      const {
        control,
        hasConsumerStoppedPropagationRef,
        checked,
        defaultChecked,
        required,
        disabled,
        name,
        value,
        form,
        bubbleInput,
        setBubbleInput
      } = useCheckboxContext(BUBBLE_INPUT_NAME, __scopeCheckbox);
      const composedRefs = useComposedRefs(forwardedRef, setBubbleInput);
      const prevChecked = usePrevious2(checked);
      const controlSize = useSize(control);
      React90.useEffect(() => {
        const input = bubbleInput;
        if (!input) return;
        const inputProto = window.HTMLInputElement.prototype;
        const descriptor = Object.getOwnPropertyDescriptor(
          inputProto,
          "checked"
        );
        const setChecked = descriptor.set;
        const bubbles = !hasConsumerStoppedPropagationRef.current;
        if (prevChecked !== checked && setChecked) {
          const event = new Event("click", { bubbles });
          input.indeterminate = isIndeterminate2(checked);
          setChecked.call(input, isIndeterminate2(checked) ? false : checked);
          input.dispatchEvent(event);
        }
      }, [bubbleInput, prevChecked, checked, hasConsumerStoppedPropagationRef]);
      const defaultCheckedRef = React90.useRef(isIndeterminate2(checked) ? false : checked);
      return /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
        Primitive2.input,
        {
          type: "checkbox",
          "aria-hidden": true,
          defaultChecked: defaultChecked ?? defaultCheckedRef.current,
          required,
          disabled,
          name,
          value,
          form,
          ...props,
          tabIndex: -1,
          ref: composedRefs,
          style: {
            ...props.style,
            ...controlSize,
            position: "absolute",
            pointerEvents: "none",
            opacity: 0,
            margin: 0,
            // We transform because the input is absolutely positioned but we have
            // rendered it **after** the button. This pulls it back to sit on top
            // of the button.
            transform: "translateX(-100%)"
          }
        }
      );
    }
  );
  CheckboxBubbleInput.displayName = BUBBLE_INPUT_NAME;
  function isFunction3(value) {
    return typeof value === "function";
  }
  function isIndeterminate2(checked) {
    return checked === "indeterminate";
  }
  function getState5(checked) {
    return isIndeterminate2(checked) ? "indeterminate" : checked ? "checked" : "unchecked";
  }

  // src/components/ui/checkbox/Checkbox.tsx
  var checkboxVariants = cva(
    [
      "border-muted-200 bg-background h-5 w-5 shrink-0 rounded border transition-[color,box-shadow] outline-none",
      "hover:not-disabled:border-primary hover:not-disabled:ring-primary hover:not-disabled:ring",
      "disabled:data-[state=checked]:bg-muted-400 disabled:data-[state=checked]:border-muted-400 disabled:pointer-events-none"
    ],
    {
      variants: {
        variant: {
          secondary: [
            "data-[state=checked]:border-foreground data-[state=checked]:bg-foreground data-[state=checked]:text-background",
            "data-[state=checked]:hover:not-disabled:border-foreground data-[state=checked]:hover:not-disabled:ring-muted-400"
          ],
          primary: [
            "data-[state=checked]:border-primary data-[state=checked]:bg-primary data-[state=checked]:text-primary-foreground dark:data-[state=checked]:border-primary-300 dark:data-[state=checked]:bg-primary-300",
            "data-[state=checked]:hover:not-disabled:border-primary data-[state=checked]:hover:not-disabled:ring-primary"
          ]
        }
      },
      defaultVariants: {
        variant: "primary"
      }
    }
  );
  var Checkbox2 = ({ ref, className, variant, ...props }) => /* @__PURE__ */ React.createElement(Checkbox, { ref, "data-slot": "checkbox", className: cn(checkboxVariants({ variant }), className), ...props }, /* @__PURE__ */ React.createElement(CheckboxIndicator, { "data-slot": "checkbox-indicator", className: "flex items-center justify-center text-inherit" }, /* @__PURE__ */ React.createElement(Check, { size: 16 })));
  Checkbox2.displayName = "Checkbox";
  var Checkbox_default = (0, import_react95.forwardRef)((props, ref) => Checkbox2({ ...props, ref }));

  // src/components/ui/checkbox-group/index.ts
  init_define_import_meta_env();

  // src/components/ui/checkbox-group/CheckboxGroup.tsx
  init_define_import_meta_env();
  var import_react96 = __toESM(require_react_shim(), 1);
  var Item9 = ({ ref, className, children, ...props }) => /* @__PURE__ */ React.createElement(
    Checkbox,
    {
      ref,
      "data-slot": "checkbox-group-item",
      className: cn(
        "group/checkbox-item border-muted-200 bg-background flex cursor-pointer items-center gap-5 rounded-md border p-3 transition-[color,box-shadow]",
        "data-[state=checked]:border-primary-50 data-[state=checked]:bg-primary-15",
        "hover:border-primary hover:ring-primary focus-visible:ring-primary focus-visible:border-primary hover:ring focus-visible:ring focus-visible:outline-none",
        "data-[state=checked]:hover:border-primary data-[state=checked]:hover:ring-primary data-[state=checked]:focus-visible:border-primary data-[state=checked]:focus-visible:ring-primary",
        "disabled:bg-muted-50 disabled:text-muted-300 disabled:pointer-events-none",
        "disabled:data-[state=checked]:bg-muted-50 disabled:data-[state=checked]:border-muted-200",
        className
      ),
      ...props
    },
    children
  );
  var Indicator = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    CheckboxIndicator,
    {
      "data-slot": "checkbox-group-indicator",
      className: cn(
        "border-muted-200 bg-background flex h-5 w-5 shrink-0 items-center justify-center rounded border transition-[color,box-shadow] outline-none",
        "data-[state=checked]:border-primary data-[state=checked]:bg-primary data-[state=checked]:text-primary-foreground dark:data-[state=checked]:bg-primary-300 dark:data-[state=checked]:border-primary-300",
        "group-hover/checkbox-item:border-primary group-hover/checkbox-item:ring-primary group-hover/checkbox-item:ring",
        "data-[state=checked]:group-hover/checkbox-item:border-primary data-[state=checked]:group-hover/checkbox-item:ring-primary",
        "group-disabled/checkbox-item:data-[state=checked]:bg-muted-400 group-disabled/checkbox-item:data-[state=checked]:border-muted-400",
        className
      ),
      forceMount: true,
      ...props
    },
    children
  );
  var CheckboxGroup = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement("div", { "data-slot": "checkbox-group", className: cn("grid w-full gap-3", className), ...props }, children);
  CheckboxGroup.Item = (0, import_react96.forwardRef)((props, ref) => Item9({ ...props, ref }));
  CheckboxGroup.Indicator = Indicator;
  CheckboxGroup.Item.displayName = "CheckboxGroupItem";
  Indicator.displayName = "CheckboxGroupIndicator";
  CheckboxGroup.displayName = "CheckboxGroup";
  var CheckboxGroup_default = CheckboxGroup;

  // src/components/ui/command/index.ts
  init_define_import_meta_env();

  // src/components/ui/command/Command.tsx
  init_define_import_meta_env();

  // node_modules/cmdk/dist/index.mjs
  init_define_import_meta_env();

  // node_modules/cmdk/dist/chunk-NZJY6EH4.mjs
  init_define_import_meta_env();
  var U = 1;
  var Y = 0.9;
  var H = 0.8;
  var J = 0.17;
  var p = 0.1;
  var u = 0.999;
  var $ = 0.9999;
  var k = 0.99;
  var m = /[\\\/_+.#"@\[\(\{&]/;
  var B = /[\\\/_+.#"@\[\(\{&]/g;
  var K = /[\s-]/;
  var X2 = /[\s-]/g;
  function G(_, C, h, P2, A, f, O) {
    if (f === C.length) return A === _.length ? U : k;
    var T2 = `${A},${f}`;
    if (O[T2] !== void 0) return O[T2];
    for (var L2 = P2.charAt(f), c = h.indexOf(L2, A), S = 0, E, N2, R, M; c >= 0; ) E = G(_, C, h, P2, c + 1, f + 1, O), E > S && (c === A ? E *= U : m.test(_.charAt(c - 1)) ? (E *= H, R = _.slice(A, c - 1).match(B), R && A > 0 && (E *= Math.pow(u, R.length))) : K.test(_.charAt(c - 1)) ? (E *= Y, M = _.slice(A, c - 1).match(X2), M && A > 0 && (E *= Math.pow(u, M.length))) : (E *= J, A > 0 && (E *= Math.pow(u, c - A))), _.charAt(c) !== C.charAt(f) && (E *= $)), (E < p && h.charAt(c - 1) === P2.charAt(f + 1) || P2.charAt(f + 1) === P2.charAt(f) && h.charAt(c - 1) !== P2.charAt(f)) && (N2 = G(_, C, h, P2, c + 1, f + 2, O), N2 * p > E && (E = N2 * p)), E > S && (S = E), c = h.indexOf(L2, c + 1);
    return O[T2] = S, S;
  }
  function D(_) {
    return _.toLowerCase().replace(X2, " ");
  }
  function W(_, C, h) {
    return _ = h && h.length > 0 ? `${_ + " " + h.join(" ")}` : _, G(_, C, D(_), D(C), 0, 0, {});
  }

  // node_modules/cmdk/dist/index.mjs
  var t2 = __toESM(require_react_shim(), 1);

  // node_modules/cmdk/node_modules/@radix-ui/react-primitive/dist/index.mjs
  init_define_import_meta_env();
  var React91 = __toESM(require_react_shim(), 1);
  var ReactDOM8 = __toESM(require_react_dom_shim(), 1);
  var import_jsx_runtime35 = __toESM(require_react_shim(), 1);
  var NODES4 = [
    "a",
    "button",
    "div",
    "form",
    "h2",
    "h3",
    "img",
    "input",
    "label",
    "li",
    "nav",
    "ol",
    "p",
    "select",
    "span",
    "svg",
    "ul"
  ];
  var Primitive4 = NODES4.reduce((primitive, node) => {
    const Slot6 = createSlot(`Primitive.${node}`);
    const Node2 = React91.forwardRef((props, forwardedRef) => {
      const { asChild, ...primitiveProps } = props;
      const Comp = asChild ? Slot6 : node;
      if (typeof window !== "undefined") {
        window[/* @__PURE__ */ Symbol.for("radix-ui")] = true;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(Comp, { ...primitiveProps, ref: forwardedRef });
    });
    Node2.displayName = `Primitive.${node}`;
    return { ...primitive, [node]: Node2 };
  }, {});

  // node_modules/cmdk/dist/index.mjs
  var N = '[cmdk-group=""]';
  var Y2 = '[cmdk-group-items=""]';
  var be = '[cmdk-group-heading=""]';
  var le = '[cmdk-item=""]';
  var ce = `${le}:not([aria-disabled="true"])`;
  var Z = "cmdk-item-select";
  var T = "data-value";
  var Re = (r2, o, n) => W(r2, o, n);
  var ue = t2.createContext(void 0);
  var K2 = () => t2.useContext(ue);
  var de = t2.createContext(void 0);
  var ee = () => t2.useContext(de);
  var fe = t2.createContext(void 0);
  var me = t2.forwardRef((r2, o) => {
    let n = L(() => {
      var e2, a;
      return { search: "", value: (a = (e2 = r2.value) != null ? e2 : r2.defaultValue) != null ? a : "", selectedItemId: void 0, filtered: { count: 0, items: /* @__PURE__ */ new Map(), groups: /* @__PURE__ */ new Set() } };
    }), u2 = L(() => /* @__PURE__ */ new Set()), c = L(() => /* @__PURE__ */ new Map()), d = L(() => /* @__PURE__ */ new Map()), f = L(() => /* @__PURE__ */ new Set()), p2 = pe(r2), { label: b, children: m2, value: R, onValueChange: x, filter: C, shouldFilter: S, loop: A, disablePointerSelection: ge = false, vimBindings: j = true, ...O } = r2, $2 = useId5(), q = useId5(), _ = useId5(), I = t2.useRef(null), v = ke();
    k2(() => {
      if (R !== void 0) {
        let e2 = R.trim();
        n.current.value = e2, E.emit();
      }
    }, [R]), k2(() => {
      v(6, ne);
    }, []);
    let E = t2.useMemo(() => ({ subscribe: (e2) => (f.current.add(e2), () => f.current.delete(e2)), snapshot: () => n.current, setState: (e2, a, s) => {
      var i, l, g, y;
      if (!Object.is(n.current[e2], a)) {
        if (n.current[e2] = a, e2 === "search") J2(), z(), v(1, W2);
        else if (e2 === "value") {
          if (document.activeElement.hasAttribute("cmdk-input") || document.activeElement.hasAttribute("cmdk-root")) {
            let h = document.getElementById(_);
            h ? h.focus() : (i = document.getElementById($2)) == null || i.focus();
          }
          if (v(7, () => {
            var h;
            n.current.selectedItemId = (h = M()) == null ? void 0 : h.id, E.emit();
          }), s || v(5, ne), ((l = p2.current) == null ? void 0 : l.value) !== void 0) {
            let h = a != null ? a : "";
            (y = (g = p2.current).onValueChange) == null || y.call(g, h);
            return;
          }
        }
        E.emit();
      }
    }, emit: () => {
      f.current.forEach((e2) => e2());
    } }), []), U2 = t2.useMemo(() => ({ value: (e2, a, s) => {
      var i;
      a !== ((i = d.current.get(e2)) == null ? void 0 : i.value) && (d.current.set(e2, { value: a, keywords: s }), n.current.filtered.items.set(e2, te(a, s)), v(2, () => {
        z(), E.emit();
      }));
    }, item: (e2, a) => (u2.current.add(e2), a && (c.current.has(a) ? c.current.get(a).add(e2) : c.current.set(a, /* @__PURE__ */ new Set([e2]))), v(3, () => {
      J2(), z(), n.current.value || W2(), E.emit();
    }), () => {
      d.current.delete(e2), u2.current.delete(e2), n.current.filtered.items.delete(e2);
      let s = M();
      v(4, () => {
        J2(), (s == null ? void 0 : s.getAttribute("id")) === e2 && W2(), E.emit();
      });
    }), group: (e2) => (c.current.has(e2) || c.current.set(e2, /* @__PURE__ */ new Set()), () => {
      d.current.delete(e2), c.current.delete(e2);
    }), filter: () => p2.current.shouldFilter, label: b || r2["aria-label"], getDisablePointerSelection: () => p2.current.disablePointerSelection, listId: $2, inputId: _, labelId: q, listInnerRef: I }), []);
    function te(e2, a) {
      var i, l;
      let s = (l = (i = p2.current) == null ? void 0 : i.filter) != null ? l : Re;
      return e2 ? s(e2, n.current.search, a) : 0;
    }
    function z() {
      if (!n.current.search || p2.current.shouldFilter === false) return;
      let e2 = n.current.filtered.items, a = [];
      n.current.filtered.groups.forEach((i) => {
        let l = c.current.get(i), g = 0;
        l.forEach((y) => {
          let h = e2.get(y);
          g = Math.max(h, g);
        }), a.push([i, g]);
      });
      let s = I.current;
      V().sort((i, l) => {
        var h, F;
        let g = i.getAttribute("id"), y = l.getAttribute("id");
        return ((h = e2.get(y)) != null ? h : 0) - ((F = e2.get(g)) != null ? F : 0);
      }).forEach((i) => {
        let l = i.closest(Y2);
        l ? l.appendChild(i.parentElement === l ? i : i.closest(`${Y2} > *`)) : s.appendChild(i.parentElement === s ? i : i.closest(`${Y2} > *`));
      }), a.sort((i, l) => l[1] - i[1]).forEach((i) => {
        var g;
        let l = (g = I.current) == null ? void 0 : g.querySelector(`${N}[${T}="${encodeURIComponent(i[0])}"]`);
        l == null || l.parentElement.appendChild(l);
      });
    }
    function W2() {
      let e2 = V().find((s) => s.getAttribute("aria-disabled") !== "true"), a = e2 == null ? void 0 : e2.getAttribute(T);
      E.setState("value", a || void 0);
    }
    function J2() {
      var a, s, i, l;
      if (!n.current.search || p2.current.shouldFilter === false) {
        n.current.filtered.count = u2.current.size;
        return;
      }
      n.current.filtered.groups = /* @__PURE__ */ new Set();
      let e2 = 0;
      for (let g of u2.current) {
        let y = (s = (a = d.current.get(g)) == null ? void 0 : a.value) != null ? s : "", h = (l = (i = d.current.get(g)) == null ? void 0 : i.keywords) != null ? l : [], F = te(y, h);
        n.current.filtered.items.set(g, F), F > 0 && e2++;
      }
      for (let [g, y] of c.current) for (let h of y) if (n.current.filtered.items.get(h) > 0) {
        n.current.filtered.groups.add(g);
        break;
      }
      n.current.filtered.count = e2;
    }
    function ne() {
      var a, s, i;
      let e2 = M();
      e2 && (((a = e2.parentElement) == null ? void 0 : a.firstChild) === e2 && ((i = (s = e2.closest(N)) == null ? void 0 : s.querySelector(be)) == null || i.scrollIntoView({ block: "nearest" })), e2.scrollIntoView({ block: "nearest" }));
    }
    function M() {
      var e2;
      return (e2 = I.current) == null ? void 0 : e2.querySelector(`${le}[aria-selected="true"]`);
    }
    function V() {
      var e2;
      return Array.from(((e2 = I.current) == null ? void 0 : e2.querySelectorAll(ce)) || []);
    }
    function X3(e2) {
      let s = V()[e2];
      s && E.setState("value", s.getAttribute(T));
    }
    function Q(e2) {
      var g;
      let a = M(), s = V(), i = s.findIndex((y) => y === a), l = s[i + e2];
      (g = p2.current) != null && g.loop && (l = i + e2 < 0 ? s[s.length - 1] : i + e2 === s.length ? s[0] : s[i + e2]), l && E.setState("value", l.getAttribute(T));
    }
    function re2(e2) {
      let a = M(), s = a == null ? void 0 : a.closest(N), i;
      for (; s && !i; ) s = e2 > 0 ? we(s, N) : De(s, N), i = s == null ? void 0 : s.querySelector(ce);
      i ? E.setState("value", i.getAttribute(T)) : Q(e2);
    }
    let oe = () => X3(V().length - 1), ie = (e2) => {
      e2.preventDefault(), e2.metaKey ? oe() : e2.altKey ? re2(1) : Q(1);
    }, se = (e2) => {
      e2.preventDefault(), e2.metaKey ? X3(0) : e2.altKey ? re2(-1) : Q(-1);
    };
    return t2.createElement(Primitive4.div, { ref: o, tabIndex: -1, ...O, "cmdk-root": "", onKeyDown: (e2) => {
      var s;
      (s = O.onKeyDown) == null || s.call(O, e2);
      let a = e2.nativeEvent.isComposing || e2.keyCode === 229;
      if (!(e2.defaultPrevented || a)) switch (e2.key) {
        case "n":
        case "j": {
          j && e2.ctrlKey && ie(e2);
          break;
        }
        case "ArrowDown": {
          ie(e2);
          break;
        }
        case "p":
        case "k": {
          j && e2.ctrlKey && se(e2);
          break;
        }
        case "ArrowUp": {
          se(e2);
          break;
        }
        case "Home": {
          e2.preventDefault(), X3(0);
          break;
        }
        case "End": {
          e2.preventDefault(), oe();
          break;
        }
        case "Enter": {
          e2.preventDefault();
          let i = M();
          if (i) {
            let l = new Event(Z);
            i.dispatchEvent(l);
          }
        }
      }
    } }, t2.createElement("label", { "cmdk-label": "", htmlFor: U2.inputId, id: U2.labelId, style: Te }, b), B2(r2, (e2) => t2.createElement(de.Provider, { value: E }, t2.createElement(ue.Provider, { value: U2 }, e2))));
  });
  var he = t2.forwardRef((r2, o) => {
    var _, I;
    let n = useId5(), u2 = t2.useRef(null), c = t2.useContext(fe), d = K2(), f = pe(r2), p2 = (I = (_ = f.current) == null ? void 0 : _.forceMount) != null ? I : c == null ? void 0 : c.forceMount;
    k2(() => {
      if (!p2) return d.item(n, c == null ? void 0 : c.id);
    }, [p2]);
    let b = ve(n, u2, [r2.value, r2.children, u2], r2.keywords), m2 = ee(), R = P((v) => v.value && v.value === b.current), x = P((v) => p2 || d.filter() === false ? true : v.search ? v.filtered.items.get(n) > 0 : true);
    t2.useEffect(() => {
      let v = u2.current;
      if (!(!v || r2.disabled)) return v.addEventListener(Z, C), () => v.removeEventListener(Z, C);
    }, [x, r2.onSelect, r2.disabled]);
    function C() {
      var v, E;
      S(), (E = (v = f.current).onSelect) == null || E.call(v, b.current);
    }
    function S() {
      m2.setState("value", b.current, true);
    }
    if (!x) return null;
    let { disabled: A, value: ge, onSelect: j, forceMount: O, keywords: $2, ...q } = r2;
    return t2.createElement(Primitive4.div, { ref: composeRefs(u2, o), ...q, id: n, "cmdk-item": "", role: "option", "aria-disabled": !!A, "aria-selected": !!R, "data-disabled": !!A, "data-selected": !!R, onPointerMove: A || d.getDisablePointerSelection() ? void 0 : S, onClick: A ? void 0 : C }, r2.children);
  });
  var Ee = t2.forwardRef((r2, o) => {
    let { heading: n, children: u2, forceMount: c, ...d } = r2, f = useId5(), p2 = t2.useRef(null), b = t2.useRef(null), m2 = useId5(), R = K2(), x = P((S) => c || R.filter() === false ? true : S.search ? S.filtered.groups.has(f) : true);
    k2(() => R.group(f), []), ve(f, p2, [r2.value, r2.heading, b]);
    let C = t2.useMemo(() => ({ id: f, forceMount: c }), [c]);
    return t2.createElement(Primitive4.div, { ref: composeRefs(p2, o), ...d, "cmdk-group": "", role: "presentation", hidden: x ? void 0 : true }, n && t2.createElement("div", { ref: b, "cmdk-group-heading": "", "aria-hidden": true, id: m2 }, n), B2(r2, (S) => t2.createElement("div", { "cmdk-group-items": "", role: "group", "aria-labelledby": n ? m2 : void 0 }, t2.createElement(fe.Provider, { value: C }, S))));
  });
  var ye = t2.forwardRef((r2, o) => {
    let { alwaysRender: n, ...u2 } = r2, c = t2.useRef(null), d = P((f) => !f.search);
    return !n && !d ? null : t2.createElement(Primitive4.div, { ref: composeRefs(c, o), ...u2, "cmdk-separator": "", role: "separator" });
  });
  var Se = t2.forwardRef((r2, o) => {
    let { onValueChange: n, ...u2 } = r2, c = r2.value != null, d = ee(), f = P((m2) => m2.search), p2 = P((m2) => m2.selectedItemId), b = K2();
    return t2.useEffect(() => {
      r2.value != null && d.setState("search", r2.value);
    }, [r2.value]), t2.createElement(Primitive4.input, { ref: o, ...u2, "cmdk-input": "", autoComplete: "off", autoCorrect: "off", spellCheck: false, "aria-autocomplete": "list", role: "combobox", "aria-expanded": true, "aria-controls": b.listId, "aria-labelledby": b.labelId, "aria-activedescendant": p2, id: b.inputId, type: "text", value: c ? r2.value : f, onChange: (m2) => {
      c || d.setState("search", m2.target.value), n == null || n(m2.target.value);
    } });
  });
  var Ce = t2.forwardRef((r2, o) => {
    let { children: n, label: u2 = "Suggestions", ...c } = r2, d = t2.useRef(null), f = t2.useRef(null), p2 = P((m2) => m2.selectedItemId), b = K2();
    return t2.useEffect(() => {
      if (f.current && d.current) {
        let m2 = f.current, R = d.current, x, C = new ResizeObserver(() => {
          x = requestAnimationFrame(() => {
            let S = m2.offsetHeight;
            R.style.setProperty("--cmdk-list-height", S.toFixed(1) + "px");
          });
        });
        return C.observe(m2), () => {
          cancelAnimationFrame(x), C.unobserve(m2);
        };
      }
    }, []), t2.createElement(Primitive4.div, { ref: composeRefs(d, o), ...c, "cmdk-list": "", role: "listbox", tabIndex: -1, "aria-activedescendant": p2, "aria-label": u2, id: b.listId }, B2(r2, (m2) => t2.createElement("div", { ref: composeRefs(f, b.listInnerRef), "cmdk-list-sizer": "" }, m2)));
  });
  var xe = t2.forwardRef((r2, o) => {
    let { open: n, onOpenChange: u2, overlayClassName: c, contentClassName: d, container: f, ...p2 } = r2;
    return t2.createElement(Root8, { open: n, onOpenChange: u2 }, t2.createElement(Portal4, { container: f }, t2.createElement(Overlay, { "cmdk-overlay": "", className: c }), t2.createElement(Content10, { "aria-label": r2.label, "cmdk-dialog": "", className: d }, t2.createElement(me, { ref: o, ...p2 }))));
  });
  var Ie = t2.forwardRef((r2, o) => P((u2) => u2.filtered.count === 0) ? t2.createElement(Primitive4.div, { ref: o, ...r2, "cmdk-empty": "", role: "presentation" }) : null);
  var Pe = t2.forwardRef((r2, o) => {
    let { progress: n, children: u2, label: c = "Loading...", ...d } = r2;
    return t2.createElement(Primitive4.div, { ref: o, ...d, "cmdk-loading": "", role: "progressbar", "aria-valuenow": n, "aria-valuemin": 0, "aria-valuemax": 100, "aria-label": c }, B2(r2, (f) => t2.createElement("div", { "aria-hidden": true }, f)));
  });
  var _e = Object.assign(me, { List: Ce, Item: he, Input: Se, Group: Ee, Separator: ye, Dialog: xe, Empty: Ie, Loading: Pe });
  function we(r2, o) {
    let n = r2.nextElementSibling;
    for (; n; ) {
      if (n.matches(o)) return n;
      n = n.nextElementSibling;
    }
  }
  function De(r2, o) {
    let n = r2.previousElementSibling;
    for (; n; ) {
      if (n.matches(o)) return n;
      n = n.previousElementSibling;
    }
  }
  function pe(r2) {
    let o = t2.useRef(r2);
    return k2(() => {
      o.current = r2;
    }), o;
  }
  var k2 = typeof window == "undefined" ? t2.useEffect : t2.useLayoutEffect;
  function L(r2) {
    let o = t2.useRef();
    return o.current === void 0 && (o.current = r2()), o;
  }
  function P(r2) {
    let o = ee(), n = () => r2(o.snapshot());
    return t2.useSyncExternalStore(o.subscribe, n, n);
  }
  function ve(r2, o, n, u2 = []) {
    let c = t2.useRef(), d = K2();
    return k2(() => {
      var b;
      let f = (() => {
        var m2;
        for (let R of n) {
          if (typeof R == "string") return R.trim();
          if (typeof R == "object" && "current" in R) return R.current ? (m2 = R.current.textContent) == null ? void 0 : m2.trim() : c.current;
        }
      })(), p2 = u2.map((m2) => m2.trim());
      d.value(r2, f, p2), (b = o.current) == null || b.setAttribute(T, f), c.current = f;
    }), c;
  }
  var ke = () => {
    let [r2, o] = t2.useState(), n = L(() => /* @__PURE__ */ new Map());
    return k2(() => {
      n.current.forEach((u2) => u2()), n.current = /* @__PURE__ */ new Map();
    }, [r2]), (u2, c) => {
      n.current.set(u2, c), o({});
    };
  };
  function Me(r2) {
    let o = r2.type;
    return typeof o == "function" ? o(r2.props) : "render" in o ? o.render(r2.props) : r2;
  }
  function B2({ asChild: r2, children: o }, n) {
    return r2 && t2.isValidElement(o) ? t2.cloneElement(Me(o), { ref: o.ref }, n(o.props.children)) : n(o);
  }
  var Te = { position: "absolute", width: "1px", height: "1px", padding: "0", margin: "-1px", overflow: "hidden", clip: "rect(0, 0, 0, 0)", whiteSpace: "nowrap", borderWidth: "0" };

  // src/components/ui/command/Command.tsx
  var CommandInput = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement("div", { className: "border-b-muted-200 flex items-center gap-2 border-b px-3", "cmdk-input-wrapper": "" }, /* @__PURE__ */ React.createElement(Search, { size: 16, className: "text-muted-foreground shrink-0" }), /* @__PURE__ */ React.createElement(
    _e.Input,
    {
      "data-slot": "command-input",
      className: cn("flex w-full rounded-md bg-transparent py-3 text-sm outline-none disabled:pointer-events-none", className),
      ...props
    }
  ), children);
  var CommandList = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    _e.List,
    {
      "data-slot": "command-list",
      className: cn("flex max-h-96 flex-col overflow-x-hidden overflow-y-auto", className),
      ...props
    }
  );
  var CommandEmpty = ({ when, ...props }) => {
    if (!when) return null;
    return /* @__PURE__ */ React.createElement(_e.Empty, { "data-slot": "command-empty", className: "py-6 text-center text-sm", ...props });
  };
  var CommandGroup = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    _e.Group,
    {
      "data-slot": "command-group",
      className: cn(
        "text-foreground **:[[cmdk-group-heading]]:overflow-hidden",
        "**:[[cmdk-group-heading]]:px-2 **:[[cmdk-group-heading]]:py-1.5 **:[[cmdk-group-heading]]:text-xs **:[[cmdk-group-heading]]:font-medium",
        className
      ),
      ...props
    }
  );
  var CommandItem = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    _e.Item,
    {
      "data-slot": "command-item",
      className: cn(
        "group/command-item bg-background border-muted-200 flex w-full items-center gap-2 px-3 py-2 text-sm",
        "relative cursor-pointer leading-none transition-colors outline-none select-none not-last:border-b",
        "hover:bg-primary-15",
        "data-[checked=true]:bg-primary-25 data-[checked=true]:text-primary-900 data-[checked=true]:border-b-muted-50",
        "data-[disabled=true]:pointer-events-none data-[disabled=true]:opacity-50",
        className
      ),
      ...props
    }
  );
  var CommandItemIndicator = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    "span",
    {
      "data-slot": "command-item-indicator",
      className: cn(
        "flex shrink-0",
        !children && "opacity-0 transition-opacity group-data-[checked=true]/command-item:opacity-100",
        className
      ),
      ...props
    },
    children ?? /* @__PURE__ */ React.createElement(Check, { size: 16 })
  );
  var CommandShortcut = ({ className, ...props }) => /* @__PURE__ */ React.createElement("span", { className: cn("ms-auto text-xs tracking-widest", className), ...props });
  var CommandItemSeparator = ({ className, ...props }) => /* @__PURE__ */ React.createElement(_e.Separator, { "data-slot": "command-separator", className: cn("bg-muted-200 h-px w-full", className), ...props });
  var CommandDialog = ({ children, ...props }) => /* @__PURE__ */ React.createElement(Dialog_default, { ...props }, /* @__PURE__ */ React.createElement(Dialog_default.Panel, { className: "overflow-hidden p-0" }, /* @__PURE__ */ React.createElement(
    Command,
    {
      className: cn(
        "**:[[cmdk-group-heading]]:px-2 **:[[cmdk-group-heading]]:font-medium",
        "[&_[cmdk-group]:not([hidden])_~[cmdk-group]]:pt-0 [&_[cmdk-input-wrapper]_svg]:h-5",
        "[&_[cmdk-input-wrapper]_svg]:w-5 [&_[cmdk-item]_svg]:h-5 [&_[cmdk-item]_svg]:w-5 **:[[cmdk-group]]:px-2",
        "**:[[cmdk-input]]:h-12 **:[[cmdk-item]]:p-3"
      )
    },
    children
  )));
  var Command = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    _e,
    {
      "data-slot": "command",
      className: cn("bg-popover text-popover-foreground flex h-full w-full flex-col overflow-hidden rounded-md shadow-xs", className),
      ...props
    }
  );
  Command.Input = CommandInput;
  Command.List = CommandList;
  Command.Empty = CommandEmpty;
  Command.Group = CommandGroup;
  Command.Item = CommandItem;
  Command.Indicator = CommandItemIndicator;
  Command.Shortcut = CommandShortcut;
  Command.Separator = CommandItemSeparator;
  Command.Dialog = CommandDialog;
  CommandInput.displayName = "CommandInput";
  CommandList.displayName = "CommandList";
  CommandEmpty.displayName = "CommandEmpty";
  CommandGroup.displayName = "CommandGroup";
  CommandItem.displayName = "CommandItem";
  CommandItemIndicator.displayName = "CommandIndicator";
  CommandShortcut.displayName = "CommandShortcut";
  CommandItemSeparator.displayName = "CommandSeparator";
  CommandDialog.displayName = "CommandDialog";
  Command.displayName = "Command";
  var Command_default = Command;

  // src/components/ui/progress/index.ts
  init_define_import_meta_env();

  // src/components/ui/progress/Progress.tsx
  init_define_import_meta_env();

  // node_modules/@radix-ui/react-progress/dist/index.mjs
  init_define_import_meta_env();
  var React94 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-progress/node_modules/@radix-ui/react-context/dist/index.mjs
  init_define_import_meta_env();
  var React92 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime36 = __toESM(require_react_shim(), 1);
  function createContextScope3(scopeName, createContextScopeDeps = []) {
    let defaultContexts = [];
    function createContext32(rootComponentName, defaultContext2) {
      const BaseContext = React92.createContext(defaultContext2);
      BaseContext.displayName = rootComponentName + "Context";
      const index2 = defaultContexts.length;
      defaultContexts = [...defaultContexts, defaultContext2];
      const Provider4 = (props) => {
        const { scope, children, ...context2 } = props;
        const Context = scope?.[scopeName]?.[index2] || BaseContext;
        const value = React92.useMemo(() => context2, Object.values(context2));
        return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(Context.Provider, { value, children });
      };
      Provider4.displayName = rootComponentName + "Provider";
      function useContext210(consumerName, scope) {
        const Context = scope?.[scopeName]?.[index2] || BaseContext;
        const context2 = React92.useContext(Context);
        if (context2) return context2;
        if (defaultContext2 !== void 0) return defaultContext2;
        throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
      }
      return [Provider4, useContext210];
    }
    const createScope = () => {
      const scopeContexts = defaultContexts.map((defaultContext2) => {
        return React92.createContext(defaultContext2);
      });
      return function useScope(scope) {
        const contexts = scope?.[scopeName] || scopeContexts;
        return React92.useMemo(
          () => ({ [`__scope${scopeName}`]: { ...scope, [scopeName]: contexts } }),
          [scope, contexts]
        );
      };
    };
    createScope.scopeName = scopeName;
    return [createContext32, composeContextScopes3(createScope, ...createContextScopeDeps)];
  }
  function composeContextScopes3(...scopes) {
    const baseScope = scopes[0];
    if (scopes.length === 1) return baseScope;
    const createScope = () => {
      const scopeHooks = scopes.map((createScope2) => ({
        useScope: createScope2(),
        scopeName: createScope2.scopeName
      }));
      return function useComposedScopes(overrideScopes) {
        const nextScopes = scopeHooks.reduce((nextScopes2, { useScope, scopeName }) => {
          const scopeProps = useScope(overrideScopes);
          const currentScope = scopeProps[`__scope${scopeName}`];
          return { ...nextScopes2, ...currentScope };
        }, {});
        return React92.useMemo(() => ({ [`__scope${baseScope.scopeName}`]: nextScopes }), [nextScopes]);
      };
    };
    createScope.scopeName = baseScope.scopeName;
    return createScope;
  }

  // node_modules/@radix-ui/react-progress/node_modules/@radix-ui/react-primitive/dist/index.mjs
  init_define_import_meta_env();
  var React93 = __toESM(require_react_shim(), 1);
  var ReactDOM9 = __toESM(require_react_dom_shim(), 1);
  var import_jsx_runtime37 = __toESM(require_react_shim(), 1);
  var NODES5 = [
    "a",
    "button",
    "div",
    "form",
    "h2",
    "h3",
    "img",
    "input",
    "label",
    "li",
    "nav",
    "ol",
    "p",
    "select",
    "span",
    "svg",
    "ul"
  ];
  var Primitive5 = NODES5.reduce((primitive, node) => {
    const Slot6 = createSlot(`Primitive.${node}`);
    const Node2 = React93.forwardRef((props, forwardedRef) => {
      const { asChild, ...primitiveProps } = props;
      const Comp = asChild ? Slot6 : node;
      if (typeof window !== "undefined") {
        window[/* @__PURE__ */ Symbol.for("radix-ui")] = true;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(Comp, { ...primitiveProps, ref: forwardedRef });
    });
    Node2.displayName = `Primitive.${node}`;
    return { ...primitive, [node]: Node2 };
  }, {});

  // node_modules/@radix-ui/react-progress/dist/index.mjs
  var import_jsx_runtime38 = __toESM(require_react_shim(), 1);
  var PROGRESS_NAME = "Progress";
  var DEFAULT_MAX = 100;
  var [createProgressContext, createProgressScope] = createContextScope3(PROGRESS_NAME);
  var [ProgressProvider, useProgressContext] = createProgressContext(PROGRESS_NAME);
  var Progress = React94.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeProgress,
        value: valueProp = null,
        max: maxProp,
        getValueLabel = defaultGetValueLabel,
        ...progressProps
      } = props;
      if ((maxProp || maxProp === 0) && !isValidMaxNumber(maxProp)) {
        console.error(getInvalidMaxError(`${maxProp}`, "Progress"));
      }
      const max3 = isValidMaxNumber(maxProp) ? maxProp : DEFAULT_MAX;
      if (valueProp !== null && !isValidValueNumber(valueProp, max3)) {
        console.error(getInvalidValueError(`${valueProp}`, "Progress"));
      }
      const value = isValidValueNumber(valueProp, max3) ? valueProp : null;
      const valueLabel = isNumber2(value) ? getValueLabel(value, max3) : void 0;
      return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(ProgressProvider, { scope: __scopeProgress, value, max: max3, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
        Primitive5.div,
        {
          "aria-valuemax": max3,
          "aria-valuemin": 0,
          "aria-valuenow": isNumber2(value) ? value : void 0,
          "aria-valuetext": valueLabel,
          role: "progressbar",
          "data-state": getProgressState(value, max3),
          "data-value": value ?? void 0,
          "data-max": max3,
          ...progressProps,
          ref: forwardedRef
        }
      ) });
    }
  );
  Progress.displayName = PROGRESS_NAME;
  var INDICATOR_NAME3 = "ProgressIndicator";
  var ProgressIndicator = React94.forwardRef(
    (props, forwardedRef) => {
      const { __scopeProgress, ...indicatorProps } = props;
      const context2 = useProgressContext(INDICATOR_NAME3, __scopeProgress);
      return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
        Primitive5.div,
        {
          "data-state": getProgressState(context2.value, context2.max),
          "data-value": context2.value ?? void 0,
          "data-max": context2.max,
          ...indicatorProps,
          ref: forwardedRef
        }
      );
    }
  );
  ProgressIndicator.displayName = INDICATOR_NAME3;
  function defaultGetValueLabel(value, max3) {
    return `${Math.round(value / max3 * 100)}%`;
  }
  function getProgressState(value, maxValue) {
    return value == null ? "indeterminate" : value === maxValue ? "complete" : "loading";
  }
  function isNumber2(value) {
    return typeof value === "number";
  }
  function isValidMaxNumber(max3) {
    return isNumber2(max3) && !isNaN(max3) && max3 > 0;
  }
  function isValidValueNumber(value, max3) {
    return isNumber2(value) && !isNaN(value) && value <= max3 && value >= 0;
  }
  function getInvalidMaxError(propValue, componentName) {
    return `Invalid prop \`max\` of value \`${propValue}\` supplied to \`${componentName}\`. Only numbers greater than 0 are valid max values. Defaulting to \`${DEFAULT_MAX}\`.`;
  }
  function getInvalidValueError(propValue, componentName) {
    return `Invalid prop \`value\` of value \`${propValue}\` supplied to \`${componentName}\`. The \`value\` prop must be:
  - a positive number
  - less than the value passed to \`max\` (or ${DEFAULT_MAX} if no \`max\` prop is set)
  - \`null\` or \`undefined\` if the progress is indeterminate.

Defaulting to \`null\`.`;
  }
  var Root10 = Progress;
  var Indicator2 = ProgressIndicator;

  // src/components/ui/progress/Progress.tsx
  var progressVariants = cva("relative w-full overflow-hidden rounded-full", {
    variants: {
      size: {
        sm: "h-2",
        md: "h-4",
        lg: "h-6"
      },
      variant: {
        default: "bg-accent",
        success: "bg-success",
        destructive: "bg-destructive",
        secondary: "bg-secondary"
      }
    },
    defaultVariants: {
      size: "md",
      variant: "default"
    }
  });
  var progressIndicatorVariants = cva("h-full animate-in duration-500", {
    variants: {
      variant: {
        default: "bg-primary",
        success: "bg-success",
        destructive: "bg-destructive",
        secondary: "bg-secondary",
        accent: "bg-accent"
      }
    },
    defaultVariants: {
      variant: "default"
    }
  });
  var Progress2 = ({ className, value, fullValue = 100, size: size4, variant, indicatorVariant, ...props }) => {
    return /* @__PURE__ */ React.createElement(Root10, { "data-slot": "progress", className: cn(progressVariants({ variant, size: size4, className })), ...props }, /* @__PURE__ */ React.createElement(
      Indicator2,
      {
        "data-slot": "progress-indicator",
        className: cn(progressIndicatorVariants({ variant: indicatorVariant })),
        style: { transform: `translateX(-${100 - value / fullValue * 100}%)` }
      }
    ));
  };
  var Progress_default = Progress2;

  // src/components/ui/scroll-area/index.ts
  init_define_import_meta_env();

  // src/components/ui/scroll-area/ScrollArea.tsx
  init_define_import_meta_env();

  // node_modules/@radix-ui/react-scroll-area/dist/index.mjs
  init_define_import_meta_env();
  var React210 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/number/dist/index.mjs
  init_define_import_meta_env();
  function clamp2(value, [min3, max3]) {
    return Math.min(max3, Math.max(min3, value));
  }

  // node_modules/@radix-ui/react-scroll-area/dist/index.mjs
  var React95 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime39 = __toESM(require_react_shim(), 1);
  function useStateMachine2(initialState2, machine) {
    return React95.useReducer((state, event) => {
      const nextState = machine[state][event];
      return nextState ?? state;
    }, initialState2);
  }
  var SCROLL_AREA_NAME = "ScrollArea";
  var [createScrollAreaContext, createScrollAreaScope] = createContextScope(SCROLL_AREA_NAME);
  var [ScrollAreaProvider, useScrollAreaContext] = createScrollAreaContext(SCROLL_AREA_NAME);
  var ScrollArea = React210.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeScrollArea,
        type = "hover",
        dir: dir2,
        scrollHideDelay = 600,
        ...scrollAreaProps
      } = props;
      const [scrollArea, setScrollArea] = React210.useState(null);
      const [viewport, setViewport] = React210.useState(null);
      const [content, setContent] = React210.useState(null);
      const [scrollbarX, setScrollbarX] = React210.useState(null);
      const [scrollbarY, setScrollbarY] = React210.useState(null);
      const [cornerWidth, setCornerWidth] = React210.useState(0);
      const [cornerHeight, setCornerHeight] = React210.useState(0);
      const [scrollbarXEnabled, setScrollbarXEnabled] = React210.useState(false);
      const [scrollbarYEnabled, setScrollbarYEnabled] = React210.useState(false);
      const composedRefs = useComposedRefs(forwardedRef, (node) => setScrollArea(node));
      const direction = useDirection(dir2);
      return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
        ScrollAreaProvider,
        {
          scope: __scopeScrollArea,
          type,
          dir: direction,
          scrollHideDelay,
          scrollArea,
          viewport,
          onViewportChange: setViewport,
          content,
          onContentChange: setContent,
          scrollbarX,
          onScrollbarXChange: setScrollbarX,
          scrollbarXEnabled,
          onScrollbarXEnabledChange: setScrollbarXEnabled,
          scrollbarY,
          onScrollbarYChange: setScrollbarY,
          scrollbarYEnabled,
          onScrollbarYEnabledChange: setScrollbarYEnabled,
          onCornerWidthChange: setCornerWidth,
          onCornerHeightChange: setCornerHeight,
          children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
            Primitive2.div,
            {
              dir: direction,
              ...scrollAreaProps,
              ref: composedRefs,
              style: {
                position: "relative",
                // Pass corner sizes as CSS vars to reduce re-renders of context consumers
                ["--radix-scroll-area-corner-width"]: cornerWidth + "px",
                ["--radix-scroll-area-corner-height"]: cornerHeight + "px",
                ...props.style
              }
            }
          )
        }
      );
    }
  );
  ScrollArea.displayName = SCROLL_AREA_NAME;
  var VIEWPORT_NAME2 = "ScrollAreaViewport";
  var ScrollAreaViewport = React210.forwardRef(
    (props, forwardedRef) => {
      const { __scopeScrollArea, children, nonce, ...viewportProps } = props;
      const context2 = useScrollAreaContext(VIEWPORT_NAME2, __scopeScrollArea);
      const ref = React210.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref, context2.onViewportChange);
      return /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)(import_jsx_runtime39.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
          "style",
          {
            dangerouslySetInnerHTML: {
              __html: `[data-radix-scroll-area-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-scroll-area-viewport]::-webkit-scrollbar{display:none}`
            },
            nonce
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
          Primitive2.div,
          {
            "data-radix-scroll-area-viewport": "",
            ...viewportProps,
            ref: composedRefs,
            style: {
              /**
               * We don't support `visible` because the intention is to have at least one scrollbar
               * if this component is used and `visible` will behave like `auto` in that case
               * https://developer.mozilla.org/en-US/docs/Web/CSS/overflow#description
               *
               * We don't handle `auto` because the intention is for the native implementation
               * to be hidden if using this component. We just want to ensure the node is scrollable
               * so could have used either `scroll` or `auto` here. We picked `scroll` to prevent
               * the browser from having to work out whether to render native scrollbars or not,
               * we tell it to with the intention of hiding them in CSS.
               */
              overflowX: context2.scrollbarXEnabled ? "scroll" : "hidden",
              overflowY: context2.scrollbarYEnabled ? "scroll" : "hidden",
              ...props.style
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("div", { ref: context2.onContentChange, style: { minWidth: "100%", display: "table" }, children })
          }
        )
      ] });
    }
  );
  ScrollAreaViewport.displayName = VIEWPORT_NAME2;
  var SCROLLBAR_NAME = "ScrollAreaScrollbar";
  var ScrollAreaScrollbar = React210.forwardRef(
    (props, forwardedRef) => {
      const { forceMount, ...scrollbarProps } = props;
      const context2 = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
      const { onScrollbarXEnabledChange, onScrollbarYEnabledChange } = context2;
      const isHorizontal = props.orientation === "horizontal";
      React210.useEffect(() => {
        isHorizontal ? onScrollbarXEnabledChange(true) : onScrollbarYEnabledChange(true);
        return () => {
          isHorizontal ? onScrollbarXEnabledChange(false) : onScrollbarYEnabledChange(false);
        };
      }, [isHorizontal, onScrollbarXEnabledChange, onScrollbarYEnabledChange]);
      return context2.type === "hover" ? /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(ScrollAreaScrollbarHover, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context2.type === "scroll" ? /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(ScrollAreaScrollbarScroll, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context2.type === "auto" ? /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(ScrollAreaScrollbarAuto, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context2.type === "always" ? /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(ScrollAreaScrollbarVisible, { ...scrollbarProps, ref: forwardedRef }) : null;
    }
  );
  ScrollAreaScrollbar.displayName = SCROLLBAR_NAME;
  var ScrollAreaScrollbarHover = React210.forwardRef((props, forwardedRef) => {
    const { forceMount, ...scrollbarProps } = props;
    const context2 = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const [visible, setVisible] = React210.useState(false);
    React210.useEffect(() => {
      const scrollArea = context2.scrollArea;
      let hideTimer = 0;
      if (scrollArea) {
        const handlePointerEnter = () => {
          window.clearTimeout(hideTimer);
          setVisible(true);
        };
        const handlePointerLeave = () => {
          hideTimer = window.setTimeout(() => setVisible(false), context2.scrollHideDelay);
        };
        scrollArea.addEventListener("pointerenter", handlePointerEnter);
        scrollArea.addEventListener("pointerleave", handlePointerLeave);
        return () => {
          window.clearTimeout(hideTimer);
          scrollArea.removeEventListener("pointerenter", handlePointerEnter);
          scrollArea.removeEventListener("pointerleave", handlePointerLeave);
        };
      }
    }, [context2.scrollArea, context2.scrollHideDelay]);
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(Presence, { present: forceMount || visible, children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
      ScrollAreaScrollbarAuto,
      {
        "data-state": visible ? "visible" : "hidden",
        ...scrollbarProps,
        ref: forwardedRef
      }
    ) });
  });
  var ScrollAreaScrollbarScroll = React210.forwardRef((props, forwardedRef) => {
    const { forceMount, ...scrollbarProps } = props;
    const context2 = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const isHorizontal = props.orientation === "horizontal";
    const debounceScrollEnd = useDebounceCallback(() => send("SCROLL_END"), 100);
    const [state, send] = useStateMachine2("hidden", {
      hidden: {
        SCROLL: "scrolling"
      },
      scrolling: {
        SCROLL_END: "idle",
        POINTER_ENTER: "interacting"
      },
      interacting: {
        SCROLL: "interacting",
        POINTER_LEAVE: "idle"
      },
      idle: {
        HIDE: "hidden",
        SCROLL: "scrolling",
        POINTER_ENTER: "interacting"
      }
    });
    React210.useEffect(() => {
      if (state === "idle") {
        const hideTimer = window.setTimeout(() => send("HIDE"), context2.scrollHideDelay);
        return () => window.clearTimeout(hideTimer);
      }
    }, [state, context2.scrollHideDelay, send]);
    React210.useEffect(() => {
      const viewport = context2.viewport;
      const scrollDirection = isHorizontal ? "scrollLeft" : "scrollTop";
      if (viewport) {
        let prevScrollPos = viewport[scrollDirection];
        const handleScroll2 = () => {
          const scrollPos = viewport[scrollDirection];
          const hasScrollInDirectionChanged = prevScrollPos !== scrollPos;
          if (hasScrollInDirectionChanged) {
            send("SCROLL");
            debounceScrollEnd();
          }
          prevScrollPos = scrollPos;
        };
        viewport.addEventListener("scroll", handleScroll2);
        return () => viewport.removeEventListener("scroll", handleScroll2);
      }
    }, [context2.viewport, isHorizontal, send, debounceScrollEnd]);
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(Presence, { present: forceMount || state !== "hidden", children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
      ScrollAreaScrollbarVisible,
      {
        "data-state": state === "hidden" ? "hidden" : "visible",
        ...scrollbarProps,
        ref: forwardedRef,
        onPointerEnter: composeEventHandlers(props.onPointerEnter, () => send("POINTER_ENTER")),
        onPointerLeave: composeEventHandlers(props.onPointerLeave, () => send("POINTER_LEAVE"))
      }
    ) });
  });
  var ScrollAreaScrollbarAuto = React210.forwardRef((props, forwardedRef) => {
    const context2 = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const { forceMount, ...scrollbarProps } = props;
    const [visible, setVisible] = React210.useState(false);
    const isHorizontal = props.orientation === "horizontal";
    const handleResize = useDebounceCallback(() => {
      if (context2.viewport) {
        const isOverflowX = context2.viewport.offsetWidth < context2.viewport.scrollWidth;
        const isOverflowY = context2.viewport.offsetHeight < context2.viewport.scrollHeight;
        setVisible(isHorizontal ? isOverflowX : isOverflowY);
      }
    }, 10);
    useResizeObserver(context2.viewport, handleResize);
    useResizeObserver(context2.content, handleResize);
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(Presence, { present: forceMount || visible, children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
      ScrollAreaScrollbarVisible,
      {
        "data-state": visible ? "visible" : "hidden",
        ...scrollbarProps,
        ref: forwardedRef
      }
    ) });
  });
  var ScrollAreaScrollbarVisible = React210.forwardRef((props, forwardedRef) => {
    const { orientation = "vertical", ...scrollbarProps } = props;
    const context2 = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const thumbRef = React210.useRef(null);
    const pointerOffsetRef = React210.useRef(0);
    const [sizes, setSizes] = React210.useState({
      content: 0,
      viewport: 0,
      scrollbar: { size: 0, paddingStart: 0, paddingEnd: 0 }
    });
    const thumbRatio = getThumbRatio(sizes.viewport, sizes.content);
    const commonProps = {
      ...scrollbarProps,
      sizes,
      onSizesChange: setSizes,
      hasThumb: Boolean(thumbRatio > 0 && thumbRatio < 1),
      onThumbChange: (thumb) => thumbRef.current = thumb,
      onThumbPointerUp: () => pointerOffsetRef.current = 0,
      onThumbPointerDown: (pointerPos) => pointerOffsetRef.current = pointerPos
    };
    function getScrollPosition(pointerPos, dir2) {
      return getScrollPositionFromPointer(pointerPos, pointerOffsetRef.current, sizes, dir2);
    }
    if (orientation === "horizontal") {
      return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
        ScrollAreaScrollbarX,
        {
          ...commonProps,
          ref: forwardedRef,
          onThumbPositionChange: () => {
            if (context2.viewport && thumbRef.current) {
              const scrollPos = context2.viewport.scrollLeft;
              const offset4 = getThumbOffsetFromScroll(scrollPos, sizes, context2.dir);
              thumbRef.current.style.transform = `translate3d(${offset4}px, 0, 0)`;
            }
          },
          onWheelScroll: (scrollPos) => {
            if (context2.viewport) context2.viewport.scrollLeft = scrollPos;
          },
          onDragScroll: (pointerPos) => {
            if (context2.viewport) {
              context2.viewport.scrollLeft = getScrollPosition(pointerPos, context2.dir);
            }
          }
        }
      );
    }
    if (orientation === "vertical") {
      return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
        ScrollAreaScrollbarY,
        {
          ...commonProps,
          ref: forwardedRef,
          onThumbPositionChange: () => {
            if (context2.viewport && thumbRef.current) {
              const scrollPos = context2.viewport.scrollTop;
              const offset4 = getThumbOffsetFromScroll(scrollPos, sizes);
              thumbRef.current.style.transform = `translate3d(0, ${offset4}px, 0)`;
            }
          },
          onWheelScroll: (scrollPos) => {
            if (context2.viewport) context2.viewport.scrollTop = scrollPos;
          },
          onDragScroll: (pointerPos) => {
            if (context2.viewport) context2.viewport.scrollTop = getScrollPosition(pointerPos);
          }
        }
      );
    }
    return null;
  });
  var ScrollAreaScrollbarX = React210.forwardRef((props, forwardedRef) => {
    const { sizes, onSizesChange, ...scrollbarProps } = props;
    const context2 = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const [computedStyle, setComputedStyle] = React210.useState();
    const ref = React210.useRef(null);
    const composeRefs2 = useComposedRefs(forwardedRef, ref, context2.onScrollbarXChange);
    React210.useEffect(() => {
      if (ref.current) setComputedStyle(getComputedStyle(ref.current));
    }, [ref]);
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
      ScrollAreaScrollbarImpl,
      {
        "data-orientation": "horizontal",
        ...scrollbarProps,
        ref: composeRefs2,
        sizes,
        style: {
          bottom: 0,
          left: context2.dir === "rtl" ? "var(--radix-scroll-area-corner-width)" : 0,
          right: context2.dir === "ltr" ? "var(--radix-scroll-area-corner-width)" : 0,
          ["--radix-scroll-area-thumb-width"]: getThumbSize(sizes) + "px",
          ...props.style
        },
        onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.x),
        onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.x),
        onWheelScroll: (event, maxScrollPos) => {
          if (context2.viewport) {
            const scrollPos = context2.viewport.scrollLeft + event.deltaX;
            props.onWheelScroll(scrollPos);
            if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
              event.preventDefault();
            }
          }
        },
        onResize: () => {
          if (ref.current && context2.viewport && computedStyle) {
            onSizesChange({
              content: context2.viewport.scrollWidth,
              viewport: context2.viewport.offsetWidth,
              scrollbar: {
                size: ref.current.clientWidth,
                paddingStart: toInt(computedStyle.paddingLeft),
                paddingEnd: toInt(computedStyle.paddingRight)
              }
            });
          }
        }
      }
    );
  });
  var ScrollAreaScrollbarY = React210.forwardRef((props, forwardedRef) => {
    const { sizes, onSizesChange, ...scrollbarProps } = props;
    const context2 = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const [computedStyle, setComputedStyle] = React210.useState();
    const ref = React210.useRef(null);
    const composeRefs2 = useComposedRefs(forwardedRef, ref, context2.onScrollbarYChange);
    React210.useEffect(() => {
      if (ref.current) setComputedStyle(getComputedStyle(ref.current));
    }, [ref]);
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
      ScrollAreaScrollbarImpl,
      {
        "data-orientation": "vertical",
        ...scrollbarProps,
        ref: composeRefs2,
        sizes,
        style: {
          top: 0,
          right: context2.dir === "ltr" ? 0 : void 0,
          left: context2.dir === "rtl" ? 0 : void 0,
          bottom: "var(--radix-scroll-area-corner-height)",
          ["--radix-scroll-area-thumb-height"]: getThumbSize(sizes) + "px",
          ...props.style
        },
        onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.y),
        onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.y),
        onWheelScroll: (event, maxScrollPos) => {
          if (context2.viewport) {
            const scrollPos = context2.viewport.scrollTop + event.deltaY;
            props.onWheelScroll(scrollPos);
            if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
              event.preventDefault();
            }
          }
        },
        onResize: () => {
          if (ref.current && context2.viewport && computedStyle) {
            onSizesChange({
              content: context2.viewport.scrollHeight,
              viewport: context2.viewport.offsetHeight,
              scrollbar: {
                size: ref.current.clientHeight,
                paddingStart: toInt(computedStyle.paddingTop),
                paddingEnd: toInt(computedStyle.paddingBottom)
              }
            });
          }
        }
      }
    );
  });
  var [ScrollbarProvider, useScrollbarContext] = createScrollAreaContext(SCROLLBAR_NAME);
  var ScrollAreaScrollbarImpl = React210.forwardRef((props, forwardedRef) => {
    const {
      __scopeScrollArea,
      sizes,
      hasThumb,
      onThumbChange,
      onThumbPointerUp,
      onThumbPointerDown,
      onThumbPositionChange,
      onDragScroll,
      onWheelScroll,
      onResize,
      ...scrollbarProps
    } = props;
    const context2 = useScrollAreaContext(SCROLLBAR_NAME, __scopeScrollArea);
    const [scrollbar, setScrollbar] = React210.useState(null);
    const composeRefs2 = useComposedRefs(forwardedRef, (node) => setScrollbar(node));
    const rectRef = React210.useRef(null);
    const prevWebkitUserSelectRef = React210.useRef("");
    const viewport = context2.viewport;
    const maxScrollPos = sizes.content - sizes.viewport;
    const handleWheelScroll = useCallbackRef(onWheelScroll);
    const handleThumbPositionChange = useCallbackRef(onThumbPositionChange);
    const handleResize = useDebounceCallback(onResize, 10);
    function handleDragScroll(event) {
      if (rectRef.current) {
        const x = event.clientX - rectRef.current.left;
        const y = event.clientY - rectRef.current.top;
        onDragScroll({ x, y });
      }
    }
    React210.useEffect(() => {
      const handleWheel = (event) => {
        const element = event.target;
        const isScrollbarWheel = scrollbar?.contains(element);
        if (isScrollbarWheel) handleWheelScroll(event, maxScrollPos);
      };
      document.addEventListener("wheel", handleWheel, { passive: false });
      return () => document.removeEventListener("wheel", handleWheel, { passive: false });
    }, [viewport, scrollbar, maxScrollPos, handleWheelScroll]);
    React210.useEffect(handleThumbPositionChange, [sizes, handleThumbPositionChange]);
    useResizeObserver(scrollbar, handleResize);
    useResizeObserver(context2.content, handleResize);
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
      ScrollbarProvider,
      {
        scope: __scopeScrollArea,
        scrollbar,
        hasThumb,
        onThumbChange: useCallbackRef(onThumbChange),
        onThumbPointerUp: useCallbackRef(onThumbPointerUp),
        onThumbPositionChange: handleThumbPositionChange,
        onThumbPointerDown: useCallbackRef(onThumbPointerDown),
        children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
          Primitive2.div,
          {
            ...scrollbarProps,
            ref: composeRefs2,
            style: { position: "absolute", ...scrollbarProps.style },
            onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
              const mainPointer = 0;
              if (event.button === mainPointer) {
                const element = event.target;
                element.setPointerCapture(event.pointerId);
                rectRef.current = scrollbar.getBoundingClientRect();
                prevWebkitUserSelectRef.current = document.body.style.webkitUserSelect;
                document.body.style.webkitUserSelect = "none";
                if (context2.viewport) context2.viewport.style.scrollBehavior = "auto";
                handleDragScroll(event);
              }
            }),
            onPointerMove: composeEventHandlers(props.onPointerMove, handleDragScroll),
            onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
              const element = event.target;
              if (element.hasPointerCapture(event.pointerId)) {
                element.releasePointerCapture(event.pointerId);
              }
              document.body.style.webkitUserSelect = prevWebkitUserSelectRef.current;
              if (context2.viewport) context2.viewport.style.scrollBehavior = "";
              rectRef.current = null;
            })
          }
        )
      }
    );
  });
  var THUMB_NAME = "ScrollAreaThumb";
  var ScrollAreaThumb = React210.forwardRef(
    (props, forwardedRef) => {
      const { forceMount, ...thumbProps } = props;
      const scrollbarContext = useScrollbarContext(THUMB_NAME, props.__scopeScrollArea);
      return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(Presence, { present: forceMount || scrollbarContext.hasThumb, children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(ScrollAreaThumbImpl, { ref: forwardedRef, ...thumbProps }) });
    }
  );
  var ScrollAreaThumbImpl = React210.forwardRef(
    (props, forwardedRef) => {
      const { __scopeScrollArea, style, ...thumbProps } = props;
      const scrollAreaContext = useScrollAreaContext(THUMB_NAME, __scopeScrollArea);
      const scrollbarContext = useScrollbarContext(THUMB_NAME, __scopeScrollArea);
      const { onThumbPositionChange } = scrollbarContext;
      const composedRef = useComposedRefs(
        forwardedRef,
        (node) => scrollbarContext.onThumbChange(node)
      );
      const removeUnlinkedScrollListenerRef = React210.useRef(void 0);
      const debounceScrollEnd = useDebounceCallback(() => {
        if (removeUnlinkedScrollListenerRef.current) {
          removeUnlinkedScrollListenerRef.current();
          removeUnlinkedScrollListenerRef.current = void 0;
        }
      }, 100);
      React210.useEffect(() => {
        const viewport = scrollAreaContext.viewport;
        if (viewport) {
          const handleScroll2 = () => {
            debounceScrollEnd();
            if (!removeUnlinkedScrollListenerRef.current) {
              const listener = addUnlinkedScrollListener(viewport, onThumbPositionChange);
              removeUnlinkedScrollListenerRef.current = listener;
              onThumbPositionChange();
            }
          };
          onThumbPositionChange();
          viewport.addEventListener("scroll", handleScroll2);
          return () => viewport.removeEventListener("scroll", handleScroll2);
        }
      }, [scrollAreaContext.viewport, debounceScrollEnd, onThumbPositionChange]);
      return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
        Primitive2.div,
        {
          "data-state": scrollbarContext.hasThumb ? "visible" : "hidden",
          ...thumbProps,
          ref: composedRef,
          style: {
            width: "var(--radix-scroll-area-thumb-width)",
            height: "var(--radix-scroll-area-thumb-height)",
            ...style
          },
          onPointerDownCapture: composeEventHandlers(props.onPointerDownCapture, (event) => {
            const thumb = event.target;
            const thumbRect = thumb.getBoundingClientRect();
            const x = event.clientX - thumbRect.left;
            const y = event.clientY - thumbRect.top;
            scrollbarContext.onThumbPointerDown({ x, y });
          }),
          onPointerUp: composeEventHandlers(props.onPointerUp, scrollbarContext.onThumbPointerUp)
        }
      );
    }
  );
  ScrollAreaThumb.displayName = THUMB_NAME;
  var CORNER_NAME = "ScrollAreaCorner";
  var ScrollAreaCorner = React210.forwardRef(
    (props, forwardedRef) => {
      const context2 = useScrollAreaContext(CORNER_NAME, props.__scopeScrollArea);
      const hasBothScrollbarsVisible = Boolean(context2.scrollbarX && context2.scrollbarY);
      const hasCorner = context2.type !== "scroll" && hasBothScrollbarsVisible;
      return hasCorner ? /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(ScrollAreaCornerImpl, { ...props, ref: forwardedRef }) : null;
    }
  );
  ScrollAreaCorner.displayName = CORNER_NAME;
  var ScrollAreaCornerImpl = React210.forwardRef((props, forwardedRef) => {
    const { __scopeScrollArea, ...cornerProps } = props;
    const context2 = useScrollAreaContext(CORNER_NAME, __scopeScrollArea);
    const [width, setWidth] = React210.useState(0);
    const [height, setHeight] = React210.useState(0);
    const hasSize = Boolean(width && height);
    useResizeObserver(context2.scrollbarX, () => {
      const height2 = context2.scrollbarX?.offsetHeight || 0;
      context2.onCornerHeightChange(height2);
      setHeight(height2);
    });
    useResizeObserver(context2.scrollbarY, () => {
      const width2 = context2.scrollbarY?.offsetWidth || 0;
      context2.onCornerWidthChange(width2);
      setWidth(width2);
    });
    return hasSize ? /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
      Primitive2.div,
      {
        ...cornerProps,
        ref: forwardedRef,
        style: {
          width,
          height,
          position: "absolute",
          right: context2.dir === "ltr" ? 0 : void 0,
          left: context2.dir === "rtl" ? 0 : void 0,
          bottom: 0,
          ...props.style
        }
      }
    ) : null;
  });
  function toInt(value) {
    return value ? parseInt(value, 10) : 0;
  }
  function getThumbRatio(viewportSize, contentSize) {
    const ratio = viewportSize / contentSize;
    return isNaN(ratio) ? 0 : ratio;
  }
  function getThumbSize(sizes) {
    const ratio = getThumbRatio(sizes.viewport, sizes.content);
    const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
    const thumbSize = (sizes.scrollbar.size - scrollbarPadding) * ratio;
    return Math.max(thumbSize, 18);
  }
  function getScrollPositionFromPointer(pointerPos, pointerOffset, sizes, dir2 = "ltr") {
    const thumbSizePx = getThumbSize(sizes);
    const thumbCenter = thumbSizePx / 2;
    const offset4 = pointerOffset || thumbCenter;
    const thumbOffsetFromEnd = thumbSizePx - offset4;
    const minPointerPos = sizes.scrollbar.paddingStart + offset4;
    const maxPointerPos = sizes.scrollbar.size - sizes.scrollbar.paddingEnd - thumbOffsetFromEnd;
    const maxScrollPos = sizes.content - sizes.viewport;
    const scrollRange = dir2 === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
    const interpolate = linearScale([minPointerPos, maxPointerPos], scrollRange);
    return interpolate(pointerPos);
  }
  function getThumbOffsetFromScroll(scrollPos, sizes, dir2 = "ltr") {
    const thumbSizePx = getThumbSize(sizes);
    const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
    const scrollbar = sizes.scrollbar.size - scrollbarPadding;
    const maxScrollPos = sizes.content - sizes.viewport;
    const maxThumbPos = scrollbar - thumbSizePx;
    const scrollClampRange = dir2 === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
    const scrollWithoutMomentum = clamp2(scrollPos, scrollClampRange);
    const interpolate = linearScale([0, maxScrollPos], [0, maxThumbPos]);
    return interpolate(scrollWithoutMomentum);
  }
  function linearScale(input, output) {
    return (value) => {
      if (input[0] === input[1] || output[0] === output[1]) return output[0];
      const ratio = (output[1] - output[0]) / (input[1] - input[0]);
      return output[0] + ratio * (value - input[0]);
    };
  }
  function isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos) {
    return scrollPos > 0 && scrollPos < maxScrollPos;
  }
  var addUnlinkedScrollListener = (node, handler = () => {
  }) => {
    let prevPosition = { left: node.scrollLeft, top: node.scrollTop };
    let rAF = 0;
    (function loop() {
      const position = { left: node.scrollLeft, top: node.scrollTop };
      const isHorizontalScroll = prevPosition.left !== position.left;
      const isVerticalScroll = prevPosition.top !== position.top;
      if (isHorizontalScroll || isVerticalScroll) handler();
      prevPosition = position;
      rAF = window.requestAnimationFrame(loop);
    })();
    return () => window.cancelAnimationFrame(rAF);
  };
  function useDebounceCallback(callback, delay) {
    const handleCallback = useCallbackRef(callback);
    const debounceTimerRef = React210.useRef(0);
    React210.useEffect(() => () => window.clearTimeout(debounceTimerRef.current), []);
    return React210.useCallback(() => {
      window.clearTimeout(debounceTimerRef.current);
      debounceTimerRef.current = window.setTimeout(handleCallback, delay);
    }, [handleCallback, delay]);
  }
  function useResizeObserver(element, onResize) {
    const handleResize = useCallbackRef(onResize);
    useLayoutEffect22(() => {
      let rAF = 0;
      if (element) {
        const resizeObserver = new ResizeObserver(() => {
          cancelAnimationFrame(rAF);
          rAF = window.requestAnimationFrame(handleResize);
        });
        resizeObserver.observe(element);
        return () => {
          window.cancelAnimationFrame(rAF);
          resizeObserver.unobserve(element);
        };
      }
    }, [element, handleResize]);
  }
  var Root11 = ScrollArea;
  var Viewport2 = ScrollAreaViewport;
  var Corner = ScrollAreaCorner;

  // src/components/ui/scroll-area/ScrollArea.tsx
  var Bar = ({ className, orientation = "vertical", ...props }) => /* @__PURE__ */ React.createElement(
    ScrollAreaScrollbar,
    {
      "data-slot": "scroll-area-bar",
      orientation,
      className: cn(
        "flex touch-none transition-colors select-none",
        "data-[state=hidden]:animate-out data-[state=hidden]:fade-out data-[state=visible]:animate-in data-[state=visible]:fade-in duration-150",
        orientation === "vertical" && "h-full w-2 border-s border-s-transparent p-px",
        orientation === "horizontal" && "h-2 flex-col border-t border-t-transparent p-px",
        className
      ),
      ...props
    },
    /* @__PURE__ */ React.createElement(ScrollAreaThumb, { "data-slot": "scroll-area-thumb", className: "bg-primary relative flex-1 rounded-md" })
  );
  var ScrollArea2 = ({ orientation = "vertical", className, viewportClassName, children, ...props }) => /* @__PURE__ */ React.createElement(
    Root11,
    {
      "data-slot": "scroll-area",
      className: cn("relative overflow-hidden", orientation === "vertical" && "[&>[data-slot=scroll-area-viewport]>div]:block!", className),
      ...props
    },
    /* @__PURE__ */ React.createElement(Viewport2, { "data-slot": "scroll-area-viewport", className: cn("h-full w-full rounded-[inherit]", viewportClassName) }, children),
    /* @__PURE__ */ React.createElement(Corner, null)
  );
  ScrollArea2.Bar = Bar;
  ScrollArea2.displayName = "ScrollArea";
  Bar.displayName = "ScrollArea.Bar";
  var ScrollArea_default = ScrollArea2;

  // src/components/ui/formatted-number/index.ts
  init_define_import_meta_env();

  // src/components/ui/formatted-number/FormattedNumber.tsx
  init_define_import_meta_env();
  var import_react98 = __toESM(require_react_shim(), 1);

  // node_modules/react-number-format/dist/react-number-format.es.js
  init_define_import_meta_env();
  var import_react97 = __toESM(require_react_shim());
  function __rest2(s, e2) {
    var t3 = {};
    for (var p2 in s) {
      if (Object.prototype.hasOwnProperty.call(s, p2) && e2.indexOf(p2) < 0) {
        t3[p2] = s[p2];
      }
    }
    if (s != null && typeof Object.getOwnPropertySymbols === "function") {
      for (var i = 0, p2 = Object.getOwnPropertySymbols(s); i < p2.length; i++) {
        if (e2.indexOf(p2[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p2[i])) {
          t3[p2[i]] = s[p2[i]];
        }
      }
    }
    return t3;
  }
  var SourceType;
  (function(SourceType2) {
    SourceType2["event"] = "event";
    SourceType2["props"] = "prop";
  })(SourceType || (SourceType = {}));
  function noop5() {
  }
  function memoizeOnce(cb) {
    var lastArgs;
    var lastValue = void 0;
    return function() {
      var args = [], len = arguments.length;
      while (len--) args[len] = arguments[len];
      if (lastArgs && args.length === lastArgs.length && args.every(function(value, index2) {
        return value === lastArgs[index2];
      })) {
        return lastValue;
      }
      lastArgs = args;
      lastValue = cb.apply(void 0, args);
      return lastValue;
    };
  }
  function charIsNumber(char) {
    return !!(char || "").match(/\d/);
  }
  function isNil(val) {
    return val === null || val === void 0;
  }
  function isNanValue(val) {
    return typeof val === "number" && isNaN(val);
  }
  function isNotValidValue(val) {
    return isNil(val) || isNanValue(val) || typeof val === "number" && !isFinite(val);
  }
  function escapeRegExp(str) {
    return str.replace(/[-[\]/{}()*+?.\\^$|]/g, "\\$&");
  }
  function getThousandsGroupRegex(thousandsGroupStyle) {
    switch (thousandsGroupStyle) {
      case "lakh":
        return /(\d+?)(?=(\d\d)+(\d)(?!\d))(\.\d+)?/g;
      case "wan":
        return /(\d)(?=(\d{4})+(?!\d))/g;
      case "thousand":
      default:
        return /(\d)(?=(\d{3})+(?!\d))/g;
    }
  }
  function applyThousandSeparator(str, thousandSeparator, thousandsGroupStyle) {
    var thousandsGroupRegex = getThousandsGroupRegex(thousandsGroupStyle);
    var index2 = str.search(/[1-9]/);
    index2 = index2 === -1 ? str.length : index2;
    return str.substring(0, index2) + str.substring(index2, str.length).replace(thousandsGroupRegex, "$1" + thousandSeparator);
  }
  function usePersistentCallback(cb) {
    var callbackRef = (0, import_react97.useRef)(cb);
    callbackRef.current = cb;
    var persistentCbRef = (0, import_react97.useRef)(function() {
      var args = [], len = arguments.length;
      while (len--) args[len] = arguments[len];
      return callbackRef.current.apply(callbackRef, args);
    });
    return persistentCbRef.current;
  }
  function splitDecimal(numStr, allowNegative) {
    if (allowNegative === void 0) allowNegative = true;
    var hasNegation = numStr[0] === "-";
    var addNegation = hasNegation && allowNegative;
    numStr = numStr.replace("-", "");
    var parts = numStr.split(".");
    var beforeDecimal = parts[0];
    var afterDecimal = parts[1] || "";
    return {
      beforeDecimal,
      afterDecimal,
      hasNegation,
      addNegation
    };
  }
  function fixLeadingZero(numStr) {
    if (!numStr) {
      return numStr;
    }
    var isNegative = numStr[0] === "-";
    if (isNegative) {
      numStr = numStr.substring(1, numStr.length);
    }
    var parts = numStr.split(".");
    var beforeDecimal = parts[0].replace(/^0+/, "") || "0";
    var afterDecimal = parts[1] || "";
    return (isNegative ? "-" : "") + beforeDecimal + (afterDecimal ? "." + afterDecimal : "");
  }
  function limitToScale(numStr, scale, fixedDecimalScale) {
    var str = "";
    var filler = fixedDecimalScale ? "0" : "";
    for (var i = 0; i <= scale - 1; i++) {
      str += numStr[i] || filler;
    }
    return str;
  }
  function repeat(str, count4) {
    return Array(count4 + 1).join(str);
  }
  function toNumericString(num) {
    var _num = num + "";
    var sign = _num[0] === "-" ? "-" : "";
    if (sign) {
      _num = _num.substring(1);
    }
    var ref = _num.split(/[eE]/g);
    var coefficient = ref[0];
    var exponent = ref[1];
    exponent = Number(exponent);
    if (!exponent) {
      return sign + coefficient;
    }
    coefficient = coefficient.replace(".", "");
    var decimalIndex = 1 + exponent;
    var coffiecientLn = coefficient.length;
    if (decimalIndex < 0) {
      coefficient = "0." + repeat("0", Math.abs(decimalIndex)) + coefficient;
    } else if (decimalIndex >= coffiecientLn) {
      coefficient = coefficient + repeat("0", decimalIndex - coffiecientLn);
    } else {
      coefficient = (coefficient.substring(0, decimalIndex) || "0") + "." + coefficient.substring(decimalIndex);
    }
    return sign + coefficient;
  }
  function roundToPrecision(numStr, scale, fixedDecimalScale) {
    if (["", "-"].indexOf(numStr) !== -1) {
      return numStr;
    }
    var shouldHaveDecimalSeparator = (numStr.indexOf(".") !== -1 || fixedDecimalScale) && scale;
    var ref = splitDecimal(numStr);
    var beforeDecimal = ref.beforeDecimal;
    var afterDecimal = ref.afterDecimal;
    var hasNegation = ref.hasNegation;
    var floatValue = parseFloat("0." + (afterDecimal || "0"));
    var floatValueStr = afterDecimal.length <= scale ? "0." + afterDecimal : floatValue.toFixed(scale);
    var roundedDecimalParts = floatValueStr.split(".");
    var intPart = beforeDecimal;
    if (beforeDecimal && Number(roundedDecimalParts[0])) {
      intPart = beforeDecimal.split("").reverse().reduce(function(roundedStr, current, idx) {
        if (roundedStr.length > idx) {
          return (Number(roundedStr[0]) + Number(current)).toString() + roundedStr.substring(1, roundedStr.length);
        }
        return current + roundedStr;
      }, roundedDecimalParts[0]);
    }
    var decimalPart = limitToScale(roundedDecimalParts[1] || "", scale, fixedDecimalScale);
    var negation = hasNegation ? "-" : "";
    var decimalSeparator = shouldHaveDecimalSeparator ? "." : "";
    return "" + negation + intPart + decimalSeparator + decimalPart;
  }
  function setCaretPosition(el, caretPos) {
    el.value = el.value;
    if (el !== null) {
      if (el.createTextRange) {
        var range = el.createTextRange();
        range.move("character", caretPos);
        range.select();
        return true;
      }
      if (el.selectionStart || el.selectionStart === 0) {
        el.focus();
        el.setSelectionRange(caretPos, caretPos);
        return true;
      }
      el.focus();
      return false;
    }
  }
  var findChangeRange = memoizeOnce(function(prevValue, newValue) {
    var i = 0, j = 0;
    var prevLength = prevValue.length;
    var newLength = newValue.length;
    while (prevValue[i] === newValue[i] && i < prevLength) {
      i++;
    }
    while (prevValue[prevLength - 1 - j] === newValue[newLength - 1 - j] && newLength - j > i && prevLength - j > i) {
      j++;
    }
    return {
      from: { start: i, end: prevLength - j },
      to: { start: i, end: newLength - j }
    };
  });
  var findChangedRangeFromCaretPositions = function(lastCaretPositions, currentCaretPosition) {
    var startPosition = Math.min(lastCaretPositions.selectionStart, currentCaretPosition);
    return {
      from: { start: startPosition, end: lastCaretPositions.selectionEnd },
      to: { start: startPosition, end: currentCaretPosition }
    };
  };
  function clamp3(num, min3, max3) {
    return Math.min(Math.max(num, min3), max3);
  }
  function geInputCaretPosition(el) {
    return Math.max(el.selectionStart, el.selectionEnd);
  }
  function addInputMode() {
    return typeof navigator !== "undefined" && !(navigator.platform && /iPhone|iPod/.test(navigator.platform));
  }
  function getDefaultChangeMeta(value) {
    return {
      from: {
        start: 0,
        end: 0
      },
      to: {
        start: 0,
        end: value.length
      },
      lastValue: ""
    };
  }
  function defaultIsCharacterSame(ref) {
    var currentValue = ref.currentValue;
    var formattedValue = ref.formattedValue;
    var currentValueIndex = ref.currentValueIndex;
    var formattedValueIndex = ref.formattedValueIndex;
    return currentValue[currentValueIndex] === formattedValue[formattedValueIndex];
  }
  function getCaretPosition(newFormattedValue, lastFormattedValue, curValue, curCaretPos, boundary, isValidInputCharacter, isCharacterSame) {
    if (isCharacterSame === void 0) isCharacterSame = defaultIsCharacterSame;
    var firstAllowedPosition = boundary.findIndex(function(b) {
      return b;
    });
    var prefixFormat = newFormattedValue.slice(0, firstAllowedPosition);
    if (!lastFormattedValue && !curValue.startsWith(prefixFormat)) {
      lastFormattedValue = prefixFormat;
      curValue = prefixFormat + curValue;
      curCaretPos = curCaretPos + prefixFormat.length;
    }
    var curValLn = curValue.length;
    var formattedValueLn = newFormattedValue.length;
    var addedIndexMap = {};
    var indexMap = new Array(curValLn);
    for (var i = 0; i < curValLn; i++) {
      indexMap[i] = -1;
      for (var j = 0, jLn = formattedValueLn; j < jLn; j++) {
        var isCharSame = isCharacterSame({
          currentValue: curValue,
          lastValue: lastFormattedValue,
          formattedValue: newFormattedValue,
          currentValueIndex: i,
          formattedValueIndex: j
        });
        if (isCharSame && addedIndexMap[j] !== true) {
          indexMap[i] = j;
          addedIndexMap[j] = true;
          break;
        }
      }
    }
    var pos = curCaretPos;
    while (pos < curValLn && (indexMap[pos] === -1 || !isValidInputCharacter(curValue[pos]))) {
      pos++;
    }
    var endIndex = pos === curValLn || indexMap[pos] === -1 ? formattedValueLn : indexMap[pos];
    pos = curCaretPos - 1;
    while (pos > 0 && indexMap[pos] === -1) {
      pos--;
    }
    var startIndex = pos === -1 || indexMap[pos] === -1 ? 0 : indexMap[pos] + 1;
    if (startIndex > endIndex) {
      return endIndex;
    }
    return curCaretPos - startIndex < endIndex - curCaretPos ? startIndex : endIndex;
  }
  function getCaretPosInBoundary(value, caretPos, boundary, direction) {
    var valLn = value.length;
    caretPos = clamp3(caretPos, 0, valLn);
    if (direction === "left") {
      while (caretPos >= 0 && !boundary[caretPos]) {
        caretPos--;
      }
      if (caretPos === -1) {
        caretPos = boundary.indexOf(true);
      }
    } else {
      while (caretPos <= valLn && !boundary[caretPos]) {
        caretPos++;
      }
      if (caretPos > valLn) {
        caretPos = boundary.lastIndexOf(true);
      }
    }
    if (caretPos === -1) {
      caretPos = valLn;
    }
    return caretPos;
  }
  function caretUnknownFormatBoundary(formattedValue) {
    var boundaryAry = Array.from({ length: formattedValue.length + 1 }).map(function() {
      return true;
    });
    for (var i = 0, ln = boundaryAry.length; i < ln; i++) {
      boundaryAry[i] = Boolean(charIsNumber(formattedValue[i]) || charIsNumber(formattedValue[i - 1]));
    }
    return boundaryAry;
  }
  function useInternalValues(value, defaultValue, valueIsNumericString, format3, removeFormatting2, onValueChange) {
    if (onValueChange === void 0) onValueChange = noop5;
    var getValues = usePersistentCallback(function(value2, valueIsNumericString2) {
      var formattedValue, numAsString;
      if (isNotValidValue(value2)) {
        numAsString = "";
        formattedValue = "";
      } else if (typeof value2 === "number" || valueIsNumericString2) {
        numAsString = typeof value2 === "number" ? toNumericString(value2) : value2;
        formattedValue = format3(numAsString);
      } else {
        numAsString = removeFormatting2(value2, void 0);
        formattedValue = format3(numAsString);
      }
      return { formattedValue, numAsString };
    });
    var ref = (0, import_react97.useState)(function() {
      return getValues(isNil(value) ? defaultValue : value, valueIsNumericString);
    });
    var values = ref[0];
    var setValues = ref[1];
    var _onValueChange = function(newValues2, sourceInfo) {
      if (newValues2.formattedValue !== values.formattedValue) {
        setValues({
          formattedValue: newValues2.formattedValue,
          numAsString: newValues2.value
        });
      }
      onValueChange(newValues2, sourceInfo);
    };
    var _value = value;
    var _valueIsNumericString = valueIsNumericString;
    if (isNil(value)) {
      _value = values.numAsString;
      _valueIsNumericString = true;
    }
    var newValues = getValues(_value, _valueIsNumericString);
    (0, import_react97.useMemo)(function() {
      setValues(newValues);
    }, [newValues.formattedValue]);
    return [values, _onValueChange];
  }
  function defaultRemoveFormatting(value) {
    return value.replace(/[^0-9]/g, "");
  }
  function defaultFormat(value) {
    return value;
  }
  function NumberFormatBase(props) {
    var type = props.type;
    if (type === void 0) type = "text";
    var displayType = props.displayType;
    if (displayType === void 0) displayType = "input";
    var customInput = props.customInput;
    var renderText = props.renderText;
    var getInputRef = props.getInputRef;
    var format3 = props.format;
    if (format3 === void 0) format3 = defaultFormat;
    var removeFormatting2 = props.removeFormatting;
    if (removeFormatting2 === void 0) removeFormatting2 = defaultRemoveFormatting;
    var defaultValue = props.defaultValue;
    var valueIsNumericString = props.valueIsNumericString;
    var onValueChange = props.onValueChange;
    var isAllowed = props.isAllowed;
    var onChange = props.onChange;
    if (onChange === void 0) onChange = noop5;
    var onKeyDown = props.onKeyDown;
    if (onKeyDown === void 0) onKeyDown = noop5;
    var onMouseUp = props.onMouseUp;
    if (onMouseUp === void 0) onMouseUp = noop5;
    var onFocus = props.onFocus;
    if (onFocus === void 0) onFocus = noop5;
    var onBlur = props.onBlur;
    if (onBlur === void 0) onBlur = noop5;
    var propValue = props.value;
    var getCaretBoundary2 = props.getCaretBoundary;
    if (getCaretBoundary2 === void 0) getCaretBoundary2 = caretUnknownFormatBoundary;
    var isValidInputCharacter = props.isValidInputCharacter;
    if (isValidInputCharacter === void 0) isValidInputCharacter = charIsNumber;
    var isCharacterSame = props.isCharacterSame;
    var otherProps = __rest2(props, ["type", "displayType", "customInput", "renderText", "getInputRef", "format", "removeFormatting", "defaultValue", "valueIsNumericString", "onValueChange", "isAllowed", "onChange", "onKeyDown", "onMouseUp", "onFocus", "onBlur", "value", "getCaretBoundary", "isValidInputCharacter", "isCharacterSame"]);
    var ref = useInternalValues(propValue, defaultValue, Boolean(valueIsNumericString), format3, removeFormatting2, onValueChange);
    var ref_0 = ref[0];
    var formattedValue = ref_0.formattedValue;
    var numAsString = ref_0.numAsString;
    var onFormattedValueChange = ref[1];
    var caretPositionBeforeChange = (0, import_react97.useRef)();
    var lastUpdatedValue = (0, import_react97.useRef)({ formattedValue, numAsString });
    var _onValueChange = function(values, source) {
      lastUpdatedValue.current = { formattedValue: values.formattedValue, numAsString: values.value };
      onFormattedValueChange(values, source);
    };
    var ref$1 = (0, import_react97.useState)(false);
    var mounted = ref$1[0];
    var setMounted = ref$1[1];
    var focusedElm = (0, import_react97.useRef)(null);
    var timeout2 = (0, import_react97.useRef)({
      setCaretTimeout: null,
      focusTimeout: null
    });
    (0, import_react97.useEffect)(function() {
      setMounted(true);
      return function() {
        clearTimeout(timeout2.current.setCaretTimeout);
        clearTimeout(timeout2.current.focusTimeout);
      };
    }, []);
    var _format = format3;
    var getValueObject = function(formattedValue2, numAsString2) {
      var floatValue = parseFloat(numAsString2);
      return {
        formattedValue: formattedValue2,
        value: numAsString2,
        floatValue: isNaN(floatValue) ? void 0 : floatValue
      };
    };
    var setPatchedCaretPosition = function(el, caretPos, currentValue) {
      if (el.selectionStart === 0 && el.selectionEnd === el.value.length) {
        return;
      }
      setCaretPosition(el, caretPos);
      timeout2.current.setCaretTimeout = setTimeout(function() {
        if (el.value === currentValue && el.selectionStart !== caretPos) {
          setCaretPosition(el, caretPos);
        }
      }, 0);
    };
    var correctCaretPosition = function(value, caretPos, direction) {
      return getCaretPosInBoundary(value, caretPos, getCaretBoundary2(value), direction);
    };
    var getNewCaretPosition = function(inputValue, newFormattedValue, caretPos) {
      var caretBoundary = getCaretBoundary2(newFormattedValue);
      var updatedCaretPos = getCaretPosition(newFormattedValue, formattedValue, inputValue, caretPos, caretBoundary, isValidInputCharacter, isCharacterSame);
      updatedCaretPos = getCaretPosInBoundary(newFormattedValue, updatedCaretPos, caretBoundary);
      return updatedCaretPos;
    };
    var updateValueAndCaretPosition = function(params) {
      var newFormattedValue = params.formattedValue;
      if (newFormattedValue === void 0) newFormattedValue = "";
      var input = params.input;
      var source = params.source;
      var event = params.event;
      var numAsString2 = params.numAsString;
      var caretPos;
      if (input) {
        var inputValue = params.inputValue || input.value;
        var currentCaretPosition2 = geInputCaretPosition(input);
        input.value = newFormattedValue;
        caretPos = getNewCaretPosition(inputValue, newFormattedValue, currentCaretPosition2);
        if (caretPos !== void 0) {
          setPatchedCaretPosition(input, caretPos, newFormattedValue);
        }
      }
      if (newFormattedValue !== formattedValue) {
        _onValueChange(getValueObject(newFormattedValue, numAsString2), { event, source });
      }
    };
    (0, import_react97.useEffect)(function() {
      var ref2 = lastUpdatedValue.current;
      var lastFormattedValue = ref2.formattedValue;
      var lastNumAsString = ref2.numAsString;
      if (formattedValue !== lastFormattedValue || numAsString !== lastNumAsString) {
        _onValueChange(getValueObject(formattedValue, numAsString), {
          event: void 0,
          source: SourceType.props
        });
      }
    }, [formattedValue, numAsString]);
    var currentCaretPosition = focusedElm.current ? geInputCaretPosition(focusedElm.current) : void 0;
    var useIsomorphicLayoutEffect4 = typeof window !== "undefined" ? import_react97.useLayoutEffect : import_react97.useEffect;
    useIsomorphicLayoutEffect4(function() {
      var input = focusedElm.current;
      if (formattedValue !== lastUpdatedValue.current.formattedValue && input) {
        var caretPos = getNewCaretPosition(lastUpdatedValue.current.formattedValue, formattedValue, currentCaretPosition);
        input.value = formattedValue;
        setPatchedCaretPosition(input, caretPos, formattedValue);
      }
    }, [formattedValue]);
    var formatInputValue = function(inputValue, event, source) {
      var input = event.target;
      var changeRange = caretPositionBeforeChange.current ? findChangedRangeFromCaretPositions(caretPositionBeforeChange.current, input.selectionEnd) : findChangeRange(formattedValue, inputValue);
      var changeMeta = Object.assign(Object.assign({}, changeRange), { lastValue: formattedValue });
      var _numAsString = removeFormatting2(inputValue, changeMeta);
      var _formattedValue = _format(_numAsString);
      _numAsString = removeFormatting2(_formattedValue, void 0);
      if (isAllowed && !isAllowed(getValueObject(_formattedValue, _numAsString))) {
        var input$1 = event.target;
        var currentCaretPosition2 = geInputCaretPosition(input$1);
        var caretPos = getNewCaretPosition(inputValue, formattedValue, currentCaretPosition2);
        input$1.value = formattedValue;
        setPatchedCaretPosition(input$1, caretPos, formattedValue);
        return false;
      }
      updateValueAndCaretPosition({
        formattedValue: _formattedValue,
        numAsString: _numAsString,
        inputValue,
        event,
        source,
        input: event.target
      });
      return true;
    };
    var setCaretPositionInfoBeforeChange = function(el, endOffset) {
      if (endOffset === void 0) endOffset = 0;
      var selectionStart = el.selectionStart;
      var selectionEnd = el.selectionEnd;
      caretPositionBeforeChange.current = { selectionStart, selectionEnd: selectionEnd + endOffset };
    };
    var _onChange = function(e2) {
      var el = e2.target;
      var inputValue = el.value;
      var changed = formatInputValue(inputValue, e2, SourceType.event);
      if (changed) {
        onChange(e2);
      }
      caretPositionBeforeChange.current = void 0;
    };
    var _onKeyDown = function(e2) {
      var el = e2.target;
      var key = e2.key;
      var selectionStart = el.selectionStart;
      var selectionEnd = el.selectionEnd;
      var value = el.value;
      if (value === void 0) value = "";
      var expectedCaretPosition;
      if (key === "ArrowLeft" || key === "Backspace") {
        expectedCaretPosition = Math.max(selectionStart - 1, 0);
      } else if (key === "ArrowRight") {
        expectedCaretPosition = Math.min(selectionStart + 1, value.length);
      } else if (key === "Delete") {
        expectedCaretPosition = selectionStart;
      }
      var endOffset = 0;
      if (key === "Delete" && selectionStart === selectionEnd) {
        endOffset = 1;
      }
      var isArrowKey = key === "ArrowLeft" || key === "ArrowRight";
      if (expectedCaretPosition === void 0 || selectionStart !== selectionEnd && !isArrowKey) {
        onKeyDown(e2);
        setCaretPositionInfoBeforeChange(el, endOffset);
        return;
      }
      var newCaretPosition = expectedCaretPosition;
      if (isArrowKey) {
        var direction = key === "ArrowLeft" ? "left" : "right";
        newCaretPosition = correctCaretPosition(value, expectedCaretPosition, direction);
        if (newCaretPosition !== expectedCaretPosition) {
          e2.preventDefault();
        }
      } else if (key === "Delete" && !isValidInputCharacter(value[expectedCaretPosition])) {
        newCaretPosition = correctCaretPosition(value, expectedCaretPosition, "right");
      } else if (key === "Backspace" && !isValidInputCharacter(value[expectedCaretPosition])) {
        newCaretPosition = correctCaretPosition(value, expectedCaretPosition, "left");
      }
      if (newCaretPosition !== expectedCaretPosition) {
        setPatchedCaretPosition(el, newCaretPosition, value);
      }
      onKeyDown(e2);
      setCaretPositionInfoBeforeChange(el, endOffset);
    };
    var _onMouseUp = function(e2) {
      var el = e2.target;
      var correctCaretPositionIfRequired = function() {
        var selectionStart = el.selectionStart;
        var selectionEnd = el.selectionEnd;
        var value = el.value;
        if (value === void 0) value = "";
        if (selectionStart === selectionEnd) {
          var caretPosition = correctCaretPosition(value, selectionStart);
          if (caretPosition !== selectionStart) {
            setPatchedCaretPosition(el, caretPosition, value);
          }
        }
      };
      correctCaretPositionIfRequired();
      requestAnimationFrame(function() {
        correctCaretPositionIfRequired();
      });
      onMouseUp(e2);
      setCaretPositionInfoBeforeChange(el);
    };
    var _onFocus = function(e2) {
      if (e2.persist) {
        e2.persist();
      }
      var el = e2.target;
      var currentTarget = e2.currentTarget;
      focusedElm.current = el;
      timeout2.current.focusTimeout = setTimeout(function() {
        var selectionStart = el.selectionStart;
        var selectionEnd = el.selectionEnd;
        var value = el.value;
        if (value === void 0) value = "";
        var caretPosition = correctCaretPosition(value, selectionStart);
        if (caretPosition !== selectionStart && !(selectionStart === 0 && selectionEnd === value.length)) {
          setPatchedCaretPosition(el, caretPosition, value);
        }
        onFocus(Object.assign(Object.assign({}, e2), { currentTarget }));
      }, 0);
    };
    var _onBlur = function(e2) {
      focusedElm.current = null;
      clearTimeout(timeout2.current.focusTimeout);
      clearTimeout(timeout2.current.setCaretTimeout);
      onBlur(e2);
    };
    var inputMode = mounted && addInputMode() ? "numeric" : void 0;
    var inputProps = Object.assign({ inputMode }, otherProps, {
      type,
      value: formattedValue,
      onChange: _onChange,
      onKeyDown: _onKeyDown,
      onMouseUp: _onMouseUp,
      onFocus: _onFocus,
      onBlur: _onBlur
    });
    if (displayType === "text") {
      return renderText ? import_react97.default.createElement(import_react97.default.Fragment, null, renderText(formattedValue, otherProps) || null) : import_react97.default.createElement("span", Object.assign({}, otherProps, { ref: getInputRef }), formattedValue);
    } else if (customInput) {
      var CustomInput = customInput;
      return import_react97.default.createElement(CustomInput, Object.assign({}, inputProps, { ref: getInputRef }));
    }
    return import_react97.default.createElement("input", Object.assign({}, inputProps, { ref: getInputRef }));
  }
  function format2(numStr, props) {
    var decimalScale = props.decimalScale;
    var fixedDecimalScale = props.fixedDecimalScale;
    var prefix = props.prefix;
    if (prefix === void 0) prefix = "";
    var suffix = props.suffix;
    if (suffix === void 0) suffix = "";
    var allowNegative = props.allowNegative;
    var thousandsGroupStyle = props.thousandsGroupStyle;
    if (thousandsGroupStyle === void 0) thousandsGroupStyle = "thousand";
    if (numStr === "" || numStr === "-") {
      return numStr;
    }
    var ref = getSeparators(props);
    var thousandSeparator = ref.thousandSeparator;
    var decimalSeparator = ref.decimalSeparator;
    var hasDecimalSeparator = decimalScale !== 0 && numStr.indexOf(".") !== -1 || decimalScale && fixedDecimalScale;
    var ref$1 = splitDecimal(numStr, allowNegative);
    var beforeDecimal = ref$1.beforeDecimal;
    var afterDecimal = ref$1.afterDecimal;
    var addNegation = ref$1.addNegation;
    if (decimalScale !== void 0) {
      afterDecimal = limitToScale(afterDecimal, decimalScale, !!fixedDecimalScale);
    }
    if (thousandSeparator) {
      beforeDecimal = applyThousandSeparator(beforeDecimal, thousandSeparator, thousandsGroupStyle);
    }
    if (prefix) {
      beforeDecimal = prefix + beforeDecimal;
    }
    if (suffix) {
      afterDecimal = afterDecimal + suffix;
    }
    if (addNegation) {
      beforeDecimal = "-" + beforeDecimal;
    }
    numStr = beforeDecimal + (hasDecimalSeparator && decimalSeparator || "") + afterDecimal;
    return numStr;
  }
  function getSeparators(props) {
    var decimalSeparator = props.decimalSeparator;
    if (decimalSeparator === void 0) decimalSeparator = ".";
    var thousandSeparator = props.thousandSeparator;
    var allowedDecimalSeparators = props.allowedDecimalSeparators;
    if (thousandSeparator === true) {
      thousandSeparator = ",";
    }
    if (!allowedDecimalSeparators) {
      allowedDecimalSeparators = [decimalSeparator, "."];
    }
    return {
      decimalSeparator,
      thousandSeparator,
      allowedDecimalSeparators
    };
  }
  function handleNegation(value, allowNegative) {
    if (value === void 0) value = "";
    var negationRegex = new RegExp("(-)");
    var doubleNegationRegex = new RegExp("(-)(.)*(-)");
    var hasNegation = negationRegex.test(value);
    var removeNegation = doubleNegationRegex.test(value);
    value = value.replace(/-/g, "");
    if (hasNegation && !removeNegation && allowNegative) {
      value = "-" + value;
    }
    return value;
  }
  function getNumberRegex(decimalSeparator, global) {
    return new RegExp("(^-)|[0-9]|" + escapeRegExp(decimalSeparator), global ? "g" : void 0);
  }
  function isNumericString(val, prefix, suffix) {
    if (val === "") {
      return true;
    }
    return !(prefix === null || prefix === void 0 ? void 0 : prefix.match(/\d/)) && !(suffix === null || suffix === void 0 ? void 0 : suffix.match(/\d/)) && typeof val === "string" && !isNaN(Number(val));
  }
  function removeFormatting(value, changeMeta, props) {
    var assign;
    if (changeMeta === void 0) changeMeta = getDefaultChangeMeta(value);
    var allowNegative = props.allowNegative;
    var prefix = props.prefix;
    if (prefix === void 0) prefix = "";
    var suffix = props.suffix;
    if (suffix === void 0) suffix = "";
    var decimalScale = props.decimalScale;
    var from = changeMeta.from;
    var to = changeMeta.to;
    var start = to.start;
    var end = to.end;
    var ref = getSeparators(props);
    var allowedDecimalSeparators = ref.allowedDecimalSeparators;
    var decimalSeparator = ref.decimalSeparator;
    var isBeforeDecimalSeparator = value[end] === decimalSeparator;
    if (charIsNumber(value) && (value === prefix || value === suffix) && changeMeta.lastValue === "") {
      return value;
    }
    if (end - start === 1 && allowedDecimalSeparators.indexOf(value[start]) !== -1) {
      var separator = decimalScale === 0 ? "" : decimalSeparator;
      value = value.substring(0, start) + separator + value.substring(start + 1, value.length);
    }
    var stripNegation = function(value2, start2, end2) {
      var hasNegation2 = false;
      var hasDoubleNegation = false;
      if (prefix.startsWith("-")) {
        hasNegation2 = false;
      } else if (value2.startsWith("--")) {
        hasNegation2 = false;
        hasDoubleNegation = true;
      } else if (suffix.startsWith("-") && value2.length === suffix.length) {
        hasNegation2 = false;
      } else if (value2[0] === "-") {
        hasNegation2 = true;
      }
      var charsToRemove = hasNegation2 ? 1 : 0;
      if (hasDoubleNegation) {
        charsToRemove = 2;
      }
      if (charsToRemove) {
        value2 = value2.substring(charsToRemove);
        start2 -= charsToRemove;
        end2 -= charsToRemove;
      }
      return { value: value2, start: start2, end: end2, hasNegation: hasNegation2 };
    };
    var toMetadata = stripNegation(value, start, end);
    var hasNegation = toMetadata.hasNegation;
    assign = toMetadata, value = assign.value, start = assign.start, end = assign.end;
    var ref$1 = stripNegation(changeMeta.lastValue, from.start, from.end);
    var fromStart = ref$1.start;
    var fromEnd = ref$1.end;
    var lastValue = ref$1.value;
    var updatedSuffixPart = value.substring(start, end);
    if (value.length && lastValue.length && (fromStart > lastValue.length - suffix.length || fromEnd < prefix.length) && !(updatedSuffixPart && suffix.startsWith(updatedSuffixPart))) {
      value = lastValue;
    }
    var startIndex = 0;
    if (value.startsWith(prefix)) {
      startIndex += prefix.length;
    } else if (start < prefix.length) {
      startIndex = start;
    }
    value = value.substring(startIndex);
    end -= startIndex;
    var endIndex = value.length;
    var suffixStartIndex = value.length - suffix.length;
    if (value.endsWith(suffix)) {
      endIndex = suffixStartIndex;
    } else if (end > suffixStartIndex) {
      endIndex = end;
    } else if (end > value.length - suffix.length) {
      endIndex = end;
    }
    value = value.substring(0, endIndex);
    value = handleNegation(hasNegation ? "-" + value : value, allowNegative);
    value = (value.match(getNumberRegex(decimalSeparator, true)) || []).join("");
    var firstIndex = value.indexOf(decimalSeparator);
    value = value.replace(new RegExp(escapeRegExp(decimalSeparator), "g"), function(match3, index2) {
      return index2 === firstIndex ? "." : "";
    });
    var ref$2 = splitDecimal(value, allowNegative);
    var beforeDecimal = ref$2.beforeDecimal;
    var afterDecimal = ref$2.afterDecimal;
    var addNegation = ref$2.addNegation;
    if (to.end - to.start < from.end - from.start && beforeDecimal === "" && isBeforeDecimalSeparator && !parseFloat(afterDecimal)) {
      value = addNegation ? "-" : "";
    }
    return value;
  }
  function getCaretBoundary(formattedValue, props) {
    var prefix = props.prefix;
    if (prefix === void 0) prefix = "";
    var suffix = props.suffix;
    if (suffix === void 0) suffix = "";
    var boundaryAry = Array.from({ length: formattedValue.length + 1 }).map(function() {
      return true;
    });
    var hasNegation = formattedValue[0] === "-";
    boundaryAry.fill(false, 0, prefix.length + (hasNegation ? 1 : 0));
    var valLn = formattedValue.length;
    boundaryAry.fill(false, valLn - suffix.length + 1, valLn + 1);
    return boundaryAry;
  }
  function validateAndUpdateProps(props) {
    var ref = getSeparators(props);
    var thousandSeparator = ref.thousandSeparator;
    var decimalSeparator = ref.decimalSeparator;
    var prefix = props.prefix;
    if (prefix === void 0) prefix = "";
    var allowNegative = props.allowNegative;
    if (allowNegative === void 0) allowNegative = true;
    if (thousandSeparator === decimalSeparator) {
      throw new Error("\n        Decimal separator can't be same as thousand separator.\n        thousandSeparator: " + thousandSeparator + ' (thousandSeparator = {true} is same as thousandSeparator = ",")\n        decimalSeparator: ' + decimalSeparator + " (default value for decimalSeparator is .)\n     ");
    }
    if (prefix.startsWith("-") && allowNegative) {
      console.error("\n      Prefix can't start with '-' when allowNegative is true.\n      prefix: " + prefix + "\n      allowNegative: " + allowNegative + "\n    ");
      allowNegative = false;
    }
    return Object.assign(Object.assign({}, props), { allowNegative });
  }
  function useNumericFormat(props) {
    props = validateAndUpdateProps(props);
    var _decimalSeparator = props.decimalSeparator;
    var _allowedDecimalSeparators = props.allowedDecimalSeparators;
    var thousandsGroupStyle = props.thousandsGroupStyle;
    var suffix = props.suffix;
    var allowNegative = props.allowNegative;
    var allowLeadingZeros = props.allowLeadingZeros;
    var onKeyDown = props.onKeyDown;
    if (onKeyDown === void 0) onKeyDown = noop5;
    var onBlur = props.onBlur;
    if (onBlur === void 0) onBlur = noop5;
    var thousandSeparator = props.thousandSeparator;
    var decimalScale = props.decimalScale;
    var fixedDecimalScale = props.fixedDecimalScale;
    var prefix = props.prefix;
    if (prefix === void 0) prefix = "";
    var defaultValue = props.defaultValue;
    var value = props.value;
    var valueIsNumericString = props.valueIsNumericString;
    var onValueChange = props.onValueChange;
    var restProps = __rest2(props, ["decimalSeparator", "allowedDecimalSeparators", "thousandsGroupStyle", "suffix", "allowNegative", "allowLeadingZeros", "onKeyDown", "onBlur", "thousandSeparator", "decimalScale", "fixedDecimalScale", "prefix", "defaultValue", "value", "valueIsNumericString", "onValueChange"]);
    var ref = getSeparators(props);
    var decimalSeparator = ref.decimalSeparator;
    var allowedDecimalSeparators = ref.allowedDecimalSeparators;
    var _format = function(numStr) {
      return format2(numStr, props);
    };
    var _removeFormatting = function(inputValue, changeMeta) {
      return removeFormatting(inputValue, changeMeta, props);
    };
    var _value = isNil(value) ? defaultValue : value;
    var _valueIsNumericString = valueIsNumericString !== null && valueIsNumericString !== void 0 ? valueIsNumericString : isNumericString(_value, prefix, suffix);
    if (!isNil(value)) {
      _valueIsNumericString = _valueIsNumericString || typeof value === "number";
    } else if (!isNil(defaultValue)) {
      _valueIsNumericString = _valueIsNumericString || typeof defaultValue === "number";
    }
    var roundIncomingValueToPrecision = function(value2) {
      if (isNotValidValue(value2)) {
        return value2;
      }
      if (typeof value2 === "number") {
        value2 = toNumericString(value2);
      }
      if (_valueIsNumericString && typeof decimalScale === "number") {
        return roundToPrecision(value2, decimalScale, Boolean(fixedDecimalScale));
      }
      return value2;
    };
    var ref$1 = useInternalValues(roundIncomingValueToPrecision(value), roundIncomingValueToPrecision(defaultValue), Boolean(_valueIsNumericString), _format, _removeFormatting, onValueChange);
    var ref$1_0 = ref$1[0];
    var numAsString = ref$1_0.numAsString;
    var formattedValue = ref$1_0.formattedValue;
    var _onValueChange = ref$1[1];
    var _onKeyDown = function(e2) {
      var el = e2.target;
      var key = e2.key;
      var selectionStart = el.selectionStart;
      var selectionEnd = el.selectionEnd;
      var value2 = el.value;
      if (value2 === void 0) value2 = "";
      if ((key === "Backspace" || key === "Delete") && selectionEnd < prefix.length) {
        e2.preventDefault();
        return;
      }
      if (selectionStart !== selectionEnd) {
        onKeyDown(e2);
        return;
      }
      if (key === "Backspace" && value2[0] === "-" && selectionStart === prefix.length + 1 && allowNegative) {
        setCaretPosition(el, 1);
      }
      if (decimalScale && fixedDecimalScale) {
        if (key === "Backspace" && value2[selectionStart - 1] === decimalSeparator) {
          setCaretPosition(el, selectionStart - 1);
          e2.preventDefault();
        } else if (key === "Delete" && value2[selectionStart] === decimalSeparator) {
          e2.preventDefault();
        }
      }
      if ((allowedDecimalSeparators === null || allowedDecimalSeparators === void 0 ? void 0 : allowedDecimalSeparators.includes(key)) && value2[selectionStart] === decimalSeparator) {
        setCaretPosition(el, selectionStart + 1);
      }
      var _thousandSeparator = thousandSeparator === true ? "," : thousandSeparator;
      if (key === "Backspace" && value2[selectionStart - 1] === _thousandSeparator) {
        setCaretPosition(el, selectionStart - 1);
      }
      if (key === "Delete" && value2[selectionStart] === _thousandSeparator) {
        setCaretPosition(el, selectionStart + 1);
      }
      onKeyDown(e2);
    };
    var _onBlur = function(e2) {
      var _value2 = numAsString;
      if (!_value2.match(/\d/g)) {
        _value2 = "";
      }
      if (!allowLeadingZeros) {
        _value2 = fixLeadingZero(_value2);
      }
      if (fixedDecimalScale && decimalScale) {
        _value2 = roundToPrecision(_value2, decimalScale, fixedDecimalScale);
      }
      if (_value2 !== numAsString) {
        var formattedValue2 = format2(_value2, props);
        _onValueChange({
          formattedValue: formattedValue2,
          value: _value2,
          floatValue: parseFloat(_value2)
        }, {
          event: e2,
          source: SourceType.event
        });
      }
      onBlur(e2);
    };
    var isValidInputCharacter = function(inputChar) {
      if (inputChar === decimalSeparator) {
        return true;
      }
      return charIsNumber(inputChar);
    };
    var isCharacterSame = function(ref2) {
      var currentValue = ref2.currentValue;
      var lastValue = ref2.lastValue;
      var formattedValue2 = ref2.formattedValue;
      var currentValueIndex = ref2.currentValueIndex;
      var formattedValueIndex = ref2.formattedValueIndex;
      var curChar = currentValue[currentValueIndex];
      var newChar = formattedValue2[formattedValueIndex];
      var typedRange = findChangeRange(lastValue, currentValue);
      var to = typedRange.to;
      var getDecimalSeparatorIndex = function(value2) {
        return _removeFormatting(value2).indexOf(".") + prefix.length;
      };
      if (value === 0 && fixedDecimalScale && decimalScale && currentValue[to.start] === decimalSeparator && getDecimalSeparatorIndex(currentValue) < currentValueIndex && getDecimalSeparatorIndex(formattedValue2) > formattedValueIndex) {
        return false;
      }
      if (currentValueIndex >= to.start && currentValueIndex < to.end && allowedDecimalSeparators && allowedDecimalSeparators.includes(curChar) && newChar === decimalSeparator) {
        return true;
      }
      return curChar === newChar;
    };
    return Object.assign(Object.assign({}, restProps), {
      value: formattedValue,
      valueIsNumericString: false,
      isValidInputCharacter,
      isCharacterSame,
      onValueChange: _onValueChange,
      format: _format,
      removeFormatting: _removeFormatting,
      getCaretBoundary: function(formattedValue2) {
        return getCaretBoundary(formattedValue2, props);
      },
      onKeyDown: _onKeyDown,
      onBlur: _onBlur
    });
  }
  function NumericFormat(props) {
    var numericFormatProps = useNumericFormat(props);
    return import_react97.default.createElement(NumberFormatBase, Object.assign({}, numericFormatProps));
  }

  // src/components/ui/formatted-number/FormattedNumber.tsx
  var clampValue = (value, min3, max3) => {
    if (min3 !== void 0 && value < min3) return min3;
    if (max3 !== void 0 && value > max3) return max3;
    return value;
  };
  var FormattedNumber = ({
    ref,
    className,
    onChange,
    min: min3 = 0,
    max: max3,
    stepper = 1,
    prefix,
    suffix,
    thousandSeparator = "",
    decimalScale,
    isDecimal = true,
    showStepButtons = false,
    value,
    ...props
  }) => {
    const isIncrementDisabled = max3 !== void 0 && (value ?? 0) >= max3;
    const isDecrementDisabled = min3 !== void 0 && (value ?? 0) <= min3;
    const onValueChange = ({ floatValue }) => onChange?.(floatValue ?? null);
    const onIncrement = () => onChange?.(clampValue((value ?? 0) + stepper, min3, max3));
    const onDecrement = () => onChange?.(clampValue((value ?? 0) - stepper, min3, max3));
    const onKeyboardChange = (e2) => {
      if (e2.key === "ArrowUp" && !isIncrementDisabled) onIncrement();
      if (e2.key === "ArrowDown" && !isDecrementDisabled) onDecrement();
    };
    return /* @__PURE__ */ React.createElement("div", { className: "flex flex-col" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-0.5" }, /* @__PURE__ */ React.createElement("div", { className: "relative w-full" }, /* @__PURE__ */ React.createElement(
      NumericFormat,
      {
        "data-slot": "formatted-number-input",
        className: cn(
          "border-muted-200 bg-background flex w-full rounded-md border p-3 text-sm shadow-xs transition-[color,box-shadow] outline-none",
          "placeholder:text-muted-foreground",
          "hover:not-disabled:border-primary hover:not-disabled:ring-primary hover:not-disabled:ring",
          "focus-visible:ring-primary focus-visible:border-primary focus-visible:ring",
          "disabled:bg-muted-50 disabled:text-muted-300 disabled:placeholder:text-muted-300 disabled:pointer-events-none",
          className
        ),
        getInputRef: ref,
        value,
        thousandSeparator,
        decimalScale: isDecimal ? decimalScale : 0,
        prefix: prefix ? `${prefix} ` : void 0,
        suffix: suffix ? `   ${suffix}` : void 0,
        allowNegative: min3 < 0,
        onValueChange,
        onKeyDown: onKeyboardChange,
        isAllowed: ({ floatValue }) => {
          if (floatValue === void 0) return true;
          return (min3 === void 0 || floatValue >= min3) && (max3 === void 0 || floatValue <= max3);
        },
        ...props
      }
    ), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !showStepButtons }, /* @__PURE__ */ React.createElement("div", { className: "absolute inset-e-2 inset-bs-1/2 z-10 flex h-fit -translate-y-1/2 flex-col justify-center" }, /* @__PURE__ */ React.createElement(
      Button_default,
      {
        type: "button",
        variant: "ghost-muted",
        className: "bg-background h-fit rounded-none rounded-t-md p-0",
        onClick: onIncrement,
        disabled: props.disabled || isIncrementDisabled
      },
      /* @__PURE__ */ React.createElement(ChevronUp, { size: 16 })
    ), /* @__PURE__ */ React.createElement(
      Button_default,
      {
        type: "button",
        variant: "ghost-muted",
        className: "bg-background h-fit rounded-none rounded-b-md p-0",
        onClick: onDecrement,
        disabled: props.disabled || isDecrementDisabled
      },
      /* @__PURE__ */ React.createElement(ChevronDown, { size: 16 })
    ))))));
  };
  FormattedNumber.displayName = "FormattedNumber";
  var FormattedNumber_default = (0, import_react98.forwardRef)((props, ref) => FormattedNumber({ ...props, ref }));

  // src/components/ui/input/index.ts
  init_define_import_meta_env();

  // src/components/ui/input/Input.tsx
  init_define_import_meta_env();
  var import_react99 = __toESM(require_react_shim(), 1);
  var Input = ({ ref, className, ...props }) => /* @__PURE__ */ React.createElement(
    "input",
    {
      "data-slot": "input",
      ref,
      className: cn(
        "border-muted-200 bg-background flex w-full rounded-md border p-3 text-sm shadow-xs transition-[color,box-shadow] outline-none",
        "placeholder:text-muted-foreground file:border-0 file:bg-transparent file:text-sm file:font-medium",
        "hover:not-disabled:border-primary hover:not-disabled:ring-primary hover:not-disabled:ring",
        "focus-visible:ring-primary focus-visible:border-primary focus-visible:ring",
        "disabled:bg-muted-50 disabled:text-muted-300 disabled:placeholder:text-muted-300 disabled:pointer-events-none",
        className
      ),
      ...props
    }
  );
  var Input_default = (0, import_react99.forwardRef)((props, ref) => Input({ ...props, ref }));

  // src/components/ui/select/index.ts
  init_define_import_meta_env();

  // src/components/ui/select/Select.tsx
  init_define_import_meta_env();
  var import_react100 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-select/dist/index.mjs
  init_define_import_meta_env();
  var React98 = __toESM(require_react_shim(), 1);
  var ReactDOM10 = __toESM(require_react_dom_shim(), 1);

  // node_modules/@radix-ui/react-select/node_modules/@radix-ui/react-slot/dist/index.mjs
  init_define_import_meta_env();
  var React97 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime40 = __toESM(require_react_shim(), 1);
  // @__NO_SIDE_EFFECTS__
  function createSlot7(ownerName) {
    const SlotClone = /* @__PURE__ */ createSlotClone7(ownerName);
    const Slot22 = React97.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      const childrenArray = React97.Children.toArray(children);
      const slottable = childrenArray.find(isSlottable7);
      if (slottable) {
        const newElement = slottable.props.children;
        const newChildren = childrenArray.map((child) => {
          if (child === slottable) {
            if (React97.Children.count(newElement) > 1) return React97.Children.only(null);
            return React97.isValidElement(newElement) ? newElement.props.children : null;
          } else {
            return child;
          }
        });
        return /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children: React97.isValidElement(newElement) ? React97.cloneElement(newElement, void 0, newChildren) : null });
      }
      return /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children });
    });
    Slot22.displayName = `${ownerName}.Slot`;
    return Slot22;
  }
  // @__NO_SIDE_EFFECTS__
  function createSlotClone7(ownerName) {
    const SlotClone = React97.forwardRef((props, forwardedRef) => {
      const { children, ...slotProps } = props;
      if (React97.isValidElement(children)) {
        const childrenRef = getElementRef8(children);
        const props2 = mergeProps7(slotProps, children.props);
        if (children.type !== React97.Fragment) {
          props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
        }
        return React97.cloneElement(children, props2);
      }
      return React97.Children.count(children) > 1 ? React97.Children.only(null) : null;
    });
    SlotClone.displayName = `${ownerName}.SlotClone`;
    return SlotClone;
  }
  var SLOTTABLE_IDENTIFIER8 = /* @__PURE__ */ Symbol("radix.slottable");
  function isSlottable7(child) {
    return React97.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER8;
  }
  function mergeProps7(slotProps, childProps) {
    const overrideProps = { ...childProps };
    for (const propName in childProps) {
      const slotPropValue = slotProps[propName];
      const childPropValue = childProps[propName];
      const isHandler = /^on[A-Z]/.test(propName);
      if (isHandler) {
        if (slotPropValue && childPropValue) {
          overrideProps[propName] = (...args) => {
            const result = childPropValue(...args);
            slotPropValue(...args);
            return result;
          };
        } else if (slotPropValue) {
          overrideProps[propName] = slotPropValue;
        }
      } else if (propName === "style") {
        overrideProps[propName] = { ...slotPropValue, ...childPropValue };
      } else if (propName === "className") {
        overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
      }
    }
    return { ...slotProps, ...overrideProps };
  }
  function getElementRef8(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }

  // node_modules/@radix-ui/react-select/dist/index.mjs
  var import_jsx_runtime41 = __toESM(require_react_shim(), 1);
  var OPEN_KEYS = [" ", "Enter", "ArrowUp", "ArrowDown"];
  var SELECTION_KEYS2 = [" ", "Enter"];
  var SELECT_NAME = "Select";
  var [Collection5, useCollection5, createCollectionScope5] = createCollection(SELECT_NAME);
  var [createSelectContext, createSelectScope] = createContextScope(SELECT_NAME, [
    createCollectionScope5,
    createPopperScope
  ]);
  var usePopperScope4 = createPopperScope();
  var [SelectProvider, useSelectContext] = createSelectContext(SELECT_NAME);
  var [SelectNativeOptionsProvider, useSelectNativeOptionsContext] = createSelectContext(SELECT_NAME);
  var Select2 = (props) => {
    const {
      __scopeSelect,
      children,
      open: openProp,
      defaultOpen,
      onOpenChange,
      value: valueProp,
      defaultValue,
      onValueChange,
      dir: dir2,
      name,
      autoComplete,
      disabled,
      required,
      form
    } = props;
    const popperScope = usePopperScope4(__scopeSelect);
    const [trigger, setTrigger] = React98.useState(null);
    const [valueNode, setValueNode] = React98.useState(null);
    const [valueNodeHasChildren, setValueNodeHasChildren] = React98.useState(false);
    const direction = useDirection(dir2);
    const [open, setOpen] = useControllableState({
      prop: openProp,
      defaultProp: defaultOpen ?? false,
      onChange: onOpenChange,
      caller: SELECT_NAME
    });
    const [value, setValue] = useControllableState({
      prop: valueProp,
      defaultProp: defaultValue,
      onChange: onValueChange,
      caller: SELECT_NAME
    });
    const triggerPointerDownPosRef = React98.useRef(null);
    const isFormControl = trigger ? form || !!trigger.closest("form") : true;
    const [nativeOptionsSet, setNativeOptionsSet] = React98.useState(/* @__PURE__ */ new Set());
    const nativeSelectKey = Array.from(nativeOptionsSet).map((option) => option.props.value).join(";");
    return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Root23, { ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime41.jsxs)(
      SelectProvider,
      {
        required,
        scope: __scopeSelect,
        trigger,
        onTriggerChange: setTrigger,
        valueNode,
        onValueNodeChange: setValueNode,
        valueNodeHasChildren,
        onValueNodeHasChildrenChange: setValueNodeHasChildren,
        contentId: useId5(),
        value,
        onValueChange: setValue,
        open,
        onOpenChange: setOpen,
        dir: direction,
        triggerPointerDownPosRef,
        disabled,
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Collection5.Provider, { scope: __scopeSelect, children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
            SelectNativeOptionsProvider,
            {
              scope: props.__scopeSelect,
              onNativeOptionAdd: React98.useCallback((option) => {
                setNativeOptionsSet((prev) => new Set(prev).add(option));
              }, []),
              onNativeOptionRemove: React98.useCallback((option) => {
                setNativeOptionsSet((prev) => {
                  const optionsSet = new Set(prev);
                  optionsSet.delete(option);
                  return optionsSet;
                });
              }, []),
              children
            }
          ) }),
          isFormControl ? /* @__PURE__ */ (0, import_jsx_runtime41.jsxs)(
            SelectBubbleInput,
            {
              "aria-hidden": true,
              required,
              tabIndex: -1,
              name,
              autoComplete,
              value,
              onChange: (event) => setValue(event.target.value),
              disabled,
              form,
              children: [
                value === void 0 ? /* @__PURE__ */ (0, import_jsx_runtime41.jsx)("option", { value: "" }) : null,
                Array.from(nativeOptionsSet)
              ]
            },
            nativeSelectKey
          ) : null
        ]
      }
    ) });
  };
  Select2.displayName = SELECT_NAME;
  var TRIGGER_NAME9 = "SelectTrigger";
  var SelectTrigger = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, disabled = false, ...triggerProps } = props;
      const popperScope = usePopperScope4(__scopeSelect);
      const context2 = useSelectContext(TRIGGER_NAME9, __scopeSelect);
      const isDisabled = context2.disabled || disabled;
      const composedRefs = useComposedRefs(forwardedRef, context2.onTriggerChange);
      const getItems = useCollection5(__scopeSelect);
      const pointerTypeRef = React98.useRef("touch");
      const [searchRef, handleTypeaheadSearch, resetTypeahead] = useTypeaheadSearch((search) => {
        const enabledItems = getItems().filter((item) => !item.disabled);
        const currentItem = enabledItems.find((item) => item.value === context2.value);
        const nextItem = findNextItem(enabledItems, search, currentItem);
        if (nextItem !== void 0) {
          context2.onValueChange(nextItem.value);
        }
      });
      const handleOpen = (pointerEvent) => {
        if (!isDisabled) {
          context2.onOpenChange(true);
          resetTypeahead();
        }
        if (pointerEvent) {
          context2.triggerPointerDownPosRef.current = {
            x: Math.round(pointerEvent.pageX),
            y: Math.round(pointerEvent.pageY)
          };
        }
      };
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Anchor, { asChild: true, ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
        Primitive2.button,
        {
          type: "button",
          role: "combobox",
          "aria-controls": context2.contentId,
          "aria-expanded": context2.open,
          "aria-required": context2.required,
          "aria-autocomplete": "none",
          dir: context2.dir,
          "data-state": context2.open ? "open" : "closed",
          disabled: isDisabled,
          "data-disabled": isDisabled ? "" : void 0,
          "data-placeholder": shouldShowPlaceholder(context2.value) ? "" : void 0,
          ...triggerProps,
          ref: composedRefs,
          onClick: composeEventHandlers(triggerProps.onClick, (event) => {
            event.currentTarget.focus();
            if (pointerTypeRef.current !== "mouse") {
              handleOpen(event);
            }
          }),
          onPointerDown: composeEventHandlers(triggerProps.onPointerDown, (event) => {
            pointerTypeRef.current = event.pointerType;
            const target = event.target;
            if (target.hasPointerCapture(event.pointerId)) {
              target.releasePointerCapture(event.pointerId);
            }
            if (event.button === 0 && event.ctrlKey === false && event.pointerType === "mouse") {
              handleOpen(event);
              event.preventDefault();
            }
          }),
          onKeyDown: composeEventHandlers(triggerProps.onKeyDown, (event) => {
            const isTypingAhead = searchRef.current !== "";
            const isModifierKey = event.ctrlKey || event.altKey || event.metaKey;
            if (!isModifierKey && event.key.length === 1) handleTypeaheadSearch(event.key);
            if (isTypingAhead && event.key === " ") return;
            if (OPEN_KEYS.includes(event.key)) {
              handleOpen();
              event.preventDefault();
            }
          })
        }
      ) });
    }
  );
  SelectTrigger.displayName = TRIGGER_NAME9;
  var VALUE_NAME = "SelectValue";
  var SelectValue = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, className, style, children, placeholder = "", ...valueProps } = props;
      const context2 = useSelectContext(VALUE_NAME, __scopeSelect);
      const { onValueNodeHasChildrenChange } = context2;
      const hasChildren = children !== void 0;
      const composedRefs = useComposedRefs(forwardedRef, context2.onValueNodeChange);
      useLayoutEffect22(() => {
        onValueNodeHasChildrenChange(hasChildren);
      }, [onValueNodeHasChildrenChange, hasChildren]);
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
        Primitive2.span,
        {
          ...valueProps,
          ref: composedRefs,
          style: { pointerEvents: "none" },
          children: shouldShowPlaceholder(context2.value) ? /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(import_jsx_runtime41.Fragment, { children: placeholder }) : children
        }
      );
    }
  );
  SelectValue.displayName = VALUE_NAME;
  var ICON_NAME = "SelectIcon";
  var SelectIcon = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, children, ...iconProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Primitive2.span, { "aria-hidden": true, ...iconProps, ref: forwardedRef, children: children || "\u25BC" });
    }
  );
  SelectIcon.displayName = ICON_NAME;
  var PORTAL_NAME7 = "SelectPortal";
  var SelectPortal = (props) => {
    return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Portal, { asChild: true, ...props });
  };
  SelectPortal.displayName = PORTAL_NAME7;
  var CONTENT_NAME10 = "SelectContent";
  var SelectContent = React98.forwardRef(
    (props, forwardedRef) => {
      const context2 = useSelectContext(CONTENT_NAME10, props.__scopeSelect);
      const [fragment, setFragment] = React98.useState();
      useLayoutEffect22(() => {
        setFragment(new DocumentFragment());
      }, []);
      if (!context2.open) {
        const frag = fragment;
        return frag ? ReactDOM10.createPortal(
          /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(SelectContentProvider, { scope: props.__scopeSelect, children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Collection5.Slot, { scope: props.__scopeSelect, children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)("div", { children: props.children }) }) }),
          frag
        ) : null;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(SelectContentImpl, { ...props, ref: forwardedRef });
    }
  );
  SelectContent.displayName = CONTENT_NAME10;
  var CONTENT_MARGIN = 10;
  var [SelectContentProvider, useSelectContentContext] = createSelectContext(CONTENT_NAME10);
  var CONTENT_IMPL_NAME = "SelectContentImpl";
  var Slot5 = createSlot7("SelectContent.RemoveScroll");
  var SelectContentImpl = React98.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeSelect,
        position = "item-aligned",
        onCloseAutoFocus,
        onEscapeKeyDown,
        onPointerDownOutside,
        //
        // PopperContent props
        side,
        sideOffset,
        align,
        alignOffset,
        arrowPadding,
        collisionBoundary,
        collisionPadding,
        sticky,
        hideWhenDetached,
        avoidCollisions,
        //
        ...contentProps
      } = props;
      const context2 = useSelectContext(CONTENT_NAME10, __scopeSelect);
      const [content, setContent] = React98.useState(null);
      const [viewport, setViewport] = React98.useState(null);
      const composedRefs = useComposedRefs(forwardedRef, (node) => setContent(node));
      const [selectedItem, setSelectedItem] = React98.useState(null);
      const [selectedItemText, setSelectedItemText] = React98.useState(
        null
      );
      const getItems = useCollection5(__scopeSelect);
      const [isPositioned, setIsPositioned] = React98.useState(false);
      const firstValidItemFoundRef = React98.useRef(false);
      React98.useEffect(() => {
        if (content) return hideOthers(content);
      }, [content]);
      useFocusGuards();
      const focusFirst5 = React98.useCallback(
        (candidates) => {
          const [firstItem, ...restItems] = getItems().map((item) => item.ref.current);
          const [lastItem] = restItems.slice(-1);
          const PREVIOUSLY_FOCUSED_ELEMENT = document.activeElement;
          for (const candidate of candidates) {
            if (candidate === PREVIOUSLY_FOCUSED_ELEMENT) return;
            candidate?.scrollIntoView({ block: "nearest" });
            if (candidate === firstItem && viewport) viewport.scrollTop = 0;
            if (candidate === lastItem && viewport) viewport.scrollTop = viewport.scrollHeight;
            candidate?.focus();
            if (document.activeElement !== PREVIOUSLY_FOCUSED_ELEMENT) return;
          }
        },
        [getItems, viewport]
      );
      const focusSelectedItem = React98.useCallback(
        () => focusFirst5([selectedItem, content]),
        [focusFirst5, selectedItem, content]
      );
      React98.useEffect(() => {
        if (isPositioned) {
          focusSelectedItem();
        }
      }, [isPositioned, focusSelectedItem]);
      const { onOpenChange, triggerPointerDownPosRef } = context2;
      React98.useEffect(() => {
        if (content) {
          let pointerMoveDelta = { x: 0, y: 0 };
          const handlePointerMove = (event) => {
            pointerMoveDelta = {
              x: Math.abs(Math.round(event.pageX) - (triggerPointerDownPosRef.current?.x ?? 0)),
              y: Math.abs(Math.round(event.pageY) - (triggerPointerDownPosRef.current?.y ?? 0))
            };
          };
          const handlePointerUp = (event) => {
            if (pointerMoveDelta.x <= 10 && pointerMoveDelta.y <= 10) {
              event.preventDefault();
            } else {
              if (!content.contains(event.target)) {
                onOpenChange(false);
              }
            }
            document.removeEventListener("pointermove", handlePointerMove);
            triggerPointerDownPosRef.current = null;
          };
          if (triggerPointerDownPosRef.current !== null) {
            document.addEventListener("pointermove", handlePointerMove);
            document.addEventListener("pointerup", handlePointerUp, { capture: true, once: true });
          }
          return () => {
            document.removeEventListener("pointermove", handlePointerMove);
            document.removeEventListener("pointerup", handlePointerUp, { capture: true });
          };
        }
      }, [content, onOpenChange, triggerPointerDownPosRef]);
      React98.useEffect(() => {
        const close = () => onOpenChange(false);
        window.addEventListener("blur", close);
        window.addEventListener("resize", close);
        return () => {
          window.removeEventListener("blur", close);
          window.removeEventListener("resize", close);
        };
      }, [onOpenChange]);
      const [searchRef, handleTypeaheadSearch] = useTypeaheadSearch((search) => {
        const enabledItems = getItems().filter((item) => !item.disabled);
        const currentItem = enabledItems.find((item) => item.ref.current === document.activeElement);
        const nextItem = findNextItem(enabledItems, search, currentItem);
        if (nextItem) {
          setTimeout(() => nextItem.ref.current.focus());
        }
      });
      const itemRefCallback = React98.useCallback(
        (node, value, disabled) => {
          const isFirstValidItem = !firstValidItemFoundRef.current && !disabled;
          const isSelectedItem = context2.value !== void 0 && context2.value === value;
          if (isSelectedItem || isFirstValidItem) {
            setSelectedItem(node);
            if (isFirstValidItem) firstValidItemFoundRef.current = true;
          }
        },
        [context2.value]
      );
      const handleItemLeave = React98.useCallback(() => content?.focus(), [content]);
      const itemTextRefCallback = React98.useCallback(
        (node, value, disabled) => {
          const isFirstValidItem = !firstValidItemFoundRef.current && !disabled;
          const isSelectedItem = context2.value !== void 0 && context2.value === value;
          if (isSelectedItem || isFirstValidItem) {
            setSelectedItemText(node);
          }
        },
        [context2.value]
      );
      const SelectPosition = position === "popper" ? SelectPopperPosition : SelectItemAlignedPosition;
      const popperContentProps = SelectPosition === SelectPopperPosition ? {
        side,
        sideOffset,
        align,
        alignOffset,
        arrowPadding,
        collisionBoundary,
        collisionPadding,
        sticky,
        hideWhenDetached,
        avoidCollisions
      } : {};
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
        SelectContentProvider,
        {
          scope: __scopeSelect,
          content,
          viewport,
          onViewportChange: setViewport,
          itemRefCallback,
          selectedItem,
          onItemLeave: handleItemLeave,
          itemTextRefCallback,
          focusSelectedItem,
          selectedItemText,
          position,
          isPositioned,
          searchRef,
          children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Combination_default, { as: Slot5, allowPinchZoom: true, children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
            FocusScope,
            {
              asChild: true,
              trapped: context2.open,
              onMountAutoFocus: (event) => {
                event.preventDefault();
              },
              onUnmountAutoFocus: composeEventHandlers(onCloseAutoFocus, (event) => {
                context2.trigger?.focus({ preventScroll: true });
                event.preventDefault();
              }),
              children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
                DismissableLayer,
                {
                  asChild: true,
                  disableOutsidePointerEvents: true,
                  onEscapeKeyDown,
                  onPointerDownOutside,
                  onFocusOutside: (event) => event.preventDefault(),
                  onDismiss: () => context2.onOpenChange(false),
                  children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
                    SelectPosition,
                    {
                      role: "listbox",
                      id: context2.contentId,
                      "data-state": context2.open ? "open" : "closed",
                      dir: context2.dir,
                      onContextMenu: (event) => event.preventDefault(),
                      ...contentProps,
                      ...popperContentProps,
                      onPlaced: () => setIsPositioned(true),
                      ref: composedRefs,
                      style: {
                        // flex layout so we can place the scroll buttons properly
                        display: "flex",
                        flexDirection: "column",
                        // reset the outline by default as the content MAY get focused
                        outline: "none",
                        ...contentProps.style
                      },
                      onKeyDown: composeEventHandlers(contentProps.onKeyDown, (event) => {
                        const isModifierKey = event.ctrlKey || event.altKey || event.metaKey;
                        if (event.key === "Tab") event.preventDefault();
                        if (!isModifierKey && event.key.length === 1) handleTypeaheadSearch(event.key);
                        if (["ArrowUp", "ArrowDown", "Home", "End"].includes(event.key)) {
                          const items = getItems().filter((item) => !item.disabled);
                          let candidateNodes = items.map((item) => item.ref.current);
                          if (["ArrowUp", "End"].includes(event.key)) {
                            candidateNodes = candidateNodes.slice().reverse();
                          }
                          if (["ArrowUp", "ArrowDown"].includes(event.key)) {
                            const currentElement = event.target;
                            const currentIndex = candidateNodes.indexOf(currentElement);
                            candidateNodes = candidateNodes.slice(currentIndex + 1);
                          }
                          setTimeout(() => focusFirst5(candidateNodes));
                          event.preventDefault();
                        }
                      })
                    }
                  )
                }
              )
            }
          ) })
        }
      );
    }
  );
  SelectContentImpl.displayName = CONTENT_IMPL_NAME;
  var ITEM_ALIGNED_POSITION_NAME = "SelectItemAlignedPosition";
  var SelectItemAlignedPosition = React98.forwardRef((props, forwardedRef) => {
    const { __scopeSelect, onPlaced, ...popperProps } = props;
    const context2 = useSelectContext(CONTENT_NAME10, __scopeSelect);
    const contentContext = useSelectContentContext(CONTENT_NAME10, __scopeSelect);
    const [contentWrapper, setContentWrapper] = React98.useState(null);
    const [content, setContent] = React98.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setContent(node));
    const getItems = useCollection5(__scopeSelect);
    const shouldExpandOnScrollRef = React98.useRef(false);
    const shouldRepositionRef = React98.useRef(true);
    const { viewport, selectedItem, selectedItemText, focusSelectedItem } = contentContext;
    const position = React98.useCallback(() => {
      if (context2.trigger && context2.valueNode && contentWrapper && content && viewport && selectedItem && selectedItemText) {
        const triggerRect = context2.trigger.getBoundingClientRect();
        const contentRect = content.getBoundingClientRect();
        const valueNodeRect = context2.valueNode.getBoundingClientRect();
        const itemTextRect = selectedItemText.getBoundingClientRect();
        if (context2.dir !== "rtl") {
          const itemTextOffset = itemTextRect.left - contentRect.left;
          const left = valueNodeRect.left - itemTextOffset;
          const leftDelta = triggerRect.left - left;
          const minContentWidth = triggerRect.width + leftDelta;
          const contentWidth = Math.max(minContentWidth, contentRect.width);
          const rightEdge = window.innerWidth - CONTENT_MARGIN;
          const clampedLeft = clamp2(left, [
            CONTENT_MARGIN,
            // Prevents the content from going off the starting edge of the
            // viewport. It may still go off the ending edge, but this can be
            // controlled by the user since they may want to manage overflow in a
            // specific way.
            // https://github.com/radix-ui/primitives/issues/2049
            Math.max(CONTENT_MARGIN, rightEdge - contentWidth)
          ]);
          contentWrapper.style.minWidth = minContentWidth + "px";
          contentWrapper.style.left = clampedLeft + "px";
        } else {
          const itemTextOffset = contentRect.right - itemTextRect.right;
          const right = window.innerWidth - valueNodeRect.right - itemTextOffset;
          const rightDelta = window.innerWidth - triggerRect.right - right;
          const minContentWidth = triggerRect.width + rightDelta;
          const contentWidth = Math.max(minContentWidth, contentRect.width);
          const leftEdge = window.innerWidth - CONTENT_MARGIN;
          const clampedRight = clamp2(right, [
            CONTENT_MARGIN,
            Math.max(CONTENT_MARGIN, leftEdge - contentWidth)
          ]);
          contentWrapper.style.minWidth = minContentWidth + "px";
          contentWrapper.style.right = clampedRight + "px";
        }
        const items = getItems();
        const availableHeight = window.innerHeight - CONTENT_MARGIN * 2;
        const itemsHeight = viewport.scrollHeight;
        const contentStyles = window.getComputedStyle(content);
        const contentBorderTopWidth = parseInt(contentStyles.borderTopWidth, 10);
        const contentPaddingTop = parseInt(contentStyles.paddingTop, 10);
        const contentBorderBottomWidth = parseInt(contentStyles.borderBottomWidth, 10);
        const contentPaddingBottom = parseInt(contentStyles.paddingBottom, 10);
        const fullContentHeight = contentBorderTopWidth + contentPaddingTop + itemsHeight + contentPaddingBottom + contentBorderBottomWidth;
        const minContentHeight = Math.min(selectedItem.offsetHeight * 5, fullContentHeight);
        const viewportStyles = window.getComputedStyle(viewport);
        const viewportPaddingTop = parseInt(viewportStyles.paddingTop, 10);
        const viewportPaddingBottom = parseInt(viewportStyles.paddingBottom, 10);
        const topEdgeToTriggerMiddle = triggerRect.top + triggerRect.height / 2 - CONTENT_MARGIN;
        const triggerMiddleToBottomEdge = availableHeight - topEdgeToTriggerMiddle;
        const selectedItemHalfHeight = selectedItem.offsetHeight / 2;
        const itemOffsetMiddle = selectedItem.offsetTop + selectedItemHalfHeight;
        const contentTopToItemMiddle = contentBorderTopWidth + contentPaddingTop + itemOffsetMiddle;
        const itemMiddleToContentBottom = fullContentHeight - contentTopToItemMiddle;
        const willAlignWithoutTopOverflow = contentTopToItemMiddle <= topEdgeToTriggerMiddle;
        if (willAlignWithoutTopOverflow) {
          const isLastItem = items.length > 0 && selectedItem === items[items.length - 1].ref.current;
          contentWrapper.style.bottom = "0px";
          const viewportOffsetBottom = content.clientHeight - viewport.offsetTop - viewport.offsetHeight;
          const clampedTriggerMiddleToBottomEdge = Math.max(
            triggerMiddleToBottomEdge,
            selectedItemHalfHeight + // viewport might have padding bottom, include it to avoid a scrollable viewport
            (isLastItem ? viewportPaddingBottom : 0) + viewportOffsetBottom + contentBorderBottomWidth
          );
          const height = contentTopToItemMiddle + clampedTriggerMiddleToBottomEdge;
          contentWrapper.style.height = height + "px";
        } else {
          const isFirstItem = items.length > 0 && selectedItem === items[0].ref.current;
          contentWrapper.style.top = "0px";
          const clampedTopEdgeToTriggerMiddle = Math.max(
            topEdgeToTriggerMiddle,
            contentBorderTopWidth + viewport.offsetTop + // viewport might have padding top, include it to avoid a scrollable viewport
            (isFirstItem ? viewportPaddingTop : 0) + selectedItemHalfHeight
          );
          const height = clampedTopEdgeToTriggerMiddle + itemMiddleToContentBottom;
          contentWrapper.style.height = height + "px";
          viewport.scrollTop = contentTopToItemMiddle - topEdgeToTriggerMiddle + viewport.offsetTop;
        }
        contentWrapper.style.margin = `${CONTENT_MARGIN}px 0`;
        contentWrapper.style.minHeight = minContentHeight + "px";
        contentWrapper.style.maxHeight = availableHeight + "px";
        onPlaced?.();
        requestAnimationFrame(() => shouldExpandOnScrollRef.current = true);
      }
    }, [
      getItems,
      context2.trigger,
      context2.valueNode,
      contentWrapper,
      content,
      viewport,
      selectedItem,
      selectedItemText,
      context2.dir,
      onPlaced
    ]);
    useLayoutEffect22(() => position(), [position]);
    const [contentZIndex, setContentZIndex] = React98.useState();
    useLayoutEffect22(() => {
      if (content) setContentZIndex(window.getComputedStyle(content).zIndex);
    }, [content]);
    const handleScrollButtonChange = React98.useCallback(
      (node) => {
        if (node && shouldRepositionRef.current === true) {
          position();
          focusSelectedItem?.();
          shouldRepositionRef.current = false;
        }
      },
      [position, focusSelectedItem]
    );
    return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
      SelectViewportProvider,
      {
        scope: __scopeSelect,
        contentWrapper,
        shouldExpandOnScrollRef,
        onScrollButtonChange: handleScrollButtonChange,
        children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
          "div",
          {
            ref: setContentWrapper,
            style: {
              display: "flex",
              flexDirection: "column",
              position: "fixed",
              zIndex: contentZIndex
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
              Primitive2.div,
              {
                ...popperProps,
                ref: composedRefs,
                style: {
                  // When we get the height of the content, it includes borders. If we were to set
                  // the height without having `boxSizing: 'border-box'` it would be too big.
                  boxSizing: "border-box",
                  // We need to ensure the content doesn't get taller than the wrapper
                  maxHeight: "100%",
                  ...popperProps.style
                }
              }
            )
          }
        )
      }
    );
  });
  SelectItemAlignedPosition.displayName = ITEM_ALIGNED_POSITION_NAME;
  var POPPER_POSITION_NAME = "SelectPopperPosition";
  var SelectPopperPosition = React98.forwardRef((props, forwardedRef) => {
    const {
      __scopeSelect,
      align = "start",
      collisionPadding = CONTENT_MARGIN,
      ...popperProps
    } = props;
    const popperScope = usePopperScope4(__scopeSelect);
    return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
      Content4,
      {
        ...popperScope,
        ...popperProps,
        ref: forwardedRef,
        align,
        collisionPadding,
        style: {
          // Ensure border-box for floating-ui calculations
          boxSizing: "border-box",
          ...popperProps.style,
          // re-namespace exposed content custom properties
          ...{
            "--radix-select-content-transform-origin": "var(--radix-popper-transform-origin)",
            "--radix-select-content-available-width": "var(--radix-popper-available-width)",
            "--radix-select-content-available-height": "var(--radix-popper-available-height)",
            "--radix-select-trigger-width": "var(--radix-popper-anchor-width)",
            "--radix-select-trigger-height": "var(--radix-popper-anchor-height)"
          }
        }
      }
    );
  });
  SelectPopperPosition.displayName = POPPER_POSITION_NAME;
  var [SelectViewportProvider, useSelectViewportContext] = createSelectContext(CONTENT_NAME10, {});
  var VIEWPORT_NAME3 = "SelectViewport";
  var SelectViewport = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, nonce, ...viewportProps } = props;
      const contentContext = useSelectContentContext(VIEWPORT_NAME3, __scopeSelect);
      const viewportContext = useSelectViewportContext(VIEWPORT_NAME3, __scopeSelect);
      const composedRefs = useComposedRefs(forwardedRef, contentContext.onViewportChange);
      const prevScrollTopRef = React98.useRef(0);
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsxs)(import_jsx_runtime41.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
          "style",
          {
            dangerouslySetInnerHTML: {
              __html: `[data-radix-select-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-select-viewport]::-webkit-scrollbar{display:none}`
            },
            nonce
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Collection5.Slot, { scope: __scopeSelect, children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
          Primitive2.div,
          {
            "data-radix-select-viewport": "",
            role: "presentation",
            ...viewportProps,
            ref: composedRefs,
            style: {
              // we use position: 'relative' here on the `viewport` so that when we call
              // `selectedItem.offsetTop` in calculations, the offset is relative to the viewport
              // (independent of the scrollUpButton).
              position: "relative",
              flex: 1,
              // Viewport should only be scrollable in the vertical direction.
              // This won't work in vertical writing modes, so we'll need to
              // revisit this if/when that is supported
              // https://developer.chrome.com/blog/vertical-form-controls
              overflow: "hidden auto",
              ...viewportProps.style
            },
            onScroll: composeEventHandlers(viewportProps.onScroll, (event) => {
              const viewport = event.currentTarget;
              const { contentWrapper, shouldExpandOnScrollRef } = viewportContext;
              if (shouldExpandOnScrollRef?.current && contentWrapper) {
                const scrolledBy = Math.abs(prevScrollTopRef.current - viewport.scrollTop);
                if (scrolledBy > 0) {
                  const availableHeight = window.innerHeight - CONTENT_MARGIN * 2;
                  const cssMinHeight = parseFloat(contentWrapper.style.minHeight);
                  const cssHeight = parseFloat(contentWrapper.style.height);
                  const prevHeight = Math.max(cssMinHeight, cssHeight);
                  if (prevHeight < availableHeight) {
                    const nextHeight = prevHeight + scrolledBy;
                    const clampedNextHeight = Math.min(availableHeight, nextHeight);
                    const heightDiff = nextHeight - clampedNextHeight;
                    contentWrapper.style.height = clampedNextHeight + "px";
                    if (contentWrapper.style.bottom === "0px") {
                      viewport.scrollTop = heightDiff > 0 ? heightDiff : 0;
                      contentWrapper.style.justifyContent = "flex-end";
                    }
                  }
                }
              }
              prevScrollTopRef.current = viewport.scrollTop;
            })
          }
        ) })
      ] });
    }
  );
  SelectViewport.displayName = VIEWPORT_NAME3;
  var GROUP_NAME4 = "SelectGroup";
  var [SelectGroupContextProvider, useSelectGroupContext] = createSelectContext(GROUP_NAME4);
  var SelectGroup = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...groupProps } = props;
      const groupId = useId5();
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(SelectGroupContextProvider, { scope: __scopeSelect, id: groupId, children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Primitive2.div, { role: "group", "aria-labelledby": groupId, ...groupProps, ref: forwardedRef }) });
    }
  );
  SelectGroup.displayName = GROUP_NAME4;
  var LABEL_NAME3 = "SelectLabel";
  var SelectLabel = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...labelProps } = props;
      const groupContext = useSelectGroupContext(LABEL_NAME3, __scopeSelect);
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Primitive2.div, { id: groupContext.id, ...labelProps, ref: forwardedRef });
    }
  );
  SelectLabel.displayName = LABEL_NAME3;
  var ITEM_NAME5 = "SelectItem";
  var [SelectItemContextProvider, useSelectItemContext] = createSelectContext(ITEM_NAME5);
  var SelectItem = React98.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeSelect,
        value,
        disabled = false,
        textValue: textValueProp,
        ...itemProps
      } = props;
      const context2 = useSelectContext(ITEM_NAME5, __scopeSelect);
      const contentContext = useSelectContentContext(ITEM_NAME5, __scopeSelect);
      const isSelected = context2.value === value;
      const [textValue, setTextValue] = React98.useState(textValueProp ?? "");
      const [isFocused, setIsFocused] = React98.useState(false);
      const composedRefs = useComposedRefs(
        forwardedRef,
        (node) => contentContext.itemRefCallback?.(node, value, disabled)
      );
      const textId = useId5();
      const pointerTypeRef = React98.useRef("touch");
      const handleSelect = () => {
        if (!disabled) {
          context2.onValueChange(value);
          context2.onOpenChange(false);
        }
      };
      if (value === "") {
        throw new Error(
          "A <Select.Item /> must have a value prop that is not an empty string. This is because the Select value can be set to an empty string to clear the selection and show the placeholder."
        );
      }
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
        SelectItemContextProvider,
        {
          scope: __scopeSelect,
          value,
          disabled,
          textId,
          isSelected,
          onItemTextChange: React98.useCallback((node) => {
            setTextValue((prevTextValue) => prevTextValue || (node?.textContent ?? "").trim());
          }, []),
          children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
            Collection5.ItemSlot,
            {
              scope: __scopeSelect,
              value,
              disabled,
              textValue,
              children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
                Primitive2.div,
                {
                  role: "option",
                  "aria-labelledby": textId,
                  "data-highlighted": isFocused ? "" : void 0,
                  "aria-selected": isSelected && isFocused,
                  "data-state": isSelected ? "checked" : "unchecked",
                  "aria-disabled": disabled || void 0,
                  "data-disabled": disabled ? "" : void 0,
                  tabIndex: disabled ? void 0 : -1,
                  ...itemProps,
                  ref: composedRefs,
                  onFocus: composeEventHandlers(itemProps.onFocus, () => setIsFocused(true)),
                  onBlur: composeEventHandlers(itemProps.onBlur, () => setIsFocused(false)),
                  onClick: composeEventHandlers(itemProps.onClick, () => {
                    if (pointerTypeRef.current !== "mouse") handleSelect();
                  }),
                  onPointerUp: composeEventHandlers(itemProps.onPointerUp, () => {
                    if (pointerTypeRef.current === "mouse") handleSelect();
                  }),
                  onPointerDown: composeEventHandlers(itemProps.onPointerDown, (event) => {
                    pointerTypeRef.current = event.pointerType;
                  }),
                  onPointerMove: composeEventHandlers(itemProps.onPointerMove, (event) => {
                    pointerTypeRef.current = event.pointerType;
                    if (disabled) {
                      contentContext.onItemLeave?.();
                    } else if (pointerTypeRef.current === "mouse") {
                      event.currentTarget.focus({ preventScroll: true });
                    }
                  }),
                  onPointerLeave: composeEventHandlers(itemProps.onPointerLeave, (event) => {
                    if (event.currentTarget === document.activeElement) {
                      contentContext.onItemLeave?.();
                    }
                  }),
                  onKeyDown: composeEventHandlers(itemProps.onKeyDown, (event) => {
                    const isTypingAhead = contentContext.searchRef?.current !== "";
                    if (isTypingAhead && event.key === " ") return;
                    if (SELECTION_KEYS2.includes(event.key)) handleSelect();
                    if (event.key === " ") event.preventDefault();
                  })
                }
              )
            }
          )
        }
      );
    }
  );
  SelectItem.displayName = ITEM_NAME5;
  var ITEM_TEXT_NAME = "SelectItemText";
  var SelectItemText = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, className, style, ...itemTextProps } = props;
      const context2 = useSelectContext(ITEM_TEXT_NAME, __scopeSelect);
      const contentContext = useSelectContentContext(ITEM_TEXT_NAME, __scopeSelect);
      const itemContext = useSelectItemContext(ITEM_TEXT_NAME, __scopeSelect);
      const nativeOptionsContext = useSelectNativeOptionsContext(ITEM_TEXT_NAME, __scopeSelect);
      const [itemTextNode, setItemTextNode] = React98.useState(null);
      const composedRefs = useComposedRefs(
        forwardedRef,
        (node) => setItemTextNode(node),
        itemContext.onItemTextChange,
        (node) => contentContext.itemTextRefCallback?.(node, itemContext.value, itemContext.disabled)
      );
      const textContent = itemTextNode?.textContent;
      const nativeOption = React98.useMemo(
        () => /* @__PURE__ */ (0, import_jsx_runtime41.jsx)("option", { value: itemContext.value, disabled: itemContext.disabled, children: textContent }, itemContext.value),
        [itemContext.disabled, itemContext.value, textContent]
      );
      const { onNativeOptionAdd, onNativeOptionRemove } = nativeOptionsContext;
      useLayoutEffect22(() => {
        onNativeOptionAdd(nativeOption);
        return () => onNativeOptionRemove(nativeOption);
      }, [onNativeOptionAdd, onNativeOptionRemove, nativeOption]);
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsxs)(import_jsx_runtime41.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Primitive2.span, { id: itemContext.textId, ...itemTextProps, ref: composedRefs }),
        itemContext.isSelected && context2.valueNode && !context2.valueNodeHasChildren ? ReactDOM10.createPortal(itemTextProps.children, context2.valueNode) : null
      ] });
    }
  );
  SelectItemText.displayName = ITEM_TEXT_NAME;
  var ITEM_INDICATOR_NAME2 = "SelectItemIndicator";
  var SelectItemIndicator = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...itemIndicatorProps } = props;
      const itemContext = useSelectItemContext(ITEM_INDICATOR_NAME2, __scopeSelect);
      return itemContext.isSelected ? /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Primitive2.span, { "aria-hidden": true, ...itemIndicatorProps, ref: forwardedRef }) : null;
    }
  );
  SelectItemIndicator.displayName = ITEM_INDICATOR_NAME2;
  var SCROLL_UP_BUTTON_NAME = "SelectScrollUpButton";
  var SelectScrollUpButton = React98.forwardRef((props, forwardedRef) => {
    const contentContext = useSelectContentContext(SCROLL_UP_BUTTON_NAME, props.__scopeSelect);
    const viewportContext = useSelectViewportContext(SCROLL_UP_BUTTON_NAME, props.__scopeSelect);
    const [canScrollUp, setCanScrollUp] = React98.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, viewportContext.onScrollButtonChange);
    useLayoutEffect22(() => {
      if (contentContext.viewport && contentContext.isPositioned) {
        let handleScroll22 = function() {
          const canScrollUp2 = viewport.scrollTop > 0;
          setCanScrollUp(canScrollUp2);
        };
        var handleScroll2 = handleScroll22;
        const viewport = contentContext.viewport;
        handleScroll22();
        viewport.addEventListener("scroll", handleScroll22);
        return () => viewport.removeEventListener("scroll", handleScroll22);
      }
    }, [contentContext.viewport, contentContext.isPositioned]);
    return canScrollUp ? /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
      SelectScrollButtonImpl,
      {
        ...props,
        ref: composedRefs,
        onAutoScroll: () => {
          const { viewport, selectedItem } = contentContext;
          if (viewport && selectedItem) {
            viewport.scrollTop = viewport.scrollTop - selectedItem.offsetHeight;
          }
        }
      }
    ) : null;
  });
  SelectScrollUpButton.displayName = SCROLL_UP_BUTTON_NAME;
  var SCROLL_DOWN_BUTTON_NAME = "SelectScrollDownButton";
  var SelectScrollDownButton = React98.forwardRef((props, forwardedRef) => {
    const contentContext = useSelectContentContext(SCROLL_DOWN_BUTTON_NAME, props.__scopeSelect);
    const viewportContext = useSelectViewportContext(SCROLL_DOWN_BUTTON_NAME, props.__scopeSelect);
    const [canScrollDown, setCanScrollDown] = React98.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, viewportContext.onScrollButtonChange);
    useLayoutEffect22(() => {
      if (contentContext.viewport && contentContext.isPositioned) {
        let handleScroll22 = function() {
          const maxScroll = viewport.scrollHeight - viewport.clientHeight;
          const canScrollDown2 = Math.ceil(viewport.scrollTop) < maxScroll;
          setCanScrollDown(canScrollDown2);
        };
        var handleScroll2 = handleScroll22;
        const viewport = contentContext.viewport;
        handleScroll22();
        viewport.addEventListener("scroll", handleScroll22);
        return () => viewport.removeEventListener("scroll", handleScroll22);
      }
    }, [contentContext.viewport, contentContext.isPositioned]);
    return canScrollDown ? /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
      SelectScrollButtonImpl,
      {
        ...props,
        ref: composedRefs,
        onAutoScroll: () => {
          const { viewport, selectedItem } = contentContext;
          if (viewport && selectedItem) {
            viewport.scrollTop = viewport.scrollTop + selectedItem.offsetHeight;
          }
        }
      }
    ) : null;
  });
  SelectScrollDownButton.displayName = SCROLL_DOWN_BUTTON_NAME;
  var SelectScrollButtonImpl = React98.forwardRef((props, forwardedRef) => {
    const { __scopeSelect, onAutoScroll, ...scrollIndicatorProps } = props;
    const contentContext = useSelectContentContext("SelectScrollButton", __scopeSelect);
    const autoScrollTimerRef = React98.useRef(null);
    const getItems = useCollection5(__scopeSelect);
    const clearAutoScrollTimer = React98.useCallback(() => {
      if (autoScrollTimerRef.current !== null) {
        window.clearInterval(autoScrollTimerRef.current);
        autoScrollTimerRef.current = null;
      }
    }, []);
    React98.useEffect(() => {
      return () => clearAutoScrollTimer();
    }, [clearAutoScrollTimer]);
    useLayoutEffect22(() => {
      const activeItem = getItems().find((item) => item.ref.current === document.activeElement);
      activeItem?.ref.current?.scrollIntoView({ block: "nearest" });
    }, [getItems]);
    return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
      Primitive2.div,
      {
        "aria-hidden": true,
        ...scrollIndicatorProps,
        ref: forwardedRef,
        style: { flexShrink: 0, ...scrollIndicatorProps.style },
        onPointerDown: composeEventHandlers(scrollIndicatorProps.onPointerDown, () => {
          if (autoScrollTimerRef.current === null) {
            autoScrollTimerRef.current = window.setInterval(onAutoScroll, 50);
          }
        }),
        onPointerMove: composeEventHandlers(scrollIndicatorProps.onPointerMove, () => {
          contentContext.onItemLeave?.();
          if (autoScrollTimerRef.current === null) {
            autoScrollTimerRef.current = window.setInterval(onAutoScroll, 50);
          }
        }),
        onPointerLeave: composeEventHandlers(scrollIndicatorProps.onPointerLeave, () => {
          clearAutoScrollTimer();
        })
      }
    );
  });
  var SEPARATOR_NAME3 = "SelectSeparator";
  var SelectSeparator = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...separatorProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Primitive2.div, { "aria-hidden": true, ...separatorProps, ref: forwardedRef });
    }
  );
  SelectSeparator.displayName = SEPARATOR_NAME3;
  var ARROW_NAME6 = "SelectArrow";
  var SelectArrow = React98.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...arrowProps } = props;
      const popperScope = usePopperScope4(__scopeSelect);
      const context2 = useSelectContext(ARROW_NAME6, __scopeSelect);
      const contentContext = useSelectContentContext(ARROW_NAME6, __scopeSelect);
      return context2.open && contentContext.position === "popper" ? /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Arrow2, { ...popperScope, ...arrowProps, ref: forwardedRef }) : null;
    }
  );
  SelectArrow.displayName = ARROW_NAME6;
  var BUBBLE_INPUT_NAME2 = "SelectBubbleInput";
  var SelectBubbleInput = React98.forwardRef(
    ({ __scopeSelect, value, ...props }, forwardedRef) => {
      const ref = React98.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      const prevValue = usePrevious2(value);
      React98.useEffect(() => {
        const select = ref.current;
        if (!select) return;
        const selectProto = window.HTMLSelectElement.prototype;
        const descriptor = Object.getOwnPropertyDescriptor(
          selectProto,
          "value"
        );
        const setValue = descriptor.set;
        if (prevValue !== value && setValue) {
          const event = new Event("change", { bubbles: true });
          setValue.call(select, value);
          select.dispatchEvent(event);
        }
      }, [prevValue, value]);
      return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(
        Primitive2.select,
        {
          ...props,
          style: { ...VISUALLY_HIDDEN_STYLES, ...props.style },
          ref: composedRefs,
          defaultValue: value
        }
      );
    }
  );
  SelectBubbleInput.displayName = BUBBLE_INPUT_NAME2;
  function shouldShowPlaceholder(value) {
    return value === "" || value === void 0;
  }
  function useTypeaheadSearch(onSearchChange) {
    const handleSearchChange = useCallbackRef(onSearchChange);
    const searchRef = React98.useRef("");
    const timerRef = React98.useRef(0);
    const handleTypeaheadSearch = React98.useCallback(
      (key) => {
        const search = searchRef.current + key;
        handleSearchChange(search);
        (function updateSearch(value) {
          searchRef.current = value;
          window.clearTimeout(timerRef.current);
          if (value !== "") timerRef.current = window.setTimeout(() => updateSearch(""), 1e3);
        })(search);
      },
      [handleSearchChange]
    );
    const resetTypeahead = React98.useCallback(() => {
      searchRef.current = "";
      window.clearTimeout(timerRef.current);
    }, []);
    React98.useEffect(() => {
      return () => window.clearTimeout(timerRef.current);
    }, []);
    return [searchRef, handleTypeaheadSearch, resetTypeahead];
  }
  function findNextItem(items, search, currentItem) {
    const isRepeated = search.length > 1 && Array.from(search).every((char) => char === search[0]);
    const normalizedSearch = isRepeated ? search[0] : search;
    const currentItemIndex = currentItem ? items.indexOf(currentItem) : -1;
    let wrappedItems = wrapArray3(items, Math.max(currentItemIndex, 0));
    const excludeCurrentItem = normalizedSearch.length === 1;
    if (excludeCurrentItem) wrappedItems = wrappedItems.filter((v) => v !== currentItem);
    const nextItem = wrappedItems.find(
      (item) => item.textValue.toLowerCase().startsWith(normalizedSearch.toLowerCase())
    );
    return nextItem !== currentItem ? nextItem : void 0;
  }
  function wrapArray3(array, startIndex) {
    return array.map((_, index2) => array[(startIndex + index2) % array.length]);
  }
  var Root28 = Select2;
  var Trigger14 = SelectTrigger;
  var Value = SelectValue;
  var Icon4 = SelectIcon;
  var Portal5 = SelectPortal;
  var Content27 = SelectContent;
  var Viewport3 = SelectViewport;
  var Group3 = SelectGroup;
  var Label6 = SelectLabel;
  var Item10 = SelectItem;
  var ItemText = SelectItemText;
  var ItemIndicator3 = SelectItemIndicator;
  var ScrollUpButton = SelectScrollUpButton;
  var ScrollDownButton = SelectScrollDownButton;
  var Separator4 = SelectSeparator;

  // src/components/ui/select/Select.tsx
  var SelectTrigger2 = ({ ref, className, children, ...props }) => /* @__PURE__ */ React.createElement(
    Trigger14,
    {
      ref,
      "data-slot": "select-trigger",
      className: cn(
        "group/select-trigger bg-background relative flex w-full cursor-pointer items-center gap-2 rounded-md border p-3 text-sm shadow-xs",
        "border-muted-200 transition-[color,box-shadow] outline-none",
        "data-[state=open]:border-primary data-[state=open]:ring-primary data-[state=open]:ring",
        "hover:not-disabled:border-primary hover:not-disabled:ring-primary hover:not-disabled:ring",
        "focus-visible:ring-primary focus-visible:border-primary focus-visible:ring",
        "disabled:bg-muted-50 disabled:text-muted-300 disabled:pointer-events-none",
        className
      ),
      ...props
    },
    children
  );
  var SelectValue2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement("span", { "data-slot": "select-value", className: cn("flex-1 text-start", className) }, /* @__PURE__ */ React.createElement(Value, { ...props }));
  var SelectIcon2 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(Icon4, { "data-slot": "select-icon", asChild: true, ...props }, children ?? /* @__PURE__ */ React.createElement(
    ChevronDown,
    {
      size: 20,
      className: cn(
        "text-muted-400 shrink-0 transition-colors",
        "group-data-[state=open]/select-trigger:text-primary",
        "group-disabled/select-trigger:text-muted-300",
        className
      )
    }
  ));
  var SelectScrollUpButton2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    ScrollUpButton,
    {
      "data-slot": "select-scroll-up",
      className: cn("flex cursor-default items-center justify-center py-1", className),
      ...props
    },
    /* @__PURE__ */ React.createElement(ChevronUp, null)
  );
  var SelectScrollDownButton2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    ScrollDownButton,
    {
      "data-slot": "select-scroll-down",
      className: cn("flex cursor-default items-center justify-center py-1", className),
      ...props
    },
    /* @__PURE__ */ React.createElement(ChevronDown, null)
  );
  var SelectContent2 = ({ className, children, position = "popper", ...props }) => /* @__PURE__ */ React.createElement(Portal5, null, /* @__PURE__ */ React.createElement(
    Content27,
    {
      "data-slot": "select-content",
      className: cn(
        "bg-popover text-popover-foreground border-muted-200 relative z-50 max-h-96 overflow-hidden rounded-md border shadow-xs",
        "data-[side=bottom]:slide-in-from-top-2 data-[state=open]:animate-in data-[state=closed]:animate-out",
        "data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95",
        "data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2",
        position === "popper" && "data-[side=bottom]:translate-y-1 data-[side=left]:-translate-x-1 data-[side=right]:translate-x-1 data-[side=top]:-translate-y-1",
        position === "popper" && "w-(--radix-select-trigger-width)",
        className
      ),
      position,
      ...props
    },
    /* @__PURE__ */ React.createElement(SelectScrollUpButton2, null),
    /* @__PURE__ */ React.createElement(
      Viewport3,
      {
        "data-slot": "select-viewport",
        className: cn(position === "popper" && "h-(--radix-select-trigger-height) w-full")
      },
      children
    ),
    /* @__PURE__ */ React.createElement(SelectScrollDownButton2, null)
  ));
  var SelectGroup2 = (props) => /* @__PURE__ */ React.createElement(Group3, { "data-slot": "select-group", ...props });
  var SelectLabel2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Label6, { "data-slot": "select-label", className: cn("px-2 py-1.5 text-sm font-semibold", className), ...props });
  var SelectItem2 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    Item10,
    {
      "data-slot": "select-item",
      className: cn(
        "bg-background border-muted-200 relative flex w-full cursor-pointer items-center gap-2 px-3 py-2 text-sm transition-colors outline-none select-none not-last:border-b",
        "data-[state=checked]:bg-primary-25 data-[state=checked]:text-primary-900 data-[state=checked]:border-b-muted-50",
        "data-highlighted:bg-primary-15",
        "focus:bg-muted-100 focus:text-foreground",
        "data-disabled:pointer-events-none data-disabled:opacity-50",
        className
      ),
      ...props
    },
    children
  );
  var SelectItemText2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(ItemText, { "data-slot": "select-item-text", className: cn(className), ...props });
  var SelectIndicator = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(ItemIndicator3, { "data-slot": "select-item-indicator", ...props }, children ?? /* @__PURE__ */ React.createElement(Check, { size: 16, className: cn(className) }));
  var SelectSeparator2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Separator4, { "data-slot": "select-item-separator", className: cn("bg-muted-200 h-px w-full", className), ...props });
  var Select3 = Root28;
  Select3.Trigger = (0, import_react100.forwardRef)((props, ref) => SelectTrigger2({ ...props, ref }));
  Select3.Value = SelectValue2;
  Select3.Icon = SelectIcon2;
  Select3.Content = SelectContent2;
  Select3.Group = SelectGroup2;
  Select3.Label = SelectLabel2;
  Select3.Item = SelectItem2;
  Select3.Text = SelectItemText2;
  Select3.Indicator = SelectIndicator;
  Select3.Separator = SelectSeparator2;
  Select3.ScrollUpButton = SelectScrollUpButton2;
  Select3.ScrollDownButton = SelectScrollDownButton2;
  Select3.Trigger.displayName = Trigger14.displayName;
  SelectValue2.displayName = Value.displayName;
  SelectIcon2.displayName = "SelectIcon";
  SelectContent2.displayName = Content27.displayName;
  SelectGroup2.displayName = Group3.displayName;
  SelectLabel2.displayName = Label6.displayName;
  SelectItem2.displayName = Item10.displayName;
  SelectItemText2.displayName = ItemText.displayName;
  SelectIndicator.displayName = "SelectIndicator";
  SelectSeparator2.displayName = Separator4.displayName;
  SelectScrollUpButton2.displayName = ScrollUpButton.displayName;
  SelectScrollDownButton2.displayName = ScrollDownButton.displayName;
  var Select_default = Select3;

  // src/components/ui/skeleton/index.ts
  init_define_import_meta_env();

  // src/components/ui/skeleton/Skeleton.tsx
  init_define_import_meta_env();
  var skeletonVariants = cva("animate-pulse bg-muted overflow-hidden", {
    variants: {
      variant: {
        default: "bg-muted-200",
        card: "bg-card",
        primary: "bg-primary/10"
      },
      size: {
        default: "[--skw:35px] [--skh2:35px] [--line:10px]",
        xs: "[--skw:20px] [--skh2:20px] [--line:5px]",
        sm: "[--skw:35px] [--skh2:35px] [--line:10px]",
        md: "[--skw:70px] [--skh2:70px] [--line:20px]",
        lg: "[--skw:140px] [--skh2:140px] [--line:40px]",
        xl: "[--skw:180px] [--skh2:180px] [--line:80px]"
      },
      shape: {
        text: "w-full h-(--line) rounded-md",
        avatar: "w-(--skw) h-(--skh2) rounded-full",
        banner: "w-full h-[calc(var(--skh2)*2)] rounded-md",
        thumbnail: "w-[calc(var(--skw)*1.6)] h-[calc(var(--skh2)*1.6)] rounded-md",
        square: "w-(--skw) h-(--skh2) rounded-md",
        rectangle: "w-[calc(var(--skw)*1.14)] h-[calc(var(--skh2)*0.57)] rounded-md",
        squareSharp: "w-[calc(var(--skw)*1.6)] h-[calc(var(--skh2)*1.6)] rounded-none",
        rectangleSharp: "w-[calc(var(--skw)*1.14)] h-[calc(var(--skh2)*0.57)] rounded-none"
      }
    },
    defaultVariants: {
      variant: "default",
      size: "default",
      shape: "text"
    }
  });
  var Skeleton = ({ className, variant, size: size4, shape, ...props }) => {
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "skeleton", className: cn(skeletonVariants({ variant, size: size4, shape }), className), ...props });
  };
  var Skeleton_default = Skeleton;

  // src/components/ui/switch/index.ts
  init_define_import_meta_env();

  // src/components/ui/switch/Switch.tsx
  init_define_import_meta_env();

  // node_modules/@radix-ui/react-switch/dist/index.mjs
  init_define_import_meta_env();
  var React99 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime42 = __toESM(require_react_shim(), 1);
  var SWITCH_NAME = "Switch";
  var [createSwitchContext, createSwitchScope] = createContextScope(SWITCH_NAME);
  var [SwitchProvider, useSwitchContext] = createSwitchContext(SWITCH_NAME);
  var Switch = React99.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeSwitch,
        name,
        checked: checkedProp,
        defaultChecked,
        required,
        disabled,
        value = "on",
        onCheckedChange,
        form,
        ...switchProps
      } = props;
      const [button, setButton] = React99.useState(null);
      const composedRefs = useComposedRefs(forwardedRef, (node) => setButton(node));
      const hasConsumerStoppedPropagationRef = React99.useRef(false);
      const isFormControl = button ? form || !!button.closest("form") : true;
      const [checked, setChecked] = useControllableState({
        prop: checkedProp,
        defaultProp: defaultChecked ?? false,
        onChange: onCheckedChange,
        caller: SWITCH_NAME
      });
      return /* @__PURE__ */ (0, import_jsx_runtime42.jsxs)(SwitchProvider, { scope: __scopeSwitch, checked, disabled, children: [
        /* @__PURE__ */ (0, import_jsx_runtime42.jsx)(
          Primitive2.button,
          {
            type: "button",
            role: "switch",
            "aria-checked": checked,
            "aria-required": required,
            "data-state": getState6(checked),
            "data-disabled": disabled ? "" : void 0,
            disabled,
            value,
            ...switchProps,
            ref: composedRefs,
            onClick: composeEventHandlers(props.onClick, (event) => {
              setChecked((prevChecked) => !prevChecked);
              if (isFormControl) {
                hasConsumerStoppedPropagationRef.current = event.isPropagationStopped();
                if (!hasConsumerStoppedPropagationRef.current) event.stopPropagation();
              }
            })
          }
        ),
        isFormControl && /* @__PURE__ */ (0, import_jsx_runtime42.jsx)(
          SwitchBubbleInput,
          {
            control: button,
            bubbles: !hasConsumerStoppedPropagationRef.current,
            name,
            value,
            checked,
            required,
            disabled,
            form,
            style: { transform: "translateX(-100%)" }
          }
        )
      ] });
    }
  );
  Switch.displayName = SWITCH_NAME;
  var THUMB_NAME2 = "SwitchThumb";
  var SwitchThumb = React99.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSwitch, ...thumbProps } = props;
      const context2 = useSwitchContext(THUMB_NAME2, __scopeSwitch);
      return /* @__PURE__ */ (0, import_jsx_runtime42.jsx)(
        Primitive2.span,
        {
          "data-state": getState6(context2.checked),
          "data-disabled": context2.disabled ? "" : void 0,
          ...thumbProps,
          ref: forwardedRef
        }
      );
    }
  );
  SwitchThumb.displayName = THUMB_NAME2;
  var BUBBLE_INPUT_NAME3 = "SwitchBubbleInput";
  var SwitchBubbleInput = React99.forwardRef(
    ({
      __scopeSwitch,
      control,
      checked,
      bubbles = true,
      ...props
    }, forwardedRef) => {
      const ref = React99.useRef(null);
      const composedRefs = useComposedRefs(ref, forwardedRef);
      const prevChecked = usePrevious2(checked);
      const controlSize = useSize(control);
      React99.useEffect(() => {
        const input = ref.current;
        if (!input) return;
        const inputProto = window.HTMLInputElement.prototype;
        const descriptor = Object.getOwnPropertyDescriptor(
          inputProto,
          "checked"
        );
        const setChecked = descriptor.set;
        if (prevChecked !== checked && setChecked) {
          const event = new Event("click", { bubbles });
          setChecked.call(input, checked);
          input.dispatchEvent(event);
        }
      }, [prevChecked, checked, bubbles]);
      return /* @__PURE__ */ (0, import_jsx_runtime42.jsx)(
        "input",
        {
          type: "checkbox",
          "aria-hidden": true,
          defaultChecked: checked,
          ...props,
          tabIndex: -1,
          ref: composedRefs,
          style: {
            ...props.style,
            ...controlSize,
            position: "absolute",
            pointerEvents: "none",
            opacity: 0,
            margin: 0
          }
        }
      );
    }
  );
  SwitchBubbleInput.displayName = BUBBLE_INPUT_NAME3;
  function getState6(checked) {
    return checked ? "checked" : "unchecked";
  }
  var Root12 = Switch;
  var Thumb = SwitchThumb;

  // src/components/ui/switch/Switch.tsx
  var switchRootVariants = cva(
    `peer inline-flex shrink-0 cursor-pointer items-center rounded-full border-2 border-transparent shadow-xs
  transition-colors focus-visible:outline-hidden focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2
  focus-visible:ring-offset-background disabled:cursor-not-allowed disabled:opacity-50 data-[state=checked]:bg-primary data-[state=unchecked]:bg-muted`,
    {
      variants: {
        size: {
          sm: "h-4 w-7",
          md: "h-5 w-9",
          lg: "h-6 w-11"
        },
        variant: {
          default: "data-[state=checked]:bg-primary",
          success: "data-[state=checked]:bg-success",
          destructive: "data-[state=checked]:bg-destructive",
          secondary: "data-[state=checked]:bg-secondary-600"
        }
      },
      defaultVariants: {
        size: "md",
        variant: "default"
      }
    }
  );
  var switchThumbVariants = cva("pointer-events-none block rounded-full bg-background shadow-lg ring-0 transition-transform", {
    variants: {
      size: {
        sm: "h-3 w-3 data-[state=checked]:translate-x-3 data-[state=unchecked]:translate-x-0",
        md: "h-4 w-4 data-[state=checked]:translate-x-4 data-[state=unchecked]:translate-x-0",
        lg: "h-5 w-5 data-[state=checked]:translate-x-5 data-[state=unchecked]:translate-x-0"
      }
    },
    defaultVariants: {
      size: "md"
    }
  });
  var Switch2 = ({ className, size: size4, variant, ...props }) => {
    return /* @__PURE__ */ React.createElement(Root12, { "data-slot": "switch", className: cn(switchRootVariants({ size: size4, variant, className })), ...props }, /* @__PURE__ */ React.createElement(Thumb, { "data-slot": "switch-thumb", className: cn(switchThumbVariants({ size: size4 })) }));
  };
  var Switch_default = Switch2;

  // src/components/ui/toggle-group/index.ts
  init_define_import_meta_env();

  // src/components/ui/toggle-group/ToggleGroup.tsx
  init_define_import_meta_env();
  var import_react102 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-toggle-group/dist/index.mjs
  init_define_import_meta_env();
  var import_react101 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-toggle/dist/index.mjs
  init_define_import_meta_env();
  var React100 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime43 = __toESM(require_react_shim(), 1);
  var NAME4 = "Toggle";
  var Toggle = React100.forwardRef((props, forwardedRef) => {
    const { pressed: pressedProp, defaultPressed, onPressedChange, ...buttonProps } = props;
    const [pressed, setPressed] = useControllableState({
      prop: pressedProp,
      onChange: onPressedChange,
      defaultProp: defaultPressed ?? false,
      caller: NAME4
    });
    return /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(
      Primitive2.button,
      {
        type: "button",
        "aria-pressed": pressed,
        "data-state": pressed ? "on" : "off",
        "data-disabled": props.disabled ? "" : void 0,
        ...buttonProps,
        ref: forwardedRef,
        onClick: composeEventHandlers(props.onClick, () => {
          if (!props.disabled) {
            setPressed(!pressed);
          }
        })
      }
    );
  });
  Toggle.displayName = NAME4;

  // node_modules/@radix-ui/react-toggle-group/dist/index.mjs
  var import_jsx_runtime44 = __toESM(require_react_shim(), 1);
  var TOGGLE_GROUP_NAME = "ToggleGroup";
  var [createToggleGroupContext, createToggleGroupScope] = createContextScope(TOGGLE_GROUP_NAME, [
    createRovingFocusGroupScope
  ]);
  var useRovingFocusGroupScope3 = createRovingFocusGroupScope();
  var ToggleGroup = import_react101.default.forwardRef((props, forwardedRef) => {
    const { type, ...toggleGroupProps } = props;
    if (type === "single") {
      const singleProps = toggleGroupProps;
      return /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(ToggleGroupImplSingle, { ...singleProps, ref: forwardedRef });
    }
    if (type === "multiple") {
      const multipleProps = toggleGroupProps;
      return /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(ToggleGroupImplMultiple, { ...multipleProps, ref: forwardedRef });
    }
    throw new Error(`Missing prop \`type\` expected on \`${TOGGLE_GROUP_NAME}\``);
  });
  ToggleGroup.displayName = TOGGLE_GROUP_NAME;
  var [ToggleGroupValueProvider, useToggleGroupValueContext] = createToggleGroupContext(TOGGLE_GROUP_NAME);
  var ToggleGroupImplSingle = import_react101.default.forwardRef((props, forwardedRef) => {
    const {
      value: valueProp,
      defaultValue,
      onValueChange = () => {
      },
      ...toggleGroupSingleProps
    } = props;
    const [value, setValue] = useControllableState({
      prop: valueProp,
      defaultProp: defaultValue ?? "",
      onChange: onValueChange,
      caller: TOGGLE_GROUP_NAME
    });
    return /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(
      ToggleGroupValueProvider,
      {
        scope: props.__scopeToggleGroup,
        type: "single",
        value: import_react101.default.useMemo(() => value ? [value] : [], [value]),
        onItemActivate: setValue,
        onItemDeactivate: import_react101.default.useCallback(() => setValue(""), [setValue]),
        children: /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(ToggleGroupImpl, { ...toggleGroupSingleProps, ref: forwardedRef })
      }
    );
  });
  var ToggleGroupImplMultiple = import_react101.default.forwardRef((props, forwardedRef) => {
    const {
      value: valueProp,
      defaultValue,
      onValueChange = () => {
      },
      ...toggleGroupMultipleProps
    } = props;
    const [value, setValue] = useControllableState({
      prop: valueProp,
      defaultProp: defaultValue ?? [],
      onChange: onValueChange,
      caller: TOGGLE_GROUP_NAME
    });
    const handleButtonActivate = import_react101.default.useCallback(
      (itemValue) => setValue((prevValue = []) => [...prevValue, itemValue]),
      [setValue]
    );
    const handleButtonDeactivate = import_react101.default.useCallback(
      (itemValue) => setValue((prevValue = []) => prevValue.filter((value2) => value2 !== itemValue)),
      [setValue]
    );
    return /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(
      ToggleGroupValueProvider,
      {
        scope: props.__scopeToggleGroup,
        type: "multiple",
        value,
        onItemActivate: handleButtonActivate,
        onItemDeactivate: handleButtonDeactivate,
        children: /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(ToggleGroupImpl, { ...toggleGroupMultipleProps, ref: forwardedRef })
      }
    );
  });
  ToggleGroup.displayName = TOGGLE_GROUP_NAME;
  var [ToggleGroupContext, useToggleGroupContext] = createToggleGroupContext(TOGGLE_GROUP_NAME);
  var ToggleGroupImpl = import_react101.default.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeToggleGroup,
        disabled = false,
        rovingFocus = true,
        orientation,
        dir: dir2,
        loop = true,
        ...toggleGroupProps
      } = props;
      const rovingFocusGroupScope = useRovingFocusGroupScope3(__scopeToggleGroup);
      const direction = useDirection(dir2);
      const commonProps = { role: "group", dir: direction, ...toggleGroupProps };
      return /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(ToggleGroupContext, { scope: __scopeToggleGroup, rovingFocus, disabled, children: rovingFocus ? /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(
        Root6,
        {
          asChild: true,
          ...rovingFocusGroupScope,
          orientation,
          dir: direction,
          loop,
          children: /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(Primitive2.div, { ...commonProps, ref: forwardedRef })
        }
      ) : /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(Primitive2.div, { ...commonProps, ref: forwardedRef }) });
    }
  );
  var ITEM_NAME6 = "ToggleGroupItem";
  var ToggleGroupItem = import_react101.default.forwardRef(
    (props, forwardedRef) => {
      const valueContext = useToggleGroupValueContext(ITEM_NAME6, props.__scopeToggleGroup);
      const context2 = useToggleGroupContext(ITEM_NAME6, props.__scopeToggleGroup);
      const rovingFocusGroupScope = useRovingFocusGroupScope3(props.__scopeToggleGroup);
      const pressed = valueContext.value.includes(props.value);
      const disabled = context2.disabled || props.disabled;
      const commonProps = { ...props, pressed, disabled };
      const ref = import_react101.default.useRef(null);
      return context2.rovingFocus ? /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(
        Item5,
        {
          asChild: true,
          ...rovingFocusGroupScope,
          focusable: !disabled,
          active: pressed,
          ref,
          children: /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(ToggleGroupItemImpl, { ...commonProps, ref: forwardedRef })
        }
      ) : /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(ToggleGroupItemImpl, { ...commonProps, ref: forwardedRef });
    }
  );
  ToggleGroupItem.displayName = ITEM_NAME6;
  var ToggleGroupItemImpl = import_react101.default.forwardRef(
    (props, forwardedRef) => {
      const { __scopeToggleGroup, value, ...itemProps } = props;
      const valueContext = useToggleGroupValueContext(ITEM_NAME6, __scopeToggleGroup);
      const singleProps = { role: "radio", "aria-checked": props.pressed, "aria-pressed": void 0 };
      const typeProps = valueContext.type === "single" ? singleProps : void 0;
      return /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(
        Toggle,
        {
          ...typeProps,
          ...itemProps,
          ref: forwardedRef,
          onPressedChange: (pressed) => {
            if (pressed) {
              valueContext.onItemActivate(value);
            } else {
              valueContext.onItemDeactivate(value);
            }
          }
        }
      );
    }
  );
  var Root29 = ToggleGroup;
  var Item24 = ToggleGroupItem;

  // src/components/ui/toggle-group/ToggleGroup.tsx
  var toggleVariants = cva(
    `
  inline-flex items-center justify-center gap-2 rounded-md text-sm font-medium ring-offset-background transition-colors text-primary cursor-pointer
  hover:bg-primary-50 focus-visible:outline-hidden focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2
  disabled:pointer-events-none disabled:opacity-50 data-[state=on]:bg-primary data-[state=on]:text-primary-foreground dark:data-[state=on]:bg-primary-300
  [&>svg]:text-primary data-[state=on]:[&>svg]:text-primary-foreground
  `,
    {
      variants: {
        variant: {
          default: "bg-primary-25",
          outline: "border border-accent text-accent-foreground bg-transparent shadow-xs hover:bg-accent"
        },
        size: {
          default: "h-10 px-3",
          sm: "h-9 px-2.5",
          lg: "h-11 px-5"
        }
      },
      defaultVariants: {
        variant: "default",
        size: "default"
      }
    }
  );
  var ToggleGroupContext2 = (0, import_react102.createContext)({
    size: "default",
    variant: "default"
  });
  var Item11 = ({ className, children, variant, size: size4, ...props }) => {
    const context2 = (0, import_react102.useContext)(ToggleGroupContext2);
    return /* @__PURE__ */ React.createElement(
      Item24,
      {
        "data-slot": "toggle-group-item",
        className: cn(toggleVariants({ variant: context2.variant || variant, size: context2.size || size4 }), className),
        ...props
      },
      children
    );
  };
  var ToggleGroup2 = ({ className, variant, size: size4, children, ...props }) => /* @__PURE__ */ React.createElement(Root29, { "data-slot": "toggle-group", className: cn("flex items-center justify-center gap-1", className), ...props }, /* @__PURE__ */ React.createElement(ToggleGroupContext2.Provider, { value: { variant, size: size4 } }, children));
  ToggleGroup2.Item = Item11;
  var ToggleGroup_default = ToggleGroup2;

  // src/components/ui/time-picker/index.ts
  init_define_import_meta_env();

  // src/components/ui/time-picker/TimePicker.tsx
  init_define_import_meta_env();
  var import_react103 = __toESM(require_react_shim(), 1);
  var TimePickerClear = ({ onClear, disabled, hasValue }) => {
    if (!hasValue || !onClear || disabled) return null;
    const handleClear = (e2) => {
      e2.preventDefault();
      e2.stopPropagation();
      onClear();
    };
    return /* @__PURE__ */ React.createElement(TooltipButton_default, { asChild: true, title: "Clear", variant: "ghost-muted-destructive", size: "unstyled", onClick: handleClear }, /* @__PURE__ */ React.createElement(X, { size: 16 }));
  };
  var TimePickerTrigger = (0, import_react103.forwardRef)(
    ({ className, error: error2, placeholder = "Select time", value, displayValue, disabled, children, ...props }, ref) => {
      const childrenArray = import_react103.Children.toArray(children);
      const clearComponent = childrenArray.find((child) => (0, import_react103.isValidElement)(child) && child.type === TimePickerClear);
      const otherChildren = childrenArray.filter((child) => !((0, import_react103.isValidElement)(child) && child.type === TimePickerClear));
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          ref,
          type: "button",
          disabled,
          "data-slot": "time-picker-trigger",
          className: cn(
            "group bg-background relative flex h-12 w-full cursor-pointer items-center justify-between rounded-md border px-3 py-3 text-xs shadow-xs",
            "disabled:pointer-events-none disabled:cursor-not-allowed disabled:opacity-50",
            "transition-colors focus-visible:outline-none",
            {
              "border-muted-200 hover:border-muted-300 focus-visible:border-primary focus-visible:ring-primary focus-visible:ring-1": !error2,
              "border-destructive focus-visible:border-destructive focus-visible:ring-destructive focus-visible:ring-1": !!error2
            },
            className
          ),
          "aria-invalid": error2,
          ...props
        },
        /* @__PURE__ */ React.createElement("div", { className: "flex min-w-0 flex-1 items-center gap-2" }, /* @__PURE__ */ React.createElement(
          Clock4,
          {
            size: 20,
            className: cn("h-5 w-5 shrink-0 transition-colors", {
              "text-destructive": !!error2,
              "text-muted-300 opacity-50": disabled,
              "text-muted-400": !error2 && !disabled,
              "group-data-[state=open]:text-primary": !error2 && !disabled
            })
          }
        ), /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!displayValue }, /* @__PURE__ */ React.createElement("span", { className: "text-primary-900 line-clamp-1 min-w-[100px] text-start" }, displayValue)), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement("span", { className: "text-muted-400 line-clamp-1 min-w-[100px] text-start" }, placeholder))), otherChildren),
        /* @__PURE__ */ React.createElement("div", { className: "flex shrink-0 items-center gap-2" }, clearComponent)
      );
    }
  );
  var TimePickerContent = ({
    className,
    value,
    tempValue,
    onTempValueChange,
    onConfirm,
    use24hFormat = false,
    min: min3 = "00:00",
    max: max3 = "23:59",
    step = 1,
    ...props
  }) => {
    const { getTimeOptions, parseTimeString } = useTime();
    const [minHour] = (0, import_react103.useMemo)(() => parseTimeString(min3), [min3, parseTimeString]);
    const [maxHour] = (0, import_react103.useMemo)(() => parseTimeString(max3), [max3, parseTimeString]);
    const timeOptions = (0, import_react103.useMemo)(() => getTimeOptions(min3, max3, step, !use24hFormat), [min3, max3, step, use24hFormat, getTimeOptions]);
    const [valHour, valMinute, valAmPm] = (0, import_react103.useMemo)(() => parseTimeString(tempValue), [tempValue, parseTimeString]);
    const hourOptions = (0, import_react103.useMemo)(
      () => timeOptions.filter((option) => option.toLowerCase().includes((valAmPm ?? "am").toLowerCase())).reduce(
        (prev, time) => {
          const hour = time.split(":")[0];
          if (prev.find((option) => option.value === parseInt(hour))) return prev;
          return [
            ...prev,
            {
              label: hour,
              value: parseInt(hour),
              hide: false
            }
          ];
        },
        []
      ),
      [timeOptions, valAmPm]
    );
    const minuteOptions = (0, import_react103.useMemo)(
      () => timeOptions.filter(
        (option) => option.toLowerCase().includes((valAmPm ?? "am").toLowerCase()) && option.includes(`${(valHour - (valAmPm === "PM" && valHour > 12 ? 12 : 0)).toString().padStart(2, "0")}:`)
      ).reduce(
        (prev, time) => prev.find((option) => option.value === parseInt(time.split(":")[1])) ? prev : [
          ...prev,
          {
            label: time.split(":")[1].split(" ")[0],
            value: parseInt(time.split(":")[1].split(" ")[0]),
            hide: false
          }
        ],
        []
      ),
      [timeOptions, valHour, valAmPm]
    );
    const ampmOptions = (0, import_react103.useMemo)(
      () => [
        { label: "AM", value: "AM", hide: minHour >= 12 },
        { label: "PM", value: "PM", hide: maxHour < 12 }
      ],
      [minHour, maxHour]
    );
    return /* @__PURE__ */ React.createElement(
      Popover_default.Content,
      {
        "data-slot": "time-picker-content",
        className: cn(
          "bg-popover text-popover-foreground border-muted-200 relative z-50 w-fit overflow-hidden rounded-md border px-2 shadow-xs",
          "data-[side=bottom]:slide-in-from-top-2 data-[state=open]:animate-in data-[state=closed]:animate-out",
          "data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95",
          "data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2",
          className
        ),
        side: "bottom",
        align: "start",
        ...props
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex gap-2 py-3" }, /* @__PURE__ */ React.createElement(
        ScrollSelector,
        {
          options: hourOptions,
          value: parseInt(tempValue.split(":")[0]),
          onSelect: (val) => onTempValueChange(
            `${val.toString().padStart(2, "0")}:${valMinute.toString().padStart(2, "0")}${use24hFormat ? "" : " " + (valAmPm ?? "AM")}`
          )
        }
      ), /* @__PURE__ */ React.createElement(
        ScrollSelector,
        {
          options: minuteOptions,
          value: valMinute,
          onSelect: (val) => onTempValueChange(`${tempValue.split(":")[0]}:${val.toString().padStart(2, "0")}${use24hFormat ? "" : " " + (valAmPm ?? "AM")}`)
        }
      ), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !use24hFormat }, /* @__PURE__ */ React.createElement(
        ScrollSelector,
        {
          options: ampmOptions,
          value: valAmPm,
          onSelect: (val) => onTempValueChange(`${tempValue.split(":")[0]}:${valMinute.toString().padStart(2, "0")} ${val}`)
        }
      ))),
      /* @__PURE__ */ React.createElement("div", { className: "mt-4 flex justify-end pb-2" }, /* @__PURE__ */ React.createElement(Button_default, { size: "sm", variant: "ghost", className: "h-auto px-2 py-1", onClick: onConfirm }, /* @__PURE__ */ React.createElement(Check, { size: 20, className: "text-primary" })))
    );
  };
  var TimePickerRoot = ({ children, ...props }) => {
    return /* @__PURE__ */ React.createElement(Popover_default, { "data-slot": "time-picker", modal: true, ...props }, children);
  };
  var TimePicker = ({
    name,
    className,
    disabled,
    placeholder = "Select time",
    clearButton = true,
    min: min3 = "00:00",
    startAt = "08:00 AM",
    max: max3 = "23:59",
    step = 1,
    use24hFormat = false,
    value,
    onChange,
    error: error2 = false
  }) => {
    const [isOpen, setIsOpen] = (0, import_react103.useState)(false);
    const [tempValue, setTempValue] = (0, import_react103.useState)("");
    const display = useDebounce(tempValue || value, 200);
    (0, import_react103.useEffect)(() => {
      if (!isOpen) {
        setTempValue("");
      } else {
        setTempValue(value || startAt || min3);
      }
    }, [isOpen, value, startAt, min3]);
    const handleConfirm = () => {
      onChange?.(tempValue);
      setIsOpen(false);
    };
    const handleClear = () => {
      onChange?.(void 0);
    };
    return /* @__PURE__ */ React.createElement(Popover_default, { onOpenChange: setIsOpen, open: isOpen, modal: true }, /* @__PURE__ */ React.createElement(Popover_default.Trigger, { id: name, asChild: true }, /* @__PURE__ */ React.createElement(
      TimePicker.Trigger,
      {
        className,
        disabled,
        error: error2,
        placeholder,
        value,
        displayValue: display
      },
      clearButton && /* @__PURE__ */ React.createElement(TimePicker.Clear, { onClear: handleClear, disabled, hasValue: !!value })
    )), /* @__PURE__ */ React.createElement(
      TimePicker.Content,
      {
        value,
        tempValue,
        onTempValueChange: setTempValue,
        onConfirm: handleConfirm,
        use24hFormat,
        min: min3,
        max: max3,
        step
      }
    ));
  };
  TimePickerContent.displayName = "TimePickerContent";
  TimePickerTrigger.displayName = "TimePickerTrigger";
  TimePickerClear.displayName = "TimePickerClear";
  TimePickerRoot.displayName = "TimePickerRoot";
  TimePicker.displayName = "TimePicker";
  TimePicker.Root = TimePickerRoot;
  TimePicker.Trigger = TimePickerTrigger;
  TimePicker.Clear = TimePickerClear;
  TimePicker.Content = TimePickerContent;
  var TimePicker_default = TimePicker;

  // src/components/ui/scroll-selector/index.ts
  init_define_import_meta_env();

  // src/components/ui/scroll-selector/ScrollSelector.tsx
  init_define_import_meta_env();
  var import_react104 = __toESM(require_react_shim(), 1);
  function ScrollSelector({ options: options3, value, maxHeight = 140, onSelect }) {
    const enabledOptions = (0, import_react104.useMemo)(() => options3.filter((o) => !o.hide), [options3]);
    const [onIdx, setOnIdx] = (0, import_react104.useState)(enabledOptions.map((o) => o.value).indexOf(value) ?? 0);
    const [elHeight, setElHeight] = (0, import_react104.useState)(0);
    const container = (0, import_react104.useRef)(null);
    const elRef = (0, import_react104.useRef)(null);
    const scrollTimeoutRef = (0, import_react104.useRef)(null);
    (0, import_react104.useEffect)(() => {
      if (elRef.current) setElHeight(elRef.current.clientHeight);
    }, []);
    (0, import_react104.useEffect)(() => {
      if (enabledOptions[onIdx]?.value !== void 0) onSelect?.(enabledOptions[onIdx].value);
    }, [onIdx]);
    (0, import_react104.useEffect)(() => {
      if (elHeight && container.current) container.current.scrollTo({ top: onIdx * elHeight, behavior: "smooth" });
    }, [elHeight]);
    (0, import_react104.useEffect)(() => {
      const valIdx = enabledOptions.map((o) => o.value).indexOf(value);
      if (valIdx !== -1) manualSelect(valIdx);
      else {
        manualSelect(0);
        if (onIdx === 0 && enabledOptions.length > 0) {
          setOnIdx(0);
          onSelect?.(enabledOptions[0].value);
        }
      }
    }, [enabledOptions.length]);
    (0, import_react104.useEffect)(() => {
      return () => {
        if (scrollTimeoutRef.current) {
          clearTimeout(scrollTimeoutRef.current);
        }
      };
    }, []);
    const onScroll = (ev) => {
      if (!elHeight) return;
      if (scrollTimeoutRef.current) {
        clearTimeout(scrollTimeoutRef.current);
      }
      scrollTimeoutRef.current = setTimeout(() => {
        const scroll = ev.target.scrollTop;
        setOnIdx(Math.round(scroll / elHeight));
      }, 100);
    };
    const manualSelect = (idx, behavior = "smooth") => {
      if (!elHeight || !container.current) return;
      if (enabledOptions.length === 1) setOnIdx(0);
      container.current.scrollTo({ top: idx * elHeight, behavior });
    };
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "no-scrollbar flex snap-y snap-mandatory flex-col overflow-y-scroll",
        onScroll,
        ref: container,
        style: { maxHeight }
      },
      /* @__PURE__ */ React.createElement("div", { className: "shrink-0", style: { height: maxHeight / 2 - 1.5 * elHeight + "px" } }),
      /* @__PURE__ */ React.createElement("div", { className: "pointer-events-none snap-center p-1 opacity-0", ref: elRef, children: "_" }),
      enabledOptions.map((option, i) => /* @__PURE__ */ React.createElement(
        "div",
        {
          key: option.value,
          className: cn("cursor-pointer snap-center p-1 transition-all", {
            "scale-y-85 opacity-50": Math.abs(i - onIdx) === 1,
            "scale-y-70 opacity-20": Math.abs(i - onIdx) > 1
          }),
          onClick: () => manualSelect(i)
        },
        option.label
      )),
      /* @__PURE__ */ React.createElement("div", { className: "shrink-0", style: { height: maxHeight / 2 - 0.5 * elHeight + "px" } })
    );
  }

  // src/components/ui/radio-dot/index.ts
  init_define_import_meta_env();

  // src/components/ui/radio-dot/RadioDot.tsx
  init_define_import_meta_env();
  var RadioDot = ({ checked, className }) => /* @__PURE__ */ React.createElement(
    "span",
    {
      "data-slot": "radio-dot",
      className: cn(
        "border-muted-200 bg-background flex h-5 w-5 shrink-0 items-center justify-center rounded-full border transition-[color,box-shadow]",
        checked && "border-primary bg-primary text-primary-foreground",
        className
      )
    },
    /* @__PURE__ */ React.createElement(Check, { size: 14, className: cn("transition-opacity", checked ? "opacity-100" : "opacity-0") })
  );
  RadioDot.displayName = "RadioDot";
  var RadioDot_default = RadioDot;

  // src/components/ui/radio-group/index.ts
  init_define_import_meta_env();

  // src/components/ui/radio-group/RadioGroup.tsx
  init_define_import_meta_env();
  var import_react105 = __toESM(require_react_shim(), 1);

  // node_modules/@radix-ui/react-radio-group/dist/index.mjs
  init_define_import_meta_env();
  var React211 = __toESM(require_react_shim(), 1);
  var React102 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime45 = __toESM(require_react_shim(), 1);
  var import_jsx_runtime46 = __toESM(require_react_shim(), 1);
  var RADIO_NAME = "Radio";
  var [createRadioContext, createRadioScope] = createContextScope(RADIO_NAME);
  var [RadioProvider, useRadioContext] = createRadioContext(RADIO_NAME);
  var Radio = React102.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeRadio,
        name,
        checked = false,
        required,
        disabled,
        value = "on",
        onCheck,
        form,
        ...radioProps
      } = props;
      const [button, setButton] = React102.useState(null);
      const composedRefs = useComposedRefs(forwardedRef, (node) => setButton(node));
      const hasConsumerStoppedPropagationRef = React102.useRef(false);
      const isFormControl = button ? form || !!button.closest("form") : true;
      return /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(RadioProvider, { scope: __scopeRadio, checked, disabled, children: [
        /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(
          Primitive2.button,
          {
            type: "button",
            role: "radio",
            "aria-checked": checked,
            "data-state": getState7(checked),
            "data-disabled": disabled ? "" : void 0,
            disabled,
            value,
            ...radioProps,
            ref: composedRefs,
            onClick: composeEventHandlers(props.onClick, (event) => {
              if (!checked) onCheck?.();
              if (isFormControl) {
                hasConsumerStoppedPropagationRef.current = event.isPropagationStopped();
                if (!hasConsumerStoppedPropagationRef.current) event.stopPropagation();
              }
            })
          }
        ),
        isFormControl && /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(
          RadioBubbleInput,
          {
            control: button,
            bubbles: !hasConsumerStoppedPropagationRef.current,
            name,
            value,
            checked,
            required,
            disabled,
            form,
            style: { transform: "translateX(-100%)" }
          }
        )
      ] });
    }
  );
  Radio.displayName = RADIO_NAME;
  var INDICATOR_NAME4 = "RadioIndicator";
  var RadioIndicator = React102.forwardRef(
    (props, forwardedRef) => {
      const { __scopeRadio, forceMount, ...indicatorProps } = props;
      const context2 = useRadioContext(INDICATOR_NAME4, __scopeRadio);
      return /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(Presence, { present: forceMount || context2.checked, children: /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(
        Primitive2.span,
        {
          "data-state": getState7(context2.checked),
          "data-disabled": context2.disabled ? "" : void 0,
          ...indicatorProps,
          ref: forwardedRef
        }
      ) });
    }
  );
  RadioIndicator.displayName = INDICATOR_NAME4;
  var BUBBLE_INPUT_NAME4 = "RadioBubbleInput";
  var RadioBubbleInput = React102.forwardRef(
    ({
      __scopeRadio,
      control,
      checked,
      bubbles = true,
      ...props
    }, forwardedRef) => {
      const ref = React102.useRef(null);
      const composedRefs = useComposedRefs(ref, forwardedRef);
      const prevChecked = usePrevious2(checked);
      const controlSize = useSize(control);
      React102.useEffect(() => {
        const input = ref.current;
        if (!input) return;
        const inputProto = window.HTMLInputElement.prototype;
        const descriptor = Object.getOwnPropertyDescriptor(
          inputProto,
          "checked"
        );
        const setChecked = descriptor.set;
        if (prevChecked !== checked && setChecked) {
          const event = new Event("click", { bubbles });
          setChecked.call(input, checked);
          input.dispatchEvent(event);
        }
      }, [prevChecked, checked, bubbles]);
      return /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(
        Primitive2.input,
        {
          type: "radio",
          "aria-hidden": true,
          defaultChecked: checked,
          ...props,
          tabIndex: -1,
          ref: composedRefs,
          style: {
            ...props.style,
            ...controlSize,
            position: "absolute",
            pointerEvents: "none",
            opacity: 0,
            margin: 0
          }
        }
      );
    }
  );
  RadioBubbleInput.displayName = BUBBLE_INPUT_NAME4;
  function getState7(checked) {
    return checked ? "checked" : "unchecked";
  }
  var ARROW_KEYS = ["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"];
  var RADIO_GROUP_NAME3 = "RadioGroup";
  var [createRadioGroupContext, createRadioGroupScope] = createContextScope(RADIO_GROUP_NAME3, [
    createRovingFocusGroupScope,
    createRadioScope
  ]);
  var useRovingFocusGroupScope4 = createRovingFocusGroupScope();
  var useRadioScope = createRadioScope();
  var [RadioGroupProvider2, useRadioGroupContext2] = createRadioGroupContext(RADIO_GROUP_NAME3);
  var RadioGroup3 = React211.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeRadioGroup,
        name,
        defaultValue,
        value: valueProp,
        required = false,
        disabled = false,
        orientation,
        dir: dir2,
        loop = true,
        onValueChange,
        ...groupProps
      } = props;
      const rovingFocusGroupScope = useRovingFocusGroupScope4(__scopeRadioGroup);
      const direction = useDirection(dir2);
      const [value, setValue] = useControllableState({
        prop: valueProp,
        defaultProp: defaultValue ?? null,
        onChange: onValueChange,
        caller: RADIO_GROUP_NAME3
      });
      return /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(
        RadioGroupProvider2,
        {
          scope: __scopeRadioGroup,
          name,
          required,
          disabled,
          value,
          onValueChange: setValue,
          children: /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(
            Root6,
            {
              asChild: true,
              ...rovingFocusGroupScope,
              orientation,
              dir: direction,
              loop,
              children: /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(
                Primitive2.div,
                {
                  role: "radiogroup",
                  "aria-required": required,
                  "aria-orientation": orientation,
                  "data-disabled": disabled ? "" : void 0,
                  dir: direction,
                  ...groupProps,
                  ref: forwardedRef
                }
              )
            }
          )
        }
      );
    }
  );
  RadioGroup3.displayName = RADIO_GROUP_NAME3;
  var ITEM_NAME7 = "RadioGroupItem";
  var RadioGroupItem = React211.forwardRef(
    (props, forwardedRef) => {
      const { __scopeRadioGroup, disabled, ...itemProps } = props;
      const context2 = useRadioGroupContext2(ITEM_NAME7, __scopeRadioGroup);
      const isDisabled = context2.disabled || disabled;
      const rovingFocusGroupScope = useRovingFocusGroupScope4(__scopeRadioGroup);
      const radioScope = useRadioScope(__scopeRadioGroup);
      const ref = React211.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      const checked = context2.value === itemProps.value;
      const isArrowKeyPressedRef = React211.useRef(false);
      React211.useEffect(() => {
        const handleKeyDown = (event) => {
          if (ARROW_KEYS.includes(event.key)) {
            isArrowKeyPressedRef.current = true;
          }
        };
        const handleKeyUp = () => isArrowKeyPressedRef.current = false;
        document.addEventListener("keydown", handleKeyDown);
        document.addEventListener("keyup", handleKeyUp);
        return () => {
          document.removeEventListener("keydown", handleKeyDown);
          document.removeEventListener("keyup", handleKeyUp);
        };
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(
        Item5,
        {
          asChild: true,
          ...rovingFocusGroupScope,
          focusable: !isDisabled,
          active: checked,
          children: /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(
            Radio,
            {
              disabled: isDisabled,
              required: context2.required,
              checked,
              ...radioScope,
              ...itemProps,
              name: context2.name,
              ref: composedRefs,
              onCheck: () => context2.onValueChange(itemProps.value),
              onKeyDown: composeEventHandlers((event) => {
                if (event.key === "Enter") event.preventDefault();
              }),
              onFocus: composeEventHandlers(itemProps.onFocus, () => {
                if (isArrowKeyPressedRef.current) ref.current?.click();
              })
            }
          )
        }
      );
    }
  );
  RadioGroupItem.displayName = ITEM_NAME7;
  var INDICATOR_NAME22 = "RadioGroupIndicator";
  var RadioGroupIndicator = React211.forwardRef(
    (props, forwardedRef) => {
      const { __scopeRadioGroup, ...indicatorProps } = props;
      const radioScope = useRadioScope(__scopeRadioGroup);
      return /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(RadioIndicator, { ...radioScope, ...indicatorProps, ref: forwardedRef });
    }
  );
  RadioGroupIndicator.displayName = INDICATOR_NAME22;
  var Root210 = RadioGroup3;
  var Item25 = RadioGroupItem;
  var Indicator3 = RadioGroupIndicator;

  // src/components/ui/radio-group/RadioGroup.tsx
  var Item12 = ({ ref, className, children, ...props }) => /* @__PURE__ */ React.createElement(
    Item25,
    {
      ref,
      "data-slot": "radio-group-item",
      className: cn(
        "group/radio-item border-muted-200 bg-background flex cursor-pointer items-center gap-5 rounded-md border p-3 transition-[color,box-shadow]",
        "data-[state=checked]:border-primary-50 data-[state=checked]:bg-primary-15",
        "hover:border-primary hover:ring-primary focus-visible:ring-primary focus-visible:border-primary hover:ring focus-visible:ring focus-visible:outline-none",
        "data-[state=checked]:hover:border-primary data-[state=checked]:hover:ring-primary data-[state=checked]:focus-visible:border-primary data-[state=checked]:focus-visible:ring-primary",
        "disabled:bg-muted-50 disabled:text-muted-300 disabled:pointer-events-none",
        "disabled:data-[state=checked]:bg-muted-50 disabled:data-[state=checked]:border-muted-200",
        className
      ),
      ...props
    },
    children
  );
  var Indicator4 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    Indicator3,
    {
      "data-slot": "radio-group-indicator",
      className: cn(
        "border-muted-200 bg-background flex h-5 w-5 shrink-0 items-center justify-center rounded-full border -outline-offset-1 transition-[color,box-shadow] outline-none",
        "data-[state=checked]:border-primary data-[state=checked]:bg-primary data-[state=checked]:text-primary-foreground dark:data-[state=checked]:bg-primary-300 dark:data-[state=checked]:border-primary-300",
        "group-hover/radio-item:border-primary group-hover/radio-item:ring-primary group-hover/radio-item:ring",
        "data-[state=checked]:group-hover/radio-item:border-primary data-[state=checked]:group-hover/radio-item:ring-primary",
        "group-disabled/radio-item:data-[state=checked]:bg-muted-400 group-disabled/radio-item:data-[state=checked]:border-muted-400",
        className
      ),
      forceMount: true,
      ...props
    },
    children
  );
  var RadioGroup4 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(Root210, { "data-slot": "radio-group", className: cn("grid w-full gap-3", className), ...props });
  RadioGroup4.Item = (0, import_react105.forwardRef)((props, ref) => Item12({ ...props, ref }));
  RadioGroup4.Indicator = Indicator4;
  RadioGroup4.displayName = "RadioGroup";
  RadioGroup4.Item.displayName = "RadioGroupItem";
  Indicator4.displayName = "RadioGroupIndicator";
  var RadioGroup_default = RadioGroup4;

  // src/components/ui/calendar-time-grid/index.ts
  init_define_import_meta_env();

  // src/components/ui/calendar-time-grid/CalendarTimeGrid.tsx
  init_define_import_meta_env();
  var import_react106 = __toESM(require_react_shim(), 1);
  var Spacer = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "aria-hidden": "true",
      "data-slot": "calendar-time-grid-spacer",
      className: cn("bg-muted-50 border-muted-200 w-16 shrink-0 border-y border-e", className),
      ...props
    }
  );
  var Header8 = ({ dayName, dayNumber, isToday, className, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "calendar-time-grid-header",
      className: cn(
        "border-muted-200 bg-muted-50 flex flex-1 items-center justify-center gap-1.5 border-y border-e px-2 py-2 last:border-e-0",
        className
      ),
      ...props
    },
    /* @__PURE__ */ React.createElement("span", { className: "text-muted-foreground text-sm font-semibold" }, dayName),
    /* @__PURE__ */ React.createElement(
      "span",
      {
        className: cn(
          "flex size-7 items-center justify-center rounded-full text-sm",
          isToday ? "bg-primary-400 text-primary-foreground font-bold" : "text-foreground"
        )
      },
      dayNumber
    )
  );
  var Axis = ({ rows, showStepLabels = false, className, style, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "calendar-time-grid-axis",
      className: cn("border-muted-200 bg-muted-50 grid w-16 shrink-0 border-e", className),
      style: { gridTemplateRows: `repeat(${rows.length}, minmax(50px, 1fr))`, minHeight: `${rows.length * 50}px`, ...style },
      ...props
    },
    rows.map((row, idx) => /* @__PURE__ */ React.createElement("div", { key: idx, className: "border-muted-200 flex items-start justify-end pe-2 pt-0.5 not-last:border-b" }, (showStepLabels || row.isHourMark) && /* @__PURE__ */ React.createElement("span", { className: "text-foreground text-2xs" }, row.label)))
  );
  var Cell2 = ({ available, className, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "aria-hidden": "true",
      "data-slot": "calendar-time-grid-cell",
      className: cn("border-muted-200 border-b last:border-b-0", available ? "bg-background" : "bg-muted-100", className),
      ...props
    }
  );
  var EventCard = ({ ref, top, height, left, width, className, type = "button", ...props }) => /* @__PURE__ */ React.createElement(
    "button",
    {
      ref,
      type,
      "data-slot": "calendar-time-grid-event-card",
      className: cn(
        "ring-offset-background absolute z-20 flex cursor-pointer overflow-hidden",
        "focus-visible:ring-ring focus-visible:ring-2 focus-visible:outline-none",
        className
      ),
      style: { top: `${top}%`, height: `${height}%`, left: `${left}%`, width: `${width}%` },
      ...props
    }
  );
  var NowLine = ({ percent, mode = "in-range", className, ...props }) => {
    if (mode === "off") return null;
    if (mode === "in-range" && (percent < 0 || percent > 100)) return null;
    const clamped = Math.max(0, Math.min(100, percent));
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        "aria-hidden": "true",
        "data-slot": "calendar-time-grid-now-line",
        className: cn("bg-destructive pointer-events-none absolute inset-x-0 z-30 h-px", className),
        style: { top: `${clamped}%` },
        ...props
      },
      /* @__PURE__ */ React.createElement("span", { className: "bg-destructive absolute -inset-s-1 -inset-bs-1 size-2 rounded-full" })
    );
  };
  var Column = ({ rowCount, className, style, children, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "calendar-time-grid-column",
      className: cn("relative grid flex-1", className),
      style: { gridTemplateRows: `repeat(${rowCount}, minmax(50px, 1fr))`, minHeight: `${rowCount * 50}px`, ...style },
      ...props
    },
    children
  );
  var CalendarTimeGrid = ({ children }) => /* @__PURE__ */ React.createElement(React.Fragment, null, children);
  CalendarTimeGrid.Spacer = Spacer;
  CalendarTimeGrid.Header = Header8;
  CalendarTimeGrid.Axis = Axis;
  CalendarTimeGrid.Cell = Cell2;
  CalendarTimeGrid.EventCard = (0, import_react106.forwardRef)((props, ref) => EventCard({ ...props, ref }));
  CalendarTimeGrid.NowLine = NowLine;
  CalendarTimeGrid.Column = Column;
  var CalendarTimeGrid_default = CalendarTimeGrid;

  // src/components/ui/calendar-month-grid/index.ts
  init_define_import_meta_env();

  // src/components/ui/calendar-month-grid/CalendarMonthGrid.tsx
  init_define_import_meta_env();
  var import_react107 = __toESM(require_react_shim(), 1);
  var Weekdays2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "calendar-month-grid-weekdays",
      className: cn("border-muted-200 bg-muted-50 grid grid-cols-7 border-b", className),
      ...props
    }
  );
  var Weekday2 = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "calendar-month-grid-weekday",
      className: cn("text-muted-foreground border-muted-200 border-e border-t py-2 text-center text-sm font-semibold last:border-e-0", className),
      ...props
    }
  );
  var Days = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "calendar-month-grid-days",
      className: cn(
        "grid flex-1 auto-rows-fr grid-cols-7",
        "[&>[data-slot=calendar-month-grid-day-cell]:nth-child(7n)]:border-e-0",
        "[&>[data-slot=calendar-month-grid-day-cell]:nth-last-child(-n+7)]:border-b-0",
        className
      ),
      ...props
    }
  );
  var DayCell = ({ dayNumber, isToday, isCurrentMonth, available, className, children, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "calendar-month-grid-day-cell",
      ...isToday ? { "aria-current": "date" } : {},
      className: cn("border-muted-200 flex min-h-25 flex-col overflow-hidden border-e border-b p-1", !available && "bg-muted-100", className),
      ...props
    },
    /* @__PURE__ */ React.createElement(
      "span",
      {
        className: cn(
          "mb-1 flex size-7 items-center justify-center rounded-full text-xs",
          isToday && "bg-primary-400 text-primary-foreground font-bold",
          !isToday && isCurrentMonth && "text-foreground",
          !isToday && !isCurrentMonth && "text-muted-400"
        )
      },
      dayNumber
    ),
    /* @__PURE__ */ React.createElement("div", { className: "flex min-h-0 flex-1 flex-col gap-0.5 overflow-hidden" }, children)
  );
  var EventChip = ({ ref, className, type = "button", ...props }) => /* @__PURE__ */ React.createElement(
    "button",
    {
      ref,
      type,
      "data-slot": "calendar-month-grid-event-chip",
      className: cn(
        "ring-offset-background block w-full cursor-pointer overflow-hidden rounded-md",
        "focus-visible:ring-ring focus-visible:ring-2 focus-visible:outline-none",
        className
      ),
      ...props
    }
  );
  var CalendarMonthGrid = ({ className, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "calendar-month-grid",
      className: cn(
        "flex min-h-0 flex-1 flex-col overflow-y-auto",
        "[scrollbar-color:var(--color-muted-200)_transparent] [scrollbar-width:thin]",
        className
      ),
      ...props
    }
  );
  CalendarMonthGrid.Weekdays = Weekdays2;
  CalendarMonthGrid.Weekday = Weekday2;
  CalendarMonthGrid.Days = Days;
  CalendarMonthGrid.DayCell = DayCell;
  CalendarMonthGrid.EventChip = (0, import_react107.forwardRef)((props, ref) => EventChip({ ...props, ref }));
  var CalendarMonthGrid_default = CalendarMonthGrid;

  // src/components/tables/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table-filters/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table-filters/data-table-faceted-filter/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table-filters/data-table-faceted-filter/DataTableFacetedFilter.tsx
  init_define_import_meta_env();
  function DataTableFacetedFilter({ column }) {
    const filterMeta = column.columnDef.meta?.filterMeta;
    if (!filterMeta) return null;
    switch (filterMeta.variant) {
      case "date":
      case "dateRange":
        return /* @__PURE__ */ React.createElement(DateFilter_default, { column, filterMeta });
      case "time":
        return /* @__PURE__ */ React.createElement(TimeFilter_default, { column, filterMeta });
      default:
        if (isAsyncFilterMeta(filterMeta)) return /* @__PURE__ */ React.createElement(AsyncSelectFilter_default, { column, filterMeta });
        return /* @__PURE__ */ React.createElement(SelectFilter_default, { column, filterMeta });
    }
  }
  var DataTableFacetedFilter_default = DataTableFacetedFilter;

  // src/components/tables/data-table-filters/filter-chip-list/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table-filters/filter-chip-list/FilterChipList.tsx
  init_define_import_meta_env();
  var MAX_VISIBLE_CHIPS = 2;
  function FilterChipList({ chips, onRemoveChip, maxVisibleChips = MAX_VISIBLE_CHIPS }) {
    if (chips.length === 0) return null;
    const visibleChips = chips.slice(0, maxVisibleChips);
    const overflowCount = chips.length - maxVisibleChips;
    return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(Divider_default, { orientation: "vertical" }), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2" }, visibleChips.map((chip) => /* @__PURE__ */ React.createElement("span", { key: chip.value, className: "text-2xs text-muted-400 flex items-center gap-1" }, chip.label, /* @__PURE__ */ React.createElement(
      CircleMinus,
      {
        size: 12,
        className: "text-muted-400 hover:text-destructive cursor-pointer transition-colors",
        onClick: (e2) => onRemoveChip(chip.value, e2)
      }
    ))), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: overflowCount > 0 }, /* @__PURE__ */ React.createElement("span", { className: "bg-primary-15 rounded-full p-0.5 leading-none" }, "+", overflowCount))));
  }
  var FilterChipList_default = FilterChipList;

  // src/components/tables/data-table-filters/select-filter/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table-filters/select-filter/SelectFilter.tsx
  init_define_import_meta_env();
  var import_react108 = __toESM(require_react_shim(), 1);
  function SelectFilter({ column, filterMeta }) {
    const isMulti = filterMeta.variant === "multiSelect";
    const options3 = filterMeta.options;
    const columnFilterValue = column.getFilterValue();
    const [open, setOpen] = (0, import_react108.useState)(false);
    const { getOptionLabel, getOptionValue } = filterMeta;
    const selectedValues = (0, import_react108.useMemo)(
      () => new Set(Array.isArray(columnFilterValue) ? columnFilterValue : []),
      [columnFilterValue]
    );
    const chips = (0, import_react108.useMemo)(
      () => options3.filter((option) => selectedValues.has(getOptionValue(option))).map((option) => ({ value: getOptionValue(option), label: getOptionLabel(option) })),
      [options3, selectedValues, getOptionValue, getOptionLabel]
    );
    const onSelectItem = (0, import_react108.useCallback)(
      (option, isSelected) => {
        const value = getOptionValue(option);
        if (isMulti) {
          const next = new Set(selectedValues);
          isSelected ? next.delete(value) : next.add(value);
          const values = Array.from(next);
          column.setFilterValue(values.length > 0 ? values : void 0);
        } else {
          column.setFilterValue(isSelected ? void 0 : [value]);
          setOpen(false);
        }
      },
      [column, getOptionValue, isMulti, selectedValues]
    );
    const onRemoveChip = (0, import_react108.useCallback)(
      (value, event) => {
        event.stopPropagation();
        const next = new Set(selectedValues);
        next.delete(value);
        const values = Array.from(next);
        column.setFilterValue(values.length > 0 ? values : void 0);
      },
      [column, selectedValues]
    );
    return /* @__PURE__ */ React.createElement(Combobox_default, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(Combobox_default.Trigger, { className: "hover:not-disabled:border-primary-25 hover:not-disabled:ring-primary-25 w-auto gap-1 rounded-sm px-2 py-1 text-xs" }, filterMeta.label, /* @__PURE__ */ React.createElement(ChevronDown, { size: 16, className: "text-muted-400" }), /* @__PURE__ */ React.createElement(FilterChipList_default, { chips, onRemoveChip, maxVisibleChips: isMulti ? void 0 : 1 })), /* @__PURE__ */ React.createElement(Combobox_default.Content, { className: "min-w-60", shouldFilter: true }, /* @__PURE__ */ React.createElement(Combobox_default.Input, { placeholder: filterMeta.placeholder ?? filterMeta.label }), /* @__PURE__ */ React.createElement(Combobox_default.List, null, /* @__PURE__ */ React.createElement(Combobox_default.Empty, { when: options3.length === 0 }, "No results"), options3.map((option) => {
      const value = getOptionValue(option);
      const label = getOptionLabel(option);
      const isSelected = selectedValues.has(value);
      return /* @__PURE__ */ React.createElement(
        Combobox_default.Item,
        {
          key: value,
          value: label,
          "data-checked": isSelected || void 0,
          onSelect: () => onSelectItem(option, isSelected)
        },
        /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: isMulti }, /* @__PURE__ */ React.createElement(Combobox_default.Indicator, null, /* @__PURE__ */ React.createElement(Checkbox_default, { checked: isSelected, variant: "primary", tabIndex: -1, className: "pointer-events-none" }))), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Combobox_default.Indicator, null, /* @__PURE__ */ React.createElement(RadioDot_default, { checked: isSelected })))),
        label
      );
    }))));
  }
  var SelectFilter_default = SelectFilter;

  // src/components/tables/data-table-filters/async-select-filter/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table-filters/async-select-filter/AsyncSelectFilter.tsx
  init_define_import_meta_env();
  var import_react109 = __toESM(require_react_shim(), 1);
  function AsyncSelectFilter({ column, filterMeta }) {
    const [open, setOpen] = (0, import_react109.useState)(false);
    const [search, setSearch] = (0, import_react109.useState)("");
    const debouncedSearch = useDebounce(search, 500);
    const isMulti = filterMeta.variant === "multiSelect";
    const columnFilterValue = column.getFilterValue();
    const selectedValues = (0, import_react109.useMemo)(() => Array.isArray(columnFilterValue) ? columnFilterValue : [], [columnFilterValue]);
    const selectedSet = (0, import_react109.useMemo)(() => new Set(selectedValues), [selectedValues]);
    const hasValueToResolve = selectedValues.length > 0;
    const infiniteQuery = useAsyncOptionsInfiniteQuery({
      fetchOptions: filterMeta.options,
      queryKey: filterMeta.queryKey ?? `filter-${column.id}`,
      searchTerm: debouncedSearch,
      selectedItemsIds: selectedValues,
      urlSearchParams: filterMeta.urlSearchParams,
      pageSize: 20,
      enabled: open || hasValueToResolve
    });
    const { handleScroll: handleScroll2 } = useInfiniteScroll({ infiniteQuery, enabled: true });
    const allOptions = (0, import_react109.useMemo)(() => infiniteQuery.data?.pages.flatMap((p2) => p2.data) ?? [], [infiniteQuery.data]);
    const chips = (0, import_react109.useMemo)(
      () => selectedValues.map((val) => {
        const option = allOptions.find((o) => filterMeta.getOptionValue(o) === val);
        return { value: val, label: option ? filterMeta.getOptionLabel(option) : val };
      }),
      [selectedValues, allOptions, filterMeta]
    );
    const onSelect = (0, import_react109.useCallback)(
      (optValue) => {
        if (isMulti) {
          const next = new Set(selectedSet);
          next.has(optValue) ? next.delete(optValue) : next.add(optValue);
          column.setFilterValue(next.size > 0 ? Array.from(next) : void 0);
        } else {
          column.setFilterValue(selectedValues[0] === optValue ? void 0 : [optValue]);
          setOpen(false);
        }
      },
      [column, isMulti, selectedSet, selectedValues]
    );
    const onRemoveChip = (0, import_react109.useCallback)(
      (chipValue, e2) => {
        e2.stopPropagation();
        e2.preventDefault();
        if (isMulti) {
          const next = selectedValues.filter((v) => v !== chipValue);
          column.setFilterValue(next.length > 0 ? next : void 0);
        } else {
          column.setFilterValue(void 0);
        }
      },
      [column, isMulti, selectedValues]
    );
    return /* @__PURE__ */ React.createElement(Combobox_default, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(Combobox_default.Trigger, { className: "hover:not-disabled:border-primary-25 hover:not-disabled:ring-primary-25 w-auto gap-1 rounded-sm px-2 py-1 text-xs" }, filterMeta.label, /* @__PURE__ */ React.createElement(ChevronDown, { size: 16, className: "text-muted-400" }), /* @__PURE__ */ React.createElement(FilterChipList_default, { chips, onRemoveChip, maxVisibleChips: isMulti ? void 0 : 1 })), /* @__PURE__ */ React.createElement(Combobox_default.Content, { className: "min-w-60", shouldFilter: false }, /* @__PURE__ */ React.createElement(Combobox_default.Input, { placeholder: filterMeta.placeholder ?? filterMeta.label, value: search, onValueChange: setSearch }, /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: infiniteQuery.isFetching || search !== debouncedSearch })), /* @__PURE__ */ React.createElement(Combobox_default.List, { onScroll: handleScroll2 }, /* @__PURE__ */ React.createElement(Combobox_default.Empty, { when: !infiniteQuery.isFetching && allOptions.length === 0 }, "No results"), allOptions.map((opt) => {
      const optValue = filterMeta.getOptionValue(opt);
      const isChecked = selectedSet.has(optValue);
      return /* @__PURE__ */ React.createElement(
        Combobox_default.Item,
        {
          key: optValue,
          value: filterMeta.getOptionLabel(opt),
          "data-checked": isChecked || void 0,
          onSelect: () => onSelect(optValue)
        },
        /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: isMulti }, /* @__PURE__ */ React.createElement(Combobox_default.Indicator, null, /* @__PURE__ */ React.createElement(Checkbox_default, { checked: isChecked, variant: "primary", tabIndex: -1, className: "pointer-events-none" }))), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Combobox_default.Indicator, null, /* @__PURE__ */ React.createElement(RadioDot_default, { checked: isChecked })))),
        filterMeta.getOptionLabel(opt)
      );
    }), /* @__PURE__ */ React.createElement(Combobox_default.Loader, { when: infiniteQuery.isFetchingNextPage, message: "Loading more..." }))));
  }
  var AsyncSelectFilter_default = AsyncSelectFilter;

  // src/components/tables/data-table-filters/date-filter/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table-filters/date-filter/DateFilter.tsx
  init_define_import_meta_env();
  var import_react110 = __toESM(require_react_shim(), 1);
  function DateFilter({ column, filterMeta }) {
    const { toDate: toDate2, toBoundaryDate, formatDate } = useDate();
    const isRange = filterMeta.variant === "dateRange";
    const displayFormat = filterMeta.dateFormat ?? "MMM dd, yyyy" /* SHORT_MONTH_DAY_YEAR */;
    const [open, setOpen] = (0, import_react110.useState)(false);
    const filterValue = column.getFilterValue();
    const single = isRange ? void 0 : filterValue;
    const range = isRange ? filterValue : void 0;
    const selectedSingle = (0, import_react110.useMemo)(() => single ? toBoundaryDate(single) : void 0, [single, toBoundaryDate]);
    const selectedRange = (0, import_react110.useMemo)(() => {
      if (!range?.from && !range?.to) return void 0;
      return {
        from: range?.from ? toBoundaryDate(range.from) : void 0,
        to: range?.to ? toBoundaryDate(range.to) : void 0
      };
    }, [range, toBoundaryDate]);
    const chips = (0, import_react110.useMemo)(() => {
      if (isRange) {
        if (!range?.from && !range?.to) return [];
        const from = range?.from ? formatDate(range.from, displayFormat) : "\u2026";
        const to = range?.to ? formatDate(range.to, displayFormat) : "\u2026";
        return [{ value: "range", label: `${from} \u2013 ${to}` }];
      }
      if (!single) return [];
      return [{ value: single, label: formatDate(single, displayFormat) }];
    }, [isRange, range, single, formatDate, displayFormat]);
    const onSelectSingle = (0, import_react110.useCallback)(
      (date) => {
        column.setFilterValue(date ? toDate2(date) : void 0);
        setOpen(false);
      },
      [column, toDate2]
    );
    const onSelectRange = (0, import_react110.useCallback)(
      (next) => {
        if (!next?.from && !next?.to) {
          column.setFilterValue(void 0);
          return;
        }
        column.setFilterValue({
          from: next?.from ? toDate2(next.from) : void 0,
          to: next?.to ? toDate2(next.to) : void 0
        });
      },
      [column, toDate2]
    );
    const onRemoveChip = (0, import_react110.useCallback)(
      (_value, event) => {
        event.stopPropagation();
        column.setFilterValue(void 0);
      },
      [column]
    );
    return /* @__PURE__ */ React.createElement(Popover_default, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(Combobox_default.Trigger, { className: "hover:not-disabled:border-primary-25 hover:not-disabled:ring-primary-25 w-auto gap-1 rounded-sm px-2 py-1 text-xs" }, filterMeta.label, /* @__PURE__ */ React.createElement(ChevronDown, { size: 16, className: "text-muted-400" }), /* @__PURE__ */ React.createElement(FilterChipList_default, { chips, onRemoveChip, maxVisibleChips: 1 })), /* @__PURE__ */ React.createElement(Popover_default.Content, { align: "start", className: "w-auto p-0" }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: isRange }, /* @__PURE__ */ React.createElement(
      Calendar_default,
      {
        mode: "range",
        numberOfMonths: 2,
        captionLayout: "dropdown",
        selected: selectedRange,
        onSelect: onSelectRange,
        autoFocus: true
      }
    )), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Calendar_default, { mode: "single", captionLayout: "dropdown", selected: selectedSingle, onSelect: onSelectSingle, autoFocus: true })))));
  }
  var DateFilter_default = DateFilter;

  // src/components/tables/data-table-filters/time-filter/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table-filters/time-filter/TimeFilter.tsx
  init_define_import_meta_env();
  var import_react111 = __toESM(require_react_shim(), 1);
  function TimeFilter({ column, filterMeta }) {
    const { use24hFormat = false, min: min3 = "00:00", max: max3 = "23:59", step = 1 } = filterMeta;
    const [open, setOpen] = (0, import_react111.useState)(false);
    const [tempValue, setTempValue] = (0, import_react111.useState)("");
    const value = column.getFilterValue();
    const startAt = use24hFormat ? "08:00" : "08:00 AM";
    (0, import_react111.useEffect)(() => {
      setTempValue(open ? value || startAt : "");
    }, [open, value, startAt]);
    const chips = (0, import_react111.useMemo)(() => value ? [{ value, label: value }] : [], [value]);
    const onConfirm = (0, import_react111.useCallback)(() => {
      column.setFilterValue(tempValue || void 0);
      setOpen(false);
    }, [column, tempValue]);
    const onRemoveChip = (0, import_react111.useCallback)(
      (_value, event) => {
        event.stopPropagation();
        column.setFilterValue(void 0);
      },
      [column]
    );
    return /* @__PURE__ */ React.createElement(Popover_default, { open, onOpenChange: setOpen, modal: true }, /* @__PURE__ */ React.createElement(Combobox_default.Trigger, { className: "hover:not-disabled:border-primary-25 hover:not-disabled:ring-primary-25 w-auto gap-1 rounded-sm px-2 py-1 text-xs" }, filterMeta.label, /* @__PURE__ */ React.createElement(ChevronDown, { size: 16, className: "text-muted-400" }), /* @__PURE__ */ React.createElement(FilterChipList_default, { chips, onRemoveChip, maxVisibleChips: 1 })), /* @__PURE__ */ React.createElement(
      TimePicker_default.Content,
      {
        value,
        tempValue,
        onTempValueChange: setTempValue,
        onConfirm,
        use24hFormat,
        min: min3,
        max: max3,
        step
      }
    ));
  }
  var TimeFilter_default = TimeFilter;

  // src/components/tables/data-table/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table/data-table-content/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table/data-table-content/DataTableContent.tsx
  init_define_import_meta_env();

  // node_modules/@tanstack/react-table/build/lib/index.mjs
  init_define_import_meta_env();
  var React103 = __toESM(require_react_shim(), 1);
  function flexRender(Comp, props) {
    return !Comp ? null : isReactComponent(Comp) ? /* @__PURE__ */ React103.createElement(Comp, props) : Comp;
  }
  function isReactComponent(component) {
    return isClassComponent(component) || typeof component === "function" || isExoticComponent(component);
  }
  function isClassComponent(component) {
    return typeof component === "function" && (() => {
      const proto = Object.getPrototypeOf(component);
      return proto.prototype && proto.prototype.isReactComponent;
    })();
  }
  function isExoticComponent(component) {
    return typeof component === "object" && typeof component.$$typeof === "symbol" && ["react.memo", "react.forward_ref"].includes(component.$$typeof.description);
  }

  // src/components/tables/data-table/data-table-content/DataTableContent.tsx
  var ARIA_SORT = {
    asc: "ascending",
    desc: "descending"
  };
  function DataTableContent({ className, emptyState }) {
    const { table, isLoading, onRowClick, getRowClassName } = useDataTableContext();
    const isEmpty = !table.getRowModel().rows?.length && !isLoading;
    const visibleColumns = table.getVisibleLeafColumns();
    const onTableRowClick = (event, row) => {
      if (isInteractiveClick(event)) return;
      onRowClick?.(row);
    };
    return /* @__PURE__ */ React.createElement(ScrollArea_default, { className: cn("bg-background min-h-0 flex-1 rounded-2xl shadow", className), viewportClassName: "[&>div]:h-full" }, /* @__PURE__ */ React.createElement(Table_default, { className: cn("w-max min-w-full", isEmpty && "min-h-full") }, /* @__PURE__ */ React.createElement(Table_default.Header, { className: "sticky inset-bs-0 z-10" }, table.getHeaderGroups().map((headerGroup) => /* @__PURE__ */ React.createElement(Table_default.Row, { key: headerGroup.id }, headerGroup.headers.map((header) => {
      const isPinned = header.column.getIsPinned();
      const canSort = header.column.getCanSort();
      const sortDirection = header.column.getIsSorted();
      const hasExplicitSize = header.column.columnDef.size !== void 0;
      return /* @__PURE__ */ React.createElement(
        Table_default.Head,
        {
          key: header.id,
          colSpan: header.colSpan,
          "aria-sort": sortDirection ? ARIA_SORT[sortDirection] : void 0,
          onClick: canSort ? header.column.getToggleSortingHandler() : void 0,
          className: cn("whitespace-nowrap", canSort && "cursor-pointer select-none"),
          style: isPinned ? getCommonPinningStyles({ column: header.column, withBorder: true }) : hasExplicitSize ? { width: header.column.getSize() } : void 0
        },
        /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !header.isPlaceholder }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-1" }, flexRender(header.column.columnDef.header, header.getContext()), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: canSort }, /* @__PURE__ */ React.createElement("span", { className: "inline-flex shrink-0" }, /* @__PURE__ */ React.createElement(SortIcon_default, { direction: sortDirection })))))
      );
    })))), /* @__PURE__ */ React.createElement(Table_default.Body, { className: "[&_tr:last-child:not([data-slot=table-empty])]:border-b" }, /* @__PURE__ */ React.createElement(Table_default.Loader, { isLoading, colSpan: visibleColumns.length, rows: 10 }), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!table.getRowModel().rows?.length && !isLoading }, table.getRowModel().rows.map((row) => /* @__PURE__ */ React.createElement(
      Table_default.Row,
      {
        key: row.id,
        onClick: onRowClick ? (event) => onTableRowClick(event, row) : void 0,
        "data-state": row.getIsSelected() ? "selected" : void 0,
        className: getRowClassName?.(row)
      },
      row.getVisibleCells().map((cell) => {
        const isPinned = cell.column.getIsPinned();
        const isDisabled = cell.column.columnDef.meta?.disableRowClick;
        const value = cell.getValue();
        return /* @__PURE__ */ React.createElement(
          Table_default.Cell,
          {
            key: cell.id,
            title: typeof value === "string" ? String(value) : void 0,
            "data-disable-row-click": isDisabled || void 0,
            style: isPinned ? getCommonPinningStyles({ column: cell.column, withBorder: true }) : void 0,
            className: cn("truncate", isPinned ? "bg-inherit" : "max-w-0", onRowClick && !isDisabled && "cursor-pointer")
          },
          flexRender(cell.column.columnDef.cell, cell.getContext())
        );
      })
    ))), /* @__PURE__ */ React.createElement(Table_default.Empty, { when: isEmpty, colSpan: visibleColumns.length, ...emptyState }))), /* @__PURE__ */ React.createElement(ScrollArea_default.Bar, { orientation: "vertical", className: "z-20" }), /* @__PURE__ */ React.createElement(ScrollArea_default.Bar, { orientation: "horizontal", className: "z-20" }));
  }
  var DataTableContent_default = DataTableContent;

  // src/components/tables/data-table/data-table-toolbar/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table/data-table-toolbar/DataTableToolbar.tsx
  init_define_import_meta_env();
  var import_react112 = __toESM(require_react_shim(), 1);
  var DataTableToolbar = ({ totalCount, totalLabel, placeholder, children, className }) => {
    const { table } = useDataTableContext();
    const [search, setSearch] = (0, import_react112.useState)(table.getState().globalFilter ?? "");
    const [showFilters, setShowFilters] = (0, import_react112.useState)(false);
    const debouncedSearch = useDebounce(search, 500);
    const filterableColumns = (0, import_react112.useMemo)(
      () => table.getAllColumns().filter((column) => column.getCanFilter() && column.columnDef.meta?.filterMeta),
      [table]
    );
    const activeFilterCount = (0, import_react112.useMemo)(() => table.getState().columnFilters.length, [table.getState().columnFilters?.length]);
    const isFiltered = activeFilterCount > 0;
    const onToggleFilters = (0, import_react112.useCallback)(() => {
      setShowFilters((prev) => !prev);
    }, []);
    const onReset = (0, import_react112.useCallback)(() => {
      table.resetColumnFilters();
      table.resetGlobalFilter();
      setSearch("");
    }, [table]);
    (0, import_react112.useEffect)(() => {
      table.setGlobalFilter(debouncedSearch);
    }, [debouncedSearch, table]);
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "data-table-toolbar", className: cn("flex w-full flex-col rounded-2xl shadow", className) }, /* @__PURE__ */ React.createElement("div", { className: cn("bg-background flex items-stretch gap-6 rounded-2xl ps-6 transition-all", showFilters && "rounded-es-none") }, /* @__PURE__ */ React.createElement("div", { className: "flex flex-1 items-center py-3" }, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: totalCount !== void 0 }, /* @__PURE__ */ React.createElement("span", { className: "text-muted-400 text-xs font-medium whitespace-nowrap" }, "Total: ", totalCount, " ", totalLabel))), /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!table?.options?.enableGlobalFilter }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center py-3" }, /* @__PURE__ */ React.createElement(Search, { size: 16, className: "text-primary" }), /* @__PURE__ */ React.createElement(
      Input_default,
      {
        name: "data-table-search",
        placeholder: placeholder ?? "Search Here",
        value: search,
        onChange: (event) => setSearch(event.target.value),
        className: "w-60 border-none px-2 py-0 text-sm shadow-none [&:hover,&:focus]:not-disabled:border-none [&:hover,&:focus]:not-disabled:ring-0 bg-transparent"
      }
    ))), children, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: filterableColumns.length > 0 }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: cn(
          "border-muted-200 text-muted-foreground relative flex cursor-pointer items-center gap-2 rounded-se-2xl border-s px-6 text-sm font-normal",
          showFilters ? "bg-primary-25" : "rounded-ee-2xl"
        ),
        onClick: onToggleFilters
      },
      /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: showFilters }, /* @__PURE__ */ React.createElement(Minus, { size: 16 })), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Plus, { size: 16 }))),
      "Filter",
      /* @__PURE__ */ React.createElement("span", { className: cn("bg-primary invisible absolute inset-e-3.5 inset-bs-2.5 size-2 rounded-full", isFiltered && "visible") })
    ))), /* @__PURE__ */ React.createElement(
      "div",
      {
        className: cn(
          "grid transition-[grid-template-rows] duration-300 ease-in-out",
          showFilters && filterableColumns.length > 0 ? "grid-rows-[1fr]" : "grid-rows-[0fr]"
        )
      },
      /* @__PURE__ */ React.createElement("div", { className: "overflow-hidden" }, /* @__PURE__ */ React.createElement(
        "div",
        {
          role: "toolbar",
          "aria-orientation": "horizontal",
          "aria-label": "Filters",
          className: "bg-primary-25 flex items-center gap-6 rounded-b-2xl px-3 py-2"
        },
        /* @__PURE__ */ React.createElement("div", { className: "flex flex-1 flex-wrap items-center gap-1" }, filterableColumns.map((column) => /* @__PURE__ */ React.createElement(DataTableFacetedFilter_default, { key: column.id, column }))),
        /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: isFiltered }, /* @__PURE__ */ React.createElement(Button_default, { variant: "ghost-primary", size: "xs", "aria-label": "Clear all filters", onClick: onReset }, "Clear All"))
      ))
    ));
  };
  var DataTableToolbar_default = DataTableToolbar;

  // src/components/tables/data-table/data-table-pagination/index.ts
  init_define_import_meta_env();

  // src/components/tables/data-table/data-table-pagination/DataTablePagination.tsx
  init_define_import_meta_env();
  var import_react113 = __toESM(require_react_shim(), 1);
  function getPageNumbers(currentPage, totalPages) {
    if (totalPages <= 7) return Array.from({ length: totalPages }, (_, i) => i + 1);
    const pages = [1];
    if (currentPage > 3) pages.push("ellipsis");
    const start = Math.max(2, currentPage - 1);
    const end = Math.min(totalPages - 1, currentPage + 1);
    for (let i = start; i <= end; i++) {
      pages.push(i);
    }
    if (currentPage < totalPages - 2) pages.push("ellipsis");
    if (totalPages > 1) pages.push(totalPages);
    return pages;
  }
  var DataTablePagination = ({ pageSizeOptions = [30, 50, 100, 200], className }) => {
    const { table } = useDataTableContext();
    const currentPage = table.getState().pagination.pageIndex + 1;
    const totalPages = table.getPageCount();
    const pageNumbers = (0, import_react113.useMemo)(() => getPageNumbers(currentPage, totalPages), [currentPage, totalPages]);
    const handlePageSizeChange = (0, import_react113.useCallback)(
      (value) => {
        table.setPageSize(Number(value));
      },
      [table]
    );
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "data-table-pagination",
        className: cn("bg-background flex w-full items-center justify-end gap-4 overflow-hidden rounded-2xl px-7 py-2 shadow", className)
      },
      /* @__PURE__ */ React.createElement("div", { className: "bg-muted-50 flex items-center gap-2 rounded-full px-3 py-2" }, /* @__PURE__ */ React.createElement("span", { className: "text-muted-400 text-xs font-normal whitespace-nowrap" }, "Rows per page"), /* @__PURE__ */ React.createElement(Select_default, { value: `${table.getState().pagination.pageSize}`, onValueChange: handlePageSizeChange }, /* @__PURE__ */ React.createElement(
        Select_default.Trigger,
        {
          className: cn(
            "border-0 bg-transparent px-1 py-0 text-xs font-normal shadow-none",
            "data-[state=open]:border-none data-[state=open]:ring-0",
            "hover:not-disabled:border-none hover:not-disabled:ring-0",
            "focus-visible:border-none focus-visible:ring-0"
          )
        },
        /* @__PURE__ */ React.createElement(Select_default.Value, { placeholder: table.getState().pagination.pageSize }),
        /* @__PURE__ */ React.createElement(Select_default.Icon, null)
      ), /* @__PURE__ */ React.createElement(Select_default.Content, { side: "top" }, pageSizeOptions.map((pageSize) => /* @__PURE__ */ React.createElement(Select_default.Item, { key: pageSize, value: `${pageSize}` }, /* @__PURE__ */ React.createElement(Select_default.Text, null, pageSize)))))),
      /* @__PURE__ */ React.createElement(Pagination_default, { className: "w-fit p-0" }, /* @__PURE__ */ React.createElement(Pagination_default.List, null, /* @__PURE__ */ React.createElement(Pagination_default.Item, null, /* @__PURE__ */ React.createElement(Pagination_default.Previous, { onClick: () => table.previousPage(), disabled: !table.getCanPreviousPage() })), pageNumbers.map((page, index2) => /* @__PURE__ */ React.createElement(Conditional_default, { key: typeof page === "number" ? page : `ellipsis-${index2}` }, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: page === "ellipsis" }, /* @__PURE__ */ React.createElement(Pagination_default.Item, null, /* @__PURE__ */ React.createElement(Pagination_default.Ellipsis, null))), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(Pagination_default.Item, null, /* @__PURE__ */ React.createElement(
        Pagination_default.NavButton,
        {
          className: "size-8.5",
          onClick: () => table.setPageIndex(page - 1),
          isActive: page === currentPage
        },
        page.toString().padStart(2, "0")
      ))))), /* @__PURE__ */ React.createElement(Pagination_default.Item, null, /* @__PURE__ */ React.createElement(Pagination_default.Next, { onClick: () => table.nextPage(), disabled: !table.getCanNextPage() }))))
    );
  };
  var DataTablePagination_default = DataTablePagination;

  // src/components/tables/data-table/DataTable.tsx
  init_define_import_meta_env();
  var import_react114 = __toESM(require_react_shim(), 1);
  var DataTableContext = (0, import_react114.createContext)(null);
  var useDataTableContext = () => {
    const context2 = (0, import_react114.useContext)(DataTableContext);
    if (!context2) throw new Error("useDataTableContext must be used within a <DataTable /> provider");
    return context2;
  };
  function DataTableRoot({ table, isLoading = false, onRowClick, getRowClassName, children, className }) {
    const contextValue = (0, import_react114.useMemo)(
      () => ({
        table,
        isLoading,
        onRowClick,
        getRowClassName
      }),
      [table, isLoading, onRowClick, getRowClassName]
    );
    return /* @__PURE__ */ React.createElement(DataTableContext.Provider, { value: contextValue }, /* @__PURE__ */ React.createElement("div", { "data-slot": "data-table", className: cn("flex h-full min-h-0 w-full flex-col gap-3", className) }, children));
  }
  var DataTable = DataTableRoot;
  DataTable.Toolbar = DataTableToolbar_default;
  DataTable.Content = DataTableContent_default;
  DataTable.Pagination = DataTablePagination_default;
  var DataTable_default = DataTable;

  // src/components/calendar/index.ts
  init_define_import_meta_env();

  // src/components/calendar/calendar-grid/index.ts
  init_define_import_meta_env();

  // src/components/calendar/calendar-grid/CalendarGrid.tsx
  init_define_import_meta_env();
  var import_react118 = __toESM(require_react_shim(), 1);

  // src/components/calendar/calendar-grid/calendar-grid-chrome/index.ts
  init_define_import_meta_env();

  // src/components/calendar/calendar-grid/calendar-grid-chrome/CalendarGridChrome.tsx
  init_define_import_meta_env();
  var import_react115 = __toESM(require_react_shim(), 1);
  var Header9 = ({ className, children, ...props }) => /* @__PURE__ */ React.createElement(
    "div",
    {
      "data-slot": "calendar-grid-header",
      className: cn("border-muted-200 flex items-center justify-between gap-4 border-b px-4 py-3", className),
      ...props
    },
    children
  );
  var Navigation = () => {
    const { navigationLabel, goPrev, goNext } = useCalendarGridContext();
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "calendar-grid-navigation", className: "flex items-center gap-2" }, /* @__PURE__ */ React.createElement(TooltipButton_default, { title: "Previous", type: "button", variant: "ghost", size: "icon-sm", onClick: goPrev }, /* @__PURE__ */ React.createElement(ChevronLeft, { size: 20, className: "text-muted-400" })), /* @__PURE__ */ React.createElement("span", { className: "text-foreground text-sm font-bold" }, navigationLabel), /* @__PURE__ */ React.createElement(TooltipButton_default, { title: "Next", type: "button", variant: "ghost", size: "icon-sm", onClick: goNext }, /* @__PURE__ */ React.createElement(ChevronRight, { size: 20, className: "text-muted-400" })));
  };
  var ViewTabs = () => {
    const { view, setView } = useCalendarGridContext();
    return /* @__PURE__ */ React.createElement(
      Tabs_default,
      {
        className: "min-w-1/3",
        value: view,
        onValueChange: (value) => {
          if (value === "day" || value === "week" || value === "month") setView(value);
        }
      },
      /* @__PURE__ */ React.createElement(Tabs_default.List, null, /* @__PURE__ */ React.createElement(Tabs_default.Trigger, { value: "day" }, "Day"), /* @__PURE__ */ React.createElement(Tabs_default.Trigger, { value: "week" }, "Week"), /* @__PURE__ */ React.createElement(Tabs_default.Trigger, { value: "month" }, "Month"))
    );
  };
  var ViewToggle = () => {
    const { viewMode, setViewMode } = useCalendarGridContext();
    return /* @__PURE__ */ React.createElement(
      ToggleGroup_default,
      {
        type: "single",
        value: viewMode,
        onValueChange: (value) => {
          if (value === "calendar" || value === "list") setViewMode(value);
        }
      },
      /* @__PURE__ */ React.createElement(ToggleGroup_default.Item, { value: "list", "aria-label": "List view" }, /* @__PURE__ */ React.createElement(List, { size: 16 })),
      /* @__PURE__ */ React.createElement(ToggleGroup_default.Item, { value: "calendar", "aria-label": "Calendar view" }, /* @__PURE__ */ React.createElement(Calendar, { size: 16 }))
    );
  };
  var Picker = () => {
    const { date, setDate, view } = useCalendarGridContext();
    const { toStartAndEndOf, toBoundaryDate, addDuration, toDate: toDate2, constants } = useDate();
    const selected = (0, import_react115.useMemo)(() => {
      if (view === "day") return date;
      const { startOf, endOf } = toStartAndEndOf(date, view === "week" ? constants.UnitOfTime.WEEK : constants.UnitOfTime.MONTH);
      return { from: startOf, to: endOf };
    }, [view, date, toStartAndEndOf, constants.UnitOfTime.WEEK, constants.UnitOfTime.MONTH]);
    return /* @__PURE__ */ React.createElement(
      CalendarPicker_default,
      {
        value: date,
        selected,
        triggerTooltip: "Jump to date",
        calendarProps: { captionLayout: "dropdown", endMonth: toBoundaryDate(addDuration(toDate2(), 10, constants.UnitOfTime.YEAR)) },
        onChange: setDate
      }
    );
  };
  var Legend = ({ items, className, ...props }) => /* @__PURE__ */ React.createElement("div", { "data-slot": "calendar-grid-legend", className: cn("flex flex-wrap items-center gap-3 px-3 py-2", className), ...props }, items.map((item) => /* @__PURE__ */ React.createElement("div", { key: item.label, "data-slot": "calendar-grid-legend-item", className: "flex items-center gap-2" }, /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true", className: cn("border-muted-200 size-3 shrink-0 rounded-full border", item.colorClass) }), /* @__PURE__ */ React.createElement("span", { className: "text-muted-foreground text-xs" }, item.label))));
  var CalendarGridChrome = () => null;
  CalendarGridChrome.Header = Header9;
  CalendarGridChrome.Navigation = Navigation;
  CalendarGridChrome.ViewTabs = ViewTabs;
  CalendarGridChrome.ViewToggle = ViewToggle;
  CalendarGridChrome.Picker = Picker;
  CalendarGridChrome.Legend = Legend;
  var CalendarGridChrome_default = CalendarGridChrome;

  // src/components/calendar/calendar-grid/calendar-grid-month-view/index.ts
  init_define_import_meta_env();

  // src/components/calendar/calendar-grid/calendar-grid-month-view/CalendarGridMonthView.tsx
  init_define_import_meta_env();
  var import_react116 = __toESM(require_react_shim(), 1);
  var MAX_VISIBLE_MONTH_EVENTS = 3;
  var WEEK_HEADERS = ["SUN", "MON", "TUE", "WED", "THU", "FRI", "SAT"];
  var MonthEventItem = ({ event, status }) => {
    const { renderEvent, popoverContent } = useCalendarGridContext();
    const [open, setOpen] = (0, import_react116.useState)(false);
    const trigger = /* @__PURE__ */ React.createElement(CalendarMonthGrid_default.EventChip, { onClick: popoverContent ? () => setOpen(true) : void 0 }, renderEvent(event, status));
    if (!popoverContent) return trigger;
    return /* @__PURE__ */ React.createElement(Popover_default, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(Popover_default.Trigger, { asChild: true }, trigger), /* @__PURE__ */ React.createElement(Popover_default.Content, { className: "w-80 p-0" }, popoverContent(event, () => setOpen(false))));
  };
  var MonthView = () => {
    const { view, viewMode, date, events, eventAccessors, slotCoverageDates } = useCalendarGridContext();
    const { formatDate, getMonthGrid, toDate: toDate2, toDateUnits, isBefore: isBefore2, isAfter: isAfter2, constants } = useDate();
    const todayKey = toDate2();
    const monthInfo = (0, import_react116.useMemo)(() => {
      const units = toDateUnits(date);
      return { year: units.years, month: units.months };
    }, [date, toDateUnits]);
    const days = (0, import_react116.useMemo)(() => getMonthGrid(monthInfo.year, monthInfo.month), [getMonthGrid, monthInfo.year, monthInfo.month]);
    const eventsByDayKey = (0, import_react116.useMemo)(() => {
      const map = /* @__PURE__ */ new Map();
      const now = toDate2();
      for (const event of events) {
        const start = eventAccessors.getStartTime(event);
        const end = eventAccessors.getEndTime(event);
        const key = toDate2(start);
        const status = isBefore2(end, now) ? "past" : isAfter2(start, now) ? "future" : "current";
        if (!map.has(key)) map.set(key, []);
        map.get(key).push({ event, status });
      }
      return map;
    }, [events, eventAccessors, toDate2, isBefore2, isAfter2]);
    if (view !== "month" || viewMode !== "calendar") return null;
    return /* @__PURE__ */ React.createElement(CalendarMonthGrid_default, null, /* @__PURE__ */ React.createElement(CalendarMonthGrid_default.Weekdays, null, WEEK_HEADERS.map((day) => /* @__PURE__ */ React.createElement(CalendarMonthGrid_default.Weekday, { key: day }, day))), /* @__PURE__ */ React.createElement(CalendarMonthGrid_default.Days, null, days.map((day) => {
      const dayKey = toDate2(day);
      const isCurrentMonth = toDateUnits(day).months === monthInfo.month;
      const isToday = dayKey === todayKey;
      const hasCoverage = slotCoverageDates.has(dayKey);
      const dayEvents = eventsByDayKey.get(dayKey) ?? [];
      const visibleEvents = dayEvents.slice(0, MAX_VISIBLE_MONTH_EVENTS);
      const overflowEvents = dayEvents.slice(MAX_VISIBLE_MONTH_EVENTS);
      const dayNumber = Number(formatDate(day, constants.DateFormats.DAY_OF_MONTH));
      return /* @__PURE__ */ React.createElement(
        CalendarMonthGrid_default.DayCell,
        {
          key: dayKey,
          dayNumber,
          isToday,
          isCurrentMonth,
          available: hasCoverage
        },
        visibleEvents.map(({ event, status }, idx) => /* @__PURE__ */ React.createElement(MonthEventItem, { key: `${eventAccessors.getId(event)}-${idx}`, event, status })),
        /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: overflowEvents.length > 0 }, /* @__PURE__ */ React.createElement(Popover_default, null, /* @__PURE__ */ React.createElement(Popover_default.Trigger, { asChild: true }, /* @__PURE__ */ React.createElement(Button_default, { variant: "link", className: "text-2xs h-auto p-0 font-medium" }, "+", overflowEvents.length, " more")), /* @__PURE__ */ React.createElement(Popover_default.Content, { className: "border-muted-200 bg-background w-60 rounded-xl border p-2", side: "bottom", align: "start" }, /* @__PURE__ */ React.createElement("div", { className: "flex flex-col gap-1" }, overflowEvents.map(({ event, status }, idx) => /* @__PURE__ */ React.createElement(MonthEventItem, { key: `${eventAccessors.getId(event)}-overflow-${idx}`, event, status }))))))
      );
    })));
  };
  var CalendarGridMonthView = () => null;
  CalendarGridMonthView.MonthView = MonthView;
  var CalendarGridMonthView_default = CalendarGridMonthView;

  // src/components/calendar/calendar-grid/calendar-grid-time-view/index.ts
  init_define_import_meta_env();

  // src/components/calendar/calendar-grid/calendar-grid-time-view/CalendarGridTimeView.tsx
  init_define_import_meta_env();
  var import_react117 = __toESM(require_react_shim(), 1);
  var MAX_VISIBLE_COLUMNS = 4;
  var MIN_HEIGHT_PERCENT = 2;
  var parseTimeMinutes = (time) => {
    const [hStr, mStr] = time.split(":");
    return Number(hStr) * 60 + Number(mStr ?? 0);
  };
  var computeRowCount = (startHour, endHour, stepMinutes) => {
    const totalMinutes = (endHour - startHour) * 60;
    return Math.max(Math.floor(totalMinutes / stepMinutes), 1);
  };
  function parseEventsForDay(events, accessors, dayKey, isoFor, toMinutes, startMinutes, endMinutes) {
    return events.filter((event) => isoFor(accessors.getStartTime(event)) === dayKey).map((event) => {
      const start = accessors.getStartTime(event);
      const end = accessors.getEndTime(event);
      const rawStart = toMinutes(start);
      const rawEnd = toMinutes(end);
      return {
        event,
        start,
        end,
        startMins: Math.max(rawStart, startMinutes),
        endMins: rawEnd === rawStart ? rawStart + 1 : Math.min(rawEnd, endMinutes)
      };
    }).sort((a, b) => a.startMins - b.startMins || b.endMins - b.startMins - (a.endMins - a.startMins));
  }
  function clusterEvents(parsed) {
    const clusters = [];
    const visited = /* @__PURE__ */ new Set();
    for (let i = 0; i < parsed.length; i++) {
      if (visited.has(i)) continue;
      const cluster = [parsed[i]];
      visited.add(i);
      let clusterEnd = parsed[i].endMins;
      for (let j = i + 1; j < parsed.length; j++) {
        if (visited.has(j)) continue;
        if (parsed[j].startMins < clusterEnd) {
          cluster.push(parsed[j]);
          visited.add(j);
          clusterEnd = Math.max(clusterEnd, parsed[j].endMins);
        }
      }
      clusters.push(cluster);
    }
    return clusters;
  }
  function placeCluster(cluster, startMinutes, totalMinutes, now, isBefore2, isAfter2) {
    const columnMap = /* @__PURE__ */ new Map();
    const result = [];
    for (const item of cluster) {
      let col = 0;
      for (; ; col++) {
        const colEvents = columnMap.get(col) ?? [];
        if (!colEvents.some((e2) => item.startMins < e2.endMins && item.endMins > e2.startMins)) break;
      }
      if (!columnMap.has(col)) columnMap.set(col, []);
      columnMap.get(col).push(item);
      let maxConcurrent = 0;
      for (const other of cluster) {
        if (item.startMins < other.endMins && item.endMins > other.startMins) maxConcurrent++;
      }
      const effectiveColumns = Math.min(maxConcurrent, MAX_VISIBLE_COLUMNS);
      const isOverflow = col >= MAX_VISIBLE_COLUMNS;
      result.push({
        event: item.event,
        status: isBefore2(item.end, now) ? "past" : isAfter2(item.start, now) ? "future" : "current",
        top: (item.startMins - startMinutes) / totalMinutes * 100,
        height: Math.max((item.endMins - item.startMins) / totalMinutes * 100, MIN_HEIGHT_PERCENT),
        left: isOverflow ? 0 : col * (100 / effectiveColumns),
        width: isOverflow ? 0 : 100 / effectiveColumns,
        isOverflow
      });
    }
    return result;
  }
  function placeEvents({
    events,
    accessors,
    startHour,
    endHour,
    dayKey,
    isoFor,
    now,
    toMinutes,
    isBefore: isBefore2,
    isAfter: isAfter2
  }) {
    const startMinutes = startHour * 60;
    const endMinutes = endHour * 60;
    const totalMinutes = endMinutes - startMinutes;
    const parsed = parseEventsForDay(events, accessors, dayKey, isoFor, toMinutes, startMinutes, endMinutes);
    const clusters = clusterEvents(parsed);
    return clusters.flatMap((cluster) => placeCluster(cluster, startMinutes, totalMinutes, now, isBefore2, isAfter2));
  }
  var DayHeader = ({ day }) => {
    const { formatDate, toDate: toDate2, constants } = useDate();
    const isToday = toDate2(day) === toDate2();
    const dayName = formatDate(day, constants.DateFormats.SHORT_DAY).toUpperCase();
    const dayNumber = Number(formatDate(day, constants.DateFormats.DAY_OF_MONTH));
    return /* @__PURE__ */ React.createElement(CalendarTimeGrid_default.Header, { dayName, dayNumber, isToday });
  };
  var TimeAxis = () => {
    const { startHour, endHour, stepMinutes, showStepLabels } = useCalendarGridContext();
    const { formatHour, constants } = useDate();
    const rows = (0, import_react117.useMemo)(() => {
      const items = [];
      const totalMinutes = (endHour - startHour) * 60;
      for (let m2 = 0; m2 < totalMinutes; m2 += stepMinutes) {
        const hour = Math.floor(m2 / 60) + startHour;
        const min3 = m2 % 60;
        const isHourMark = min3 === 0;
        items.push({
          label: isHourMark ? formatHour(hour, constants.TimeFormats.HOUR_MINUTES_12) : "",
          isHourMark
        });
      }
      return items;
    }, [startHour, endHour, stepMinutes, formatHour, constants]);
    return /* @__PURE__ */ React.createElement(CalendarTimeGrid_default.Axis, { rows, showStepLabels });
  };
  var EventSlot = ({ placement }) => {
    const { renderEvent, popoverContent } = useCalendarGridContext();
    const [open, setOpen] = (0, import_react117.useState)(false);
    const trigger = /* @__PURE__ */ React.createElement(
      CalendarTimeGrid_default.EventCard,
      {
        top: placement.top,
        height: placement.height,
        left: placement.left,
        width: placement.width,
        onClick: popoverContent ? () => setOpen(true) : void 0
      },
      renderEvent(placement.event, placement.status)
    );
    if (!popoverContent) return trigger;
    return /* @__PURE__ */ React.createElement(Popover_default, { open, onOpenChange: setOpen }, /* @__PURE__ */ React.createElement(Popover_default.Trigger, { asChild: true }, trigger), /* @__PURE__ */ React.createElement(Popover_default.Content, { className: "w-80 p-0" }, popoverContent(placement.event, () => setOpen(false))));
  };
  var NowIndicator = ({ day }) => {
    const { startHour, endHour, nowLine } = useCalendarGridContext();
    const { toDate: toDate2, toDateUnits } = useDate();
    if (toDate2(day) !== toDate2()) return null;
    const { hours, minutes } = toDateUnits();
    const currentMinutes = hours * 60 + minutes;
    const totalMinutes = (endHour - startHour) * 60;
    const percent = (currentMinutes - startHour * 60) / totalMinutes * 100;
    return /* @__PURE__ */ React.createElement(CalendarTimeGrid_default.NowLine, { percent, mode: nowLine });
  };
  var DayColumn = ({ day, className }) => {
    const { events, eventAccessors, slots, startHour, endHour, stepMinutes } = useCalendarGridContext();
    const { toDate: toDate2, toDateUnits, isBefore: isBefore2, isAfter: isAfter2 } = useDate();
    const dayKey = toDate2(day);
    const rowCount = computeRowCount(startHour, endHour, stepMinutes);
    const cells = (0, import_react117.useMemo)(() => {
      const slotRanges = (slots?.[dayKey] ?? []).map((s) => ({
        start: parseTimeMinutes(s.startTime),
        end: parseTimeMinutes(s.endTime)
      }));
      return Array.from({ length: rowCount }, (_, idx) => {
        const cellStart = startHour * 60 + idx * stepMinutes;
        const cellEnd = cellStart + stepMinutes;
        const isAvailable = slotRanges.some((r2) => cellStart < r2.end && cellEnd > r2.start);
        return { isAvailable };
      });
    }, [slots, dayKey, rowCount, startHour, stepMinutes]);
    const placements2 = (0, import_react117.useMemo)(() => {
      const toMinutes = (input) => {
        const { hours, minutes } = toDateUnits(input);
        return hours * 60 + minutes;
      };
      return placeEvents({
        events,
        accessors: eventAccessors,
        startHour,
        endHour,
        dayKey,
        isoFor: toDate2,
        now: toDate2(),
        toMinutes,
        isBefore: isBefore2,
        isAfter: isAfter2
      });
    }, [events, eventAccessors, startHour, endHour, dayKey, toDate2, toDateUnits, isBefore2, isAfter2]);
    return /* @__PURE__ */ React.createElement(CalendarTimeGrid_default.Column, { rowCount, className }, /* @__PURE__ */ React.createElement(NowIndicator, { day }), placements2.map((placement, idx) => /* @__PURE__ */ React.createElement(EventSlot, { key: `${eventAccessors.getId(placement.event)}-${idx}`, placement })), cells.map((cell, idx) => /* @__PURE__ */ React.createElement(CalendarTimeGrid_default.Cell, { key: idx, available: cell.isAvailable })));
  };
  var DayView = () => {
    const { view, viewMode, date } = useCalendarGridContext();
    if (view !== "day" || viewMode !== "calendar") return null;
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "calendar-grid-day-view", className: "flex min-h-0 flex-1 flex-col" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "calendar-grid-header-row",
        className: "flex shrink-0 overflow-y-scroll [scrollbar-color:var(--color-muted-50)_var(--color-muted-50)] [scrollbar-gutter:stable] [scrollbar-width:thin]"
      },
      /* @__PURE__ */ React.createElement(CalendarTimeGrid_default.Spacer, null),
      /* @__PURE__ */ React.createElement(DayHeader, { day: date })
    ), /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "calendar-grid-body-row",
        className: "flex min-h-128 flex-1 overflow-y-auto [scrollbar-color:var(--color-muted-200)_transparent] [scrollbar-gutter:stable] [scrollbar-width:thin]"
      },
      /* @__PURE__ */ React.createElement(TimeAxis, null),
      /* @__PURE__ */ React.createElement(DayColumn, { day: date })
    ));
  };
  var WeekView = () => {
    const { view, viewMode, range } = useCalendarGridContext();
    const { addDuration, toDate: toDate2, constants } = useDate();
    const days = (0, import_react117.useMemo)(() => {
      const result = [];
      let current = range.startDate;
      for (let i = 0; i < 7; i++) {
        result.push(current);
        current = addDuration(current, 1, constants.UnitOfTime.DAY);
      }
      return result;
    }, [range.startDate, addDuration, constants]);
    if (view !== "week" || viewMode !== "calendar") return null;
    return /* @__PURE__ */ React.createElement("div", { "data-slot": "calendar-grid-week-view", className: "flex min-h-0 flex-1 flex-col" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "calendar-grid-header-row",
        className: "flex shrink-0 overflow-y-scroll [scrollbar-color:var(--color-muted-50)_var(--color-muted-50)] [scrollbar-gutter:stable] [scrollbar-width:thin]"
      },
      /* @__PURE__ */ React.createElement(CalendarTimeGrid_default.Spacer, null),
      /* @__PURE__ */ React.createElement("div", { "data-slot": "calendar-grid-day-headers", className: "flex flex-1" }, days.map((day) => /* @__PURE__ */ React.createElement(DayHeader, { key: toDate2(day), day })))
    ), /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "calendar-grid-body-row",
        className: "flex min-h-128 flex-1 overflow-y-auto [scrollbar-color:var(--color-muted-200)_transparent] [scrollbar-gutter:stable] [scrollbar-width:thin]"
      },
      /* @__PURE__ */ React.createElement(TimeAxis, null),
      days.map((day) => /* @__PURE__ */ React.createElement(DayColumn, { key: toDate2(day), day, className: "border-muted-200 border-e last:border-e-0" }))
    ));
  };
  var CalendarGridTimeView = () => null;
  CalendarGridTimeView.DayView = DayView;
  CalendarGridTimeView.WeekView = WeekView;
  var CalendarGridTimeView_default = CalendarGridTimeView;

  // src/components/calendar/calendar-grid/CalendarGrid.tsx
  var DEFAULT_START_HOUR = 8;
  var DEFAULT_END_HOUR = 24;
  var DEFAULT_STEP_MINUTES = 30;
  var VIEW_TO_UNIT = {
    day: "day" /* DAY */,
    week: "week" /* WEEK */,
    month: "month" /* MONTH */
  };
  var CalendarGridContext = (0, import_react118.createContext)(null);
  var useCalendarGridContext = () => {
    const context2 = (0, import_react118.useContext)(CalendarGridContext);
    if (!context2) throw new Error("CalendarGrid subcomponents must be used within <CalendarGrid>");
    return context2;
  };
  var EventTemplate = (_) => null;
  var PopoverTemplate = (_) => null;
  function CalendarGridRoot({
    initialView = "month",
    initialDate,
    view: controlledView,
    date: controlledDate,
    onViewChange,
    onDateChange,
    onRangeChange,
    initialViewMode = "calendar",
    viewMode: controlledViewMode,
    onViewModeChange,
    events,
    eventAccessors,
    slots,
    startHour = DEFAULT_START_HOUR,
    endHour = DEFAULT_END_HOUR,
    stepMinutes = DEFAULT_STEP_MINUTES,
    showStepLabels = false,
    nowLine = "in-range",
    isLoading = false,
    className,
    children,
    ...divProps
  }) {
    const childArray = import_react118.Children.toArray(children);
    const eventTemplateEl = childArray.find((c) => (0, import_react118.isValidElement)(c) && c.type === EventTemplate);
    const popoverTemplateEl = childArray.find((c) => (0, import_react118.isValidElement)(c) && c.type === PopoverTemplate);
    const renderEvent = eventTemplateEl?.props.children;
    const popoverContent = popoverTemplateEl?.props.children;
    const otherChildren = childArray.filter((c) => c !== eventTemplateEl && c !== popoverTemplateEl);
    if (events.length > 0 && !renderEvent) {
      throw new Error("CalendarGrid: <CalendarGrid.EventTemplate> is required when events are provided.");
    }
    const { toStartAndEndOf, addDuration, subtractDuration, formatDate, toDate: toDate2, constants } = useDate();
    const [internalView, setInternalView] = (0, import_react118.useState)(initialView);
    const [internalDate, setInternalDate] = (0, import_react118.useState)(() => toDate2(initialDate));
    const [internalViewMode, setInternalViewMode] = (0, import_react118.useState)(initialViewMode);
    const view = controlledView ?? internalView;
    const date = controlledDate !== void 0 ? toDate2(controlledDate) : internalDate;
    const viewMode = controlledViewMode ?? internalViewMode;
    const range = (0, import_react118.useMemo)(() => {
      if (view === "day") return { startDate: date, endDate: date };
      if (view === "week") {
        const { startOf, endOf } = toStartAndEndOf(date, constants.UnitOfTime.WEEK);
        return { startDate: startOf, endDate: endOf };
      }
      const month = toStartAndEndOf(date, constants.UnitOfTime.MONTH);
      const gridStart = toStartAndEndOf(month.startOf, constants.UnitOfTime.WEEK).startOf;
      const gridEnd = toStartAndEndOf(month.endOf, constants.UnitOfTime.WEEK).endOf;
      return { startDate: gridStart, endDate: gridEnd };
    }, [view, date, toStartAndEndOf, constants.UnitOfTime.MONTH, constants.UnitOfTime.WEEK]);
    const navigationLabel = (0, import_react118.useMemo)(() => {
      if (view === "day") return formatDate(date, constants.DateFormats.FULL_DAY_MONTH_DATE);
      if (view === "week") {
        const startMonth = formatDate(range.startDate, constants.DateFormats.SHORT_MONTH);
        const endMonth = formatDate(range.endDate, constants.DateFormats.SHORT_MONTH);
        if (startMonth !== endMonth) {
          const year = formatDate(range.endDate, constants.DateFormats.YEAR);
          return `${startMonth} - ${endMonth}, ${year}`;
        }
      }
      return formatDate(date, constants.DateFormats.FULL_MONTH_YEAR);
    }, [view, date, range, formatDate, constants]);
    const slotCoverageDates = (0, import_react118.useMemo)(() => slots ? new Set(Object.keys(slots)) : /* @__PURE__ */ new Set(), [slots]);
    const setView = (0, import_react118.useCallback)(
      (v) => {
        onViewChange?.(v);
        if (controlledView === void 0) setInternalView(v);
      },
      [controlledView, onViewChange]
    );
    const setDate = (0, import_react118.useCallback)(
      (d) => {
        const iso = toDate2(d);
        onDateChange?.(iso);
        if (controlledDate === void 0) setInternalDate(iso);
      },
      [controlledDate, onDateChange, toDate2]
    );
    const setViewMode = (0, import_react118.useCallback)(
      (m2) => {
        onViewModeChange?.(m2);
        if (controlledViewMode === void 0) setInternalViewMode(m2);
      },
      [controlledViewMode, onViewModeChange]
    );
    const goNext = (0, import_react118.useCallback)(() => {
      setDate(addDuration(date, 1, VIEW_TO_UNIT[view]));
    }, [setDate, addDuration, date, view]);
    const goPrev = (0, import_react118.useCallback)(() => {
      setDate(subtractDuration(date, 1, VIEW_TO_UNIT[view]));
    }, [setDate, subtractDuration, date, view]);
    const goToday = (0, import_react118.useCallback)(() => {
      setDate(toDate2());
    }, [setDate, toDate2]);
    const contextValue = (0, import_react118.useMemo)(
      () => ({
        view,
        date,
        viewMode,
        setView,
        setDate,
        setViewMode,
        range,
        navigationLabel,
        goNext,
        goPrev,
        goToday,
        events,
        eventAccessors,
        renderEvent,
        popoverContent,
        slots,
        slotCoverageDates,
        startHour,
        endHour,
        stepMinutes,
        showStepLabels,
        nowLine,
        isLoading
      }),
      [
        view,
        date,
        viewMode,
        setView,
        setDate,
        setViewMode,
        range,
        navigationLabel,
        goNext,
        goPrev,
        goToday,
        events,
        eventAccessors,
        renderEvent,
        popoverContent,
        slots,
        slotCoverageDates,
        startHour,
        endHour,
        stepMinutes,
        showStepLabels,
        nowLine,
        isLoading
      ]
    );
    (0, import_react118.useEffect)(() => {
      onRangeChange?.(range);
    }, [range.startDate, range.endDate]);
    if (isLoading && events.length === 0) {
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          "data-slot": "calendar-grid-skeleton",
          "aria-busy": "true",
          "aria-live": "polite",
          className: cn("border-muted-200 bg-background flex min-h-180 flex-col gap-3 overflow-clip rounded-2xl border p-4", className)
        },
        /* @__PURE__ */ React.createElement(Skeleton_default, { shape: "text", size: "md" }),
        /* @__PURE__ */ React.createElement(Skeleton_default, { shape: "text", size: "xs" }),
        /* @__PURE__ */ React.createElement(Skeleton_default, { shape: "banner", size: "xl", className: "flex-1" })
      );
    }
    return /* @__PURE__ */ React.createElement(CalendarGridContext.Provider, { value: contextValue }, /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-slot": "calendar-grid",
        role: "application",
        "aria-roledescription": "calendar",
        className: cn("border-muted-200 bg-background flex max-h-[80dvh] min-h-180 flex-col overflow-clip rounded-2xl border", className),
        ...divProps
      },
      otherChildren
    ));
  }
  var CalendarGrid = CalendarGridRoot;
  CalendarGrid.Header = CalendarGridChrome_default.Header;
  CalendarGrid.Navigation = CalendarGridChrome_default.Navigation;
  CalendarGrid.ViewTabs = CalendarGridChrome_default.ViewTabs;
  CalendarGrid.ViewToggle = CalendarGridChrome_default.ViewToggle;
  CalendarGrid.Picker = CalendarGridChrome_default.Picker;
  CalendarGrid.Legend = CalendarGridChrome_default.Legend;
  CalendarGrid.EventTemplate = EventTemplate;
  CalendarGrid.PopoverTemplate = PopoverTemplate;
  CalendarGrid.DayView = CalendarGridTimeView_default.DayView;
  CalendarGrid.WeekView = CalendarGridTimeView_default.WeekView;
  CalendarGrid.MonthView = CalendarGridMonthView_default.MonthView;
  var CalendarGrid_default = CalendarGrid;

  // src/components/calendar/calendar-picker/index.ts
  init_define_import_meta_env();

  // src/components/calendar/calendar-picker/CalendarPicker.tsx
  init_define_import_meta_env();
  var import_react119 = __toESM(require_react_shim(), 1);
  var isRangeShape = (value) => value != null && typeof value === "object" && "from" in value;
  var CalendarPickerContext = (0, import_react119.createContext)(null);
  var useCalendarPickerContext = () => {
    const context2 = (0, import_react119.useContext)(CalendarPickerContext);
    if (!context2) throw new Error("CalendarPicker.Trigger / CalendarPicker.Content must be used inside <CalendarPicker>");
    return context2;
  };
  var CalendarPickerTrigger = ({ children }) => {
    const { value, label, isOpen } = useCalendarPickerContext();
    const resolved = typeof children === "function" ? children({ value, label, isOpen }) : children;
    return /* @__PURE__ */ React.createElement(Popover_default.Trigger, { asChild: true }, resolved);
  };
  var CalendarPickerContent = ({ className, children, ...props }) => {
    const ctx = useCalendarPickerContext();
    const resolved = typeof children === "function" ? children(ctx) : children;
    return /* @__PURE__ */ React.createElement(Popover_default.Content, { className: cn("p-0", className), ...props }, resolved);
  };
  var CalendarPicker = (props) => {
    const {
      open: controlledOpen,
      onOpenChange,
      closeOn = "select",
      dismissable = true,
      triggerIcon,
      triggerTooltip,
      popoverSide,
      popoverAlign,
      popoverSideOffset,
      calendarProps: consumerCalendarProps,
      children
    } = props;
    const { formatNullableDate, toBoundaryDate, toDate: toDate2, constants } = useDate();
    const [internalOpen, setInternalOpen] = (0, import_react119.useState)(false);
    const childArray = import_react119.Children.toArray(children);
    const triggerChild = childArray.find((child) => (0, import_react119.isValidElement)(child) && child.type === CalendarPickerTrigger);
    const contentChild = childArray.find((child) => (0, import_react119.isValidElement)(child) && child.type === CalendarPickerContent);
    const isOpen = controlledOpen ?? internalOpen;
    const selectedRaw = props.selected ?? props.value;
    const range = isRangeShape(selectedRaw) ? { from: toBoundaryDate(selectedRaw.from), to: toBoundaryDate(selectedRaw.to) } : void 0;
    const anchor = range?.from ?? (selectedRaw != null && !isRangeShape(selectedRaw) ? toBoundaryDate(selectedRaw) : void 0);
    const label = isRangeShape(props.value) ? `${formatNullableDate(props.value.from, constants.DateFormats.SHORT_MONTH_DAY_YEAR, "")} \u2013 ${formatNullableDate(props.value.to, constants.DateFormats.SHORT_MONTH_DAY_YEAR, "")}` : formatNullableDate(props.value, constants.DateFormats.SHORT_MONTH_DAY_YEAR, "");
    const setIsOpen = (next) => {
      onOpenChange?.(next);
      if (controlledOpen === void 0) setInternalOpen(next);
    };
    const close = () => setIsOpen(false);
    const calendarProps = props.mode === "range" ? {
      mode: "range",
      required: false,
      selected: range,
      onSelect: (next) => {
        if (!next?.from || !next?.to) return;
        props.onChange({ from: toDate2(next.from), to: toDate2(next.to) });
        closeOn !== "never" && close();
      }
    } : {
      mode: "single",
      selected: anchor,
      modifiers: range ? { range_start: range.from, range_end: range.to, range_middle: { after: range.from, before: range.to } } : void 0,
      onSelect: (date) => {
        if (!date) return;
        props.onChange(toDate2(date));
        closeOn !== "never" && close();
      }
    };
    const sharedContextFields = { value: props.value, label, isOpen, close };
    const contextValue = props.mode === "range" ? { ...sharedContextFields, mode: "range", selected: props.selected ?? props.value, onSelect: props.onChange } : {
      ...sharedContextFields,
      mode: "single",
      selected: isRangeShape(props.selected) ? props.value : props.selected ?? props.value,
      onSelect: props.onChange
    };
    return /* @__PURE__ */ React.createElement(CalendarPickerContext.Provider, { value: contextValue }, /* @__PURE__ */ React.createElement(Popover_default, { open: isOpen, onOpenChange: setIsOpen }, /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!triggerChild }, triggerChild), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(CalendarPickerTrigger, null, /* @__PURE__ */ React.createElement(TooltipButton_default, { title: triggerTooltip, type: "button", variant: "ghost", size: "icon-sm" }, triggerIcon ?? /* @__PURE__ */ React.createElement(Calendar, { size: 20 }))))), /* @__PURE__ */ React.createElement(Conditional_default, null, /* @__PURE__ */ React.createElement(Conditional_default.If, { condition: !!contentChild }, contentChild), /* @__PURE__ */ React.createElement(Conditional_default.Else, null, /* @__PURE__ */ React.createElement(
      CalendarPickerContent,
      {
        side: popoverSide,
        align: popoverAlign,
        sideOffset: popoverSideOffset,
        onPointerDownOutside: dismissable ? void 0 : (e2) => e2.preventDefault(),
        onEscapeKeyDown: dismissable ? void 0 : (e2) => e2.preventDefault()
      },
      /* @__PURE__ */ React.createElement(Calendar_default, { ...calendarProps, defaultMonth: anchor, ...consumerCalendarProps })
    )))));
  };
  CalendarPicker.Trigger = CalendarPickerTrigger;
  CalendarPicker.Content = CalendarPickerContent;
  var CalendarPicker_default = CalendarPicker;
  return __toCommonJS(bundle_entry_exports);
})();
/*! Bundled license information:

use-sync-external-store/cjs/use-sync-external-store-shim.development.js:
  (**
   * @license React
   * use-sync-external-store-shim.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@remix-run/router/dist/router.js:
  (**
   * @remix-run/router v1.23.2
   *
   * Copyright (c) Remix Software Inc.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE.md file in the root directory of this source tree.
   *
   * @license MIT
   *)

react-router/dist/index.js:
  (**
   * React Router v6.30.3
   *
   * Copyright (c) Remix Software Inc.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE.md file in the root directory of this source tree.
   *
   * @license MIT
   *)

react-router-dom/dist/index.js:
  (**
   * React Router DOM v6.30.3
   *
   * Copyright (c) Remix Software Inc.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE.md file in the root directory of this source tree.
   *
   * @license MIT
   *)

lucide-react/dist/esm/shared/src/utils.js:
lucide-react/dist/esm/defaultAttributes.js:
lucide-react/dist/esm/Icon.js:
lucide-react/dist/esm/createLucideIcon.js:
lucide-react/dist/esm/icons/arrow-left.js:
lucide-react/dist/esm/icons/arrow-right.js:
lucide-react/dist/esm/icons/asterisk.js:
lucide-react/dist/esm/icons/calendar.js:
lucide-react/dist/esm/icons/check.js:
lucide-react/dist/esm/icons/chevron-down.js:
lucide-react/dist/esm/icons/chevron-left.js:
lucide-react/dist/esm/icons/chevron-right.js:
lucide-react/dist/esm/icons/chevron-up.js:
lucide-react/dist/esm/icons/chevrons-up-down.js:
lucide-react/dist/esm/icons/circle-alert.js:
lucide-react/dist/esm/icons/circle-check-big.js:
lucide-react/dist/esm/icons/circle-check.js:
lucide-react/dist/esm/icons/circle-minus.js:
lucide-react/dist/esm/icons/circle-x.js:
lucide-react/dist/esm/icons/circle.js:
lucide-react/dist/esm/icons/clock-4.js:
lucide-react/dist/esm/icons/component.js:
lucide-react/dist/esm/icons/download.js:
lucide-react/dist/esm/icons/ellipsis.js:
lucide-react/dist/esm/icons/file-audio.js:
lucide-react/dist/esm/icons/file-image.js:
lucide-react/dist/esm/icons/file-spreadsheet.js:
lucide-react/dist/esm/icons/file-text.js:
lucide-react/dist/esm/icons/file-video.js:
lucide-react/dist/esm/icons/file.js:
lucide-react/dist/esm/icons/folder-open.js:
lucide-react/dist/esm/icons/folder.js:
lucide-react/dist/esm/icons/info.js:
lucide-react/dist/esm/icons/layout-dashboard.js:
lucide-react/dist/esm/icons/list.js:
lucide-react/dist/esm/icons/loader-circle.js:
lucide-react/dist/esm/icons/log-out.js:
lucide-react/dist/esm/icons/minus.js:
lucide-react/dist/esm/icons/moon.js:
lucide-react/dist/esm/icons/paperclip.js:
lucide-react/dist/esm/icons/plus.js:
lucide-react/dist/esm/icons/search.js:
lucide-react/dist/esm/icons/settings.js:
lucide-react/dist/esm/icons/sun.js:
lucide-react/dist/esm/icons/triangle-alert.js:
lucide-react/dist/esm/icons/users.js:
lucide-react/dist/esm/icons/x.js:
lucide-react/dist/esm/lucide-react.js:
  (**
   * @license lucide-react v0.469.0 - ISC
   *
   * This source code is licensed under the ISC license.
   * See the LICENSE file in the root directory of this source tree.
   *)

@tanstack/react-table/build/lib/index.mjs:
  (**
     * react-table
     *
     * Copyright (c) TanStack
     *
     * This source code is licensed under the MIT license found in the
     * LICENSE.md file in the root directory of this source tree.
     *
     * @license MIT
     *)
*/
window.RSK=RSK.__dsMainNs?Object.assign({},RSK,RSK.__dsMainNs,{__dsMainNs:undefined}):RSK;
